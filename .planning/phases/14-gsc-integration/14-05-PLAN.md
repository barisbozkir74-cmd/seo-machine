---
phase: 14-gsc-integration
plan: 05
type: execute
wave: 5
depends_on:
  - "14-04"
files_modified:
  - src/app/(dashboard)/projeler/[id]/sayfa-paketi/GscIndexBadge.tsx
  - src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/HtmlReadyBanner.tsx
  - src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx
  - src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts
autonomous: true
requirements:
  - GSC-02

must_haves:
  truths:
    - "GscIndexBadge 3 renk state ile doğru render edilir (indexed/not_indexed/crawled_not_indexed)"
    - "HtmlReadyBanner'da 'Index Durumunu Kontrol Et' butonu mevcuttur ve checkIndexStatus action'ı çağırır"
    - "PagePackageEditor sol panelinde her sayfa için GscIndexBadge görünür"
    - "checkIndexStatus server action URL Inspection API'yi çağırır ve page_packages.gsc_index_status + gsc_index_checked_at günceller"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/GscIndexBadge.tsx"
      provides: "GscIndexBadge client component — 3 renk state"
      exports: ["GscIndexBadge"]
    - path: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts"
      provides: "checkIndexStatus server action"
      contains: "checkIndexStatus"
    - path: "src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/HtmlReadyBanner.tsx"
      provides: "GSC index check butonu ve badge entegrasyonu"
      contains: "Index Durumunu Kontrol Et"
    - path: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx"
      provides: "Sol panel sayfa listesinde GscIndexBadge"
      contains: "GscIndexBadge"
  key_links:
    - from: "HtmlReadyBanner"
      to: "checkIndexStatus"
      via: "onClick → server action call"
      pattern: "checkIndexStatus"
    - from: "checkIndexStatus"
      to: "page_packages.gsc_index_status"
      via: "supabase.from('page_packages').update({gsc_index_status, gsc_index_checked_at})"
      pattern: "gsc_index_status"
    - from: "PagePackageEditor"
      to: "GscIndexBadge"
      via: "page.pkg?.gsc_index_status prop"
      pattern: "GscIndexBadge"
---

<objective>
GSC index durumunu görselleştiren GscIndexBadge bileşenini, index kontrolü yapan checkIndexStatus server action'ını ve bu badge'i iki noktaya entegre eden güncellemeleri yapar.

Purpose: D-06 (manuel index kontrolü), D-07 (index badge her iki yerde), D-08 (page_packages kolonları) kararlarını tamamlar. GSC-02 gereksinimini kapatır.
Output: GscIndexBadge bileşeni + checkIndexStatus action + HtmlReadyBanner + PagePackageEditor güncellemeleri
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-gsc-integration/14-CONTEXT.md
@.planning/phases/14-gsc-integration/14-RESEARCH.md
@.planning/phases/14-gsc-integration/14-UI-SPEC.md
@src/app/(dashboard)/projeler/[id]/sayfa-paketi/PackageStatusBadge.tsx
@src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx
@src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/HtmlReadyBanner.tsx
@src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts

<interfaces>
<!-- GscIndexBadge props -->
type GscIndexBadgeProps = {
  status: string | null
}

<!-- checkIndexStatus server action imzası -->
export async function checkIndexStatus(
  pagePackageId: string,
  projectId: string,
  inspectionUrl: string  // page_packages.wp_post_url
): Promise<{ success: boolean; status?: string; error?: string }>

<!-- HtmlReadyBanner'a eklenecek yeni props -->
// Mevcut: projectId, pageId, wpPostUrl, wpStatus, isWpConfigured
// Yeni eklenenler:
gscIndexStatus?: string | null      // page_packages.gsc_index_status
gscIndexCheckedAt?: string | null   // page_packages.gsc_index_checked_at (ISO string)
isGscConnected?: boolean            // GSC bağlı mı?
pagePackageId?: string | null       // page_packages.id (index check için)

<!-- PackageStatusBadge pattern (model için) -->
const STATUS_CONFIG = {
  draft:    { label: '...', className: '...' },
  approved: { label: '...', className: '...' },
  locked:   { label: '...', className: '...' },
}

<!-- PagePackageEditor'daki PageData tipi — gsc alanları eklenecek -->
// pkg?.gsc_index_status: string | null
// pkg?.gsc_index_checked_at: string | null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GscIndexBadge bileşeni + checkIndexStatus server action</name>
  <files>
    src/app/(dashboard)/projeler/[id]/sayfa-paketi/GscIndexBadge.tsx
    src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts
  </files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/PackageStatusBadge.tsx — badge pattern model (STATUS_CONFIG, className direkt, variant prop yok)
    - .planning/phases/14-gsc-integration/14-RESEARCH.md §Pattern 6 (GscIndexBadge satır 441-453) — tam className değerleri
    - .planning/phases/14-gsc-integration/14-UI-SPEC.md §GscIndexBadge Durumları — 4 state (null + 3 aktif)
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts — mevcut dosya; checkIndexStatus eklenecek
    - src/lib/gsc/auth.ts — getValidGscToken imzası
    - src/lib/gsc/index-check.ts — checkUrlIndexStatus imzası
  </read_first>
  <action>
**src/app/(dashboard)/projeler/[id]/sayfa-paketi/GscIndexBadge.tsx** — yeni dosya:

```typescript
'use client'

import { cn } from '@/lib/utils'

const GSC_INDEX_STATUS_CONFIG = {
  indexed: {
    label: 'İndekslendi',
    className: 'bg-emerald-900/40 text-emerald-400',
  },
  not_indexed: {
    label: 'İndekslenmedi',
    className: 'bg-red-900/40 text-red-400',
  },
  crawled_not_indexed: {
    label: 'Tarandı/İndekslenmedi',
    className: 'bg-amber-900/40 text-amber-400',
  },
} as const

type GscIndexStatus = keyof typeof GSC_INDEX_STATUS_CONFIG

type Props = {
  status: string | null
}

export function GscIndexBadge({ status }: Props) {
  if (!status) return null
  const cfg = GSC_INDEX_STATUS_CONFIG[status as GscIndexStatus]
  if (!cfg) return null
  return (
    <span
      className={cn('inline-flex px-1.5 py-0.5 rounded text-[10px]', cfg.className)}
      aria-live="polite"
    >
      {cfg.label}
    </span>
  )
}
```

**src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts** — mevcut dosyayı oku, sonuna ekle:

```typescript
// ─── GSC Index Status Actions ──────────────────────────────────────────────

import { getValidGscToken } from '@/lib/gsc/auth'
import { checkUrlIndexStatus } from '@/lib/gsc/index-check'

// D-06: Manuel index kontrolü — URL Inspection API → page_packages güncelleme
// T-14-03: Ownership triple-check (user + project + page_package)
export async function checkIndexStatus(
  pagePackageId: string,
  projectId: string,
  inspectionUrl: string // page_packages.wp_post_url
): Promise<{ success: boolean; status?: string; error?: string }> {
  'use server'
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Oturum bulunamadı.' }

  // Triple ownership: user → project → page_package
  const { data: project } = await supabase
    .from('projects')
    .select('id, gsc_property_url')
    .eq('id', projectId)
    .eq('user_id', user.id)
    .single()
  if (!project) return { success: false, error: 'Proje bulunamadı.' }
  if (!project.gsc_property_url) return { success: false, error: 'GSC property seçilmemiş.' }

  const { data: pkg } = await supabase
    .from('page_packages')
    .select('id')
    .eq('id', pagePackageId)
    .eq('project_id', projectId)
    .single()
  if (!pkg) return { success: false, error: 'Sayfa paketi bulunamadı.' }

  // Token al
  const accessToken = await getValidGscToken(projectId, user.id)
  if (!accessToken) return { success: false, error: 'GSC bağlantısı geçersiz. Yeniden bağlanın.' }

  // URL Inspection API
  const status = await checkUrlIndexStatus(accessToken, inspectionUrl, project.gsc_property_url)

  if (status === 'unknown') {
    return { success: false, error: 'Index durumu alınamadı. Tekrar deneyin.' }
  }

  // Sonucu kaydet
  const { error: updateError } = await supabase
    .from('page_packages')
    .update({
      gsc_index_status: status,
      gsc_index_checked_at: new Date().toISOString(),
    })
    .eq('id', pagePackageId)
    .eq('project_id', projectId) // ek güvenlik filtresi

  if (updateError) return { success: false, error: 'Sonuç kaydedilemedi.' }

  return { success: true, status }
}
```

Not: actions.ts'te `createClient` zaten import edilmiş olmalı. `import { createClient }` satırı dosyanın başında mevcutsa tekrar ekleme. getValidGscToken ve checkUrlIndexStatus import'larını dosyanın import bloğuna ekle.
  </action>
  <verify>
    <automated>grep -n "GscIndexBadge\|indexed\|not_indexed\|crawled_not_indexed\|bg-emerald-900" src/app/(dashboard)/projeler/[id]/sayfa-paketi/GscIndexBadge.tsx | head -15</automated>
  </verify>
  <done>
    - GscIndexBadge.tsx mevcuttur ve `GscIndexBadge` export eder
    - 3 status: indexed (bg-emerald-900/40 text-emerald-400), not_indexed (bg-red-900/40 text-red-400), crawled_not_indexed (bg-amber-900/40 text-amber-400)
    - status null ise null döner (badge gösterilmez)
    - `aria-live="polite"` mevcuttur
    - actions.ts içinde `checkIndexStatus` mevcuttur
    - checkIndexStatus triple-check: user.id + projectId + pagePackageId
    - checkIndexStatus `gsc_index_status` ve `gsc_index_checked_at` günceller
  </done>
</task>

<task type="auto">
  <name>Task 2: HtmlReadyBanner index kontrol butonu + PagePackageEditor sol panel badge entegrasyonu</name>
  <files>
    src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/HtmlReadyBanner.tsx
    src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/HtmlReadyBanner.tsx — mevcut dosyanın tam içeriği; Props tipini genişlet
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx — tam içerik; PageData tipi, sol panel render alanı, pkg alanları
    - .planning/phases/14-gsc-integration/14-UI-SPEC.md §"Index Durumunu Kontrol Et" Butonu — 5 state
    - .planning/phases/14-gsc-integration/14-UI-SPEC.md §GscIndexBadge Yerleşimi — "WP publish badge'lerinin yanına inline"
    - .planning/phases/14-gsc-integration/14-CONTEXT.md §D-07 — badge her iki yerde
  </read_first>
  <action>
**HtmlReadyBanner.tsx güncellemesi** — mevcut dosyayı oku, Props tipini ve bileşeni şöyle güncelle:

1. Import ekle:
```typescript
import { useState } from 'react' // zaten mevcut olabilir
import { GscIndexBadge } from '../../sayfa-paketi/GscIndexBadge'
import { checkIndexStatus } from '../../sayfa-paketi/actions'
```

2. Props tipini genişlet (mevcut Props tipine ekle):
```typescript
type Props = {
  projectId: string
  pageId: string
  wpPostUrl?: string | null
  wpStatus?: string | null
  isWpConfigured: boolean
  // GSC (D-07): index badge ve kontrol butonu
  pagePackageId?: string | null
  gscIndexStatus?: string | null
  gscIndexCheckedAt?: string | null
  isGscConnected?: boolean
}
```

3. useState ekle (mevcut state'lerin yanına):
```typescript
const [indexStatus, setIndexStatus] = useState<string | null>(gscIndexStatus ?? null)
const [indexCheckedAt, setIndexCheckedAt] = useState<string | null>(gscIndexCheckedAt ?? null)
const [checkingIndex, setCheckingIndex] = useState(false)
const [indexError, setIndexError] = useState<string | null>(null)
```

4. Handler ekle:
```typescript
const handleCheckIndex = async () => {
  if (!pagePackageId || !localWpPostUrl) return
  setCheckingIndex(true)
  setIndexError(null)
  try {
    const result = await checkIndexStatus(pagePackageId, projectId, localWpPostUrl)
    if (result.success && result.status) {
      setIndexStatus(result.status)
      setIndexCheckedAt(new Date().toISOString())
    } else {
      setIndexError(result.error ?? 'Index durumu alınamadı. Tekrar deneyin.')
    }
  } catch {
    setIndexError('Index durumu alınamadı. Tekrar deneyin.')
  } finally {
    setCheckingIndex(false)
  }
}
```

5. Banner içeriğinde (tüm banner varyantlarında ortak) "GSC index bölümü" ekle — mevcut `<div className="flex-1 min-w-0">` bloğunun altına veya button alanının yanına:
```tsx
{/* GSC Index kontrolü — D-07 */}
<div className="flex items-center gap-2 mt-2">
  <GscIndexBadge status={indexStatus} />
  {isGscConnected && localWpPostUrl && (
    <Button
      variant="outline"
      size="sm"
      onClick={handleCheckIndex}
      disabled={checkingIndex || !pagePackageId}
      title={!isGscConnected ? 'Önce GSC bağlantısını yapılandırın' : undefined}
      aria-label="GSC'de index durumunu kontrol et"
    >
      {checkingIndex ? 'Kontrol Ediliyor...' : 'Index Durumunu Kontrol Et'}
    </Button>
  )}
  {indexCheckedAt && (
    <span className="text-xs text-muted-foreground">
      Son kontrol: {new Date(indexCheckedAt).toLocaleString('tr-TR', { day: 'numeric', month: 'short', year: 'numeric', hour: '2-digit', minute: '2-digit' })}
    </span>
  )}
  {indexError && (
    <span className="text-xs text-destructive" role="alert">{indexError}</span>
  )}
</div>
```

Bu GSC bölümünü tüm 3 banner varyantına (yayında / taslak / henüz gönderilmemiş) ekle.

---

**PagePackageEditor.tsx güncellemesi:**

1. PageData tipine gsc alanları ekle (pkg nesnesi içine):
```typescript
pkg?: {
  // ... mevcut alanlar
  gsc_index_status?: string | null
  gsc_index_checked_at?: string | null
} | null
```

2. GscIndexBadge import et:
```typescript
import { GscIndexBadge } from './GscIndexBadge'
```

3. Sol panelde her sayfa öğesini gösteren alanda (sayfa başlıklarının yanında badge'ler olduğu yerde) GscIndexBadge ekle. PackageStatusBadge ve wp badge'lerinin yanına:
```tsx
<GscIndexBadge status={page.pkg?.gsc_index_status ?? null} />
```

Sol panel render alanını bulmak için: PagePackageEditor'da sayfa listesi genellikle `pages.map(page => ...)` ile render edilir ve `PackageStatusBadge` zaten orada kullanılıyordur — aynı `flex items-center gap-1.5` bloğuna ekle.
  </action>
  <verify>
    <automated>grep -n "Index Durumunu Kontrol Et\|GscIndexBadge\|checkIndexStatus\|gsc_index_status" src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/HtmlReadyBanner.tsx | head -15</automated>
  </verify>
  <done>
    - HtmlReadyBanner.tsx: Props'a `pagePackageId`, `gscIndexStatus`, `gscIndexCheckedAt`, `isGscConnected` eklenmiştir
    - HtmlReadyBanner.tsx: `GscIndexBadge` import edilmiştir
    - HtmlReadyBanner.tsx: `checkIndexStatus` action import edilmiştir
    - HtmlReadyBanner.tsx: "Index Durumunu Kontrol Et" butonu mevcuttur
    - HtmlReadyBanner.tsx: "Kontrol Ediliyor..." disabled state mevcuttur
    - HtmlReadyBanner.tsx: indexError `role="alert"` ve `text-xs text-destructive` ile gösterilir
    - PagePackageEditor.tsx: PageData.pkg'ye `gsc_index_status` ve `gsc_index_checked_at` eklenmiştir
    - PagePackageEditor.tsx: `GscIndexBadge` sol panelde render edilir
    - TypeScript derleme hatası yoktur
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HtmlReadyBanner → checkIndexStatus | Server action — projectId ve pagePackageId server tarafında doğrulanır |
| checkIndexStatus → GSC URL Inspection API | Bearer token — getValidGscToken ownership check sonrası alınır |
| checkIndexStatus → page_packages | UPDATE — project_id filtresi ile ownership ek güvencesi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Elevation of Privilege | checkIndexStatus ownership | mitigate | Triple-check: auth.getUser() → projects.eq(user_id) → page_packages.eq(project_id) — başka kullanıcının sayfasının index durumu değiştirilemez |
| T-14-RL | Denial of Service | URL Inspection rate limit | accept | Günde 2000 istek limiti; manuel buton ile organik kullanımda aşılmaz; gsc_index_checked_at ile son kontrol zamanı kaydedilir (1 saatten sık kontrol UI'da önlenebilir) |
| T-14-I1 | Information Disclosure | GscIndexBadge status | accept | Index status değeri public değil; sadece authenticated kullanıcının kendi projesi — badge render client'ta gerçekleşir, SSR'dan prop olarak gelir |
</threat_model>

<verification>
- `grep "Index Durumunu Kontrol Et" src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/HtmlReadyBanner.tsx` match bulur
- `grep "GscIndexBadge" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` match bulur
- `grep "checkIndexStatus" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` match bulur
- `grep "gsc_index_status\|gsc_index_checked_at" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` match bulur
- `npm run build` TypeScript hatası vermez
- `npm test` tüm testler yeşil
</verification>

<success_criteria>
- GscIndexBadge: 3 renk state doğru (emerald/red/amber) + null durumunda gizlenir
- HtmlReadyBanner: "Index Durumunu Kontrol Et" butonu → checkIndexStatus → badge güncellenir
- PagePackageEditor sol panel: her sayfa için GscIndexBadge mevcut (null ise görünmez)
- checkIndexStatus: triple-check + URL Inspection API + page_packages UPDATE
- Phase 14 GSC-02 gereksinimi tamamen kapatılmıştır
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/14-gsc-integration/14-05-SUMMARY.md` dosyasını oluştur.
</output>
