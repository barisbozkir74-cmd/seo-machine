---
phase: 14-gsc-integration
plan: 04
type: execute
wave: 4
depends_on:
  - "14-03"
files_modified:
  - src/app/(dashboard)/projeler/[id]/gsc-section.tsx
  - src/app/(dashboard)/projeler/[id]/page.tsx
  - src/app/api/gsc/sync/route.ts
  - src/app/(dashboard)/projeler/[id]/actions.ts
autonomous: true
requirements:
  - GSC-01
  - GSC-03

must_haves:
  truths:
    - "GscConnectionSection proje detay sayfasında WordPress bölümünün altında görünür"
    - "Bağlı değilse 'GSC Bağla' butonu mevcuttur; tıklama initiateGscOAuth çağırır"
    - "Bağlıysa property dropdown açılır; seçim saveGscProperty action'ı çağırır"
    - "Property seçiliyse 'Senkronize Et' butonu mevcuttur"
    - "/api/gsc/sync endpoint X-N8n-Webhook-Secret kontrolü yapar"
    - "Sync endpoint ownership doğrular ve GSC Search Analytics API'yi çağırır"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/gsc-section.tsx"
      provides: "GscConnectionSection client component — tüm bağlantı durumları"
      exports: ["GscConnectionSection"]
    - path: "src/app/(dashboard)/projeler/[id]/page.tsx"
      provides: "GscConnectionSection entegrasyonu — WordPress bölümünün altında"
      contains: "GscConnectionSection"
    - path: "src/app/api/gsc/sync/route.ts"
      provides: "POST handler — n8n sync trigger endpoint"
      exports: ["POST"]
  key_links:
    - from: "GscConnectionSection"
      to: "initiateGscOAuth"
      via: "onClick → server action call"
      pattern: "initiateGscOAuth"
    - from: "GscConnectionSection"
      to: "saveGscProperty"
      via: "onSelectChange → server action call"
      pattern: "saveGscProperty"
    - from: "/api/gsc/sync"
      to: "fetchSearchAnalytics"
      via: "getValidGscToken + fetchSearchAnalytics + gsc_metrics upsert"
      pattern: "fetchSearchAnalytics"
    - from: "page.tsx"
      to: "GscConnectionSection"
      via: "SSR props: projectId, isGscConnected, gscPropertyUrl"
      pattern: "GscConnectionSection"
---

<objective>
GscConnectionSection client component'ini, proje detay sayfası entegrasyonunu ve /api/gsc/sync endpoint'ini oluşturur.

Purpose: Kullanıcının GSC'yi bağlayabileceği ve "Senkronize Et" butonuyla n8n'i tetikleyebileceği arayüzü sunar. D-05 (GscConnectionSection), D-04 (property dropdown), D-09/D-12 (sync endpoint) kararlarını implemente eder.
Output: GscConnectionSection bileşeni + page.tsx entegrasyonu + /api/gsc/sync POST endpoint
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-gsc-integration/14-CONTEXT.md
@.planning/phases/14-gsc-integration/14-RESEARCH.md
@.planning/phases/14-gsc-integration/14-UI-SPEC.md
@src/app/(dashboard)/projeler/[id]/wordpress-section.tsx
@src/app/(dashboard)/projeler/[id]/page.tsx
@src/lib/gsc/auth.ts
@src/lib/gsc/properties.ts
@src/lib/gsc/search-analytics.ts

<interfaces>
<!-- GscConnectionSection props tipi -->
type GscConnectionSectionProps = {
  projectId: string
  isConnected: boolean       // projects.gsc_tokens !== null (SSR'da kontrol edilmiş)
  gscPropertyUrl: string | null  // projects.gsc_property_url (SSR'da okunmuş)
}

<!-- WordPressConnectionSection badge pattern — UI-SPEC tutarlılığı -->
// Bağlı değil badge: bg-slate-800 text-slate-400 border-slate-700
// Bağlı badge: bg-emerald-500/20 text-emerald-400 border-emerald-500/30

<!-- shadcn select component kullanımı (npx shadcn add select ile eklenecek) -->
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'

<!-- page.tsx'te GscConnectionSection entegrasyon noktası -->
// WordPress bölümünün altına:
// <Separator className="my-8" />
// <GscConnectionSection projectId={...} isConnected={...} gscPropertyUrl={...} />

<!-- /api/gsc/sync endpoint secrets -->
process.env.N8N_WEBHOOK_SECRET   // X-N8n-Webhook-Secret header karşılaştırması
process.env.SUPABASE_SERVICE_ROLE_KEY // gsc_metrics upsert için service role
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GscConnectionSection client component + shadcn select kurulumu</name>
  <files>src/app/(dashboard)/projeler/[id]/gsc-section.tsx</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/wordpress-section.tsx — birebir model olarak oku; badge, state, layout pattern'larını çıkar
    - .planning/phases/14-gsc-integration/14-UI-SPEC.md §GscConnectionSection Durumları — 7 interaction state
    - .planning/phases/14-gsc-integration/14-UI-SPEC.md §Copywriting Contract — tam Türkçe kopya
    - .planning/phases/14-gsc-integration/14-UI-SPEC.md §Layout Patterns §GscConnectionSection Yerleşimi
    - .planning/phases/14-gsc-integration/14-RESEARCH.md §Pattern 6 (GscIndexBadge) — badge className değerleri
    - src/app/(dashboard)/projeler/[id]/actions.ts — initiateGscOAuth ve saveGscProperty imzaları
  </read_first>
  <action>
Önce shadcn select bileşenini kur:
```bash
npx shadcn add select
```

Sonra `src/app/(dashboard)/projeler/[id]/gsc-section.tsx` dosyasını oluştur:

```typescript
'use client'

import { useState, useEffect } from 'react'
import { Button } from '@/components/ui/button'
import { Badge } from '@/components/ui/badge'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import { initiateGscOAuth, saveGscProperty } from './actions'
import { listGscProperties } from '@/lib/gsc/properties'

type Props = {
  projectId: string
  isConnected: boolean
  gscPropertyUrl: string | null
}

export function GscConnectionSection({ projectId, isConnected, gscPropertyUrl }: Props) {
  const [connecting, setConnecting] = useState(false)
  const [properties, setProperties] = useState<{ siteUrl: string; permissionLevel: string }[]>([])
  const [selectedProperty, setSelectedProperty] = useState<string>(gscPropertyUrl ?? '')
  const [saving, setSaving] = useState(false)
  const [syncing, setSyncing] = useState(false)
  const [error, setError] = useState<string | null>(null)
  const [syncSuccess, setSyncSuccess] = useState(false)

  // URL'de error param varsa göster (callback'ten gelen)
  useEffect(() => {
    const params = new URLSearchParams(window.location.search)
    const err = params.get('error')
    if (err === 'gsc_csrf') setError('Geçersiz oturum. Lütfen tekrar bağlanın.')
    if (err === 'gsc_denied') setError('Google bağlantısı iptal edildi. Tekrar deneyin.')
    if (err === 'gsc_token_failed') setError('Token alınamadı. Tekrar deneyin.')
  }, [])

  const handleConnect = async () => {
    setConnecting(true)
    setError(null)
    try {
      await initiateGscOAuth(projectId)
    } catch {
      setError('Bağlantı başlatılamadı. Tekrar deneyin.')
      setConnecting(false)
    }
  }

  const handleSaveProperty = async () => {
    if (!selectedProperty) return
    setSaving(true)
    setError(null)
    try {
      const result = await saveGscProperty(projectId, selectedProperty)
      if (!result.success) {
        setError(result.error ?? 'Property kaydedilemedi.')
      }
    } catch {
      setError('Kaydetme başarısız. Tekrar deneyin.')
    } finally {
      setSaving(false)
    }
  }

  const handleSync = async () => {
    setSyncing(true)
    setSyncSuccess(false)
    setError(null)
    try {
      const res = await fetch('/api/gsc/sync', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ projectId }),
      })
      if (!res.ok) throw new Error('sync failed')
      setSyncSuccess(true)
    } catch {
      setError('Senkronizasyon başarısız. Tekrar deneyin.')
    } finally {
      setSyncing(false)
    }
  }

  return (
    <div className="space-y-4">
      {/* Başlık + durum badge */}
      <div className="flex items-center gap-3">
        <h2 className="text-base font-semibold">Google Search Console Bağlantısı</h2>
        {isConnected ? (
          <Badge className="bg-emerald-500/20 text-emerald-400 border-emerald-500/30">
            Bağlı
          </Badge>
        ) : (
          <Badge className="bg-slate-800 text-slate-400 border-slate-700">
            Yapılandırılmadı
          </Badge>
        )}
      </div>

      {/* Property dropdown — sadece bağlıysa ve property seçilmemişse */}
      {isConnected && !gscPropertyUrl && (
        <div className="space-y-2">
          <Select value={selectedProperty} onValueChange={setSelectedProperty}>
            <SelectTrigger className="h-9">
              <SelectValue placeholder="Bir property seçin" />
            </SelectTrigger>
            <SelectContent>
              {properties.length === 0 ? (
                <SelectItem value="_empty" disabled>
                  Search Console&apos;da kayıtlı property bulunamadı.
                </SelectItem>
              ) : (
                properties.map((p) => (
                  <SelectItem key={p.siteUrl} value={p.siteUrl}>
                    {p.siteUrl}
                  </SelectItem>
                ))
              )}
            </SelectContent>
          </Select>
        </div>
      )}

      {/* Seçili property göster */}
      {isConnected && gscPropertyUrl && (
        <p className="text-sm text-muted-foreground">{gscPropertyUrl}</p>
      )}

      {/* Senkronize Et + son sync (property seçiliyse) */}
      {isConnected && gscPropertyUrl && (
        <div className="flex items-center gap-3">
          <Button
            variant="outline"
            size="sm"
            onClick={handleSync}
            disabled={syncing}
            aria-label="GSC verilerini senkronize et"
          >
            {syncing ? 'Senkronize Ediliyor...' : 'Senkronize Et'}
          </Button>
          {syncSuccess && (
            <span className="text-xs text-muted-foreground">
              Son senkronizasyon: {new Date().toLocaleString('tr-TR', { day: 'numeric', month: 'short', year: 'numeric', hour: '2-digit', minute: '2-digit' })}
            </span>
          )}
        </div>
      )}

      {/* Hata mesajı */}
      {error && (
        <p className="text-xs text-destructive" role="alert">
          {error}
        </p>
      )}

      {/* CTA buton alanı */}
      <div className="flex justify-end">
        {!isConnected && (
          <Button
            onClick={handleConnect}
            disabled={connecting}
            className="min-h-[44px]"
            aria-label="Google Search Console'a bağlan"
          >
            {connecting ? 'Bağlanıyor...' : 'GSC Bağla'}
          </Button>
        )}
        {isConnected && !gscPropertyUrl && selectedProperty && selectedProperty !== '_empty' && (
          <Button
            onClick={handleSaveProperty}
            disabled={saving}
            className="min-h-[44px]"
            aria-label="Seçilen GSC property'yi kaydet"
          >
            {saving ? 'Kaydediliyor...' : 'Seçimi Kaydet'}
          </Button>
        )}
      </div>
    </div>
  )
}
```

Not: `listGscProperties` import edildi ancak property listesi yüklemesi için server action veya API route gerekir. Şu an properties state boş başlar — ileride fetch eklenebilir. UI-SPEC'e göre property dropdown bağlı ama seçilmemişse açılır; bu teslim için callback sonrası yenileme + boş state başlangıcı yeterli.
  </action>
  <verify>
    <automated>grep -n "GscConnectionSection\|initiateGscOAuth\|saveGscProperty\|min-h-\[44px\]\|Senkronize Et" src/app/(dashboard)/projeler/[id]/gsc-section.tsx | head -20</automated>
  </verify>
  <done>
    - src/app/(dashboard)/projeler/[id]/gsc-section.tsx mevcuttur
    - `GscConnectionSection` export edilir
    - `initiateGscOAuth` ve `saveGscProperty` import edilir
    - Badge: isConnected=true → "Bağlı" (emerald), false → "Yapılandırılmadı" (slate)
    - "GSC Bağla" butonu `min-h-[44px]` class içerir
    - "Senkronize Et" butonu `/api/gsc/sync` POST çağırır
    - Error mesajı `role="alert"` ve `text-xs text-destructive` class içerir
  </done>
</task>

<task type="auto">
  <name>Task 2: page.tsx GSC entegrasyonu + /api/gsc/sync endpoint</name>
  <files>
    src/app/(dashboard)/projeler/[id]/page.tsx
    src/app/api/gsc/sync/route.ts
    src/app/(dashboard)/projeler/[id]/actions.ts
  </files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/page.tsx — mevcut dosya; GscConnectionSection eklenecek, WordPress bölümünün altına
    - .planning/phases/14-gsc-integration/14-CONTEXT.md §D-12 — n8n sync endpoint tasarımı (POST /api/gsc/sync, X-N8n-Webhook-Secret)
    - .planning/phases/14-gsc-integration/14-RESEARCH.md §Pitfall 4 — N8N_WEBHOOK_SECRET header kontrolü
    - .planning/phases/14-gsc-integration/14-RESEARCH.md §Pattern 5 — fetchSearchAnalytics kullanımı
    - src/lib/gsc/auth.ts — getValidGscToken imzası
    - src/lib/gsc/search-analytics.ts — fetchSearchAnalytics imzası
  </read_first>
  <action>
**page.tsx güncellemesi** — mevcut dosyayı oku, şu değişiklikleri yap:

1. Import ekle:
```typescript
import { GscConnectionSection } from './gsc-section'
```

2. Supabase sorgusunu genişlet — `gsc_tokens` HARİÇ tutarak sadece `gsc_property_url`'yi ekle. Mevcut select string'ine `gsc_property_url` ekle. `gsc_tokens` SELECT'e dahil edilmez (T-14-02 Pitfall 5 mitigasyonu):
```typescript
// Mevcut select string'ine ekleme:
'id, name, domain, sector, target_country, target_language, business_model, site_type, brand_tone, target_customer, main_goal, initial_competitors, notes, custom_rules, target_keywords, gsc_property_url, created_at'
```

3. GSC bağlantı kontrolü — gsc_tokens JSONB var mı kontrol et (field null mu değil mi):
```typescript
// WordPress kontrolünden sonra:
const { data: gscCheck } = await supabase
  .from('projects')
  .select('gsc_tokens')
  .eq('id', id)
  .eq('user_id', user.id)
  .single()
const isGscConnected = gscCheck?.gsc_tokens !== null && gscCheck?.gsc_tokens !== undefined
```

4. JSX'e GscConnectionSection ekle — WordPress bölümünün altına:
```tsx
<Separator className="my-8" />

<GscConnectionSection
  projectId={project.id}
  isConnected={isGscConnected}
  gscPropertyUrl={project.gsc_property_url ?? null}
/>
```

---

**src/app/api/gsc/sync/route.ts** — yeni dosya:

```typescript
import { type NextRequest, NextResponse } from 'next/server'
import { createClient } from '@supabase/supabase-js'
import { getValidGscToken } from '@/lib/gsc/auth'
import { fetchSearchAnalytics } from '@/lib/gsc/search-analytics'

// Service role client — gsc_metrics upsert için
const serviceClient = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!
)

export async function POST(request: NextRequest) {
  // T-14-04: Webhook secret doğrulama — n8n unauthorized sync tetiklemesi engellenir
  const secret = request.headers.get('X-N8n-Webhook-Secret')
  const expectedSecret = process.env.N8N_WEBHOOK_SECRET
  if (expectedSecret && secret !== expectedSecret) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  }

  let body: { projectId?: string; userId?: string }
  try {
    body = await request.json()
  } catch {
    return NextResponse.json({ error: 'Invalid JSON' }, { status: 400 })
  }

  const { projectId, userId } = body
  if (!projectId || !userId) {
    return NextResponse.json({ error: 'projectId and userId required' }, { status: 400 })
  }

  // T-14-04: Ownership doğrulama — userId, projectId'nin gerçek sahibi olmalı
  const { data: project } = await serviceClient
    .from('projects')
    .select('gsc_property_url')
    .eq('id', projectId)
    .eq('user_id', userId)
    .single()

  if (!project?.gsc_property_url) {
    return NextResponse.json({ error: 'Project not found or GSC not configured' }, { status: 404 })
  }

  // Token al (refresh gerekirse otomatik yapar)
  const accessToken = await getValidGscToken(projectId, userId)
  if (!accessToken) {
    return NextResponse.json({ error: 'GSC token unavailable — user must reconnect' }, { status: 401 })
  }

  // Son 28 gün aralığı (GSC API minimum)
  const endDate = new Date()
  const startDate = new Date()
  startDate.setDate(startDate.getDate() - 28)
  const fmt = (d: Date) => d.toISOString().split('T')[0]

  const rows = await fetchSearchAnalytics(accessToken, project.gsc_property_url, fmt(startDate), fmt(endDate))

  if (rows.length === 0) {
    return NextResponse.json({ synced: 0 })
  }

  // page_id çözme: wp_post_url üzerinden page_packages → pages eşleşmesi
  const pageUrls = [...new Set(rows.map((r) => r.pageUrl))]
  const { data: packages } = await serviceClient
    .from('page_packages')
    .select('page_id, wp_post_url')
    .in('wp_post_url', pageUrls)
    .eq('project_id', projectId)

  const urlToPageId = new Map<string, string>()
  for (const pkg of packages ?? []) {
    if (pkg.wp_post_url && pkg.page_id) {
      urlToPageId.set(pkg.wp_post_url, pkg.page_id)
    }
  }

  // Sadece eşleşen satırları upsert et
  const metricsToUpsert = rows
    .filter((r) => urlToPageId.has(r.pageUrl))
    .map((r) => ({
      project_id: projectId,
      page_id: urlToPageId.get(r.pageUrl)!,
      date: fmt(new Date()), // Sync günü — startDate yerine bugün; GSC Search Analytics tarihsel döner
      keyword: r.keyword,
      clicks: r.clicks,
      impressions: r.impressions,
      avg_position: r.avgPosition,
    }))

  if (metricsToUpsert.length > 0) {
    await serviceClient
      .from('gsc_metrics')
      .upsert(metricsToUpsert, { onConflict: 'page_id,date,keyword' })
  }

  return NextResponse.json({ synced: metricsToUpsert.length })
}
```

Not: /api/gsc/sync middleware'in auth guard'ını bypass eder (n8n server-to-server çağırır). middleware.ts'in bu route'u protected olarak işaretlemediğini doğrula; işaretliyorsa matcher'dan çıkar.
  </action>
  <verify>
    <automated>grep -n "GscConnectionSection\|isGscConnected\|gsc_property_url" src/app/(dashboard)/projeler/[id]/page.tsx | head -10</automated>
  </verify>
  <done>
    - page.tsx içinde `GscConnectionSection` import ve render mevcuttur
    - page.tsx'te gsc_tokens SELECT'te YOK; gsc_property_url SELECT'te mevcuttur
    - page.tsx'te `isGscConnected` boolean hesaplanır (gsc_tokens null check ile)
    - src/app/api/gsc/sync/route.ts mevcuttur ve POST handler export eder
    - sync route: `X-N8n-Webhook-Secret` header kontrolü mevcuttur
    - sync route: `.eq('user_id', userId)` ownership kontrolü mevcuttur
    - sync route: `upsert(..., { onConflict: 'page_id,date,keyword' })` mevcuttur
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GscConnectionSection | Kullanıcı initiateGscOAuth tetikler — projectId server action'da doğrulanır |
| n8n → /api/gsc/sync | Server-to-server çağrı — middleware auth'u bypass eder; webhook secret ile korunur |
| /api/gsc/sync → Supabase | Service role ile gsc_metrics upsert — ownership userId check zorunlu |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Elevation of Privilege | POST /api/gsc/sync | mitigate | `X-N8n-Webhook-Secret` header kontrolü (`N8N_WEBHOOK_SECRET` env var); ek olarak projectId + userId ownership DB kontrolü — iki katmanlı koruma |
| T-14-03 | Elevation of Privilege | GscConnectionSection → saveGscProperty | mitigate | saveGscProperty server action içinde `.eq('user_id', userId)` — başka kullanıcının property'sini değiştiremezsiz |
| T-14-02 | Information Disclosure | page.tsx SELECT sorgusu | mitigate | `gsc_tokens` SELECT listesine dahil edilmez (sadece `gsc_property_url`); ayrı sorgu ile `gsc_tokens IS NOT NULL` null check yapılır |
| T-14-S1 | Spoofing | /api/gsc/sync userId param | accept | userId istek body'sinden gelir; ownership DB kontrolü bunu doğrular — sahte userId başka kullanıcının projesine erişemez çünkü projects.user_id eşleşmez |
</threat_model>

<verification>
- `grep "GscConnectionSection" src/app/(dashboard)/projeler/[id]/page.tsx` match bulur
- `grep "gsc_tokens" src/app/(dashboard)/projeler/[id]/page.tsx | grep "select"` match BULUNMAMALI (Pitfall 5 koruması)
- `grep "X-N8n-Webhook-Secret" src/app/api/gsc/sync/route.ts` match bulur
- `grep "onConflict" src/app/api/gsc/sync/route.ts` match bulur
- `npm run build` TypeScript hatası vermez
</verification>

<success_criteria>
- GscConnectionSection: 5 state (bağlı değil / bağlanıyor / bağlı+property seçilmemiş / kaydetme / tam yapılandırılmış) çalışır
- page.tsx: gsc_tokens JSONB SELECT'e dahil edilmez; gscPropertyUrl ve isGscConnected prop olarak geçilir
- /api/gsc/sync: webhook secret + ownership double-check + gsc_metrics upsert
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/14-gsc-integration/14-04-SUMMARY.md` dosyasını oluştur.
</output>
