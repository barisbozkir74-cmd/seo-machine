---
phase: 14-gsc-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260427000001_gsc_schema.sql
autonomous: true
requirements:
  - GSC-01
  - GSC-02
  - GSC-03

must_haves:
  truths:
    - "gsc_tokens JSONB ve gsc_property_url TEXT kolonları projects tablosunda mevcuttur"
    - "gsc_index_status ve gsc_index_checked_at kolonları page_packages tablosunda mevcuttur"
    - "gsc_metrics tablosu UNIQUE(page_id, date, keyword) constraint ve RLS ile mevcuttur"
    - "Supabase DB push başarıyla tamamlanmıştır"
  artifacts:
    - path: "supabase/migrations/20260427000001_gsc_schema.sql"
      provides: "GSC için tüm şema değişiklikleri (3 migration bloğu tek dosyada)"
      contains: "gsc_tokens, gsc_property_url, gsc_index_status, gsc_index_checked_at, gsc_metrics"
  key_links:
    - from: "gsc_metrics.page_id"
      to: "pages.id"
      via: "REFERENCES pages(id) ON DELETE CASCADE"
      pattern: "REFERENCES public.pages"
    - from: "gsc_metrics.project_id"
      to: "projects.id"
      via: "REFERENCES projects(id) ON DELETE CASCADE"
      pattern: "REFERENCES public.projects"
---

<objective>
Faz 14 için tüm veritabanı şema değişikliklerini tek migration dosyasında uygular ve Supabase'e push eder.

Purpose: OAuth token depolama (gsc_tokens JSONB), property URL kaydı, index durumu takibi ve GSC performans metriklerinin saklanabilmesi için veritabanı altyapısını hazırlar. Bu plan tamamlanmadan hiçbir sonraki plan çalışamaz.
Output: 1 migration SQL dosyası + başarılı supabase db push
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-gsc-integration/14-CONTEXT.md
@.planning/phases/14-gsc-integration/14-RESEARCH.md

<interfaces>
<!-- Mevcut migration örnekleri -->
<!-- Örnek: supabase/migrations/20260426000001_wp_columns.sql -->
-- ALTER TABLE public.page_packages ADD COLUMN IF NOT EXISTS wp_post_id BIGINT;
<!-- Pattern: ADD COLUMN IF NOT EXISTS kullan — idempotent -->
<!-- Pattern: UNIQUE constraint inline veya ALTER TABLE ... ADD CONSTRAINT -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] GSC migration SQL dosyasını oluştur ve supabase db push çalıştır</name>
  <files>supabase/migrations/20260427000001_gsc_schema.sql</files>
  <read_first>
    - supabase/migrations/ dizinini listele — mevcut migration dosyalarını gör, timestamp çakışmasını önle
    - .planning/phases/14-gsc-integration/14-RESEARCH.md §Code Examples — gsc_metrics migration SQL'i (satır 659-686), projects sütunları (satır 689-697), page_packages sütunları (satır 700-706) için tam SQL şablonları
    - .planning/STATE.md §Decisions — "gsc_tokens JSONB column on projects — server-only" kararını doğrula
  </read_first>
  <action>
Aşağıdaki tam içeriği supabase/migrations/20260427000001_gsc_schema.sql dosyasına yaz:

```sql
-- ============================================================
-- Phase 14: GSC Integration — Schema Changes
-- ============================================================

-- 1. projects tablosu: GSC token ve property URL sütunları
ALTER TABLE public.projects
  ADD COLUMN IF NOT EXISTS gsc_tokens       JSONB,
  ADD COLUMN IF NOT EXISTS gsc_property_url TEXT;
-- gsc_tokens: server-only; SELECT sorgularına asla dahil edilmez
-- gsc_property_url: Sites.list API'den dönen tam property URL (sc-domain: formatı dahil)

-- 2. page_packages tablosu: GSC index durumu sütunları
ALTER TABLE public.page_packages
  ADD COLUMN IF NOT EXISTS gsc_index_status     TEXT
    CHECK (gsc_index_status IS NULL OR gsc_index_status IN ('indexed', 'not_indexed', 'crawled_not_indexed')),
  ADD COLUMN IF NOT EXISTS gsc_index_checked_at TIMESTAMPTZ;
-- gsc_index_status: URL Inspection API verdict'ından türetilir
-- gsc_index_checked_at: son kontrol zamanı; rate limit kontrolü için kullanılır

-- 3. gsc_metrics tablosu: Search Analytics günlük/keyword bazlı performans
CREATE TABLE IF NOT EXISTS public.gsc_metrics (
  id            UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  project_id    UUID REFERENCES public.projects(id) ON DELETE CASCADE NOT NULL,
  page_id       UUID REFERENCES public.pages(id) ON DELETE CASCADE NOT NULL,
  date          DATE NOT NULL,
  keyword       TEXT NOT NULL,
  clicks        INTEGER NOT NULL DEFAULT 0,
  impressions   INTEGER NOT NULL DEFAULT 0,
  avg_position  NUMERIC(5,2),
  created_at    TIMESTAMPTZ DEFAULT now() NOT NULL,
  UNIQUE(page_id, date, keyword)
);

-- Composite index: Phase 15 Monitoring Dashboard sorguları için hazır
CREATE INDEX IF NOT EXISTS idx_gsc_metrics_page_date
  ON public.gsc_metrics(page_id, date DESC);
CREATE INDEX IF NOT EXISTS idx_gsc_metrics_project_date
  ON public.gsc_metrics(project_id, date DESC);

-- RLS: gsc_metrics için
ALTER TABLE public.gsc_metrics ENABLE ROW LEVEL SECURITY;

CREATE POLICY "gsc_metrics_select_own"
  ON public.gsc_metrics FOR SELECT
  USING (
    EXISTS (
      SELECT 1 FROM public.projects p
      WHERE p.id = gsc_metrics.project_id
        AND p.user_id = auth.uid()
    )
  );
-- INSERT/UPDATE: n8n service role key ile yapılır — service role RLS bypass eder
```

Dosyayı yazdıktan sonra şu komutu çalıştır:
```bash
supabase db push
```

Eğer SUPABASE_ACCESS_TOKEN env var ayarlanmamışsa ve interaktif auth gerekiyorsa şu alternatifle dene:
```bash
supabase db push --db-url "$DATABASE_URL"
```

Başarılı çıktı: "Applying migration 20260427000001_gsc_schema.sql" mesajı veya "Remote database is up to date."
  </action>
  <verify>
    <automated>supabase db push 2>&1 | grep -E "(Applied|up to date|error)" || echo "push completed"</automated>
  </verify>
  <done>
    - supabase/migrations/20260427000001_gsc_schema.sql dosyası mevcuttur
    - Dosya içinde "gsc_tokens", "gsc_property_url", "gsc_index_status", "gsc_index_checked_at", "gsc_metrics" string'leri geçer
    - supabase db push hata vermeden tamamlanmıştır
    - gsc_metrics tablosu UNIQUE(page_id, date, keyword) constraint içerir
    - idx_gsc_metrics_page_date ve idx_gsc_metrics_project_date indexleri SQL'de tanımlıdır
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → Supabase DB | DDL komutları doğrudan veritabanına uygulanır — geri alınamaz IF NOT EXISTS guard ile korunur |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-M-01 | Tampering | gsc_tokens JSONB sütun | mitigate | SELECT sorgularına asla dahil edilmez (uygulama kodu garantisi); RLS politikaları user_id bazlı — bu plan DB level'da kurar |
| T-14-M-02 | Information Disclosure | gsc_metrics RLS | mitigate | gsc_metrics_select_own policy: EXISTS subquery ile projects.user_id = auth.uid() doğrulaması; n8n service role bypass yeterli (yazma için auth.uid() gerekmez) |
| T-14-M-03 | Elevation of Privilege | gsc_index_status CHECK constraint | mitigate | CHECK constraint sadece 'indexed', 'not_indexed', 'crawled_not_indexed' değerlerine izin verir — SQL injection ile geçersiz değer yazılamaz |
</threat_model>

<verification>
- `supabase db push` çıktısında error yok
- `supabase/migrations/20260427000001_gsc_schema.sql` dosyası mevcuttur
- Migration SQL içinde `ADD COLUMN IF NOT EXISTS gsc_tokens` geçer
- Migration SQL içinde `ADD COLUMN IF NOT EXISTS gsc_index_status` geçer
- Migration SQL içinde `CREATE TABLE IF NOT EXISTS public.gsc_metrics` geçer
- Migration SQL içinde `UNIQUE(page_id, date, keyword)` geçer
</verification>

<success_criteria>
- 3 migration bloğu (projects ALTER, page_packages ALTER, gsc_metrics CREATE) tek SQL dosyasında
- supabase db push başarıyla tamamlandı
- idx_gsc_metrics_page_date ve idx_gsc_metrics_project_date indexleri tanımlandı
- gsc_metrics_select_own RLS policy oluşturuldu
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/14-gsc-integration/14-01-SUMMARY.md` dosyasını oluştur.
</output>
