---
phase: 14-gsc-integration
plan: 03
type: execute
wave: 3
depends_on:
  - "14-02"
files_modified:
  - src/app/(dashboard)/projeler/[id]/actions.ts
  - src/app/api/gsc/callback/route.ts
  - .env.local.example
autonomous: true
requirements:
  - GSC-01

must_haves:
  truths:
    - "initiateGscOAuth server action state'i HttpOnly cookie'ye yazar ve Google'a redirect eder"
    - "OAuth callback route CSRF state kontrolü yapar, token exchange gerçekleştirir, projects.gsc_tokens'a yazar"
    - "Callback sonrası kullanıcı /projeler/[id] sayfasına yönlendirilir"
    - "GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, NEXT_PUBLIC_APP_URL .env.local.example'de tanımlıdır"
    - "saveGscProperty server action projects.gsc_property_url günceller"
  artifacts:
    - path: "src/app/api/gsc/callback/route.ts"
      provides: "GET handler — OAuth callback, CSRF check, token exchange, Supabase write"
      exports: ["GET"]
    - path: "src/app/(dashboard)/projeler/[id]/actions.ts"
      provides: "initiateGscOAuth, saveGscProperty server actions"
      contains: "initiateGscOAuth"
    - path: ".env.local.example"
      provides: "GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, NEXT_PUBLIC_APP_URL"
      contains: "GOOGLE_CLIENT_ID"
  key_links:
    - from: "initiateGscOAuth"
      to: "/api/gsc/callback"
      via: "Google OAuth redirect_uri"
      pattern: "gsc/callback"
    - from: "/api/gsc/callback"
      to: "projects.gsc_tokens"
      via: "supabase.from('projects').update({gsc_tokens: ...}).eq('user_id', user.id)"
      pattern: "gsc_tokens"
    - from: "/api/gsc/callback"
      to: "gsc_oauth_state cookie"
      via: "cookieStore.get('gsc_oauth_state')"
      pattern: "gsc_oauth_state"

user_setup:
  - service: google-oauth
    why: "GSC OAuth flow için Google Cloud Console'da OAuth2 credentials oluşturulmalı"
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console → APIs & Services → Credentials → Create OAuth 2.0 Client ID"
      - name: GOOGLE_CLIENT_SECRET
        source: "Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID → Client secret"
      - name: NEXT_PUBLIC_APP_URL
        source: "Uygulamanın public URL'i (örn: http://localhost:3000 dev'de, https://yourdomain.com prod'da)"
    dashboard_config:
      - task: "Authorized redirect URI ekle"
        location: "Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID → Authorized redirect URIs"
        value: "{NEXT_PUBLIC_APP_URL}/api/gsc/callback"
      - task: "OAuth Consent Screen'de Google Search Console API scope ekle"
        location: "Google Cloud Console → APIs & Services → OAuth consent screen → Scopes"
        value: "https://www.googleapis.com/auth/webmasters.readonly + https://www.googleapis.com/auth/webmasters"
---

<objective>
Google OAuth 2.0 flow'unu implemente eder: state cookie oluşturan + Google'a redirect eden server action, CSRF kontrolü + token exchange yapan callback route, ve seçilen property'yi kaydeden server action.

Purpose: Kullanıcının proje başına Google Search Console hesabını güvenli şekilde bağlayabilmesi. D-01 (API route callback), D-02 (HttpOnly state cookie), D-03 (scope'lar) kararlarını implemente eder.
Output: initiateGscOAuth + saveGscProperty server actions + /api/gsc/callback route handler
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-gsc-integration/14-RESEARCH.md
@.planning/phases/14-gsc-integration/14-CONTEXT.md
@src/app/(dashboard)/projeler/[id]/actions.ts
@src/lib/gsc/auth.ts

<interfaces>
<!-- Mevcut actions.ts içinde başka action'lar var — DOSYAYI OKU, tamamen üzerine yazma -->
<!-- saveWordPressCredentials server action pattern (Phase 13) -->
'use server'
import { createClient } from '@/lib/supabase/server'
// ownership pattern: supabase.from(...).update(...).eq('id', ...).eq('user_id', user.id)

<!-- Next.js App Router cookie API -->
import { cookies } from 'next/headers'
const cookieStore = await cookies()
cookieStore.set('gsc_oauth_state', state, { httpOnly: true, secure: ..., sameSite: 'lax', maxAge: 600, path: '/' })

<!-- redirect (server action içinde) -->
import { redirect } from 'next/navigation'
redirect(authUrl.toString())

<!-- Route Handler pattern -->
import { type NextRequest, NextResponse } from 'next/server'
export async function GET(request: NextRequest) { ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: initiateGscOAuth + saveGscProperty server actions</name>
  <files>src/app/(dashboard)/projeler/[id]/actions.ts</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/actions.ts — mevcut dosya içeriği; ekleme yapılacak, üzerine yazılmayacak
    - .planning/phases/14-gsc-integration/14-RESEARCH.md §Pattern 1 (OAuth State Cookie, satır 206-242) — initiateGscOAuth tam implementasyon şablonu
    - .planning/phases/14-gsc-integration/14-CONTEXT.md §D-02, D-03 — HttpOnly cookie ve scope kararları
  </read_first>
  <action>
Mevcut actions.ts dosyasını oku. Dosyanın SONUNA şu fonksiyonları ekle (mevcut içerik korunur):

```typescript
// ─── GSC Integration Actions ───────────────────────────────────────────────

import { cookies } from 'next/headers'
import { redirect } from 'next/navigation'

// D-01: OAuth flow başlatma — state HttpOnly cookie'ye, redirect Google'a
// D-02: HttpOnly cookie ile CSRF koruması
// D-03: Scopes: webmasters.readonly + webmasters (URL Inspection için)
export async function initiateGscOAuth(projectId: string) {
  'use server'
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  // Ownership doğrula
  const { data: project } = await supabase
    .from('projects')
    .select('id')
    .eq('id', projectId)
    .eq('user_id', user.id)
    .single()
  if (!project) redirect('/projeler')

  // State: "projectId:uuid" formatı — callback'te projectId çözülür
  const state = `${projectId}:${crypto.randomUUID()}`
  const cookieStore = await cookies()
  cookieStore.set('gsc_oauth_state', state, {
    httpOnly: true,
    secure: process.env.NODE_ENV === 'production',
    sameSite: 'lax',
    maxAge: 60 * 10, // 10 dakika
    path: '/',
  })

  const authUrl = new URL('https://accounts.google.com/o/oauth2/v2/auth')
  authUrl.searchParams.set('client_id', process.env.GOOGLE_CLIENT_ID!)
  authUrl.searchParams.set('redirect_uri', `${process.env.NEXT_PUBLIC_APP_URL}/api/gsc/callback`)
  authUrl.searchParams.set('response_type', 'code')
  authUrl.searchParams.set('scope', [
    'https://www.googleapis.com/auth/webmasters.readonly',
    'https://www.googleapis.com/auth/webmasters',
  ].join(' '))
  authUrl.searchParams.set('access_type', 'offline')
  authUrl.searchParams.set('prompt', 'consent') // RESEARCH.md Pitfall 1: prompt=consent refresh_token garantisi
  authUrl.searchParams.set('state', state)

  redirect(authUrl.toString())
}

// D-04: Seçilen property'yi projects.gsc_property_url'e yaz
export async function saveGscProperty(projectId: string, propertyUrl: string) {
  'use server'
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Oturum bulunamadı.' }

  const { error } = await supabase
    .from('projects')
    .update({ gsc_property_url: propertyUrl })
    .eq('id', projectId)
    .eq('user_id', user.id) // T-14-02: ownership garantisi

  if (error) return { success: false, error: 'Property kaydedilemedi.' }
  return { success: true }
}
```

Not: `import { cookies }` ve `import { redirect }` zaten mevcut olabilir — duplicate import'ları kaldır. `'use server'` her fonksiyon başına eklenebilir ya da dosyanın başında zaten varsa ek gerekmez.
  </action>
  <verify>
    <automated>grep -n "initiateGscOAuth\|saveGscProperty\|gsc_oauth_state\|prompt.*consent" src/app/(dashboard)/projeler/[id]/actions.ts | head -20</automated>
  </verify>
  <done>
    - actions.ts içinde `initiateGscOAuth` fonksiyonu mevcuttur
    - actions.ts içinde `saveGscProperty` fonksiyonu mevcuttur
    - `gsc_oauth_state` cookie string'i mevcuttur
    - `prompt.*consent` string'i mevcuttur (Pitfall 1 mitigasyonu)
    - `webmasters.readonly` ve `webmasters` scope'ları mevcuttur
    - Mevcut actions (saveWordPressCredentials vb.) korunmuştur
  </done>
</task>

<task type="auto">
  <name>Task 2: /api/gsc/callback route handler + .env.local.example güncelleme</name>
  <files>
    src/app/api/gsc/callback/route.ts
    .env.local.example
  </files>
  <read_first>
    - .planning/phases/14-gsc-integration/14-RESEARCH.md §Pattern 2 (Callback Route Handler, satır 248-311) — tam implementasyon şablonu
    - .planning/phases/14-gsc-integration/14-RESEARCH.md §T-14-01 (OAuth state CSRF), T-14-05 (Open redirect)
    - .env.local.example — mevcut içerik; yeni satırlar eklenecek
    - src/lib/gsc/auth.ts — saveGscTokens imzası
  </read_first>
  <action>
**src/app/api/gsc/callback/route.ts** — yeni dosya oluştur:

```typescript
import { type NextRequest, NextResponse } from 'next/server'
import { cookies } from 'next/headers'
import { createClient } from '@/lib/supabase/server'
import { saveGscTokens } from '@/lib/gsc/auth'

export async function GET(request: NextRequest) {
  const { searchParams, origin } = request.nextUrl
  const code = searchParams.get('code')
  const state = searchParams.get('state')
  const error = searchParams.get('error')

  // T-14-01: CSRF — state cookie karşılaştırması
  const cookieStore = await cookies()
  const savedState = cookieStore.get('gsc_oauth_state')?.value
  cookieStore.delete('gsc_oauth_state') // tek kullanımlık — hemen sil

  if (!savedState || savedState !== state) {
    // T-14-05: Open redirect engeli — sadece kendi origin'imize redirect
    return NextResponse.redirect(new URL('/projeler?error=gsc_csrf', origin))
  }

  if (error || !code) {
    return NextResponse.redirect(new URL('/projeler?error=gsc_denied', origin))
  }

  // projectId state'ten çıkar (format: "projectId:uuid")
  const projectId = savedState.split(':')[0]

  // Kullanıcı oturumunu doğrula
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) {
    return NextResponse.redirect(new URL('/login', origin))
  }

  // Token exchange
  const tokenRes = await fetch('https://oauth2.googleapis.com/token', {
    method: 'POST',
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    body: new URLSearchParams({
      code,
      client_id: process.env.GOOGLE_CLIENT_ID!,
      client_secret: process.env.GOOGLE_CLIENT_SECRET!,
      redirect_uri: `${process.env.NEXT_PUBLIC_APP_URL}/api/gsc/callback`,
      grant_type: 'authorization_code',
    }),
  })

  if (!tokenRes.ok) {
    return NextResponse.redirect(new URL(`/projeler/${projectId}?error=gsc_token_failed`, origin))
  }

  const tokens = await tokenRes.json()
  // RESEARCH.md Pitfall 1: refresh_token yoksa kullanıcı tekrar OAuth yapmalı
  if (!tokens.access_token) {
    return NextResponse.redirect(new URL(`/projeler/${projectId}?error=gsc_token_failed`, origin))
  }

  // Ownership doğrulama + token kaydet
  await saveGscTokens(projectId, user.id, {
    access_token: tokens.access_token,
    refresh_token: tokens.refresh_token ?? '', // refresh_token bazen gelmeyebilir (Pitfall 1)
    expires_at: Date.now() + (tokens.expires_in ?? 3600) * 1000,
    token_type: tokens.token_type ?? 'Bearer',
  })

  // D-04: Callback sonrası proje sayfasına yönlendir
  return NextResponse.redirect(new URL(`/projeler/${projectId}`, origin))
}
```

**.env.local.example** — mevcut dosyayı oku ve GSC satırlarını ekle:
```
# Google Search Console OAuth2
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
NEXT_PUBLIC_APP_URL=http://localhost:3000

# n8n GSC Sync
N8N_WEBHOOK_URL=http://your-n8n-instance/webhook/gsc-sync
N8N_WEBHOOK_SECRET=your-shared-secret
```
  </action>
  <verify>
    <automated>grep -n "gsc_csrf\|gsc_oauth_state\|GOOGLE_CLIENT_ID\|saveGscTokens\|projectId.*split" src/app/api/gsc/callback/route.ts | head -20</automated>
  </verify>
  <done>
    - src/app/api/gsc/callback/route.ts mevcuttur ve GET handler export eder
    - `gsc_oauth_state` cookie silinir (cookieStore.delete) CSRF sonrası
    - `savedState !== state` CSRF kontrolü mevcuttur
    - `origin` redirect için kullanılır — external URL'e redirect yoktur (T-14-05)
    - `saveGscTokens` fonksiyonu import edilir
    - `.env.local.example` içinde `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `NEXT_PUBLIC_APP_URL` satırları mevcuttur
    - `.env.local.example` içinde `N8N_WEBHOOK_URL` ve `N8N_WEBHOOK_SECRET` mevcuttur
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Google OAuth | Kullanıcı Google consent screen'i görür; code Google'dan gelir |
| Google → /api/gsc/callback | Code + state query param'ları dışarıdan gelir — doğrulanmalı |
| /api/gsc/callback → Supabase | Token yazılırken user_id ownership kontrolü |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Spoofing | OAuth state CSRF | mitigate | `savedState !== state` kontrolü; cookie hemen silinir (tek kullanım); state mismatch → `/projeler?error=gsc_csrf` redirect |
| T-14-02 | Information Disclosure | gsc_tokens yazma | mitigate | saveGscTokens `.eq('user_id', userId)` — başka kullanıcının projesine yazılamaz; refresh_token loglanmaz |
| T-14-05 | Elevation of Privilege | Open redirect | mitigate | Tüm redirect'ler `new URL('/path', origin)` pattern — harici URL'e redirect imkansız; `origin` isteğin kendi domain'i |
| T-14-Cb | Spoofing | Callback code tampering | accept | Google authorization server imzalı code döner; PKCE olmadan da token exchange sunucu-sunucu gerçekleşir — düşük risk, standard web OAuth pattern |
</threat_model>

<verification>
- `grep "gsc_oauth_state" src/app/api/gsc/callback/route.ts` match bulur
- `grep "savedState !== state" src/app/api/gsc/callback/route.ts` match bulur
- `grep "cookieStore.delete" src/app/api/gsc/callback/route.ts` match bulur
- `grep "new URL.*origin" src/app/api/gsc/callback/route.ts` match bulur (T-14-05)
- `grep "GOOGLE_CLIENT_ID" .env.local.example` match bulur
- `npm run build` TypeScript hatası vermez
</verification>

<success_criteria>
- initiateGscOAuth: state={projectId:uuid} cookie + Google auth URL redirect
- /api/gsc/callback: CSRF check → token exchange → saveGscTokens → /projeler/[id] redirect
- saveGscProperty: projects.gsc_property_url güncellemesi
- .env.local.example: 5 yeni env var (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, NEXT_PUBLIC_APP_URL, N8N_WEBHOOK_URL, N8N_WEBHOOK_SECRET)
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/14-gsc-integration/14-03-SUMMARY.md` dosyasını oluştur.
</output>
