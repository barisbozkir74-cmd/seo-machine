---
phase: 14-gsc-integration
plan: 02
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - src/lib/gsc/auth.ts
  - src/lib/gsc/properties.ts
  - src/lib/gsc/index-check.ts
  - src/lib/gsc/search-analytics.ts
  - src/lib/gsc/__tests__/auth.test.ts
  - src/lib/gsc/__tests__/index-check.test.ts
  - src/lib/gsc/__tests__/search-analytics.test.ts
autonomous: true
requirements:
  - GSC-01
  - GSC-02
  - GSC-03

must_haves:
  truths:
    - "GSC token okuma, yenileme ve geçerlilik kontrolü tek helper'da kapsüllenmiştir"
    - "URL Inspection API verdict (PASS/FAIL/NEUTRAL) doğru badge state'ine map edilir"
    - "Search Analytics API satırları (keys[0]=page, keys[1]=keyword) parse edilir"
    - "Tüm lib dosyaları server-only — client'a sızmaz"
    - "Birim testleri yeşildir"
  artifacts:
    - path: "src/lib/gsc/auth.ts"
      provides: "getValidGscToken, saveGscTokens, GscTokens tipi"
      exports: ["getValidGscToken", "saveGscTokens", "GscTokensSchema", "GscTokens"]
    - path: "src/lib/gsc/properties.ts"
      provides: "listGscProperties — Sites.list API çağrısı"
      exports: ["listGscProperties"]
    - path: "src/lib/gsc/index-check.ts"
      provides: "checkUrlIndexStatus — verdict → status map"
      exports: ["checkUrlIndexStatus"]
    - path: "src/lib/gsc/search-analytics.ts"
      provides: "fetchSearchAnalytics — Search Analytics API çağrısı"
      exports: ["fetchSearchAnalytics"]
    - path: "src/lib/gsc/__tests__/auth.test.ts"
      provides: "Token yönetimi birim testleri"
      contains: "getValidGscToken"
    - path: "src/lib/gsc/__tests__/index-check.test.ts"
      provides: "Verdict mapping birim testi"
      contains: "PASS.*indexed"
    - path: "src/lib/gsc/__tests__/search-analytics.test.ts"
      provides: "Row parse birim testi"
      contains: "keys"
  key_links:
    - from: "src/lib/gsc/auth.ts"
      to: "projects.gsc_tokens"
      via: "Supabase server client SELECT + UPDATE"
      pattern: "from('projects').select('gsc_tokens')"
    - from: "src/lib/gsc/index-check.ts"
      to: "searchconsole.googleapis.com/v1/urlInspection"
      via: "fetch POST + Bearer token"
      pattern: "urlInspection/index:inspect"
---

<objective>
GSC API ile iletişim kuran tüm lib fonksiyonlarını oluşturur: token yönetimi, property listesi, index kontrolü ve search analytics. Birim testleri aynı wave'de yazılır.

Purpose: Sonraki planların (OAuth flow, UI, sync endpoint) import edebileceği stabil, test edilmiş bir lib katmanı. Server-only guard ile token'lar client'a sızmaz.
Output: src/lib/gsc/ altında 4 lib dosyası + 3 test dosyası
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-gsc-integration/14-RESEARCH.md
@.planning/phases/14-gsc-integration/14-CONTEXT.md
@src/lib/supabase/vault.ts

<interfaces>
<!-- vault.ts server-only pattern — auth.ts aynı kalıbı izler -->
import 'server-only'
import { createClient } from '@supabase/supabase-js'
const serviceClient = createClient(...)
// auth.ts ise server component client kullanır (user context gerekli):
import { createClient } from '@/lib/supabase/server'

<!-- Zod v4 syntax (projenin standardı) -->
import { z } from 'zod'
export const GscTokensSchema = z.object({
  access_token: z.string(),
  refresh_token: z.string(),
  expires_at: z.number(),
  token_type: z.string().default('Bearer'),
})
export type GscTokens = z.infer<typeof GscTokensSchema>

<!-- Mevcut createClient import path'i -->
import { createClient } from '@/lib/supabase/server'

<!-- vitest test syntax (projenin standardı) -->
import { describe, it, expect, vi } from 'vitest'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GSC lib katmanı — auth.ts + properties.ts + index-check.ts + search-analytics.ts</name>
  <files>
    src/lib/gsc/auth.ts
    src/lib/gsc/properties.ts
    src/lib/gsc/index-check.ts
    src/lib/gsc/search-analytics.ts
  </files>
  <read_first>
    - .planning/phases/14-gsc-integration/14-RESEARCH.md §Pattern 3 (Token Auto-Refresh, satır 314-374) — getValidGscToken tam implementasyon
    - .planning/phases/14-gsc-integration/14-RESEARCH.md §Pattern 4 (URL Inspection, satır 379-405) — checkUrlIndexStatus tam implementasyon
    - .planning/phases/14-gsc-integration/14-RESEARCH.md §Pattern 5 (Search Analytics, satır 409-438) — fetchSearchAnalytics tam implementasyon
    - .planning/phases/14-gsc-integration/14-RESEARCH.md §gsc_tokens JSONB Schema (Zod) satır 618-629 — GscTokensSchema
    - src/lib/supabase/vault.ts — server-only import pattern ve createClient kullanımı
  </read_first>
  <behavior>
    - getValidGscToken(projectId, userId): projects.gsc_tokens okur → expires_at < now+5dk ise refresh yapar → güncel access_token döner; gsc_tokens null ise null döner
    - getValidGscToken refresh: POST https://oauth2.googleapis.com/token body=URLSearchParams({client_id, client_secret, refresh_token, grant_type:'refresh_token'}); başarılıysa projects güncellenir
    - checkUrlIndexStatus('PASS') → 'indexed'; 'FAIL' → 'not_indexed'; 'NEUTRAL' → 'crawled_not_indexed'; diğer → 'unknown'
    - fetchSearchAnalytics response.rows[].keys[0] = pageUrl, keys[1] = keyword
    - saveGscTokens: projects tablosuna gsc_tokens JSONB yazar — .eq('user_id', userId) ownership garantisi
    - listGscProperties: GET https://www.googleapis.com/webmasters/v3/sites Bearer token ile; response.siteEntry[].siteUrl dizisi döner
  </behavior>
  <action>
Önce `src/lib/gsc/` dizinini oluştur. Sonra her dosyayı aşağıdaki spesifikasyona göre oluştur:

**src/lib/gsc/auth.ts** — tam içerik:
```typescript
import 'server-only'
import { z } from 'zod'
import { createClient } from '@/lib/supabase/server'

export const GscTokensSchema = z.object({
  access_token: z.string(),
  refresh_token: z.string(),
  expires_at: z.number(), // ms timestamp
  token_type: z.string().default('Bearer'),
})
export type GscTokens = z.infer<typeof GscTokensSchema>

export async function saveGscTokens(
  projectId: string,
  userId: string,
  tokens: GscTokens
): Promise<void> {
  const supabase = await createClient()
  await supabase
    .from('projects')
    .update({ gsc_tokens: tokens })
    .eq('id', projectId)
    .eq('user_id', userId) // T-14-02: ownership garantisi — user_id tampering engellenir
}

export async function getValidGscToken(
  projectId: string,
  userId: string
): Promise<string | null> {
  const supabase = await createClient()
  const { data: project } = await supabase
    .from('projects')
    .select('gsc_tokens')
    .eq('id', projectId)
    .eq('user_id', userId)
    .single()

  if (!project?.gsc_tokens) return null

  const parseResult = GscTokensSchema.safeParse(project.gsc_tokens)
  if (!parseResult.success) return null
  const tokens = parseResult.data

  // Token hala geçerli (5 dakika tolerans)
  if (tokens.expires_at > Date.now() + 5 * 60 * 1000) {
    return tokens.access_token
  }

  // Token süresi dolmuş — refresh
  const refreshRes = await fetch('https://oauth2.googleapis.com/token', {
    method: 'POST',
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    body: new URLSearchParams({
      client_id: process.env.GOOGLE_CLIENT_ID!,
      client_secret: process.env.GOOGLE_CLIENT_SECRET!,
      refresh_token: tokens.refresh_token,
      grant_type: 'refresh_token',
    }),
  })
  const refreshed = await refreshRes.json()
  if (!refreshed.access_token) return null // refresh başarısız

  const newTokens: GscTokens = {
    ...tokens,
    access_token: refreshed.access_token,
    expires_at: Date.now() + (refreshed.expires_in ?? 3600) * 1000,
  }

  await saveGscTokens(projectId, userId, newTokens)
  return newTokens.access_token
}
```

**src/lib/gsc/properties.ts** — tam içerik:
```typescript
import 'server-only'

export type GscProperty = {
  siteUrl: string
  permissionLevel: string
}

export async function listGscProperties(
  accessToken: string
): Promise<GscProperty[]> {
  const res = await fetch(
    'https://www.googleapis.com/webmasters/v3/sites',
    {
      headers: { Authorization: `Bearer ${accessToken}` },
    }
  )
  if (!res.ok) return []
  const data = await res.json()
  return (data.siteEntry ?? []) as GscProperty[]
}
```

**src/lib/gsc/index-check.ts** — tam içerik:
```typescript
import 'server-only'

export type IndexStatus = 'indexed' | 'not_indexed' | 'crawled_not_indexed' | 'unknown'

export async function checkUrlIndexStatus(
  accessToken: string,
  inspectionUrl: string,
  siteUrl: string
): Promise<IndexStatus> {
  const res = await fetch(
    'https://searchconsole.googleapis.com/v1/urlInspection/index:inspect',
    {
      method: 'POST',
      headers: {
        Authorization: `Bearer ${accessToken}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ inspectionUrl, siteUrl }),
    }
  )
  if (!res.ok) return 'unknown'
  const data = await res.json()
  const verdict: string | undefined = data?.inspectionResult?.indexStatusResult?.verdict

  // PASS=indexed, FAIL=not_indexed, NEUTRAL=crawled_not_indexed
  if (verdict === 'PASS') return 'indexed'
  if (verdict === 'FAIL') return 'not_indexed'
  if (verdict === 'NEUTRAL') return 'crawled_not_indexed'
  return 'unknown'
}
```

**src/lib/gsc/search-analytics.ts** — tam içerik:
```typescript
import 'server-only'

export type SearchAnalyticsRow = {
  pageUrl: string
  keyword: string
  clicks: number
  impressions: number
  avgPosition: number
}

export async function fetchSearchAnalytics(
  accessToken: string,
  siteUrl: string,
  startDate: string, // YYYY-MM-DD
  endDate: string
): Promise<SearchAnalyticsRow[]> {
  const encodedSite = encodeURIComponent(siteUrl)
  const res = await fetch(
    `https://www.googleapis.com/webmasters/v3/sites/${encodedSite}/searchAnalytics/query`,
    {
      method: 'POST',
      headers: {
        Authorization: `Bearer ${accessToken}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        startDate,
        endDate,
        dimensions: ['page', 'query'],
        rowLimit: 25000,
        dataState: 'final',
      }),
    }
  )
  if (!res.ok) return []
  const data = await res.json()
  if (!data.rows) return []

  return data.rows.map((row: {
    keys: string[]
    clicks: number
    impressions: number
    position: number
  }) => ({
    pageUrl: row.keys[0],
    keyword: row.keys[1],
    clicks: row.clicks,
    impressions: row.impressions,
    avgPosition: Math.round(row.position * 100) / 100,
  }))
}
```
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error TS|Error)" | head -20 || echo "build check passed"</automated>
  </verify>
  <done>
    - 4 dosya mevcuttur: auth.ts, properties.ts, index-check.ts, search-analytics.ts
    - Her dosyada `import 'server-only'` direktifi vardır
    - auth.ts içinde `GscTokensSchema` ve `getValidGscToken` export edilir
    - index-check.ts `checkUrlIndexStatus` export eder ve PASS/FAIL/NEUTRAL mapping'i içerir
    - search-analytics.ts `fetchSearchAnalytics` export eder ve `keys[0]`/`keys[1]` parse eder
    - TypeScript derleme hatası yoktur
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GSC lib birim testleri — auth, index-check, search-analytics</name>
  <files>
    src/lib/gsc/__tests__/auth.test.ts
    src/lib/gsc/__tests__/index-check.test.ts
    src/lib/gsc/__tests__/search-analytics.test.ts
  </files>
  <read_first>
    - src/lib/gsc/auth.ts — test edilecek fonksiyon imzaları
    - src/lib/gsc/index-check.ts — verdict mapping testi için
    - src/lib/gsc/search-analytics.ts — row parse testi için
    - vitest.config.ts veya package.json scripts.test — test runner konfigürasyonu
  </read_first>
  <behavior>
    - auth.test.ts: getValidGscToken — geçerli token döner (expires_at gelecekte); süresi dolmuş token refresh tetikler; gsc_tokens null ise null döner; refresh başarısız ise null döner
    - index-check.test.ts: checkUrlIndexStatus — PASS → 'indexed'; FAIL → 'not_indexed'; NEUTRAL → 'crawled_not_indexed'; bilinmeyen verdict → 'unknown'; res.ok false ise 'unknown'
    - search-analytics.test.ts: fetchSearchAnalytics — rows parse: keys[0]=pageUrl, keys[1]=keyword, position=1.5 → avgPosition=1.5; boş rows → []
  </behavior>
  <action>
Supabase createClient'ı mock'la (vi.mock('@/lib/supabase/server')) ve fetch'i vi.stubGlobal('fetch', ...) ile taklit et.

**src/lib/gsc/__tests__/auth.test.ts**:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

// Supabase server client mock
vi.mock('@/lib/supabase/server', () => ({
  createClient: vi.fn(),
}))
// server-only mock
vi.mock('server-only', () => ({}))

import { getValidGscToken, GscTokensSchema } from '../auth'
import { createClient } from '@/lib/supabase/server'

const mockSupabase = {
  from: vi.fn().mockReturnThis(),
  select: vi.fn().mockReturnThis(),
  eq: vi.fn().mockReturnThis(),
  update: vi.fn().mockReturnThis(),
  single: vi.fn(),
}

beforeEach(() => {
  vi.mocked(createClient).mockResolvedValue(mockSupabase as never)
  mockSupabase.single.mockReset()
  mockSupabase.update.mockReturnValue({ eq: vi.fn().mockResolvedValue({}) })
})

describe('getValidGscToken', () => {
  it('geçerli token döner (expires_at gelecekte)', async () => {
    const futureExpiry = Date.now() + 60 * 60 * 1000 // 1 saat sonra
    mockSupabase.single.mockResolvedValue({
      data: {
        gsc_tokens: {
          access_token: 'valid-token',
          refresh_token: 'refresh',
          expires_at: futureExpiry,
          token_type: 'Bearer',
        },
      },
    })

    const token = await getValidGscToken('proj-1', 'user-1')
    expect(token).toBe('valid-token')
  })

  it('gsc_tokens null ise null döner', async () => {
    mockSupabase.single.mockResolvedValue({ data: { gsc_tokens: null } })
    const token = await getValidGscToken('proj-1', 'user-1')
    expect(token).toBeNull()
  })

  it('süresi dolmuş token refresh tetikler', async () => {
    const pastExpiry = Date.now() - 1000 // geçmişte
    mockSupabase.single.mockResolvedValue({
      data: {
        gsc_tokens: {
          access_token: 'old-token',
          refresh_token: 'refresh-token',
          expires_at: pastExpiry,
          token_type: 'Bearer',
        },
      },
    })

    vi.stubGlobal('fetch', vi.fn().mockResolvedValue({
      json: vi.fn().mockResolvedValue({
        access_token: 'new-token',
        expires_in: 3600,
      }),
      ok: true,
    }))

    const token = await getValidGscToken('proj-1', 'user-1')
    expect(token).toBe('new-token')

    vi.unstubAllGlobals()
  })

  it('refresh başarısız ise null döner', async () => {
    const pastExpiry = Date.now() - 1000
    mockSupabase.single.mockResolvedValue({
      data: {
        gsc_tokens: {
          access_token: 'old-token',
          refresh_token: 'bad-refresh',
          expires_at: pastExpiry,
          token_type: 'Bearer',
        },
      },
    })

    vi.stubGlobal('fetch', vi.fn().mockResolvedValue({
      json: vi.fn().mockResolvedValue({}), // access_token yok
      ok: true,
    }))

    const token = await getValidGscToken('proj-1', 'user-1')
    expect(token).toBeNull()

    vi.unstubAllGlobals()
  })
})
```

**src/lib/gsc/__tests__/index-check.test.ts**:
```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'

vi.mock('server-only', () => ({}))

import { checkUrlIndexStatus } from '../index-check'

function mockFetchWithVerdict(verdict: string | undefined, ok = true) {
  vi.stubGlobal('fetch', vi.fn().mockResolvedValue({
    ok,
    json: vi.fn().mockResolvedValue(
      verdict !== undefined
        ? { inspectionResult: { indexStatusResult: { verdict } } }
        : {}
    ),
  }))
}

afterEach(() => vi.unstubAllGlobals())

describe('checkUrlIndexStatus', () => {
  it('PASS → indexed', async () => {
    mockFetchWithVerdict('PASS')
    const result = await checkUrlIndexStatus('token', 'https://example.com/page', 'https://example.com/')
    expect(result).toBe('indexed')
  })

  it('FAIL → not_indexed', async () => {
    mockFetchWithVerdict('FAIL')
    const result = await checkUrlIndexStatus('token', 'https://example.com/page', 'https://example.com/')
    expect(result).toBe('not_indexed')
  })

  it('NEUTRAL → crawled_not_indexed', async () => {
    mockFetchWithVerdict('NEUTRAL')
    const result = await checkUrlIndexStatus('token', 'https://example.com/page', 'https://example.com/')
    expect(result).toBe('crawled_not_indexed')
  })

  it('bilinmeyen verdict → unknown', async () => {
    mockFetchWithVerdict('SOMETHING_ELSE')
    const result = await checkUrlIndexStatus('token', 'https://example.com/page', 'https://example.com/')
    expect(result).toBe('unknown')
  })

  it('res.ok false → unknown', async () => {
    mockFetchWithVerdict('PASS', false)
    const result = await checkUrlIndexStatus('token', 'https://example.com/page', 'https://example.com/')
    expect(result).toBe('unknown')
  })
})
```

**src/lib/gsc/__tests__/search-analytics.test.ts**:
```typescript
import { describe, it, expect, vi, afterEach } from 'vitest'

vi.mock('server-only', () => ({}))

import { fetchSearchAnalytics } from '../search-analytics'

afterEach(() => vi.unstubAllGlobals())

describe('fetchSearchAnalytics', () => {
  it('rows parse: keys[0]=pageUrl, keys[1]=keyword', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue({
      ok: true,
      json: vi.fn().mockResolvedValue({
        rows: [
          { keys: ['https://example.com/blog', 'seo ipuçları'], clicks: 10, impressions: 100, position: 3.7 },
        ],
      }),
    }))

    const rows = await fetchSearchAnalytics('token', 'https://example.com/', '2026-04-01', '2026-04-27')
    expect(rows).toHaveLength(1)
    expect(rows[0].pageUrl).toBe('https://example.com/blog')
    expect(rows[0].keyword).toBe('seo ipuçları')
    expect(rows[0].clicks).toBe(10)
    expect(rows[0].impressions).toBe(100)
    expect(rows[0].avgPosition).toBe(3.7)
  })

  it('boş rows → []', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue({
      ok: true,
      json: vi.fn().mockResolvedValue({}), // rows yok
    }))

    const rows = await fetchSearchAnalytics('token', 'https://example.com/', '2026-04-01', '2026-04-27')
    expect(rows).toEqual([])
  })

  it('res.ok false → []', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue({ ok: false }))
    const rows = await fetchSearchAnalytics('token', 'https://example.com/', '2026-04-01', '2026-04-27')
    expect(rows).toEqual([])
  })
})
```
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose src/lib/gsc 2>&1 | tail -30</automated>
  </verify>
  <done>
    - 3 test dosyası mevcuttur
    - `npm test -- src/lib/gsc` yeşil çıktı verir (tüm testler pass)
    - auth.test.ts: 4 test case (geçerli token, null tokens, refresh tetikleme, refresh başarısız)
    - index-check.test.ts: 5 test case (PASS/FAIL/NEUTRAL/bilinmeyen/res.ok false)
    - search-analytics.test.ts: 3 test case (parse, boş rows, res.ok false)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lib/gsc → Google OAuth2 endpoint | Token refresh isteği — client_secret server-only env var'dan gelir |
| lib/gsc → GSC API endpoints | Bearer token ile çağrı — token asla log'lanmaz |
| lib/gsc → Supabase projects | gsc_tokens JSONB okuma/yazma — user_id ownership check zorunlu |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Information Disclosure | gsc_tokens JSONB | mitigate | auth.ts'de `import 'server-only'` — Next.js client bundle'a dahil edilmesi derleme hatası verir; saveGscTokens her zaman `.eq('user_id', userId)` filtresi uygular |
| T-14-02b | Information Disclosure | refresh_token | mitigate | refresh_token loglanmaz — response sadece access_token çıkarılır; console.log veya hata mesajlarında token string'i geçmez |
| T-14-03 | Elevation of Privilege | getValidGscToken | mitigate | Her çağrıda projectId + userId çifti doğrulanır — bir kullanıcı başka projenin token'ına erişemez |
</threat_model>

<verification>
- `npm test -- src/lib/gsc` tüm testler yeşil
- `import 'server-only'` her 4 lib dosyasında mevcuttur
- `grep -r "PASS.*indexed\|FAIL.*not_indexed\|NEUTRAL.*crawled" src/lib/gsc/` match bulur
- `grep "keys\[0\]" src/lib/gsc/search-analytics.ts` match bulur
- `grep "user_id.*userId\|userId.*user_id" src/lib/gsc/auth.ts` match bulur
</verification>

<success_criteria>
- 4 lib dosyası server-only guard ile oluşturuldu
- 3 test dosyası, toplam 12 test case yeşil
- GscTokensSchema Zod ile validate eder — geçersiz shape null döndürür
- checkUrlIndexStatus PASS/FAIL/NEUTRAL tam mapping içerir
- fetchSearchAnalytics keys[0]/keys[1] pozisyon parse eder
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/14-gsc-integration/14-02-SUMMARY.md` dosyasını oluştur.
</output>
