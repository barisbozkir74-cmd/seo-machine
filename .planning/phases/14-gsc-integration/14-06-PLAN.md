---
phase: 14-gsc-integration
plan: 06
type: execute
wave: 6
depends_on: []
files_modified:
  - middleware.ts
  - src/app/(dashboard)/projeler/[id]/gsc-section.tsx
  - src/app/(dashboard)/projeler/[id]/page.tsx
autonomous: true
gap_closure: true
requirements:
  - GSC-02
  - GSC-03

must_haves:
  truths:
    - "n8n server-to-server POST /api/gsc/sync geçer, 307 redirect almaz"
    - "GscConnectionSection handleSync body'de userId gönderir, route handler 400 döndürmez"
  artifacts:
    - path: "middleware.ts"
      provides: "/api/gsc/sync hariç tutma"
      contains: "pathname.startsWith('/api/gsc/sync')"
    - path: "src/app/(dashboard)/projeler/[id]/gsc-section.tsx"
      provides: "userId prop ve sync body"
      contains: "userId"
    - path: "src/app/(dashboard)/projeler/[id]/page.tsx"
      provides: "userId prop geçişi"
      contains: "userId={user.id}"
  key_links:
    - from: "middleware.ts"
      to: "src/app/api/gsc/sync/route.ts"
      via: "pathname exception — bypass auth redirect"
      pattern: "pathname\\.startsWith\\('/api/gsc/sync'\\)"
    - from: "src/app/(dashboard)/projeler/[id]/page.tsx"
      to: "src/app/(dashboard)/projeler/[id]/gsc-section.tsx"
      via: "userId prop"
      pattern: "userId=\\{user\\.id\\}"
    - from: "src/app/(dashboard)/projeler/[id]/gsc-section.tsx"
      to: "/api/gsc/sync"
      via: "fetch body"
      pattern: "userId"
---

<objective>
İki doğrulama gap'ini kapat: (1) middleware'in /api/gsc/sync rotasını n8n'in server-to-server çağrıları için bypass etmesi, (2) GscConnectionSection'ın sync isteğinde userId göndermesi.

Purpose: Bu iki gap olmadan GSC sync tamamen işlevsiz — middleware 307 döndürür, body eksik userId yüzünden route 400 döner.
Output: middleware.ts güncellenmiş pathname exception ile, gsc-section.tsx userId prop ile, page.tsx userId geçişi ile.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: middleware.ts — /api/gsc/sync pathname exception ekle</name>
  <files>middleware.ts</files>
  <read_first>middleware.ts</read_first>
  <action>
middleware.ts satır 45'teki koşulu güncelle. Mevcut:

```typescript
if (!user && !pathname.startsWith('/login') && !pathname.startsWith('/signup') && pathname !== '/') {
```

Şu hale getir:

```typescript
if (!user && !pathname.startsWith('/login') && !pathname.startsWith('/signup') && !pathname.startsWith('/api/gsc/sync') && pathname !== '/') {
```

Bu değişiklik `/api/gsc/sync`'i session cookie olmadan gelen isteklerde (n8n gibi server-to-server çağrılar) auth redirect'ten muaf tutar. Route handler kendi bearer token doğrulamasını yapıyor — middleware bypass güvenli.

Başka hiçbir şeyi değiştirme. Stale refresh token bloğu (satır 31-41) ve matcher config (satır 60-64) dokunulmadan kalmalı.
  </action>
  <verify>
    <automated>grep -n "pathname.startsWith('/api/gsc/sync')" middleware.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "pathname.startsWith('/api/gsc/sync')" middleware.ts` en az 1 satır döndürür
    - Satır 45 civarında koşul dört `&&` ifadesi içerir (login, signup, /api/gsc/sync, pathname !== '/')
    - TypeScript derleme hatası yok: `npx tsc --noEmit` temiz çıkar
  </acceptance_criteria>
  <done>middleware.ts /api/gsc/sync isteklerini auth redirect'ten muaf tutar; n8n POST isteği 307 yerine route handler'a ulaşır.</done>
</task>

<task type="auto">
  <name>Task 2: userId prop — page.tsx geçişi ve gsc-section.tsx kullanımı</name>
  <files>
    src/app/(dashboard)/projeler/[id]/page.tsx
    src/app/(dashboard)/projeler/[id]/gsc-section.tsx
  </files>
  <read_first>
    src/app/(dashboard)/projeler/[id]/page.tsx
    src/app/(dashboard)/projeler/[id]/gsc-section.tsx
  </read_first>
  <action>
**1. gsc-section.tsx — Props tipine userId ekle:**

Mevcut Props tipi (satır 17-21):
```typescript
type Props = {
  projectId: string
  isConnected: boolean
  gscPropertyUrl: string | null
}
```

Şu hale getir:
```typescript
type Props = {
  projectId: string
  userId: string
  isConnected: boolean
  gscPropertyUrl: string | null
}
```

**2. gsc-section.tsx — Fonksiyon imzasına userId ekle:**

Mevcut (satır 23):
```typescript
export function GscConnectionSection({ projectId, isConnected, gscPropertyUrl }: Props) {
```

Şu hale getir:
```typescript
export function GscConnectionSection({ projectId, userId, isConnected, gscPropertyUrl }: Props) {
```

**3. gsc-section.tsx — handleSync body'e userId ekle:**

Mevcut (satır 92-96):
```typescript
      const res = await fetch('/api/gsc/sync', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ projectId }),
      })
```

Şu hale getir:
```typescript
      const res = await fetch('/api/gsc/sync', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ projectId, userId }),
      })
```

**4. page.tsx — GscConnectionSection'a userId prop geç:**

page.tsx'te user zaten mevcut (satır 19: `const { data: { user } } = await supabase.auth.getUser()`).

Mevcut GscConnectionSection render (satır 97-101):
```tsx
          <GscConnectionSection
            projectId={project.id}
            isConnected={isGscConnected}
            gscPropertyUrl={project.gsc_property_url ?? null}
          />
```

Şu hale getir:
```tsx
          <GscConnectionSection
            projectId={project.id}
            userId={user.id}
            isConnected={isGscConnected}
            gscPropertyUrl={project.gsc_property_url ?? null}
          />
```

Başka hiçbir şeyi değiştirme.
  </action>
  <verify>
    <automated>grep -n "userId" src/app/\(dashboard\)/projeler/\[id\]/gsc-section.tsx && grep -n "userId={user.id}" src/app/\(dashboard\)/projeler/\[id\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "userId" src/app/(dashboard)/projeler/[id]/gsc-section.tsx` Props tipinde, destructuring'de ve fetch body'de toplam 3+ satır döndürür
    - `grep "userId={user.id}" src/app/(dashboard)/projeler/[id]/page.tsx` en az 1 satır döndürür
    - `grep "projectId, userId" src/app/(dashboard)/projeler/[id]/gsc-section.tsx` fetch body satırını bulur
    - TypeScript derleme hatası yok: `npx tsc --noEmit` temiz çıkar
  </acceptance_criteria>
  <done>GscConnectionSection userId prop alır; handleSync POST body'de { projectId, userId } gönderir; route handler 400 yerine işlemi işler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n → /api/gsc/sync | Sunucudan sunucuya çağrı; oturum çerezi yok, route handler bearer token doğrulaması yapıyor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-gap-01 | Spoofing | middleware.ts — /api/gsc/sync bypass | mitigate | Route handler kendi N8N_WEBHOOK_SECRET bearer token kontrolünü yapıyor; middleware bypass sadece session redirect'i atlar, auth'u kaldırmaz |
| T-14-gap-02 | Tampering | gsc-section.tsx — userId client'tan geliyor | accept | userId Supabase auth'dan server component tarafından geçiriliyor (user.id); route handler userId'yi Supabase token'dan da doğrulayabilir — düşük risk |
</threat_model>

<verification>
Her iki gap kapatıldıktan sonra:

1. middleware exception — `grep "pathname.startsWith('/api/gsc/sync')" middleware.ts` sonuç döndürür
2. userId prop geçişi — `grep "userId={user.id}" src/app/(dashboard)/projeler/[id]/page.tsx` sonuç döndürür
3. sync body — `grep "userId" src/app/(dashboard)/projeler/[id]/gsc-section.tsx` 3+ satır döndürür
4. TypeScript temiz — `npx tsc --noEmit` hata yok
5. Uçtan uca — n8n POST /api/gsc/sync 200 döner (307 değil), dashboard "Senkronize Et" butonu 400 değil başarı döner
</verification>

<success_criteria>
- n8n server-to-server POST /api/gsc/sync isteği middleware'den geçer (307 yok)
- "Senkronize Et" butonu { projectId, userId } ile route'a ulaşır, 400 almaz
- Başka hiçbir middleware davranışı değişmez (login/signup redirect korunur)
- TypeScript derleme hatası yok
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/14-gsc-integration/14-06-SUMMARY.md` oluştur.
</output>
