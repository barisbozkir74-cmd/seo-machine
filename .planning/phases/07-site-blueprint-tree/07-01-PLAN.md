---
phase: 07-site-blueprint-tree
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/pages/slugify.ts
  - src/lib/pages/slugify.test.ts
  - src/app/(dashboard)/projeler/[id]/site-blueprint/actions.ts
autonomous: true
requirements: [BLUE-01, BLUE-02, BLUE-03]
tags: [site-blueprint, server-actions, slugify, clustering, page-generation]

must_haves:
  truths:
    - "Bir projede kümelenmiş keyword'ler varsa, sistem tek çağrıda her cluster için bir pages satırı oluşturur"
    - "Aynı cluster_id için ikinci kez üretim denendiğinde mevcut sayfa korunur ve skip sayısı raporlanır"
    - "Üretilen her sayfanın focus_keyword_id değeri cluster'ın primary_keyword_id değerine eşittir"
    - "Üretilen her sayfanın slug değeri focus keyword'ün D-06 kurallarına göre normalize edilmiş halidir"
    - "Proje içinde aynı slug varsa, otomatik olarak -2, -3 suffix eklenir"
    - "Sayfa tipi cluster.intent'ten D-02 tablosu ile türetilir (transactional/commercial→hizmet, informational/unknown→blog, navigational→ana-sayfa)"
    - "reorderPage çağrısı ile bir sayfa aynı parent_id altındaki komşu kardeşiyle sort_order değerini takas eder"
    - "Slugify yardımcı fonksiyonu Türkçe karakterleri (ç,ş,ğ,ü,ö,ı) ASCII'ye dönüştürür"
  artifacts:
    - path: "src/lib/pages/slugify.ts"
      provides: "slugify(text, existingSlugs) — D-06 kurallı slug üretici + unique suffix"
      exports: ["slugify"]
    - path: "src/lib/pages/slugify.test.ts"
      provides: "Vitest unit tests — Turkish char map, max 60, duplicate -2/-3 suffix"
    - path: "src/app/(dashboard)/projeler/[id]/site-blueprint/actions.ts"
      provides: "generatePagesFromClusters + reorderPage Server Actions (+ INTENT_TO_PAGE_TYPE export)"
      exports:
        - "generatePagesFromClusters"
        - "reorderPage"
        - "INTENT_TO_PAGE_TYPE"
        - "GenerateRowInput"
        - "GenerateResult"
  key_links:
    - from: "generatePagesFromClusters"
      to: "keyword_clusters table"
      via: "supabase.from('keyword_clusters').select('id, cluster_name, intent, primary_keyword_id')"
      pattern: "from\\('keyword_clusters'\\)"
    - from: "generatePagesFromClusters"
      to: "pages table INSERT"
      via: "supabase.from('pages').insert({ cluster_id, focus_keyword_id, slug, page_type, title, ... })"
      pattern: "from\\('pages'\\)[\\s\\S]*?\\.insert"
    - from: "generatePagesFromClusters"
      to: "slugify helper"
      via: "import { slugify } from '@/lib/pages/slugify'"
      pattern: "from ['\"]@/lib/pages/slugify['\"]"
    - from: "reorderPage"
      to: "pages sort_order swap"
      via: "two UPDATE calls (Promise.all) swapping sort_order between adjacent siblings"
      pattern: "Promise\\.all\\([\\s\\S]*?\\.update\\([\\s\\S]*?sort_order"
---

<objective>
Phase 7 backend temellerini kur — keyword kümelerinden otomatik sayfa üreten `generatePagesFromClusters`, kardeş sayfalar arasında sort_order takası yapan `reorderPage` Server Action'ları ve D-06 kurallı `slugify` yardımcı fonksiyonu.

Purpose: UI plan'ının (07-02) tüm data yazma operasyonlarını güvenle çağırabileceği, ownership'li ve duplicate-korumalı bir veritabanı yüzeyi sağlamak. BLUE-01 (cluster → sayfa), BLUE-02 (keyword→page mapping: focus_keyword_id) ve BLUE-03 (yeniden sıralama) gereksinimlerinin write path'i bu plan'da tamamlanır.

Output: `src/lib/pages/slugify.ts` + unit testleri ve `site-blueprint/actions.ts` içine iki yeni Server Action + INTENT_TO_PAGE_TYPE sabit haritası.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-site-blueprint-tree/07-CONTEXT.md
@.planning/phases/07-site-blueprint-tree/07-PATTERNS.md
@src/app/(dashboard)/projeler/[id]/site-blueprint/actions.ts
@src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts
@src/lib/keywords/scoring.ts

<interfaces>
<!-- Mevcut actions.ts'de tanımlı ve koruyarak üzerine eklenecek sözleşmeler -->

From src/app/(dashboard)/projeler/[id]/site-blueprint/actions.ts (korunacak, YENI action'lar aynı dosyaya eklenecek):
```typescript
export type ActionResult = { success: true } | { success: false; error: string }
export type Priority = 'yüksek' | 'orta' | 'düşük'

// Mevcut helper — reorderPage ve generatePagesFromClusters bunu yeniden kullanacak
async function verifyProjectOwnership(
  supabase: Awaited<ReturnType<typeof createClient>>,
  projectId: string,
  userId: string
): Promise<boolean>

// Mevcut — dokunulmayacak
export async function addPage(projectId: string, input: AddPageInput): Promise<ActionResult>
export async function deletePage(projectId: string, pageId: string): Promise<ActionResult>
export async function upsertMenu(projectId: string, menuType: MenuType, items: MenuItem[]): Promise<ActionResult>
```

From src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts (referans — clusterAndScoreKeywords UUID guard ve Promise.all pattern'i):
```typescript
// UUID validation (07-01 de aynı pattern'i kullanır):
if (!projectId || !/^[0-9a-f-]{36}$/i.test(projectId)) {
  return { success: false, error: 'Geçersiz proje ID.' }
}
```

Pages table schema (supabase/migrations/20260422000001_create_tables.sql satır 145–160):
- id UUID PK, user_id UUID NOT NULL, project_id UUID NOT NULL
- parent_id UUID NULL, cluster_id UUID NULL, focus_keyword_id UUID NULL
- title TEXT NOT NULL, slug TEXT NOT NULL, page_type TEXT NOT NULL DEFAULT 'page'
- search_intent TEXT NULL, priority TEXT NOT NULL DEFAULT 'medium', sort_order INTEGER NOT NULL DEFAULT 0

NOT: Mevcut `addPage` uygulaması `priority` için Türkçe 'orta' kullanır (default değil); generatePagesFromClusters da Türkçe `priority: 'orta'` kullanmalı — tutarlılık için.

keyword_clusters schema (satır 120–133):
- id, user_id, project_id, cluster_name, total_volume, primary_keyword_id, intent
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: slugify yardımcı fonksiyonu + vitest testleri</name>
  <files>src/lib/pages/slugify.ts, src/lib/pages/slugify.test.ts</files>

  <read_first>
    - src/lib/pages/slugify.ts (mevcut değil — yeni dosya)
    - src/lib/keywords/scoring.ts (analog TypeScript pure-function stili; export pattern'ini kopyala)
    - src/lib/keywords/scoring.test.ts (analog vitest dosya layout'u)
    - .planning/phases/07-site-blueprint-tree/07-CONTEXT.md (D-06 slug kuralları)
  </read_first>

  <behavior>
    - Test 1: `slugify('Conservatory Blinds')` → `'conservatory-blinds'` (lowercase + boşluk→hyphen)
    - Test 2: `slugify('Çatı Şemsiyesi Örtüleri')` → `'cati-semsiyesi-ortuleri'` (ç→c, ş→s, ö→o)
    - Test 3: `slugify('Ürün İğne Ğüzel')` → `'urun-igne-guzel'` (ü→u, ı→i, İ→i, ğ→g, Ğ→g)
    - Test 4: `slugify('Hello! World@2024 #test')` → `'hello-world2024-test'` (özel karakterler silinir; tireler korunur; ardışık tireler tek'e iner)
    - Test 5: `slugify('a'.repeat(80))` → 60 karakterden uzun olamaz (`.length === 60`)
    - Test 6: `slugify('test', ['test'])` → `'test-2'` (duplicate suffix)
    - Test 7: `slugify('test', ['test', 'test-2'])` → `'test-3'`
    - Test 8: `slugify('test', ['test', 'test-2', 'test-3'])` → `'test-4'`
    - Test 9: Leading/trailing hyphen trim — `slugify('  -hello-  ')` → `'hello'`
    - Test 10: Boş/whitespace-only input → `'page'` fallback (DB NOT NULL slug constraint'i için)
  </behavior>

  <action>
    1) `src/lib/pages/slugify.ts` dosyasını oluştur:

    ```typescript
    // D-06 slug üretim kuralları: lowercase, Türkçe normalize, max 60, -2/-3 suffix
    const TR_MAP: Record<string, string> = {
      'ç': 'c', 'Ç': 'c',
      'ş': 's', 'Ş': 's',
      'ğ': 'g', 'Ğ': 'g',
      'ü': 'u', 'Ü': 'u',
      'ö': 'o', 'Ö': 'o',
      'ı': 'i', 'İ': 'i',
    }

    const MAX_SLUG_LENGTH = 60
    const FALLBACK_SLUG = 'page'

    function normalize(input: string): string {
      let out = ''
      for (const ch of input) {
        out += TR_MAP[ch] ?? ch
      }
      return out
        .toLowerCase()
        .replace(/[^a-z0-9\s-]/g, '')   // özel karakterleri sil (harfler, rakamlar, boşluk, tire kalır)
        .replace(/\s+/g, '-')            // boşlukları tireye çevir
        .replace(/-+/g, '-')             // ardışık tireleri tek'e indir
        .replace(/^-+|-+$/g, '')         // baş/son tirelerini kırp
        .slice(0, MAX_SLUG_LENGTH)
        .replace(/-+$/g, '')             // trim'den sonra oluşan sondaki tireyi kırp (60. karakter denk gelirse)
    }

    /**
     * D-06 kurallarıyla slug üretir. existingSlugs içinde çakışma varsa -2, -3 ... ekler.
     * Boş input fallback olarak 'page' döner (pages.slug NOT NULL).
     */
    export function slugify(text: string, existingSlugs: string[] = []): string {
      let base = normalize(text)
      if (!base) base = FALLBACK_SLUG

      const used = new Set(existingSlugs)
      if (!used.has(base)) return base

      let n = 2
      while (used.has(`${base}-${n}`)) n++
      return `${base}-${n}`
    }
    ```

    2) `src/lib/pages/slugify.test.ts` dosyasını oluştur — 10 test case'ini (Test 1–10 above) doğrudan çevir:

    ```typescript
    import { describe, it, expect } from 'vitest'
    import { slugify } from './slugify'

    describe('slugify', () => {
      it('lowercases and replaces spaces with hyphens', () => {
        expect(slugify('Conservatory Blinds')).toBe('conservatory-blinds')
      })
      it('normalizes Turkish chars ç ş ö', () => {
        expect(slugify('Çatı Şemsiyesi Örtüleri')).toBe('cati-semsiyesi-ortuleri')
      })
      it('normalizes ü ı İ ğ', () => {
        expect(slugify('Ürün İğne Ğüzel')).toBe('urun-igne-guzel')
      })
      it('strips special chars and collapses hyphens', () => {
        expect(slugify('Hello! World@2024 #test')).toBe('hello-world2024-test')
      })
      it('enforces 60-char max length', () => {
        expect(slugify('a'.repeat(80)).length).toBe(60)
      })
      it('appends -2 when slug taken', () => {
        expect(slugify('test', ['test'])).toBe('test-2')
      })
      it('appends -3 when -2 also taken', () => {
        expect(slugify('test', ['test', 'test-2'])).toBe('test-3')
      })
      it('appends -4 when -3 also taken', () => {
        expect(slugify('test', ['test', 'test-2', 'test-3'])).toBe('test-4')
      })
      it('trims leading/trailing hyphens', () => {
        expect(slugify('  -hello-  ')).toBe('hello')
      })
      it('falls back to "page" when input empty after normalize', () => {
        expect(slugify('   ')).toBe('page')
        expect(slugify('!!!')).toBe('page')
      })
    })
    ```
  </action>

  <verify>
    <automated>npx vitest run src/lib/pages/slugify.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `src/lib/pages/slugify.ts` dosyası mevcut
    - `src/lib/pages/slugify.test.ts` dosyası mevcut
    - `slugify.ts` içinde `export function slugify` bulunur (grep: `^export function slugify`)
    - `slugify.ts` içinde `TR_MAP` sabiti tüm 6 harf çiftini içerir (grep: `'ç': 'c'` ve `'ş': 's'` ve `'ğ': 'g'` ve `'ü': 'u'` ve `'ö': 'o'` ve `'ı': 'i'`)
    - `slugify.ts` içinde `MAX_SLUG_LENGTH = 60` literal değeri bulunur
    - `slugify.test.ts` en az 10 `it(` bloğu içerir (grep count)
    - `npx vitest run src/lib/pages/slugify.test.ts` komutu 10/10 test PASS döndürür
  </acceptance_criteria>

  <done>
    - slugify fonksiyonu D-06 kurallarını tam karşılar
    - 10/10 vitest case geçer
    - `npx tsc --noEmit` sadece pre-existing hataları gösterir (yeni dosyalardan hata yok — PLAN 06-01/06-02 SUMMARY'de belgelenmiş kapsam)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generatePagesFromClusters + reorderPage + INTENT_TO_PAGE_TYPE Server Actions</name>
  <files>src/app/(dashboard)/projeler/[id]/site-blueprint/actions.ts</files>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/site-blueprint/actions.ts (mevcut dosyaya EKLENECEK — addPage/deletePage/upsertMenu/verifyProjectOwnership dokunulmaz)
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts (satır 186–288: clusterAndScoreKeywords pattern — UUID regex, auth, Promise.all update)
    - src/lib/pages/slugify.ts (Task 1'de oluşturuldu)
    - .planning/phases/07-site-blueprint-tree/07-PATTERNS.md (generatePagesFromClusters ve reorderPage adaptation blokları)
    - .planning/phases/07-site-blueprint-tree/07-CONTEXT.md (D-01, D-02, D-06 kuralları)
    - supabase/migrations/20260422000001_create_tables.sql (pages satır 145–160; keyword_clusters satır 120–133)
  </read_first>

  <behavior>
    - Test 1: `generatePagesFromClusters('bad-uuid', [])` → `{ success: false, error: 'Geçersiz proje ID.' }`
    - Test 2: Auth yoksa → `{ success: false, error: 'Oturum bulunamadı.' }`
    - Test 3: Project ownership yok → `{ success: false, error: 'Proje bulunamadı.' }`
    - Test 4: Boş rows → `{ success: true, created: 0, skipped: 0 }`
    - Test 5: `reorderPage('bad-uuid', 'up', projectId)` → `{ success: false, error: 'Geçersiz sayfa ID.' }`
    - Test 6: `reorderPage(pageId, 'invalid', projectId)` → `{ success: false, error: 'Geçersiz yön.' }`
    - (Entegrasyon: Test 2-3 unit test boyutunda değildir — manuel doğrulama yeterlidir; bu task'ta unit test yazımı zorunlu değildir; UUID/yön guard'ları içinde açık if-early-return olmalıdır.)
  </behavior>

  <action>
    Mevcut `site-blueprint/actions.ts` dosyasına (satır 139'dan sonra) aşağıdaki bloğu EKLE. Mevcut export'lar (`addPage`, `deletePage`, `upsertMenu`, `verifyProjectOwnership`, `ActionResult`, `Priority`, `MenuType`, `MenuItem`, `AddPageInput`) değiştirilmez.

    1) Dosyanın en üstündeki import'a `slugify` ekle:
    ```typescript
    import { slugify } from '@/lib/pages/slugify'
    ```

    2) Dosyanın sonuna ekle:

    ```typescript
    // ─── Phase 7: Site Blueprint Generation ────────────────────────────────────

    /**
     * D-02 Intent → page_type haritası.
     * Unknown/null/eşleşmeyen değerler 'blog' döner.
     */
    export const INTENT_TO_PAGE_TYPE: Record<string, string> = {
      transactional: 'hizmet',
      commercial: 'hizmet',
      informational: 'blog',
      navigational: 'ana-sayfa',
    }

    export function intentToPageType(intent: string | null | undefined): string {
      if (!intent) return 'blog'
      const key = intent.toLowerCase().trim()
      return INTENT_TO_PAGE_TYPE[key] ?? 'blog'
    }

    export type GenerateRowInput = {
      clusterId: string
      pageName: string      // kullanıcının dialog'ta düzenlediği başlık (trim)
      pageType: string      // hizmet | blog | ana-sayfa | kategori | urun | landing ...
      focusKeywordId: string | null  // cluster'ın primary_keyword_id (null olabilir)
    }

    export type GenerateResult =
      | { success: true; created: number; skipped: number }
      | { success: false; error: string }

    /**
     * BLUE-01 + BLUE-02 + D-01: kümelerden toplu sayfa üretir.
     * Duplicate guard: aynı cluster_id için mevcut sayfa varsa skip.
     * Slug D-06 kurallarıyla üretilir (Türkçe normalize + -2/-3 suffix).
     */
    export async function generatePagesFromClusters(
      projectId: string,
      rows: GenerateRowInput[]
    ): Promise<GenerateResult> {
      // UUID guard
      if (!projectId || !/^[0-9a-f-]{36}$/i.test(projectId)) {
        return { success: false, error: 'Geçersiz proje ID.' }
      }
      if (!Array.isArray(rows)) {
        return { success: false, error: 'Geçersiz satır listesi.' }
      }
      if (rows.length === 0) {
        return { success: true, created: 0, skipped: 0 }
      }
      // Her cluster_id UUID doğrula (T-07-03 tampering önlemi)
      for (const r of rows) {
        if (!r.clusterId || !/^[0-9a-f-]{36}$/i.test(r.clusterId)) {
          return { success: false, error: 'Geçersiz küme ID.' }
        }
        if (r.focusKeywordId && !/^[0-9a-f-]{36}$/i.test(r.focusKeywordId)) {
          return { success: false, error: 'Geçersiz focus keyword ID.' }
        }
        if (!r.pageName || !r.pageName.trim()) {
          return { success: false, error: 'Sayfa adı boş olamaz.' }
        }
      }
      // DoS guard
      if (rows.length > 500) {
        return { success: false, error: `Tek seferde 500'den fazla sayfa oluşturulamaz (${rows.length}).` }
      }

      const supabase = await createClient()
      const {
        data: { user },
      } = await supabase.auth.getUser()
      if (!user) return { success: false, error: 'Oturum bulunamadı.' }

      const isOwner = await verifyProjectOwnership(supabase, projectId, user.id)
      if (!isOwner) return { success: false, error: 'Proje bulunamadı.' }

      // Cluster ownership doğrula — rows'daki tüm clusterId'ler bu proje altında mı?
      const clusterIds = rows.map((r) => r.clusterId)
      const { data: ownedClusters } = await supabase
        .from('keyword_clusters')
        .select('id')
        .eq('project_id', projectId)
        .eq('user_id', user.id)
        .in('id', clusterIds)

      const ownedClusterSet = new Set((ownedClusters ?? []).map((c) => c.id))
      if (ownedClusterSet.size !== clusterIds.length) {
        return { success: false, error: 'Bazı kümeler projeye ait değil.' }
      }

      // Duplicate guard: bu projede cluster_id'si eşleşen mevcut sayfaları çek
      const { data: existingPages } = await supabase
        .from('pages')
        .select('id, cluster_id, slug, sort_order')
        .eq('project_id', projectId)
        .eq('user_id', user.id)

      const takenClusterIds = new Set(
        (existingPages ?? [])
          .filter((p: { cluster_id: string | null }) => p.cluster_id !== null)
          .map((p: { cluster_id: string | null }) => p.cluster_id as string)
      )
      const existingSlugs = new Set(
        (existingPages ?? []).map((p: { slug: string }) => p.slug)
      )

      let nextSortOrder =
        ((existingPages ?? []).reduce(
          (max: number, p: { sort_order: number }) => Math.max(max, p.sort_order),
          0
        )) + 1

      // Insert payload'ları hazırla — skip'leri tespit et
      let skipped = 0
      const payloads: Array<Record<string, unknown>> = []

      for (const r of rows) {
        if (takenClusterIds.has(r.clusterId)) {
          skipped++
          continue
        }
        const slug = slugify(r.pageName.trim(), Array.from(existingSlugs))
        existingSlugs.add(slug)  // bu batch içinde de duplicate'i önle

        payloads.push({
          user_id: user.id,
          project_id: projectId,
          parent_id: null,
          cluster_id: r.clusterId,
          focus_keyword_id: r.focusKeywordId,
          title: r.pageName.trim(),
          slug,
          page_type: r.pageType || 'blog',
          priority: 'orta', // mevcut addPage ile tutarlı (Türkçe değer)
          sort_order: nextSortOrder++,
        })
      }

      let created = 0
      if (payloads.length > 0) {
        const { error, data: inserted } = await supabase
          .from('pages')
          .insert(payloads)
          .select('id')
        if (error) {
          return { success: false, error: 'Sayfalar oluşturulurken hata oluştu.' }
        }
        created = (inserted ?? []).length
      }

      revalidatePath(`/projeler/${projectId}/site-blueprint`)
      return { success: true, created, skipped }
    }

    // ─── Phase 7: Reorder Page ─────────────────────────────────────────────────

    /**
     * BLUE-03 + D-04: aynı parent altındaki bitişik kardeşle sort_order takası yapar.
     * 'up'   → mevcut sort_order'dan küçük, en yakın kardeş
     * 'down' → mevcut sort_order'dan büyük, en yakın kardeş
     */
    export async function reorderPage(
      pageId: string,
      direction: 'up' | 'down',
      projectId: string
    ): Promise<ActionResult> {
      // UUID guards
      if (!pageId || !/^[0-9a-f-]{36}$/i.test(pageId)) {
        return { success: false, error: 'Geçersiz sayfa ID.' }
      }
      if (!projectId || !/^[0-9a-f-]{36}$/i.test(projectId)) {
        return { success: false, error: 'Geçersiz proje ID.' }
      }
      if (direction !== 'up' && direction !== 'down') {
        return { success: false, error: 'Geçersiz yön.' }
      }

      const supabase = await createClient()
      const {
        data: { user },
      } = await supabase.auth.getUser()
      if (!user) return { success: false, error: 'Oturum bulunamadı.' }

      const isOwner = await verifyProjectOwnership(supabase, projectId, user.id)
      if (!isOwner) return { success: false, error: 'Proje bulunamadı.' }

      // Hedef sayfayı çek
      const { data: targetPage } = await supabase
        .from('pages')
        .select('id, parent_id, sort_order')
        .eq('id', pageId)
        .eq('project_id', projectId)
        .eq('user_id', user.id)
        .single()

      if (!targetPage) return { success: false, error: 'Sayfa bulunamadı.' }

      // Kardeşleri çek — aynı parent_id, aynı project_id
      let siblingsQuery = supabase
        .from('pages')
        .select('id, sort_order')
        .eq('project_id', projectId)
        .eq('user_id', user.id)
        .order('sort_order', { ascending: true })

      siblingsQuery =
        targetPage.parent_id === null
          ? siblingsQuery.is('parent_id', null)
          : siblingsQuery.eq('parent_id', targetPage.parent_id)

      const { data: siblings } = await siblingsQuery
      const siblingList = siblings ?? []

      const idx = siblingList.findIndex((s: { id: string }) => s.id === pageId)
      if (idx === -1) return { success: false, error: 'Sayfa kardeşler arasında bulunamadı.' }

      const neighborIdx = direction === 'up' ? idx - 1 : idx + 1
      if (neighborIdx < 0 || neighborIdx >= siblingList.length) {
        // Kenar durum: ilk/son — no-op (başarı dön ki UI hata göstermesin)
        return { success: true }
      }

      const current = siblingList[idx]
      const neighbor = siblingList[neighborIdx]

      // sort_order takası — iki UPDATE, Promise.all
      const [r1, r2] = await Promise.all([
        supabase
          .from('pages')
          .update({ sort_order: neighbor.sort_order })
          .eq('id', current.id)
          .eq('user_id', user.id),
        supabase
          .from('pages')
          .update({ sort_order: current.sort_order })
          .eq('id', neighbor.id)
          .eq('user_id', user.id),
      ])

      if (r1.error || r2.error) {
        return { success: false, error: 'Sıralama güncellenemedi.' }
      }

      revalidatePath(`/projeler/${projectId}/site-blueprint`)
      return { success: true }
    }
    ```

    3) **Kritik:** Mevcut `addPage`, `deletePage`, `upsertMenu`, `verifyProjectOwnership` fonksiyonları ve `ActionResult`/`Priority`/`MenuType`/`MenuItem`/`AddPageInput` tip export'ları aynen korunur. Bu task sadece EKLER.

    4) TypeScript doğrulaması:
    ```bash
    npx tsc --noEmit
    ```
    Yeni kod bloğundan yeni hata çıkmamalı. Pre-existing hatalar (KeywordImport, ProjectNav, SeoFetchButton, CompetitorDeleteButton eksiklikleri — SUMMARY 06-01 ve 06-02'de belgelenmiş) kapsam dışı.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(site-blueprint/actions\.ts|lib/pages/slugify)" | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `site-blueprint/actions.ts` içinde `export async function generatePagesFromClusters` bulunur
    - `site-blueprint/actions.ts` içinde `export async function reorderPage` bulunur
    - `site-blueprint/actions.ts` içinde `export const INTENT_TO_PAGE_TYPE` bulunur
    - `site-blueprint/actions.ts` içinde `export function intentToPageType` bulunur
    - `site-blueprint/actions.ts` içinde `export type GenerateRowInput` bulunur
    - `site-blueprint/actions.ts` içinde `export type GenerateResult` bulunur
    - `site-blueprint/actions.ts` içinde `import { slugify } from '@/lib/pages/slugify'` satırı bulunur
    - `site-blueprint/actions.ts` içinde `export async function addPage` hâlâ bulunur (silinmemiş)
    - `site-blueprint/actions.ts` içinde `export async function deletePage` hâlâ bulunur
    - `site-blueprint/actions.ts` içinde `export async function upsertMenu` hâlâ bulunur
    - `generatePagesFromClusters` içinde `transactional: 'hizmet'`, `commercial: 'hizmet'`, `informational: 'blog'`, `navigational: 'ana-sayfa'` literal string'leri bulunur (D-02)
    - `reorderPage` içinde `Promise.all([` pattern'i bulunur (iki UPDATE paralel)
    - `generatePagesFromClusters` içinde `takenClusterIds` veya `cluster_id` duplicate guard pattern'i bulunur
    - `generatePagesFromClusters` içinde `priority: 'orta'` literal bulunur (mevcut addPage ile tutarlı)
    - `npx tsc --noEmit` çıktısında `site-blueprint/actions.ts` veya `lib/pages/slugify` için yeni hata YOK (grep çıktısında 0 satır)
  </acceptance_criteria>

  <done>
    - İki yeni Server Action + slugify import + INTENT_TO_PAGE_TYPE sabit haritası actions.ts'e eklendi
    - Mevcut addPage/deletePage/upsertMenu/verifyProjectOwnership hiç dokunulmadan korundu
    - UUID + ownership + duplicate guard'ları uygulandı (T-07-01, T-07-02, T-07-03)
    - Yeni eklenen dosyalar TypeScript compile hatası üretmiyor
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action (generatePagesFromClusters) | Client dialog'undan gelen rows[] — clusterId/focusKeywordId/pageName dahil untrusted input |
| client → Server Action (reorderPage) | Client inline form'dan gelen pageId + direction — UUID + enum tampering riski |
| Server Action → Supabase (pages INSERT) | user_id ve project_id kendi sorgumuzdan alınır; client'tan doğrudan geçmez |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `generatePagesFromClusters` clusterId/focusKeywordId/projectId | mitigate | UUID regex validation (`/^[0-9a-f-]{36}$/i`) tüm UUID parametrelerinde; ek olarak `keyword_clusters.in('id', clusterIds).eq('user_id', user.id)` ownership double-check — başka kullanıcının cluster'ını projeye bağlayamaz |
| T-07-02 | Information Disclosure / IDOR | `reorderPage` pageId | mitigate | `verifyProjectOwnership(projectId, user.id)` + `.eq('user_id', user.id).eq('project_id', projectId)` — başka kullanıcının sayfasını sıralayamaz |
| T-07-03 | Elevation of Privilege | `generatePagesFromClusters` user_id injection | mitigate | user_id her zaman `supabase.auth.getUser()` sonucundan alınır, client payload'dan değil; RLS `pages_insert_own` WITH CHECK (auth.uid() = user_id) defense-in-depth |
| T-07-04 | Denial of Service | `generatePagesFromClusters` rows.length | mitigate | `rows.length > 500` erken döner (`Tek seferde 500'den fazla sayfa oluşturulamaz`) — clusterAndScoreKeywords T-06-03 ile aynı sınır |
| T-07-05 | Tampering | `reorderPage` direction enum | mitigate | `direction !== 'up' && direction !== 'down'` early return; SQL injection yok çünkü string literal olarak kullanılmıyor |
| T-07-06 | Tampering | slug injection (path/URL traversal karakterleri) | mitigate | `slugify` sadece `[a-z0-9\s-]` karakterlere indirger; `..`, `/`, `\` vb. otomatik strip edilir |
| T-07-07 | Repudiation | page generation auditing | accept | Audits tablosu MVP'de page CRUD için zorunlu değil; küme başına bir INSERT izi DB'de yeterli (Phase 8'de yeniden değerlendirilir) |
</threat_model>

<verification>
## Task-level

- Task 1 (slugify): `npx vitest run src/lib/pages/slugify.test.ts` → 10/10 PASS
- Task 2 (actions): `npx tsc --noEmit` → site-blueprint/actions.ts ve slugify için yeni hata YOK

## Plan-level

- Dosya kontrol: `src/lib/pages/slugify.ts`, `src/lib/pages/slugify.test.ts`, `src/app/(dashboard)/projeler/[id]/site-blueprint/actions.ts` üçü de mevcut
- `grep "export async function generatePagesFromClusters" src/app/(dashboard)/projeler/[id]/site-blueprint/actions.ts` → 1 eşleşme
- `grep "export async function reorderPage" src/app/(dashboard)/projeler/[id]/site-blueprint/actions.ts` → 1 eşleşme
- `grep "export async function addPage" src/app/(dashboard)/projeler/[id]/site-blueprint/actions.ts` → 1 eşleşme (korundu)
- `grep "export const INTENT_TO_PAGE_TYPE" src/app/(dashboard)/projeler/[id]/site-blueprint/actions.ts` → 1 eşleşme
</verification>

<success_criteria>
- slugify Türkçe karakterleri (ç,ş,ğ,ü,ö,ı) ASCII'ye dönüştürür ve 10/10 vitest PASS
- slugify aynı proje içindeki duplicate slug için -2, -3 suffix ekler
- `generatePagesFromClusters(projectId, rows)` valid input ile `{ success: true, created, skipped }` döner
- `generatePagesFromClusters` mevcut `cluster_id` eşleşmelerini skip eder, sayıyı `skipped` olarak döner
- `generatePagesFromClusters` cluster.intent'ten D-02 tablosu ile page_type türetir (UI'dan override gelirse UI değerini kullanır)
- `reorderPage(pageId, 'up', projectId)` aynı parent kardeşiyle sort_order takası yapar
- `reorderPage` ilk/son kardeşte no-op `{ success: true }` döner (UI disabled prop'u bu eski yaklaşım için gerekli)
- Mevcut addPage/deletePage/upsertMenu/AddPageModal/MenuEditor işlevleri bozulmadı
</success_criteria>

<output>
After completion, create `.planning/phases/07-site-blueprint-tree/07-01-SUMMARY.md` per templates/summary.md.
</output>
