---
phase: 07-site-blueprint-tree
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/app/(dashboard)/projeler/[id]/site-blueprint/GeneratePagesDialog.tsx
  - src/app/(dashboard)/projeler/[id]/site-blueprint/GenerateFromClustersButton.tsx
  - src/app/(dashboard)/projeler/[id]/site-blueprint/ReorderButton.tsx
  - src/app/(dashboard)/projeler/[id]/site-blueprint/KeywordMappingTab.tsx
  - src/app/(dashboard)/projeler/[id]/site-blueprint/page.tsx
autonomous: true
requirements: [BLUE-01, BLUE-02, BLUE-03]
tags: [site-blueprint, ui, client-components, dialog, tab-switcher, reorder]

must_haves:
  truths:
    - "Kullanıcı site-blueprint sayfasında 'Kümelerden Oluştur' butonu görür; 0 küme varsa buton disabled ve tooltip 'Önce keyword kümeleme yapın' gösterir"
    - "Buton tıklanınca 5 sütunlu preview tablolu dialog açılır: Küme Adı, Sayfa Adı (editable), Tip (select), Odak Keyword, Dahil Et (checkbox)"
    - "Cluster_id zaten bir sayfaya bağlıysa satırda 'Zaten var' amber badge gösterilir ve checkbox pre-checked=false gelir"
    - "Dialog 'Oluştur' butonu sadece include=true satırları generatePagesFromClusters'a gönderir ve başarı sonrası kapanır"
    - "Sayfa iki sekme gösterir: 'Ağaç Görünümü' (default, ?tab=tree) ve 'Keyword Eşleme' (?tab=mapping)"
    - "Ağaç Görünümü tab'ında her satır ↑ ↓ reorder butonları gösterir; ilk satırda ↑ disabled, son satırda ↓ disabled"
    - "↑ veya ↓ butonuna tıklayınca reorderPage çağrılır ve tablo yeni sıra ile yeniden render edilir"
    - "Aynı keyword text'i iki farklı cluster-sayfa'ya bağlıysa her iki sayfada da '⚠ Çakışma' kırmızı badge gösterilir"
    - "Keyword Eşleme tab'ı 'Eşleşen Keyword'ler' 4-sütun tablosu + 'Eşleşmeyenler' amber warning bloğu gösterir"
    - "Eşleşmeyenler listesi cluster_id IS NULL olan keyword'leri içerir"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/site-blueprint/GenerateFromClustersButton.tsx"
      provides: "Dialog açan button + disabled state + tooltip"
      exports: ["GenerateFromClustersButton"]
    - path: "src/app/(dashboard)/projeler/[id]/site-blueprint/GeneratePagesDialog.tsx"
      provides: "5-sütun preview tablolu dialog + generatePagesFromClusters çağrısı"
      exports: ["GeneratePagesDialog"]
    - path: "src/app/(dashboard)/projeler/[id]/site-blueprint/ReorderButton.tsx"
      provides: "↑↓ tek yönlü buton — reorderPage çağrısı + disabled prop"
      exports: ["ReorderButton"]
    - path: "src/app/(dashboard)/projeler/[id]/site-blueprint/KeywordMappingTab.tsx"
      provides: "Eşleşen + Eşleşmeyenler iki bölümlü keyword eşleme görünümü"
      exports: ["KeywordMappingTab"]
    - path: "src/app/(dashboard)/projeler/[id]/site-blueprint/page.tsx"
      provides: "searchParams tab routing + conflict query + header action entegrasyonu + reorder kolonu"
      contains: "tab === 'mapping'"
  key_links:
    - from: "page.tsx"
      to: "GenerateFromClustersButton"
      via: "import ve render (header action bar)"
      pattern: "<GenerateFromClustersButton"
    - from: "GenerateFromClustersButton"
      to: "GeneratePagesDialog"
      via: "Dialog open state — button dialog'u açar"
      pattern: "GeneratePagesDialog"
    - from: "GeneratePagesDialog"
      to: "generatePagesFromClusters"
      via: "import '@/app/(dashboard)/projeler/[id]/site-blueprint/actions' + startTransition"
      pattern: "generatePagesFromClusters"
    - from: "page.tsx"
      to: "ReorderButton"
      via: "tree table satırı reorder kolonunda render"
      pattern: "<ReorderButton"
    - from: "ReorderButton"
      to: "reorderPage"
      via: "startTransition(async) + reorderPage(pageId, direction, projectId)"
      pattern: "reorderPage\\("
    - from: "page.tsx"
      to: "KeywordMappingTab"
      via: "tab === 'mapping' koşullu render"
      pattern: "KeywordMappingTab"
    - from: "page.tsx"
      to: "conflictPageIds Set"
      via: "SSR query — aynı keyword.keyword text'i birden fazla cluster-sayfa için tekrarlıyor mu?"
      pattern: "conflictPageIds"
---

<objective>
Phase 7'nin kullanıcıya dokunan yüzeyini tamamla — site-blueprint sayfasına tab switcher, 'Kümelerden Oluştur' dialog akışı, tree table'a reorder okları, conflict badge'leri ve yeni 'Keyword Eşleme' tab'ı ekle.

Purpose: BLUE-01 (cluster→sayfa akışı kullanıcıya görünür), BLUE-02 (keyword-to-page eşlemesi + conflict detection UI) ve BLUE-03 (yeniden sıralama UI) gereksinimlerinin kullanıcı görünür yüzeyini inşa etmek. 07-01 Server Action'larını gerçek data ile tüketir.

Output: 4 yeni Client Component + site-blueprint/page.tsx'in searchParams tab routing, reorder kolonu, conflict query ve header action entegrasyonu.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-site-blueprint-tree/07-CONTEXT.md
@.planning/phases/07-site-blueprint-tree/07-PATTERNS.md
@.planning/phases/07-site-blueprint-tree/07-UI-SPEC.md
@.planning/phases/07-site-blueprint-tree/07-01-PLAN.md
@src/app/(dashboard)/projeler/[id]/site-blueprint/page.tsx
@src/app/(dashboard)/projeler/[id]/site-blueprint/AddPageModal.tsx
@src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterButton.tsx
@src/app/(dashboard)/projeler/[id]/keyword-stratejisi/MoveKeywordDialog.tsx
@src/app/(dashboard)/projeler/[id]/keyword-stratejisi/IntentBadge.tsx

<interfaces>
<!-- 07-01 tarafından sağlanan Server Action sözleşmeleri (bu plan bunları çağırır, değiştirmez) -->

From src/app/(dashboard)/projeler/[id]/site-blueprint/actions.ts (07-01'de eklendi):
```typescript
export const INTENT_TO_PAGE_TYPE: Record<string, string> = {
  transactional: 'hizmet',
  commercial: 'hizmet',
  informational: 'blog',
  navigational: 'ana-sayfa',
}

export function intentToPageType(intent: string | null | undefined): string

export type GenerateRowInput = {
  clusterId: string
  pageName: string
  pageType: string
  focusKeywordId: string | null
}

export type GenerateResult =
  | { success: true; created: number; skipped: number }
  | { success: false; error: string }

export async function generatePagesFromClusters(
  projectId: string,
  rows: GenerateRowInput[]
): Promise<GenerateResult>

export async function reorderPage(
  pageId: string,
  direction: 'up' | 'down',
  projectId: string
): Promise<ActionResult>
```

Existing (dokunulmaz):
```typescript
export type ActionResult = { success: true } | { success: false; error: string }
export async function addPage(projectId: string, input: AddPageInput): Promise<ActionResult>
export async function deletePage(projectId: string, pageId: string): Promise<ActionResult>
```

Reused components:
```typescript
// src/app/(dashboard)/projeler/[id]/keyword-stratejisi/IntentBadge.tsx
export function IntentBadge({ intent }: { intent: string | null }): JSX.Element
```

Design contract (UI-SPEC must-reads):
- Dialog: `max-w-2xl`, DialogTrigger render prop pattern (asChild yasak)
- "Zaten var" badge: `ml-2 text-xs px-1.5 py-0.5 rounded bg-amber-500/20 text-amber-400`
- "⚠ Çakışma" badge: `ml-2 text-xs px-1.5 py-0.5 rounded bg-red-500/20 text-red-400 shrink-0`
- Reorder button: `h-7 w-7 p-0 text-muted-foreground hover:text-foreground` ghost variant
- Tab active: `bg-secondary text-foreground font-semibold`
- Tab inactive: `text-muted-foreground font-normal hover:text-foreground`
- Font weight: sadece `font-normal` ve `font-semibold` — `font-medium` yasak
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GenerateFromClustersButton + GeneratePagesDialog Client Components</name>
  <files>src/app/(dashboard)/projeler/[id]/site-blueprint/GenerateFromClustersButton.tsx, src/app/(dashboard)/projeler/[id]/site-blueprint/GeneratePagesDialog.tsx</files>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/site-blueprint/actions.ts (07-01'de oluşturulan generatePagesFromClusters, INTENT_TO_PAGE_TYPE, GenerateRowInput, GenerateResult)
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterButton.tsx (useTransition + loading spinner pattern — tam dosya)
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/MoveKeywordDialog.tsx (DialogTrigger render prop + Dialog open state + handleOpenChange reset pattern — tam dosya)
    - src/app/(dashboard)/projeler/[id]/site-blueprint/AddPageModal.tsx (native select ve error inline pattern — tam dosya)
    - .planning/phases/07-site-blueprint-tree/07-UI-SPEC.md (section 1 + section 3 — button copy ve dialog table layout)
  </read_first>

  <behavior>
    - GenerateFromClustersButton: `hasEnrichedClusters=false` → disabled prop + wrapper span `title="Önce keyword kümeleme yapın"`
    - GenerateFromClustersButton: tıklayınca dialog açılır; dialog kendi `useState(open)` ile yönetilir (bu component sadece trigger sarmalayıcısıdır)
    - GeneratePagesDialog: props olarak projectId + rows[] alır; rows her biri `{ clusterId, clusterName, proposedName, proposedType, focusKeyword, focusKeywordId, alreadyExists }` içerir
    - Her satır için state: `{ pageName: string (editable), pageType: string (select), include: boolean (checkbox) }`
    - `alreadyExists=true` satırlarında checkbox pre-checked=false, input disabled opacity-50, satır arka planı bg-amber-500/10, sayfa adı cell'inde "Zaten var" amber badge
    - "Oluştur" butonu tıklayınca `include=true` satırların array'i `generatePagesFromClusters` Server Action'a gönderilir, success sonrası dialog kapanır, error varsa inline gösterilir
    - Dialog kapanınca form state reset (handleOpenChange pattern)
    - Pending durumunda tüm inputlar disabled + "Oluşturuluyor…" button text
  </behavior>

  <action>
    **A) `src/app/(dashboard)/projeler/[id]/site-blueprint/GenerateFromClustersButton.tsx` oluştur:**

    ```tsx
    'use client'

    import { useState } from 'react'
    import { Button } from '@/components/ui/button'
    import { GeneratePagesDialog, type DialogRow } from './GeneratePagesDialog'

    type Props = {
      projectId: string
      rows: DialogRow[]
      hasEnrichedClusters: boolean
    }

    export function GenerateFromClustersButton({ projectId, rows, hasEnrichedClusters }: Props) {
      const [open, setOpen] = useState(false)

      if (!hasEnrichedClusters) {
        return (
          <span title="Önce keyword kümeleme yapın" className="inline-flex">
            <Button variant="outline" size="sm" disabled>
              Kümelerden Oluştur
            </Button>
          </span>
        )
      }

      return (
        <GeneratePagesDialog
          projectId={projectId}
          rows={rows}
          open={open}
          onOpenChange={setOpen}
          trigger={
            <Button variant="outline" size="sm" onClick={() => setOpen(true)}>
              Kümelerden Oluştur
            </Button>
          }
        />
      )
    }
    ```

    **B) `src/app/(dashboard)/projeler/[id]/site-blueprint/GeneratePagesDialog.tsx` oluştur:**

    ```tsx
    'use client'

    import { useMemo, useState, useTransition, type ReactNode } from 'react'
    import { Button } from '@/components/ui/button'
    import {
      Dialog,
      DialogContent,
      DialogHeader,
      DialogTitle,
      DialogFooter,
    } from '@/components/ui/dialog'
    import { generatePagesFromClusters, type GenerateRowInput } from './actions'

    // PAGE_TYPE_LABELS — sayfalar/page.tsx'ten kopya (import etmiyoruz; yerel sabit)
    const PAGE_TYPE_LABELS: Record<string, string> = {
      'ana-sayfa': 'Ana Sayfa',
      kategori: 'Kategori Sayfası',
      hizmet: 'Hizmet Sayfası',
      urun: 'Ürün Sayfası',
      blog: 'Blog Yazısı',
      landing: 'Landing Page',
      hakkimizda: 'Hakkımızda',
      iletisim: 'İletişim',
      sss: 'SSS Sayfası',
      fiyatlandirma: 'Fiyatlandırma',
    }

    export type DialogRow = {
      clusterId: string
      clusterName: string
      proposedName: string       // strip intent suffix edilmiş cluster adı
      proposedType: string        // intent → page_type (D-02)
      focusKeyword: string | null
      focusKeywordId: string | null
      alreadyExists: boolean
    }

    type RowState = {
      pageName: string
      pageType: string
      include: boolean
    }

    export function GeneratePagesDialog({
      projectId,
      rows,
      open,
      onOpenChange,
      trigger,
    }: {
      projectId: string
      rows: DialogRow[]
      open: boolean
      onOpenChange: (next: boolean) => void
      trigger?: ReactNode
    }) {
      const [isPending, startTransition] = useTransition()
      const [error, setError] = useState<string | null>(null)

      // Her satır için form state — satır index'iyle eşleşir
      const initialState = useMemo<RowState[]>(
        () =>
          rows.map((r) => ({
            pageName: r.proposedName,
            pageType: r.proposedType,
            include: !r.alreadyExists, // alreadyExists → default skip
          })),
        [rows]
      )

      const [state, setState] = useState<RowState[]>(initialState)

      const handleOpenChange = (next: boolean) => {
        if (!next) {
          setState(initialState)
          setError(null)
        }
        onOpenChange(next)
      }

      const updateRow = (idx: number, patch: Partial<RowState>) => {
        setState((prev) => prev.map((r, i) => (i === idx ? { ...r, ...patch } : r)))
      }

      const includedCount = state.filter((r) => r.include).length

      const handleSubmit = () => {
        setError(null)
        const payload: GenerateRowInput[] = []
        for (let i = 0; i < rows.length; i++) {
          if (!state[i].include) continue
          if (!state[i].pageName.trim()) {
            setError(`"${rows[i].clusterName}" için sayfa adı boş olamaz.`)
            return
          }
          payload.push({
            clusterId: rows[i].clusterId,
            pageName: state[i].pageName.trim(),
            pageType: state[i].pageType,
            focusKeywordId: rows[i].focusKeywordId,
          })
        }

        startTransition(async () => {
          const result = await generatePagesFromClusters(projectId, payload)
          if (!result.success) {
            setError(result.error)
            return
          }
          handleOpenChange(false)
        })
      }

      return (
        <Dialog open={open} onOpenChange={handleOpenChange}>
          {trigger}
          <DialogContent className="max-w-2xl">
            <DialogHeader>
              <DialogTitle>Kümelerden Sayfa Oluştur</DialogTitle>
            </DialogHeader>

            {rows.length === 0 ? (
              <p className="text-sm text-muted-foreground py-8 text-center">
                Kümelenmiş keyword bulunamadı. Önce keyword stratejisi sayfasından kümeleme yapın.
              </p>
            ) : (
              <div className="rounded-md border border-border overflow-hidden">
                {/* Başlık satırı */}
                <div className="grid grid-cols-[1fr_1fr_9rem_10rem_3rem] gap-0 bg-muted/50 border-b border-border text-xs text-muted-foreground font-normal">
                  <div className="px-3 py-2">Küme Adı</div>
                  <div className="px-3 py-2">Sayfa Adı</div>
                  <div className="px-3 py-2">Tip</div>
                  <div className="px-3 py-2">Odak Keyword</div>
                  <div className="px-3 py-2 text-center">Dahil Et</div>
                </div>

                <div className="max-h-96 overflow-y-auto">
                  {rows.map((r, idx) => {
                    const rowState = state[idx]
                    const disabled = r.alreadyExists || isPending
                    const rowBg = r.alreadyExists ? 'bg-amber-500/10' : ''
                    return (
                      <div
                        key={r.clusterId}
                        className={`grid grid-cols-[1fr_1fr_9rem_10rem_3rem] gap-0 border-b border-border last:border-0 items-center text-sm ${rowBg}`}
                      >
                        {/* Küme Adı — read-only */}
                        <div className="px-3 py-2 text-sm text-muted-foreground truncate">
                          {r.clusterName}
                        </div>

                        {/* Sayfa Adı — editable input + Zaten var badge */}
                        <div className="px-3 py-2 flex items-center gap-1">
                          <input
                            type="text"
                            value={rowState.pageName}
                            onChange={(e) => updateRow(idx, { pageName: e.target.value })}
                            disabled={disabled}
                            className={`w-full bg-transparent border-b border-border focus:outline-none focus:border-ring text-sm py-0.5 ${
                              disabled ? 'opacity-50 cursor-not-allowed' : ''
                            }`}
                          />
                          {r.alreadyExists && (
                            <span className="ml-2 text-xs px-1.5 py-0.5 rounded bg-amber-500/20 text-amber-400 shrink-0">
                              Zaten var
                            </span>
                          )}
                        </div>

                        {/* Tip — native select */}
                        <div className="px-3 py-2">
                          <select
                            value={rowState.pageType}
                            onChange={(e) => updateRow(idx, { pageType: e.target.value })}
                            disabled={disabled}
                            className="w-full h-9 rounded-md border border-input bg-background px-3 py-1 text-sm shadow-sm focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring"
                          >
                            {Object.entries(PAGE_TYPE_LABELS).map(([value, label]) => (
                              <option key={value} value={value}>
                                {label}
                              </option>
                            ))}
                          </select>
                        </div>

                        {/* Odak Keyword — read-only */}
                        <div className="px-3 py-2 text-xs text-muted-foreground font-mono truncate">
                          {r.focusKeyword ?? '—'}
                        </div>

                        {/* Dahil Et — checkbox */}
                        <div className="px-3 py-2 flex justify-center">
                          <input
                            type="checkbox"
                            checked={rowState.include}
                            onChange={(e) => updateRow(idx, { include: e.target.checked })}
                            disabled={isPending}
                            className="h-4 w-4 accent-primary"
                          />
                        </div>
                      </div>
                    )
                  })}
                </div>
              </div>
            )}

            {error && <p className="text-sm text-destructive">{error}</p>}

            <DialogFooter className="sm:justify-between">
              <span className="text-sm text-muted-foreground self-center">
                {includedCount} sayfa oluşturulacak
              </span>
              <div className="flex gap-2">
                <Button
                  variant="ghost"
                  size="sm"
                  onClick={() => handleOpenChange(false)}
                  disabled={isPending}
                >
                  İptal
                </Button>
                <Button
                  size="sm"
                  onClick={handleSubmit}
                  disabled={isPending || includedCount === 0 || rows.length === 0}
                  className={isPending ? 'opacity-50 cursor-wait' : ''}
                >
                  {isPending ? 'Oluşturuluyor…' : 'Oluştur'}
                </Button>
              </div>
            </DialogFooter>
          </DialogContent>
        </Dialog>
      )
    }
    ```

    **Kritik noktalar:**
    - `'use client'` directive en üstte
    - `DialogTrigger` kullanılmıyor — trigger prop'u parent'tan geçiyor (GenerateFromClustersButton kendi Button'unu render ediyor). Bu pattern MoveKeywordDialog'un hibrid versiyonu — render prop'u kullanmıyoruz çünkü disabled state wrapper span içinde yönetiliyor.
    - Font-weight kuralı: UI-SPEC gereği sadece `font-normal` veya `font-semibold`. Bu komponentlerde `font-medium` KULLANILMAZ.
    - Grid kolonları `grid-cols-[1fr_1fr_9rem_10rem_3rem]` — UI-SPEC Section 3 table genişlik tanımları ile uyumlu.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(GenerateFromClustersButton|GeneratePagesDialog)\.tsx" | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `GenerateFromClustersButton.tsx` ve `GeneratePagesDialog.tsx` dosyaları mevcut
    - `GenerateFromClustersButton.tsx` içinde `'use client'` directive bulunur
    - `GenerateFromClustersButton.tsx` içinde `title="Önce keyword kümeleme yapın"` literal string bulunur (D-05 tooltip)
    - `GenerateFromClustersButton.tsx` içinde `Kümelerden Oluştur` label'ı bulunur
    - `GeneratePagesDialog.tsx` içinde `'use client'` directive bulunur
    - `GeneratePagesDialog.tsx` içinde `import { generatePagesFromClusters` bulunur
    - `GeneratePagesDialog.tsx` içinde `Kümelerden Sayfa Oluştur` DialogTitle literal bulunur
    - `GeneratePagesDialog.tsx` içinde `Zaten var` literal bulunur (D-05 duplicate badge)
    - `GeneratePagesDialog.tsx` içinde `bg-amber-500/10` rowBg className bulunur
    - `GeneratePagesDialog.tsx` içinde `bg-amber-500/20 text-amber-400` badge sınıfı bulunur
    - `GeneratePagesDialog.tsx` içinde `sayfa oluşturulacak` literal bulunur (count summary)
    - `GeneratePagesDialog.tsx` içinde `Oluşturuluyor…` pending state literal bulunur
    - `GeneratePagesDialog.tsx` içinde `max-w-2xl` DialogContent className bulunur (UI-SPEC section 3)
    - `GeneratePagesDialog.tsx` içinde `PAGE_TYPE_LABELS` map 10 entry içerir
    - Dosyalarda hiç `font-medium` sınıfı geçmez (font-weight kuralı — grep -c "font-medium" her iki dosyada 0 olmalı)
    - Dosyalarda hiç `asChild` prop kullanımı yok
    - `npx tsc --noEmit` çıktısında bu iki dosya için yeni hata YOK (grep count = 0)
  </acceptance_criteria>

  <done>
    - İki Client Component oluşturuldu, 07-01 Server Action'ı tüketiyor
    - D-05 dialog UX tam uygulandı (5-kolon, Zaten var badge, count summary)
    - UI-SPEC copywriting contract'ı (Turkish) uygulandı
    - TypeScript compile hatası yeni dosyalardan çıkmıyor
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReorderButton + KeywordMappingTab Client Components</name>
  <files>src/app/(dashboard)/projeler/[id]/site-blueprint/ReorderButton.tsx, src/app/(dashboard)/projeler/[id]/site-blueprint/KeywordMappingTab.tsx</files>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/site-blueprint/actions.ts (07-01 reorderPage sözleşmesi)
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterButton.tsx (useTransition + spinner pattern)
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordDeleteButton.tsx (ghost button hover-reveal pattern)
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/IntentBadge.tsx (reuse edilecek — import path: `../keyword-stratejisi/IntentBadge`)
    - .planning/phases/07-site-blueprint-tree/07-UI-SPEC.md (section 4 reorder + section 5 conflict badge + section 6 keyword mapping tab)
  </read_first>

  <behavior>
    - ReorderButton: tıklayınca `useTransition` ile `reorderPage(pageId, direction, projectId)` çağırır
    - `disabled` prop true ise buton gri (`opacity-30`) ve tıklanamaz
    - Pending durumunda buton `opacity-50 cursor-wait`
    - Hata durumunda hata inline gösterilmez (tree satırı içinde yer yok) — sessizce fail (revalidatePath olmuşsa zaten page refresh eder); 07-01 edge case (ilk/son) `success=true` döner
    - KeywordMappingTab: "Eşleşen" tablosu (sayfa adı, odak keyword, küme keywords comma-list, intent badge) + "Eşleşmeyenler" amber warning bloğu (count > 0 ise görünür, liste halinde)
    - KeywordMappingTab satırında sayfa mapping conflict'i varsa sayfa adı yanında "⚠ Çakışma" kırmızı badge
  </behavior>

  <action>
    **A) `src/app/(dashboard)/projeler/[id]/site-blueprint/ReorderButton.tsx` oluştur:**

    ```tsx
    'use client'

    import { useTransition } from 'react'
    import { Button } from '@/components/ui/button'
    import { reorderPage } from './actions'

    type Props = {
      pageId: string
      projectId: string
      direction: 'up' | 'down'
      disabled?: boolean
    }

    export function ReorderButton({ pageId, projectId, direction, disabled = false }: Props) {
      const [isPending, startTransition] = useTransition()

      const handleClick = () => {
        startTransition(async () => {
          await reorderPage(pageId, direction, projectId)
          // hata olursa UI reload ile eski state'e dönecek; toast yok, MVP
        })
      }

      const label = direction === 'up' ? '↑' : '↓'
      const aria = direction === 'up' ? 'Yukarı taşı' : 'Aşağı taşı'

      return (
        <Button
          type="button"
          variant="ghost"
          size="sm"
          onClick={handleClick}
          disabled={disabled || isPending}
          aria-label={aria}
          className={`h-7 w-7 p-0 text-muted-foreground hover:text-foreground ${
            disabled ? 'opacity-30' : ''
          } ${isPending ? 'opacity-50 cursor-wait' : ''}`}
        >
          {label}
        </Button>
      )
    }
    ```

    **B) `src/app/(dashboard)/projeler/[id]/site-blueprint/KeywordMappingTab.tsx` oluştur:**

    ```tsx
    'use client'
    // NOT: Server Component olarak da çalışabilirdi (state yok), ama IntentBadge path'i relative.
    // useClient kullanmaya gerek yok — IntentBadge pure component. Server Component olarak bırakıyoruz.

    // ⬇ 'use client' direktifini KALDIR; aşağıdaki import'lar SSR-safe
    ```

    **Düzeltme — KeywordMappingTab Server Component olarak yaz:**

    ```tsx
    // DOSYA: src/app/(dashboard)/projeler/[id]/site-blueprint/KeywordMappingTab.tsx
    // 'use client' YOK — Server Component, state yok, sadece render.

    import { IntentBadge } from '../keyword-stratejisi/IntentBadge'

    export type MappedPage = {
      id: string
      title: string
      focusKeyword: string | null
      clusterKeywords: string[]  // comma-list render için; cluster'ın tüm keyword'leri
      intent: string | null
      hasConflict: boolean
    }

    export type UnmappedKeyword = {
      id: string
      keyword: string
    }

    export function KeywordMappingTab({
      mappedPages,
      unmappedKeywords,
    }: {
      mappedPages: MappedPage[]
      unmappedKeywords: UnmappedKeyword[]
    }) {
      return (
        <div className="space-y-8">
          {/* Section A: Eşleşen Keyword'ler */}
          <section className="space-y-4">
            <h2 className="text-base font-semibold">Eşleşen Keyword&apos;ler</h2>

            {mappedPages.length === 0 ? (
              <div className="rounded-md border border-border px-4 py-8 text-center text-sm text-muted-foreground">
                Sayfa oluşturulmamış. &ldquo;Kümelerden Oluştur&rdquo; ile başlayın.
              </div>
            ) : (
              <div className="rounded-md border border-border overflow-hidden">
                <div className="grid grid-cols-[1fr_1fr_2fr_7rem] gap-0 bg-muted/50 border-b border-border text-xs text-muted-foreground font-normal">
                  <div className="px-4 py-2">Sayfa Adı</div>
                  <div className="px-4 py-2">Odak Keyword</div>
                  <div className="px-4 py-2">Küme Keyword&apos;leri</div>
                  <div className="px-4 py-2">Intent</div>
                </div>
                {mappedPages.map((p) => (
                  <div
                    key={p.id}
                    className="grid grid-cols-[1fr_1fr_2fr_7rem] gap-0 border-b border-border last:border-0 items-center text-sm hover:bg-muted/30 transition-colors"
                  >
                    <div className="px-4 py-2 font-normal truncate flex items-center">
                      {p.title}
                      {p.hasConflict && (
                        <span className="ml-2 text-xs px-1.5 py-0.5 rounded bg-red-500/20 text-red-400 shrink-0">
                          ⚠ Çakışma
                        </span>
                      )}
                    </div>
                    <div className="px-4 py-2 text-sm text-muted-foreground truncate">
                      {p.focusKeyword ?? '—'}
                    </div>
                    <div className="px-4 py-2 text-xs text-muted-foreground truncate">
                      {p.clusterKeywords.join(', ')}
                    </div>
                    <div className="px-4 py-2">
                      <IntentBadge intent={p.intent} />
                    </div>
                  </div>
                ))}
              </div>
            )}
          </section>

          {/* Section B: Eşleşmeyenler */}
          {unmappedKeywords.length > 0 && (
            <section className="space-y-3">
              <h2 className="text-sm font-semibold text-amber-400">
                Eşleşmeyen Keyword&apos;ler ({unmappedKeywords.length})
              </h2>

              <div className="rounded-md border border-amber-500/30 bg-amber-500/10 px-4 py-3 text-sm text-amber-400">
                Bu keyword&apos;ler henüz bir sayfaya bağlı değil. Küme oluşturup
                &ldquo;Kümelerden Oluştur&rdquo; ile sayfa atayın.
              </div>

              <ul className="space-y-1">
                {unmappedKeywords.map((k) => (
                  <li key={k.id} className="text-sm text-muted-foreground py-1">
                    {k.keyword}
                  </li>
                ))}
              </ul>
            </section>
          )}
        </div>
      )
    }
    ```

    **C) ReorderButton için Client Component gerekli** ('use client' — useTransition kullanıyor). KeywordMappingTab Server Component — daha az bundle.

    **Kritik:** Dosyaları iki ayrı dosya olarak yaz. KeywordMappingTab'ın ilk satırı `import` — YANİ `'use client'` directive YOK.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(ReorderButton|KeywordMappingTab)\.tsx" | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `ReorderButton.tsx` dosyası mevcut ve ilk satırında `'use client'` var
    - `KeywordMappingTab.tsx` dosyası mevcut ve içinde `'use client'` directive YOK (Server Component)
    - `ReorderButton.tsx` içinde `import { reorderPage } from './actions'` bulunur
    - `ReorderButton.tsx` içinde `direction === 'up'` ve `'↑'`, `'↓'` literal karakterleri bulunur
    - `ReorderButton.tsx` içinde `h-7 w-7 p-0` className literal'ı bulunur (UI-SPEC section 4)
    - `ReorderButton.tsx` içinde `opacity-30` (disabled) literal'ı bulunur
    - `KeywordMappingTab.tsx` içinde `import { IntentBadge } from '../keyword-stratejisi/IntentBadge'` bulunur
    - `KeywordMappingTab.tsx` içinde `⚠ Çakışma` literal string bulunur
    - `KeywordMappingTab.tsx` içinde `bg-red-500/20 text-red-400` conflict badge sınıfı bulunur
    - `KeywordMappingTab.tsx` içinde `Eşleşen Keyword` heading literal'ı bulunur
    - `KeywordMappingTab.tsx` içinde `Eşleşmeyen Keyword` heading literal'ı bulunur
    - `KeywordMappingTab.tsx` içinde `bg-amber-500/10` ve `border-amber-500/30` warning banner classları bulunur
    - `KeywordMappingTab.tsx` içinde `export type MappedPage` ve `export type UnmappedKeyword` type export'ları bulunur
    - Hiçbir dosyada `font-medium` sınıfı YOK (grep = 0)
    - `npx tsc --noEmit` çıktısında bu iki dosya için yeni hata YOK (grep count = 0)
  </acceptance_criteria>

  <done>
    - İki yeni komponent oluşturuldu; ReorderButton Client, KeywordMappingTab Server
    - UI-SPEC section 4 (reorder) ve section 5 (conflict badge) ve section 6 (mapping tab) tam uyumlu
    - IntentBadge reuse edildi
    - TypeScript compile hatası yok
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: site-blueprint/page.tsx entegrasyonu — tab routing, header actions, reorder column, conflict query</name>
  <files>src/app/(dashboard)/projeler/[id]/site-blueprint/page.tsx</files>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/site-blueprint/page.tsx (mevcut dosya — tam oku; section yapısı, grid kolonları, flattenTree ile bütünleşecek)
    - src/app/(dashboard)/projeler/[id]/site-blueprint/actions.ts (07-01 + 07-02 Task 1/2 sonrası güncel export'lar)
    - src/app/(dashboard)/projeler/[id]/site-blueprint/GenerateFromClustersButton.tsx (Task 1)
    - src/app/(dashboard)/projeler/[id]/site-blueprint/ReorderButton.tsx (Task 2)
    - src/app/(dashboard)/projeler/[id]/site-blueprint/KeywordMappingTab.tsx (Task 2)
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx (satır 54–90: searchParams Promise pattern)
    - .planning/phases/07-site-blueprint-tree/07-UI-SPEC.md (section 2 tab switcher + section 7 page structure reference)
    - .planning/phases/07-site-blueprint-tree/07-CONTEXT.md (D-01 page name = cluster name stripped of intent suffix; D-03 ?tab=mapping)
  </read_first>

  <action>
    Mevcut `page.tsx` dosyasını AŞAĞIDAKİ yapıya dönüştür. **Mevcut `MenuEditor`, `AddPageModal`, `flattenTree`, `priorityClass`, `MENU_CONFIGS`, `deletePage` inline form'u KORUNUR.**

    **1) Import'lara ekle:**
    ```typescript
    import { intentToPageType } from './actions'
    import { GenerateFromClustersButton } from './GenerateFromClustersButton'
    import { ReorderButton } from './ReorderButton'
    import { KeywordMappingTab, type MappedPage, type UnmappedKeyword } from './KeywordMappingTab'
    import type { DialogRow } from './GeneratePagesDialog'
    ```

    **2) Fonksiyon imzasını değiştir — searchParams ekle:**
    ```typescript
    export default async function SiteBlueprintPage({
      params,
      searchParams,
    }: {
      params: Promise<{ id: string }>
      searchParams: Promise<{ tab?: string }>
    }) {
      const { id } = await params
      const { tab } = await searchParams
      const isMappingTab = tab === 'mapping'
      // ... mevcut auth ve project check kodu
    ```

    **3) Mevcut `pages` sorgusundan sonra — SSR data query bloklarını ekle:**
    ```typescript
    // Kümeleri + primary keyword + cluster keywords çek (Generation dialog ve mapping için)
    const { data: clustersRaw } = await supabase
      .from('keyword_clusters')
      .select(`
        id,
        cluster_name,
        intent,
        primary_keyword_id,
        keywords!inner ( id, keyword )
      `)
      .eq('project_id', id)
      .eq('user_id', user.id)

    type ClusterRow = {
      id: string
      cluster_name: string
      intent: string | null
      primary_keyword_id: string | null
      keywords: Array<{ id: string; keyword: string }>
    }
    const clusters: ClusterRow[] = (clustersRaw ?? []) as ClusterRow[]

    // hasEnrichedClusters: primary_keyword_id'si atanmış cluster var mı (07-01 GenerateRowInput için gerekli)
    const hasEnrichedClusters = clusters.some((c) => c.primary_keyword_id !== null)

    // Dialog satırları: her cluster → DialogRow
    // Strip intent suffix (D-01): "foo bar (commercial)" → "Foo Bar"
    function stripIntentSuffix(name: string): string {
      const stripped = name.replace(/\s*\((commercial|informational|navigational|transactional|unknown)\)\s*$/i, '').trim()
      // Title-case (ilk harf büyük, kelimeler boşlukla)
      return stripped
        .split(/\s+/)
        .map((w) => (w.length > 0 ? w[0].toUpperCase() + w.slice(1).toLowerCase() : ''))
        .join(' ')
    }

    // cluster_id'si mevcut sayfalara bağlı olanları tespit et (alreadyExists için)
    const clusterIdsWithPages = new Set(
      pages.filter((p) => (p as Page & { cluster_id?: string }).cluster_id).map((p) => (p as Page & { cluster_id?: string }).cluster_id as string)
    )

    // primary keyword id'ye karşılık gelen keyword text'ini bulmak için map
    const primaryKeywordTextById = new Map<string, string>()
    for (const c of clusters) {
      if (c.primary_keyword_id) {
        const pk = c.keywords.find((k) => k.id === c.primary_keyword_id)
        if (pk) primaryKeywordTextById.set(c.primary_keyword_id, pk.keyword)
      }
    }

    const dialogRows: DialogRow[] = clusters
      .filter((c) => c.primary_keyword_id !== null)
      .map((c) => ({
        clusterId: c.id,
        clusterName: c.cluster_name,
        proposedName: stripIntentSuffix(c.cluster_name),
        proposedType: intentToPageType(c.intent),
        focusKeyword: c.primary_keyword_id ? primaryKeywordTextById.get(c.primary_keyword_id) ?? null : null,
        focusKeywordId: c.primary_keyword_id,
        alreadyExists: clusterIdsWithPages.has(c.id),
      }))
    ```

    **4) Page'lere `cluster_id` alanını SELECT'e ekle** — mevcut sorgu:
    ```typescript
    // ÖNCEKİ: .select('id, title, slug, page_type, priority, parent_id, sort_order')
    // YENİ:
    const { data: pagesData } = await supabase
      .from('pages')
      .select('id, title, slug, page_type, priority, parent_id, sort_order, cluster_id, focus_keyword_id')
      .eq('project_id', id)
      .eq('user_id', user.id)
      .order('sort_order', { ascending: true })
    ```

    Ve `Page` type'ını güncelle:
    ```typescript
    type Page = {
      id: string
      title: string
      slug: string
      page_type: string
      priority: string
      parent_id: string | null
      sort_order: number
      cluster_id: string | null
      focus_keyword_id: string | null
    }
    ```

    **5) Conflict detection SSR query — BLUE-02 mapping conflict:**
    ```typescript
    // Aynı keyword text'i birden fazla cluster-sayfa'ya bağlıysa conflict
    // Yaklaşım: pages → cluster → keywords ilişkisi; keyword text aynı ama cluster farklı ve her iki cluster'ın sayfası var
    const conflictPageIds = new Set<string>()

    // Hangi cluster hangi page'e bağlı?
    const clusterIdToPageId = new Map<string, string>()
    for (const p of pages) {
      if (p.cluster_id) clusterIdToPageId.set(p.cluster_id, p.id)
    }

    // Her cluster'ın keyword text'lerini topla — keyword text → [clusterId...]
    const keywordTextToClusters = new Map<string, Set<string>>()
    for (const c of clusters) {
      if (!clusterIdToPageId.has(c.id)) continue // sadece sayfası olan cluster'lar sayılır
      for (const kw of c.keywords) {
        const key = kw.keyword.toLowerCase().trim()
        if (!keywordTextToClusters.has(key)) keywordTextToClusters.set(key, new Set())
        keywordTextToClusters.get(key)!.add(c.id)
      }
    }

    for (const [, clusterSet] of keywordTextToClusters) {
      if (clusterSet.size > 1) {
        // Bu keyword 2+ cluster-sayfa'da var → tüm ilgili page ID'leri conflict
        for (const cid of clusterSet) {
          const pid = clusterIdToPageId.get(cid)
          if (pid) conflictPageIds.add(pid)
        }
      }
    }
    ```

    **6) Keyword mapping tab data — mappedPages + unmappedKeywords SSR:**
    ```typescript
    // mappedPages: pages with cluster_id !== null
    const clusterById = new Map<string, ClusterRow>(clusters.map((c) => [c.id, c]))

    // keyword id → keyword text (focus keyword lookup için zaten primaryKeywordTextById var, ama focus_keyword_id !== primary olabilir)
    // Pratikte 07-01 focus_keyword_id = cluster.primary_keyword_id, ama yine de keywords sorgusu gerekli
    const focusKeywordIds = pages
      .map((p) => p.focus_keyword_id)
      .filter((v): v is string => v !== null)

    const { data: focusKeywordRows } = focusKeywordIds.length > 0
      ? await supabase
          .from('keywords')
          .select('id, keyword')
          .in('id', focusKeywordIds)
          .eq('user_id', user.id)
      : { data: [] as Array<{ id: string; keyword: string }> }

    const focusKeywordTextById = new Map<string, string>()
    for (const row of focusKeywordRows ?? []) {
      focusKeywordTextById.set(row.id, row.keyword)
    }

    const mappedPages: MappedPage[] = pages
      .filter((p) => p.cluster_id !== null)
      .map((p) => {
        const cluster = p.cluster_id ? clusterById.get(p.cluster_id) : null
        return {
          id: p.id,
          title: p.title,
          focusKeyword: p.focus_keyword_id ? focusKeywordTextById.get(p.focus_keyword_id) ?? null : null,
          clusterKeywords: cluster ? cluster.keywords.map((k) => k.keyword) : [],
          intent: cluster?.intent ?? null,
          hasConflict: conflictPageIds.has(p.id),
        }
      })

    // Unmapped keywords: cluster_id IS NULL
    const { data: unmappedRaw } = await supabase
      .from('keywords')
      .select('id, keyword')
      .eq('project_id', id)
      .eq('user_id', user.id)
      .is('cluster_id', null)

    const unmappedKeywords: UnmappedKeyword[] = (unmappedRaw ?? []).map((k) => ({
      id: k.id,
      keyword: k.keyword,
    }))
    ```

    **7) Render — tab switcher + koşullu içerik. Mevcut return bloğunu şu yapıya dönüştür:**

    - Header bölümü (h1 + breadcrumb) KORUNUR
    - Sol nav (ProjectNav) KORUNUR
    - Sağ içerik alanında:
      - Yeni satır: Tab bar (Link-based) + header actions (GenerateFromClustersButton + AddPageModal) aynı flex row'da
      - `!isMappingTab` koşulunda: Site Ağacı section (güncellenmiş grid + reorder column + conflict badge) + Menüler section (KORUNUR)
      - `isMappingTab` koşulunda: `<KeywordMappingTab mappedPages={...} unmappedKeywords={...} />`

    ```tsx
    // Sağ içerik — dönüşüm:
    <div className="flex-1 min-w-0 overflow-y-auto p-8 space-y-12">

      {/* Tab bar + header actions — yeni birleşik satır */}
      <div className="flex items-center justify-between">
        <div className="flex items-center gap-1 border border-border rounded-md p-0.5">
          <Link
            href={`/projeler/${id}/site-blueprint?tab=tree`}
            className={`px-3 py-1.5 rounded text-sm transition-colors ${
              !isMappingTab
                ? 'bg-secondary text-foreground font-semibold'
                : 'text-muted-foreground hover:text-foreground font-normal'
            }`}
          >
            Ağaç Görünümü
          </Link>
          <Link
            href={`/projeler/${id}/site-blueprint?tab=mapping`}
            className={`px-3 py-1.5 rounded text-sm transition-colors ${
              isMappingTab
                ? 'bg-secondary text-foreground font-semibold'
                : 'text-muted-foreground hover:text-foreground font-normal'
            }`}
          >
            Keyword Eşleme
          </Link>
        </div>

        {!isMappingTab && (
          <div className="flex items-center gap-2">
            <GenerateFromClustersButton
              projectId={id}
              rows={dialogRows}
              hasEnrichedClusters={hasEnrichedClusters}
            />
            <AddPageModal projectId={id} existingPages={pages.map((p) => ({ id: p.id, title: p.title }))} />
          </div>
        )}
      </div>

      {isMappingTab ? (
        <KeywordMappingTab mappedPages={mappedPages} unmappedKeywords={unmappedKeywords} />
      ) : (
        <>
          {/* Site Ağacı section — mevcut + reorder kolonu + conflict badge */}
          <section className="space-y-4">
            <div className="flex items-center justify-between">
              <h2 className="text-base font-semibold">Site Ağacı</h2>
            </div>

            <div className="rounded-md border border-border overflow-hidden">
              {/* Tablo başlığı — grid güncellendi: 1fr_1fr_auto_auto_auto_auto (reorder kolonu eklendi) */}
              <div className="grid grid-cols-[1fr_1fr_auto_auto_auto_auto] gap-0 bg-muted/50 border-b border-border text-xs text-muted-foreground font-normal">
                <div className="px-4 py-2">Sayfa Adı</div>
                <div className="px-4 py-2">Slug</div>
                <div className="px-4 py-2 w-32">Tip</div>
                <div className="px-4 py-2 w-20">Öncelik</div>
                <div className="px-4 py-2 w-16 text-center">Sıra</div>
                <div className="px-4 py-2 w-16"></div>
              </div>

              {flatPages.length === 0 ? (
                <div className="px-4 py-8 text-center text-sm text-muted-foreground">
                  Henüz sayfa yok. &ldquo;Sayfa Ekle&rdquo; ile başlayın.
                </div>
              ) : (
                flatPages.map((page, index) => {
                  // Aynı parent içindeki index'i bul — ilk/son tespiti için
                  const siblingList = flatPages.filter((p) => p.parent_id === page.parent_id)
                  const siblingIndex = siblingList.findIndex((p) => p.id === page.id)
                  const isFirstSibling = siblingIndex === 0
                  const isLastSibling = siblingIndex === siblingList.length - 1
                  const hasConflict = conflictPageIds.has(page.id)
                  return (
                    <div
                      key={page.id}
                      className="grid grid-cols-[1fr_1fr_auto_auto_auto_auto] gap-0 border-b border-border last:border-0 items-center text-sm hover:bg-muted/30 transition-colors group"
                    >
                      {/* Sayfa adı — girintili + conflict badge */}
                      <div
                        className="px-4 py-2 font-normal truncate flex items-center"
                        style={{ paddingLeft: `${16 + page.depth * 20}px` }}
                      >
                        {page.depth > 0 && (
                          <span className="text-muted-foreground mr-1.5">↳</span>
                        )}
                        <span className="truncate">{page.title}</span>
                        {hasConflict && (
                          <span className="ml-2 text-xs px-1.5 py-0.5 rounded bg-red-500/20 text-red-400 shrink-0">
                            ⚠ Çakışma
                          </span>
                        )}
                      </div>

                      {/* Slug */}
                      <div className="px-4 py-2 text-muted-foreground truncate font-mono text-xs">
                        {page.slug}
                      </div>

                      {/* Tip */}
                      <div className="px-4 py-2 w-32 text-muted-foreground truncate">
                        {page.page_type}
                      </div>

                      {/* Öncelik */}
                      <div className={cn('px-4 py-2 w-20 text-xs font-normal', priorityClass(page.priority))}>
                        {page.priority}
                      </div>

                      {/* Reorder ↑↓ */}
                      <div className="px-2 py-2 w-16 flex items-center justify-center gap-0.5">
                        <ReorderButton pageId={page.id} projectId={id} direction="up" disabled={isFirstSibling} />
                        <ReorderButton pageId={page.id} projectId={id} direction="down" disabled={isLastSibling} />
                      </div>

                      {/* Sil butonu — mevcut inline form korundu */}
                      <div className="px-4 py-2 w-16 text-right">
                        <form
                          action={async () => {
                            'use server'
                            await deletePage(id, page.id)
                          }}
                        >
                          <Button
                            variant="ghost"
                            size="sm"
                            type="submit"
                            className="h-7 px-2 text-muted-foreground hover:text-destructive"
                          >
                            Sil
                          </Button>
                        </form>
                      </div>
                    </div>
                  )
                })
              )}
            </div>
          </section>

          {/* Menüler section — TAMAMEN KORUNUR (mevcut map) */}
          <section className="space-y-4">
            <h2 className="text-base font-semibold">Menüler</h2>
            <div className="space-y-8">
              {MENU_CONFIGS.map(({ type, title }) => (
                <MenuEditor
                  key={type}
                  projectId={id}
                  menuType={type}
                  menuTitle={title}
                  initialItems={menuMap[type] ?? []}
                />
              ))}
            </div>
          </section>
        </>
      )}

    </div>
    ```

    **8) Mevcut `font-medium` kullanımlarını `font-normal` veya `font-semibold` ile değiştir** — UI-SPEC kuralı gereği. Grep ile bul:
    - `className="... font-medium"` → koşula göre `font-normal` (body/cell) veya `font-semibold` (heading)
    - Mevcut satır: `className="px-4 py-2 font-medium truncate"` → `font-normal`
    - Mevcut satır: `grid ... text-xs text-muted-foreground font-medium` → `font-normal`
    - Mevcut satır: `px-4 py-2 w-20 text-xs font-medium` (priorityClass) → `font-normal`

    **9) TypeScript doğrulaması:**
    ```bash
    npx tsc --noEmit
    ```
    page.tsx için yeni hata çıkmamalı. Pre-existing hatalar (ProjectNav vb.) kapsam dışı.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "site-blueprint/page\.tsx" | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `site-blueprint/page.tsx` içinde `searchParams: Promise<{ tab?: string }>` type annotation bulunur
    - `site-blueprint/page.tsx` içinde `const isMappingTab = tab === 'mapping'` literal bulunur
    - `site-blueprint/page.tsx` içinde `import { GenerateFromClustersButton }` bulunur
    - `site-blueprint/page.tsx` içinde `import { ReorderButton }` bulunur
    - `site-blueprint/page.tsx` içinde `import { KeywordMappingTab` bulunur
    - `site-blueprint/page.tsx` içinde `import { intentToPageType }` bulunur
    - `site-blueprint/page.tsx` içinde `from('keyword_clusters')` sorgusu bulunur
    - `site-blueprint/page.tsx` içinde `.select('id, title, slug, page_type, priority, parent_id, sort_order, cluster_id, focus_keyword_id')` literal bulunur (cluster_id + focus_keyword_id eklendi)
    - `site-blueprint/page.tsx` içinde `hasEnrichedClusters` değişkeni bulunur
    - `site-blueprint/page.tsx` içinde `clusterIdsWithPages` değişkeni bulunur (duplicate guard için dialog row'ları)
    - `site-blueprint/page.tsx` içinde `conflictPageIds` değişkeni bulunur
    - `site-blueprint/page.tsx` içinde `stripIntentSuffix` fonksiyonu tanımı bulunur
    - `site-blueprint/page.tsx` içinde `?tab=mapping` ve `?tab=tree` URL literal'ları bulunur
    - `site-blueprint/page.tsx` içinde `Ağaç Görünümü` ve `Keyword Eşleme` tab label'ları bulunur
    - `site-blueprint/page.tsx` içinde `<ReorderButton` JSX kullanımı bulunur (en az 2 kez — up + down)
    - `site-blueprint/page.tsx` içinde `<KeywordMappingTab` JSX kullanımı bulunur
    - `site-blueprint/page.tsx` içinde `<GenerateFromClustersButton` JSX kullanımı bulunur
    - `site-blueprint/page.tsx` içinde `grid-cols-[1fr_1fr_auto_auto_auto_auto]` (6-kolon grid) bulunur (reorder kolonu eklendi)
    - `site-blueprint/page.tsx` içinde `⚠ Çakışma` literal bulunur
    - `site-blueprint/page.tsx` içinde `is('cluster_id', null)` sorgusu bulunur (unmapped keywords)
    - `site-blueprint/page.tsx` içinde `<AddPageModal` HÂLÂ bulunur (mevcut davranış korundu)
    - `site-blueprint/page.tsx` içinde `<MenuEditor` HÂLÂ bulunur (mevcut davranış korundu)
    - `site-blueprint/page.tsx` içinde inline `await deletePage(id, page.id)` HÂLÂ bulunur (mevcut sil butonu korundu)
    - `site-blueprint/page.tsx` içinde `font-medium` literal'ı YOK (grep count = 0, UI-SPEC font-weight kuralı)
    - `npx tsc --noEmit` çıktısında `site-blueprint/page.tsx` için yeni hata YOK (grep count = 0)
  </acceptance_criteria>

  <done>
    - site-blueprint/page.tsx iki-sekmeli yapıya dönüştürüldü
    - Conflict detection SSR pattern'i ile hesaplandı ve hem tree hem mapping tab'ında ⚠ Çakışma badge'i gösteriyor
    - Reorder butonları tree row'larına eklendi; ilk/son sibling tespiti siblingList indeksi ile yapılıyor
    - Kümelerden Oluştur butonu hasEnrichedClusters=false olduğunda disabled + tooltip
    - Mevcut AddPageModal, MenuEditor, inline deletePage form'u değişmedi
    - font-medium kullanımları font-normal/font-semibold ile yer değiştirdi
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 7 full UI + backend devrede. Kullanıcı artık keyword kümelerinden tek tıkla sayfa üretebilir, conflict'leri görebilir, sayfaları yukarı/aşağı taşıyabilir ve Keyword Eşleme tab'ında her keyword'ün hangi sayfaya bağlı olduğunu kontrol edebilir.
  </what-built>

  <how-to-verify>
    1. Dev sunucuyu başlat: `npm run dev`
    2. Bir projeye git → Site Blueprint sayfasına git: `/projeler/{id}/site-blueprint`
    3. **Tab switcher kontrolü:**
       - İki tab görünmeli: "Ağaç Görünümü" (aktif/default) + "Keyword Eşleme"
       - Tab tıklayınca URL `?tab=mapping` olmalı, içerik değişmeli
       - Aktif tab `bg-secondary font-semibold`, inaktif `text-muted-foreground font-normal`
    4. **Kümelerden Oluştur — 0 küme durumu:**
       - Henüz kümeleme yapılmamış bir projede buton disabled görünmeli
       - Butonun üzerine gelince tooltip "Önce keyword kümeleme yapın" çıkmalı
    5. **Kümelerden Oluştur — dolu durum:**
       - Kümeli bir projeye git (keyword-stratejisi'nde önceden Kümelere Böl yapılmış)
       - "Kümelerden Oluştur" tıkla → dialog açılmalı
       - 5 sütunlu preview tablosu görünmeli: Küme Adı | Sayfa Adı (editable) | Tip (select) | Odak Keyword | Dahil Et (checkbox)
       - Sayfa adı input'unu düzenleyebilmelisin
       - Tip select'ini değiştirebilmelisin (Ana Sayfa, Hizmet, Blog ...)
       - Checkbox işaretle/kaldır
       - Bottom'da "{N} sayfa oluşturulacak" count görünmeli
       - "Oluştur" tıkla → dialog kapanmalı, tree table'da yeni sayfalar görünmeli
    6. **Zaten var badge:**
       - Aynı kümeden ikinci kez oluştur çalıştır
       - O satırda amber "Zaten var" badge + checkbox pre-checked=false + input disabled olmalı
       - "Oluştur" tıklayınca sadece seçili yeni satırlar eklenmeli
    7. **Reorder oklarını test et:**
       - Tree table'daki bir sayfanın ↑ ↓ butonlarını kullan
       - İlk sayfa'da ↑ disabled (opacity-30)
       - Son sayfa'da ↓ disabled
       - Bir sayfayı aşağı taşı → revalidatePath ile liste yeniden sıralanmalı
    8. **Conflict badge (mapping conflict):**
       - İki farklı cluster'da aynı keyword text'i varsa (manuel DB ile veya keyword taşıma ile yarat) ve her ikisinde de sayfa oluştur
       - Her iki sayfada da "⚠ Çakışma" kırmızı badge görünmeli (sayfa adı yanında)
    9. **Keyword Eşleme tab'ı:**
       - Tab'a geç → "Eşleşen Keyword'ler" tablosu (sayfa adı, odak keyword, tüm küme keyword'leri, intent badge)
       - `cluster_id IS NULL` keyword varsa aşağıda amber warning bloğu + liste görünmeli ("Eşleşmeyen Keyword'ler ({N})")
       - Conflict olan sayfalarda burada da "⚠ Çakışma" badge
    10. **Mevcut davranış bozulmamış mı?**
       - AddPageModal hâlâ çalışıyor mu? ("+ Sayfa Ekle")
       - Menüler bölümü hâlâ görünüyor mu? (Üst Menü, Ana Menü, Footer)
       - Sil butonu hâlâ çalışıyor mu?
  </how-to-verify>

  <resume-signal>Tümü yeşilse "approved" yaz. Hata varsa listele; gerekirse 07-03 gap closure plan açılır.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL searchParam → Server Component | `?tab=...` parametresi — sadece `=== 'mapping'` boolean kontrolü; injection yüzeyi yok |
| Client form (GeneratePagesDialog) → Server Action | Dialog'un düzenlenen rows'u clusterId + focusKeywordId + pageName + pageType taşır — 07-01 action'ında validasyon var |
| Client (ReorderButton) → Server Action | pageId + direction + projectId — 07-01 guard'ları enforce eder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-UI-01 | Tampering | GeneratePagesDialog clusterId/focusKeywordId payload | mitigate | Client payload tampering mümkün ancak 07-01 `generatePagesFromClusters` içinde UUID regex + `keyword_clusters.in('id', clusterIds).eq('user_id', user.id)` ownership double-check — başka kullanıcının cluster'ını projeye bağlayamaz (T-07-01 ile eşleşir) |
| T-07-UI-02 | Information Disclosure | page.tsx SSR sorguları (clusters + unmapped keywords) | mitigate | Her Supabase sorgusunda `.eq('user_id', user.id)` + `.eq('project_id', id)` çift filtre; RLS defense-in-depth |
| T-07-UI-03 | Tampering | ReorderButton direction prop | mitigate | Client tarafında `'up' | 'down'` TS literal; 07-01 action'ı runtime enum guard ile doğrular (T-07-05 ile eşleşir) |
| T-07-UI-04 | Cross-Site Scripting (XSS) | Sayfa adı / küme adı / keyword text render | mitigate | React otomatik HTML escaping — tüm string render'lar `{value}` JSX expression, `dangerouslySetInnerHTML` yok |
| T-07-UI-05 | Information Disclosure | Tab URL param leaks state in browser history | accept | `?tab=mapping` tam listeleme değil, sadece tab adı; yan etki yok |
| T-07-UI-06 | CSRF (reorder/generate) | Server Actions called via POST | mitigate | Next.js 15 Server Actions built-in CSRF token + Origin check — ek önlem gerekmez |
| T-07-UI-07 | Denial of Service | Dialog ile çok büyük cluster sayısı render | mitigate | 07-01'de `rows.length > 500` guard; UI'da `max-h-96 overflow-y-auto` ile görsel sınır — tarayıcıda DOM patlaması yok |
</threat_model>

<verification>
## Task-level

- Task 1 (Dialog + Button): `npx tsc --noEmit` → GenerateFromClustersButton + GeneratePagesDialog için yeni hata YOK
- Task 2 (Reorder + Mapping tab): `npx tsc --noEmit` → ReorderButton + KeywordMappingTab için yeni hata YOK
- Task 3 (page.tsx): `npx tsc --noEmit` → site-blueprint/page.tsx için yeni hata YOK
- Task 4: Human verification — 10 senaryo (checklist)

## Plan-level

- 4 yeni dosya mevcut: GenerateFromClustersButton.tsx, GeneratePagesDialog.tsx, ReorderButton.tsx, KeywordMappingTab.tsx
- page.tsx searchParams destructure + tab routing + conflict query + reorder column + 3 yeni JSX komponent entegrasyonu
- Hiçbir dosyada `font-medium` yok (UI-SPEC kuralı)
- Hiçbir dosyada `asChild` prop yok (base-ui kuralı)
</verification>

<success_criteria>
- BLUE-01: Kullanıcı "Kümelerden Oluştur" butonuna tıklayarak tek akışta kümelerden pages oluşturabilir
- BLUE-02: Her keyword mapping Keyword Eşleme tab'ında görünür; conflict var ise hem tree hem mapping tab'ında ⚠ badge çıkar
- BLUE-03: Kullanıcı ↑↓ okları ile aynı parent altındaki sayfaları yeniden sıralayabilir
- D-01: Üretilen sayfa adı "Foo Bar" formatında (intent suffix stripped, title-case)
- D-02: Select default'u intent→page_type kuralına göre gelir (transactional/commercial→hizmet, informational/unknown→blog, navigational→ana-sayfa)
- D-03: İki tab arası geçiş ?tab=mapping / ?tab=tree URL'si ile çalışır
- D-04: ↑↓ butonları ilk/son kardeşte disabled
- D-05: Dialog 5-sütun + Zaten var badge + count summary + İptal/Oluştur
- D-06: Üretilen slug lowercase, Türkçe normalize, <= 60 char, duplicate'lara -2/-3 suffix (Task 1 slugify unit testleri)
- Mevcut AddPageModal/MenuEditor/deletePage davranışları korundu
- font-medium hiçbir yeni/modifiye dosyada kullanılmıyor
- human-verify checklist (Task 4) 10/10 PASS
</success_criteria>

<output>
After completion, create `.planning/phases/07-site-blueprint-tree/07-02-SUMMARY.md` per templates/summary.md.
</output>
