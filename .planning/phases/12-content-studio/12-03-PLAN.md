---
phase: 12-content-studio
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts
autonomous: true
requirements:
  - CONT-03
  - CONT-04
  - CONT-05

must_haves:
  truths:
    - "approveSection server action content_sections[i].status = 'approved' yazar ve content'i kaydeder"
    - "Tüm bölümler approved olduğunda approveSection html_content'i otomatik birleştirir"
    - "rejectSection server action content_sections[i].status = 'rejected' yazar"
    - "saveContentSections server action tüm sections dizisini bulk yazar (streaming sonrası ilk kayıt)"
    - "Her action ownership doğrulaması yapar (projectId + user_id)"
    - "Her action revalidatePath ile hem içerik studio hem sayfa paketi sayfasını yeniler"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts"
      provides: "approveSection, rejectSection, saveContentSections actions + ContentSection tipi"
      exports: ["approveSection", "rejectSection", "saveContentSections", "ContentSection"]
  key_links:
    - from: "ContentStudioShell (client)"
      to: "approveSection (server action)"
      via: "useTransition + startTransition"
      pattern: "approveSection"
    - from: "approveSection"
      to: "page_packages.html_content"
      via: "assembleHtml() — all sections approved → write html_content"
      pattern: "html_content"
---

<objective>
sayfa-paketi/actions.ts dosyasını genişlet: üç yeni server action ve ContentSection tipi ekle.

Purpose: Content Studio client bileşenleri bu action'ları çağırır — bölüm onayı, reddi ve stream sonrası ilk kayıt. Tüm bölümler onaylandığında HTML birleştirme de bu katmanda tetiklenir (CONT-05).
Output: `actions.ts` dosyasına üç yeni export (approveSection, rejectSection, saveContentSections) + ContentSection tipi.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-content-studio/12-CONTEXT.md
@.planning/phases/12-content-studio/12-PATTERNS.md

<interfaces>
<!-- Mevcut actions.ts yapısı (key excerpts) -->

// Return tipi — yeni action'larda aynı tip kullanılır
export type ActionResult = { success: true } | { success: false; error: string }

// verifyOwnership helper — yeni action'lar bu fonksiyonu kullanır
async function verifyOwnership(supabase, projectId, userId) {
  const { data } = await supabase.from('projects').select('id')
    .eq('id', projectId).eq('user_id', userId).single()
  return data
}

// updatePackageStatus pattern — yeni action'lar bu yapıyı izler:
export async function updatePackageStatus(projectId, packageId, newStatus): Promise<ActionResult> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Oturum bulunamadı.' }
  const project = await verifyOwnership(supabase, projectId, user.id)
  if (!project) return { success: false, error: 'Proje bulunamadı.' }
  // DB işlemi...
  revalidatePath(`/projeler/${projectId}/sayfa-paketi`)
  return { success: true }
}

<!-- D-04 content_sections JSONB schema (12-CONTEXT.md) -->
ContentSection şeması:
{
  heading: string      // H2 başlığı
  level: number        // 2 veya 3
  sub_headings: string[]  // H3 başlıkları (varsa)
  content: string      // üretilen veya düzenlenmiş metin
  status: 'pending' | 'generating' | 'draft' | 'approved' | 'rejected'
}

<!-- D-04 html_content assembly kuralı -->
Tüm bölümler approved olduğunda:
- Her bölüm için: `<h2>${section.heading}</h2>\n<p>${section.content}</p>\n`
- sub_headings varsa her biri için: `<h3>${sub}</h3>\n`
- Birleştirilmiş HTML html_content TEXT sütununa yazılır
- Boş satırla bölümler arasında ayrılır: `\n\n`

<!-- revalidatePath kuralı — iki path birlikte -->
revalidatePath(`/projeler/${projectId}/icerik-studio/${pageId}`)
revalidatePath(`/projeler/${projectId}/sayfa-paketi`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContentSection tipi + approveSection + rejectSection + saveContentSections ekle</name>
  <files>src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts (tam mevcut dosya — önce oku)
  </read_first>
  <action>
Mevcut `actions.ts` dosyasını oku, ardından dosyanın sonuna aşağıdaki içeriği ekle. Mevcut kodu değiştirme — sadece ekle.

**1. ContentSection tipi — dosyanın en üstüne, ActionResult tipinin hemen altına ekle:**
```typescript
export type ContentSection = {
  heading: string
  level: number
  sub_headings: string[]
  content: string
  status: 'pending' | 'generating' | 'draft' | 'approved' | 'rejected'
}
```

**2. HTML birleştirme helper — dosyanın ortasına, verifyOwnership'ten sonra ekle:**
```typescript
function assembleHtml(sections: ContentSection[]): string {
  return sections
    .map((section) => {
      let html = `<h2>${section.heading}</h2>\n<p>${section.content}</p>`
      if (section.sub_headings && section.sub_headings.length > 0) {
        const subHtml = section.sub_headings.map((sub) => `<h3>${sub}</h3>`).join('\n')
        html = `<h2>${section.heading}</h2>\n${subHtml}\n<p>${section.content}</p>`
      }
      return html
    })
    .join('\n\n')
}
```

**3. saveContentSections action — ilk stream tamamlandığında client çağırır:**
```typescript
/**
 * saveContentSections: Streaming tamamlandıktan sonra tüm bölümleri bulk yazar.
 * Client, stream bittikten sonra sections[] dizisini (status='draft') bu action'a gönderir.
 */
export async function saveContentSections(
  projectId: string,
  pageId: string,
  sections: ContentSection[]
): Promise<ActionResult> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Oturum bulunamadı.' }

  const project = await verifyOwnership(supabase, projectId, user.id)
  if (!project) return { success: false, error: 'Proje bulunamadı.' }

  // Ownership: page_packages'ı page_id + project_id + user_id üçlüsüyle doğrula
  const { data: pkg } = await supabase
    .from('page_packages')
    .select('id, status')
    .eq('page_id', pageId)
    .eq('project_id', projectId)
    .eq('user_id', user.id)
    .single()
  if (!pkg) return { success: false, error: 'Paket bulunamadı.' }
  if (pkg.status !== 'locked') return { success: false, error: 'Paket kilitli değil.' }

  const { error } = await supabase
    .from('page_packages')
    .update({ content_sections: sections, updated_at: new Date().toISOString() })
    .eq('id', pkg.id)
    .eq('user_id', user.id)

  if (error) return { success: false, error: 'Bölümler kaydedilemedi: ' + error.message }

  revalidatePath(`/projeler/${projectId}/icerik-studio/${pageId}`)
  revalidatePath(`/projeler/${projectId}/sayfa-paketi`)
  return { success: true }
}
```

**4. approveSection action — bölüm onayı, html assembly tetikleme:**
```typescript
/**
 * approveSection: Belirtilen bölümü approved yapar, içeriği kaydeder.
 * Tüm bölümler approved ise html_content otomatik birleştirilir.
 */
export async function approveSection(
  projectId: string,
  pageId: string,
  sectionIndex: number,
  content: string
): Promise<ActionResult> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Oturum bulunamadı.' }

  const project = await verifyOwnership(supabase, projectId, user.id)
  if (!project) return { success: false, error: 'Proje bulunamadı.' }

  const { data: pkg } = await supabase
    .from('page_packages')
    .select('id, status, content_sections')
    .eq('page_id', pageId)
    .eq('project_id', projectId)
    .eq('user_id', user.id)
    .single()
  if (!pkg) return { success: false, error: 'Paket bulunamadı.' }
  if (pkg.status !== 'locked') return { success: false, error: 'Paket kilitli değil.' }

  const sections: ContentSection[] = Array.isArray(pkg.content_sections)
    ? (pkg.content_sections as ContentSection[])
    : []

  if (sectionIndex < 0 || sectionIndex >= sections.length) {
    return { success: false, error: 'Geçersiz bölüm indeksi.' }
  }

  sections[sectionIndex] = { ...sections[sectionIndex], content, status: 'approved' }

  // Tüm bölümler approved ise HTML birleştir
  const allApproved = sections.every((s) => s.status === 'approved')
  const htmlContent = allApproved ? assembleHtml(sections) : undefined

  const updatePayload: Record<string, unknown> = {
    content_sections: sections,
    updated_at: new Date().toISOString(),
  }
  if (htmlContent !== undefined) {
    updatePayload.html_content = htmlContent
  }

  const { error } = await supabase
    .from('page_packages')
    .update(updatePayload)
    .eq('id', pkg.id)
    .eq('user_id', user.id)

  if (error) return { success: false, error: 'Bölüm onaylanamadı: ' + error.message }

  revalidatePath(`/projeler/${projectId}/icerik-studio/${pageId}`)
  revalidatePath(`/projeler/${projectId}/sayfa-paketi`)
  return { success: true }
}
```

**5. rejectSection action:**
```typescript
/**
 * rejectSection: Belirtilen bölümü rejected yapar.
 * Client bölümü pending'e döndürür ve yeniden üretebilir.
 */
export async function rejectSection(
  projectId: string,
  pageId: string,
  sectionIndex: number
): Promise<ActionResult> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Oturum bulunamadı.' }

  const project = await verifyOwnership(supabase, projectId, user.id)
  if (!project) return { success: false, error: 'Proje bulunamadı.' }

  const { data: pkg } = await supabase
    .from('page_packages')
    .select('id, status, content_sections')
    .eq('page_id', pageId)
    .eq('project_id', projectId)
    .eq('user_id', user.id)
    .single()
  if (!pkg) return { success: false, error: 'Paket bulunamadı.' }
  if (pkg.status !== 'locked') return { success: false, error: 'Paket kilitli değil.' }

  const sections: ContentSection[] = Array.isArray(pkg.content_sections)
    ? (pkg.content_sections as ContentSection[])
    : []

  if (sectionIndex < 0 || sectionIndex >= sections.length) {
    return { success: false, error: 'Geçersiz bölüm indeksi.' }
  }

  sections[sectionIndex] = { ...sections[sectionIndex], status: 'rejected' }

  const { error } = await supabase
    .from('page_packages')
    .update({ content_sections: sections, updated_at: new Date().toISOString() })
    .eq('id', pkg.id)
    .eq('user_id', user.id)

  if (error) return { success: false, error: 'Bölüm reddedilemedi: ' + error.message }

  revalidatePath(`/projeler/${projectId}/icerik-studio/${pageId}`)
  revalidatePath(`/projeler/${projectId}/sayfa-paketi`)
  return { success: true }
}
```

PagePackageData tipine `content_sections` ve `html_content` alanlarını da ekle (updatePagePackage'da kullanılmıyor ama type bütünlüğü için):
```typescript
// PagePackageData tipine ekle:
content_sections?: unknown
html_content?: string
```
  </action>
  <verify>
    `grep "export async function approveSection" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` — export var.
    `grep "export async function rejectSection" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` — export var.
    `grep "export async function saveContentSections" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` — export var.
    `grep "export type ContentSection" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` — tip export var.
    `grep "assembleHtml" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` — HTML assembly var.
    `grep "html_content" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` — html_content yazımı var.
    `npx tsc --noEmit` — hatasız.
  </verify>
  <done>
    actions.ts dosyasında:
    - ContentSection tipi export edildi
    - saveContentSections: bulk JSONB yazımı, locked kontrolü, revalidate ikili path
    - approveSection: section[i].status='approved', content yazımı, allApproved → html_content assembly
    - rejectSection: section[i].status='rejected'
    - Her action: ownership verify (project + page_package), locked check, revalidatePath ikili
    `npx tsc --noEmit` hatasız.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server action | sectionIndex ve content kullanıcıdan geliyor — bounds check ve ownership zorunlu |
| server action → page_packages UPDATE | user_id filtresi olmadan başka kullanıcının verisi güncellenebilir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Spoofing | approveSection/rejectSection/saveContentSections | mitigate | `supabase.auth.getUser()` + `verifyOwnership()` her action'da |
| T-12-03-02 | Information Disclosure | IDOR via pageId | mitigate | `page_packages` sorgusu `.eq('project_id', projectId).eq('user_id', user.id)` üçlü filtre |
| T-12-03-03 | Tampering | sectionIndex out-of-bounds | mitigate | `sectionIndex < 0 || sectionIndex >= sections.length` kontrolü → early return error |
| T-12-03-04 | Tampering | content_sections overwrite | mitigate | UPDATE `pkg.id` + `.eq('user_id', user.id)` — başka user'ın paketi güncellenemez |
| T-12-03-05 | Elevation of Privilege | locked bypass via saveContentSections | mitigate | `pkg.status !== 'locked'` kontrolü her üç action'da da mevcuttur |
</threat_model>

<verification>
1. `grep "export type ContentSection" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts`
2. `grep "export async function approveSection" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts`
3. `grep "export async function rejectSection" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts`
4. `grep "export async function saveContentSections" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts`
5. `grep "html_content" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts`
6. `grep "allApproved" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts`
7. `npx tsc --noEmit`
</verification>

<success_criteria>
- ContentSection tipi export edildi
- approveSection: onay + content kayıt + allApproved → html_content assembly
- rejectSection: status='rejected'
- saveContentSections: bulk stream sonrası kayıt
- Tüm action'lar: locked check + ownership verify + ikili revalidatePath
- `npx tsc --noEmit` hatasız
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/12-content-studio/12-03-SUMMARY.md` oluştur.
</output>
