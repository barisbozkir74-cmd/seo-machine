---
phase: 12-content-studio
plan: 04
type: execute
wave: 3
depends_on:
  - 12-02
  - 12-03
files_modified:
  - src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/page.tsx
  - src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx
autonomous: true
requirements:
  - CONT-01

must_haves:
  truths:
    - "GET /projeler/[id]/icerik-studio/[pageId] erişilebilir ve paket verilerini SSR ile yükler"
    - "Kilitli olmayan paket için route uyarı gösterir, notFound() çağırmaz"
    - "Sayfa paketi listesinde locked paketler için 'İçerik Üret' link butonu görünür"
    - "Kilitli olmayan paketlerde 'İçerik Üret' butonu render edilmez"
    - "SSR sorgusu: page_packages'ten content_sections, html_content, heading_hierarchy dahil edilir"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/page.tsx"
      provides: "Content Studio SSR route — paket ve proje verilerini yükler, ContentStudioShell'e geçer"
      exports: ["default (async function IcerikStudioPage)"]
    - path: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx"
      provides: "Sayfa listesinde locked paket için 'İçerik Üret' link butonu (additive change)"
  key_links:
    - from: "sayfa-paketi/page.tsx list item"
      to: "/projeler/[id]/icerik-studio/[pageId]"
      via: "Link href — pkg?.status === 'locked' koşuluyla"
      pattern: "icerik-studio"
    - from: "icerik-studio/page.tsx"
      to: "ContentStudioShell (client component)"
      via: "SSR props geçişi"
      pattern: "ContentStudioShell"
---

<objective>
Content Studio SSR route dosyasını oluştur ve sayfa paketi listesine giriş noktası ekle.

Purpose: Kullanıcının `/projeler/[id]/icerik-studio/[pageId]` adresine gidebilmesi için SSR page.tsx gerekli. Sayfa paketi listesindeki "İçerik Üret" butonu bu route'a yönlendirir.
Output: 
- `src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/page.tsx` (yeni SSR route)
- `src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx` (additive: "İçerik Üret" link eklendi)
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-content-studio/12-CONTEXT.md
@.planning/phases/12-content-studio/12-PATTERNS.md

<interfaces>
<!-- Analog: rakipler/[competitorId]/page.tsx — dual-param nested route pattern -->
// Params shape:
export default async function IcerikStudioPage({
  params,
}: {
  params: Promise<{ id: string; pageId: string }>
}) {
  const { id, pageId } = await params
  // ...
}

<!-- page_packages select sorgusu — icerik-studio için gerekli alanlar -->
supabase.from('page_packages')
  .select('id, status, seo_title, meta_description, h1, focus_keyword_id, search_intent, page_type, strategic_purpose, heading_hierarchy, content_sections, html_content')
  .eq('page_id', pageId)
  .eq('project_id', id)
  .eq('user_id', user.id)
  .single()

<!-- Kilitli olmayan paket için uyarı (notFound() değil) — CONTEXT.md D-01 -->
Kilitli olmayan paket → inline uyarı + sayfa paketi sayfasına geri link:
"Bu sayfa paketi henüz kilitlenmemiş. İçerik üretmek için önce paketi kilitleyin."

<!-- sayfa-paketi/page.tsx — additive change (12-PATTERNS.md) -->
Mevcut: <PackageStatusBadge status={pkg?.status ?? null} />
Eklenecek (hemen sonrasına):
{pkg?.status === 'locked' && (
  <Link
    href={`/projeler/${id}/icerik-studio/${page.id}`}
    className="text-xs px-2 py-0.5 rounded bg-secondary border border-border text-muted-foreground hover:text-foreground hover:border-foreground/30 transition-colors"
    onClick={(e) => e.stopPropagation()}
  >
    İçerik Üret
  </Link>
)}

<!-- ContentStudioShell — Plan 12-05'te oluşturulacak client component -->
// page.tsx içinde placeholder olarak import et:
// import { ContentStudioShell } from './components/ContentStudioShell'
// Props tipi (page.tsx içinde tanımla):
type ContentStudioShellProps = {
  projectId: string
  pageId: string
  pageTitle: string
  pkg: PackageData
  resolvedRules: Record<string, boolean>
}

<!-- resolvedRules pattern — sayfa-paketi/page.tsx'ten aynı pattern -->
// Global + project override rules query (tam implementasyon sayfa-paketi/page.tsx 51-80 satırlar)
</interfaces>

<!-- heading_hierarchy parse helper (client'ta değil SSR'da değil — client component içinde olacak,
     ama page.tsx heading_hierarchy raw JSONB'yi prop olarak geçer) -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: icerik-studio/[pageId]/page.tsx — SSR route oluştur</name>
  <files>src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/rakipler/[competitorId]/page.tsx (tam dosya — dual-param route pattern)
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx (resolvedRules pattern, lines 51-80)
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts (ContentSection tipi — import için)
    - src/lib/rules/rule-meta.ts (RULE_META import)
  </read_first>
  <action>
`src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/` dizinini oluştur ve içinde `page.tsx` dosyasını yaz.

**Dosya spesifikasyonu:**

```typescript
import { notFound } from 'next/navigation'
import Link from 'next/link'
import { createClient } from '@/lib/supabase/server'
import { RULE_META } from '@/lib/rules/rule-meta'
import { ProjectNav } from '../../ProjectNav'
import { ContentStudioShell } from './components/ContentStudioShell'

// PackageData tipi — ContentStudioShell'e geçilecek
type PackageData = {
  id: string
  status: string
  seo_title: string | null
  meta_description: string | null
  h1: string | null
  focus_keyword_id: string | null
  search_intent: string | null
  page_type: string | null
  strategic_purpose: string | null
  heading_hierarchy: unknown
  content_sections: unknown
  html_content: string | null
}

export default async function IcerikStudioPage({
  params,
}: {
  params: Promise<{ id: string; pageId: string }>
}) {
  const { id, pageId } = await params
  const supabase = await createClient()

  const { data: { user } } = await supabase.auth.getUser()
  if (!user) notFound()

  // Proje sahipliği
  const { data: project } = await supabase
    .from('projects')
    .select('id, name, domain, sector, brand_tone, target_language')
    .eq('id', id)
    .eq('user_id', user.id)
    .single()
  if (!project) notFound()

  // Sayfa
  const { data: page } = await supabase
    .from('pages')
    .select('id, title, slug, page_type, focus_keyword_id')
    .eq('id', pageId)
    .eq('project_id', id)
    .eq('user_id', user.id)
    .single()
  if (!page) notFound()

  // Page package — content_sections + html_content dahil
  const { data: pkg } = await supabase
    .from('page_packages')
    .select('id, status, seo_title, meta_description, h1, focus_keyword_id, search_intent, page_type, strategic_purpose, heading_hierarchy, content_sections, html_content')
    .eq('page_id', pageId)
    .eq('project_id', id)
    .eq('user_id', user.id)
    .single()

  // Paket yoksa sayfa paketi sayfasına yönlendir
  if (!pkg) {
    return (
      <div className="flex flex-col h-screen">
        <div className="p-8">
          <Link href={`/projeler/${id}/sayfa-paketi?page=${pageId}`}
            className="text-sm text-muted-foreground hover:text-foreground flex items-center gap-1 mb-4">
            ← Sayfa Paketi
          </Link>
          <p className="text-sm text-muted-foreground">Bu sayfa için henüz paket oluşturulmamış.</p>
        </div>
      </div>
    )
  }

  // Rules — sayfa-paketi/page.tsx ile aynı pattern (global + project override)
  const { data: globalRulesData } = await supabase
    .from('rules').select('rule_key, rule_value')
    .eq('user_id', user.id).eq('scope', 'global').is('project_id', null)
  const { data: projectRulesData } = await supabase
    .from('rules').select('rule_key, rule_value')
    .eq('user_id', user.id).eq('project_id', id).eq('scope', 'project')

  const projectOverrides = Object.fromEntries((projectRulesData ?? []).map((r) => [r.rule_key, r.rule_value]))
  const globalValues = Object.fromEntries((globalRulesData ?? []).map((r) => [r.rule_key, r.rule_value]))
  const resolvedRules: Record<string, boolean> = Object.fromEntries(
    Object.keys(RULE_META).map((ruleKey) => {
      const hasOverride = ruleKey in projectOverrides
      const value = hasOverride ? projectOverrides[ruleKey] === 'true' : (globalValues[ruleKey] ?? 'true') === 'true'
      return [ruleKey, value]
    })
  )

  // Kilitli olmayan paket için uyarı (notFound() değil — kullanıcı paket durumunu görür)
  if (pkg.status !== 'locked') {
    return (
      <div className="flex flex-col h-screen">
        <div className="p-8 pb-4 shrink-0">
          <Link href={`/projeler/${id}/sayfa-paketi?page=${pageId}`}
            className="text-sm text-muted-foreground hover:text-foreground flex items-center gap-1 mb-4">
            ← Sayfa Paketi
          </Link>
          <h1 className="text-xl font-semibold">{page.title} — İçerik Stüdyosu</h1>
        </div>
        <div className="flex flex-1 min-h-0">
          <div className="w-64 shrink-0 border-r border-border overflow-y-auto p-4">
            <ProjectNav projectId={id} activePath={`/projeler/${id}/sayfa-paketi`} />
          </div>
          <div className="flex-1 min-w-0 overflow-y-auto p-8">
            <div className="p-4 rounded-md border border-border bg-secondary/30 text-sm text-muted-foreground">
              Bu sayfa paketi henüz kilitlenmemiş. İçerik üretmek için önce paketi kilitleyin.
            </div>
          </div>
        </div>
      </div>
    )
  }

  return (
    <div className="flex flex-col h-screen">
      {/* ContentStudioShell — Plan 12-05'te implement edilecek client component */}
      <ContentStudioShell
        projectId={id}
        pageId={pageId}
        pageTitle={page.title}
        pkg={pkg as PackageData}
        resolvedRules={resolvedRules}
      />
    </div>
  )
}
```

**Dizin oluşturma:**
`src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/` dizini yoksa oluştur.
`src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/` alt dizinini de oluştur (Plan 12-05 bileşenler buraya gidecek).

**ContentStudioShell placeholder:** Plan 12-05 bu bileşeni oluşturacak. Şimdilik `components/ContentStudioShell.tsx` dosyasını şu içerikle oluştur (placeholder):
```typescript
'use client'
// TODO: Plan 12-05 — tam implementasyon
export type { ContentStudioShellProps }
type ContentStudioShellProps = {
  projectId: string
  pageId: string
  pageTitle: string
  pkg: unknown
  resolvedRules: Record<string, boolean>
}
export function ContentStudioShell(_props: ContentStudioShellProps) {
  return <div className="p-8 text-sm text-muted-foreground">İçerik Stüdyosu yükleniyor...</div>
}
```
  </action>
  <verify>
    `ls src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/page.tsx` — dosya mevcut.
    `ls src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/ContentStudioShell.tsx` — placeholder mevcut.
    `grep "status !== 'locked'" src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/page.tsx` — lock guard var.
    `grep "content_sections" src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/page.tsx` — sorgu içerisinde.
    `grep "html_content" src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/page.tsx` — sorgu içerisinde.
    `npx tsc --noEmit` — hatasız.
  </verify>
  <done>
    Content Studio SSR route oluşturuldu:
    - Auth + ownership + page + pkg sorguları
    - Paket yoksa: uyarı render
    - Kilitli değilse: "kilitleyin" uyarı render
    - Kilitliyse: ContentStudioShell'e props geçişi
    - ContentStudioShell placeholder bileşeni oluşturuldu (Plan 12-05 implement edecek)
  </done>
</task>

<task type="auto">
  <name>Task 2: sayfa-paketi/page.tsx — "İçerik Üret" link butonu ekle (additive)</name>
  <files>src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx (tam mevcut dosya — önce oku)
  </read_first>
  <action>
Mevcut `sayfa-paketi/page.tsx` dosyasını oku. Sadece sayfa listesindeki `<li>` item içinde `<PackageStatusBadge />` render eden satırı bul ve hemen ardından `İçerik Üret` link ekle.

**Ekleme konumu:** `<PackageStatusBadge status={pkg?.status ?? null} />` satırından hemen sonra.

**Eklenecek kod:**
```typescript
{pkg?.status === 'locked' && (
  <Link
    href={`/projeler/${id}/icerik-studio/${page.id}`}
    className="text-xs px-2 py-0.5 rounded bg-secondary border border-border text-muted-foreground hover:text-foreground hover:border-foreground/30 transition-colors"
    onClick={(e) => e.stopPropagation()}
  >
    İçerik Üret
  </Link>
)}
```

**Önemli:** `Link` Next.js'ten zaten import edilmiş (`import Link from 'next/link'`). Yeni import ekleme — sadece JSX ekle.
Başka hiçbir şeyi değiştirme. Yapısal değişiklik yok.
  </action>
  <verify>
    `grep "icerik-studio" src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx` — link mevcut.
    `grep "İçerik Üret" src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx` — buton metni mevcut.
    `grep "status === 'locked'" src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx` — koşullu render var.
    `npx tsc --noEmit` — hatasız.
  </verify>
  <done>
    sayfa-paketi/page.tsx'te sayfa listesi item'larına additive olarak "İçerik Üret" linki eklendi.
    Yalnızca status='locked' paketlerde görünür. Mevcut davranış değiştirilmedi.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL param pageId → SSR DB query | pageId URL'den geliyor; ownership check olmadan başka sayfaların verisine erişilebilir |
| SSR → ContentStudioShell | Server component'ten client component'e JSONB props geçişi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Information Disclosure | pageId IDOR | mitigate | Her sorgu `.eq('project_id', id).eq('user_id', user.id)` üçlü filtre; başka kullanıcının sayfası görüntülenemez |
| T-12-04-02 | Spoofing | unauthenticated route access | mitigate | `supabase.auth.getUser()` + `if (!user) notFound()` — redirect to auth |
| T-12-04-03 | Elevation of Privilege | locked check bypass | mitigate | `pkg.status !== 'locked'` koşulu uyarı render eder; ContentStudioShell render edilmez |
</threat_model>

<verification>
1. `ls src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/page.tsx`
2. `grep "status !== 'locked'" src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/page.tsx`
3. `grep "content_sections" src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/page.tsx`
4. `grep "icerik-studio" src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx`
5. `grep "İçerik Üret" src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx`
6. `npx tsc --noEmit`
</verification>

<success_criteria>
- `icerik-studio/[pageId]/page.tsx` oluşturuldu — auth + ownership + pkg sorgusu + lock check
- Kilitli olmayan paket: uyarı mesajı render edilir
- Kilitli paket: ContentStudioShell'e doğru props geçilir
- sayfa-paketi/page.tsx'te locked paketler için "İçerik Üret" link butonu eklendi
- `npx tsc --noEmit` hatasız
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/12-content-studio/12-04-SUMMARY.md` oluştur.
</output>
