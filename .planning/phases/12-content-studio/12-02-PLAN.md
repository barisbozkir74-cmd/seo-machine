---
phase: 12-content-studio
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/app/api/ai/generate-section/route.ts
autonomous: true
requirements:
  - CONT-02

must_haves:
  truths:
    - "POST /api/ai/generate-section unauthenticated çağrıları 401 ile reddeder"
    - "Kilitli olmayan paket için istek 403 döner"
    - "Sahipsiz proje/sayfa için 404 döner"
    - "Başarılı istek streamed plain-text döner (Content-Type: text/plain)"
    - "Stream model: claude-sonnet-4-6"
    - "Prompt D-03 kurallarına göre bölüm context + proje kuralları içerir"
  artifacts:
    - path: "src/app/api/ai/generate-section/route.ts"
      provides: "Bölüm bazlı streaming AI içerik üretim endpoint'i"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/ai/generate-section/route.ts"
      to: "Anthropic claude-sonnet-4-6"
      via: "client.messages.stream()"
      pattern: "client\\.messages\\.stream"
    - from: "src/app/api/ai/generate-section/route.ts"
      to: "page_packages (status=locked check)"
      via: "supabase.from('page_packages')"
      pattern: "status.*locked"
---

<objective>
Content Studio bölüm üretimi için streaming API route oluştur.

Purpose: Her bölüm kartı bu endpoint'e POST atar ve SSE-style ReadableStream üzerinden token alır. Hem "Tümünü Üret" paralel çağrıları hem de tekil "Yeniden Üret" bu route'u kullanır.
Output: `src/app/api/ai/generate-section/route.ts` — tam çalışır streaming endpoint.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-content-studio/12-CONTEXT.md
@.planning/phases/12-content-studio/12-PATTERNS.md

<interfaces>
<!-- Canonical streaming pattern — src/app/api/ai/generate-page-package/route.ts -->
```typescript
import { NextRequest } from 'next/server'
import Anthropic from '@anthropic-ai/sdk'
import { createClient } from '@/lib/supabase/server'

const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })

export async function POST(req: NextRequest) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return new Response('Unauthorized', { status: 401 })
  // ...body parse, ownership check, lock check...
  const stream = await client.messages.stream({
    model: 'claude-sonnet-4-6',
    max_tokens: 4000,
    messages: [{ role: 'user', content: prompt }],
  })
  const encoder = new TextEncoder()
  const readable = new ReadableStream({
    async start(controller) {
      for await (const chunk of stream) {
        if (chunk.type === 'content_block_delta' && chunk.delta.type === 'text_delta') {
          controller.enqueue(encoder.encode(chunk.delta.text))
        }
      }
      controller.close()
    },
  })
  return new Response(readable, {
    headers: {
      'Content-Type': 'text/plain; charset=utf-8',
      'Transfer-Encoding': 'chunked',
      'Cache-Control': 'no-cache',
    },
  })
}
```

<!-- D-03 context injection rules (12-CONTEXT.md) -->
Paralel üretim bölüm context:
- project: name, domain, sector, brand_tone, target_language
- page package: seo_title, meta_description, h1, focus_keyword, search_intent, page_type, strategic_purpose
- heading_hierarchy (tam yapı)
- hedef H2 bloğu (hangi bölüm üretileceği — sectionIndex ile eşleşen)
- proje kuralları (aktif SEO kuralları — RULE_META)

Yeniden üretim ek context:
- approvedSections: o ana kadar approved olan bölümlerin heading + content'i

<!-- generate-section body shape (12-PATTERNS.md) -->
Body: { projectId, pageId, sectionIndex, headingHierarchy, approvedSections?, isRegenerate? }
- sectionIndex: hangi H2 bloğunun üretileceği (0-indexed)
- headingHierarchy: client'ın parse ettiği heading yapısı (JSONB)
- approvedSections: isRegenerate=true ise client approved section listesini gönderir

<!-- Rules query pattern — src/lib/rules/rule-meta.ts -->
import { RULE_META } from '@/lib/rules/rule-meta'
// Kural key'leri: title_starts_with_keyword, h1_exact_match, slug_exact_match, vb.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: generate-section/route.ts — streaming endpoint oluştur</name>
  <files>src/app/api/ai/generate-section/route.ts</files>
  <read_first>
    - src/app/api/ai/generate-page-package/route.ts (canonical streaming pattern — tam dosya)
    - src/lib/rules/rule-meta.ts (RULE_META export — kural key'leri)
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts (verifyOwnership helper signature)
  </read_first>
  <action>
Aşağıdaki spesifikasyona göre `src/app/api/ai/generate-section/route.ts` oluştur:

**Imports:**
```typescript
import { NextRequest } from 'next/server'
import Anthropic from '@anthropic-ai/sdk'
import { createClient } from '@/lib/supabase/server'
import { RULE_META } from '@/lib/rules/rule-meta'

const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })
```

**Body type:**
```typescript
type GenerateSectionBody = {
  projectId: string
  pageId: string
  sectionIndex: number
  headingHierarchy: Array<{ level: string; text: string }>
  approvedSections?: Array<{ heading: string; content: string }>
  isRegenerate?: boolean
}
```

**POST handler logic (sırayla):**

1. Auth check: `supabase.auth.getUser()` → 401 if no user
2. Body parse: `req.json()` wrapped in try/catch → 400 on invalid JSON
3. Input validation: projectId, pageId, sectionIndex (number), headingHierarchy (array) hepsi required → 400 if missing
4. Project ownership: `supabase.from('projects').select('id, name, domain, sector, target_language, brand_tone').eq('id', projectId).eq('user_id', user.id).single()` → 404 if not found
5. Page query: `supabase.from('pages').select('id, title, slug, page_type, focus_keyword_id').eq('id', pageId).eq('project_id', projectId).eq('user_id', user.id).single()` → 404 if not found
6. Package lock check: `supabase.from('page_packages').select('id, status, seo_title, meta_description, h1, search_intent, strategic_purpose').eq('page_id', pageId).eq('project_id', projectId).eq('user_id', user.id).single()`
   → `if (!pkg || pkg.status !== 'locked') return new Response('Package must be locked to generate content.', { status: 403 })`
7. Focus keyword: `supabase.from('keywords').select('keyword').eq('id', page.focus_keyword_id).single()` — skip if no focus_keyword_id
8. Rules query: global + project override pattern (aşağıdaki tam kod):
```typescript
const { data: globalRulesData } = await supabase
  .from('rules').select('rule_key, rule_value')
  .eq('user_id', user.id).eq('scope', 'global').is('project_id', null)
const { data: projectRulesData } = await supabase
  .from('rules').select('rule_key, rule_value')
  .eq('user_id', user.id).eq('project_id', projectId).eq('scope', 'project')
const projectOverrides = Object.fromEntries((projectRulesData ?? []).map(r => [r.rule_key, r.rule_value]))
const globalValues = Object.fromEntries((globalRulesData ?? []).map(r => [r.rule_key, r.rule_value]))
const resolvedRules: Record<string, boolean> = Object.fromEntries(
  Object.keys(RULE_META).map((ruleKey) => {
    const hasOverride = ruleKey in projectOverrides
    const value = hasOverride ? projectOverrides[ruleKey] === 'true' : (globalValues[ruleKey] ?? 'true') === 'true'
    return [ruleKey, value]
  })
)
```
9. Build prompt: `buildSectionPrompt()` helper fonksiyonu (aşağıda)
10. Stream: `generate-page-package/route.ts` ile birebir aynı `client.messages.stream()` + `ReadableStream` pattern, `max_tokens: 2000`
11. Return streaming response with identical headers

**`buildSectionPrompt()` fonksiyonu:**
```typescript
function buildSectionPrompt({
  project, pkg, page, focusKeyword, headingHierarchy, sectionIndex, approvedSections, resolvedRules
}: {
  project: { name: string; domain: string; sector: string | null; brand_tone: string | null; target_language: string | null }
  pkg: { seo_title: string | null; meta_description: string | null; h1: string | null; search_intent: string | null; strategic_purpose: string | null; page_type: string | null }
  page: { title: string; page_type: string | null }
  focusKeyword: string
  headingHierarchy: Array<{ level: string; text: string }>
  sectionIndex: number
  approvedSections?: Array<{ heading: string; content: string }>
  resolvedRules: Record<string, boolean>
}): string

// Prompt içeriği (Türkçe, SEO odaklı):
// - Proje: domain, sektör, marka tonu, hedef dil
// - Sayfa paketi: seo_title, h1, meta_description, search_intent, strategic_purpose
// - Focus keyword
// - Tüm heading_hierarchy (sayfa yapısı bağlamı)
// - Hedef bölüm: headingHierarchy[sectionIndex].text (bu bölümü üret)
// - Eğer approvedSections varsa: "Önceden onaylanan bölümler (akış tutarlılığı için):" listesi
// - Aktif SEO kuralları: resolvedRules'dan true olan key'leri listele (örn: "SEO title focus keyword ile başlamalı")
// - Görev: Sadece bu bölüm için akıcı, SEO odaklı, marka tonuna uygun Türkçe içerik üret.
//   JSON değil, düz metin. Başlık tekrar etme. Yalnızca paragraf içeriği.
// - max_tokens: 2000 (bölüm başına ~400-600 kelime yeterli)
```

**Prompt injection güvenlik notu:** Kullanıcı kontrollü `brand_tone`, `sector`, `strategic_purpose` alanları prompt'a eklenir. Bu alanları prompt içinde `---` ile sınırlandırılmış bloklar içine al; kullanıcı içeriği talimat bölümünden ayrılsın.

Dosya oluşturulacak dizin: `src/app/api/ai/generate-section/` — dizini mkdir ile oluştur.
  </action>
  <verify>
    `ls src/app/api/ai/generate-section/route.ts` — dosya mevcut.
    `grep "client.messages.stream" src/app/api/ai/generate-section/route.ts` — stream çağrısı var.
    `grep "status !== 'locked'" src/app/api/ai/generate-section/route.ts` — lock guard var.
    `grep "status: 401" src/app/api/ai/generate-section/route.ts` — auth guard var.
    `grep "claude-sonnet-4-6" src/app/api/ai/generate-section/route.ts` — doğru model.
    `grep "RULE_META" src/app/api/ai/generate-section/route.ts` — rules injection var.
    TypeScript: `npx tsc --noEmit` hatasız tamamlanmalı.
  </verify>
  <done>
    POST /api/ai/generate-section:
    - 401: unauthenticated
    - 400: eksik body fields
    - 403: pkg.status !== 'locked'
    - 404: project/page not found
    - 200: ReadableStream, Content-Type: text/plain; charset=utf-8
    Model: claude-sonnet-4-6, max_tokens: 2000
    Prompt: proje + paket + kurallar + heading yapısı + hedef bölüm + (isteğe bağlı approved sections)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/ai/generate-section | Kullanıcı kontrollü body; projectId/pageId IDOR riski |
| user content → Anthropic prompt | brand_tone, sector, strategic_purpose prompt'a ekleniyor — injection riski |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Spoofing | /api/ai/generate-section | mitigate | `supabase.auth.getUser()` ile session doğrulanır; 401 unauthenticated |
| T-12-02-02 | Information Disclosure | IDOR (projectId/pageId) | mitigate | Her sorgu `.eq('user_id', user.id)` içerir; başka kullanıcının verisi okunamaz |
| T-12-02-03 | Elevation of Privilege | Lock bypass | mitigate | `pkg.status !== 'locked'` kontrolü 403 döner; kilitli olmayan paket için üretim engellenir |
| T-12-02-04 | Tampering | Prompt injection | mitigate | Kullanıcı kontrollü alanlar (brand_tone, sector vb.) `---` sınırlı bloklara alınır; talimat bölümünden izole edilir |
| T-12-02-05 | Denial of Service | Paralel stream calls | accept | max_tokens: 2000; Anthropic rate limit mevcuttur; dahili araç, düşük risk |
</threat_model>

<verification>
1. `ls src/app/api/ai/generate-section/route.ts` — dosya mevcut
2. `grep -n "status: 401" src/app/api/ai/generate-section/route.ts` — auth guard satırı
3. `grep -n "status !== 'locked'" src/app/api/ai/generate-section/route.ts` — lock guard satırı
4. `grep -n "claude-sonnet-4-6" src/app/api/ai/generate-section/route.ts` — model sabit
5. `grep -n "RULE_META" src/app/api/ai/generate-section/route.ts` — rules import
6. `npx tsc --noEmit` — TypeScript hatasız
</verification>

<success_criteria>
- `src/app/api/ai/generate-section/route.ts` oluşturuldu
- POST handler: 401 (unauth), 400 (bad body), 403 (not locked), 404 (not found), 200 (stream)
- Model: claude-sonnet-4-6 (STATE.md'de kilitli)
- Prompt D-03 spesifikasyonuna uygun (proje + paket + kurallar + heading + hedef bölüm)
- `npx tsc --noEmit` hatasız
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/12-content-studio/12-02-SUMMARY.md` oluştur.
</output>
