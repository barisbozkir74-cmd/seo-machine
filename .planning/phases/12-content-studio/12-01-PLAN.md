---
phase: 12-content-studio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260426000001_add_content_studio_columns.sql
autonomous: true
requirements:
  - CONT-04
  - CONT-05

must_haves:
  truths:
    - "page_packages tablosu content_sections JSONB sütununa sahip"
    - "page_packages tablosu html_content TEXT sütununa sahip"
    - "Canlı veritabanı schema değişikliğini yansıtıyor (runtime failure olmaz)"
  artifacts:
    - path: "supabase/migrations/20260426000001_add_content_studio_columns.sql"
      provides: "content_sections ve html_content sütunlarını ekleyen migration"
      contains: "ADD COLUMN IF NOT EXISTS content_sections"
  key_links:
    - from: "supabase/migrations/20260426000001_add_content_studio_columns.sql"
      to: "canlı veritabanı page_packages tablosu"
      via: "supabase db push"
      pattern: "content_sections|html_content"
---

<objective>
page_packages tablosuna Content Studio için iki yeni sütun ekle ve canlı veritabanına uygula.

Purpose: Sonraki tüm planlar bu sütunlara yazar/okur. DB push olmadan runtime'da "column does not exist" hatası alınır. Bu plan diğer tüm planlar için engelleyicidir (BLOCKING).
Output: Migration SQL dosyası + canlı DB'de iki yeni sütun.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-content-studio/12-CONTEXT.md

Mevcut migration pattern referansı:
Dosya: supabase/migrations/20260425000001_add_schema_jsonld.sql
İçerik:
```sql
ALTER TABLE public.page_packages
  ADD COLUMN IF NOT EXISTS schema_jsonld JSONB;
```

D-04 (CONTEXT.md): page_packages tablosuna eklenmesi gereken sütunlar:
  ADD COLUMN content_sections JSONB  — bölüm durumlarını ve içeriklerini tutan JSONB dizisi
  ADD COLUMN html_content TEXT       — tüm bölümler onaylandığında birleştirilen WordPress-ready HTML
</context>

<tasks>

<task type="auto">
  <name>Task 1: content_sections + html_content migration SQL dosyasını oluştur</name>
  <files>supabase/migrations/20260426000001_add_content_studio_columns.sql</files>
  <read_first>
    - supabase/migrations/20260425000001_add_schema_jsonld.sql (exact ALTER TABLE pattern)
    - supabase/migrations/20260424000005_create_page_packages.sql (mevcut page_packages yapısı, sütun adı çakışması yok mu kontrol)
  </read_first>
  <action>
Aşağıdaki içerikle dosyayı oluştur — pattern: mevcut add_schema_jsonld.sql ile birebir aynı ALTER TABLE yapısı:

```sql
-- Phase 12: Content Studio — section-by-section AI content generation
-- Adds two columns to page_packages:
--   content_sections JSONB: array of section objects with heading, level, sub_headings, content, status
--   html_content TEXT: WordPress-ready assembled HTML (populated when all sections approved)
ALTER TABLE public.page_packages
  ADD COLUMN IF NOT EXISTS content_sections JSONB,
  ADD COLUMN IF NOT EXISTS html_content     TEXT;
```

Hiçbir RLS değişikliği yapma — mevcut page_packages UPDATE policy (USING + WITH CHECK) bu sütunları otomatik kapsar.
  </action>
  <verify>
    SQL dosyası oluşturuldu: `ls supabase/migrations/20260426000001_add_content_studio_columns.sql` çıktı vermeli.
    İçerik doğrulaması: dosya `ADD COLUMN IF NOT EXISTS content_sections JSONB` satırını içermeli.
    İçerik doğrulaması: dosya `ADD COLUMN IF NOT EXISTS html_content` satırını içermeli.
  </verify>
  <done>Migration SQL dosyası oluşturuldu; ADD COLUMN IF NOT EXISTS ile hem content_sections JSONB hem html_content TEXT ekleniyor.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] supabase db push — migration'ı canlı DB'ye uygula</name>
  <files></files>
  <read_first>
    - supabase/migrations/20260426000001_add_content_studio_columns.sql (push edilecek dosyayı doğrula)
  </read_first>
  <action>
Proje kökünden aşağıdaki komutu çalıştır:

```bash
supabase db push
```

Komut interaktif prompt isteyebilir (remote DB onayı). Eğer non-TTY ortamda `SUPABASE_ACCESS_TOKEN` env var mevcut değilse ve interaktif prompt gerekiyorsa, bu task `autonomous: false` olarak işaretle ve kullanıcıya:
"Terminalde `supabase db push` komutunu çalıştırın — phase 12 içerik sütunlarını DB'ye uygular."

Push başarılı ise migration dosyası supabase migration history'e eklenir.
  </action>
  <verify>
    Başarı: `supabase db push` çıktısında "Migration 20260426000001_add_content_studio_columns.sql applied" veya "All migrations are up to date" mesajı.
    Alternatif doğrulama: `supabase migration list` ile migration'ın applied listesinde görünmesi.
  </verify>
  <done>Migration canlı DB'ye uygulandı; page_packages tablosunda content_sections ve html_content sütunları mevcuttur. Sonraki planlar (12-02 ve 12-03) bu task tamamlanmadan başlayamaz.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live DB | Migration SQL Supabase'e push edilir; yanlış yazılan SQL production verilerini etkileyebilir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | migration SQL | mitigate | `ADD COLUMN IF NOT EXISTS` kullanılır — idempotent; var olan sütunu silmez |
| T-12-01-02 | Denial of Service | supabase db push | accept | Migration sadece iki sütun ekliyor; table lock süresi ihmal edilebilir |
</threat_model>

<verification>
1. `ls supabase/migrations/20260426000001_add_content_studio_columns.sql` — dosya mevcut
2. `grep "content_sections" supabase/migrations/20260426000001_add_content_studio_columns.sql` — eşleşme var
3. `grep "html_content" supabase/migrations/20260426000001_add_content_studio_columns.sql` — eşleşme var
4. `supabase migration list` — migration applied olarak görünür
</verification>

<success_criteria>
- Migration dosyası oluşturuldu: `supabase/migrations/20260426000001_add_content_studio_columns.sql`
- Dosya `ADD COLUMN IF NOT EXISTS content_sections JSONB` içeriyor
- Dosya `ADD COLUMN IF NOT EXISTS html_content TEXT` içeriyor
- `supabase db push` başarıyla tamamlandı (no errors)
- Canlı veritabanında page_packages tablosu artık content_sections ve html_content sütunlarına sahip
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/12-content-studio/12-01-SUMMARY.md` oluştur.
</output>
