---
phase: 06-keyword-clustering-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/keywords/clustering.ts
  - src/lib/keywords/scoring.ts
  - src/lib/keywords/scoring.test.ts
  - src/lib/keywords/clustering.test.ts
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts
  - package.json
autonomous: true
requirements:
  - KEYW-04
  - KEYW-05
  - KEYW-06

must_haves:
  truths:
    - "clusterAndScoreKeywords Server Action, enriched keyword'leri intent-first hibrid algoritma ile kümelere böler ve DB'ye yazar"
    - "Her keyword için opportunity_score (0-100) hesaplanır ve keywords tablosuna yazılır"
    - "moveKeywordToCluster, keyword'ü yeni cluster'a taşır; eski cluster_id otomatik ezilir (cannibalization prevention)"
    - "setPrimaryKeyword, cluster'ın primary_keyword_id'sini günceller; keyword.cluster_id doğrulaması yapılır"
    - "Her action getUser() + project_id + user_id ownership doğrulaması yapar"
    - "DoS guard: keyword count > 500 ise clusterAndScoreKeywords erken döner"
  artifacts:
    - path: "src/lib/keywords/scoring.ts"
      provides: "calculateOpportunityScore ve buildScoringContext — pure functions"
      exports: ["calculateOpportunityScore", "buildScoringContext", "INTENT_MULTIPLIERS"]
    - path: "src/lib/keywords/clustering.ts"
      provides: "clusterEnrichedKeywords — intent-first hibrid algoritma"
      exports: ["clusterEnrichedKeywords", "ClusterInput", "ClusterResult"]
    - path: "src/lib/keywords/scoring.test.ts"
      provides: "Vitest birim testleri — KEYW-06"
      contains: "calculateOpportunityScore"
    - path: "src/lib/keywords/clustering.test.ts"
      provides: "Vitest birim testleri — KEYW-04, KEYW-05"
      contains: "clusterEnrichedKeywords"
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts"
      provides: "clusterAndScoreKeywords, moveKeywordToCluster, setPrimaryKeyword Server Actions"
      exports: ["clusterAndScoreKeywords", "moveKeywordToCluster", "setPrimaryKeyword"]
  key_links:
    - from: "actions.ts clusterAndScoreKeywords"
      to: "src/lib/keywords/clustering.ts clusterEnrichedKeywords"
      via: "import { clusterEnrichedKeywords } from '@/lib/keywords/clustering'"
      pattern: "clusterEnrichedKeywords\\("
    - from: "actions.ts clusterAndScoreKeywords"
      to: "src/lib/keywords/scoring.ts calculateOpportunityScore"
      via: "import { calculateOpportunityScore, buildScoringContext } from '@/lib/keywords/scoring'"
      pattern: "calculateOpportunityScore\\("
    - from: "actions.ts clusterAndScoreKeywords"
      to: "keyword_clusters table"
      via: "supabase.from('keyword_clusters').upsert(...)"
      pattern: "keyword_clusters.*upsert"
    - from: "actions.ts clusterAndScoreKeywords"
      to: "keywords.opportunity_score column"
      via: "supabase.from('keywords').update({ cluster_id, opportunity_score })"
      pattern: "opportunity_score"
---

<objective>
Backend core: intent-first hibrid keyword clustering algoritması, opportunity score hesaplama fonksiyonları ve üç yeni Server Action (clusterAndScoreKeywords, moveKeywordToCluster, setPrimaryKeyword). Vitest kurulumu ve birim testleri dahil.

Purpose: Plan 02 (UI) bu plan'ın Server Action'larına bağlıdır — önce backend, sonra UI.
Output: scoring.ts (yeni), clustering.ts (genişletilmiş), actions.ts (genişletilmiş), vitest testleri, package.json (vitest devDep)
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-keyword-clustering-scoring/06-RESEARCH.md
@.planning/phases/06-keyword-clustering-scoring/06-PATTERNS.md

<interfaces>
<!-- Mevcut clustering.ts — genişletilecek -->
From src/lib/keywords/clustering.ts:
```typescript
import type { ParsedKeyword } from './parser'

export type Cluster = {
  name: string
  totalVolume: number
  keywords: ParsedKeyword[]
}

// overlapsWithCluster(keyword: string, headKeyword: string): boolean — KORUNACAK
// getSignificantWords(keyword: string): Set<string> — KORUNACAK
// clusterKeywords(keywords: ParsedKeyword[]): Cluster[] — KORUNACAK (importKeywords kullanıyor)
```

<!-- Mevcut actions.ts — ownership pattern -->
From src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts:
```typescript
'use server'
import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'

// Auth + ownership pattern (her yeni action'da kopyala):
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) return { success: false, error: 'Oturum bulunamadı.' }

const { data: project } = await supabase
  .from('projects').select('id')
  .eq('id', projectId).eq('user_id', user.id).single()
if (!project) return { success: false, error: 'Proje bulunamadı.' }

// Cluster upsert pattern:
await supabase.from('keyword_clusters').upsert(
  { user_id, project_id, cluster_name, total_volume },
  { onConflict: 'project_id,cluster_name', ignoreDuplicates: false }
).select('id').single()

// Keyword ownership pattern:
const { data: kw } = await supabase.from('keywords')
  .select('id, cluster_id')
  .eq('id', keywordId).eq('project_id', projectId).eq('user_id', user.id).single()
if (!kw) return { success: false, error: 'Keyword bulunamadı.' }

// revalidatePath (her mutating action sonunda):
revalidatePath(`/projeler/${projectId}/keyword-stratejisi`)
```

<!-- DB Schema -->
keywords tablosu: id UUID, user_id UUID, project_id UUID, keyword TEXT, volume INTEGER,
  cpc NUMERIC(10,2), difficulty INTEGER (0-100), search_intent TEXT, opportunity_score NUMERIC(5,2),
  enriched_at TIMESTAMPTZ, cluster_id UUID FK keyword_clusters, UNIQUE(project_id, keyword)

keyword_clusters tablosu: id UUID, user_id UUID, project_id UUID, cluster_name TEXT,
  primary_keyword_id UUID FK keywords ON DELETE SET NULL, intent TEXT, total_volume INTEGER,
  UNIQUE(project_id, cluster_name)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scoring.ts oluştur + vitest kurulumu + testler</name>
  <read_first>
    - src/lib/keywords/clustering.ts (pure utility pattern — kopyalanacak yapı)
    - package.json (vitest mevcut mu diye kontrol et)
    - .planning/phases/06-keyword-clustering-scoring/06-RESEARCH.md (Pattern 2: Opportunity Score Formula)
    - .planning/phases/06-keyword-clustering-scoring/06-PATTERNS.md (scoring.ts tam dosya pattern'ı)
  </read_first>

  <behavior>
    - calculateOpportunityScore({volume, cpc, difficulty, search_intent}, {maxVolume, maxCpc}) → number (0–100, 1 decimal)
    - Transactional intent, commercial'den yüksek skor alır (1.0 vs 0.85 çarpan)
    - difficulty=0 keyword, difficulty=100'den yüksek skor alır
    - Tüm null girdiler 0/50 default ile güvenli handle edilir
    - calculateOpportunityScore sonucu her zaman 0-100 arasındadır
    - buildScoringContext boş dizi için {maxVolume:0, maxCpc:0} döner
  </behavior>

  <action>
  **Adım 1 — Vitest kurulumu:**
  package.json'da "vitest" yoksa: `npm install -D vitest`
  vitest.config.ts yoksa oluştur:
  ```typescript
  import { defineConfig } from 'vitest/config'
  export default defineConfig({
    test: { environment: 'node' }
  })
  ```
  package.json scripts'e ekle: `"test": "vitest run"`

  **Adım 2 — src/lib/keywords/scoring.ts oluştur:**
  ```typescript
  // Pure math — no imports needed

  export const INTENT_MULTIPLIERS: Record<string, number> = {
    transactional: 1.0,
    commercial:    0.85,
    informational: 0.5,
    navigational:  0.3,
    unknown:       0.5,
  }

  function normalizeVolume(volume: number, maxVolume: number): number {
    if (maxVolume === 0) return 0
    return Math.min(volume / maxVolume, 1)
  }

  function normalizeCpc(cpc: number, maxCpc: number): number {
    if (maxCpc === 0) return 0
    return Math.min(cpc / maxCpc, 1)
  }

  export function calculateOpportunityScore(
    keyword: {
      volume: number | null
      cpc: number | null
      difficulty: number | null
      search_intent: string | null
    },
    context: { maxVolume: number; maxCpc: number }
  ): number {
    const volumeScore = normalizeVolume(keyword.volume ?? 0, context.maxVolume)
    const cpcScore    = normalizeCpc(keyword.cpc ?? 0, context.maxCpc)
    const kdScore     = (100 - (keyword.difficulty ?? 50)) / 100
    const intentMult  = INTENT_MULTIPLIERS[keyword.search_intent?.toLowerCase() ?? 'unknown'] ?? 0.5

    const raw = (volumeScore * 0.40) + (cpcScore * 0.25) + (kdScore * 0.15) + (intentMult * 0.20)
    return Math.round(raw * 100 * 10) / 10  // 0-100, 1 decimal
  }

  export function buildScoringContext(
    keywords: Array<{ volume: number | null; cpc: number | null }>
  ): { maxVolume: number; maxCpc: number } {
    return {
      maxVolume: Math.max(...keywords.map((k) => k.volume ?? 0), 0),
      maxCpc:    Math.max(...keywords.map((k) => k.cpc ?? 0), 0),
    }
  }
  ```

  **Adım 3 — src/lib/keywords/scoring.test.ts oluştur:**
  ```typescript
  import { describe, it, expect } from 'vitest'
  import { calculateOpportunityScore, buildScoringContext, INTENT_MULTIPLIERS } from './scoring'

  const ctx = { maxVolume: 10000, maxCpc: 5.0 }

  describe('calculateOpportunityScore', () => {
    it('returns a number between 0 and 100', () => {
      const score = calculateOpportunityScore(
        { volume: 5000, cpc: 2.5, difficulty: 30, search_intent: 'commercial' },
        ctx
      )
      expect(score).toBeGreaterThanOrEqual(0)
      expect(score).toBeLessThanOrEqual(100)
    })

    it('transactional scores higher than commercial for identical keyword data', () => {
      const base = { volume: 5000, cpc: 2.5, difficulty: 40 }
      const transactional = calculateOpportunityScore({ ...base, search_intent: 'transactional' }, ctx)
      const commercial    = calculateOpportunityScore({ ...base, search_intent: 'commercial' }, ctx)
      expect(transactional).toBeGreaterThan(commercial)
    })

    it('low difficulty scores higher than high difficulty', () => {
      const base = { volume: 5000, cpc: 2.5, search_intent: 'commercial' }
      const easy = calculateOpportunityScore({ ...base, difficulty: 10 }, ctx)
      const hard = calculateOpportunityScore({ ...base, difficulty: 90 }, ctx)
      expect(easy).toBeGreaterThan(hard)
    })

    it('handles all-null inputs without throwing', () => {
      const score = calculateOpportunityScore(
        { volume: null, cpc: null, difficulty: null, search_intent: null },
        { maxVolume: 0, maxCpc: 0 }
      )
      expect(score).toBeGreaterThanOrEqual(0)
      expect(score).toBeLessThanOrEqual(100)
    })

    it('returns 1 decimal precision', () => {
      const score = calculateOpportunityScore(
        { volume: 3333, cpc: 1.77, difficulty: 45, search_intent: 'informational' },
        ctx
      )
      expect(score.toString()).toMatch(/^\d+(\.\d)?$/)
    })
  })

  describe('buildScoringContext', () => {
    it('returns maxVolume and maxCpc from array', () => {
      const ctx = buildScoringContext([
        { volume: 1000, cpc: 2.0 },
        { volume: 5000, cpc: 1.5 },
        { volume: null, cpc: null },
      ])
      expect(ctx.maxVolume).toBe(5000)
      expect(ctx.maxCpc).toBe(2.0)
    })

    it('returns zeros for empty array', () => {
      const ctx = buildScoringContext([])
      expect(ctx.maxVolume).toBe(0)
      expect(ctx.maxCpc).toBe(0)
    })
  })
  ```

  **Adım 4 — RED → GREEN doğrula:**
  `npx vitest run src/lib/keywords/scoring.test.ts`
  Tüm testler geçmeli.
  </action>

  <verify>
    <automated>npx vitest run src/lib/keywords/scoring.test.ts --reporter=verbose</automated>
  </verify>

  <done>
    - scoring.ts mevcut, calculateOpportunityScore ve buildScoringContext export ediyor
    - scoring.test.ts mevcut, 7 test geçiyor
    - vitest kurulu (package.json devDependencies'de "vitest" var)
    - `npx vitest run src/lib/keywords/scoring.test.ts` çıkışı "all tests passed" veya "X passed"
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: clustering.ts genişlet — clusterEnrichedKeywords + testler</name>
  <read_first>
    - src/lib/keywords/clustering.ts (mevcut tam dosya — overlapsWithCluster ve getSignificantWords korunacak)
    - .planning/phases/06-keyword-clustering-scoring/06-PATTERNS.md (clusterEnrichedKeywords tam pattern)
    - .planning/phases/06-keyword-clustering-scoring/06-RESEARCH.md (Pitfall 3: cluster_name format)
  </read_first>

  <behavior>
    - clusterEnrichedKeywords, aynı intent'teki keyword'leri önce gruplayıp metin benzerliğiyle alt kümeler oluşturur
    - cluster.name formatı `${head_keyword} (${intent})` — UNIQUE constraint çakışmasını önler (Pitfall 3)
    - search_intent null olan keyword'ler 'unknown' grubu olarak kümelenir
    - Boş array girişinde boş array döner
    - Sonuç totalVolume azalan sırayla sıralı gelir
    - Mevcut clusterKeywords fonksiyonu (ParsedKeyword tabanlı) DOKUNULMADAN kalır — importKeywords action'ı hala kullanıyor
  </behavior>

  <action>
  **src/lib/keywords/clustering.ts dosyasını aç ve sonuna ekle** (mevcut kodu SILME):

  Dosyanın sonuna şu tipleri ve fonksiyonu append et:

  ```typescript
  // ─── Phase 6: Intent-First Clustering for DB keywords ────────────────────────

  export type ClusterInput = {
    id: string
    keyword: string
    volume: number | null
    difficulty: number | null
    cpc: number | null
    search_intent: string | null
  }

  export type ClusterResult = {
    name: string        // "${head_keyword} (${intent})" — Pitfall 3: UNIQUE constraint önleme
    intent: string | null
    totalVolume: number
    keywords: ClusterInput[]
  }

  export function clusterEnrichedKeywords(keywords: ClusterInput[]): ClusterResult[] {
    if (keywords.length === 0) return []

    // Adım 1: intent gruplaması
    const intentGroups = new Map<string, ClusterInput[]>()
    for (const kw of keywords) {
      const intent = kw.search_intent?.toLowerCase() ?? 'unknown'
      if (!intentGroups.has(intent)) intentGroups.set(intent, [])
      intentGroups.get(intent)!.push(kw)
    }

    const results: ClusterResult[] = []

    for (const [intent, group] of intentGroups) {
      // Intent grubu içinde volume azalan sıralama
      const sorted = [...group].sort((a, b) => (b.volume ?? 0) - (a.volume ?? 0))

      // Alt kümeleme: mevcut overlapsWithCluster algoritması ile
      const subClusters: Array<{ head: ClusterInput; members: ClusterInput[] }> = []
      const assigned = new Set<string>()

      for (const kw of sorted) {
        if (assigned.has(kw.keyword)) continue

        let placed = false
        for (const sc of subClusters) {
          if (overlapsWithCluster(kw.keyword, sc.head.keyword)) {
            sc.members.push(kw)
            assigned.add(kw.keyword)
            placed = true
            break
          }
        }

        if (!placed) {
          subClusters.push({ head: kw, members: [kw] })
          assigned.add(kw.keyword)
        }
      }

      for (const sc of subClusters) {
        const members = sc.members.sort((a, b) => (b.volume ?? 0) - (a.volume ?? 0))
        const totalVolume = members.reduce((sum, k) => sum + (k.volume ?? 0), 0)
        results.push({
          name: `${sc.head.keyword} (${intent})`,   // Pitfall 3: intent suffix
          intent,
          totalVolume,
          keywords: members,
        })
      }
    }

    // Toplam volume azalan sıralama
    return results.sort((a, b) => b.totalVolume - a.totalVolume)
  }
  ```

  **src/lib/keywords/clustering.test.ts oluştur:**
  ```typescript
  import { describe, it, expect } from 'vitest'
  import { clusterEnrichedKeywords, type ClusterInput } from './clustering'

  function kw(overrides: Partial<ClusterInput> & { keyword: string }): ClusterInput {
    return {
      id: Math.random().toString(),
      volume: 1000,
      difficulty: 30,
      cpc: 1.5,
      search_intent: 'informational',
      ...overrides,
    }
  }

  describe('clusterEnrichedKeywords', () => {
    it('returns empty array for empty input', () => {
      expect(clusterEnrichedKeywords([])).toEqual([])
    })

    it('separates keywords by intent into different clusters', () => {
      const keywords = [
        kw({ keyword: 'istanbul seo', search_intent: 'commercial', volume: 5000 }),
        kw({ keyword: 'seo nedir', search_intent: 'informational', volume: 3000 }),
      ]
      const clusters = clusterEnrichedKeywords(keywords)
      const intents = clusters.map((c) => c.intent)
      expect(intents).toContain('commercial')
      expect(intents).toContain('informational')
    })

    it('cluster name uses "${head_keyword} (${intent})" format', () => {
      const keywords = [kw({ keyword: 'seo ajansı', search_intent: 'commercial', volume: 8000 })]
      const clusters = clusterEnrichedKeywords(keywords)
      expect(clusters[0].name).toBe('seo ajansı (commercial)')
    })

    it('assigns null intent keyword to "unknown" group', () => {
      const keywords = [kw({ keyword: 'test keyword', search_intent: null })]
      const clusters = clusterEnrichedKeywords(keywords)
      expect(clusters[0].intent).toBe('unknown')
      expect(clusters[0].name).toContain('(unknown)')
    })

    it('groups text-similar keywords within same intent', () => {
      const keywords = [
        kw({ keyword: 'istanbul seo ajans', search_intent: 'commercial', volume: 5000 }),
        kw({ keyword: 'istanbul seo hizmet', search_intent: 'commercial', volume: 3000 }),
        kw({ keyword: 'ankara dijital pazarlama', search_intent: 'commercial', volume: 2000 }),
      ]
      const clusters = clusterEnrichedKeywords(keywords)
      // istanbul seo keywords should cluster together
      const istanbulCluster = clusters.find((c) => c.name.includes('istanbul'))
      expect(istanbulCluster?.keywords.length).toBeGreaterThanOrEqual(2)
    })

    it('a keyword belongs to at most one cluster (no duplication)', () => {
      const keywords = [
        kw({ keyword: 'seo nedir', search_intent: 'informational', volume: 4000 }),
        kw({ keyword: 'seo ne demek', search_intent: 'informational', volume: 2000 }),
        kw({ keyword: 'dijital pazarlama', search_intent: 'commercial', volume: 3000 }),
      ]
      const clusters = clusterEnrichedKeywords(keywords)
      const allKeywordIds = clusters.flatMap((c) => c.keywords.map((k) => k.id))
      const uniqueIds = new Set(allKeywordIds)
      expect(uniqueIds.size).toBe(allKeywordIds.length)
    })

    it('sorts clusters by totalVolume descending', () => {
      const keywords = [
        kw({ keyword: 'düşük volume', search_intent: 'navigational', volume: 100 }),
        kw({ keyword: 'yüksek volume', search_intent: 'transactional', volume: 9000 }),
      ]
      const clusters = clusterEnrichedKeywords(keywords)
      expect(clusters[0].totalVolume).toBeGreaterThanOrEqual(clusters[1]?.totalVolume ?? 0)
    })
  })
  ```

  `npx vitest run src/lib/keywords/clustering.test.ts` — tüm testler geçmeli.
  </action>

  <verify>
    <automated>npx vitest run src/lib/keywords/clustering.test.ts --reporter=verbose</automated>
  </verify>

  <done>
    - clustering.ts, ClusterInput, ClusterResult tiplerini ve clusterEnrichedKeywords fonksiyonunu export ediyor
    - Mevcut Cluster tipi ve clusterKeywords fonksiyonu hala mevcut (silinmedi)
    - clustering.test.ts mevcut, 6 test geçiyor
    - `npx vitest run src/lib/keywords/clustering.test.ts` çıkışı "all passed"
  </done>
</task>

<task type="auto">
  <name>Task 3: actions.ts — clusterAndScoreKeywords, moveKeywordToCluster, setPrimaryKeyword</name>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts (mevcut tam dosya — auth pattern + cluster upsert pattern)
    - src/lib/keywords/clustering.ts (ClusterInput, ClusterResult tipleri — yeni importlar)
    - src/lib/keywords/scoring.ts (calculateOpportunityScore, buildScoringContext — yeni importlar)
    - .planning/phases/06-keyword-clustering-scoring/06-RESEARCH.md (Pattern 3: Cannibalization Prevention, Pattern 5: Clustering Server Action, Pitfall 2)
    - .planning/phases/06-keyword-clustering-scoring/06-PATTERNS.md (actions.ts pattern excerpts)
  </read_first>

  <action>
  **src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts dosyasını aç.**

  **1. Import block'unu güncelle** — dosyanın üstüne şu importları ekle (mevcut importların altına):
  ```typescript
  import { clusterEnrichedKeywords } from '@/lib/keywords/clustering'
  import { calculateOpportunityScore, buildScoringContext } from '@/lib/keywords/scoring'
  ```

  **2. Dosyanın sonuna üç yeni action + return type ekle:**

  ```typescript
  // ─── Phase 6: Clustering & Scoring Actions ───────────────────────────────────

  export type ClusterAndScoreResult =
    | { success: true; clusterCount: number }
    | { success: false; error: string }

  export async function clusterAndScoreKeywords(
    projectId: string
  ): Promise<ClusterAndScoreResult> {
    if (!projectId || !/^[0-9a-f-]{36}$/i.test(projectId)) {
      return { success: false, error: 'Geçersiz proje ID.' }
    }

    const supabase = await createClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) return { success: false, error: 'Oturum bulunamadı.' }

    const { data: project } = await supabase
      .from('projects')
      .select('id')
      .eq('id', projectId)
      .eq('user_id', user.id)
      .single()
    if (!project) return { success: false, error: 'Proje bulunamadı.' }

    // Enriched keywords'ü çek (Pitfall 1: enriched_at IS NOT NULL filtresi)
    const { data: keywordsRaw } = await supabase
      .from('keywords')
      .select('id, keyword, volume, cpc, difficulty, search_intent')
      .eq('project_id', projectId)
      .eq('user_id', user.id)
      .not('enriched_at', 'is', null)

    const keywords = keywordsRaw ?? []

    if (keywords.length === 0) {
      return { success: false, error: 'Kümelenecek keyword bulunamadı. Önce zenginleştirme çalıştırın.' }
    }

    // DoS guard: 500+ keyword için uyarı döndür
    if (keywords.length > 500) {
      return { success: false, error: `Proje çok fazla keyword içeriyor (${keywords.length}). Kümeleme 500 keyword ile sınırlıdır.` }
    }

    // Intent-first hibrid clustering
    const clusters = clusterEnrichedKeywords(keywords)

    // Opportunity score context (batch normalizasyon için max değerler)
    const scoringCtx = buildScoringContext(keywords)

    let clusterCount = 0

    for (const cluster of clusters) {
      // keyword_clusters UPSERT — onConflict: project_id,cluster_name (Pitfall 3 önlemi: name "(intent)" suffix zaten içeriyor)
      const { data: clusterRow, error: clusterErr } = await supabase
        .from('keyword_clusters')
        .upsert(
          {
            user_id: user.id,
            project_id: projectId,
            cluster_name: cluster.name,
            total_volume: cluster.totalVolume,
            intent: cluster.intent,
          },
          { onConflict: 'project_id,cluster_name', ignoreDuplicates: false }
        )
        .select('id')
        .single()

      if (clusterErr || !clusterRow) continue
      clusterCount++

      // Her keyword için opportunity_score hesapla ve cluster_id + score güncelle
      const updates = cluster.keywords.map((kw) => ({
        id: kw.id,
        cluster_id: clusterRow.id,
        opportunity_score: calculateOpportunityScore(kw, scoringCtx),
      }))

      // Toplu UPDATE — her keyword ayrı çağrı yerine Promise.all (Pitfall 4)
      await Promise.all(
        updates.map((u) =>
          supabase
            .from('keywords')
            .update({ cluster_id: u.cluster_id, opportunity_score: u.opportunity_score })
            .eq('id', u.id)
            .eq('user_id', user.id)
        )
      )

      // Primary keyword otomatik ata: cluster'ın ilk keyword'ü (en yüksek volume — clusterEnrichedKeywords sıralı döner)
      if (cluster.keywords.length > 0) {
        await supabase
          .from('keyword_clusters')
          .update({ primary_keyword_id: cluster.keywords[0].id })
          .eq('id', clusterRow.id)
          .eq('user_id', user.id)
      }
    }

    revalidatePath(`/projeler/${projectId}/keyword-stratejisi`)
    return { success: true, clusterCount }
  }

  export type MoveKeywordResult = { success: true } | { success: false; error: string }

  export async function moveKeywordToCluster(
    keywordId: string,
    newClusterId: string,
    projectId: string
  ): Promise<MoveKeywordResult> {
    // UUID format validation
    const uuidRegex = /^[0-9a-f-]{36}$/i
    if (!uuidRegex.test(keywordId) || !uuidRegex.test(newClusterId) || !uuidRegex.test(projectId)) {
      return { success: false, error: 'Geçersiz ID formatı.' }
    }

    const supabase = await createClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) return { success: false, error: 'Oturum bulunamadı.' }

    // Keyword ownership doğrula (Elevation of Privilege — T-06-01)
    const { data: kw } = await supabase
      .from('keywords')
      .select('id, cluster_id')
      .eq('id', keywordId)
      .eq('project_id', projectId)
      .eq('user_id', user.id)
      .single()
    if (!kw) return { success: false, error: 'Keyword bulunamadı.' }

    // Hedef cluster'ın bu projeye ve user'a ait olduğunu doğrula (Elevation of Privilege)
    const { data: targetCluster } = await supabase
      .from('keyword_clusters')
      .select('id')
      .eq('id', newClusterId)
      .eq('project_id', projectId)
      .eq('user_id', user.id)
      .single()
    if (!targetCluster) return { success: false, error: 'Hedef küme bulunamadı.' }

    // Pitfall 2: Keyword eski cluster'ın primary'si ise primary_keyword_id NULL yap
    if (kw.cluster_id) {
      await supabase
        .from('keyword_clusters')
        .update({ primary_keyword_id: null })
        .eq('id', kw.cluster_id)
        .eq('primary_keyword_id', keywordId)
        .eq('user_id', user.id)
    }

    // Keyword cluster_id UPDATE — eski cluster_id otomatik ezilir (cannibalization prevention)
    const { error } = await supabase
      .from('keywords')
      .update({ cluster_id: newClusterId })
      .eq('id', keywordId)
      .eq('user_id', user.id)

    if (error) return { success: false, error: 'Küme ataması başarısız.' }

    revalidatePath(`/projeler/${projectId}/keyword-stratejisi`)
    return { success: true }
  }

  export type SetPrimaryKeywordResult = { success: true } | { success: false; error: string }

  export async function setPrimaryKeyword(
    clusterId: string,
    keywordId: string,
    projectId: string
  ): Promise<SetPrimaryKeywordResult> {
    // UUID format validation
    const uuidRegex = /^[0-9a-f-]{36}$/i
    if (!uuidRegex.test(clusterId) || !uuidRegex.test(keywordId) || !uuidRegex.test(projectId)) {
      return { success: false, error: 'Geçersiz ID formatı.' }
    }

    const supabase = await createClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) return { success: false, error: 'Oturum bulunamadı.' }

    // Tampering: keyword'ün bu cluster'a VE bu projeye VE bu user'a ait olduğunu doğrula (T-06-02)
    const { data: kw } = await supabase
      .from('keywords')
      .select('id, cluster_id, project_id')
      .eq('id', keywordId)
      .eq('project_id', projectId)
      .eq('user_id', user.id)
      .single()
    if (!kw) return { success: false, error: 'Keyword bulunamadı.' }

    // Keyword bu cluster'ın üyesi mi? (başka projenin keyword UUID'si saldırısına karşı)
    if (kw.cluster_id !== clusterId) {
      return { success: false, error: 'Keyword bu kümenin üyesi değil.' }
    }

    const { error } = await supabase
      .from('keyword_clusters')
      .update({ primary_keyword_id: keywordId })
      .eq('id', clusterId)
      .eq('project_id', projectId)
      .eq('user_id', user.id)

    if (error) return { success: false, error: 'Primary atama başarısız.' }

    revalidatePath(`/projeler/${projectId}/keyword-stratejisi`)
    return { success: true }
  }
  ```
  </action>

  <acceptance_criteria>
    - actions.ts'de `export async function clusterAndScoreKeywords` mevcut: `grep -n "export async function clusterAndScoreKeywords" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts`
    - actions.ts'de `export async function moveKeywordToCluster` mevcut: `grep -n "export async function moveKeywordToCluster" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts`
    - actions.ts'de `export async function setPrimaryKeyword` mevcut: `grep -n "export async function setPrimaryKeyword" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts`
    - DoS guard mevcut: `grep -n "keywords.length > 500" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts`
    - UUID validation mevcut: `grep -n "uuidRegex" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts`
    - Pitfall 2 guard mevcut: `grep -n "primary_keyword_id: null" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts`
    - revalidatePath tüm mutating action'larda mevcut: `grep -c "revalidatePath" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → 4 veya daha fazla (import/deleteCluster/deleteKeyword/yeni 3 action)
    - TypeScript compile: `npx tsc --noEmit` hatasız tamamlanmalı
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit && echo "TS OK"</automated>
  </verify>

  <done>
    - 3 yeni Server Action actions.ts'e eklendi
    - Her action UUID format validation yapıyor
    - clusterAndScoreKeywords: enriched_at filtresi, DoS guard (>500), intent-first clustering, opportunity_score hesaplama, primary_keyword_id otomatik atama, revalidatePath
    - moveKeywordToCluster: her iki ownership check (keyword + cluster), Pitfall 2 primary temizleme, revalidatePath
    - setPrimaryKeyword: keyword.cluster_id === clusterId doğrulaması (tampering önleme), revalidatePath
    - TypeScript compile hatasız
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Açıklama |
|----------|----------|
| Browser → Server Action | projectId, clusterId, keywordId parametreleri client'tan gelir — tahrif edilebilir |
| Server Action → Supabase | user.id + project_id çifti her sorguda doğrulanmali |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-01 | Elevation of Privilege | moveKeywordToCluster | mitigate | keyword VE hedef cluster'ın her ikisi de `.eq('user_id', user.id).eq('project_id', projectId)` ile doğrulanır — başka kullanıcının cluster'ına taşıma engellenir |
| T-06-02 | Tampering | setPrimaryKeyword | mitigate | `kw.cluster_id !== clusterId` kontrolü — başka projenin keyword UUID'si ile primary_keyword_id tahrifi önlenir |
| T-06-03 | DoS | clusterAndScoreKeywords | mitigate | `keywords.length > 500` guard — sunucu-tarafı keyword sayısı limiti, aşılınca hata döner |
| T-06-04 | Elevation of Privilege | clusterAndScoreKeywords | mitigate | getUser() + project ownership check — başka kullanıcının projesini kümeleme engellenir |
| T-06-05 | Input Validation | tüm yeni Server Actions | mitigate | `/^[0-9a-f-]{36}$/i` UUID regex — projectId, clusterId, keywordId format doğrulaması |
</threat_model>

<verification>
Tüm tests geçmeli:
```
npx vitest run src/lib/keywords/
```
Beklenen: 13 test (7 scoring + 6 clustering), tümü geçiyor.

TypeScript compile:
```
npx tsc --noEmit
```
Beklenen: 0 hata.

Actions kontrolü:
```
grep -n "export async function" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts
```
Beklenen: importKeywords, deleteCluster, deleteKeyword, clusterAndScoreKeywords, moveKeywordToCluster, setPrimaryKeyword — toplam 6 function.
</verification>

<success_criteria>
- scoring.ts oluşturuldu, testler geçiyor
- clustering.ts'e clusterEnrichedKeywords eklendi, testler geçiyor
- actions.ts'e 3 yeni Server Action eklendi (clusterAndScoreKeywords, moveKeywordToCluster, setPrimaryKeyword)
- Her action UUID validation, getUser(), project ownership yapıyor
- DoS guard (>500 keyword) aktif
- Pitfall 2 (primary_keyword_id temizleme), Pitfall 3 (cluster name format) önlemleri uygulandı
- TypeScript compile hatasız
</success_criteria>

<output>
Plan tamamlandığında `.planning/phases/06-keyword-clustering-scoring/06-01-SUMMARY.md` oluştur.
</output>
