---
phase: 15.5
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/wp/import.test.ts
  - src/lib/wp/normalize.test.ts
  - src/lib/wp/url-normalize.test.ts
  - src/lib/wp/audit-flags.test.ts
autonomous: true
requirements:
  - IMP-01
  - IMP-03
  - IMP-05

must_haves:
  truths:
    - "Tüm test dosyaları mevcut ve vitest çalıştırılabilir durumda"
    - "Test dosyaları FAILING (stub) — Wave 0 kırık testler Wave 2+ implementasyonla yeşile döner"
    - "Test isimleri VALIDATION.md per-task verification map ile bire bir eşleşiyor"
  artifacts:
    - path: "src/lib/wp/import.test.ts"
      provides: "WP pagination fetch + upsert unit testleri"
      min_lines: 30
    - path: "src/lib/wp/normalize.test.ts"
      provides: "WP response → ImportedPage shape normalization testleri"
      min_lines: 20
    - path: "src/lib/wp/url-normalize.test.ts"
      provides: "URL normalization edge case testleri"
      min_lines: 25
    - path: "src/lib/wp/audit-flags.test.ts"
      provides: "Audit flag threshold testleri (D-10, D-11, D-12, D-13)"
      min_lines: 40
  key_links:
    - from: "audit-flags.test.ts"
      to: "src/lib/wp/audit-flags.ts"
      via: "import { computeAuditFlags } from './audit-flags'"
      pattern: "computeAuditFlags"
---

<objective>
Wave 0 test dosyalarını yaz — tüm lib/wp/ modülleri için failing stub testleri. Bu dosyalar Wave 2+ implementasyon planlarındaki automated verify komutları tarafından kullanılır.

Purpose: Nyquist compliance — her implementasyon task'ının automated verify komutu çalışabilir olmalı.
Output: 4 test dosyası, hepsi `describe` + `it.todo` veya başlangıç implementasyonu olmadan failing stub olarak
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15.5-wordpress-site-import-engine/15.5-VALIDATION.md
@.planning/phases/15.5-wordpress-site-import-engine/15.5-CONTEXT.md

<interfaces>
<!-- Vitest framework: 4.1.5, vitest.config.ts mevcut -->
<!-- Test pattern: describe + it + expect — Phase 6 clusterEnrichedKeywords test pattern'ı analog -->
<!-- src/lib/wp/ dizini henüz mevcut değil — dosyalar oluşturulacak -->

Audit flag eşikleri (CONTEXT.md D-10..D-13):
- weak_page: gsc_clicks < 10 AND avg_position > 20 (son 28 gün) — GSC yoksa null
- outdated: wp_modified_at < now() - 12 months
- orphan: parent_wp_id null OR parent_wp_id === 0 (root page) AND WP'de silinmiş
- missing_metadata: yoast_head_json.title veya .description eksik; fallback title.rendered + excerpt.rendered

URL normalization hedefleri (D-14):
- https://www.example.com/page/ → https://example.com/page
- http://example.com/page → https://example.com/page
- https://example.com/page?q=1#section → https://example.com/page
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: url-normalize.test.ts ve audit-flags.test.ts</name>
  <files>src/lib/wp/url-normalize.test.ts, src/lib/wp/audit-flags.test.ts</files>
  <read_first>
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-VALIDATION.md (per-task verification map)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-CONTEXT.md (D-10, D-11, D-12, D-13 flag eşikleri)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-RESEARCH.md §Pattern 3 (URL normalization)
  </read_first>
  <behavior>
    url-normalize.test.ts:
    - normalizeUrl('https://www.example.com/page/') → 'https://example.com/page'
    - normalizeUrl('http://example.com/page') → 'https://example.com/page'
    - normalizeUrl('https://example.com/page?q=1#section') → 'https://example.com/page'
    - normalizeUrl('https://example.com/') → 'https://example.com'
    - normalizeUrl('https://example.com/path') → 'https://example.com/path' (değişmeden)

    audit-flags.test.ts (computeAuditFlags pure function):
    - weak_page=true: gsc_clicks=5, gsc_avg_position=25 (< 10 ve > 20)
    - weak_page=false: gsc_clicks=15, gsc_avg_position=25 (>= 10)
    - weak_page=null: gsc_clicks=null, gsc_avg_position=null (GSC verisi yok)
    - outdated=true: wp_modified_at = 13 ay önce
    - outdated=false: wp_modified_at = 11 ay önce
    - orphan=true: parent_wp_id=null, flag_orphan=true (WP'de silinmiş)
    - missing_metadata=true: yoast_title=null, yoast_description=null, native_title=null
    - missing_metadata=false: yoast_title='SEO başlık' mevcut
  </behavior>
  <action>
`src/lib/wp/` dizinini oluştur, ardından iki test dosyasını yaz:

**src/lib/wp/url-normalize.test.ts:**
```typescript
import { describe, it, expect } from 'vitest'
import { normalizeUrl } from './url-normalize'

describe('normalizeUrl', () => {
  it('www prefix kaldırır', () => {
    expect(normalizeUrl('https://www.example.com/page/')).toBe('https://example.com/page')
  })
  it('http → https dönüştürür', () => {
    expect(normalizeUrl('http://example.com/page')).toBe('https://example.com/page')
  })
  it('query string ve fragment kaldırır', () => {
    expect(normalizeUrl('https://example.com/page?q=1#section')).toBe('https://example.com/page')
  })
  it('trailing slash kaldırır', () => {
    expect(normalizeUrl('https://example.com/')).toBe('https://example.com')
  })
  it('temiz URL değiştirmeden döndürür', () => {
    expect(normalizeUrl('https://example.com/path')).toBe('https://example.com/path')
  })
})
```

**src/lib/wp/audit-flags.test.ts:**
```typescript
import { describe, it, expect } from 'vitest'
import { computeAuditFlags } from './audit-flags'

// ImportedPageInput type — audit-flags.ts henüz yok, testler import edince fail eder (Wave 0)
describe('computeAuditFlags', () => {
  const baseInput = {
    gsc_clicks: null,
    gsc_avg_position: null,
    wp_modified_at: new Date().toISOString(),
    parent_wp_id: null,
    flag_orphan_deleted: false,
    yoast_title: 'Test Title',
    yoast_description: 'Test Desc',
    native_title: 'Test Title',
    native_excerpt: 'Excerpt',
    project_keywords: ['test'],
    page_link: 'https://example.com/test',
  }

  it('weak_page null döner — GSC verisi yok', () => {
    const flags = computeAuditFlags({ ...baseInput })
    expect(flags.flag_weak_page).toBeNull()
  })

  it('weak_page true — clicks<10 AND pos>20', () => {
    const flags = computeAuditFlags({ ...baseInput, gsc_clicks: 5, gsc_avg_position: 25 })
    expect(flags.flag_weak_page).toBe(true)
  })

  it('weak_page false — clicks>=10', () => {
    const flags = computeAuditFlags({ ...baseInput, gsc_clicks: 15, gsc_avg_position: 25 })
    expect(flags.flag_weak_page).toBe(false)
  })

  it('outdated true — 13 ay önce güncellendi', () => {
    const thirteenMonthsAgo = new Date()
    thirteenMonthsAgo.setMonth(thirteenMonthsAgo.getMonth() - 13)
    const flags = computeAuditFlags({ ...baseInput, wp_modified_at: thirteenMonthsAgo.toISOString() })
    expect(flags.flag_outdated).toBe(true)
  })

  it('outdated false — 11 ay önce güncellendi', () => {
    const elevenMonthsAgo = new Date()
    elevenMonthsAgo.setMonth(elevenMonthsAgo.getMonth() - 11)
    const flags = computeAuditFlags({ ...baseInput, wp_modified_at: elevenMonthsAgo.toISOString() })
    expect(flags.flag_outdated).toBe(false)
  })

  it('missing_metadata true — Yoast ve native title/excerpt eksik', () => {
    const flags = computeAuditFlags({
      ...baseInput,
      yoast_title: null,
      yoast_description: null,
      native_title: null,
      native_excerpt: null,
    })
    expect(flags.flag_missing_metadata).toBe(true)
  })

  it('missing_metadata false — Yoast title mevcut', () => {
    const flags = computeAuditFlags({ ...baseInput, yoast_title: 'SEO Başlık' })
    expect(flags.flag_missing_metadata).toBe(false)
  })
})
```
  </action>
  <verify>
    <automated>cd "C:/Users/baris/Documents/seo-machine" && npx vitest run src/lib/wp/url-normalize.test.ts src/lib/wp/audit-flags.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/wp/url-normalize.test.ts` dosyası mevcut
    - `src/lib/wp/audit-flags.test.ts` dosyası mevcut
    - Her iki dosya `import` ifadesiyle başlıyor (vitest + kaynak modül)
    - vitest çalıştırma komutu hata vermeden sonuçlanıyor (testler fail olabilir — modüller henüz yok, bu beklenen)
    - Test isimleri VALIDATION.md'deki task ID'leri ile eşleşiyor (weak_page, outdated, missing_metadata)
  </acceptance_criteria>
  <done>url-normalize.test.ts ve audit-flags.test.ts oluşturuldu; vitest dosyaları tanıyor</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: import.test.ts ve normalize.test.ts</name>
  <files>src/lib/wp/import.test.ts, src/lib/wp/normalize.test.ts</files>
  <read_first>
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-RESEARCH.md §Pattern 1 (WP pagination loop)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-RESEARCH.md §Pattern 4 (flattenImportedTree)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-CONTEXT.md (D-08, D-09 kararları)
  </read_first>
  <behavior>
    import.test.ts (fetchAllWpContent):
    - Tek sayfalık response → doğru item sayısı döner
    - İki sayfalık response (X-WP-TotalPages: 2) → tüm item'lar döner (pagination loop)
    - WP API 401 hatası → hata fırlatır

    normalize.test.ts (normalizeWpPage + flattenImportedTree):
    - normalizeWpPage WP page object'ini ImportedPage shape'ine dönüştürür
    - parent=0 → parent_wp_id=null (kök sayfa)
    - parent=5 → parent_wp_id=5
    - flattenImportedTree: parent/child sırası doğru (parent önce, child sonra, depth artar)
    - flattenImportedTree: root sayfalar depth=0, child depth=1
  </behavior>
  <action>
**src/lib/wp/import.test.ts:**
```typescript
import { describe, it, expect, vi } from 'vitest'
import { fetchAllWpContent } from './import'

describe('fetchAllWpContent', () => {
  it('tek sayfalık response — tüm item döner', async () => {
    global.fetch = vi.fn().mockResolvedValueOnce({
      ok: true,
      headers: { get: (h: string) => h === 'X-WP-TotalPages' ? '1' : null },
      json: async () => [{ id: 1, title: { rendered: 'Sayfa 1' }, link: 'https://ex.com/page-1', slug: 'page-1', parent: 0, date: '2025-01-01T00:00:00', modified: '2025-01-01T00:00:00', status: 'publish', menu_order: 0 }],
    } as unknown as Response)
    const result = await fetchAllWpContent('https://ex.com', 'user:pass_base64', 'pages')
    expect(result).toHaveLength(1)
    expect(result[0].id).toBe(1)
  })

  it('iki sayfalık response — tüm item döner (pagination loop)', async () => {
    const page1Item = { id: 1, title: { rendered: 'P1' }, link: 'https://ex.com/p1', slug: 'p1', parent: 0, date: '2025-01-01T00:00:00', modified: '2025-01-01T00:00:00', status: 'publish', menu_order: 0 }
    const page2Item = { id: 2, title: { rendered: 'P2' }, link: 'https://ex.com/p2', slug: 'p2', parent: 0, date: '2025-01-01T00:00:00', modified: '2025-01-01T00:00:00', status: 'publish', menu_order: 0 }
    global.fetch = vi.fn()
      .mockResolvedValueOnce({
        ok: true,
        headers: { get: (h: string) => h === 'X-WP-TotalPages' ? '2' : null },
        json: async () => [page1Item],
      } as unknown as Response)
      .mockResolvedValueOnce({
        ok: true,
        headers: { get: () => null },
        json: async () => [page2Item],
      } as unknown as Response)
    const result = await fetchAllWpContent('https://ex.com', 'user:pass_base64', 'pages')
    expect(result).toHaveLength(2)
  })

  it('WP API 4xx → hata fırlatır', async () => {
    global.fetch = vi.fn().mockResolvedValueOnce({
      ok: false,
      status: 401,
    } as unknown as Response)
    await expect(fetchAllWpContent('https://ex.com', 'bad_creds', 'pages')).rejects.toThrow()
  })
})
```

**src/lib/wp/normalize.test.ts:**
```typescript
import { describe, it, expect } from 'vitest'
import { normalizeWpPage, flattenImportedTree } from './normalize'

describe('normalizeWpPage', () => {
  const wpPageBase = {
    id: 42,
    title: { rendered: 'Test Sayfası' },
    link: 'https://example.com/test-sayfasi/',
    slug: 'test-sayfasi',
    parent: 0,
    date: '2024-01-15T10:00:00',
    modified: '2024-06-01T12:00:00',
    status: 'publish',
    menu_order: 0,
    yoast_head_json: { title: 'SEO Başlık', description: 'SEO Meta' },
    excerpt: { rendered: '<p>Excerpt</p>' },
  }

  it('WP page object ImportedPage shape\'ine dönüşür', () => {
    const result = normalizeWpPage(wpPageBase, 'proj-123', 'page')
    expect(result.wp_id).toBe(42)
    expect(result.project_id).toBe('proj-123')
    expect(result.title).toBe('Test Sayfası')
    expect(result.wp_type).toBe('page')
  })

  it('parent=0 → parent_wp_id=null (kök sayfa)', () => {
    const result = normalizeWpPage({ ...wpPageBase, parent: 0 }, 'proj-123', 'page')
    expect(result.parent_wp_id).toBeNull()
  })

  it('parent=5 → parent_wp_id=5', () => {
    const result = normalizeWpPage({ ...wpPageBase, parent: 5 }, 'proj-123', 'page')
    expect(result.parent_wp_id).toBe(5)
  })
})

describe('flattenImportedTree', () => {
  it('parent önce, child sonra sıralanır', () => {
    const pages = [
      { wp_id: 10, parent_wp_id: null, title: 'Ana', slug: 'ana' },
      { wp_id: 20, parent_wp_id: 10, title: 'Alt', slug: 'alt' },
    ]
    const flat = flattenImportedTree(pages as never)
    expect(flat[0].wp_id).toBe(10)
    expect(flat[0].depth).toBe(0)
    expect(flat[1].wp_id).toBe(20)
    expect(flat[1].depth).toBe(1)
  })
})
```
  </action>
  <verify>
    <automated>cd "C:/Users/baris/Documents/seo-machine" && npx vitest run src/lib/wp/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/wp/import.test.ts` mevcut, `describe('fetchAllWpContent'` ifadesi içeriyor
    - `src/lib/wp/normalize.test.ts` mevcut, `describe('normalizeWpPage'` ve `describe('flattenImportedTree'` içeriyor
    - `npx vitest run src/lib/wp/` komutu hata vermeden çalışıyor (testler fail olabilir — modüller yok)
    - 4 test dosyası da `src/lib/wp/` altında mevcut
  </acceptance_criteria>
  <done>Tüm 4 Wave 0 test dosyası oluşturuldu; vitest src/lib/wp/ dizinini tarayabiliyor</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → implementation | Testler sadece lib/wp/ modüllerini import eder — DB/network doğrudan erişilmez |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.5-02-01 | — | test files | accept | Test dosyaları üretim kodu değil; güvenlik tehdidi yok |
</threat_model>

<verification>
```bash
cd "C:/Users/baris/Documents/seo-machine" && npx vitest run src/lib/wp/ --reporter=verbose 2>&1 | grep -E "(FAIL|PASS|ERROR|describe)" | head -20
```
4 test dosyası görünmeli (testler fail olabilir — beklenen durum).
</verification>

<success_criteria>
- src/lib/wp/ altında 4 test dosyası mevcut
- Her test dosyası doğru describe/it yapısıyla yazılmış
- vitest src/lib/wp/ komutu çalışıyor (modüller eksik olduğu için testler fail — normal)
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/15.5-wordpress-site-import-engine/15.5-02-SUMMARY.md` oluştur.
</output>
