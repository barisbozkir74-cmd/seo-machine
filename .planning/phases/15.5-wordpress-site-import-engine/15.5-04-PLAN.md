---
phase: 15.5
plan: 04
type: execute
wave: 2
depends_on:
  - 15.5-01
  - 15.5-02
  - 15.5-03
files_modified:
  - src/lib/wp/audit-flags.ts
  - src/lib/wp/gsc-match.ts
autonomous: true
requirements:
  - IMP-03
  - IMP-05

must_haves:
  truths:
    - "computeAuditFlags() D-10..D-13 eşiklerine göre flag'leri doğru hesaplıyor"
    - "flag_weak_page null döner — GSC verisi yoksa (D-10)"
    - "matchGscData() imported_pages.link URL'lerini GSC Search Analytics API ile eşleştiriyor"
    - "audit-flags.test.ts ve gsc-match.test.ts yeşile döndü"
  artifacts:
    - path: "src/lib/wp/audit-flags.ts"
      provides: "computeAuditFlags pure function + computeDuplicateIntentFlags"
      exports: ["computeAuditFlags", "computeDuplicateIntentFlags", "AuditFlagInput"]
    - path: "src/lib/wp/gsc-match.ts"
      provides: "matchGscData — GSC Search Analytics'ten URL bazlı trafik verisi"
      exports: ["matchGscData", "GscPageMetric"]
  key_links:
    - from: "src/lib/wp/audit-flags.ts"
      to: "src/lib/wp/url-normalize.ts"
      via: "normalizeUrl import"
      pattern: "normalizeUrl"
    - from: "src/lib/wp/gsc-match.ts"
      to: "src/lib/gsc/auth.ts"
      via: "getValidGscToken(projectId)"
      pattern: "getValidGscToken"
---

<objective>
Audit flag hesaplama (audit-flags.ts) ve GSC URL eşleştirme (gsc-match.ts) lib modüllerini yaz. Bu iki modül saf hesaplama katmanıdır — Route Handler tarafından çağrılır.

Purpose: IMP-05 flag hesaplama + IMP-03 GSC eşleştirme. Her ikisi pure function veya bağımsız servis katmanı.
Output: 2 lib dosyası + ilgili testler yeşil
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15.5-wordpress-site-import-engine/15.5-CONTEXT.md
@.planning/phases/15.5-wordpress-site-import-engine/15.5-RESEARCH.md
@.planning/phases/15.5-wordpress-site-import-engine/15.5-PATTERNS.md

<interfaces>
<!-- audit-flags.ts girdisi — normalize.ts ImportedPage'den türetilir -->
```typescript
// computeAuditFlags input shape:
interface AuditFlagInput {
  gsc_clicks: number | null
  gsc_avg_position: number | null
  wp_modified_at: string | null
  parent_wp_id: number | null
  flag_orphan_deleted: boolean    // WP'de silinmiş → re-import'ta orphan
  yoast_title: string | null
  yoast_description: string | null
  native_title: string | null
  native_excerpt: string | null
  project_keywords: string[]      // keywords tablosundan — missing_keyword için
  page_link: string | null        // imported page URL
}
```

<!-- D-10: weak_page eşiği -->
// gsc_clicks < 10 AND avg_position > 20 → true
// gsc_clicks = null → null (GSC verisi yok)

<!-- D-11: outdated eşiği -->
// wp_modified_at < now() - 12 months → true

<!-- D-12: orphan eşiği (basit versiyon) -->
// parent_wp_id = null AND flag_orphan_deleted = false → potansiyel orphan (menu_order=0 CONTEXT.md)
// WP'de silinen sayfa → flag_orphan = true (D-09)

<!-- D-13: missing_metadata -->
// RESEARCH.md missing_metadata araştırması:
// 1. yoast_head_json.title veya .description → null/empty = flag
// 2. Fallback: native title.rendered + excerpt.rendered

<!-- A5 missing_keyword: URL segment exact match -->
// page_link URL path segmentlerinde herhangi bir project_keywords değeri geçiyor mu?

<!-- gsc-match.ts: GSC Search Analytics API doğrudan çağrısı -->
// RESEARCH.md Open Question 1 kararı: gsc_metrics tablosunu bypass et
// getValidGscToken pattern — src/lib/gsc/auth.ts
// fetchSearchAnalytics pattern — src/lib/gsc/search-analytics.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: audit-flags.ts</name>
  <files>src/lib/wp/audit-flags.ts</files>
  <read_first>
    - src/lib/wp/audit-flags.test.ts (test beklentileri — önce oku, testleri geçirecek şekilde yaz)
    - src/lib/wp/url-normalize.ts (normalizeUrl — missing_keyword URL match için)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-CONTEXT.md (D-10, D-11, D-12, D-13)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-RESEARCH.md §Pitfall 3 (missing_metadata)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-RESEARCH.md §Pitfall 6 (duplicate_intent sırası)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-PATTERNS.md §audit-flags.ts bölümü
  </read_first>
  <behavior>
    computeAuditFlags (Faz A — AI enrichment öncesi):
    - weak_page=null: gsc_clicks=null → flag hesaplanamaz
    - weak_page=true: clicks=5, pos=25 (< 10 VE > 20)
    - weak_page=false: clicks=15 (>= 10)
    - outdated=true: wp_modified_at 13 ay önce
    - outdated=false: wp_modified_at 11 ay önce
    - orphan=true: flag_orphan_deleted=true (WP'de silindi)
    - missing_metadata=true: yoast=null, native=null
    - missing_metadata=false: yoast_title mevcut
    - missing_keyword=false: project_keywords empty → belirsiz, false döner
    - missing_keyword=true: page_link URL segment'inde hiçbir keyword geçmiyor

    computeDuplicateIntentFlags (Faz B — AI enrichment sonrası):
    - Aynı primary_intent'e sahip 2+ sayfa aynı project'te → flag_duplicate_intent=true
  </behavior>
  <action>
`src/lib/wp/audit-flags.ts` dosyasını yaz:

```typescript
/**
 * Audit flag hesaplama — pure function.
 * RESEARCH.md Pattern: aggregation.ts isDecayed flag mantığı analog.
 * Pitfall 6: duplicate_intent (Faz B) ayrı fonksiyonda — AI enrichment sonrası çağrılır.
 */

export interface AuditFlagInput {
  gsc_clicks: number | null
  gsc_avg_position: number | null
  wp_modified_at: string | null
  parent_wp_id: number | null
  flag_orphan_deleted: boolean  // WP'de artık yok (re-import'ta saptanır)
  yoast_title: string | null
  yoast_description: string | null
  native_title: string | null
  native_excerpt: string | null
  project_keywords: string[]    // keywords tablosundan — missing_keyword için
  page_link: string | null
}

export interface AuditFlagOutput {
  flag_orphan: boolean
  flag_weak_page: boolean | null  // null = GSC verisi yok (D-10)
  flag_outdated: boolean
  flag_missing_metadata: boolean
  flag_missing_keyword: boolean
}

/**
 * Faz A flag hesaplama — AI enrichment gerekmez.
 * D-10: weak_page = gsc_clicks < 10 AND avg_position > 20 (son 28 gün)
 * D-11: outdated = wp_modified_at < now() - 12 months
 * D-12: orphan = WP'de silindi (flag_orphan_deleted) VEYA parent_wp_id=null AND menu_order=0
 * D-13: missing_metadata = yoast eksik VE native title/excerpt eksik
 * A5: missing_keyword = page_link URL'de hiçbir keyword geçmiyor (exact slug match)
 */
export function computeAuditFlags(input: AuditFlagInput): AuditFlagOutput {
  // weak_page (D-10)
  let flag_weak_page: boolean | null = null
  if (input.gsc_clicks !== null && input.gsc_avg_position !== null) {
    flag_weak_page = input.gsc_clicks < 10 && input.gsc_avg_position > 20
  }

  // outdated (D-11) — 12 ay = 365 gün
  let flag_outdated = false
  if (input.wp_modified_at) {
    const modified = new Date(input.wp_modified_at)
    const twelveMonthsAgo = new Date()
    twelveMonthsAgo.setFullYear(twelveMonthsAgo.getFullYear() - 1)
    flag_outdated = modified < twelveMonthsAgo
  }

  // orphan (D-12) — WP'de silindi veya parent_wp_id=null (potansiyel orphan)
  // Basit kural: flag_orphan_deleted=true → kesin orphan
  // parent_wp_id=null → root sayfa (ana sayfa değil ise orphan candidate)
  const flag_orphan = input.flag_orphan_deleted

  // missing_metadata (D-13, RESEARCH.md Pitfall 3)
  // 1. Yoast title veya description varsa → false
  // 2. Native title.rendered ve excerpt.rendered her ikisi de eksikse → true
  const hasYoast = Boolean(input.yoast_title?.trim() || input.yoast_description?.trim())
  const hasNative = Boolean(input.native_title?.trim() || input.native_excerpt?.trim())
  const flag_missing_metadata = !hasYoast && !hasNative

  // missing_keyword (A5 — exact URL segment match)
  let flag_missing_keyword = false
  if (input.project_keywords.length > 0 && input.page_link) {
    const linkLower = input.page_link.toLowerCase()
    const hasKeyword = input.project_keywords.some(kw => {
      const slug = kw.toLowerCase().replace(/\s+/g, '-')
      return linkLower.includes(slug) || linkLower.includes(kw.toLowerCase())
    })
    flag_missing_keyword = !hasKeyword
  }

  return { flag_orphan, flag_weak_page, flag_outdated, flag_missing_metadata, flag_missing_keyword }
}

/**
 * Faz B — AI enrichment tamamlandıktan sonra çağrılır.
 * D-13: duplicate_intent = aynı project'te aynı primary_intent'e sahip 2+ sayfa.
 * RESEARCH.md Pitfall 6: Bu fonksiyon AI enrichment DB'ye yazıldıktan SONRA çağrılır.
 * @param pages project_imported_pages dizisi — primary_intent dolu olmalı
 * @returns wp_id → flag_duplicate_intent map
 */
export function computeDuplicateIntentFlags(
  pages: Array<{ wp_id: number; primary_intent: string | null }>
): Map<number, boolean> {
  const intentCount = new Map<string, number>()
  for (const page of pages) {
    if (page.primary_intent) {
      intentCount.set(page.primary_intent, (intentCount.get(page.primary_intent) ?? 0) + 1)
    }
  }

  const result = new Map<number, boolean>()
  for (const page of pages) {
    const count = page.primary_intent ? (intentCount.get(page.primary_intent) ?? 0) : 0
    result.set(page.wp_id, count > 1)
  }
  return result
}
```
  </action>
  <verify>
    <automated>cd "C:/Users/baris/Documents/seo-machine" && npx vitest run src/lib/wp/audit-flags.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/wp/audit-flags.ts` mevcut, `export function computeAuditFlags` ve `export function computeDuplicateIntentFlags` içeriyor
    - `npx vitest run src/lib/wp/audit-flags.test.ts` → tüm testler PASS
    - `flag_weak_page: boolean | null` tip tanımı — NOT NULL değil (D-10 GSC yoksa null)
  </acceptance_criteria>
  <done>audit-flags.ts yazıldı; tüm flag threshold testleri yeşil</done>
</task>

<task type="auto">
  <name>Task 2: gsc-match.ts</name>
  <files>src/lib/wp/gsc-match.ts</files>
  <read_first>
    - src/lib/gsc/auth.ts (getValidGscToken signature)
    - src/lib/gsc/search-analytics.ts (fetchSearchAnalytics signature + dönüş tipi)
    - src/lib/monitoring/aggregation.ts (URL join pattern — satır 196-230)
    - src/lib/wp/url-normalize.ts (normalizeUrl)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-RESEARCH.md §GSC URL Matching (Open Question 1 kararı)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-RESEARCH.md §Pitfall 5 (sc-domain: property)
  </read_first>
  <action>
`src/lib/wp/gsc-match.ts` yaz. RESEARCH.md Open Question 1 kararı: gsc_metrics tablosunu bypass et, Search Analytics API'ye doğrudan istek at:

```typescript
'use server'
/**
 * GSC URL eşleştirme — imported_pages için GSC trafik verisi.
 * RESEARCH.md Open Question 1 kararı: gsc_metrics tablosunu bypass et;
 * Search Analytics API'ye doğrudan istek at (import zamanı snapshot).
 * Pitfall 5: sc-domain: property → URL bazlı join yapılamaz → null döner.
 * D-14: URL normalization zinciri — trailing slash, www, http normalize edilir.
 * D-15: projects.gsc_property_url domain ile imported_page.link domain eşleşmeli.
 */

import { getValidGscToken } from '@/lib/gsc/auth'
import { normalizeUrl } from './url-normalize'

export interface GscPageMetric {
  url: string            // normalize edilmiş URL
  clicks: number
  impressions: number
  avgPosition: number | null
}

/**
 * Proje için GSC'den son 28 günlük sayfa bazlı trafik verisi çeker.
 * @param projectId Supabase project UUID
 * @param gscPropertyUrl GSC property URL (projects.gsc_property_url)
 * @returns URL → GscPageMetric map (normalize edilmiş URL key)
 * @returns null — GSC bağlı değil, sc-domain: property, veya token hatası
 */
export async function matchGscData(
  projectId: string,
  gscPropertyUrl: string | null
): Promise<Map<string, GscPageMetric> | null> {
  // GSC bağlı değil
  if (!gscPropertyUrl) return null

  // Pitfall 5: sc-domain: property — URL bazlı join yapılamaz
  if (gscPropertyUrl.startsWith('sc-domain:')) {
    console.warn(`[gsc-match] sc-domain: property detected (${gscPropertyUrl}) — URL join atlandı`)
    return null
  }

  // Token al
  const token = await getValidGscToken(projectId)
  if (!token) return null

  try {
    // Son 28 gün — D-10 weak_page threshold ile uyumlu
    const endDate = new Date()
    const startDate = new Date()
    startDate.setDate(startDate.getDate() - 28)
    const formatDate = (d: Date) => d.toISOString().split('T')[0]

    // Search Analytics API'ye direkt istek — page bazlı
    const response = await fetch(
      `https://www.googleapis.com/webmasters/v3/sites/${encodeURIComponent(gscPropertyUrl)}/searchAnalytics/query`,
      {
        method: 'POST',
        headers: {
          Authorization: `Bearer ${token}`,
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({
          startDate: formatDate(startDate),
          endDate: formatDate(endDate),
          dimensions: ['page'],
          rowLimit: 25000,
        }),
        signal: AbortSignal.timeout(30_000),
      }
    )

    if (!response.ok) {
      console.error(`[gsc-match] GSC API error: ${response.status}`)
      return null
    }

    const data = await response.json()
    const rows: Array<{ keys: string[]; clicks: number; impressions: number; position: number }> =
      data.rows ?? []

    // URL → GscPageMetric map (normalize edilmiş URL key)
    const metricsMap = new Map<string, GscPageMetric>()
    for (const row of rows) {
      const rawUrl = row.keys[0]
      const normalizedUrl = normalizeUrl(rawUrl)
      metricsMap.set(normalizedUrl, {
        url: normalizedUrl,
        clicks: row.clicks,
        impressions: row.impressions,
        avgPosition: row.position ?? null,
      })
    }

    return metricsMap
  } catch (err) {
    console.error('[gsc-match] GSC data fetch failed:', err)
    return null
  }
}

/**
 * Imported pages için GSC metriklerini URL eşleştirmesi ile atar.
 * D-14: Eşleşme bulunamazsa GSC alanları null kalır.
 */
export function enrichWithGscMetrics(
  importedPages: Array<{ link: string | null }>,
  gscMap: Map<string, GscPageMetric> | null
): Array<{ gsc_clicks: number | null; gsc_impressions: number | null; gsc_avg_position: number | null }> {
  if (!gscMap) {
    return importedPages.map(() => ({ gsc_clicks: null, gsc_impressions: null, gsc_avg_position: null }))
  }

  return importedPages.map(page => {
    if (!page.link) {
      return { gsc_clicks: null, gsc_impressions: null, gsc_avg_position: null }
    }
    const normalizedLink = normalizeUrl(page.link)
    const metric = gscMap.get(normalizedLink)
    if (!metric) {
      return { gsc_clicks: null, gsc_impressions: null, gsc_avg_position: null }
    }
    return {
      gsc_clicks: metric.clicks,
      gsc_impressions: metric.impressions,
      gsc_avg_position: metric.avgPosition,
    }
  })
}
```
  </action>
  <verify>
    <automated>cd "C:/Users/baris/Documents/seo-machine" && npx vitest run src/lib/wp/gsc-match.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/wp/gsc-match.ts` mevcut, `export async function matchGscData` ve `export function enrichWithGscMetrics` içeriyor
    - `sc-domain:` property guard kodu dosyada geçiyor
    - `normalizeUrl` import'u mevcut
    - gsc-match.test.ts (Wave 0 stub) çalışıyor — testler fail ise modül dosya olarak tanınıyor
  </acceptance_criteria>
  <done>gsc-match.ts yazıldı; GSC URL eşleştirme ve enrichWithGscMetrics hazır</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gsc-match.ts → GSC API | Bearer token harici servise gönderiliyor |
| audit-flags.ts | Pure function — DB/network erişimi yok |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.5-04-01 | Information Disclosure | gsc-match.ts GSC token | mitigate | Token `Authorization: Bearer` header'da; asla loglanmaz |
| T-15.5-04-02 | Tampering | computeAuditFlags input | accept | Pure function — DB okuma yok; input Route Handler'dan geliyor, ownership orada kontrol edildi |
</threat_model>

<verification>
```bash
cd "C:/Users/baris/Documents/seo-machine" && npx vitest run src/lib/wp/audit-flags.test.ts --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|✓|×)"
```
Tüm audit-flags testleri PASS olmalı.
</verification>

<success_criteria>
- audit-flags.ts: tüm test senaryoları PASS (weak_page null/true/false, outdated, missing_metadata)
- gsc-match.ts: yazıldı, sc-domain guard mevcut, normalizeUrl kullanılıyor
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/15.5-wordpress-site-import-engine/15.5-04-SUMMARY.md` oluştur.
</output>
