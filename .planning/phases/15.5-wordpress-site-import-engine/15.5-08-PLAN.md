---
phase: 15.5
plan: 08
type: execute
wave: 5
depends_on:
  - 15.5-07
files_modified:
  - src/app/(dashboard)/projeler/[id]/site-analizi/page.tsx
  - src/app/(dashboard)/projeler/[id]/site-analizi/AuditFlagChips.tsx
  - src/app/(dashboard)/projeler/[id]/site-analizi/ImportedPageTree.tsx
  - src/app/(dashboard)/projeler/[id]/site-analizi/AuditFlagBadge.tsx
  - src/app/(dashboard)/projeler/[id]/ProjectNav.tsx
autonomous: false
requirements:
  - IMP-02
  - IMP-03
  - IMP-04
  - IMP-05

must_haves:
  truths:
    - "ProjectNav'da 'Site Analizi' item İzleme'nin hemen ardından görünüyor (D-19)"
    - "/projeler/[id]/site-analizi SSR sayfası imported pages'i tree görünümde listeliyor"
    - "Her satırda: başlık + URL + audit flag rozetleri + GSC tıklama + ort. pozisyon (D-17)"
    - "Audit flag chip'leri multiselect OR mantığıyla çalışıyor (D-18)"
    - "Tree depth-based indent: paddingLeft = 16 + depth*20px, depth>0 için ↳ prefix (D-16)"
    - "Import hiç yapılmamışsa empty state görünüyor"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/site-analizi/page.tsx"
      provides: "SSR site analizi sayfası — flattenImportedTree + AuditFlagChips + ImportedPageTree"
      min_lines: 80
    - path: "src/app/(dashboard)/projeler/[id]/site-analizi/AuditFlagChips.tsx"
      provides: "Multiselect audit flag filtre chip'leri"
      min_lines: 50
    - path: "src/app/(dashboard)/projeler/[id]/site-analizi/ImportedPageTree.tsx"
      provides: "Depth-indent tree görünümü, audit flag badge'leri, GSC metrikleri"
      min_lines: 80
    - path: "src/app/(dashboard)/projeler/[id]/site-analizi/AuditFlagBadge.tsx"
      provides: "Flag badge wrapper — hardcoded className renkleri"
      min_lines: 20
  key_links:
    - from: "src/app/(dashboard)/projeler/[id]/site-analizi/page.tsx"
      to: "src/lib/wp/normalize.ts"
      via: "flattenImportedTree import"
      pattern: "flattenImportedTree"
    - from: "src/app/(dashboard)/projeler/[id]/ProjectNav.tsx"
      to: "src/app/(dashboard)/projeler/[id]/site-analizi/page.tsx"
      via: "nav item href"
      pattern: "site-analizi"
---

<objective>
Site analizi sayfası ve tüm bileşenlerini yaz: tree görünüm, audit flag chip filtreleri, rozet sistemi, ProjectNav güncellemesi.

Purpose: IMP-02 + IMP-05 — kullanıcı imported pages'i hiyerarşik tree'de görür, audit flag'lere göre filtreler.
Output: site-analizi/ dizini (4 bileşen) + ProjectNav güncellemesi
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15.5-wordpress-site-import-engine/15.5-CONTEXT.md
@.planning/phases/15.5-wordpress-site-import-engine/15.5-UI-SPEC.md
@.planning/phases/15.5-wordpress-site-import-engine/15.5-PATTERNS.md

<interfaces>
<!-- site-blueprint/page.tsx flattenTree + tree row — EXACT ANALOG -->
```typescript
// PATTERNS.md §ImportedPageTree.tsx bölümü:
// Grid: grid-cols-[1fr_1fr_auto_auto_auto_auto]
// Tree row depth: style={{ paddingLeft: `${16 + page.depth * 20}px` }}
// Child prefix: {page.depth > 0 && <span className="text-muted-foreground mr-1.5">↳</span>}
```

<!-- izleme/period-tab-bar.tsx — AuditFlagChips analog -->
```typescript
// PATTERNS.md §AuditFlagChips.tsx:
// 'use client', cn() utility
// isActive → bg-blue-500/20 text-blue-400 font-medium border border-blue-500/30
// inactive → text-muted-foreground hover:text-foreground
// aria-pressed={isActive}
// min-h-[44px] touch target
```

<!-- izleme/page.tsx — site-analizi page layout EXACT ANALOG -->
```typescript
// PATTERNS.md §site-analizi/page.tsx:
// flex flex-col h-screen
// p-8 pb-4 header area
// flex flex-1 min-h-0 body
// w-64 shrink-0 border-r sidebar (ProjectNav)
// flex-1 min-w-0 overflow-y-auto p-8 content
```

<!-- UI-SPEC Audit flag badge renkleri (hardcoded className): -->
// flag_orphan: 'bg-yellow-500/20 text-yellow-400'
// flag_weak_page: 'bg-red-500/20 text-red-400'
// flag_outdated: 'bg-orange-500/20 text-orange-400'
// flag_missing_metadata/keyword/duplicate_intent: 'bg-slate-700 text-slate-400'

<!-- UI-SPEC Chip label (Türkçe, sırayla): -->
// Orphan | Zayıf | Eski İçerik | Meta Eksik | Anahtar Kelime Yok | Tekrarlı Intent
// Chip key: flag_orphan | flag_weak_page | flag_outdated | flag_missing_metadata | flag_missing_keyword | flag_duplicate_intent

<!-- flattenImportedTree import: src/lib/wp/normalize -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuditFlagBadge + AuditFlagChips + ImportedPageTree bileşenleri</name>
  <files>
    src/app/(dashboard)/projeler/[id]/site-analizi/AuditFlagBadge.tsx,
    src/app/(dashboard)/projeler/[id]/site-analizi/AuditFlagChips.tsx,
    src/app/(dashboard)/projeler/[id]/site-analizi/ImportedPageTree.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/izleme/period-tab-bar.tsx (AuditFlagChips analog — tam dosya)
    - src/app/(dashboard)/projeler/[id]/site-blueprint/page.tsx (tree row pattern — satır 358-510)
    - src/components/ui/badge.tsx (Badge component interface)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-UI-SPEC.md §Component Inventory ve §Color
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-PATTERNS.md §AuditFlagChips ve §ImportedPageTree bölümleri
  </read_first>
  <action>
`src/app/(dashboard)/projeler/[id]/site-analizi/` dizinini oluştur.

**AuditFlagBadge.tsx:**
```tsx
import { Badge } from '@/components/ui/badge'

// UI-SPEC §Color — hardcoded className, variant prop kullanılmaz
const FLAG_BADGE_CONFIG: Record<string, { className: string; label: string }> = {
  flag_orphan:           { className: 'bg-yellow-500/20 text-yellow-400', label: 'Orphan' },
  flag_weak_page:        { className: 'bg-red-500/20 text-red-400', label: 'Zayıf' },
  flag_outdated:         { className: 'bg-orange-500/20 text-orange-400', label: 'Eski İçerik' },
  flag_missing_metadata: { className: 'bg-slate-700 text-slate-400', label: 'Meta Eksik' },
  flag_missing_keyword:  { className: 'bg-slate-700 text-slate-400', label: 'Anahtar Kelime Yok' },
  flag_duplicate_intent: { className: 'bg-slate-700 text-slate-400', label: 'Tekrarlı Intent' },
}

interface AuditFlagBadgeProps {
  flag: keyof typeof FLAG_BADGE_CONFIG
}

export function AuditFlagBadge({ flag }: AuditFlagBadgeProps) {
  const config = FLAG_BADGE_CONFIG[flag]
  if (!config) return null
  return (
    <Badge className={`px-2 py-0.5 text-xs font-normal ${config.className}`}>
      {config.label}
    </Badge>
  )
}
```

**AuditFlagChips.tsx:**
period-tab-bar.tsx pattern — Set-based toggle state, OR mantığı:
```tsx
'use client'
import { cn } from '@/lib/utils'

const CHIPS = [
  { key: 'flag_orphan',           label: 'Orphan' },
  { key: 'flag_weak_page',        label: 'Zayıf' },
  { key: 'flag_outdated',         label: 'Eski İçerik' },
  { key: 'flag_missing_metadata', label: 'Meta Eksik' },
  { key: 'flag_missing_keyword',  label: 'Anahtar Kelime Yok' },
  { key: 'flag_duplicate_intent', label: 'Tekrarlı Intent' },
] as const

type FlagKey = typeof CHIPS[number]['key']

interface AuditFlagChipsProps {
  activeFlags: Set<FlagKey>
  onToggle: (flag: FlagKey) => void
  onClearAll: () => void
}

export function AuditFlagChips({ activeFlags, onToggle, onClearAll }: AuditFlagChipsProps) {
  return (
    <div className="flex items-center gap-2 mb-4 flex-wrap">
      <div className="inline-flex items-center gap-1 bg-card border border-border rounded-lg p-1 flex-wrap">
        {CHIPS.map(chip => {
          const isActive = activeFlags.has(chip.key)
          return (
            <button
              key={chip.key}
              type="button"
              aria-pressed={isActive}
              onClick={() => onToggle(chip.key)}
              className={cn(
                'px-4 py-2 rounded-md text-sm transition-colors min-h-[44px]',
                isActive
                  ? 'bg-blue-500/20 text-blue-400 font-medium border border-blue-500/30'
                  : 'text-muted-foreground hover:text-foreground'
              )}
            >
              {chip.label}
            </button>
          )
        })}
      </div>
      {activeFlags.size > 0 && (
        <button
          type="button"
          onClick={onClearAll}
          className="text-sm text-muted-foreground hover:text-foreground"
        >
          Filtreleri temizle
        </button>
      )}
    </div>
  )
}
```

**ImportedPageTree.tsx:**
site-blueprint/page.tsx tree row pattern — grid-cols-[1fr_1fr_auto_auto_auto_auto]:
```tsx
'use client'
import { useState } from 'react'
import Link from 'next/link'
import type { ImportedPageWithDepth } from '@/lib/wp/normalize'
import { AuditFlagBadge } from './AuditFlagBadge'
import { AuditFlagChips } from './AuditFlagChips'
import { cn } from '@/lib/utils'

type FlagKey = 'flag_orphan' | 'flag_weak_page' | 'flag_outdated' | 'flag_missing_metadata' | 'flag_missing_keyword' | 'flag_duplicate_intent'
const ALL_FLAGS: FlagKey[] = ['flag_orphan', 'flag_weak_page', 'flag_outdated', 'flag_missing_metadata', 'flag_missing_keyword', 'flag_duplicate_intent']

interface ImportedPageTreeProps {
  pages: ImportedPageWithDepth[]
}

export function ImportedPageTree({ pages }: ImportedPageTreeProps) {
  const [activeFlags, setActiveFlags] = useState<Set<FlagKey>>(new Set())

  function toggleFlag(flag: FlagKey) {
    setActiveFlags(prev => {
      const next = new Set(prev)
      if (next.has(flag)) next.delete(flag)
      else next.add(flag)
      return next
    })
  }

  // OR mantığı: herhangi bir aktif flag'i taşıyan sayfalar gösterilir
  const filtered = activeFlags.size === 0
    ? pages
    : pages.filter(page => [...activeFlags].some(flag => Boolean(page[flag as keyof typeof page])))

  return (
    <div>
      <AuditFlagChips
        activeFlags={activeFlags}
        onToggle={toggleFlag}
        onClearAll={() => setActiveFlags(new Set())}
      />

      {filtered.length === 0 ? (
        <div className="rounded-lg border border-border bg-card p-8 text-center">
          <p className="text-sm text-muted-foreground">Bu filtrelere uyan sayfa bulunamadı.</p>
        </div>
      ) : (
        <div className="rounded-md border border-border overflow-hidden">
          {/* Header */}
          <div className="grid grid-cols-[1fr_1fr_auto_auto_auto_auto] gap-0 bg-muted/50 border-b border-border text-xs text-muted-foreground font-normal">
            <div className="px-4 py-2">Sayfa</div>
            <div className="px-4 py-2">URL</div>
            <div className="px-4 py-2 min-w-[180px]">Durum</div>
            <div className="px-4 py-2 w-20 text-right">Tıklama</div>
            <div className="px-4 py-2 w-20 text-right">Pozisyon</div>
            <div className="px-4 py-2 w-8"></div>
          </div>

          {/* Rows */}
          {filtered.map(page => (
            <div
              key={page.wp_id}
              className="grid grid-cols-[1fr_1fr_auto_auto_auto_auto] gap-0 border-b border-border last:border-0 items-center text-sm hover:bg-muted/30 transition-colors"
            >
              {/* Başlık — depth indent + ↳ */}
              <div
                className="px-4 py-2 truncate flex items-center"
                style={{ paddingLeft: `${16 + page.depth * 20}px` }}
              >
                {page.depth > 0 && (
                  <span className="text-muted-foreground mr-1.5">↳</span>
                )}
                <span className="truncate font-normal">{page.title}</span>
              </div>

              {/* URL */}
              <div className="px-4 py-2 truncate">
                {page.link ? (
                  <a
                    href={page.link}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="text-xs text-muted-foreground hover:text-foreground truncate block"
                  >
                    {page.slug || page.link}
                  </a>
                ) : (
                  <span className="text-xs text-muted-foreground">—</span>
                )}
              </div>

              {/* Audit flag badge'leri */}
              <div className="px-4 py-2 flex items-center gap-1 flex-wrap min-w-[180px]">
                {ALL_FLAGS.map(flag =>
                  page[flag as keyof typeof page] ? (
                    <AuditFlagBadge key={flag} flag={flag} />
                  ) : null
                )}
                {page.primary_intent === null && page.content_summary === null && (
                  <span className="text-xs bg-slate-800 text-slate-400 px-2 py-0.5 rounded-sm">
                    Analiz ediliyor...
                  </span>
                )}
              </div>

              {/* GSC tıklama */}
              <div className="px-4 py-2 w-20 text-right">
                <span
                  className={cn('text-xs', page.gsc_clicks !== null ? 'text-foreground' : 'text-muted-foreground')}
                  aria-label={page.gsc_clicks === null ? 'GSC verisi yok' : undefined}
                >
                  {page.gsc_clicks !== null ? page.gsc_clicks.toLocaleString('tr-TR') : '—'}
                </span>
              </div>

              {/* GSC pozisyon */}
              <div className="px-4 py-2 w-20 text-right">
                <span
                  className={cn('text-xs', page.gsc_avg_position !== null ? 'text-foreground' : 'text-muted-foreground')}
                  aria-label={page.gsc_avg_position === null ? 'GSC verisi yok' : undefined}
                >
                  {page.gsc_avg_position !== null ? Number(page.gsc_avg_position).toFixed(1) : '—'}
                </span>
              </div>

              {/* Boş aksiyon alanı */}
              <div className="px-4 py-2 w-8"></div>
            </div>
          ))}
        </div>
      )}
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd "C:/Users/baris/Documents/seo-machine" && ls "src/app/(dashboard)/projeler/[id]/site-analizi/" 2>/dev/null | wc -l || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - `AuditFlagBadge.tsx` mevcut, `FLAG_BADGE_CONFIG` hardcoded className renkleri içeriyor (variant prop yok)
    - `AuditFlagChips.tsx` mevcut, `'use client'`, `aria-pressed`, `min-h-[44px]`, `bg-blue-500/20 text-blue-400` içeriyor
    - `ImportedPageTree.tsx` mevcut, `'use client'`, `activeFlags`, `Set<FlagKey>` state içeriyor
    - `paddingLeft: \`${16 + page.depth * 20}px\`` pattern ImportedPageTree.tsx'de geçiyor
    - `↳` karakteri ImportedPageTree.tsx'de geçiyor
    - `Filtreleri temizle` metni AuditFlagChips.tsx'de geçiyor
    - `aria-label` GSC "verisi yok" için mevcut
  </acceptance_criteria>
  <done>3 bileşen oluşturuldu — badge, chip, tree; tüm UI-SPEC kuralları uygulandı</done>
</task>

<task type="auto">
  <name>Task 2: site-analizi/page.tsx + ProjectNav güncellemesi</name>
  <files>
    src/app/(dashboard)/projeler/[id]/site-analizi/page.tsx,
    src/app/(dashboard)/projeler/[id]/ProjectNav.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/izleme/page.tsx (SSR page EXACT analog — tam dosya)
    - src/app/(dashboard)/projeler/[id]/ProjectNav.tsx (mevcut — 'Site Analizi' item eklenecek)
    - src/lib/wp/normalize.ts (flattenImportedTree + ImportedPage + ImportedPageWithDepth)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-UI-SPEC.md §Layout Patterns
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-PATTERNS.md §site-analizi/page.tsx ve §ProjectNav bölümleri
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-CONTEXT.md (D-16, D-19)
  </read_first>
  <action>
**site-analizi/page.tsx** yaz (izleme/page.tsx EXACT analog):

SSR flow:
1. `createClient()` → `auth.getUser()` → notFound() if no user
2. `projects` tablosundan project + import_status çek (ownership check)
3. `project_imported_pages` tablosundan tüm sayfaları çek (project_id + user ownership)
4. `flattenImportedTree(pages)` ile hiyerarşik liste oluştur
5. Render: izleme/page.tsx layout (flex flex-col h-screen)

Empty state (import hiç yapılmamış — import_status null veya 'idle'):
```tsx
<div className="rounded-lg border border-border bg-card p-8 text-center">
  <p className="text-base font-semibold">Henüz içerik aktarılmadı</p>
  <p className="text-sm text-muted-foreground mt-2">
    WordPress sitenizin içeriğini içe aktararak SEO denetimini başlatın.
  </p>
  <Link href={`/projeler/${id}`} className="inline-flex items-center mt-4 text-sm text-foreground hover:underline">
    WordPress Sitemi İçeri Al →
  </Link>
</div>
```

Import sürüyor state (import_status 'running' veya 'enriching'):
```tsx
<div className="text-center py-12">
  <p className="text-sm text-muted-foreground">İçe aktarma devam ediyor...</p>
</div>
```

Import tamamlandı — ImportedPageTree render:
```tsx
<ImportedPageTree pages={flattenedPages} />
```

Select sorgusu (project_imported_pages):
```typescript
const { data: importedPages } = await supabase
  .from('project_imported_pages')
  .select(`
    wp_id, wp_type, title, slug, link, parent_wp_id,
    wp_modified_at, content_summary, primary_intent,
    gsc_clicks, gsc_impressions, gsc_avg_position,
    flag_orphan, flag_weak_page, flag_outdated,
    flag_missing_metadata, flag_missing_keyword, flag_duplicate_intent
  `)
  .eq('project_id', id)
  .order('wp_id', { ascending: true })
```

---

**ProjectNav.tsx güncellemesi:**
Mevcut getNavItems() dizisine İzleme'den hemen sonra ekle:
```typescript
{ label: 'Site Analizi', href: `/projeler/${projectId}/site-analizi`, built: true },
```

activePath prop karşılaştırması `/projeler/${id}/site-analizi` ile çalışacak.
  </action>
  <verify>
    <automated>cd "C:/Users/baris/Documents/seo-machine" && grep -c "Site Analizi" "src/app/(dashboard)/projeler/[id]/ProjectNav.tsx" 2>/dev/null || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - `site-analizi/page.tsx` mevcut, `notFound()` ve `createClient()` ve `flattenImportedTree` import içeriyor
    - `ImportedPageTree` import ve render mevcut
    - `flex flex-col h-screen` layout class geçiyor
    - `w-64 shrink-0 border-r border-border` sidebar class geçiyor (izleme/page.tsx ile aynı)
    - `ProjectNav.tsx`'de `'Site Analizi'` string geçiyor
    - ProjectNav'da `site-analizi` href geçiyor
    - "Henüz içerik aktarılmadı" empty state metni page.tsx'de mevcut
    - "İçe aktarma devam ediyor..." importing state metni page.tsx'de mevcut
  </acceptance_criteria>
  <done>site-analizi/page.tsx + ProjectNav güncellemesi tamamlandı</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| site-analizi/page.tsx → Supabase | SSR user-scoped client; RLS SELECT policy devreye giriyor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.5-08-01 | Information Disclosure | project_imported_pages SELECT | mitigate | User-scoped createClient() + RLS SELECT policy (plan 01'de tanımlandı) — cross-user veri erişimi imkânsız |
| T-15.5-08-02 | — | External link (WP) | accept | `target="_blank" rel="noopener noreferrer"` — tabnabbing önlendi |
</threat_model>

<verification>
```bash
grep "Site Analizi" "src/app/(dashboard)/projeler/[id]/ProjectNav.tsx"
grep "flattenImportedTree" "src/app/(dashboard)/projeler/[id]/site-analizi/page.tsx"
grep "paddingLeft" "src/app/(dashboard)/projeler/[id]/site-analizi/ImportedPageTree.tsx"
```
3 satır bulunmalı.
</verification>

<success_criteria>
- site-analizi/page.tsx: SSR, empty state, tree render, izleme/page.tsx layout
- ProjectNav: 'Site Analizi' item İzleme'den sonra
- ImportedPageTree: depth indent, flag badge'leri, GSC metrikleri
- AuditFlagChips: multiselect, OR mantığı, Türkçe chip label'lar
- AuditFlagBadge: hardcoded className'ler, 6 flag tipi

**Checkpoint:** Kullanıcı /projeler/[id]/site-analizi sayfasını tarayıcıda açarak:
1. ProjectNav'da "Site Analizi" görüyor mu?
2. Import yapılmamışsa empty state görünüyor mu?
3. Import tamamlandıysa tree satırları depth indent ile görünüyor mu?
4. Flag chip'leri tıklanıyor mu ve filtre çalışıyor mu?
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/15.5-wordpress-site-import-engine/15.5-08-SUMMARY.md` oluştur.
</output>
