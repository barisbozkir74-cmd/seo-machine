---
phase: 15.5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260428000001_imported_pages.sql
autonomous: true
requirements:
  - IMP-01
  - IMP-02
  - IMP-03
  - IMP-05

must_haves:
  truths:
    - "project_imported_pages tablosu UNIQUE(project_id, wp_id) constraint ile oluştu"
    - "projects tablosunda import_status, import_current, import_total, import_completed_at kolonları mevcut"
    - "RLS SELECT policy user'ın sadece kendi projelerinin sayfalarını görmesini sağlıyor"
    - "supabase db push hatasız tamamlandı"
  artifacts:
    - path: "supabase/migrations/20260428000001_imported_pages.sql"
      provides: "project_imported_pages tablo tanımı + projects ALTER"
      contains: "UNIQUE (project_id, wp_id)"
  key_links:
    - from: "project_imported_pages.project_id"
      to: "projects.id"
      via: "ON DELETE CASCADE FK"
      pattern: "REFERENCES public.projects\\(id\\) ON DELETE CASCADE"
---

<objective>
project_imported_pages tablosunu ve projects tablosundaki import progress kolonlarını oluşturan Supabase migration'ı yaz ve push et.

Purpose: Bu tablo olmadan WP import pipeline'ının hiçbir adımı çalışamaz. [BLOCKING] — Wave 2 ve sonrası bu tabloyu varsayar.
Output: Tek migration dosyası + başarılı supabase db push
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15.5-wordpress-site-import-engine/15.5-CONTEXT.md
@.planning/phases/15.5-wordpress-site-import-engine/15.5-RESEARCH.md

<interfaces>
<!-- Analog migration — supabase/migrations/20260427000002_gsc_schema.sql pattern -->
<!-- RLS policy: EXISTS subquery ile projects.user_id = auth.uid() -->
<!-- INSERT/UPDATE: service role yapar — RLS policy sadece SELECT tanımlanır -->

Mevcut projects tablosundaki mevcut import kolonları (YOKTUR — yeni eklenecek):
- import_status TEXT CHECK (... IN ('idle','running','enriching','complete','error'))
- import_current INTEGER DEFAULT 0
- import_total INTEGER DEFAULT 0
- import_completed_at TIMESTAMPTZ

gsc_schema.sql pattern (birebir kopyalanacak format):
```sql
ALTER TABLE public.projects
  ADD COLUMN IF NOT EXISTS gsc_tokens       JSONB,
  ADD COLUMN IF NOT EXISTS gsc_property_url TEXT;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration dosyasını yaz</name>
  <files>supabase/migrations/20260428000001_imported_pages.sql</files>
  <read_first>
    - supabase/migrations/20260427000002_gsc_schema.sql (RLS + index + UNIQUE pattern — EXACT analog)
    - supabase/migrations/20260426000002_add_wp_columns.sql (ALTER TABLE pattern)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-RESEARCH.md §DB Schema bölümü (tam şema)
  </read_first>
  <action>
Aşağıdaki tam SQL içeriğini `supabase/migrations/20260428000001_imported_pages.sql` dosyasına yaz:

**Bölüm 1 — projects tablosu genişletmesi:**
```sql
ALTER TABLE public.projects
  ADD COLUMN IF NOT EXISTS import_status       TEXT
    CHECK (import_status IS NULL OR import_status IN ('idle','running','enriching','complete','error')),
  ADD COLUMN IF NOT EXISTS import_current      INTEGER DEFAULT 0,
  ADD COLUMN IF NOT EXISTS import_total        INTEGER DEFAULT 0,
  ADD COLUMN IF NOT EXISTS import_completed_at TIMESTAMPTZ;
```

**Bölüm 2 — project_imported_pages tablosu:**
RESEARCH.md §DB Schema bölümündeki tam CREATE TABLE ifadesini kullan:
- id UUID DEFAULT gen_random_uuid() PRIMARY KEY
- project_id UUID REFERENCES public.projects(id) ON DELETE CASCADE NOT NULL
- wp_id INTEGER NOT NULL
- wp_type TEXT NOT NULL CHECK (wp_type IN ('page','post','category','tag'))
- title TEXT NOT NULL
- slug TEXT
- link TEXT (public WP URL — 'link' field)
- parent_wp_id INTEGER (WP parent ID — 0 = root, null = kök)
- wp_created_at TIMESTAMPTZ
- wp_modified_at TIMESTAMPTZ
- content_summary TEXT (max 200 char — AI doldurur)
- primary_intent TEXT CHECK (primary_intent IS NULL OR primary_intent IN ('informational','commercial','transactional','navigational'))
- gsc_clicks INTEGER
- gsc_impressions INTEGER
- gsc_avg_position NUMERIC(5,2)
- flag_orphan BOOLEAN NOT NULL DEFAULT false
- flag_weak_page BOOLEAN (GSC yoksa NULL — D-10)
- flag_outdated BOOLEAN NOT NULL DEFAULT false
- flag_missing_metadata BOOLEAN NOT NULL DEFAULT false
- flag_missing_keyword BOOLEAN NOT NULL DEFAULT false
- flag_duplicate_intent BOOLEAN NOT NULL DEFAULT false
- imported_at TIMESTAMPTZ DEFAULT now() NOT NULL
- updated_at TIMESTAMPTZ DEFAULT now() NOT NULL
- UNIQUE (project_id, wp_id)

**Bölüm 3 — Index:**
```sql
CREATE INDEX IF NOT EXISTS idx_imported_pages_project
  ON public.project_imported_pages(project_id);
```

**Bölüm 4 — RLS:**
gsc_schema.sql'deki EXACT pattern ile (EXISTS subquery, sadece SELECT policy, INSERT/UPDATE yorum satırı):
```sql
ALTER TABLE public.project_imported_pages ENABLE ROW LEVEL SECURITY;

CREATE POLICY "imported_pages_select_own"
  ON public.project_imported_pages FOR SELECT
  USING (
    EXISTS (
      SELECT 1 FROM public.projects p
      WHERE p.id = project_imported_pages.project_id
        AND p.user_id = auth.uid()
    )
  );
-- INSERT/UPDATE: service role ile yapılır — service role RLS bypass eder
```
  </action>
  <verify>
    <automated>grep -c "UNIQUE (project_id, wp_id)" "C:/Users/baris/Documents/seo-machine/supabase/migrations/20260428000001_imported_pages.sql"</automated>
  </verify>
  <acceptance_criteria>
    - Dosya `supabase/migrations/20260428000001_imported_pages.sql` mevcutsa
    - `UNIQUE (project_id, wp_id)` ifadesi dosyada geçiyorsa
    - `import_status` kolonu CHECK constraint ile projects tablosuna ekleniyor
    - `flag_weak_page BOOLEAN` (NOT NULL DEĞİL — GSC yoksa NULL olabilmeli, D-10)
    - `imported_pages_select_own` policy adı tam bu şekilde yazılmış
  </acceptance_criteria>
  <done>Migration dosyası tüm kolonları ve constraint'leri içeriyor</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] supabase db push</name>
  <files></files>
  <read_first>
    - supabase/migrations/20260428000001_imported_pages.sql (bir önceki task'ta oluşturulan dosya)
  </read_first>
  <action>
Aşağıdaki komutu çalıştır:
```bash
cd "C:/Users/baris/Documents/seo-machine" && npx supabase db push
```

Başarı çıktısı: "Applying migration ... imported_pages.sql" ve sıfır hata.

Hata durumu: Eğer "already exists" hatası varsa `-- IF NOT EXISTS` guard'larının eksiksiz olduğunu kontrol et (migration dosyasını düzelt, tekrar çalıştır).
  </action>
  <verify>
    <automated>cd "C:/Users/baris/Documents/seo-machine" && npx supabase db push 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase db push` komutu exit code 0 ile tamamlanıyor
    - Çıktıda "ERROR" veya "error" içeren satır yok
    - Çıktıda "imported_pages" adını içeren migration uygulandığına dair mesaj var (veya "No new migrations to apply" — zaten push edilmişse)
  </acceptance_criteria>
  <done>DB schema push hatasız tamamlandı; project_imported_pages tablosu canlı DB'de mevcut</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service role → project_imported_pages | INSERT/UPDATE RLS bypass ile yapılır; sadece import Route Handler erişir |
| user → project_imported_pages | SELECT RLS ile kısıtlı; sadece kendi projelerinin sayfaları |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.5-01-01 | Tampering | project_imported_pages INSERT | mitigate | INSERT/UPDATE sadece service role — RLS'de policy tanımlanmadı; anon/authenticated role yazamaz |
| T-15.5-01-02 | Information Disclosure | SELECT policy | mitigate | EXISTS(projects.user_id = auth.uid()) — cross-project veri sızıntısı engellendi |
</threat_model>

<verification>
Migration dosyası oluştu + supabase db push başarıyla tamamlandı.

```bash
grep -c "UNIQUE (project_id, wp_id)" supabase/migrations/20260428000001_imported_pages.sql
# → 1
grep "import_status" supabase/migrations/20260428000001_imported_pages.sql
# → ADD COLUMN IF NOT EXISTS import_status ...
```
</verification>

<success_criteria>
- project_imported_pages tablosu Supabase'de oluştu
- projects tablosunda import_status / import_current / import_total / import_completed_at kolonları eklendi
- RLS SELECT policy aktif
- supabase db push exit code 0
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/15.5-wordpress-site-import-engine/15.5-01-SUMMARY.md` oluştur.
</output>
