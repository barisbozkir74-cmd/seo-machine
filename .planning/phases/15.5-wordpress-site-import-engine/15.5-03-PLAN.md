---
phase: 15.5
plan: 03
type: execute
wave: 2
depends_on:
  - 15.5-01
  - 15.5-02
files_modified:
  - src/lib/wp/import.ts
  - src/lib/wp/normalize.ts
  - src/lib/wp/url-normalize.ts
autonomous: true
requirements:
  - IMP-01
  - IMP-02

must_haves:
  truths:
    - "fetchAllWpContent() pages/posts/categories/tags endpoint'lerini per_page=100 ile paginate ederek çeker"
    - "normalizeWpPage() WP response objesini ImportedPage DB shape'ine dönüştürür"
    - "normalizeUrl() trailing slash / www / http / query string normalize eder"
    - "flattenImportedTree() parent/child ilişkisini depth ile doğru çözüyor"
    - "normalize.test.ts ve url-normalize.test.ts yeşile döndü"
    - "ImportedPage interface yoast_title ve yoast_description alanlarını içeriyor"
  artifacts:
    - path: "src/lib/wp/import.ts"
      provides: "fetchAllWpContent pagination loop + service client upsert"
      exports: ["fetchAllWpContent"]
    - path: "src/lib/wp/normalize.ts"
      provides: "normalizeWpPage + flattenImportedTree"
      exports: ["normalizeWpPage", "flattenImportedTree", "ImportedPage", "ImportedPageWithDepth"]
    - path: "src/lib/wp/url-normalize.ts"
      provides: "normalizeUrl pure function"
      exports: ["normalizeUrl"]
  key_links:
    - from: "src/lib/wp/import.ts"
      to: "src/lib/supabase/vault.ts"
      via: "getWordPressCredentials(projectId)"
      pattern: "getWordPressCredentials"
    - from: "src/app/api/wp/import/route.ts"
      to: "src/lib/wp/import.ts"
      via: "fetchAllWpContent import"
      pattern: "fetchAllWpContent"
    - from: "ImportedPage.yoast_title"
      to: "computeAuditFlags flag_missing_metadata"
      via: "yoast_title: string|null geçirilir; null ise flag=true"
      pattern: "yoast_title"
---

<objective>
WP REST API pagination loop (import.ts), WP response normalizasyonu (normalize.ts) ve URL normalizasyon yardımcısını (url-normalize.ts) yaz. Test dosyaları bu plan tamamlandıktan sonra yeşile dönmeli.

Purpose: Import pipeline'ının veri toplama ve dönüştürme katmanı — Route Handler bu lib'leri çağırır.
Output: 3 lib dosyası + normalize ve url-normalize testleri green
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15.5-wordpress-site-import-engine/15.5-CONTEXT.md
@.planning/phases/15.5-wordpress-site-import-engine/15.5-RESEARCH.md
@.planning/phases/15.5-wordpress-site-import-engine/15.5-PATTERNS.md

<interfaces>
<!-- vault.ts getWordPressCredentials signature: -->
```typescript
// src/lib/supabase/vault.ts — VERIFIED
export async function getWordPressCredentials(
  projectId: string
): Promise<{ wpUrl: string; appPassword: string } | null>
```

<!-- gsc/sync/route.ts service client pattern — EXACT COPY: -->
```typescript
import { createClient as createServiceClient } from '@supabase/supabase-js'
function getServiceClient() {
  return createServiceClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!
  )
}
```

<!-- project_imported_pages upsert conflict key: -->
```typescript
// RESEARCH.md Pattern 2
onConflict: 'project_id,wp_id', ignoreDuplicates: false
```

<!-- WP REST API X-WP-TotalPages header (RESEARCH.md Pattern 1): -->
```typescript
totalPages = parseInt(res.headers.get('X-WP-TotalPages') ?? '1', 10)
```

<!-- Gerekli WP _fields parametresi (RESEARCH.md Pitfall 2): -->
// _fields=id,title,link,slug,parent,date,modified,status,menu_order,yoast_head_json,excerpt
// Content HTML asla çekilmez
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: url-normalize.ts</name>
  <files>src/lib/wp/url-normalize.ts</files>
  <read_first>
    - src/lib/wp/url-normalize.test.ts (test beklentilerini oku — önce bu dosya)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-RESEARCH.md §Pattern 3 (URL normalization)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-CONTEXT.md (D-14, D-15)
  </read_first>
  <behavior>
    - 'https://www.example.com/page/' → 'https://example.com/page'
    - 'http://example.com/page' → 'https://example.com/page'
    - 'https://example.com/page?q=1#section' → 'https://example.com/page'
    - 'https://example.com/' → 'https://example.com'
    - Geçersiz URL (parse hatası) → input değiştirilmeden döndürülür
  </behavior>
  <action>
`src/lib/wp/url-normalize.ts` dosyasını oluştur. RESEARCH.md Pattern 3'teki regex chain yaklaşımı yerine native `URL` API kullan (RESEARCH.md "Don't Hand-Roll" bölümü):

```typescript
/**
 * URL normalizasyonu — GSC ve WP imported page URL'lerini karşılaştırmak için.
 * D-14: trailing slash, https/http, www/non-www normalize edilir.
 * Query string ve fragment kaldırılır.
 */
export function normalizeUrl(url: string): string {
  try {
    const parsed = new URL(url.trim())
    // https/http normalize
    parsed.protocol = 'https:'
    // www kaldır
    if (parsed.hostname.startsWith('www.')) {
      parsed.hostname = parsed.hostname.slice(4)
    }
    // query string ve fragment kaldır
    parsed.search = ''
    parsed.hash = ''
    // trailing slash kaldır (root hariç — ama root için de kaldırılır beklentiye göre)
    let normalized = parsed.toString()
    if (normalized.endsWith('/') && parsed.pathname !== '/') {
      normalized = normalized.slice(0, -1)
    } else if (normalized.endsWith('/') && parsed.pathname === '/') {
      // https://example.com/ → https://example.com
      normalized = normalized.slice(0, -1)
    }
    return normalized
  } catch {
    // Parse edilemeyen URL → olduğu gibi döndür
    return url
  }
}
```
  </action>
  <verify>
    <automated>cd "C:/Users/baris/Documents/seo-machine" && npx vitest run src/lib/wp/url-normalize.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/wp/url-normalize.ts` mevcut, `export function normalizeUrl` içeriyor
    - `npx vitest run src/lib/wp/url-normalize.test.ts` → tüm testler PASS (5/5)
    - Dosyada `new URL(` kullanılıyor (regex chain değil)
  </acceptance_criteria>
  <done>url-normalize.ts yazıldı; 5 test yeşil</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: normalize.ts ve import.ts</name>
  <files>src/lib/wp/normalize.ts, src/lib/wp/import.ts</files>
  <read_first>
    - src/lib/wp/normalize.test.ts (test beklentileri — önce oku)
    - src/lib/wp/import.test.ts (pagination loop beklentileri)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-RESEARCH.md §Pattern 1 (WP pagination loop)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-RESEARCH.md §Pattern 4 (flattenImportedTree)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-PATTERNS.md §normalize.ts bölümü
    - src/lib/supabase/vault.ts (getWordPressCredentials signature)
  </read_first>
  <behavior>
    normalize.ts:
    - normalizeWpPage(wpItem, projectId, wpType) → ImportedPage shape (DB kolonlarıyla birebir eşleşen)
    - WP parent=0 → parent_wp_id=null
    - WP parent>0 → parent_wp_id=parent değeri
    - title.rendered HTML decode edilmiş string olarak alınır (WP bazen &#8220; gibi entity gönderir)
    - flattenImportedTree(pages) → ImportedPageWithDepth[] (parent önce, child sonra, root depth=0)
    - yoast_title: item.yoast_head_json?.title ?? null (Yoast eklentisi yoksa null — RESEARCH.md Pitfall 3)
    - yoast_description: item.yoast_head_json?.description ?? null

    import.ts:
    - fetchAllWpContent(wpUrl, authHeader, endpoint) → WpItem[]
    - X-WP-TotalPages header ile toplam sayfa sayısı alınır
    - Her sayfa için per_page=100 ile loop
    - WP API !ok → Error fırlatır
    - _fields parametresi zorunlu (RESEARCH.md Pitfall 2)
  </behavior>
  <action>
**src/lib/wp/normalize.ts** yaz:

```typescript
/**
 * WP REST API response objesini project_imported_pages DB shape'ine normalize eder.
 * D-08: pages, posts, categories, tags için aynı normalize fonksiyonu.
 */

export interface ImportedPage {
  project_id: string
  wp_id: number
  wp_type: 'page' | 'post' | 'category' | 'tag'
  title: string
  slug: string | null
  link: string | null
  parent_wp_id: number | null  // WP parent=0 → null (kök sayfa D-12)
  wp_created_at: string | null
  wp_modified_at: string | null
  // AI enrichment alanları (sonradan doldurulur)
  content_summary: null
  primary_intent: null
  // GSC (sonradan doldurulur)
  gsc_clicks: null
  gsc_impressions: null
  gsc_avg_position: null
  // Yoast SEO alanları — flag_missing_metadata için gerekli (RESEARCH.md Pitfall 3)
  // Yoast eklentisi kurulu değilse null; computeAuditFlags bu değerlere erişir
  yoast_title: string | null
  yoast_description: string | null
  // Audit flags (computeAuditFlags ile doldurulur)
  flag_orphan: boolean
  flag_weak_page: null  // GSC öncesi null
  flag_outdated: boolean
  flag_missing_metadata: boolean
  flag_missing_keyword: boolean
  flag_duplicate_intent: boolean
}

export interface ImportedPageWithDepth extends ImportedPage {
  depth: number
}

// WP REST API raw item shape (sadece _fields ile çekilen alanlar)
interface WpItem {
  id: number
  title: { rendered: string }
  link: string
  slug: string
  parent: number
  date: string
  modified: string
  status: string
  menu_order: number
  yoast_head_json?: { title?: string; description?: string } | null
  excerpt?: { rendered?: string } | null
}

/**
 * WP item → ImportedPage. Audit flag'leri başlangıçta false/null.
 * computeAuditFlags() ayrıca çağrılarak flag'ler güncellenir.
 */
export function normalizeWpPage(
  item: WpItem,
  projectId: string,
  wpType: 'page' | 'post' | 'category' | 'tag'
): ImportedPage {
  // HTML entity decode — WP title.rendered bazen &#8220; gibi entity içerir
  const decodeHtml = (str: string) =>
    str.replace(/&amp;/g, '&').replace(/&lt;/g, '<').replace(/&gt;/g, '>').replace(/&#(\d+);/g, (_, c) => String.fromCharCode(c))

  return {
    project_id: projectId,
    wp_id: item.id,
    wp_type: wpType,
    title: decodeHtml(item.title.rendered),
    slug: item.slug || null,
    link: item.link || null,
    parent_wp_id: item.parent && item.parent > 0 ? item.parent : null,
    wp_created_at: item.date || null,
    wp_modified_at: item.modified || null,
    content_summary: null,
    primary_intent: null,
    gsc_clicks: null,
    gsc_impressions: null,
    gsc_avg_position: null,
    // RESEARCH.md Pitfall 3: yoast_head_json eklenti yoksa field eksik gelir — null guard zorunlu
    yoast_title: item.yoast_head_json?.title ?? null,
    yoast_description: item.yoast_head_json?.description ?? null,
    flag_orphan: false,
    flag_weak_page: null,
    flag_outdated: false,
    flag_missing_metadata: false,
    flag_missing_keyword: false,
    flag_duplicate_intent: false,
  }
}

/**
 * Düz ImportedPage dizisini parent/child ilişkisine göre depth'li listeye çevirir.
 * RESEARCH.md Pattern 4 — site-blueprint/page.tsx flattenTree() EXACT analog.
 * parent_wp_id null veya 0 → kök sayfa (__root__).
 */
export function flattenImportedTree(pages: ImportedPage[]): ImportedPageWithDepth[] {
  const childrenMap: Record<string, ImportedPage[]> = {}
  for (const page of pages) {
    const key = page.parent_wp_id ? page.parent_wp_id.toString() : '__root__'
    if (!childrenMap[key]) childrenMap[key] = []
    childrenMap[key].push(page)
  }

  function walk(parentKey: string, depth: number): ImportedPageWithDepth[] {
    const children = (childrenMap[parentKey] ?? []).sort((a, b) => a.wp_id - b.wp_id)
    return children.flatMap(child => [
      { ...child, depth },
      ...walk(child.wp_id.toString(), depth + 1),
    ])
  }

  return walk('__root__', 0)
}
```

**src/lib/wp/import.ts** yaz:

WP REST API'yi `per_page=100` ile paginate eden, sonuçları döndüren servis modülü. DB upsert bu dosyada değil — upsert Route Handler'da yapılır (tek sorumluluk prensibi):

```typescript
/**
 * WP REST API'den tüm içeriği çeken pagination loop.
 * RESEARCH.md Pattern 1 — gsc/sync/route.ts pagination analog.
 * Pitfall 2: _fields parametresi zorunlu — content HTML asla çekilmez.
 * Rate limit: her batch sonrası 50ms bekle (Claude's Discretion — RESEARCH.md).
 */

// WP REST API raw item — normalize.ts'teki WpItem ile aynı shape
export interface WpRawItem {
  id: number
  title: { rendered: string }
  link: string
  slug: string
  parent: number
  date: string
  modified: string
  status: string
  menu_order: number
  yoast_head_json?: { title?: string; description?: string } | null
  excerpt?: { rendered?: string } | null
}

const WP_FIELDS = 'id,title,link,slug,parent,date,modified,status,menu_order,yoast_head_json,excerpt'

/**
 * Exponential backoff retry — RESEARCH.md "Rate Limiting Strategy".
 */
async function fetchWithRetry(url: string, options: RequestInit, maxRetries = 3): Promise<Response> {
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    const res = await fetch(url, options)
    if (res.status === 429) {
      const retryAfter = parseInt(res.headers.get('Retry-After') ?? '5', 10)
      await new Promise(r => setTimeout(r, retryAfter * 1000))
      continue
    }
    return res
  }
  throw new Error('WP API: Max retries exceeded')
}

/**
 * Belirtilen endpoint için tüm sayfaları çeker (pagination loop).
 * @param wpUrl WordPress site URL (https://example.com)
 * @param authHeader "Basic base64(username:appPassword)" string
 * @param endpoint 'pages' | 'posts' | 'categories' | 'tags'
 * @returns Tüm WP item'ları
 */
export async function fetchAllWpContent(
  wpUrl: string,
  authHeader: string,
  endpoint: 'pages' | 'posts' | 'categories' | 'tags'
): Promise<WpRawItem[]> {
  const results: WpRawItem[] = []
  let page = 1
  let totalPages = 1

  do {
    const url = `${wpUrl.replace(/\/$/, '')}/wp-json/wp/v2/${endpoint}?per_page=100&page=${page}&_fields=${WP_FIELDS}`
    const res = await fetchWithRetry(url, {
      headers: {
        Authorization: authHeader,
        'Content-Type': 'application/json',
      },
      signal: AbortSignal.timeout(30_000),
    })

    if (!res.ok) {
      throw new Error(`WP API error: ${res.status} on ${endpoint} page ${page}`)
    }

    if (page === 1) {
      totalPages = parseInt(res.headers.get('X-WP-TotalPages') ?? '1', 10)
    }

    const items: WpRawItem[] = await res.json()
    results.push(...items)

    // Rate limit önlemi: 50ms bekle (Claude's Discretion — RESEARCH.md)
    if (page < totalPages) {
      await new Promise(r => setTimeout(r, 50))
    }

    page++
  } while (page <= totalPages)

  return results
}
```
  </action>
  <verify>
    <automated>cd "C:/Users/baris/Documents/seo-machine" && npx vitest run src/lib/wp/normalize.test.ts src/lib/wp/url-normalize.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/wp/normalize.ts` mevcut, `export function normalizeWpPage` ve `export function flattenImportedTree` ve `export interface ImportedPage` içeriyor
    - `ImportedPage` interface'inde `yoast_title: string | null` ve `yoast_description: string | null` alanları mevcut
    - `normalizeWpPage` return'ünde `yoast_title: item.yoast_head_json?.title ?? null` ataması var
    - `normalizeWpPage` return'ünde `yoast_description: item.yoast_head_json?.description ?? null` ataması var
    - `src/lib/wp/import.ts` mevcut, `export async function fetchAllWpContent` içeriyor
    - `npx vitest run src/lib/wp/normalize.test.ts` → tüm testler PASS
    - `npx vitest run src/lib/wp/url-normalize.test.ts` → tüm testler PASS
    - import.test.ts pagination loop testleri PASS (global.fetch mock ile)
    - `_fields=` string import.ts içinde geçiyor (Pitfall 2 guard)
  </acceptance_criteria>
  <done>normalize.ts ve import.ts yazıldı; ImportedPage yoast alanlarını içeriyor; normalize + url-normalize testleri yeşil</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import.ts → WP REST API | Harici servis çağrısı; appPassword asla loglanmaz |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.5-03-01 | Information Disclosure | fetchAllWpContent auth header | mitigate | authHeader değişkeni asla console.log'a yazılmaz; timeout 30s ile DoS riski azaltılır |
| T-15.5-03-02 | DoS | WP REST API | mitigate | AbortSignal.timeout(30_000) her request için; 50ms batch delay |
</threat_model>

<verification>
```bash
cd "C:/Users/baris/Documents/seo-machine" && npx vitest run src/lib/wp/ --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|✓|×)"
```
normalize.test.ts ve url-normalize.test.ts PASS olmalı.
</verification>

<success_criteria>
- url-normalize.ts: 5 test PASS
- normalize.ts: normalizeWpPage + flattenImportedTree testleri PASS; ImportedPage yoast_title/yoast_description alanlarını içeriyor
- import.ts: fetchAllWpContent yazıldı (pagination loop testleri PASS)
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/15.5-wordpress-site-import-engine/15.5-03-SUMMARY.md` oluştur.
</output>
