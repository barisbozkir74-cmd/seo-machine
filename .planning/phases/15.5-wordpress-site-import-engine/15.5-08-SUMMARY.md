---
phase: 15.5
plan: "08"
subsystem: site-analizi-ui
tags: [site-analizi, tree-view, audit-flags, project-nav, ssr]
dependency_graph:
  requires: [15.5-07, 15.5-01]
  provides: [site-analizi-page, audit-flag-ui, imported-page-tree]
  affects: [project-nav]
tech_stack:
  added: []
  patterns:
    - SSR page with user-scoped Supabase client (izleme/page.tsx analog)
    - Set-based multiselect toggle state (AuditFlagChips)
    - Depth-indent tree row (site-blueprint/page.tsx analog)
    - Hardcoded className badge colors (no variant prop)
key_files:
  created:
    - src/app/(dashboard)/projeler/[id]/site-analizi/AuditFlagBadge.tsx
    - src/app/(dashboard)/projeler/[id]/site-analizi/AuditFlagChips.tsx
    - src/app/(dashboard)/projeler/[id]/site-analizi/ImportedPageTree.tsx
    - src/app/(dashboard)/projeler/[id]/site-analizi/page.tsx
  modified:
    - src/app/(dashboard)/projeler/[id]/ProjectNav.tsx
decisions:
  - "AuditFlagBadge hardcoded className kullanıyor — variant prop yok (UI-SPEC §Color)"
  - "ImportedPageTree OR mantığı ile multiselect filtreleme — activeFlags Set"
  - "page.tsx import_status null veya idle → empty state, running/enriching → loading state, diğer → tree render"
metrics:
  duration: "~10 dakika"
  completed: "2026-04-28T16:53:38Z"
  tasks_completed: 2
  tasks_total: 2
  files_created: 4
  files_modified: 1
---

# Phase 15.5 Plan 08: Site Analizi UI Summary

Site analizi sayfası ve tüm bileşenleri tamamlandı — hiyerarşik tree görünüm, audit flag chip filtreleri, rozet sistemi ve ProjectNav güncellemesi.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | AuditFlagBadge + AuditFlagChips + ImportedPageTree bileşenleri | 5aa51d4 | AuditFlagBadge.tsx, AuditFlagChips.tsx, ImportedPageTree.tsx |
| 2 | site-analizi/page.tsx + ProjectNav güncellemesi | fc7078f | page.tsx (yeni), ProjectNav.tsx (güncellendi) |

## What Was Built

**AuditFlagBadge.tsx** — 6 audit flag tipi için hardcoded className badge wrapper. variant prop kullanılmaz, UI-SPEC §Color kuralına uygun: flag_orphan sarı, flag_weak_page kırmızı, flag_outdated turuncu, diğerleri slate.

**AuditFlagChips.tsx** — Multiselect audit flag filtre chip'leri. Set-based toggle state, OR mantığı, Türkçe label'lar, aria-pressed, min-h-[44px] touch target, "Filtreleri temizle" butonu.

**ImportedPageTree.tsx** — Depth-indent tree görünümü. `paddingLeft: ${16 + page.depth * 20}px` pattern, depth > 0 için ↳ prefix, flag badge'leri, GSC tıklama + pozisyon metrikleri, aria-label erişilebilirlik desteği.

**site-analizi/page.tsx** — SSR sayfası. createClient() + user-scoped RLS. 3 render durumu: (1) import_status null/idle → empty state "Henüz içerik aktarılmadı", (2) running/enriching → "İçe aktarma devam ediyor...", (3) tamamlandı → ImportedPageTree. izleme/page.tsx ile identik layout.

**ProjectNav.tsx** — İzleme nav item'ından hemen sonra `Site Analizi` eklendi, href: `/projeler/${projectId}/site-analizi`, built: true.

## Deviations from Plan

None — plan exactly as written executed.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| T-15.5-08-01 mitigated | site-analizi/page.tsx | createClient() + .eq('user_id', user.id) ownership check — RLS SELECT policy devreye giriyor |
| T-15.5-08-02 accepted | ImportedPageTree.tsx | target="_blank" rel="noopener noreferrer" — tabnabbing önlendi |

## Self-Check: PASSED

All created files exist on disk. Both task commits verified in git log.
