# Phase 15.5: WordPress Site Import Engine — Pattern Map

**Mapped:** 2026-04-28
**Files analyzed:** 11 (yeni/değiştirilen dosya)
**Analogs found:** 11 / 11

---

## File Classification

| Yeni/Değiştirilen Dosya | Rol | Veri Akışı | En Yakın Analog | Eşleşme Kalitesi |
|------------------------|-----|-----------|----------------|-----------------|
| `supabase/migrations/20260428000001_imported_pages.sql` | migration | CRUD | `supabase/migrations/20260427000002_gsc_schema.sql` | exact |
| `src/lib/wp/import.ts` | service | request-response (pagination) | `src/app/api/gsc/sync/route.ts` | role-match |
| `src/lib/wp/enrichment.ts` | service | batch/transform | `src/app/api/ai/qa-audit/route.ts` | role-match |
| `src/lib/wp/gsc-match.ts` | utility | transform | `src/lib/monitoring/aggregation.ts` | role-match |
| `src/lib/wp/audit-flags.ts` | utility | transform | `src/lib/monitoring/aggregation.ts` (reduceMetrics + isDecayed pattern) | role-match |
| `src/app/api/wp/import/route.ts` | controller | request-response (long-running) | `src/app/api/gsc/sync/route.ts` | exact |
| `src/app/api/wp/import-status/route.ts` | controller | request-response | `src/app/api/gsc/sync/route.ts` | role-match |
| `src/app/(dashboard)/projeler/[id]/site-analizi/page.tsx` | component (SSR page) | request-response | `src/app/(dashboard)/projeler/[id]/izleme/page.tsx` | exact |
| `src/app/(dashboard)/projeler/[id]/site-analizi/ImportedPageTree.tsx` (veya satır bileşeni) | component | CRUD (render) | `src/app/(dashboard)/projeler/[id]/site-blueprint/page.tsx` (flattenTree + tree row) | exact |
| `src/app/(dashboard)/projeler/[id]/site-analizi/AuditFlagChips.tsx` | component | event-driven | `src/app/(dashboard)/projeler/[id]/izleme/period-tab-bar.tsx` | exact |
| `src/app/(dashboard)/projeler/[id]/site-import-section.tsx` + `ProjectNav.tsx` değişikliği | component | request-response | `src/app/(dashboard)/projeler/[id]/wordpress-section.tsx` + `ProjectNav.tsx` | exact |

---

## Pattern Assignments

### `supabase/migrations/20260428000001_imported_pages.sql` (migration, CRUD)

**Analog:** `supabase/migrations/20260427000002_gsc_schema.sql`

**Tablo oluşturma pattern** (satır 21-52):
```sql
CREATE TABLE IF NOT EXISTS public.gsc_metrics (
  id            UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  project_id    UUID REFERENCES public.projects(id) ON DELETE CASCADE NOT NULL,
  ...
  UNIQUE(page_id, date, keyword)
);
```
Kopyalanacak pattern: `UUID DEFAULT gen_random_uuid() PRIMARY KEY`, `REFERENCES public.projects(id) ON DELETE CASCADE NOT NULL`, `UNIQUE(project_id, wp_id)` constraint.

**Index pattern** (satır 35-38):
```sql
CREATE INDEX IF NOT EXISTS idx_gsc_metrics_page_date
  ON public.gsc_metrics(page_id, date DESC);
CREATE INDEX IF NOT EXISTS idx_gsc_metrics_project_date
  ON public.gsc_metrics(project_id, date DESC);
```
Kopyalanacak: `idx_imported_pages_project ON public.project_imported_pages(project_id)` aynı formatla.

**RLS pattern** (satır 40-52):
```sql
ALTER TABLE public.gsc_metrics ENABLE ROW LEVEL SECURITY;

CREATE POLICY "gsc_metrics_select_own"
  ON public.gsc_metrics FOR SELECT
  USING (
    EXISTS (
      SELECT 1 FROM public.projects p
      WHERE p.id = gsc_metrics.project_id
        AND p.user_id = auth.uid()
    )
  );
-- INSERT/UPDATE: service role key ile yapılır — service role RLS bypass eder
```
Policy adı convention: `"imported_pages_select_own"`. `INSERT/UPDATE` comment'i service role açıklamasıyla aynı.

**projects tablosu ALTER pattern** (satır 6-8):
```sql
ALTER TABLE public.projects
  ADD COLUMN IF NOT EXISTS gsc_tokens       JSONB,
  ADD COLUMN IF NOT EXISTS gsc_property_url TEXT;
```
`import_status`, `import_current`, `import_total`, `import_completed_at` kolonları aynı `ADD COLUMN IF NOT EXISTS` formatıyla eklenir.

---

### `src/lib/wp/import.ts` (service, request-response/pagination)

**Analog:** `src/app/api/gsc/sync/route.ts`

**Service role client pattern** (satır 7-10):
```typescript
// Source: src/app/api/gsc/sync/route.ts satır 7-10
import { createClient as createServiceClient } from '@supabase/supabase-js'

function getServiceClient() {
  return createServiceClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!
  )
}
```

**WP credentials okuma pattern** (vault.ts satır 83-101):
```typescript
// Source: src/lib/supabase/vault.ts satır 83-101
export async function getWordPressCredentials(
  projectId: string
): Promise<{ wpUrl: string; appPassword: string } | null> {
  const urlKey = `wp_url_${projectId}`
  const passKey = `wp_app_password_${projectId}`

  const { data, error } = await serviceClient
    .from('vault.decrypted_secrets')
    .select('name, decrypted_secret')
    .in('name', [urlKey, passKey])

  if (error || !data?.length) return null

  const urlRow = data.find((s) => s.name === urlKey)
  const passRow = data.find((s) => s.name === passKey)
  if (!urlRow || !passRow) return null

  return { wpUrl: urlRow.decrypted_secret, appPassword: passRow.decrypted_secret }
}
```
`import.ts` içinde: `import { getWordPressCredentials } from '@/lib/supabase/vault'`

**WP auth header pattern** (wordpress-section.tsx'ten, saveWordPressCredentials çağrısı pattern'ından):
```typescript
// WP REST API Basic Auth — RESEARCH.md Pattern 1'den türetilmiş
const authHeader = 'Basic ' + Buffer.from(`${username}:${appPassword}`).toString('base64')
```

**Upsert pattern** (satır 107-111):
```typescript
// Source: src/app/api/gsc/sync/route.ts satır 107-111
await serviceClient
  .from('gsc_metrics')
  .upsert(metricsToUpsert, { onConflict: 'page_id,date,keyword' })
```
`project_imported_pages` için: `onConflict: 'project_id,wp_id'`, `ignoreDuplicates: false`.

**URL→pageId Map pattern** (satır 79-89):
```typescript
// Source: src/app/api/gsc/sync/route.ts satır 79-89
const pageUrls = [...new Set(rows.map((r) => r.pageUrl))]
const { data: packages } = await serviceClient
  .from('page_packages')
  .select('page_id, wp_post_url')
  .in('wp_post_url', pageUrls)
  .eq('project_id', projectId)

const urlToPageId = new Map<string, string>()
for (const pkg of packages ?? []) {
  if (pkg.wp_post_url && pkg.page_id) {
    urlToPageId.set(pkg.wp_post_url, pkg.page_id)
  }
}
```
Bu pattern `gsc-match.ts`'teki URL normalization + map lookup için temel alınacak.

---

### `src/lib/wp/enrichment.ts` (service, batch/transform)

**Analog:** `src/app/api/ai/qa-audit/route.ts`

**Anthropic SDK init pattern** (satır 1-8):
```typescript
// Source: src/app/api/ai/qa-audit/route.ts satır 1-8
import Anthropic from '@anthropic-ai/sdk'

if (!process.env.ANTHROPIC_API_KEY) {
  throw new Error('ANTHROPIC_API_KEY environment variable is not set')
}
const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })
```

**`messages.create` call pattern** (satır 80-87):
```typescript
// Source: src/app/api/ai/qa-audit/route.ts satır 80-87
message = await client.messages.create({
  model: 'claude-sonnet-4-6',   // → enrichment.ts'te 'claude-haiku-4-5' olacak
  max_tokens: 1000,             // → enrichment.ts'te 100 olacak (200 char summary için)
  messages: [{ role: 'user', content: prompt }],
})
```

**JSON extract pattern** (satır 94-103):
```typescript
// Source: src/app/api/ai/qa-audit/route.ts satır 94-103
const jsonMatch =
  text.match(/```json\s*([\s\S]*?)```/) ?? text.match(/(\{[\s\S]*\})/)
const jsonText = jsonMatch ? jsonMatch[1] : text

let result: unknown
try {
  result = JSON.parse(jsonText.trim())
} catch {
  return new Response('Invalid JSON from Claude', { status: 502 })
}
```
`enrichment.ts`'te: parse hatası halinde sayfa AI alanları null bırakılır, import başarılı sayılır (D-04).

**Hata handling pattern** (satır 85-87):
```typescript
// Source: src/app/api/ai/qa-audit/route.ts satır 85-87
} catch {
  return new Response('Claude API error', { status: 502 })
}
```
`enrichment.ts`'te: `catch` bloğu `console.error` yazar, null döndürür — throw etmez.

---

### `src/lib/wp/gsc-match.ts` (utility, transform)

**Analog:** `src/lib/monitoring/aggregation.ts` (satır 196-218 — page_packages URL resolution pattern)

**URL resolution — page_packages join pattern** (satır 204-218):
```typescript
// Source: src/lib/monitoring/aggregation.ts satır 204-218
const { data: packagesData } = await supabase
  .from('page_packages')
  .select('page_id, wp_post_url')
  .eq('project_id', projectId)

const packagesMap = new Map<string, string>()
for (const pkg of (packagesData ?? []) as Array<{
  page_id: string
  wp_post_url: string | null
}>) {
  if (pkg.wp_post_url) {
    packagesMap.set(pkg.page_id, pkg.wp_post_url)
  }
}
```
`gsc-match.ts`'te bu pattern tersine çevrilir: `imported_pages.link` → normalize → GSC property domain ile karşılaştırma.

**Reduce/aggregate pattern** (satır 222-244):
```typescript
// Source: src/lib/monitoring/aggregation.ts satır 222-244
function reduceMetrics(rows: MetricRaw[]): Map<string, Agg> {
  const map = new Map<string, Agg>()
  for (const row of rows) {
    const existing = map.get(row.page_id)
    const posValue = row.avg_position ?? 0
    if (existing) {
      existing.clicks += row.clicks
      existing.impressions += row.impressions
      existing.posSum += posValue
      existing.posCount += 1
    } else {
      map.set(row.page_id, {
        clicks: row.clicks,
        impressions: row.impressions,
        posSum: posValue,
        posCount: 1,
      })
    }
  }
  return map
}
```
GSC clicks/impressions/avg_position aggregate için bu pattern kullanılır.

---

### `src/lib/wp/audit-flags.ts` (utility, transform)

**Analog:** `src/lib/monitoring/aggregation.ts` (isDecayed flag hesaplama pattern — satır 27-28)

**Decay/flag hesaplama mantığı** (type definition satır 27-28):
```typescript
// Source: src/lib/monitoring/aggregation.ts satır 27-28
isDecayed: boolean           // deltaPosition !== null && deltaPosition >= 5 && impressions > 10
```
Aynı threshold-based boolean flag mantığı. `computeAuditFlags()` pure function olarak yazılır:
- Input: `ImportedPage` (gsc_clicks, gsc_avg_position, wp_modified_at, parent_wp_id)
- Output: `{ flag_orphan, flag_weak_page, flag_outdated, flag_missing_metadata, flag_missing_keyword }`
- `flag_duplicate_intent` ayrı "Faz B" pass'ında hesaplanır (AI enrichment sonrası).

**Null-coerce pattern** (aggregation.ts satır 116, 191):
```typescript
// Source: src/lib/monitoring/aggregation.ts satır 116, 191
const posValue = row.avg_position ?? 0
const currentMetrics: MetricRaw[] = currentResult.data ?? []
```
`flag_weak_page`: `gsc_clicks === null` ise `null` döner (D-10), sıfıra coerce edilmez.

---

### `src/app/api/wp/import/route.ts` (controller, request-response/long-running)

**Analog:** `src/app/api/gsc/sync/route.ts`

**Import bloğu** (satır 1-4):
```typescript
// Source: src/app/api/gsc/sync/route.ts satır 1-4
import { type NextRequest, NextResponse } from 'next/server'
import { createClient as createServiceClient } from '@supabase/supabase-js'
```
`wp/import/route.ts`'te ekstra: `import { getWordPressCredentials } from '@/lib/supabase/vault'`

**Ownership doğrulama pattern** (satır 36-49):
```typescript
// Source: src/app/api/gsc/sync/route.ts satır 36-49
const { data: project } = await serviceClient
  .from('projects')
  .select('gsc_property_url')
  .eq('id', projectId)
  .eq('user_id', userId)
  .single()

if (!project?.gsc_property_url) {
  return NextResponse.json(
    { error: 'Project not found or GSC not configured' },
    { status: 404 }
  )
}
```
`wp/import/route.ts`'te: `.select('id, name')` + `if (!project)` check. `userId` body'den değil, session'dan alınır: `const { data: { user } } = await createClient().auth.getUser()`.

**Body parse + validation pattern** (satır 22-31):
```typescript
// Source: src/app/api/gsc/sync/route.ts satır 22-31
let body: { projectId?: string; userId?: string }
try {
  body = await request.json()
} catch {
  return NextResponse.json({ error: 'Invalid JSON' }, { status: 400 })
}

const { projectId, userId } = body
if (!projectId || !userId) {
  return NextResponse.json({ error: 'projectId and userId required' }, { status: 400 })
}
```

**Error response format** (satır 44-48):
```typescript
// Source: src/app/api/gsc/sync/route.ts satır 44-48
return NextResponse.json(
  { error: 'Project not found or GSC not configured' },
  { status: 404 }
)
```
Tüm error response'lar `NextResponse.json({ error: '...' }, { status: N })` formatında.

---

### `src/app/api/wp/import-status/route.ts` (controller, request-response/GET)

**Analog:** `src/app/api/gsc/sync/route.ts` (GET endpoint yapısı için genel Route Handler pattern)

```typescript
// Pattern: GET handler — query param okuma, service client, single row select
export async function GET(request: NextRequest) {
  const projectId = request.nextUrl.searchParams.get('projectId')
  if (!projectId) {
    return NextResponse.json({ error: 'projectId required' }, { status: 400 })
  }
  const serviceClient = getServiceClient()  // aynı getServiceClient() helper
  const { data: project } = await serviceClient
    .from('projects')
    .select('import_status, import_current, import_total')
    .eq('id', projectId)
    .single()
  return NextResponse.json({
    status: project?.import_status ?? 'idle',
    current: project?.import_current ?? 0,
    total: project?.import_total ?? 0,
  })
}
```
Ownership check `import/route.ts`'teki session auth ile sağlanır; bu GET endpoint'te de ownership check zorunlu (D-IDOR).

---

### `src/app/(dashboard)/projeler/[id]/site-analizi/page.tsx` (component/SSR page)

**Analog:** `src/app/(dashboard)/projeler/[id]/izleme/page.tsx`

**Import bloğu** (satır 1-8):
```typescript
// Source: izleme/page.tsx satır 1-8
import { notFound } from 'next/navigation'
import Link from 'next/link'
import { createClient } from '@/lib/supabase/server'
import { ProjectNav } from '../ProjectNav'
```
`site-analizi/page.tsx`'te ekstra: `AuditFlagChips` ve tree bileşeni import edilir.

**Auth + ownership pattern** (satır 21-31):
```typescript
// Source: izleme/page.tsx satır 21-31
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) notFound()

const { data: project } = await supabase
  .from('projects')
  .select('id, name, domain, gsc_property_url')
  .eq('id', id)
  .eq('user_id', user.id)
  .single()
if (!project) notFound()
```

**Page layout pattern** (satır 44-92):
```typescript
// Source: izleme/page.tsx satır 44-92
return (
  <div className="flex flex-col h-screen">
    <div className="p-8 pb-4">
      <Link href={`/projeler/${id}`} className="text-sm text-muted-foreground hover:text-foreground flex items-center gap-1 mb-4">
        ← {project.name}
      </Link>
      <h1 className="text-xl font-semibold">İzleme</h1>   {/* → "Site Analizi" */}
    </div>

    <div className="flex flex-1 min-h-0">
      <div className="w-64 shrink-0 border-r border-border overflow-y-auto p-4">
        <ProjectNav projectId={id} activePath={`/projeler/${id}/izleme`} />
      </div>

      <div className="flex-1 min-w-0 overflow-y-auto p-8">
        {/* içerik */}
      </div>
    </div>
  </div>
)
```
`activePath={'/projeler/${id}/site-analizi'}` olarak değiştirilir.

**Empty state pattern** (satır 61-71):
```typescript
// Source: izleme/page.tsx satır 61-71
<div className="rounded-lg border border-border bg-card p-8 text-center">
  <p className="text-base font-semibold">GSC verisi bulunamadı</p>
  <p className="text-sm text-muted-foreground mt-2">...</p>
  <Link href={`/projeler/${id}`} className="inline-flex items-center mt-4 text-sm text-foreground hover:underline">
    Proje Bilgileri →
  </Link>
</div>
```
`site-analizi/page.tsx`'te "Henüz içerik aktarılmadı" mesajı + "WordPress Sitemi İçeri Al" butonu ile aynı container yapısı.

---

### `src/app/(dashboard)/projeler/[id]/site-analizi/ImportedPageTree.tsx` (component, render/CRUD)

**Analog:** `src/app/(dashboard)/projeler/[id]/site-blueprint/page.tsx`

**`flattenTree()` fonksiyonu — EXACT COPY** (satır 49-73):
```typescript
// Source: site-blueprint/page.tsx satır 49-73
function flattenTree(pages: Page[]): PageWithDepth[] {
  const childrenMap: Record<string, Page[]> = {}

  for (const page of pages) {
    const key = page.parent_id ?? '__root__'
    if (!childrenMap[key]) childrenMap[key] = []
    childrenMap[key].push(page)
  }

  function sortPages(list: Page[]): Page[] {
    return [...list].sort((a, b) => a.sort_order - b.sort_order)
  }

  function walk(parentKey: string, depth: number): PageWithDepth[] {
    const children = sortPages(childrenMap[parentKey] ?? [])
    const result: PageWithDepth[] = []
    for (const child of children) {
      result.push({ ...child, depth })
      result.push(...walk(child.id, depth + 1))
    }
    return result
  }

  return walk('__root__', 0)
}
```
`ImportedPageTree.tsx`'te: `parent_id` → `parent_wp_id`, `sort_order` → `wp_id` (sort key), `__root__` key'i için `parent_wp_id === null || parent_wp_id === 0`.

**Grid container pattern** (satır 358-367):
```typescript
// Source: site-blueprint/page.tsx satır 358-367
<div className="rounded-md border border-border overflow-hidden">
  {/* Header */}
  <div className="grid grid-cols-[1fr_1fr_auto_auto_auto_auto] gap-0 bg-muted/50 border-b border-border text-xs text-muted-foreground font-normal">
    <div className="px-4 py-2">Sayfa Adı</div>   {/* → "Sayfa" */}
    <div className="px-4 py-2">Slug</div>         {/* → "URL" */}
    <div className="px-4 py-2 w-32">Tip</div>     {/* → audit flag badge'leri alanı */}
    <div className="px-4 py-2 w-20">Öncelik</div> {/* → "Tıklama" */}
    <div className="px-4 py-2 w-16 text-center">Sıra</div> {/* → "Pozisyon" */}
    <div className="px-4 py-2 w-16"></div>
  </div>
```

**Tree row — depth indent + ↳ pattern** (satır 386-403):
```typescript
// Source: site-blueprint/page.tsx satır 386-403
<div
  key={page.id}
  className="grid grid-cols-[1fr_1fr_auto_auto_auto_auto] gap-0 border-b border-border last:border-0 items-center text-sm hover:bg-muted/30 transition-colors group"
>
  <div
    className="px-4 py-2 font-normal truncate flex items-center"
    style={{ paddingLeft: `${16 + page.depth * 20}px` }}
  >
    {page.depth > 0 && (
      <span className="text-muted-foreground mr-1.5">↳</span>
    )}
    <span className="truncate">{page.title}</span>
  </div>
```
`ImportedPageTree.tsx`'te aynı `style={{ paddingLeft }}` ve `↳` pattern kullanılır.

---

### `src/app/(dashboard)/projeler/[id]/site-analizi/AuditFlagChips.tsx` (component, event-driven)

**Analog:** `src/app/(dashboard)/projeler/[id]/izleme/period-tab-bar.tsx`

**Tam chip container pattern** (satır 1-40):
```typescript
// Source: izleme/period-tab-bar.tsx satır 1-40
'use client'

import { cn } from '@/lib/utils'

// PeriodTabBar yerine AuditFlagChips:
// - router.push() yerine Set-based toggle state
// - isActive → bg-blue-500/20 text-blue-400 (period-tab'ın bg-secondary değil)
// - aria-pressed={isActive} eklenir (accessibility)

export function AuditFlagChips({ ... }) {
  return (
    <div className="inline-flex items-center gap-1 bg-card border border-border rounded-lg p-1 mb-4 flex-wrap">
      {chips.map((chip) => {
        const isActive = activeFlags.has(chip.key)
        return (
          <button
            key={chip.key}
            type="button"
            aria-pressed={isActive}
            onClick={() => toggleFlag(chip.key)}
            className={cn(
              'px-4 py-2 rounded-md text-sm transition-colors min-h-[44px]',
              isActive
                ? 'bg-blue-500/20 text-blue-400 font-medium border border-blue-500/30'
                : 'text-muted-foreground hover:text-foreground'
            )}
          >
            {chip.label}
          </button>
        )
      })}
    </div>
  )
}
```
`min-h-[44px]` period-tab-bar'dan alındı (touch target). Fark: `bg-secondary` → `bg-blue-500/20 text-blue-400`.

---

### `src/app/(dashboard)/projeler/[id]/site-import-section.tsx` (component, request-response)

**Analog:** `src/app/(dashboard)/projeler/[id]/wordpress-section.tsx`

**'use client' + state pattern** (satır 1-23):
```typescript
// Source: wordpress-section.tsx satır 1-23
'use client'

import { useState } from 'react'
import { Button } from '@/components/ui/button'
import { Badge } from '@/components/ui/badge'
```
`site-import-section.tsx`'te: `import { HugeiconsIcon } from '@hugeicons/react'` ile ikonlar eklenebilir (wordpress-section.tsx pattern'ı).

**Başlık + durum badge pattern** (satır 58-71):
```typescript
// Source: wordpress-section.tsx satır 58-71
<div className="space-y-4">
  <div className="flex items-center gap-3">
    <h2 className="text-base font-semibold">WordPress Bağlantısı</h2>
    <Badge
      aria-live="polite"
      className={
        saved
          ? 'bg-emerald-500/20 text-emerald-400 border-emerald-500/30'
          : 'bg-slate-800 text-slate-400 border-slate-700'
      }
    >
      {saved ? 'Bağlı' : 'Yapılandırılmadı'}
    </Badge>
  </div>
```
`site-import-section.tsx`'te başlık "WordPress Site İçe Aktarma", badge durumları: idle/importing/complete/error.

**Hata metni pattern** (satır 129-131):
```typescript
// Source: wordpress-section.tsx satır 129-131
{error && (
  <p className="text-xs text-destructive">{error}</p>
)}
```

**CTA buton pattern** (satır 133-140):
```typescript
// Source: wordpress-section.tsx satır 133-140
<div className="flex justify-end">
  <Button
    onClick={handleSave}
    disabled={saving || !isDirty}
    className="min-h-[44px]"
  >
    {saving ? 'Kaydediliyor...' : 'Kaydet'}
  </Button>
</div>
```
`site-import-section.tsx`'te: `{importing ? 'İçe Aktarılıyor...' : hasImported ? 'WordPress Sitemi Yenile' : 'WordPress Sitemi İçeri Al'}`. İlk import primary variant, re-import outline variant.

---

### `src/app/(dashboard)/projeler/[id]/ProjectNav.tsx` değişikliği

**Analog:** `src/app/(dashboard)/projeler/[id]/ProjectNav.tsx` (mevcut dosya, satır 10-24)

**Eklenecek nav item** (satır 22'nin ardına):
```typescript
// Source: ProjectNav.tsx satır 10-24 — getNavItems() array
// Mevcut son item (satır 22):
{ label: 'İzleme', href: `/projeler/${projectId}/izleme`, built: true },
// Eklenecek (İzleme'nin hemen ardına):
{ label: 'Site Analizi', href: `/projeler/${projectId}/site-analizi`, built: true },
```

**Link render pattern** (satır 52-66) — değişiklik yok, item eklendikten sonra otomatik render edilir:
```typescript
// Source: ProjectNav.tsx satır 52-66
<Link
  href={item.href}
  className={cn(
    'flex items-center px-3 py-2 rounded-md text-sm transition-colors',
    isActive
      ? 'bg-secondary text-foreground font-medium'
      : 'text-muted-foreground hover:text-foreground hover:bg-secondary'
  )}
>
  {item.label}
</Link>
```

---

## Shared Patterns

### Authentication (Ownership Check)
**Kaynak:** `src/app/api/gsc/sync/route.ts` satır 36-49
**Uygulanacak:** Tüm Route Handler'lar (`/api/wp/import/route.ts`, `/api/wp/import-status/route.ts`)
```typescript
const { data: project } = await serviceClient
  .from('projects')
  .select('id, name')
  .eq('id', projectId)
  .eq('user_id', userId)   // IDOR koruması — başkasının projesine erişim engellenir
  .single()
if (!project) {
  return NextResponse.json({ error: 'Project not found' }, { status: 404 })
}
```

### Service Role Client
**Kaynak:** `src/app/api/gsc/sync/route.ts` satır 7-10
**Uygulanacak:** `import/route.ts`, `import-status/route.ts`, `src/lib/wp/import.ts`
```typescript
import { createClient as createServiceClient } from '@supabase/supabase-js'

function getServiceClient() {
  return createServiceClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!
  )
}
```

### Error Handling (Route Handler)
**Kaynak:** `src/app/api/gsc/sync/route.ts` satır 22-31 + `src/app/api/ai/qa-audit/route.ts` satır 85-87
**Uygulanacak:** Tüm Route Handler'lar
```typescript
// Body parse hataları:
try { body = await request.json() } catch {
  return NextResponse.json({ error: 'Invalid JSON' }, { status: 400 })
}
// External API hataları:
} catch {
  return NextResponse.json({ error: 'WP API error' }, { status: 502 })
}
```

### Error Handling (Component)
**Kaynak:** `src/app/(dashboard)/projeler/[id]/wordpress-section.tsx` satır 129-131
**Uygulanacak:** `site-import-section.tsx`
```typescript
{error && <p className="text-xs text-destructive">{error}</p>}
```

### Badge Color Convention
**Kaynak:** `src/app/(dashboard)/projeler/[id]/wordpress-section.tsx` satır 61-71 + UI-SPEC
**Uygulanacak:** `AuditFlagBadge.tsx`, `site-import-section.tsx` durum badge
```typescript
// Hardcoded className — variant prop kullanılmaz
const FLAG_BADGE_CLASSES: Record<string, string> = {
  flag_orphan:           'bg-yellow-500/20 text-yellow-400',
  flag_weak_page:        'bg-red-500/20 text-red-400',
  flag_outdated:         'bg-orange-500/20 text-orange-400',
  flag_missing_metadata: 'bg-slate-700 text-slate-400',
  flag_missing_keyword:  'bg-slate-700 text-slate-400',
  flag_duplicate_intent: 'bg-slate-700 text-slate-400',
}
// Import tamamlama: 'bg-emerald-500/20 text-emerald-400 border-emerald-500/30' (wordpress-section 'Bağlı' badge)
```

### Supabase User-Scoped Client (SSR Pages)
**Kaynak:** `src/app/(dashboard)/projeler/[id]/izleme/page.tsx` satır 21-31
**Uygulanacak:** `site-analizi/page.tsx`
```typescript
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) notFound()
```

### cn() Utility
**Kaynak:** `src/app/(dashboard)/projeler/[id]/izleme/period-tab-bar.tsx` satır 3 + `site-blueprint/page.tsx`
**Uygulanacak:** `AuditFlagChips.tsx`, `ImportedPageTree.tsx`
```typescript
import { cn } from '@/lib/utils'
```

---

## No Analog Found

Bu fazda tüm dosyalar için codebase'de güçlü analog bulundu. Aşağıdaki tablo kısmi farklılıkları özetler:

| Dosya | Rol | Not |
|-------|-----|-----|
| `src/lib/wp/audit-flags.ts` | utility (pure function) | Codebase'de pure audit flag fonksiyonu yok — `isDecayed` boolean'ı aggregation.ts'teki tek analog; RESEARCH.md Pattern 5 flag eşiklerini (D-10..D-13) uygular |
| `src/lib/wp/import.ts` (WP pagination loop) | service | `gsc/sync/route.ts` Supabase upsert pattern'ına benziyor ancak WP REST API `X-WP-TotalPages` header'ı ile pagination loop yeni; RESEARCH.md Pattern 1'deki kod örneği referans alınır |

---

## Metadata

**Analog arama kapsamı:**
- `src/app/api/` — Route Handler pattern'ları
- `src/lib/supabase/` — Vault ve client pattern'ları
- `src/lib/monitoring/` — aggregation ve URL join pattern'ları
- `src/app/(dashboard)/projeler/[id]/` — Page + component pattern'ları
- `supabase/migrations/` — Migration şema pattern'ları

**Taranan dosya sayısı:** 11 analog dosya (doğrudan okuma)
**Pattern extraction tarihi:** 2026-04-28
