---
phase: 15.5
plan: 05
type: execute
wave: 2
depends_on:
  - 15.5-01
  - 15.5-02
files_modified:
  - src/lib/wp/enrichment.ts
autonomous: true
requirements:
  - IMP-04

must_haves:
  truths:
    - "enrichImportedPages() claude-haiku-4-5 ile toplu AI çağrısı yapıyor"
    - "content_summary 200 karakteri geçmiyor"
    - "primary_intent enum değerlerinden biri: informational|commercial|transactional|navigational"
    - "AI hatası sayfa AI alanlarını null bırakır — import başarılı sayılır (D-04)"
    - "BATCH_SIZE=20 ile paralel çağrı — rate limit kontrolü (D-05)"
  artifacts:
    - path: "src/lib/wp/enrichment.ts"
      provides: "enrichImportedPages batch AI çağrısı"
      exports: ["enrichImportedPages", "EnrichmentResult"]
  key_links:
    - from: "src/lib/wp/enrichment.ts"
      to: "src/app/api/ai/qa-audit/route.ts"
      via: "Anthropic SDK messages.create pattern — EXACT ANALOG"
      pattern: "anthropic.messages.create"
---

<objective>
AI enrichment modülünü yaz — import tamamlandıktan sonra tüm sayfalar için toplu claude-haiku-4-5 çağrısıyla content_summary ve primary_intent üretir.

Purpose: IMP-04 — her imported page için content_summary (max 200 char) + primary_intent enum.
Output: enrichment.ts modülü + enrichment.test.ts testleri yeşil
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15.5-wordpress-site-import-engine/15.5-CONTEXT.md
@.planning/phases/15.5-wordpress-site-import-engine/15.5-RESEARCH.md
@.planning/phases/15.5-wordpress-site-import-engine/15.5-PATTERNS.md

<interfaces>
<!-- qa-audit/route.ts Anthropic SDK pattern — EXACT ANALOG -->
```typescript
// src/app/api/ai/qa-audit/route.ts satır 1-8
import Anthropic from '@anthropic-ai/sdk'
if (!process.env.ANTHROPIC_API_KEY) {
  throw new Error('ANTHROPIC_API_KEY environment variable is not set')
}
const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })

// messages.create call (satır 80-87):
const message = await client.messages.create({
  model: 'claude-sonnet-4-6',   // → enrichment'ta 'claude-haiku-4-5'
  max_tokens: 1000,             // → enrichment'ta 100
  messages: [{ role: 'user', content: prompt }],
})
```

<!-- JSON extract pattern (qa-audit/route.ts satır 94-103): -->
```typescript
const jsonMatch =
  text.match(/```json\s*([\s\S]*?)```/) ?? text.match(/(\{[\s\S]*\})/)
const jsonText = jsonMatch ? jsonMatch[1] : text
try {
  result = JSON.parse(jsonText.trim())
} catch {
  // enrichment'ta: null döner, import başarılı sayılır (D-04)
}
```

<!-- D-04: Enrichment import sonrası toplu çalışır -->
<!-- D-05: claude-haiku-4-5, BATCH_SIZE=20 paralel, 100 sayfa ≈ $0.05 -->
<!-- D-06: content_summary max 200 char, primary_intent enum -->

<!-- RESEARCH.md Pattern 5: Toplu Haiku çağrısı -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: enrichment.ts</name>
  <files>src/lib/wp/enrichment.ts</files>
  <read_first>
    - src/lib/wp/enrichment.test.ts (Wave 0 test beklentileri — önce oku)
    - src/app/api/ai/qa-audit/route.ts (Anthropic SDK pattern — satır 1-103)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-RESEARCH.md §Pattern 5 (Toplu Haiku çağrısı)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-CONTEXT.md (D-04, D-05, D-06)
    - .planning/phases/15.5-wordpress-site-import-engine/15.5-PATTERNS.md §enrichment.ts bölümü
  </read_first>
  <behavior>
    enrichImportedPages(pages, serviceClient):
    - Model: claude-haiku-4-5 (D-05)
    - BATCH_SIZE=20 paralel çağrı
    - content_summary max 200 char truncate (D-06)
    - primary_intent: geçerli enum değilse null
    - JSON parse hatası → content_summary=null, primary_intent=null (D-04 — import başarılı)
    - DB update: serviceClient ile project_imported_pages güncellenir
  </behavior>
  <action>
`src/lib/wp/enrichment.ts` dosyasını yaz. qa-audit/route.ts Anthropic SDK pattern'ını EXACT COPY olarak kullan (model ve max_tokens değiştirilir):

```typescript
import 'server-only'
/**
 * AI enrichment — claude-haiku-4-5 ile toplu content_summary + primary_intent üretimi.
 * D-04: Import biter bitmez ayrı batch çağrısı — DB write sonrası.
 * D-05: claude-haiku-4-5, BATCH_SIZE=20 paralel.
 * D-06: content_summary max 200 char, primary_intent enum.
 * Hata handling: parse hatası → null, import başarılı sayılır.
 */

import Anthropic from '@anthropic-ai/sdk'
import type { SupabaseClient } from '@supabase/supabase-js'

if (!process.env.ANTHROPIC_API_KEY) {
  throw new Error('ANTHROPIC_API_KEY environment variable is not set')
}
const anthropic = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })

const BATCH_SIZE = 20
const VALID_INTENTS = ['informational', 'commercial', 'transactional', 'navigational'] as const
type PrimaryIntent = typeof VALID_INTENTS[number]

export interface EnrichmentResult {
  wp_id: number
  content_summary: string | null
  primary_intent: PrimaryIntent | null
}

interface PageToEnrich {
  wp_id: number
  title: string
  link: string | null
  slug: string | null
}

/**
 * Tek sayfa için AI enrichment çağrısı.
 * qa-audit/route.ts messages.create pattern — EXACT ANALOG.
 */
async function enrichSinglePage(page: PageToEnrich): Promise<EnrichmentResult> {
  try {
    const prompt = `Aşağıdaki web sayfasını analiz et ve JSON formatında yanıt ver.
Sayfa başlığı: ${page.title}
URL: ${page.link ?? page.slug ?? 'bilinmiyor'}

Görev:
1. Sayfayı 200 karakteri KESINLIKLE geçmeyecek şekilde Türkçe özetle (summary)
2. Sayfanın arama intent'ini belirle (intent)

Yanıtı SADECE JSON formatında ver, başka hiçbir şey ekleme:
{"summary": "...", "intent": "informational|commercial|transactional|navigational"}`

    const message = await anthropic.messages.create({
      model: 'claude-haiku-4-5',
      max_tokens: 150,
      messages: [{ role: 'user', content: prompt }],
    })

    const text = message.content[0]?.type === 'text' ? message.content[0].text : ''

    // JSON extract — qa-audit/route.ts pattern EXACT COPY
    const jsonMatch =
      text.match(/```json\s*([\s\S]*?)```/) ?? text.match(/(\{[\s\S]*\})/)
    const jsonText = jsonMatch ? jsonMatch[1] : text

    let parsed: { summary?: string; intent?: string }
    try {
      parsed = JSON.parse(jsonText.trim())
    } catch {
      // D-04: parse hatası → null, import başarılı
      return { wp_id: page.wp_id, content_summary: null, primary_intent: null }
    }

    // D-06: max 200 char truncate
    const summary = typeof parsed.summary === 'string'
      ? parsed.summary.slice(0, 200)
      : null

    // D-06: valid enum kontrolü
    const intent = VALID_INTENTS.includes(parsed.intent as PrimaryIntent)
      ? (parsed.intent as PrimaryIntent)
      : null

    return { wp_id: page.wp_id, content_summary: summary, primary_intent: intent }
  } catch {
    // D-04: AI hatası → null, import başarılı
    return { wp_id: page.wp_id, content_summary: null, primary_intent: null }
  }
}

/**
 * Tüm sayfalar için toplu AI enrichment.
 * BATCH_SIZE=20 paralel, batch'ler arası sıralı.
 * DB güncelleme: serviceClient (service role) ile project_imported_pages.
 */
export async function enrichImportedPages(
  pages: PageToEnrich[],
  serviceClient: SupabaseClient,
  projectId: string
): Promise<void> {
  for (let i = 0; i < pages.length; i += BATCH_SIZE) {
    const batch = pages.slice(i, i + BATCH_SIZE)

    const results = await Promise.all(batch.map(page => enrichSinglePage(page)))

    // Batch sonuçlarını DB'ye yaz
    for (const result of results) {
      if (result.content_summary !== null || result.primary_intent !== null) {
        await serviceClient
          .from('project_imported_pages')
          .update({
            content_summary: result.content_summary,
            primary_intent: result.primary_intent,
            updated_at: new Date().toISOString(),
          })
          .eq('project_id', projectId)
          .eq('wp_id', result.wp_id)
      }
    }
  }
}
```
  </action>
  <verify>
    <automated>cd "C:/Users/baris/Documents/seo-machine" && npx vitest run src/lib/wp/enrichment.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/wp/enrichment.ts` mevcut, `export async function enrichImportedPages` içeriyor
    - `claude-haiku-4-5` string dosyada geçiyor (D-05)
    - `slice(0, 200)` truncation kodu mevcut (D-06)
    - `VALID_INTENTS` enum kontrolü var — geçersiz intent null döner
    - enrichment.test.ts çalıştırılıyor (modül import edilebiliyor)
  </acceptance_criteria>
  <done>enrichment.ts yazıldı; Haiku batch çağrısı, truncation, enum guard hazır</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enrichment.ts → Anthropic API | ANTHROPIC_API_KEY server-only |
| enrichment.ts → Supabase | service role client ile DB güncelleme |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.5-05-01 | Information Disclosure | ANTHROPIC_API_KEY | mitigate | `import 'server-only'` — client bundle'a sızmaz |
| T-15.5-05-02 | DoS | Haiku batch calls | accept | BATCH_SIZE=20 ile rate limit kontrol; 100 sayfa ≈ $0.05 maliyet (D-05) |
</threat_model>

<verification>
```bash
grep "claude-haiku-4-5" src/lib/wp/enrichment.ts
grep "slice(0, 200)" src/lib/wp/enrichment.ts
```
Her ikisi 1 sonuç döndürmeli.
</verification>

<success_criteria>
- enrichment.ts: claude-haiku-4-5, BATCH_SIZE=20, max 200 char truncation, enum guard
- enrichImportedPages export edilmiş
- enrichment.test.ts import hatası vermiyor
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/15.5-wordpress-site-import-engine/15.5-05-SUMMARY.md` oluştur.
</output>
