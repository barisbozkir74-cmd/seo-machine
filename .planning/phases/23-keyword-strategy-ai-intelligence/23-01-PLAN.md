---
phase: 23-keyword-strategy-ai-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260512000001_ai_memory.sql
autonomous: true
requirements:
  - KWST-06
  - KWST-07

must_haves:
  truths:
    - "ai_memory tablosu Supabase'de oluşturulmuş ve erişilebilir durumda"
    - "RLS politikası etkin — sadece kendi user_id'sine ait satırlar okunabilir"
    - "UNIQUE constraint (project_id, module, key) üzerine kurulu — UPSERT ON CONFLICT çalışır"
    - "İki index mevcut: idx_ai_memory_project_id ve idx_ai_memory_project_module"
  artifacts:
    - path: "supabase/migrations/20260512000001_ai_memory.sql"
      provides: "ai_memory tablosu DDL + RLS + index"
      contains: "CREATE TABLE IF NOT EXISTS public.ai_memory"
  key_links:
    - from: "supabase/migrations/20260512000001_ai_memory.sql"
      to: "Supabase DB (remote)"
      via: "supabase db push"
      pattern: "supabase db push"
---

<objective>
`ai_memory` Supabase tablosunu oluşturacak migration dosyasını yazar ve `supabase db push` ile uzak veritabanına uygular.

Purpose: D-07'de tanımlanan AI hafıza altyapısı. Plan 23-02 ve 23-03'te yazılacak tüm kod bu tabloya bağımlı — DB'de mevcut olmadan çalışamaz. Bu plan tüm diğer planların önkoşuludur.

Output: `supabase/migrations/20260512000001_ai_memory.sql` dosyası + uzak DB'de tablo
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-keyword-strategy-ai-intelligence/23-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ai_memory migration dosyasını yaz</name>
  <files>supabase/migrations/20260512000001_ai_memory.sql</files>

  <read_first>
    - `supabase/migrations/20260509000003_project_memories.sql` — RLS + index pattern için analog (exact match)
    - `supabase/migrations/20260510000001_page_package_revisions.sql` — updated_at trigger kullanımı için referans
  </read_first>

  <action>
`supabase/migrations/20260512000001_ai_memory.sql` dosyasını oluştur. İçerik birebir şu DDL olmalı:

```sql
-- AI Memory: structured per-project decision memory for keyword strategy AI
-- D-07: project_id + user_id + module + key + value(JSONB) + updated_at
-- D-08 modules: 'rules' | 'clusters' | 'analysis' | 'blueprint'

CREATE TABLE IF NOT EXISTS public.ai_memory (
  id          UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id     UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  project_id  UUID REFERENCES public.projects(id) ON DELETE CASCADE NOT NULL,
  module      TEXT NOT NULL,
  key         TEXT NOT NULL,
  value       JSONB NOT NULL DEFAULT '{}',
  updated_at  TIMESTAMPTZ DEFAULT now() NOT NULL,
  UNIQUE(project_id, module, key)
);

CREATE INDEX IF NOT EXISTS idx_ai_memory_project_id
  ON public.ai_memory(project_id);

CREATE INDEX IF NOT EXISTS idx_ai_memory_project_module
  ON public.ai_memory(project_id, module);

ALTER TABLE public.ai_memory ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users own their ai_memory"
  ON public.ai_memory
  FOR ALL
  USING (user_id = auth.uid())
  WITH CHECK (user_id = auth.uid());
```

`project_memories.sql` pattern'inden farklı: `memory_text TEXT` yerine `value JSONB` kullanılır (D-08 yapılandırılmış veri gerektirir). UNIQUE constraint `(project_id, module, key)` üzerine kurulur (section tek sütun değil, module+key çifti).
  </action>

  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS public.ai_memory" "supabase/migrations/20260512000001_ai_memory.sql"</automated>
  </verify>

  <acceptance_criteria>
    - `supabase/migrations/20260512000001_ai_memory.sql` dosyası mevcut
    - Dosya `CREATE TABLE IF NOT EXISTS public.ai_memory` içeriyor
    - Dosya `UNIQUE(project_id, module, key)` içeriyor
    - Dosya `value JSONB NOT NULL DEFAULT '{}'` içeriyor
    - Dosya `idx_ai_memory_project_id` index tanımı içeriyor
    - Dosya `idx_ai_memory_project_module` index tanımı içeriyor
    - Dosya `ENABLE ROW LEVEL SECURITY` içeriyor
    - Dosya `Users own their ai_memory` policy içeriyor
    - Dosya `user_id = auth.uid()` hem USING hem WITH CHECK içeriyor
  </acceptance_criteria>

  <done>Migration dosyası tüm DDL, RLS ve index tanımlarıyla birlikte yazılmış durumda.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] supabase db push — ai_memory tablosunu uzak DB'ye uygula</name>
  <files></files>

  <read_first>
    - `supabase/migrations/20260512000001_ai_memory.sql` — Task 1'de oluşturulan dosya (push'dan önce doğru içerik kontrolü)
  </read_first>

  <action>
Terminal'de şu komutu çalıştır:

```bash
supabase db push
```

Bu komut `supabase/migrations/` altındaki henüz uygulanmamış tüm migration'ları uzak Supabase veritabanına uygular. `ai_memory` tablosu DB'de oluşturulur.

Eğer interaktif prompt çıkarsa (örn. "Do you want to push to production?") `y` ile onayla. Komut başarılı çıkış kodu ile bitmelidir.

ÖNEMLI: Bu task Task 1'den sonra çalışır. Tablo DB'de olmadan 23-02 ve 23-03 planları hata verir.
  </action>

  <verify>
    <automated>supabase db push 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `supabase db push` komutu hata vermeden tamamlanmış
    - Çıktıda "ai_memory" migration'ının uygulandığına dair satır mevcut VEYA "already applied" mesajı (idempotent)
    - Push sonrası başka migration error yoktur
  </acceptance_criteria>

  <done>ai_memory tablosu uzak Supabase veritabanında oluşturulmuş, sonraki planlar tabloya yazabilir.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → Supabase DB | DDL değişikliği üretim veritabanına uygulanır |
| RLS policy | Her SELECT/INSERT/UPDATE/DELETE için user_id = auth.uid() doğrulaması |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | ai_memory table (RLS) | mitigate | `ENABLE ROW LEVEL SECURITY` + `USING (user_id = auth.uid())` policy — diğer kullanıcıların ai_memory satırlarını okuması/yazması engellenir |
| T-23-02 | Elevation of Privilege | ai_memory UNIQUE constraint | accept | UNIQUE(project_id, module, key) — proje bazlı ayrım; project_id ayrıca foreign key olarak projects tablosunu referans alır |
| T-23-03 | Tampering | Migration script injection | accept | Migration dosyası git repository'de yönetilen statik SQL; çalıştırma sadece yerel supabase CLI ile yapılır |
</threat_model>

<verification>
1. `supabase/migrations/20260512000001_ai_memory.sql` dosyası mevcut ve tüm gerekli DDL içeriyor
2. `supabase db push` başarıyla tamamlanmış
3. Sonraki planlarda `ai_memory` tablosu mevcut olduğu için TypeScript hataları oluşmaz
</verification>

<success_criteria>
- Migration dosyası git'te mevcut
- `supabase db push` başarılı — ai_memory tablosu uzak DB'de oluşturulmuş
- RLS + UNIQUE + index doğrulanmış
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/23-keyword-strategy-ai-intelligence/23-01-SUMMARY.md` oluştur.
</output>
