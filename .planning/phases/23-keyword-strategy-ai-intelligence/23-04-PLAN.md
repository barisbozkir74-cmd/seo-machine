---
phase: 23-keyword-strategy-ai-intelligence
plan: 04
type: execute
wave: 3
depends_on:
  - "23-01"
  - "23-02"
  - "23-03"
files_modified:
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts
autonomous: true
requirements:
  - KWST-07

must_haves:
  truths:
    - "approveStrategy action, strateji onaylanınca (approved===true) 4 modüle batch yayılma başlatıyor"
    - "Yayılma non-fatal — hata olursa loglanır ama strateji onayı bloklanmaz"
    - "Site Blueprint (pages): onaylı cluster'larla eşleşen sayfalar ai_memory blueprint namespace'ine yazılıyor"
    - "Sayfa Paketi (page_packages): cluster_id ile bağlı sayfaların mevcut paket bilgisi ai_memory'ye kaydediliyor"
    - "İç Link Haritası (internal_links): cluster bazlı iç link önerileri ai_memory'ye işaretleme olarak yazılıyor"
    - "Monitoring (ai_memory): onaylanan cluster snapshot'ı monitoring namespace'ine kaydediliyor"
    - "approveStrategy mevcut UUID doğrulama + auth + ownership guard'ları değişmiyor"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts"
      provides: "approveStrategy genişletmesi — 4 modül propagation"
      contains: "propagateClusterDecisions"
  key_links:
    - from: "approveStrategy (actions.ts)"
      to: "ai_memory table"
      via: "supabase.from('ai_memory').upsert"
      pattern: "propagateClusterDecisions"
    - from: "approveStrategy"
      to: "keyword_clusters (approved status)"
      via: "supabase.from('keyword_clusters').select status=approved"
      pattern: "status.*approved"
---

<objective>
`actions.ts` içindeki `approveStrategy` server action'ını genişletir. `approved === true` olduğunda 4 modüle (Site Blueprint, Sayfa Paketi, İç Link Haritası, Monitoring) batch yayılma gerçekleştirir. Yayılma `ai_memory` tablosuna UPSERT yazarak modül-bazlı kararları kaydeder.

Purpose: D-09 + D-10 kararları — strateji onayı tüm modüllere yayılır; yayılma toplu ve non-fatal; Phase 20'deki `approveStrategy` gate'i korunur.

Output: Genişletilmiş `approveStrategy` + yeni `propagateClusterDecisions` yardımcı fonksiyon
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-keyword-strategy-ai-intelligence/23-CONTEXT.md
@.planning/phases/23-keyword-strategy-ai-intelligence/23-PATTERNS.md

<interfaces>
<!-- Mevcut approveStrategy (actions.ts lines 880-913): -->
```typescript
export async function approveStrategy(
  projectId: string,
  approved: boolean
): Promise<{ success: true } | { success: false; error: string }> {
  const uuidRegex = /^[0-9a-f-]{36}$/i
  if (!uuidRegex.test(projectId)) {
    return { success: false, error: 'Geçersiz ID formatı.' }
  }

  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Oturum bulunamadı.' }

  const { data: project } = await supabase
    .from('projects')
    .select('id')
    .eq('id', projectId)
    .eq('user_id', user.id)
    .single()
  if (!project) return { success: false, error: 'Proje bulunamadı.' }

  const { error } = await supabase
    .from('projects')
    .update({ keyword_strategy_approved: approved })
    .eq('id', projectId)
    .eq('user_id', user.id)

  if (error) return { success: false, error: 'Strateji onayı kaydedilemedi.' }

  // Phase 23 EKLEME NOKTASI — revalidatePath'den önce
  revalidatePath(`/projeler/${projectId}/keyword-stratejisi`)
  return { success: true }
}
```

Non-fatal batch pattern (actions.ts):
```typescript
try {
  await Promise.all([...])
} catch (err) {
  console.error('[approveStrategy] propagation failed:', err)
}
```

ai_memory UPSERT pattern (23-PATTERNS.md):
```typescript
await supabase
  .from('ai_memory')
  .upsert(
    { user_id: userId, project_id: projectId, module: '...', key: '...', value: {...}, updated_at: new Date().toISOString() },
    { onConflict: 'project_id,module,key' }
  )
```

D-09 deferred not: "Sayfa Paketi" yayılması ÖNERI/İŞARETLEME — otomatik paket oluşturma değil.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: propagateClusterDecisions + approveStrategy genişletme</name>
  <files>src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts</files>

  <read_first>
    - `src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` — lines 880-913 (mevcut approveStrategy), lines 289-444 (clusterAndScoreKeywords batch pattern) — ekleme noktasını ve mevcut kodu anlamak için
    - `.planning/phases/23-keyword-strategy-ai-intelligence/23-CONTEXT.md` — D-09 (4 modül), D-10 (non-fatal, revalidatePath'den önce), deferred section (sayfa paketi otomatik değil)
    - `.planning/phases/23-keyword-strategy-ai-intelligence/23-PATTERNS.md` — Section 4 (approveStrategy genişletme), batch propagation pattern
  </read_first>

  <action>
`actions.ts` dosyasında iki değişiklik yap:

**Değişiklik 1 — `propagateClusterDecisions` yardımcı fonksiyonunu ekle:**

`approveStrategy` fonksiyonunun HEMEN ÜSTÜNe (line 880 öncesine) şu fonksiyonu ekle:

```typescript
async function propagateClusterDecisions(
  projectId: string,
  userId: string,
  supabase: Awaited<ReturnType<typeof createClient>>
): Promise<void> {
  // D-09: Onaylı cluster'ları çek
  const { data: approvedClusters } = await supabase
    .from('keyword_clusters')
    .select('id, cluster_name, intent, revenue_type, total_volume, primary_keyword_id')
    .eq('project_id', projectId)
    .eq('user_id', userId)
    .eq('status', 'approved')

  if (!approvedClusters || approvedClusters.length === 0) return

  // Blueprint sayfalarını çek — cluster eşleşmeleri için
  const { data: blueprintPages } = await supabase
    .from('pages')
    .select('id, title, slug, page_type, cluster_id')
    .eq('project_id', projectId)
    .eq('user_id', userId)

  // D-08: 4 modül için ai_memory UPSERT'leri hazırla
  const now = new Date().toISOString()
  const clusterSnapshot = approvedClusters.map((c) => ({
    id: c.id,
    name: c.cluster_name,
    intent: c.intent,
    revenue_type: c.revenue_type,
    total_volume: c.total_volume,
  }))

  const blueprintSnapshot = (blueprintPages ?? []).map((p) => ({
    id: p.id,
    title: p.title,
    slug: p.slug,
    page_type: p.page_type,
    cluster_id: p.cluster_id,
  }))

  // Cluster'lara atanmış sayfa ID'leri — iç link işaretleme için
  const clusterPageIds = (blueprintPages ?? [])
    .filter((p) => approvedClusters.some((c) => c.id === p.cluster_id))
    .map((p) => p.id)

  // D-10: Batch UPSERT — 4 modül
  await supabase
    .from('ai_memory')
    .upsert(
      [
        // 1. Site Blueprint — onaylı cluster'lara göre blueprint snapshot
        {
          user_id: userId,
          project_id: projectId,
          module: 'blueprint',
          key: 'approved_cluster_pages',
          value: { pages: blueprintSnapshot, updated_at: now },
          updated_at: now,
        },
        // 2. Sayfa Paketi — cluster'a atanmış sayfalar (öneri/işaretleme — D-09 deferred: otomatik oluşturma yok)
        {
          user_id: userId,
          project_id: projectId,
          module: 'page_packages',
          key: 'cluster_page_map',
          value: { cluster_page_ids: clusterPageIds, suggested_at: now },
          updated_at: now,
        },
        // 3. İç Link Haritası — cluster intent bazlı iç link önerileri
        {
          user_id: userId,
          project_id: projectId,
          module: 'internal_links',
          key: 'cluster_link_suggestions',
          value: {
            clusters: approvedClusters.map((c) => ({
              cluster_id: c.id,
              cluster_name: c.cluster_name,
              intent: c.intent,
              suggested_at: now,
            })),
          },
          updated_at: now,
        },
        // 4. Monitoring — onaylı cluster snapshot (izleme için referans noktası)
        {
          user_id: userId,
          project_id: projectId,
          module: 'clusters',
          key: 'approved_snapshot',
          value: { clusters: clusterSnapshot, approved_at: now },
          updated_at: now,
        },
      ],
      { onConflict: 'project_id,module,key' }
    )
}
```

**Değişiklik 2 — `approveStrategy` içine propagation bloğu ekle:**

Mevcut `approveStrategy` fonksiyonunda `if (error) return { success: false, ... }` satırından sonra, `revalidatePath(...)` satırından ÖNCE şu bloğu ekle:

```typescript
  // D-09 + D-10: Strateji onaylandıysa 4 modüle batch yayılma (non-fatal)
  if (approved) {
    try {
      await propagateClusterDecisions(projectId, user.id, supabase)
    } catch (propagationErr) {
      // D-10: Yayılma hatası strateji onayını bloklamaz — sadece logla
      console.error('[approveStrategy] propagation failed:', propagationErr)
    }
  }
```

**DEĞİŞTİRİLMEYECEK BÖLÜMLER:**
- UUID validation regex
- Auth + ownership guard
- `projects.keyword_strategy_approved` UPDATE sorgusu
- `revalidatePath` çağrısı
- Fonksiyonun return type'ı ve return statement'ları
- `DraftCluster` type ve diğer export'lar
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "actions" | head -10</automated>
  </verify>

  <acceptance_criteria>
    - `src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` dosyasında `propagateClusterDecisions` fonksiyon tanımı mevcut
    - Dosya `module: 'blueprint'` içeriyor (blueprint namespace)
    - Dosya `module: 'page_packages'` içeriyor (page packages namespace)
    - Dosya `module: 'internal_links'` içeriyor (internal links namespace)
    - Dosya `module: 'clusters'` içeriyor (monitoring namespace)
    - Dosya `[approveStrategy] propagation failed` log mesajı içeriyor (non-fatal error handling)
    - Dosya `if (approved)` bloğu içeriyor (yayılma sadece onay durumunda)
    - Dosya `propagateClusterDecisions(projectId, user.id, supabase)` çağrısı içeriyor
    - Dosya hâlâ `revalidatePath` içeriyor ve bu satır propagation bloğundan SONRA
    - `npx tsc --noEmit` çalıştırıldığında `actions` ile ilgili TypeScript hatası yok
  </acceptance_criteria>

  <done>
approveStrategy action genişletildi: strateji onaylanınca propagateClusterDecisions çalışır, 4 modül için ai_memory tablosuna UPSERT yapar, hata durumunda strateji onayı bloklanmaz.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → Supabase ai_memory | UPSERT işlemi user_id + project_id ile guard'lı |
| Server Action → keyword_clusters | SELECT sadece approved status + user_id + project_id filtreli |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-12 | Elevation of Privilege | propagateClusterDecisions UPSERT | mitigate | Her UPSERT payload'da `user_id: userId` set edilir; RLS politikası ayrıca `auth.uid()` doğrular — başka kullanıcının ai_memory'sine yazılamaz |
| T-23-13 | Information Disclosure | keyword_clusters SELECT | mitigate | `.eq('user_id', userId)` filtresi — başka kullanıcının cluster'ları okunamaz |
| T-23-14 | Denial of Service | Batch UPSERT performansı | accept | 4 satır UPSERT — negligible load; approvedClusters.length === 0 early return ile boş proje için gereksiz sorgu önlenir |
| T-23-15 | Tampering | Non-fatal propagation | accept | D-10 kararı gereği propagation hatası strateji onayını bloklamaz; hata console.error ile loglanır; veri tutarsızlığı riski düşük (ai_memory ikincil önbellektir, kaynak gerçek tablolardır) |
</threat_model>

<verification>
1. `actions.ts` dosyasında `propagateClusterDecisions` fonksiyonu mevcut
2. `approveStrategy` içinde `if (approved)` bloğu + try/catch non-fatal pattern mevcut
3. 4 modül namespace (`blueprint`, `page_packages`, `internal_links`, `clusters`) hepsi kapsanmış
4. `npx tsc --noEmit` TypeScript hatasız
5. Mevcut `approveStrategy` return type ve auth guard'ları değişmemiş
</verification>

<success_criteria>
- approveStrategy genişletilmiş: strateji onayı 4 modüle batch yayılıyor
- Yayılma non-fatal: hata loglanıyor, strateji onayı bloklanmıyor
- UPSERT payload'ları user_id ile guard'lı
- TypeScript hatasız compile
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/23-keyword-strategy-ai-intelligence/23-04-SUMMARY.md` oluştur.
</output>
