---
phase: 23-keyword-strategy-ai-intelligence
plan: 02
type: execute
wave: 2
depends_on:
  - "23-01"
files_modified:
  - src/app/api/keywords/analyze/route.ts
autonomous: true
requirements:
  - KWST-06

must_haves:
  truths:
    - "POST /api/keywords/analyze endpoint'i kimlik doğrulama + IDOR korumasıyla çalışıyor"
    - "Endpoint, proje keyword'lerini, cluster'larını ve blueprint sayfalarını context olarak topluyor"
    - "ai_memory tablosundan önceki hafıza prompt'a dahil ediliyor"
    - "Primary AI (claude-sonnet-4-6) stream açıyor ve stratejik analiz üretiyor"
    - "Review AI ikinci aşamada devreye giriyor, __REVIEW_START__ separator'ı stream'e yazılıyor"
    - "Analiz sonrası ai_memory tablosuna UPSERT ON CONFLICT yazma yapılıyor"
    - "Response header'ları mevcut chat route pattern'iyle eşleşiyor"
  artifacts:
    - path: "src/app/api/keywords/analyze/route.ts"
      provides: "İki aşamalı AI analiz streaming endpoint"
      exports: ["POST"]
  key_links:
    - from: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordChat.tsx"
      to: "/api/keywords/analyze"
      via: "fetch POST"
      pattern: "fetch.*api/keywords/analyze"
    - from: "src/app/api/keywords/analyze/route.ts"
      to: "ai_memory"
      via: "supabase.from('ai_memory').upsert"
      pattern: "ai_memory.*upsert"
---

<objective>
`src/app/api/keywords/analyze/route.ts` adında yeni bir Next.js API route oluşturur. Bu route iki aşamalı streaming AI pipeline uygular: Primary AI (SEO Strategist) stratejik analiz üretir, ardından Review AI (Traffic Architect) intent çakışması + cannibalization denetimi yapar. Her iki AI da `claude-sonnet-4-6` modelini kullanır.

Purpose: D-02 + D-03 + D-05 kararları — AI analiz UI'da chat mesajı olarak görünür, Review AI Primary sonrası otomatik devreye girer, sadece Site Blueprint verisi cross-module context olarak kullanılır.

Output: `src/app/api/keywords/analyze/route.ts` — Production streaming endpoint
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-keyword-strategy-ai-intelligence/23-CONTEXT.md
@.planning/phases/23-keyword-strategy-ai-intelligence/23-PATTERNS.md

<interfaces>
<!-- Mevcut analog: src/app/api/keywords/chat/route.ts (birebir pattern referansı) -->

Auth + IDOR guard (chat/route.ts lines 7-28):
```typescript
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) return new Response('Unauthorized', { status: 401 })

const { data: project } = await supabase
  .from('projects')
  .select('id, name, domain')
  .eq('id', projectId)
  .eq('user_id', user.id)
  .single()
if (!project) return new Response('Not Found', { status: 404 })
```

Anthropic SDK pattern (src/app/api/ai/generate-page-package/route.ts lines 1-5):
```typescript
import Anthropic from '@anthropic-ai/sdk'
const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })
```

Streaming response headers (chat/route.ts lines 103-109):
```typescript
return new Response(readable, {
  headers: {
    'Content-Type': 'text/plain; charset=utf-8',
    'Transfer-Encoding': 'chunked',
    'X-Content-Type-Options': 'nosniff',
  },
})
```

ai_memory UPSERT pattern (23-PATTERNS.md):
```typescript
await supabase
  .from('ai_memory')
  .upsert(
    { user_id: user.id, project_id: projectId, module: 'analysis', key: 'last_primary',
      value: { summary: primaryOutput.slice(0, 500), analyzed_at: new Date().toISOString() },
      updated_at: new Date().toISOString() },
    { onConflict: 'project_id,module,key' }
  )
```

Blueprint sorgusu (D-05 — sadece pages tablosu):
```typescript
supabase
  .from('pages')
  .select('id, title, slug, page_type, priority, parent_id, cluster_id')
  .eq('project_id', projectId)
  .eq('user_id', user.id)
  .order('sort_order', { ascending: true })
```

Separator token (client ile contract):
```
'\n\n__REVIEW_START__\n\n'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /api/keywords/analyze route.ts — iki aşamalı Anthropic streaming pipeline</name>
  <files>src/app/api/keywords/analyze/route.ts</files>

  <read_first>
    - `src/app/api/keywords/chat/route.ts` — auth guard, IDOR check, keyword+cluster context toplama, streaming response headers için tam analog
    - `src/app/api/ai/generate-page-package/route.ts` — Anthropic SDK import ve `client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })` init pattern
    - `.planning/phases/23-keyword-strategy-ai-intelligence/23-PATTERNS.md` — iki aşamalı streaming, ai_memory UPSERT, separator token contract
    - `.planning/phases/23-keyword-strategy-ai-intelligence/23-CONTEXT.md` — D-05 (sadece blueprint context), D-07 (ai_memory UPSERT), D-08 (modül yapısı)
  </read_first>

  <action>
`src/app/api/keywords/analyze/route.ts` dosyasını oluştur. Yapı:

**1. Import ve client init:**
```typescript
import { NextRequest } from 'next/server'
import Anthropic from '@anthropic-ai/sdk'
import { createClient } from '@/lib/supabase/server'

const anthropic = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })
```
OpenAI import KULLANMA — bu route Anthropic SDK kullanır (PATTERNS.md kritik not #1).

**2. Auth + IDOR guard:** `chat/route.ts` lines 7-28 pattern birebir kopyala. Request body'den `{ projectId }` çek (messages yok — proaktif analiz, kullanıcı mesajı almaz).

**3. Paralel context sorguları (Promise.all ile 3 sorgu):**
- Keywords: `chat/route.ts` lines 32-39 pattern (parent_keyword_id IS NULL, limit 200)
- Clusters: `chat/route.ts` lines 40-46 pattern
- Blueprint pages (D-05): `.from('pages').select('id, title, slug, page_type, priority, parent_id, cluster_id').eq('project_id', projectId).eq('user_id', user.id).order('sort_order', { ascending: true })`

**4. ai_memory okuma (D-06):**
```typescript
const { data: memoryRows } = await supabase
  .from('ai_memory')
  .select('module, key, value')
  .eq('project_id', projectId)
  .eq('user_id', user.id)
```

**5. Context metni oluştur:**
- `clusterSummary`: `chat/route.ts` lines 51-59 pattern (cluster adı, intent, revenue, volume, top 5 keyword)
- `blueprintSummary`: pages array'ini formatla — `(pages ?? []).slice(0, 30).map(p => \`• [${p.page_type ?? '?'}] ${p.title} (${p.slug})\`).join('\n')`
- `memorySummary`: memoryRows'u formatla — `(memoryRows ?? []).map(r => \`[${r.module}/${r.key}]: ${JSON.stringify(r.value).slice(0, 200)}\`).join('\n')`

**6. Primary AI system prompt:**
```
Sen bir SEO Strateji Uzmanısın (Traffic Architect rolü). Aşağıdaki proje verisini analiz et ve proaktif stratejik bulgularını Türkçe olarak sun.

Proje: ${project.name} (${project.domain})
Toplam keyword: ${(keywords ?? []).length} | Toplam küme: ${(clusters ?? []).length}

KÜMELER:
${clusterSummary || '(küme yok)'}

SITE BLUEPRINT (mevcut sayfalar):
${blueprintSummary || '(blueprint yok)'}

AI HAFIZA (önceki kararlar):
${memorySummary || '(hafıza yok)'}

Görevin:
1. Intent dağılımı analizi (informational vs commercial vs transactional)
2. Commercial gap tespiti (yüksek gelir potansiyeli olan eksik kümeler)
3. Cannibalization riski (aynı intent'e hizmet eden birden fazla küme)
4. Pillar/support cluster map (hangi kümeler ana sayfa, hangileri destek içerik)
5. Blueprint uyum analizi (blueprint sayfaları ile cluster'lar arasındaki boşluklar)

Kısa ve aksiyon odaklı yaz. Her bulgu için somut öneri ver.
```

**7. Review AI system prompt:**
```
Sen bir SEO Denetçisisin. Aşağıdaki Primary AI analizini incele ve şu 3 konuda kısa denetim raporu yaz:
1. Intent çakışması: Birden fazla küme aynı intent'e mi hizmet ediyor?
2. Cannibalization riski: Hangi keyword grupları birbiriyle rekabet ediyor?
3. Eksik commercial page'ler: Blueprint'te olması gereken ama olmayan sayfalar

Bulgularını madde madde, Türkçe ve kısa yaz. Onaylanan noktalarda "✓" kullan.
```

**8. İki aşamalı Anthropic streaming:**

```typescript
const encoder = new TextEncoder()
const readable = new ReadableStream({
  async start(controller) {
    let primaryOutput = ''

    // Aşama 1: Primary AI
    const primaryStream = await anthropic.messages.create({
      model: 'claude-sonnet-4-6',
      max_tokens: 1024,
      temperature: 0.4 as never, // Anthropic SDK type uyumu
      stream: true,
      system: primarySystemPrompt,
      messages: [{ role: 'user', content: 'Keyword stratejimi analiz et.' }],
    })

    for await (const event of primaryStream) {
      if (event.type === 'content_block_delta' && event.delta.type === 'text_delta') {
        const text = event.delta.text
        primaryOutput += text
        controller.enqueue(encoder.encode(text))
      }
    }

    // Separator token — client bu token'ı görünce Review mesajını state'e ekler
    controller.enqueue(encoder.encode('\n\n__REVIEW_START__\n\n'))

    // Aşama 2: Review AI — Primary çıktısı context olarak verilir
    const reviewStream = await anthropic.messages.create({
      model: 'claude-sonnet-4-6',
      max_tokens: 512,
      temperature: 0.3 as never,
      stream: true,
      system: reviewSystemPrompt,
      messages: [{ role: 'user', content: primaryOutput }],
    })

    for await (const event of reviewStream) {
      if (event.type === 'content_block_delta' && event.delta.type === 'text_delta') {
        controller.enqueue(encoder.encode(event.delta.text))
      }
    }

    // Analiz tamamlandı — ai_memory'ye yaz (D-07, D-08)
    await supabase
      .from('ai_memory')
      .upsert(
        {
          user_id: user.id,
          project_id: projectId,
          module: 'analysis',
          key: 'last_primary',
          value: { summary: primaryOutput.slice(0, 500), analyzed_at: new Date().toISOString() },
          updated_at: new Date().toISOString(),
        },
        { onConflict: 'project_id,module,key' }
      )

    controller.close()
  },
})
```

**9. Response headers:** `chat/route.ts` lines 103-109 ile birebir aynı (Content-Type, Transfer-Encoding, X-Content-Type-Options).

**Önemli Notlar:**
- `temperature` Anthropic SDK'da `messages.create` parametresi olarak geçerli — `as never` TypeScript type uyumu için
- `anthropic.messages.create` stream event type'ları: `content_block_delta` + `delta.type === 'text_delta'` + `delta.text`
- ai_memory UPSERT stream `controller.close()` çağrısından önce yapılır
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "analyze/route" | head -10</automated>
  </verify>

  <acceptance_criteria>
    - `src/app/api/keywords/analyze/route.ts` dosyası mevcut
    - Dosya `import Anthropic from '@anthropic-ai/sdk'` içeriyor (OpenAI import YOK)
    - Dosya `export async function POST` içeriyor
    - Dosya `const anthropic = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })` içeriyor
    - Dosya `.from('ai_memory')` içeriyor (okuma ve UPSERT)
    - Dosya `__REVIEW_START__` separator token içeriyor
    - Dosya `.from('pages')` içeriyor (D-05 blueprint context)
    - Dosya `claude-sonnet-4-6` model adı içeriyor
    - Dosya `controller.close()` içeriyor
    - `npx tsc --noEmit` çalıştırıldığında `analyze/route` ile ilgili TypeScript hatası yok
  </acceptance_criteria>

  <done>
POST /api/keywords/analyze endpoint'i çalışıyor: auth + IDOR korumalı, keyword+cluster+blueprint context topluyor, Primary→Review iki aşamalı Anthropic streaming yapıyor, __REVIEW_START__ separator token'ı yayıyor, analiz sonrası ai_memory'yi güncelliyor.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /api/keywords/analyze | projectId parametresi istemciden geliyor — doğrulanmadan kullanılamaz |
| Server → Anthropic API | ANTHROPIC_API_KEY sunucu ortam değişkeninden okunuyor — client-side asla ulaşamaz |
| Prompt → AI | Keyword metinleri AI prompt'a gömülüyor — prompt injection vektörü |
| Server → ai_memory (Supabase) | UPSERT işlemi her iki user_id + project_id ile guard'lı olmalı |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Spoofing | POST /api/keywords/analyze auth | mitigate | `supabase.auth.getUser()` ile session doğrulaması; user null ise 401 döner |
| T-23-05 | Information Disclosure (IDOR) | projectId ownership check | mitigate | `.eq('user_id', user.id)` ile projects tablosunda sahiplik doğrulaması yapılır; başka kullanıcının proje verisi okunmaz |
| T-23-06 | Tampering (Prompt Injection) | keyword/cluster adları AI prompt'a gömülüyor | mitigate | Context verisi system prompt'a eklenir (user mesajı olarak değil); Anthropic system/user role ayrımı injection etkisini minimize eder. Keyword değerleri `slice(0, 200)` ile truncate edilir |
| T-23-07 | Information Disclosure | ANTHROPIC_API_KEY | mitigate | `process.env.ANTHROPIC_API_KEY` sunucu-side only; route.ts Edge/Node runtime'da çalışır, client bundle'a girmez |
| T-23-08 | Tampering | ai_memory UPSERT | mitigate | UPSERT payload'da `user_id: user.id` açıkça set edilir; RLS politikası ayrıca `auth.uid()` doğrular |
  </action>
</threat_model>

<verification>
1. `src/app/api/keywords/analyze/route.ts` mevcut
2. `npx tsc --noEmit` TypeScript hatası yok
3. Dosya `@anthropic-ai/sdk` import ediyor (OpenAI değil)
4. `__REVIEW_START__` separator token hem route.ts'de hem 23-03-PLAN.md'deki client kodunda aynı string
</verification>

<success_criteria>
- Route dosyası oluşturulmuş ve TypeScript hatasız compile oluyor
- İki aşamalı streaming pipeline kodu doğru yapıda — Primary biter, separator yazılır, Review başlar
- ai_memory UPSERT analiz sonrası yapılıyor
- Tüm Supabase sorguları hem project_id hem user_id filtresi içeriyor (IDOR koruması)
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/23-keyword-strategy-ai-intelligence/23-02-SUMMARY.md` oluştur.
</output>
