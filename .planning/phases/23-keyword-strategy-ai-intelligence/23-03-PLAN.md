---
phase: 23-keyword-strategy-ai-intelligence
plan: 03
type: execute
wave: 2
depends_on:
  - "23-01"
files_modified:
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordChat.tsx
autonomous: true
requirements:
  - KWST-06

must_haves:
  truths:
    - "KeywordChat bileşeni sayfa ilk yüklenince localStorage kontrolü yapıyor ve analizi otomatik tetikliyor"
    - "Header satırında 'Stratejiyi Yenile' butonu collapse chevron'dan önce görünüyor"
    - "Primary AI mesajı chat'e birinci assistant mesajı olarak stremleniyor"
    - "Review AI mesajı __REVIEW_START__ token gelince chat'e ikinci assistant mesajı olarak ekleniyor"
    - "Stratejiyi Yenile butonu streaming sırasında disabled ve 'Analiz ediliyor...' spinner ile gösteriliyor"
    - "Analiz tamamlanınca localStorage'a kwai_analyzed_{projectId} timestamp yazılıyor"
    - "Mevcut STARTERS panel, input textarea, send butonu değişmiyor"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordChat.tsx"
      provides: "Proaktif analiz + Stratejiyi Yenile + dual-message streaming"
      contains: "runAnalysis"
  key_links:
    - from: "KeywordChat.tsx useEffect"
      to: "localStorage kwai_analyzed_{projectId}"
      via: "localStorage.getItem"
      pattern: "kwai_analyzed_"
    - from: "KeywordChat.tsx runAnalysis"
      to: "/api/keywords/analyze"
      via: "fetch POST"
      pattern: "api/keywords/analyze"
    - from: "stream reader"
      to: "__REVIEW_START__ separator"
      via: "accumulated.includes"
      pattern: "__REVIEW_START__"
---

<objective>
Mevcut `KeywordChat.tsx` bileşenini genişletir: (1) sayfa yüklenmesinde otomatik analiz tetiklemesi, (2) header'a "Stratejiyi Yenile" butonu, (3) iki aşamalı mesaj state machine — Primary ve Review AI mesajlarını ayrı chat balonları olarak stremleme.

Purpose: D-01 (auto-trigger + yenile butonu), D-02 (AI analizi chat'te göster), D-03 (Review AI otomatik), D-04 (iki ayrı chat mesajı). UI-SPEC'in tüm interaction contract'larını karşılar.

Output: Genişletilmiş `KeywordChat.tsx` — mevcut send() akışı korunmuş, runAnalysis() eklemi
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-keyword-strategy-ai-intelligence/23-CONTEXT.md
@.planning/phases/23-keyword-strategy-ai-intelligence/23-PATTERNS.md
@.planning/phases/23-keyword-strategy-ai-intelligence/23-UI-SPEC.md

<interfaces>
<!-- Mevcut KeywordChat.tsx yapısı (218 satır) — değiştirilmemesi gereken bölümler: -->

Korunacak state (lines 27-31):
```typescript
const [messages, setMessages] = useState<Message[]>([])
const [input, setInput] = useState('')
const [isStreaming, setIsStreaming] = useState(false)
const bottomRef = useRef<HTMLDivElement>(null)
const inputRef = useRef<HTMLTextAreaElement>(null)
```

Korunacak auto-scroll (lines 33-35):
```typescript
useEffect(() => {
  bottomRef.current?.scrollIntoView({ behavior: 'smooth' })
}, [messages])
```

Korunacak mevcut header (lines 124-141) — genişletilecek:
```
[icon] AI Asistan          [collapse chevron]
→ değiştirilecek:
[icon] AI Asistan     [Stratejiyi Yenile btn]  [collapse chevron]
```

Korunacak streaming dots pattern (lines 174-179) — her iki AI mesajı bu pattern'i kullanır:
```typescript
{msg.role === 'assistant' && msg.content === '' ? (
  <span className="inline-flex gap-1 items-center text-muted-foreground">
    <span className="animate-bounce [animation-delay:0ms]">·</span>
    <span className="animate-bounce [animation-delay:150ms]">·</span>
    <span className="animate-bounce [animation-delay:300ms]">·</span>
  </span>
) : (
  <span className="whitespace-pre-wrap">{msg.content}</span>
)}
```

Separator contract (23-02-PLAN.md ile senkronize):
```
'\n\n__REVIEW_START__\n\n'
```

localStorage key (23-PATTERNS.md kritik not #3):
```
`kwai_analyzed_${projectId}`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KeywordChat.tsx — runAnalysis() + auto-trigger useEffect + Stratejiyi Yenile butonu</name>
  <files>src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordChat.tsx</files>

  <read_first>
    - `src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordChat.tsx` — mevcut 218 satırlık dosyanın tamamı (ne korunacak, nereye ekleme yapılacak bilinmeli)
    - `src/app/(dashboard)/projeler/[id]/keyword-stratejisi/AiSuggestButton.tsx` — Stratejiyi Yenile butonunun violet stil + spinner pattern'i için (UI-SPEC'te referans veriliyor)
    - `.planning/phases/23-keyword-strategy-ai-intelligence/23-UI-SPEC.md` — Stratejiyi Yenile buton spec, state machine tablosu, copywriting contract
    - `.planning/phases/23-keyword-strategy-ai-intelligence/23-PATTERNS.md` — runAnalysis() implementasyonu (Section 3, tam kod bloğu), header layout değişikliği
  </read_first>

  <action>
`KeywordChat.tsx` dosyasını aşağıdaki değişikliklerle güncelle. Mevcut `send()` fonksiyonu, `handleKeyDown`, tüm JSX render blokları (collapsed branch, STARTERS, message map, input area) değişmeden korunur.

**1. Mevcut auto-scroll useEffect'ten sonra (line 35 sonrasına) auto-trigger useEffect ekle:**

```typescript
// D-01: İlk yüklemede analiz yapılmamışsa otomatik tetikle
useEffect(() => {
  const stored = localStorage.getItem(`kwai_analyzed_${projectId}`)
  if (!stored) {
    runAnalysis()
  }
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, [projectId])
```

NOT: `runAnalysis` fonksiyonu bu useEffect'ten sonra tanımlanır — ancak JavaScript hoisting değil, dosya sırasını şöyle düzenle: useEffect tanımları → send() tanımı → runAnalysis() tanımı. eslint-disable yorumu dependency array uyarısını baskılamak için gereklidir.

**2. `send()` fonksiyonundan sonra `runAnalysis()` fonksiyonunu ekle:**

```typescript
const runAnalysis = async () => {
  if (isStreaming) return

  const primaryId = crypto.randomUUID()
  const reviewId = crypto.randomUUID()

  // Primary AI için placeholder mesaj ekle
  setMessages([{ id: primaryId, role: 'assistant', content: '' }])
  setIsStreaming(true)

  try {
    const res = await fetch('/api/keywords/analyze', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ projectId }),
    })

    if (!res.ok || !res.body) {
      setMessages([{
        id: primaryId,
        role: 'assistant',
        content: 'Analiz başarısız oldu. Stratejiyi Yenile butonuna tıklayarak tekrar dene.',
      }])
      return
    }

    const reader = res.body.getReader()
    const decoder = new TextDecoder()
    let accumulated = ''
    let inReview = false

    while (true) {
      const { done, value } = await reader.read()
      if (done) break
      accumulated += decoder.decode(value, { stream: true })

      if (!inReview && accumulated.includes('__REVIEW_START__')) {
        // Primary bitti — Review mesajını state'e ekle
        const primaryContent = accumulated.split('__REVIEW_START__')[0]
        setMessages([
          { id: primaryId, role: 'assistant', content: primaryContent },
          { id: reviewId, role: 'assistant', content: '' },
        ])
        accumulated = accumulated.split('__REVIEW_START__')[1] ?? ''
        inReview = true
      } else if (inReview) {
        const snap = accumulated
        setMessages((prev) =>
          prev.map((m) =>
            m.id === reviewId ? { ...m, content: 'Denetim: ' + snap } : m
          )
        )
      } else {
        const snap = accumulated
        setMessages((prev) =>
          prev.map((m) => (m.id === primaryId ? { ...m, content: snap } : m))
        )
      }
    }

    // D-01: Başarılı analiz timestamp'ini kaydet
    localStorage.setItem(`kwai_analyzed_${projectId}`, new Date().toISOString())
  } catch {
    setMessages([{
      id: primaryId,
      role: 'assistant',
      content: 'Analiz başarısız oldu. Stratejiyi Yenile butonuna tıklayarak tekrar dene.',
    }])
  } finally {
    setIsStreaming(false)
  }
}
```

**3. Header'a Stratejiyi Yenile butonu ekle (mevcut header — lines 123-141):**

Mevcut header JSX'i şu şekilde güncelle — yalnızca collapse chevron butonundan ÖNCE bir buton ekle:

```typescript
{/* Header */}
<div className="flex items-center justify-between px-3 py-2 border-b border-border shrink-0">
  <div className="flex items-center gap-2">
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" className="text-violet-400">
      <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z" />
    </svg>
    <span className="text-xs font-medium">AI Asistan</span>
  </div>
  <div className="flex items-center gap-1">
    {/* Stratejiyi Yenile — UI-SPEC: violet-400 + spinner on loading */}
    <button
      onClick={() => { setMessages([]); runAnalysis() }}
      disabled={isStreaming}
      title="Stratejiyi yeniden analiz et"
      className="text-xs text-violet-400 hover:text-violet-300 transition-colors disabled:opacity-50 px-2 py-0.5 shrink-0"
    >
      {isStreaming ? (
        <span className="inline-flex items-center gap-1">
          <svg className="animate-spin h-3 w-3" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
            <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
            <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z" />
          </svg>
          Analiz ediliyor...
        </span>
      ) : 'Stratejiyi Yenile'}
    </button>
    {/* Collapse butonu — değişmez */}
    <button
      onClick={onToggle}
      className="text-muted-foreground hover:text-foreground transition-colors"
      title="Daralt"
    >
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
        <polyline points="13 17 18 12 13 7" />
        <polyline points="6 17 11 12 6 7" />
      </svg>
    </button>
  </div>
</div>
```

Mevcut header'daki `justify-between` korunur; sağ tarafa yeni `div.flex.items-center.gap-1` wrapper eklenerek iki buton yan yana hizalanır.

**Değiştirilmeyecek bölümler (kesinlikle dokunma):**
- `collapsed` branch render bloğu (lines 102-119)
- STARTERS array ve starter butonlar render'ı
- User mesajı bubble stilleri (`bg-primary text-primary-foreground rounded-xl rounded-br-sm`)
- Streaming dots pattern (3 bounce nokta) — mevcut `msg.content === ''` kontrolü Review mesajına da otomatik uygulanır
- Input textarea ve send butonu
- `send()` fonksiyonu
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "KeywordChat" | head -10</automated>
  </verify>

  <acceptance_criteria>
    - `src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordChat.tsx` dosyasında `runAnalysis` fonksiyon tanımı mevcut
    - Dosya `kwai_analyzed_${projectId}` string içeriyor (localStorage key)
    - Dosya `__REVIEW_START__` string içeriyor (separator token kontrolü)
    - Dosya `Stratejiyi Yenile` text içeriyor (buton etiketi)
    - Dosya `Analiz ediliyor...` text içeriyor (loading etiketi)
    - Dosya `Stratejiyi yeniden analiz et` text içeriyor (title attr)
    - Dosya `api/keywords/analyze` içeriyor (fetch target)
    - Dosya `Denetim: ` prefix içeriyor (Review AI mesaj prefix)
    - `npx tsc --noEmit` çalıştırıldığında `KeywordChat` ile ilgili TypeScript hatası yok
    - `send()` fonksiyonu hâlâ mevcut ve değişmemiş (`/api/keywords/chat` endpoint'ini çağırıyor)
    - `STARTERS` array tanımı hâlâ mevcut
  </acceptance_criteria>

  <done>
KeywordChat bileşeni proaktif analiz özelliğine kavuştu: sayfa yüklenince localStorage kontrolü yapılıyor, analiz yoksa otomatik başlatılıyor, header'da Stratejiyi Yenile butonu mevcut, Primary ve Review AI mesajları ayrı balonlar olarak stremleniyor.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage → KeywordChat | localStorage değeri clientside — başkası okuyamaz ama sunucu değil |
| KeywordChat → /api/keywords/analyze | fetch POST, sunucu tarafında auth+IDOR kontrolü (23-02-PLAN'da ele alındı) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-09 | Information Disclosure | localStorage kwai_analyzed_{projectId} | accept | Yalnızca timestamp (ISO string) — kişisel veri değil; projectId zaten URL'de görünür |
| T-23-10 | Denial of Service | isStreaming guard | mitigate | `if (isStreaming) return` — çift tıklama veya race condition önlenir; tek analiz aynı anda çalışır |
| T-23-11 | Spoofing | Separator token __REVIEW_START__ | accept | Token rastgele seçilmiş, özel bir güvenlik iddiası yok; API sadece sunucu tarafında üretir, client'tan gelen veriye dayanmaz |
</threat_model>

<verification>
1. `KeywordChat.tsx` dosyası `runAnalysis` içeriyor
2. `__REVIEW_START__` hem route.ts (23-02) hem KeywordChat.tsx'de aynı string
3. `send()` fonksiyonu değişmemiş — `/api/keywords/chat` çağrısı korunuyor
4. `npx tsc --noEmit` TypeScript hatasız
</verification>

<success_criteria>
- KeywordChat genişletilmiş: auto-trigger, Stratejiyi Yenile butonu, dual-message state machine
- Mevcut sohbet akışı (send + STARTERS) bozulmamış
- UI-SPEC'teki tüm copywriting değerleri uygulanmış
- TypeScript hatasız compile
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/23-keyword-strategy-ai-intelligence/23-03-SUMMARY.md` oluştur.
</output>
