---
phase: 10-schema-center
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02-PLAN.md
files_modified:
  - src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx
autonomous: true
requirements:
  - PAGE-02
  - PAGE-02b

must_haves:
  truths:
    - "PagePackageEditor'da Paket ve Schema sekmeleri görünüyor (sadece pkg mevcut olduğunda)"
    - "Schema sekmesinde 'Schema Üret' butonu page_type'a göre doğru JSON-LD üretiyor"
    - "Üretilen JSON-LD schemaJsonLd state'ine yazılıyor, textarea'da görünüyor"
    - "Kopyala butonu clipboard'a yazar, 2sn sonra geri döner (emerald-500 / red-400)"
    - "handleSave() schema_jsonld'yi de updatePagePackage'e gönderiyor"
    - "Paket sekmesi içeriği değişmeden korunuyor"
    - "Package locked iken Schema Üret butonu ve textarea disabled"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx"
      provides: "Tab navigasyonu + generateSchemaJsonLd fonksiyonu + Schema sekmesi UI"
      contains: "activeTab"
      exports: ["PagePackageEditor", "PageData"]
  key_links:
    - from: "generateSchemaJsonLd(page)"
      to: "schemaJsonLd state"
      via: "JSON.stringify(result, null, 2) ile setSchemaJsonLd"
      pattern: "generateSchemaJsonLd"
    - from: "handleSave"
      to: "updatePagePackage"
      via: "schema_jsonld: parseJsonField(schemaJsonLd)"
      pattern: "schema_jsonld.*parseJsonField"
    - from: "pkg?.schema_jsonld"
      to: "schemaJsonLd initial state"
      via: "useState(jsonString(pkg?.schema_jsonld))"
      pattern: "jsonString.*schema_jsonld"
---

<objective>
`PagePackageEditor.tsx`'e iki sekme navigasyonu ve tam Schema sekmesi ekler: tab bar, generateSchemaJsonLd pure function, handleGenerateSchema/handleCopySchema handler'ları, Schema sekmesi UI (boş durum + dolu durum).

Purpose: PAGE-02 ve PAGE-02b gereksinimlerini karşılayan son implementasyon katmanı — kullanıcı schema üretebilir, düzenleyebilir, kopyalayabilir ve kaydedebilir.
Output: Tek dosya değişikliği — tüm Phase 10 UI mantığı PagePackageEditor.tsx içinde.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-schema-center/10-CONTEXT.md
@.planning/phases/10-schema-center/10-UI-SPEC.md
@.planning/phases/10-schema-center/10-PATTERNS.md

<interfaces>
<!-- PageData tipi — bu planda güncelleniyor (satır 23–50) -->
Mevcut pkg bloğuna schema_jsonld: unknown eklenecek:
```typescript
export type PageData = {
  id: string
  title: string
  slug: string | null
  page_type: string | null
  focus_keyword_id: string | null
  focus_keyword_text?: string | null
  pkg?: {
    id: string
    status: string
    generated_by: string
    seo_title: string | null
    meta_description: string | null
    h1: string | null
    slug: string | null
    search_intent: string | null
    strategic_purpose: string | null
    secondary_keywords: unknown
    heading_hierarchy: unknown
    content_blocks: unknown
    cta_blocks: unknown
    image_plan: unknown
    alt_texts: unknown
    schema_type: string | null
    canonical_url: string | null
    faq: unknown
    schema_jsonld: unknown   // YENİ — bu planda ekleniyor
  } | null
}
```

<!-- State blokları — genişletilecek (satır 163–205 civarı) -->
```typescript
// Mevcut son state satırları:
const [faq, setFaq] = useState(jsonString(pkg?.faq))
const [saveStatus, setSaveStatus] = useState<'idle' | 'success' | 'error'>('idle')
const [aiStatus, setAiStatus] = useState<...>('idle')

// YENİ state'ler eklenecek (faq state'inin hemen altına):
const [schemaJsonLd, setSchemaJsonLd] = useState(jsonString(pkg?.schema_jsonld))
const [activeTab, setActiveTab] = useState<'paket' | 'schema'>('paket')
const [copyStatus, setCopyStatus] = useState<'idle' | 'copied' | 'error'>('idle')
```

<!-- handleSave mevcut updatePagePackage çağrısı (satır 305–322) -->
```typescript
const result = await updatePagePackage(projectId, page.id, {
  page_type: pageType || undefined,
  // ...diğer alanlar...
  faq: parseJsonField(faq),
  // YENİ — faq satırının hemen altına:
  schema_jsonld: parseJsonField(schemaJsonLd),
})
```

<!-- generateSchemaJsonLd — component üstüne eklenecek pure function -->
```typescript
function generateSchemaJsonLd(page: PageData): object | object[] {
  const pkg = page.pkg
  const pageType = page.page_type ?? null
  const name = pkg?.seo_title || page.title || ''
  const description = pkg?.meta_description || ''
  const url = pkg?.canonical_url || ''

  const baseSchema: Record<string, unknown> = {
    '@context': 'https://schema.org',
    '@type': (() => {
      switch (pageType) {
        case 'homepage': return ['Organization', 'WebSite']
        case 'service':  return 'Service'
        case 'product':  return 'Product'
        case 'blog':     return 'Article'
        case 'category':
        case 'landing':
        default:         return 'WebPage'
      }
    })(),
    ...(name        && { name }),
    ...(description && { description }),
    ...(url         && { url }),
  }

  // FAQPage augmentation
  let faqItems: Array<{ soru?: string; cevap?: string }> = []
  try {
    const parsed = typeof pkg?.faq === 'string' ? JSON.parse(pkg.faq as string) : pkg?.faq
    if (Array.isArray(parsed) && parsed.length > 0) faqItems = parsed
  } catch { /* ignore */ }

  if (faqItems.length > 0) {
    const faqSchema = {
      '@context': 'https://schema.org',
      '@type': 'FAQPage',
      mainEntity: faqItems.map((item) => ({
        '@type': 'Question',
        name: item.soru ?? '',
        acceptedAnswer: {
          '@type': 'Answer',
          text: item.cevap ?? '',
        },
      })),
    }
    return [baseSchema, faqSchema]
  }

  return baseSchema
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PageData tipini güncelle + yeni state'leri ve handler'ları ekle</name>
  <files>src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx (tüm dosya — mevcut durum)
  </read_first>
  <behavior>
    - PageData.pkg tipine schema_jsonld: unknown eklendi → TypeScript tip hatası yok
    - generateSchemaJsonLd('homepage') → { '@context': 'https://schema.org', '@type': ['Organization', 'WebSite'] }
    - generateSchemaJsonLd('service') → { '@type': 'Service' }
    - generateSchemaJsonLd(null/unknown) → { '@type': 'WebPage' } (fallback)
    - generateSchemaJsonLd + faq dolu → [baseSchema, { '@type': 'FAQPage', mainEntity: [...] }]
    - handleCopySchema başarılı → setCopyStatus('copied'), 2000ms sonra 'idle'
    - handleCopySchema hata → setCopyStatus('error'), 2000ms sonra 'idle'
    - handleSave schema_jsonld: parseJsonField(schemaJsonLd) içeriyor
  </behavior>
  <action>
Aşağıdaki değişiklikleri sırayla yap:

**1. PageData tipine schema_jsonld ekle (satır ~30–49, pkg bloğu sonu):**
`faq: unknown` satırının hemen altına ekle:
```typescript
    schema_jsonld: unknown
```

**2. generateSchemaJsonLd pure function'ı ekle — component export'unun ÜSTÜNE (PagePackageEditor fonksiyon tanımının hemen üstüne):**
```typescript
function generateSchemaJsonLd(page: PageData): object | object[] {
  const pkg = page.pkg
  const pageType = page.page_type ?? null
  const name = pkg?.seo_title || page.title || ''
  const description = pkg?.meta_description || ''
  const url = pkg?.canonical_url || ''

  const baseSchema: Record<string, unknown> = {
    '@context': 'https://schema.org',
    '@type': (() => {
      switch (pageType) {
        case 'homepage': return ['Organization', 'WebSite']
        case 'service':  return 'Service'
        case 'product':  return 'Product'
        case 'blog':     return 'Article'
        case 'category':
        case 'landing':
        default:         return 'WebPage'
      }
    })(),
    ...(name        && { name }),
    ...(description && { description }),
    ...(url         && { url }),
  }

  let faqItems: Array<{ soru?: string; cevap?: string }> = []
  try {
    const parsed = typeof pkg?.faq === 'string' ? JSON.parse(pkg.faq as string) : pkg?.faq
    if (Array.isArray(parsed) && parsed.length > 0) faqItems = parsed
  } catch { /* ignore */ }

  if (faqItems.length > 0) {
    const faqSchema = {
      '@context': 'https://schema.org',
      '@type': 'FAQPage',
      mainEntity: faqItems.map((item) => ({
        '@type': 'Question',
        name: item.soru ?? '',
        acceptedAnswer: {
          '@type': 'Answer',
          text: item.cevap ?? '',
        },
      })),
    }
    return [baseSchema, faqSchema]
  }

  return baseSchema
}
```

**3. Component içinde yeni state'leri ekle — `const [faq, setFaq] = ...` satırının hemen ALTINA:**
```typescript
  // Schema (Phase 10)
  const [schemaJsonLd, setSchemaJsonLd] = useState(jsonString(pkg?.schema_jsonld))
  const [activeTab, setActiveTab] = useState<'paket' | 'schema'>('paket')
  const [copyStatus, setCopyStatus] = useState<'idle' | 'copied' | 'error'>('idle')
```

**4. handleGenerateSchema ve handleCopySchema handler'larını ekle — `handleSave` fonksiyonunun hemen ÜSTÜNE:**
```typescript
  function handleGenerateSchema() {
    const result = generateSchemaJsonLd(page)
    setSchemaJsonLd(JSON.stringify(result, null, 2))
  }

  async function handleCopySchema() {
    try {
      await navigator.clipboard.writeText(schemaJsonLd)
      setCopyStatus('copied')
      setTimeout(() => setCopyStatus('idle'), 2000)
    } catch {
      setCopyStatus('error')
      setTimeout(() => setCopyStatus('idle'), 2000)
    }
  }
```

**5. handleSave içinde updatePagePackage çağrısını güncelle — `faq: parseJsonField(faq),` satırının hemen ALTINA:**
```typescript
        schema_jsonld: parseJsonField(schemaJsonLd),
```

UYARILAR:
- `font-medium` KULLANMA — bu class projeye yasak
- Hiçbir mevcut satırı silme veya yeniden sıralama
  </action>
  <verify>
    <automated>grep -n "schema_jsonld\|activeTab\|copyStatus\|generateSchemaJsonLd\|handleGenerateSchema\|handleCopySchema" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx</automated>
  </verify>
  <done>
    - PageData.pkg'de "schema_jsonld: unknown" var
    - generateSchemaJsonLd fonksiyonu tanımlı
    - 3 yeni state (schemaJsonLd, activeTab, copyStatus) var
    - handleGenerateSchema ve handleCopySchema handler'ları var
    - handleSave'de "schema_jsonld: parseJsonField(schemaJsonLd)" satırı var
  </done>
</task>

<task type="auto">
  <name>Task 2: Tab bar ve Schema sekmesi UI ekle</name>
  <files>src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx (Task 1 sonrası güncel durum)
  </read_first>
  <action>
**6. Tab bar ekle — LockedBanner bloğunun hemen ALTINA, ilk `<section>` (Temel Bilgiler) başlamadan ÖNCE:**

```tsx
      {/* Tab navigasyonu — sadece pkg mevcut iken göster */}
      {pkg !== null && (
        <div className="flex gap-1 border-b border-border">
          <button
            onClick={() => setActiveTab('paket')}
            className={cn(
              'px-3 py-2 text-sm',
              activeTab === 'paket'
                ? 'border-b-2 border-foreground font-semibold'
                : 'text-muted-foreground'
            )}
          >
            Paket
          </button>
          <button
            onClick={() => setActiveTab('schema')}
            className={cn(
              'px-3 py-2 text-sm',
              activeTab === 'schema'
                ? 'border-b-2 border-foreground font-semibold'
                : 'text-muted-foreground'
            )}
          >
            Schema
          </button>
        </div>
      )}
```

**7. Mevcut tüm section'ları Paket sekmesi koşulu ile sar — tab bar'ın ALTINDAN, Kaydet bloğundan ÖNCE:**

Şu an 9 adet `<section>` var (Temel Bilgiler, Meta & URL, Heading Yapısı, Schema, İçerik Blokları, CTA Blokları, Görsel Planı, Secondary Keywords, FAQ). Bu section'ların tamamını aşağıdaki şekilde sar:

```tsx
      {/* Paket sekmesi içeriği */}
      {(pkg === null || activeTab === 'paket') && (
        <>
          {/* 1. Temel Bilgiler */}
          <section className="space-y-4">
            {/* ... mevcut içerik tamamen korunacak ... */}
          </section>
          {/* ... diğer tüm section'lar değişmeden ... */}
          {/* 9. FAQ */}
          <section className="space-y-4">
            {/* ... mevcut içerik tamamen korunacak ... */}
          </section>
        </>
      )}
```

NOT: `pkg === null` koşulu ekleniyor — pkg yokken tab bar görünmez ama form yine de gösterilmeli (yeni paket başlatma akışı).

**8. Schema sekmesi içeriğini ekle — Paket koşul bloğunun ALTINA, Kaydet bloğunun ÜSTÜNE:**

```tsx
      {/* Schema sekmesi içeriği */}
      {pkg !== null && activeTab === 'schema' && (
        <section className="space-y-4">
          <h3 className="text-sm font-semibold border-b border-border pb-2">JSON-LD Schema</h3>

          {!schemaJsonLd ? (
            /* Boş durum */
            <div className="space-y-3 py-4">
              <p className="text-sm text-muted-foreground">
                Bu sayfa için henüz schema üretilmedi.
              </p>
              <p className="text-xs text-muted-foreground">
                Sayfa tipine göre otomatik JSON-LD oluşturmak için Schema Üret butonuna tıkla.
              </p>
              <Button
                variant="outline"
                size="sm"
                onClick={handleGenerateSchema}
                disabled={isLocked}
              >
                Schema Üret
              </Button>
            </div>
          ) : (
            /* Dolu durum */
            <div className="space-y-3">
              <div className="flex items-center justify-between">
                <Label className="text-xs font-normal text-muted-foreground uppercase tracking-wide">
                  JSON-LD
                </Label>
                <Button
                  variant="ghost"
                  size="sm"
                  onClick={handleCopySchema}
                  className={cn(
                    copyStatus === 'copied' && 'text-emerald-500',
                    copyStatus === 'error' && 'text-red-400'
                  )}
                >
                  {copyStatus === 'copied'
                    ? 'Kopyalandı ✓'
                    : copyStatus === 'error'
                    ? 'Kopyalanamadı'
                    : 'Kopyala'}
                </Button>
              </div>
              <Field
                label=""
                id="schema_jsonld"
                value={schemaJsonLd}
                onChange={setSchemaJsonLd}
                multiline
                rows={12}
                mono
                placeholder="Schema Üret butonuna tıklayarak JSON-LD oluştur."
                disabled={isLocked}
              />
              <Button
                variant="outline"
                size="sm"
                onClick={handleGenerateSchema}
                disabled={isLocked}
              >
                Schema Üret
              </Button>
            </div>
          )}
        </section>
      )}
```

UYARILAR:
- `font-medium` KULLANMA (projeye yasak — aktif tab `font-semibold` kullanıyor, bu doğru)
- `variant` prop Badge bileşenine uygulanmıyor (bu planda Badge yok, sorun değil)
- `DialogTrigger asChild` yok (bu planda dialog yok, sorun değil)
- Mevcut 9 section'ın içeriğine dokunma — sadece sar
  </action>
  <verify>
    <automated>grep -n "activeTab\|Schema Üret\|Kopyala\|Kopyalandı\|schema_jsonld\|font-medium" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx</automated>
  </verify>
  <done>
    - "activeTab === 'paket'" ve "activeTab === 'schema'" koşulları var
    - "Schema Üret" copy var
    - "Kopyala" ve "Kopyalandı ✓" copy var
    - "schema_jsonld" Field id'si var
    - "font-medium" grep'i 0 sonuç döner (yasak class yok)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Schema sekmesi görsel ve işlevsel doğrulama</name>
  <what-built>
    - Paket / Schema tab bar (pkg mevcut iken)
    - Schema Üret butonu (page_type'a göre deterministik JSON-LD üretimi)
    - Mono textarea (rows=12, resize-y)
    - Kopyala butonu (ghost, emerald-500 feedback)
    - Kaydet akışı schema_jsonld'yi de gönderiyor
  </what-built>
  <how-to-verify>
    1. `npm run dev` başlat, tarayıcıda bir projenin Sayfa Paketi sayfasına git
    2. Soldaki listeden bir sayfa seç (package'ı olan bir sayfa)
    3. Editör üstünde "Paket" ve "Schema" sekmelerini gör — "Paket" varsayılan aktif olmalı
    4. "Schema" sekmesine tıkla → boş durum mesajı: "Bu sayfa için henüz schema üretilmedi." + "Schema Üret" butonu görünmeli
    5. "Schema Üret"e tıkla → textarea açılmalı, page_type'a göre JSON-LD gösterilmeli (geçerli JSON, @context ve @type içermeli)
    6. homepage tipli sayfa için: @type array olmalı ["Organization", "WebSite"]
    7. "Kopyala" butonuna tıkla → "Kopyalandı ✓" (yeşil) 2 sn görünmeli, sonra "Kopyala"ya dönmeli
    8. Textarea'da JSON'u düzenle, "Kaydet"e tıkla → "Kaydedildi." mesajı görünmeli
    9. Sayfayı yenile → düzenlenen schema textarea'da korunmalı
    10. "Paket" sekmesine dön → mevcut form alanları değişmeden olmalı
    11. Locked paket için: Schema Üret ve textarea disabled olmalı
    12. `npx tsc --noEmit` → 0 exit code
  </how-to-verify>
  <resume-signal>"approved" yaz veya karşılaştığın sorunları açıkla.</resume-signal>
  <done>
    Tüm doğrulama adımları geçti: tab navigasyon çalışıyor, schema üretimi doğru, kopyalama feedback'i çalışıyor, kaydetme persist ediyor, Paket sekmesi değişmemiş, locked state korunuyor.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client component → clipboard API | navigator.clipboard.writeText — tarayıcı izin gerektirebilir |
| client → server action | handleSave → updatePagePackage, schema_jsonld JSONB olarak yazılıyor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Tampering | generateSchemaJsonLd client-side | accept | Pure function, deterministik, user'ın kendi verisiyle çalışıyor; server validation updatePagePackage'de mevcut |
| T-10-03-02 | Information Disclosure | schema_jsonld client state | accept | State yalnızca authenticated user'ın kendi page package'ini içeriyor; başka kullanıcı verisi yok |
| T-10-03-03 | Spoofing | handleSave schema_jsonld payload | mitigate | parseJsonField() geçersiz JSON'u string olarak saklar, crash olmaz; updatePagePackage verifyOwnership() ile user_id doğrulaması yapıyor (T-09-01-01 kapsamında mevcut) |
| T-10-03-04 | Denial of Service | handleCopySchema clipboard | accept | Clipboard API başarısız olursa try/catch ile 'error' state, kullanıcıya mesaj gösterilir — uygulama çökmez |
| T-10-03-05 | Tampering | schema textarea free-form edit | accept | Kullanıcı kendi sayfasını düzenliyor; malicious JSON DB'ye JSONB olarak yazılır — XSS riski yok (React escaped rendering, server render'da kullanılmıyor) |
</threat_model>

<verification>
- `grep -c "schema_jsonld" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → ≥5 sonuç (type, state, handler, Field id, handleSave)
- `grep "font-medium" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → 0 sonuç
- `npx tsc --noEmit` → 0 exit code
- Checkpoint onayı: görsel ve işlevsel doğrulama geçti
</verification>

<success_criteria>
- PagePackageEditor'da çalışan tab navigasyon (Paket | Schema)
- generateSchemaJsonLd tüm page_type mapping'lerini doğru üretiyor
- Schema sekmesi boş/dolu durum geçişleri çalışıyor
- Kopyala feedback (emerald/red) çalışıyor
- Kaydet + sayfa yenileme sonrası schema korunuyor
- TypeScript derleme hatası yok
- font-medium class yok
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/10-schema-center/10-03-SUMMARY.md` oluştur.
</output>
