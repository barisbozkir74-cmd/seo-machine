---
phase: 10-schema-center
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260425000001_add_schema_jsonld.sql
autonomous: false
requirements:
  - PAGE-02

must_haves:
  truths:
    - "page_packages tablosunda schema_jsonld JSONB kolonu var"
    - "Mevcut satırlar etkilenmedi (schema_jsonld null, breaking change yok)"
  artifacts:
    - path: "supabase/migrations/20260425000001_add_schema_jsonld.sql"
      provides: "schema_jsonld kolonu migration dosyası"
      contains: "ADD COLUMN IF NOT EXISTS schema_jsonld JSONB"
  key_links:
    - from: "supabase/migrations/20260425000001_add_schema_jsonld.sql"
      to: "public.page_packages"
      via: "supabase db push"
      pattern: "schema_jsonld"
---

<objective>
`page_packages` tablosuna `schema_jsonld JSONB` kolonu ekler ve Supabase remote'a push eder.

Purpose: Phase 10 Schema sekmesinin JSON-LD verisi depolamak için schema_jsonld kolonuna ihtiyacı var. Bu migration olmadan hiçbir kaydetme işlemi çalışmaz.
Output: Migration dosyası + Supabase remote'da uygulanan sütun değişikliği.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-schema-center/10-CONTEXT.md
@.planning/phases/10-schema-center/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration dosyasını oluştur</name>
  <files>supabase/migrations/20260425000001_add_schema_jsonld.sql</files>
  <read_first>
    - supabase/migrations/20260424000003_competitors_seo_columns.sql (additive ALTER TABLE pattern — IF NOT EXISTS kullanımı)
    - supabase/migrations/20260424000005_create_page_packages.sql (mevcut tablo yapısı — sütun yerini anlamak için)
  </read_first>
  <action>
Aşağıdaki içeriği tam olarak yaz (başka bir şey ekleme):

```sql
-- Phase 10: Schema Center — schema_jsonld column
-- Adds JSONB column to page_packages for storing generated JSON-LD objects.
ALTER TABLE public.page_packages
  ADD COLUMN IF NOT EXISTS schema_jsonld JSONB;
```

Notlar (uygula):
- `IF NOT EXISTS` zorunlu — proje convention (tüm additive migration'larda var)
- `DEFAULT` gerekmiyor — null = "henüz üretilmedi" (Phase 10 empty state sinyali)
- INDEX gerekmiyor — page_id lookup ile erişiliyor, schema_jsonld üzerinde filtre/sort yok
- RLS değişikliği gerekmiyor — mevcut tablo-düzeyi politikalar tüm kolonları kapsar
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS schema_jsonld JSONB" supabase/migrations/20260425000001_add_schema_jsonld.sql</automated>
  </verify>
  <done>Dosya 3 satır içeriyor, "ADD COLUMN IF NOT EXISTS schema_jsonld JSONB" string'i tam olarak var.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] supabase db push — schema_jsonld kolonu remote'a uygula</name>
  <read_first>
    - supabase/migrations/20260425000001_add_schema_jsonld.sql (az önce oluşturuldu — push ediliyor)
  </read_first>
  <action>
Supabase remote'a migration'ı push et. Aşağıdaki komutu çalıştır:

```bash
supabase db push
```

Eğer interactive prompt gelirse ve TTY yoksa:
```bash
SUPABASE_ACCESS_TOKEN=<token> supabase db push
```

Komut başarılı çıkış kodu (0) döndürmeli ve migration dosyasını "Applied" veya "Migrating" olarak listelemeli.

Başarısız olursa:
- "already exists" hatası alırsan: migration daha önce uygulanmış, devam et
- Bağlantı hatası alırsan: `supabase status` ile local/remote durumu kontrol et
- Diğer hatalar: hata mesajını olduğu gibi bildir
  </action>
  <what-built>Task 1'de oluşturulan migration dosyası (20260425000001_add_schema_jsonld.sql)</what-built>
  <how-to-verify>
    1. `supabase db push` komutunu çalıştır
    2. Çıktıda "20260425000001_add_schema_jsonld" satırının "Applied" veya "Migrating" durumunda göründüğünü doğrula
    3. Supabase Studio'da (veya `supabase db diff` ile) page_packages tablosunda schema_jsonld kolonunun var olduğunu doğrula
  </how-to-verify>
  <resume-signal>Push başarılı olduktan sonra "approved" yaz veya karşılaştığın hata mesajını paylaş.</resume-signal>
  <done>Supabase remote page_packages tablosunda schema_jsonld JSONB kolonu mevcut, push komutu 0 ile çıktı.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → Supabase remote | Migration push işlemi authenticated Supabase CLI ile yapılır |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | Migration SQL | accept | Dosya git'te versiyonlanıyor; yalnızca IF NOT EXISTS — idempotent, tekrar çalıştırılabilir |
| T-10-01-02 | Information Disclosure | schema_jsonld kolonu | accept | Mevcut RLS politikaları tüm kolonları user_id bazında kısıtlıyor; yeni kolon özel policy gerektirmiyor |
| T-10-01-03 | Denial of Service | supabase db push | accept | Additive ALTER TABLE — production trafiğini kilitlemez; JSONB ekleme lightweight |
</threat_model>

<verification>
- `grep "schema_jsonld" supabase/migrations/20260425000001_add_schema_jsonld.sql` → "ADD COLUMN IF NOT EXISTS schema_jsonld JSONB" döner
- Supabase remote push başarılı (checkpoint onayı)
</verification>

<success_criteria>
- Migration dosyası doğru içerikle var
- supabase db push 0 exit code ile tamamlandı
- page_packages tablosunda schema_jsonld JSONB kolonu aktif
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/10-schema-center/10-01-SUMMARY.md` oluştur.
</output>
