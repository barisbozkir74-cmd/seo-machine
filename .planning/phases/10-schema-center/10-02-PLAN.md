---
phase: 10-schema-center
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01-PLAN.md
files_modified:
  - src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts
  - src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx
autonomous: true
requirements:
  - PAGE-02

must_haves:
  truths:
    - "updatePagePackage action schema_jsonld alanını kabul ediyor"
    - "page.tsx SELECT sorgusunda schema_jsonld kolonu var"
    - "PageData tipinde pkg.schema_jsonld alanı tanımlı"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts"
      provides: "PagePackageData tipinde schema_jsonld? unknown alanı"
      contains: "schema_jsonld"
    - path: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx"
      provides: "SELECT sorgusunda schema_jsonld kolonu"
      contains: "schema_jsonld"
  key_links:
    - from: "actions.ts PagePackageData type"
      to: "updatePagePackage upsert spread"
      via: "...data spread — schema_jsonld otomatik akıyor"
      pattern: "schema_jsonld.*unknown"
    - from: "page.tsx SELECT"
      to: "PagePackageEditor PageData prop"
      via: "pkg cast as PageData['pkg']"
      pattern: "schema_jsonld"
---

<objective>
`actions.ts` ve `page.tsx` dosyalarına minimal, additive değişiklikler yapar: tip tanımına `schema_jsonld` ekler ve SELECT sorgusunu genişletir.

Purpose: PagePackageEditor'ın Schema sekmesi `pkg.schema_jsonld` verisine ihtiyaç duyuyor. Bu plan veri akışını server-to-client olarak tamamlar: DB → SELECT → prop → component.
Output: actions.ts güncellenmiş tip, page.tsx genişletilmiş SELECT sorgusu.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-schema-center/10-CONTEXT.md
@.planning/phases/10-schema-center/10-PATTERNS.md

<interfaces>
<!-- actions.ts mevcut PagePackageData tipi (satır 12–39) -->
```typescript
export type PagePackageData = {
  // Temel Bilgiler
  page_type?: string
  strategic_purpose?: string
  search_intent?: string
  // Meta & URL
  slug?: string
  seo_title?: string
  meta_description?: string
  h1?: string
  canonical_url?: string
  // Heading
  heading_hierarchy?: unknown
  // Schema
  schema_type?: string
  // İçerik
  content_blocks?: unknown
  cta_blocks?: unknown
  // Görsel
  image_plan?: unknown
  alt_texts?: unknown
  // Keywords
  secondary_keywords?: unknown
  // FAQ
  faq?: unknown
  // QA
  qa_scores?: unknown
}
```

<!-- page.tsx SELECT sorgusu (satır 103–106) -->
```typescript
.select(
  'id, status, generated_by, seo_title, meta_description, h1, slug, search_intent, strategic_purpose, secondary_keywords, heading_hierarchy, content_blocks, cta_blocks, image_plan, alt_texts, schema_type, canonical_url, faq'
)
```

<!-- PagePackageEditor.tsx PageData tipi (satır 23–50) — plan 03'te güncellenecek -->
```typescript
export type PageData = {
  id: string
  title: string
  slug: string | null
  page_type: string | null
  focus_keyword_id: string | null
  focus_keyword_text?: string | null
  pkg?: {
    id: string
    status: string
    generated_by: string
    seo_title: string | null
    meta_description: string | null
    h1: string | null
    slug: string | null
    search_intent: string | null
    strategic_purpose: string | null
    secondary_keywords: unknown
    heading_hierarchy: unknown
    content_blocks: unknown
    cta_blocks: unknown
    image_plan: unknown
    alt_texts: unknown
    schema_type: string | null
    canonical_url: string | null
    faq: unknown
  } | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: actions.ts — PagePackageData tipine schema_jsonld ekle</name>
  <files>src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts (tüm dosya — mevcut durum)
  </read_first>
  <action>
`PagePackageData` tipinde `faq?: unknown` satırının hemen ALTINA, `qa_scores?: unknown` satırının ÜSTÜNE şu alanı ekle:

```typescript
  // Schema JSON-LD (Phase 10)
  schema_jsonld?: unknown
```

Sonuç blok şu şekilde görünmeli:
```typescript
  // FAQ
  faq?: unknown
  // Schema JSON-LD (Phase 10)
  schema_jsonld?: unknown
  // QA
  qa_scores?: unknown
```

Başka hiçbir satıra dokunma. `updatePagePackage` action'ı `...data` spread kullandığı için (satır 76–83) yeni alan otomatik olarak upsert'e dahil olur — action gövdesinde değişiklik gerekmez.
  </action>
  <verify>
    <automated>grep -n "schema_jsonld" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts</automated>
  </verify>
  <done>actions.ts'de "schema_jsonld?: unknown" satırı var, PagePackageData tipinin bir üyesi olarak görünüyor.</done>
</task>

<task type="auto">
  <name>Task 2: page.tsx — SELECT sorgusuna schema_jsonld ekle</name>
  <files>src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx (tüm dosya — özellikle satır 99–115 arası)
  </read_first>
  <action>
`page.tsx` dosyasında, seçili sayfanın `page_packages` verisini çeken `.select(...)` string'ini bul (mevcut hali: `'id, status, generated_by, ..., faq'`).

Bu string'i şu şekilde güncelle — `faq` sonrasına `, schema_jsonld` ekle:

```typescript
        .select(
          'id, status, generated_by, seo_title, meta_description, h1, slug, search_intent, strategic_purpose, secondary_keywords, heading_hierarchy, content_blocks, cta_blocks, image_plan, alt_texts, schema_type, canonical_url, faq, schema_jsonld'
        )
```

Sadece `faq` sonrasına `, schema_jsonld` ekleniyor. Başka hiçbir değişiklik yapma.

NOT: `PageData['pkg']` tipi plan 03'te `PagePackageEditor.tsx`'de güncellenecek. Bu dosyada tip değişikliği yok.
  </action>
  <verify>
    <automated>grep -n "schema_jsonld" src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx</automated>
  </verify>
  <done>page.tsx'de SELECT string'i "schema_jsonld" içeriyor, .select() çağrısında yer alıyor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server component → Supabase | Authenticated server-side query, user_id zaten doğrulanıyor |
| server component → client component | PageData prop — schema_jsonld client bundle'a aktarılıyor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Information Disclosure | page.tsx SELECT | accept | Mevcut ownership check (user.id + project.id) tüm kolonları kapsıyor; schema_jsonld için ek policy gerekmiyor |
| T-10-02-02 | Tampering | actions.ts updatePagePackage | accept | verifyOwnership() helper mevcut; schema_jsonld user'ın kendi paketi için yazılıyor; RLS UPDATE policy USING+WITH CHECK ile T-09-01-01 kapsamında |
| T-10-02-03 | Elevation of Privilege | PagePackageData schema_jsonld | accept | JSONB alanı sunucuya gönderilmeden önce parseJsonField() ile parse ediliyor (plan 03'te); ham string kabul edilmiyor |
</threat_model>

<verification>
- `grep "schema_jsonld" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` → "schema_jsonld?: unknown" döner
- `grep "schema_jsonld" src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx` → SELECT string'inde görünür
- `npx tsc --noEmit` 0 exit code ile tamamlanır (tip uyumluluğu)
</verification>

<success_criteria>
- actions.ts PagePackageData tipinde schema_jsonld?: unknown alanı var
- page.tsx SELECT sorgusunda schema_jsonld kolonu var
- TypeScript derleme hatası yok
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/10-schema-center/10-02-SUMMARY.md` oluştur.
</output>
