---
phase: 03-rules-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/switch.tsx
  - src/app/(dashboard)/ayarlar/kurallar/actions.ts
  - src/app/(dashboard)/ayarlar/kurallar/page.tsx
autonomous: true
requirements:
  - RULE-01
  - RULE-02

must_haves:
  truths:
    - "Kullanıcı /ayarlar/kurallar adresine gittiğinde 12 SEO kuralını 4 kategori altında görebilir"
    - "İlk açılışta kurallar otomatik seed edilir — boş sayfa hiçbir zaman görünmez"
    - "Her kural satırında kural etiketi, önerilen değer ipucu ve toggle switch görünür"
    - "Toggle tıklanınca global kural anında Supabase'e yazılır, sayfa yenilenir"
  artifacts:
    - path: "src/components/ui/switch.tsx"
      provides: "shadcn Switch bileşeni"
    - path: "src/app/(dashboard)/ayarlar/kurallar/actions.ts"
      provides: "seedGlobalRules + toggleRule Server Actions"
      exports: ["seedGlobalRules", "toggleRule", "ToggleRuleResult"]
    - path: "src/app/(dashboard)/ayarlar/kurallar/page.tsx"
      provides: "Global kurallar sayfası (Server Component)"
  key_links:
    - from: "src/app/(dashboard)/ayarlar/kurallar/page.tsx"
      to: "src/app/(dashboard)/ayarlar/kurallar/actions.ts"
      via: "import seedGlobalRules, toggleRule"
      pattern: "seedGlobalRules|toggleRule"
    - from: "src/app/(dashboard)/ayarlar/kurallar/actions.ts"
      to: "rules table"
      via: "supabase.from('rules').upsert / .update"
      pattern: "supabase\\.from\\('rules'\\)"
---

<objective>
shadcn Switch bileşenini yükle, global kurallar için Server Action'ları yaz ve /ayarlar/kurallar sayfasını oluştur. Sayfa ilk açılışta otomatik seed yapar; her kural satırında toggle, etiket ve önerilen değer gösterilir.

Purpose: RULE-01 ve RULE-02'nin global tarafı — 12 boolean kuralın tanımlanması ve inline güncellenmesi.
Output: switch.tsx bileşeni, kurallar/actions.ts (seedGlobalRules + toggleRule), kurallar/page.tsx
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-rules-engine/03-CONTEXT.md
@.planning/phases/03-rules-engine/03-UI-SPEC.md
@.planning/phases/03-rules-engine/03-PATTERNS.md

<interfaces>
<!-- Mevcut patterns — executor dosyaları okumadan kullanabilir -->

src/app/(dashboard)/projeler/[id]/actions.ts — Server Action şablonu:
```typescript
'use server'
import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'

// Auth guard pattern:
const { data: { user } } = await supabase.auth.getUser()
if (!user) return { success: false, error: 'Oturum bulunamadı.' }

// Return type pattern:
export type XResult = { success: true } | { success: false; error: string }
```

src/app/(dashboard)/projeler/page.tsx — Badge className (variant prop YOK):
```tsx
<Badge className="bg-slate-500/20 text-slate-400 border border-slate-500/30">Global</Badge>
```

Supabase client:
```typescript
import { createClient } from '@/lib/supabase/server'
const supabase = await createClient()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch bileşeni kur + seedGlobalRules ve toggleRule Server Actions yaz</name>
  <files>src/components/ui/switch.tsx, src/app/(dashboard)/ayarlar/kurallar/actions.ts</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/actions.ts (auth guard ve return type pattern)
    - src/components/ui/badge.tsx (mevcut bileşen pattern kontrolü)
    - package.json (mevcut bağımlılıklar)
  </read_first>
  <action>
**Adım 1 — Switch bileşeni:**
```bash
npx shadcn add switch
```
Bu komut `src/components/ui/switch.tsx` dosyasını oluşturur.

**Adım 2 — `src/app/(dashboard)/ayarlar/kurallar/actions.ts` oluştur:**

```typescript
'use server'

import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'

export type ToggleRuleResult =
  | { success: true }
  | { success: false; error: string }

// D-01 kural seed verisi — 12 boolean kural, 4 kategori
const GLOBAL_RULES_SEED = [
  // SEO Title
  { rule_key: 'title_starts_with_keyword', rule_value: 'true' },
  { rule_key: 'title_max_length_enforced', rule_value: 'true' },
  { rule_key: 'title_includes_brand', rule_value: 'false' },
  // H1
  { rule_key: 'h1_exact_match', rule_value: 'false' },
  { rule_key: 'h1_single_per_page', rule_value: 'true' },
  { rule_key: 'h1_includes_keyword', rule_value: 'true' },
  // Slug
  { rule_key: 'slug_exact_match', rule_value: 'false' },
  { rule_key: 'slug_lowercase_hyphen', rule_value: 'true' },
  { rule_key: 'slug_no_stopwords', rule_value: 'false' },
  // Meta Description
  { rule_key: 'meta_desc_required', rule_value: 'true' },
  { rule_key: 'meta_desc_includes_keyword', rule_value: 'true' },
  { rule_key: 'meta_desc_length_enforced', rule_value: 'true' },
] as const

export async function seedGlobalRules(): Promise<void> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return

  await supabase.from('rules').upsert(
    GLOBAL_RULES_SEED.map((rule) => ({
      user_id: user.id,
      project_id: null,
      scope: 'global' as const,
      rule_key: rule.rule_key,
      rule_value: rule.rule_value,
    })),
    { onConflict: 'user_id,rule_key,scope' }
  )

  revalidatePath('/ayarlar/kurallar')
}

export async function toggleRule(
  ruleKey: string,
  newValue: boolean
): Promise<ToggleRuleResult> {
  const supabase = await createClient()

  const { data: { user } } = await supabase.auth.getUser()
  if (!user) {
    return { success: false, error: 'Oturum bulunamadı.' }
  }

  const { error } = await supabase
    .from('rules')
    .update({ rule_value: String(newValue) })
    .eq('rule_key', ruleKey)
    .eq('user_id', user.id)
    .eq('scope', 'global')
    .is('project_id', null)

  if (error) {
    return { success: false, error: 'Kural güncellenemedi. Lütfen tekrar deneyin.' }
  }

  revalidatePath('/ayarlar/kurallar')
  return { success: true }
}
```

NOT: `upsert` için `onConflict: 'user_id,rule_key,scope'` kullanılıyor. Eğer rules tablosunda bu unique constraint yoksa Supabase hatası alınır. Supabase migration'ı şu an doğrulanamıyor; executor bu unique constraint'in Phase 1'de eklendiğini varsaymalı. Eklenmediyse `upsert` yerine önce SELECT + koşullu INSERT kullan.
  </action>
  <verify>
    <automated>ls C:/Users/baris/Documents/seo-machine/src/components/ui/switch.tsx && grep -n "seedGlobalRules\|toggleRule\|GLOBAL_RULES_SEED" C:/Users/baris/Documents/seo-machine/src/app/(dashboard)/ayarlar/kurallar/actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/switch.tsx dosyası mevcut
    - actions.ts içinde `export async function seedGlobalRules` satırı var
    - actions.ts içinde `export async function toggleRule` satırı var
    - actions.ts içinde `GLOBAL_RULES_SEED` array'i 12 eleman içeriyor
    - actions.ts içinde `onConflict: 'user_id,rule_key,scope'` metni var
    - actions.ts içinde `revalidatePath('/ayarlar/kurallar')` satırı var
    - actions.ts içinde `'use server'` direktifi dosyanın ilk satırında var
  </acceptance_criteria>
  <done>Switch bileşeni kuruldu, seedGlobalRules ve toggleRule actions yazıldı.</done>
</task>

<task type="auto">
  <name>Task 2: Global kurallar sayfası oluştur (/ayarlar/kurallar/page.tsx)</name>
  <files>src/app/(dashboard)/ayarlar/kurallar/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/projeler/page.tsx (sayfa yapısı ve tablo pattern'ı)
    - src/app/(dashboard)/ayarlar/kurallar/actions.ts (az önce oluşturuldu — seedGlobalRules import)
    - src/components/ui/switch.tsx (az önce kuruldu — import path kontrolü)
    - src/components/ui/table.tsx (mevcut table bileşeni import path)
    - .planning/phases/03-rules-engine/03-UI-SPEC.md (tablo anatomy ve copywriting)
  </read_first>
  <action>
`src/app/(dashboard)/ayarlar/kurallar/page.tsx` dosyasını oluştur. Bu bir Server Component — interaktivite (toggle) için `RuleToggleRow` Client Component import edilecek (Plan 03-02'de oluşturulacak). Şimdilik placeholder kullan ve Plan 03-02 tamamlandıktan sonra wiring yapılacak.

**Kural metadata sabiti** (her kural için etiket, önerilen değer ve kategori):
```typescript
const RULE_META: Record<string, { label: string; recommended: boolean; category: string }> = {
  title_starts_with_keyword: { label: 'SEO title focus keyword ile başlamalı', recommended: true, category: 'SEO Title' },
  title_max_length_enforced: { label: 'Max 60 karakter sınırı zorunlu', recommended: true, category: 'SEO Title' },
  title_includes_brand:      { label: 'SEO title marka adıyla bitmeli', recommended: false, category: 'SEO Title' },
  h1_exact_match:            { label: 'H1 focus keyword exact match olmalı', recommended: false, category: 'H1' },
  h1_single_per_page:        { label: 'Sayfada yalnızca 1 adet H1 kullanılmalı', recommended: true, category: 'H1' },
  h1_includes_keyword:       { label: 'H1 focus keyword içermeli', recommended: true, category: 'H1' },
  slug_exact_match:          { label: 'Slug focus keyword exact match olmalı', recommended: false, category: 'Slug' },
  slug_lowercase_hyphen:     { label: 'Slug küçük harf + tire zorunlu, noktalama yasak', recommended: true, category: 'Slug' },
  slug_no_stopwords:         { label: "Slug'dan Türkçe stopword çıkarılmalı", recommended: false, category: 'Slug' },
  meta_desc_required:        { label: 'Meta description zorunlu', recommended: true, category: 'Meta Description' },
  meta_desc_includes_keyword:{ label: 'Meta desc focus keyword içermeli', recommended: true, category: 'Meta Description' },
  meta_desc_length_enforced: { label: '120–160 karakter aralığı zorunlu', recommended: true, category: 'Meta Description' },
}

const CATEGORIES = ['SEO Title', 'H1', 'Slug', 'Meta Description'] as const
```

**Sayfa yapısı:**
```typescript
import { notFound } from 'next/navigation'
import Link from 'next/link'
import { createClient } from '@/lib/supabase/server'
import {
  Table, TableBody, TableCell, TableHead, TableHeader, TableRow,
} from '@/components/ui/table'
import { seedGlobalRules } from './actions'
// RuleToggleRow Plan 03-02'de eklenecek — şimdi sadece tablo satırını statik render et

export default async function GlobalKurallarPage() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) notFound()

  // Global kuralları çek
  let { data: rules } = await supabase
    .from('rules')
    .select('id, rule_key, rule_value')
    .eq('user_id', user.id)
    .eq('scope', 'global')
    .is('project_id', null)

  // Seed guard: kurallar yoksa seed et ve yeniden çek
  if (!rules || rules.length === 0) {
    await seedGlobalRules()
    const { data: seeded } = await supabase
      .from('rules')
      .select('id, rule_key, rule_value')
      .eq('user_id', user.id)
      .eq('scope', 'global')
      .is('project_id', null)
    rules = seeded ?? []
  }

  // rule_key → rule_value map
  const ruleValues = Object.fromEntries(
    (rules ?? []).map((r) => [r.rule_key, r.rule_value === 'true'])
  )

  return (
    <main className="p-8">
      <Link
        href="/projeler"
        className="text-sm text-muted-foreground hover:text-foreground flex items-center gap-1 mb-4"
      >
        ← Dashboard
      </Link>
      <h1 className="text-xl font-semibold">Global SEO Kuralları</h1>
      <p className="text-sm text-muted-foreground mt-1 mb-6">
        Tüm projeler için varsayılan kural seti. Proje bazlı override yapılmayan kurallar bu değerleri kullanır.
      </p>

      <div className="space-y-8">
        {CATEGORIES.map((category) => {
          const categoryRules = Object.entries(RULE_META).filter(
            ([, meta]) => meta.category === category
          )
          return (
            <div key={category}>
              <p className="text-xs font-normal uppercase text-muted-foreground mb-3">
                {category}
              </p>
              <Table>
                <TableHeader>
                  <TableRow>
                    <TableHead className="text-xs font-normal uppercase text-muted-foreground">
                      Kural
                    </TableHead>
                    <TableHead className="text-xs font-normal uppercase text-muted-foreground w-48">
                      Önerilen
                    </TableHead>
                    <TableHead className="text-xs font-normal uppercase text-muted-foreground w-16">
                      Durum
                    </TableHead>
                  </TableRow>
                </TableHeader>
                <TableBody>
                  {categoryRules.map(([ruleKey, meta]) => {
                    const currentValue = ruleValues[ruleKey] ?? meta.recommended
                    const matchesRecommended = currentValue === meta.recommended
                    return (
                      <TableRow key={ruleKey}>
                        <TableCell className="text-sm">{meta.label}</TableCell>
                        <TableCell>
                          <span className={matchesRecommended ? 'text-xs text-muted-foreground' : 'text-xs text-amber-400'}>
                            Önerilen: {meta.recommended ? 'Açık' : 'Kapalı'}
                          </span>
                        </TableCell>
                        <TableCell>
                          {/* Plan 03-02'de RuleToggleRow ile değiştirilecek */}
                          <span className="text-xs text-muted-foreground">
                            {currentValue ? 'Açık' : 'Kapalı'}
                          </span>
                        </TableCell>
                      </TableRow>
                    )
                  })}
                </TableBody>
              </Table>
            </div>
          )
        })}
      </div>
    </main>
  )
}
```

NOT: Toggle interaktivitesi (RuleToggleRow) Plan 03-02'de eklenecek. Şimdi statik değer gösterimi yeterli — sayfa render edilebilir ve seed logic çalışıyor olmalı.
  </action>
  <verify>
    <automated>grep -n "seedGlobalRules\|GlobalKurallarPage\|CATEGORIES\|RULE_META\|seed guard" C:/Users/baris/Documents/seo-machine/src/app/(dashboard)/ayarlar/kurallar/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx içinde `export default async function GlobalKurallarPage` satırı var
    - page.tsx içinde `RULE_META` sabiti 12 kural içeriyor (`Object.keys(RULE_META).length === 12` kontrolü: `meta_desc_length_enforced` dahil tüm rule_key'ler mevcut)
    - page.tsx içinde `CATEGORIES` array'i 4 eleman içeriyor: 'SEO Title', 'H1', 'Slug', 'Meta Description'
    - page.tsx içinde seed guard logic var: `if (!rules || rules.length === 0)` pattern'ı
    - page.tsx içinde `await seedGlobalRules()` çağrısı var
    - page.tsx içinde `text-amber-400` class var (sapma uyarısı rengi)
    - page.tsx içinde `← Dashboard` breadcrumb linki var
    - page.tsx içinde `Global SEO Kuralları` h1 başlığı var
    - `npx tsc --noEmit` 0 hatayla çıkıyor (yeni dosyalar için)
  </acceptance_criteria>
  <done>Global kurallar sayfası oluşturuldu. Seed logic, tablo yapısı ve önerilen değer gösterimi çalışıyor. Toggle interaktivitesi Plan 03-02'de eklenecek.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Server Action | toggleRule ve seedGlobalRules'a gelen ruleKey ve newValue parametreleri güvenilmez |
| Server Action→Supabase | rules tablosu RLS ile korunuyor — user_id eşleşmesi zorunlu |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Spoofing | toggleRule action | mitigate | `supabase.auth.getUser()` her action'da çağrılıyor; JWT doğrulaması Supabase tarafında yapılıyor |
| T-03-01-02 | Tampering | toggleRule action — ruleKey parametresi | mitigate | Supabase `.eq('user_id', user.id)` filtresi başka kullanıcının kuralını güncellemeyi engeller; RLS WITH CHECK ikinci savunma katmanı |
| T-03-01-03 | Tampering | seedGlobalRules — project_id=null zorunluluğu | mitigate | upsert scope='global' ve project_id=null ile sabitleniyor; kullanıcı input ile proje bazlı kural oluşturamaz |
| T-03-01-04 | Elevation of Privilege | Global kurallar sayfası — auth guard | mitigate | `if (!user) notFound()` — oturumsuz erişim 404 döner |
| T-03-01-05 | Information Disclosure | ruleKey string validation | accept | ruleKey enum listesi dışına çıksa bile Supabase `.eq('rule_key', ruleKey)` eşleşme bulamaz, sadece 0 satır güncellenir; bilgi sızıntısı riski düşük |
</threat_model>

<verification>
1. `/ayarlar/kurallar` sayfası ilk açılışta 12 kuralı 4 kategori altında gösteriyor
2. İlk açılışta rules tablosuna 12 global kural insert ediliyor (seed guard çalışıyor)
3. Sayfa render'ı TypeScript hatasız tamamlanıyor (`npx tsc --noEmit`)
4. seedGlobalRules action tekrar çağrıldığında duplicate kayıt oluşturmuyor (upsert idempotent)
5. toggleRule başarı durumunda `{ success: true }` dönüyor
</verification>

<success_criteria>
- Switch bileşeni `src/components/ui/switch.tsx` olarak mevcut
- `seedGlobalRules` ve `toggleRule` actions TypeScript hatasız export ediliyor
- `/ayarlar/kurallar` sayfası 12 kuralı 4 kategori altında render ediyor
- Seed guard: rules yoksa otomatik seed, sayfa hiçbir zaman boş görünmüyor
- Önerilen değerden sapan kurallar `text-amber-400` ile işaretleniyor
</success_criteria>

<output>
Tamamlandığında `.planning/phases/03-rules-engine/03-01-SUMMARY.md` oluştur.
</output>
