---
phase: 03-rules-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/app/(dashboard)/projeler/[id]/kurallar/page.tsx
  - src/app/(dashboard)/layout.tsx
  - src/app/(dashboard)/projeler/[id]/page.tsx
autonomous: true
requirements:
  - RULE-01
  - RULE-02
  - RULE-03

must_haves:
  truths:
    - "Kullanıcı dashboard'da üst navda 'Ayarlar' linkini görebilir ve /ayarlar/kurallar sayfasına ulaşabilir"
    - "Proje detay sayfasında sol sütunun altında 'Proje Kuralları' linki görünür"
    - "/projeler/[id]/kurallar sayfası 12 kuralı proje + global override birleştirmesiyle gösterir"
    - "Global değer taşıyan kurallar [Global] badge, override olan kurallar [Proje] ✕ badge gösterir"
    - "Proje override toggle değiştirilince toggleProjectRule çağrılır"
    - "✕ tıklanınca resetProjectRule çağrılır ve kural [Global]'e döner"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/kurallar/page.tsx"
      provides: "Proje kuralları sayfası — global + project override birleştirmesi"
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Üst nav: Projeler + Ayarlar linkleri"
    - path: "src/app/(dashboard)/projeler/[id]/page.tsx"
      provides: "Sol sütun: Separator + Proje Kuralları linki eklendi"
  key_links:
    - from: "src/app/(dashboard)/projeler/[id]/kurallar/page.tsx"
      to: "src/app/(dashboard)/projeler/[id]/kurallar/actions.ts"
      via: "toggleProjectRule.bind(null, id) ve resetProjectRule.bind(null, id, ruleKey)"
      pattern: "toggleProjectRule|resetProjectRule"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "/ayarlar/kurallar"
      via: "Link href=/ayarlar/kurallar"
      pattern: "ayarlar/kurallar"
    - from: "src/app/(dashboard)/projeler/[id]/page.tsx"
      to: "/projeler/[id]/kurallar"
      via: "Link href=`/projeler/${id}/kurallar`"
      pattern: "kurallar"
---

<objective>
Proje bazlı kural override sayfasını oluştur, dashboard layout'a üst nav ekle ve proje detay sol sütununa "Proje Kuralları" linki ekle. Bu plan Phase 3'ün son parçası — kullanıcı navigasyonu ve proje scope kural yönetimini tamamlar.

Purpose: RULE-01 + RULE-02 + RULE-03'ün proje scope tarafı. Global/Proje inheritance görünümü ve nav bağlantıları.
Output: kurallar/page.tsx (proje), layout.tsx güncellemesi, projeler/[id]/page.tsx güncellemesi
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-rules-engine/03-CONTEXT.md
@.planning/phases/03-rules-engine/03-UI-SPEC.md
@.planning/phases/03-rules-engine/03-PATTERNS.md
@.planning/phases/03-rules-engine/03-01-SUMMARY.md
@.planning/phases/03-rules-engine/03-02-SUMMARY.md

<interfaces>
<!-- Plan 03-01 ve 03-02'de oluşturulan interfaces -->

src/app/(dashboard)/projeler/[id]/kurallar/actions.ts:
```typescript
export type ToggleProjectRuleResult = { success: true } | { success: false; error: string }
export async function toggleProjectRule(projectId: string, ruleKey: string, newValue: boolean): Promise<ToggleProjectRuleResult>
export async function resetProjectRule(projectId: string, ruleKey: string): Promise<ToggleProjectRuleResult>
```

src/components/rules/RuleToggleRow.tsx:
```typescript
interface RuleToggleRowProps {
  ruleKey: string
  label: string
  currentValue: boolean
  recommendedValue: boolean
  scope: 'global' | 'project'
  toggleAction: (ruleKey: string, newValue: boolean) => Promise<{ success: boolean; error?: string }>
  resetAction?: () => Promise<{ success: boolean; error?: string }>
}
export function RuleToggleRow(props: RuleToggleRowProps): JSX.Element
```

src/app/(dashboard)/layout.tsx — mevcut (tam dosya):
```typescript
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
export default async function DashboardLayout({ children }) {
  // auth check + redirect('/login')
  return (
    <div className="min-h-screen bg-background">
      {children}
    </div>
  )
}
```

src/app/(dashboard)/projeler/[id]/page.tsx — sol sütun sonu (satır 99–167):
```tsx
<div className="w-64 shrink-0 border-r border-border overflow-y-auto p-4">
  <p className="text-xs font-normal uppercase text-muted-foreground mb-3">Aşamalar</p>
  <ul className="space-y-1">
    {stageList.map((stage) => ( ... ))}
  </ul>
  {/* BURAYA EKLENECEK: Separator + Proje Kuralları linki */}
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proje kuralları sayfası oluştur (/projeler/[id]/kurallar/page.tsx)</name>
  <files>src/app/(dashboard)/projeler/[id]/kurallar/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/page.tsx (2 sütunlu layout ve auth pattern — tam dosya)
    - src/app/(dashboard)/projeler/[id]/kurallar/actions.ts (toggleProjectRule + resetProjectRule)
    - src/components/rules/RuleToggleRow.tsx (props interface)
    - src/app/(dashboard)/ayarlar/kurallar/page.tsx (RULE_META ve CATEGORIES sabitleri — aynısını kullanacak veya kopyalayacak)
    - .planning/phases/03-rules-engine/03-UI-SPEC.md (Sayfa 2 yapısı ve tablo anatomy)
  </read_first>
  <action>
`src/app/(dashboard)/projeler/[id]/kurallar/page.tsx` oluştur. Bu sayfa 2 sütunlu layout kullanır (sol: stage listesi + link, sağ: kural tablosu).

**Veri çekme stratejisi:**
- `projects` tablosundan proje adı ve domain çek (ownership check dahil)
- `stages` tablosundan stage listesi çek (sol sütun için)
- Global kuralları çek (`scope='global', project_id=null`)
- Proje override'larını çek (`scope='project', project_id=id`)
- Birleştir: her rule_key için proje override varsa scope='project', yoksa scope='global' döner

**RULE_META ve CATEGORIES** sabitlerini `src/app/(dashboard)/ayarlar/kurallar/page.tsx` dosyasından kopyala veya ayrı bir `src/lib/rules/rule-meta.ts` dosyasına çıkarıp her iki sayfada import et. Executor tercihini kullanabilir — plan her iki yaklaşımı kabul eder, ancak duplicate kod varsa `rule-meta.ts` çıkarmayı tercih et.

```typescript
import { notFound } from 'next/navigation'
import Link from 'next/link'
import { createClient } from '@/lib/supabase/server'
import { Badge } from '@/components/ui/badge'
import { Separator } from '@/components/ui/separator'
import { cn } from '@/lib/utils'
import {
  Table, TableBody, TableHead, TableHeader, TableRow,
} from '@/components/ui/table'
import { HugeiconsIcon } from '@hugeicons/react'
import { Tick02Icon, RadioButtonIcon, CircleIcon } from '@hugeicons/core-free-icons'
import { RuleToggleRow } from '@/components/rules/RuleToggleRow'
import { toggleProjectRule, resetProjectRule } from './actions'
import { RULE_META, CATEGORIES } from '@/lib/rules/rule-meta' // veya inline sabit

type Stage = {
  id: string
  stage_name: string
  status: 'active' | 'completed' | 'pending'
}

export default async function ProjeKurallarPage({
  params,
}: {
  params: Promise<{ id: string }>
}) {
  const { id } = await params
  const supabase = await createClient()

  const { data: { user } } = await supabase.auth.getUser()
  if (!user) notFound()

  const { data: project } = await supabase
    .from('projects')
    .select('id, name, domain')
    .eq('id', id)
    .eq('user_id', user.id)
    .single()

  if (!project) notFound()

  const { data: stages } = await supabase
    .from('stages')
    .select('id, stage_name, status')
    .eq('project_id', id)
    .eq('user_id', user.id)
    .order('created_at', { ascending: true })

  const stageList = (stages ?? []) as Stage[]

  // Global kurallar
  const { data: globalRules } = await supabase
    .from('rules')
    .select('rule_key, rule_value')
    .eq('user_id', user.id)
    .eq('scope', 'global')
    .is('project_id', null)

  // Proje override'ları
  const { data: projectRules } = await supabase
    .from('rules')
    .select('rule_key, rule_value')
    .eq('user_id', user.id)
    .eq('project_id', id)
    .eq('scope', 'project')

  // Proje override map: rule_key → rule_value
  const projectOverrides = Object.fromEntries(
    (projectRules ?? []).map((r) => [r.rule_key, r.rule_value])
  )

  // Global map: rule_key → rule_value
  const globalValues = Object.fromEntries(
    (globalRules ?? []).map((r) => [r.rule_key, r.rule_value])
  )

  // Her kural için: proje override varsa scope='project', yoksa scope='global'
  const resolvedRules = Object.fromEntries(
    Object.keys(RULE_META).map((ruleKey) => {
      const hasOverride = ruleKey in projectOverrides
      const value = hasOverride
        ? projectOverrides[ruleKey] === 'true'
        : (globalValues[ruleKey] ?? 'true') === 'true'
      return [ruleKey, { value, scope: hasOverride ? 'project' : 'global' }]
    })
  )

  return (
    <div className="flex flex-col h-screen">
      {/* Breadcrumb + başlık */}
      <div className="p-8 pb-4">
        <Link
          href={`/projeler/${id}`}
          className="text-sm text-muted-foreground hover:text-foreground flex items-center gap-1 mb-4"
        >
          ← {project.name}
        </Link>
        <h1 className="text-xl font-semibold">Proje Kuralları</h1>
        <p className="text-sm text-muted-foreground mt-1">
          Bu projeye özel kural override'ları. Override yapılmayan kurallar global ayarları kullanır.
        </p>
      </div>

      {/* 2 sütunlu içerik */}
      <div className="flex flex-1 min-h-0">
        {/* Sol sütun — stage listesi + Proje Kuralları aktif link */}
        <div className="w-64 shrink-0 border-r border-border overflow-y-auto p-4">
          <p className="text-xs font-normal uppercase text-muted-foreground mb-3">
            Aşamalar
          </p>
          <ul className="space-y-1">
            {stageList.map((stage) => (
              <li
                key={stage.id}
                className={cn(
                  'flex items-center justify-between py-3 px-3 rounded-md',
                  stage.status === 'active' && 'border-l-2 border-blue-500 bg-blue-500/5'
                )}
              >
                <div className="flex items-center gap-2 min-w-0">
                  {stage.status === 'completed' && (
                    <HugeiconsIcon icon={Tick02Icon} size={14} className="text-emerald-400 shrink-0" />
                  )}
                  {stage.status === 'active' && (
                    <HugeiconsIcon icon={RadioButtonIcon} size={14} className="text-blue-400 shrink-0" />
                  )}
                  {stage.status === 'pending' && (
                    <HugeiconsIcon icon={CircleIcon} size={14} className="text-muted-foreground shrink-0" />
                  )}
                  <span
                    className={cn(
                      'text-sm truncate',
                      stage.status === 'active' && 'font-semibold text-foreground',
                      stage.status === 'completed' && 'text-emerald-400',
                      stage.status === 'pending' && 'text-muted-foreground'
                    )}
                  >
                    {stage.stage_name}
                  </span>
                </div>
              </li>
            ))}
          </ul>
          <Separator className="my-4" />
          <Link
            href={`/projeler/${id}/kurallar`}
            className="text-sm text-foreground font-semibold flex items-center gap-1 px-3 py-2 rounded-md bg-secondary"
          >
            Proje Kuralları
          </Link>
        </div>

        {/* Sağ sütun — kural tablosu */}
        <div className="flex-1 min-w-0 overflow-y-auto p-8">
          <div className="space-y-8">
            {CATEGORIES.map((category) => {
              const categoryRules = Object.entries(RULE_META).filter(
                ([, meta]) => meta.category === category
              )
              return (
                <div key={category}>
                  <p className="text-xs font-normal uppercase text-muted-foreground mb-3">
                    {category}
                  </p>
                  <Table>
                    <TableHeader>
                      <TableRow>
                        <TableHead className="text-xs font-normal uppercase text-muted-foreground w-24">
                          Kaynak
                        </TableHead>
                        <TableHead className="text-xs font-normal uppercase text-muted-foreground">
                          Kural
                        </TableHead>
                        <TableHead className="text-xs font-normal uppercase text-muted-foreground w-48">
                          Önerilen
                        </TableHead>
                        <TableHead className="text-xs font-normal uppercase text-muted-foreground w-16">
                          Durum
                        </TableHead>
                      </TableRow>
                    </TableHeader>
                    <TableBody>
                      {categoryRules.map(([ruleKey, meta]) => {
                        const resolved = resolvedRules[ruleKey]
                        const isProjectScope = resolved.scope === 'project'
                        return (
                          <TableRow key={ruleKey}>
                            <RuleToggleRow
                              ruleKey={ruleKey}
                              label={meta.label}
                              currentValue={resolved.value}
                              recommendedValue={meta.recommended}
                              scope={resolved.scope}
                              toggleAction={toggleProjectRule.bind(null, id)}
                              resetAction={
                                isProjectScope
                                  ? resetProjectRule.bind(null, id, ruleKey)
                                  : undefined
                              }
                            />
                          </TableRow>
                        )
                      })}
                    </TableBody>
                  </Table>
                </div>
              )
            })}
          </div>
        </div>
      </div>
    </div>
  )
}
```

NOT: `toggleProjectRule.bind(null, id)` — Server Action'ı bind ile partial application yapar. Bu Next.js Server Actions ile geçerli bir pattern — `bind` ile client'a geçirildiğinde first argument sabitlenir.
  </action>
  <verify>
    <automated>grep -n "ProjeKurallarPage\|toggleProjectRule\|resetProjectRule\|resolvedRules\|projectOverrides" C:/Users/baris/Documents/seo-machine/src/app/(dashboard)/projeler/[id]/kurallar/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx içinde `export default async function ProjeKurallarPage` satırı var
    - page.tsx içinde `resolvedRules` objesi var ve `projectOverrides` ile `globalValues` birleştirmesi yapıyor
    - page.tsx içinde `scope: hasOverride ? 'project' : 'global'` logic var
    - page.tsx içinde `RuleToggleRow` kullanımı var
    - page.tsx içinde `toggleProjectRule.bind(null, id)` var
    - page.tsx içinde `resetProjectRule.bind(null, id, ruleKey)` var
    - page.tsx içinde `isProjectScope` kontrolü var (resetAction sadece project scope'a geçiliyor)
    - page.tsx içinde Separator + "Proje Kuralları" linki sol sütunda var
    - page.tsx içinde `← {project.name}` breadcrumb var
    - `npx tsc --noEmit` 0 hatayla çıkıyor
  </acceptance_criteria>
  <done>Proje kuralları sayfası global+proje override birleştirmesiyle oluşturuldu.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard layout üst nav ekle + proje detay sol sütununa Proje Kuralları linki ekle</name>
  <files>src/app/(dashboard)/layout.tsx, src/app/(dashboard)/projeler/[id]/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/layout.tsx (mevcut tam dosya — nav eklenmeden önce)
    - src/app/(dashboard)/projeler/[id]/page.tsx (mevcut tam dosya — sol sütun sonu bulunacak, satır 99–167 arası)
    - .planning/phases/03-rules-engine/03-PATTERNS.md (layout nav pattern ve sol sütun ekleme pattern)
  </read_first>
  <action>
**Adım 1 — `src/app/(dashboard)/layout.tsx` güncelle:**

Mevcut `return` bloğunu değiştir. `import Link from 'next/link'` ekle.

```typescript
import { redirect } from 'next/navigation'
import Link from 'next/link'
import { createClient } from '@/lib/supabase/server'

export default async function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()

  if (!user) {
    redirect('/login')
  }

  return (
    <div className="min-h-screen bg-background">
      <nav className="border-b border-border px-8 py-3 flex items-center gap-6">
        <Link
          href="/projeler"
          className="text-sm text-muted-foreground hover:text-foreground"
        >
          Projeler
        </Link>
        <Link
          href="/ayarlar/kurallar"
          className="text-sm text-muted-foreground hover:text-foreground"
        >
          Ayarlar
        </Link>
      </nav>
      {children}
    </div>
  )
}
```

**Adım 2 — `src/app/(dashboard)/projeler/[id]/page.tsx` güncelle:**

Sol sütundaki `</ul>` kapanma etiketinin hemen sonrasına (stage listesi bittikten sonra, `</div>` kapanmadan önce) Separator + link ekle:

```tsx
          </ul>
          {/* Proje Kuralları — Separator + nav linki (Phase 3) */}
          <Separator className="my-4" />
          <Link
            href={`/projeler/${id}/kurallar`}
            className="text-sm text-muted-foreground hover:text-foreground flex items-center gap-1 px-3 py-2 rounded-md hover:bg-secondary"
          >
            Proje Kuralları
          </Link>
        </div>
```

NOT: `Separator` zaten import edilmiş (`src/app/(dashboard)/projeler/[id]/page.tsx` line 5). `Link` de mevcut (line 2). Ek import gerekmez — mevcut import'ları koru.
  </action>
  <verify>
    <automated>grep -n "ayarlar/kurallar\|Ayarlar\|nav.*border-b" C:/Users/baris/Documents/seo-machine/src/app/(dashboard)/layout.tsx && grep -n "Proje Kuralları\|projeler.*kurallar" C:/Users/baris/Documents/seo-machine/src/app/(dashboard)/projeler/[id]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx içinde `href="/ayarlar/kurallar"` var
    - layout.tsx içinde `Ayarlar` link text var
    - layout.tsx içinde `href="/projeler"` ve `Projeler` link var
    - layout.tsx içinde `<nav className="border-b border-border` var
    - layout.tsx içinde `import Link from 'next/link'` var
    - projeler/[id]/page.tsx içinde `Proje Kuralları` text var
    - projeler/[id]/page.tsx içinde `href={\`/projeler/${id}/kurallar\`}` var (template literal)
    - projeler/[id]/page.tsx içinde `<Separator className="my-4"` yeni ekleme var (stage listesi sonunda)
    - `npx tsc --noEmit` 0 hatayla çıkıyor
  </acceptance_criteria>
  <done>Dashboard layout'ta üst nav Projeler + Ayarlar linkleriyle güncellendi. Proje detay sol sütununa Proje Kuralları linki eklendi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Server Component | Proje kuralları sayfasına params.id üzerinden gelen değer güvenilmez |
| Server Component→Supabase | Project ownership + RLS double protection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Spoofing | ProjeKurallarPage — auth guard | mitigate | `if (!user) notFound()` — oturumsuz erişim 404 döner |
| T-03-03-02 | Tampering | ProjeKurallarPage — params.id | mitigate | `.eq('id', id).eq('user_id', user.id).single()` — başkasının proje ID'siyle erişilirse notFound() |
| T-03-03-03 | Information Disclosure | ProjeKurallarPage — global rules query | mitigate | `.eq('user_id', user.id)` filtresi — başka kullanıcının global kuralları görünmez |
| T-03-03-04 | Tampering | toggleProjectRule.bind(null, id) — client-side bind | accept | bind sadece UI convenience — gerçek yetki kontrolü Server Action'da; client id'yi değiştiremez (SSR rendered prop) |
| T-03-03-05 | Spoofing | Dashboard nav — auth guard | mitigate | DashboardLayout server-side auth guard koruyor; nav linkleri sadece authenticated session'da görünür |
</threat_model>

<verification>
1. `/ayarlar/kurallar` linki dashboard nav'da görünüyor ve tıklanınca açılıyor
2. Proje detay sayfasında sol sütun altında "Proje Kuralları" linki var
3. `/projeler/[id]/kurallar` sayfası 12 kuralı yükliyor
4. Global değer taşıyan kurallar [Global] badge, override'lı kurallar [Proje] ✕ badge gösteriyor
5. ✕ tıklanınca kural [Global]'e dönüyor (satır siliniyor, sayfa yenileniyor)
6. `npx tsc --noEmit` hata yok
7. `npm run build` başarılı tamamlanıyor
</verification>

<success_criteria>
- Dashboard üst navda "Projeler" ve "Ayarlar" linkleri görünüyor
- Proje detay sol sütununda stage listesi altında Separator + "Proje Kuralları" linki var
- Proje kuralları sayfası global+proje override birleştirmesiyle doğru scope badge gösteriyor
- Proje override oluşturulunca [Proje] badge, silinince [Global] badge geliyor
- RULE-01, RULE-02, RULE-03 gereksinimleri tamamlandı
</success_criteria>

<output>
Tamamlandığında `.planning/phases/03-rules-engine/03-03-SUMMARY.md` oluştur.
</output>
