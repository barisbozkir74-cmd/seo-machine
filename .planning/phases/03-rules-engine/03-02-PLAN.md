---
phase: 03-rules-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/rules/RuleToggleRow.tsx
  - src/app/(dashboard)/projeler/[id]/kurallar/actions.ts
  - src/app/(dashboard)/ayarlar/kurallar/page.tsx
autonomous: true
requirements:
  - RULE-02
  - RULE-03

must_haves:
  truths:
    - "RuleToggleRow bileşeni toggle tıklandığında ilgili Server Action'ı çağırır"
    - "Toggle geçişi sırasında disabled + opacity-50 loading state gösterilir (D-06)"
    - "Hata durumunda toggle önceki değere döner ve inline hata mesajı görünür"
    - "toggleProjectRule proje ownership kontrolü yaparak upsert gerçekleştirir"
    - "resetProjectRule proje override satırını siler (global'e döner)"
    - "Global kurallar sayfasında RuleToggleRow bileşeni toggle'larla wire edilmiştir"
  artifacts:
    - path: "src/components/rules/RuleToggleRow.tsx"
      provides: "Client Component — optimistic toggle + error state + scope badge"
      exports: ["RuleToggleRow"]
    - path: "src/app/(dashboard)/projeler/[id]/kurallar/actions.ts"
      provides: "toggleProjectRule + resetProjectRule Server Actions"
      exports: ["toggleProjectRule", "resetProjectRule", "ToggleProjectRuleResult"]
  key_links:
    - from: "src/components/rules/RuleToggleRow.tsx"
      to: "src/app/(dashboard)/ayarlar/kurallar/actions.ts"
      via: "toggleAction prop — global sayfada toggleRule pass edilir"
      pattern: "toggleAction"
    - from: "src/components/rules/RuleToggleRow.tsx"
      to: "src/app/(dashboard)/projeler/[id]/kurallar/actions.ts"
      via: "toggleAction prop — proje sayfasında toggleProjectRule.bind(null, projectId) pass edilir"
      pattern: "toggleProjectRule"
    - from: "src/app/(dashboard)/projeler/[id]/kurallar/actions.ts"
      to: "rules table"
      via: "supabase.from('rules').upsert / .delete"
      pattern: "supabase\\.from\\('rules'\\)"
---

<objective>
RuleToggleRow Client Component'ini oluştur, proje bazlı Server Actions yaz ve global kurallar sayfasını gerçek toggle'larla wire et. Bu plan RULE-02 (inline güncelleme) ve RULE-03 (scope ayrımı) için kritik interaktivite katmanını tamamlar.

Purpose: Toggle UX'i ve proje bazlı kural override altyapısı. Plan 03-01'in statik sayfasını tam fonksiyonlu hale getirir.
Output: RuleToggleRow.tsx (Client), kurallar/actions.ts (project actions), page.tsx güncellemesi
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-rules-engine/03-CONTEXT.md
@.planning/phases/03-rules-engine/03-UI-SPEC.md
@.planning/phases/03-rules-engine/03-PATTERNS.md
@.planning/phases/03-rules-engine/03-01-SUMMARY.md

<interfaces>
<!-- Plan 03-01'de oluşturulan interfaces -->

src/app/(dashboard)/ayarlar/kurallar/actions.ts:
```typescript
export type ToggleRuleResult = { success: true } | { success: false; error: string }
export async function toggleRule(ruleKey: string, newValue: boolean): Promise<ToggleRuleResult>
export async function seedGlobalRules(): Promise<void>
```

Mevcut analog — stage-transition.tsx isPending pattern:
```typescript
'use client'
import { useState } from 'react'
const [isPending, setIsPending] = useState(false)
// setIsPending(true) → action call → setIsPending(false) in finally
```

Scope badge pattern (variant prop KULLANILMAZ):
```tsx
<Badge className="bg-slate-500/20 text-slate-400 border border-slate-500/30">Global</Badge>
<Badge className="bg-blue-500/20 text-blue-400 border border-blue-500/30">Proje</Badge>
```

Inline error pattern (notes-section.tsx):
```tsx
{error && <p className="text-sm text-destructive">{error}</p>}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RuleToggleRow Client Component ve proje Server Actions yaz</name>
  <files>src/components/rules/RuleToggleRow.tsx, src/app/(dashboard)/projeler/[id]/kurallar/actions.ts</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/stage-transition.tsx (isPending pattern — tam dosya)
    - src/app/(dashboard)/projeler/[id]/notes-section.tsx (inline error pattern)
    - src/components/ui/switch.tsx (import path doğrulama)
    - src/components/ui/badge.tsx (import path doğrulama)
    - src/app/(dashboard)/ayarlar/kurallar/actions.ts (ToggleRuleResult type import için)
    - src/app/(dashboard)/projeler/[id]/actions.ts (project ownership check pattern)
  </read_first>
  <action>
**Adım 1 — `src/components/rules/RuleToggleRow.tsx` oluştur:**

Dizin `src/components/rules/` yoksa oluştur.

```typescript
'use client'

import { useState } from 'react'
import { Switch } from '@/components/ui/switch'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'

interface RuleToggleRowProps {
  ruleKey: string
  label: string
  currentValue: boolean
  recommendedValue: boolean
  scope: 'global' | 'project'
  // toggleAction: global sayfada toggleRule, proje sayfasında toggleProjectRule.bind(null, projectId)
  toggleAction: (ruleKey: string, newValue: boolean) => Promise<{ success: boolean; error?: string }>
  // Sadece scope==='project' satırlarında mevcut
  resetAction?: () => Promise<{ success: boolean; error?: string }>
}

export function RuleToggleRow({
  ruleKey,
  label,
  currentValue,
  recommendedValue,
  scope,
  toggleAction,
  resetAction,
}: RuleToggleRowProps) {
  const [localValue, setLocalValue] = useState(currentValue)
  const [isPending, setIsPending] = useState(false)
  const [error, setError] = useState<string | null>(null)

  const handleToggle = async (checked: boolean) => {
    setIsPending(true)
    setError(null)
    const prev = localValue
    setLocalValue(checked) // optimistic update
    try {
      const result = await toggleAction(ruleKey, checked)
      if (!result.success) {
        setLocalValue(prev) // rollback
        setError(result.error ?? 'Kural güncellenemedi. Lütfen tekrar deneyin.')
      }
    } catch {
      setLocalValue(prev)
      setError('Kural güncellenemedi. Lütfen tekrar deneyin.')
    } finally {
      setIsPending(false)
    }
  }

  const handleReset = async () => {
    if (!resetAction) return
    setIsPending(true)
    setError(null)
    try {
      const result = await resetAction()
      if (!result.success) {
        setError(result.error ?? 'Kural sıfırlanamadı. Lütfen tekrar deneyin.')
      }
      // Başarı: revalidatePath sayfa yeniler, scope badge Global'e döner
    } catch {
      setError('Kural sıfırlanamadı. Lütfen tekrar deneyin.')
    } finally {
      setIsPending(false)
    }
  }

  const matchesRecommended = localValue === recommendedValue

  return (
    <>
      <td className="w-24 px-4 py-3">
        {scope === 'global' ? (
          <Badge className="bg-slate-500/20 text-slate-400 border border-slate-500/30 text-xs">
            Global
          </Badge>
        ) : (
          <Badge className="bg-blue-500/20 text-blue-400 border border-blue-500/30 text-xs flex items-center gap-1 w-fit">
            Proje
            {resetAction && (
              <Button
                variant="ghost"
                size="sm"
                title="Global'e döndür"
                onClick={handleReset}
                disabled={isPending}
                className="text-blue-400 hover:text-red-400 ml-1 h-auto p-0 leading-none"
              >
                ✕
              </Button>
            )}
          </Badge>
        )}
      </td>
      <td className="flex-1 px-4 py-3">
        <span className="text-sm">{label}</span>
        {error && (
          <p className="text-xs text-destructive mt-1">{error}</p>
        )}
      </td>
      <td className="w-48 px-4 py-3">
        <span className={matchesRecommended ? 'text-xs text-muted-foreground' : 'text-xs text-amber-400'}>
          Önerilen: {recommendedValue ? 'Açık' : 'Kapalı'}
        </span>
      </td>
      <td className="w-16 px-4 py-3 text-right">
        <Switch
          checked={localValue}
          onCheckedChange={handleToggle}
          disabled={isPending}
          className={isPending ? 'opacity-50 cursor-wait' : ''}
        />
      </td>
    </>
  )
}
```

NOT: RuleToggleRow `<td>` elementleri döndürüyor — sayfa bileşeni bunu `<TableRow>` içine wrap eder. Bu pattern sayfa içi table row composition sağlar.

**Adım 2 — `src/app/(dashboard)/projeler/[id]/kurallar/actions.ts` oluştur:**

```typescript
'use server'

import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'

export type ToggleProjectRuleResult =
  | { success: true }
  | { success: false; error: string }

export async function toggleProjectRule(
  projectId: string,
  ruleKey: string,
  newValue: boolean
): Promise<ToggleProjectRuleResult> {
  const supabase = await createClient()

  const { data: { user } } = await supabase.auth.getUser()
  if (!user) {
    return { success: false, error: 'Oturum bulunamadı.' }
  }

  // Proje ownership kontrolü (D-08: inheritance modeli — proje yoksa global geçerli)
  const { data: project } = await supabase
    .from('projects')
    .select('id')
    .eq('id', projectId)
    .eq('user_id', user.id)
    .single()

  if (!project) {
    return { success: false, error: 'Proje bulunamadı.' }
  }

  // UPSERT — override varsa güncelle, yoksa ekle (D-08: sadece override yapıldığında yazılır)
  const { error } = await supabase.from('rules').upsert(
    {
      user_id: user.id,
      project_id: projectId,
      scope: 'project',
      rule_key: ruleKey,
      rule_value: String(newValue),
    },
    { onConflict: 'user_id,project_id,rule_key,scope' }
  )

  if (error) {
    return { success: false, error: 'Kural güncellenemedi. Lütfen tekrar deneyin.' }
  }

  revalidatePath(`/projeler/${projectId}/kurallar`)
  return { success: true }
}

export async function resetProjectRule(
  projectId: string,
  ruleKey: string
): Promise<ToggleProjectRuleResult> {
  const supabase = await createClient()

  const { data: { user } } = await supabase.auth.getUser()
  if (!user) {
    return { success: false, error: 'Oturum bulunamadı.' }
  }

  // Ownership check — kullanıcının projesi olduğunu doğrula
  const { data: project } = await supabase
    .from('projects')
    .select('id')
    .eq('id', projectId)
    .eq('user_id', user.id)
    .single()

  if (!project) {
    return { success: false, error: 'Proje bulunamadı.' }
  }

  const { error } = await supabase
    .from('rules')
    .delete()
    .eq('user_id', user.id)
    .eq('project_id', projectId)
    .eq('rule_key', ruleKey)
    .eq('scope', 'project')

  if (error) {
    return { success: false, error: 'Kural sıfırlanamadı. Lütfen tekrar deneyin.' }
  }

  revalidatePath(`/projeler/${projectId}/kurallar`)
  return { success: true }
}
```
  </action>
  <verify>
    <automated>grep -n "RuleToggleRow\|handleToggle\|handleReset\|isPending\|optimistic" C:/Users/baris/Documents/seo-machine/src/components/rules/RuleToggleRow.tsx && grep -n "toggleProjectRule\|resetProjectRule\|onConflict" C:/Users/baris/Documents/seo-machine/src/app/(dashboard)/projeler/[id]/kurallar/actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - RuleToggleRow.tsx içinde `'use client'` direktifi ilk satırda var
    - RuleToggleRow.tsx içinde `export function RuleToggleRow` satırı var
    - RuleToggleRow.tsx içinde `const [isPending, setIsPending] = useState(false)` var
    - RuleToggleRow.tsx içinde `setLocalValue(prev)` (rollback) var
    - RuleToggleRow.tsx içinde `opacity-50 cursor-wait` loading state class var
    - RuleToggleRow.tsx içinde `bg-slate-500/20 text-slate-400` Global badge var
    - RuleToggleRow.tsx içinde `bg-blue-500/20 text-blue-400` Proje badge var
    - RuleToggleRow.tsx içinde `text-amber-400` sapma uyarısı var
    - RuleToggleRow.tsx içinde `Global'e döndür` title attribute var
    - kurallar/actions.ts içinde `'use server'` direktifi ilk satırda var
    - kurallar/actions.ts içinde `export async function toggleProjectRule` satırı var
    - kurallar/actions.ts içinde `export async function resetProjectRule` satırı var
    - kurallar/actions.ts içinde `onConflict: 'user_id,project_id,rule_key,scope'` metni var
    - kurallar/actions.ts içinde `.delete()` ve `.eq('scope', 'project')` birlikte var
    - `npx tsc --noEmit` 0 hatayla çıkıyor
  </acceptance_criteria>
  <done>RuleToggleRow Client Component ve proje Server Actions oluşturuldu.</done>
</task>

<task type="auto">
  <name>Task 2: Global kurallar sayfasını RuleToggleRow ile wire et</name>
  <files>src/app/(dashboard)/ayarlar/kurallar/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/ayarlar/kurallar/page.tsx (mevcut tam dosya — Plan 03-01'de oluşturuldu)
    - src/components/rules/RuleToggleRow.tsx (az önce oluşturuldu — props interface)
    - src/app/(dashboard)/ayarlar/kurallar/actions.ts (toggleRule signature)
  </read_first>
  <action>
Plan 03-01'de oluşturulan global kurallar sayfasındaki statik `<span>{currentValue ? 'Açık' : 'Kapalı'}</span>` placeholder'larını `RuleToggleRow` bileşeniyle değiştir.

**Değişiklikler:**
1. Import ekle: `import { RuleToggleRow } from '@/components/rules/RuleToggleRow'`
2. Import ekle: `import { toggleRule } from './actions'`
3. Her `<TableRow>` içindeki 3 ayrı `<TableCell>` bloğunu kaldır
4. Yerine `<TableRow><RuleToggleRow ... /></TableRow>` kullan

**RuleToggleRow kullanımı:**
```tsx
import { RuleToggleRow } from '@/components/rules/RuleToggleRow'
import { toggleRule } from './actions'

// TableRow içinde:
<TableRow key={ruleKey}>
  <RuleToggleRow
    ruleKey={ruleKey}
    label={meta.label}
    currentValue={currentValue}
    recommendedValue={meta.recommended}
    scope="global"
    toggleAction={toggleRule}
  />
</TableRow>
```

NOT: `toggleAction={toggleRule}` — global sayfada proje ID'si yok, `toggleRule(ruleKey, newValue)` imzası `(ruleKey: string, newValue: boolean) => Promise<ToggleRuleResult>` ile RuleToggleRow prop tipiyle eşleşiyor.

Table kolon yapısını koru — TableHeader'daki başlıkları kaldırma. Sadece TableBody içindeki satır render'ını değiştir.
  </action>
  <verify>
    <automated>grep -n "RuleToggleRow\|toggleRule\|scope=\"global\"" C:/Users/baris/Documents/seo-machine/src/app/(dashboard)/ayarlar/kurallar/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx içinde `import { RuleToggleRow }` satırı var
    - page.tsx içinde `import { toggleRule }` satırı var
    - page.tsx içinde `<RuleToggleRow` kullanımı var
    - page.tsx içinde `scope="global"` attr var
    - page.tsx içinde `toggleAction={toggleRule}` var
    - page.tsx içinde eski `<span>{currentValue ? 'Açık' : 'Kapalı'}</span>` placeholder YOK
    - `npx tsc --noEmit` 0 hatayla çıkıyor
  </acceptance_criteria>
  <done>Global kurallar sayfası RuleToggleRow ile wire edildi — toggle tıklandığında toggleRule Server Action tetiklenecek.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Server Action | toggleProjectRule ve resetProjectRule'a projectId, ruleKey, newValue parametreleri güvenilmez |
| Server Action→Supabase | Proje ownership double-check zorunlu — sadece user'ın projelerine yazılır |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Spoofing | toggleProjectRule / resetProjectRule | mitigate | `supabase.auth.getUser()` her action'da — JWT doğrulaması Supabase tarafında |
| T-03-02-02 | Tampering | toggleProjectRule — projectId parametresi | mitigate | `.eq('id', projectId).eq('user_id', user.id)` ownership double-check; başkasının projesine kural yazılamaz |
| T-03-02-03 | Tampering | resetProjectRule — DELETE scope sabitleme | mitigate | `.eq('scope', 'project')` ile yalnızca project-scope satırlar silinir; global kurallar silinemez |
| T-03-02-04 | Elevation of Privilege | RuleToggleRow client-side | accept | RuleToggleRow sadece UI state tutar; gerçek yetki kontrolü Server Action'da yapılıyor; client-side bypass sahte sonuç gösterir ama veri değişmez |
| T-03-02-05 | Denial of Service | toggleProjectRule upsert flood | accept | Supabase row-level RLS ve rate limit yeterli; bu internal tool, public erişim yok |
</threat_model>

<verification>
1. RuleToggleRow toggle tıklandığında disabled + opacity-50 gösteriyor
2. toggleRule başarısız dönünce toggle eski değerine rollback yapıyor
3. Global kurallar sayfasında her kural için Switch görünüyor
4. toggleProjectRule proje sahibi olmayan user için `{ success: false, error: 'Proje bulunamadı.' }` dönüyor
5. resetProjectRule proje override satırını siliyor; global değer geri geliyor
6. `npx tsc --noEmit` hata yok
</verification>

<success_criteria>
- RuleToggleRow Client Component optimistic update + rollback + loading state ile çalışıyor
- Global badge (slate) ve Proje badge (blue + ✕) doğru render ediliyor
- toggleProjectRule ownership check + upsert tamamlandı
- resetProjectRule proje scope satırını DELETE ile siliyor
- Global kurallar sayfası interactive toggle'larla wire edildi
</success_criteria>

<output>
Tamamlandığında `.planning/phases/03-rules-engine/03-02-SUMMARY.md` oluştur.
</output>
