---
phase: 13-wordpress-publishing
plan: 03
type: execute
wave: 2
depends_on:
  - 13-02
files_modified:
  - src/app/(dashboard)/projeler/[id]/wordpress-section.tsx
  - src/app/(dashboard)/projeler/[id]/page.tsx
autonomous: true
requirements:
  - PUBL-01
must_haves:
  truths:
    - "Proje detay sayfasında 'WordPress Bağlantısı' bölümü görünür"
    - "wp_url ve wp_app_password alanları form olarak sunulur; şifre alanı maskelidir"
    - "Bağlantı durumu badge'i Vault'tan kontrol edilerek render edilir"
    - "Kaydet butonu saveWordPressCredentials action'ını çağırır"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/wordpress-section.tsx"
      provides: "WordPressConnectionSection client component"
      exports: ["WordPressConnectionSection"]
    - path: "src/app/(dashboard)/projeler/[id]/page.tsx"
      provides: "Proje detay sayfası — WordPress bölümü entegre"
      contains: "WordPressConnectionSection"
  key_links:
    - from: "src/app/(dashboard)/projeler/[id]/page.tsx"
      to: "src/app/(dashboard)/projeler/[id]/wordpress-section.tsx"
      via: "import { WordPressConnectionSection }"
      pattern: "WordPressConnectionSection"
    - from: "src/app/(dashboard)/projeler/[id]/wordpress-section.tsx"
      to: "src/app/(dashboard)/projeler/[id]/actions.ts"
      via: "import { saveWordPressCredentials }"
      pattern: "saveWordPressCredentials"
---

<objective>
WordPress bağlantı bilgisi formu (WordPressConnectionSection) client component'ini oluştur ve proje detay sayfasına entegre et. Kullanıcı proje başına wp_url ve wp_app_password girebilir.

Purpose: PUBL-01 — kimlik bilgisi yönetimi UI'ı. Plan 05'teki PublishDialog'un "WordPress bağlantısı yapılandırılmış mı?" kontrolü bu component'in sağladığı `isConfigured` prop'una dayanır.
Output: `wordpress-section.tsx` (yeni dosya) + `projeler/[id]/page.tsx` (genişletilmiş).
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-wordpress-publishing/13-CONTEXT.md
@.planning/phases/13-wordpress-publishing/13-PATTERNS.md
@.planning/phases/13-wordpress-publishing/13-UI-SPEC.md

<interfaces>
<!-- Plan 02 artifact'ları — bu plan bunları kullanır: -->
From src/app/(dashboard)/projeler/[id]/actions.ts (Plan 02'de eklendi):
```typescript
export type SaveWpCredentialsResult =
  | { success: true }
  | { success: false; error: string }

export async function saveWordPressCredentials(
  projectId: string,
  wpUrl: string,
  appPassword: string
): Promise<SaveWpCredentialsResult>
```

From src/lib/supabase/vault.ts (Plan 02'de eklendi):
```typescript
export async function hasWordPressCredentials(projectId: string): Promise<boolean>
```

<!-- Mevcut proje detay sayfası yapısı (genişletilecek): -->
From src/app/(dashboard)/projeler/[id]/page.tsx:
```typescript
// Sağ içerik area — genişletilecek kısım:
<div className="flex-1 min-w-0 overflow-y-auto p-8">
  <h2 className="text-base font-semibold mb-6">Proje Bilgileri</h2>
  <ProjectInfoSection ... />
  {/* BURAYA EKLENECEK: <Separator /> + <WordPressConnectionSection /> */}
</div>
```

<!-- UI-SPEC renk ve bileşen kuralları: -->
Badge renk ataması className ile (variant prop kullanılmaz):
  Bağlı:            className="bg-emerald-500/20 text-emerald-400"
  Yapılandırılmadı: className="bg-slate-800 text-slate-400"

Hugeicons (free tier):
  Eye01Icon    — şifre göster
  EyeOff01Icon — şifre gizle

font-medium YASAK — font-semibold (600) kullanılır
DialogTrigger asChild YASAK — render prop kullanılır
min-h-[44px] touch target zorunlu (Kaydet butonu)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WordPressConnectionSection client component oluştur</name>
  <files>src/app/(dashboard)/projeler/[id]/wordpress-section.tsx</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/ProjectInfoSection.tsx (rol eşleşmesi analog — form state pattern)
    - .planning/phases/13-wordpress-publishing/13-UI-SPEC.md (Layout Contract §1, Interaction Contracts §Kimlik Bilgisi Formu, Copywriting Contract)
    - .planning/phases/13-wordpress-publishing/13-PATTERNS.md (wordpress-section.tsx pattern bölümü)
  </read_first>
  <action>
Aşağıdaki içerikle `src/app/(dashboard)/projeler/[id]/wordpress-section.tsx` dosyasını oluştur:

```typescript
'use client'

import { useState } from 'react'
import { HugeiconsIcon } from '@hugeicons/react'
import { Eye01Icon, EyeOff01Icon } from '@hugeicons/core-free-icons'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Badge } from '@/components/ui/badge'
import { saveWordPressCredentials } from './actions'

type Props = {
  projectId: string
  isConfigured: boolean // Vault'tan SSR'da kontrol edilmiş
}

export function WordPressConnectionSection({ projectId, isConfigured }: Props) {
  const [wpUrl, setWpUrl] = useState('')
  const [appPassword, setAppPassword] = useState('')
  const [showPassword, setShowPassword] = useState(false)
  const [saving, setSaving] = useState(false)
  const [error, setError] = useState<string | null>(null)
  const [saved, setSaved] = useState(isConfigured)

  const isDirty = wpUrl.trim().length > 0 || appPassword.trim().length > 0

  const handleSave = async () => {
    setError(null)

    // Client-side validation
    if (!wpUrl.startsWith('https://')) {
      setError('Geçerli bir WordPress URL\'si girin (https:// ile başlamalı).')
      return
    }
    if (!appPassword.trim()) {
      setError('Uygulama Şifresi gereklidir.')
      return
    }

    setSaving(true)
    try {
      const result = await saveWordPressCredentials(projectId, wpUrl, appPassword)
      if (!result.success) {
        setError(result.error)
        return
      }
      setSaved(true)
      setWpUrl('')
      setAppPassword('')
    } catch {
      setError('İşlem başarısız. Lütfen tekrar deneyin.')
    } finally {
      setSaving(false)
    }
  }

  return (
    <div className="space-y-4">
      {/* Başlık + Durum Badge */}
      <div className="flex items-center gap-3">
        <h2 className="text-base font-semibold">WordPress Bağlantısı</h2>
        <Badge
          aria-live="polite"
          className={
            saved
              ? 'bg-emerald-500/20 text-emerald-400 border-emerald-500/30'
              : 'bg-slate-800 text-slate-400 border-slate-700'
          }
        >
          {saved ? 'Bağlı' : 'Yapılandırılmadı'}
        </Badge>
      </div>

      {/* WordPress URL */}
      <div className="space-y-1.5">
        <label htmlFor="wp-url" className="text-sm text-muted-foreground">
          WordPress URL
        </label>
        <Input
          id="wp-url"
          type="url"
          value={wpUrl}
          onChange={(e) => setWpUrl(e.target.value)}
          placeholder="https://example.com"
          className="text-sm h-9"
          disabled={saving}
        />
      </div>

      {/* Uygulama Şifresi */}
      <div className="space-y-1.5">
        <label htmlFor="wp-app-password" className="text-sm text-muted-foreground">
          Uygulama Şifresi
        </label>
        <div className="relative">
          <Input
            id="wp-app-password"
            type={showPassword ? 'text' : 'password'}
            value={appPassword}
            onChange={(e) => setAppPassword(e.target.value)}
            placeholder={saved ? '••••••••••••' : 'xxxx xxxx xxxx xxxx'}
            className="text-sm h-9 pr-10"
            disabled={saving}
            aria-label="Uygulama Şifresi"
          />
          <Button
            type="button"
            variant="ghost"
            size="sm"
            className="absolute right-0 top-0 h-9 w-10 px-0"
            aria-label={showPassword ? 'Şifreyi gizle' : 'Şifreyi göster'}
            onMouseDown={(e) => {
              e.preventDefault() // input focus korunur
              setShowPassword((v) => !v)
            }}
          >
            <HugeiconsIcon
              icon={showPassword ? EyeOff01Icon : Eye01Icon}
              size={16}
              className="text-muted-foreground"
            />
          </Button>
        </div>
        <p className="text-xs text-muted-foreground">
          Uygulama Şifresini WordPress Admin &gt; Profil &gt; Uygulama Şifrelerinden oluşturun.
        </p>
      </div>

      {/* Hata mesajı */}
      {error && (
        <p className="text-xs text-destructive">{error}</p>
      )}

      {/* Kaydet butonu */}
      <div className="flex justify-end">
        <Button
          onClick={handleSave}
          disabled={saving || !isDirty}
          className="min-h-[44px]"
        >
          {saving ? 'Kaydediliyor...' : 'Kaydet'}
        </Button>
      </div>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>grep -c "export function WordPressConnectionSection" "C:/Users/baris/Documents/seo-machine/src/app/(dashboard)/projeler/[id]/wordpress-section.tsx"</automated>
  </verify>
  <done>wordpress-section.tsx mevcuttur. 'use client' direktifi var. Eye01Icon/EyeOff01Icon kullanılmış. Badge variant prop kullanılmamış — className ile renk atanmış. min-h-[44px] Kaydet butonunda var. font-medium kullanılmamış.</done>
</task>

<task type="auto">
  <name>Task 2: projeler/[id]/page.tsx — WordPressConnectionSection entegre et</name>
  <files>src/app/(dashboard)/projeler/[id]/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/page.tsx (mevcut dosyanın tamamı)
    - src/lib/supabase/vault.ts (hasWordPressCredentials imzası — SSR'da çağrılacak)
    - .planning/phases/13-wordpress-publishing/13-UI-SPEC.md (Layout Contract §1 — Separator + bölüm konumu)
  </read_first>
  <action>
`src/app/(dashboard)/projeler/[id]/page.tsx` dosyasını oku. Şu değişiklikleri uygula:

**1. Import bloğuna ekle:**
```typescript
import { hasWordPressCredentials } from '@/lib/supabase/vault'
import { WordPressConnectionSection } from './wordpress-section'
```

**2. Supabase client + auth sorgusundan sonra, return'den önce ekle:**
```typescript
// WordPress bağlantı durumu — Vault'tan SSR kontrolü
const isWpConfigured = await hasWordPressCredentials(id)
```

**3. JSX'te sağ içerik area'sının içinde ProjectInfoSection'ın hemen altına ekle:**
```tsx
<Separator className="my-8" />

<h2 className="text-base font-semibold mb-6">WordPress Bağlantısı</h2>
<WordPressConnectionSection
  projectId={project.id}
  isConfigured={isWpConfigured}
/>
```

Mevcut `<Separator />` import'u zaten var — tekrar ekleme.
Dosyanın geri kalanına dokunma.
  </action>
  <verify>
    <automated>grep -c "WordPressConnectionSection" "C:/Users/baris/Documents/seo-machine/src/app/(dashboard)/projeler/[id]/page.tsx"</automated>
  </verify>
  <done>page.tsx'te WordPressConnectionSection import edilmiş ve render edilmiş. hasWordPressCredentials SSR'da çağrılmış ve isWpConfigured prop olarak geçilmiş. Separator bölümler arasında mevcut.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | wp_url ve appPassword client'tan server action'a POST ile gönderilir |
| Server Action → vault.ts | saveWordPressCredentials, appPassword'u vault.ts'e iletir; client bundle'a sızmaz |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Information Disclosure | wordpress-section.tsx | mitigate | 'use client' component — appPassword state sadece submit anında server action'a gider; DOM'da type="password" masked |
| T-13-03-02 | Spoofing | saveWordPressCredentials | mitigate | Plan 02'de server-side getUser() + ownership check uygulandı |
| T-13-03-03 | Information Disclosure | hasWordPressCredentials SSR | accept | Boolean döner; wpUrl veya appPassword expose etmez — güvenli |
| T-13-03-04 | Tampering | projectId client'tan gelir | mitigate | Server action'da eq('user_id', user.id) — başka kullanıcının projectId'sini kullanamazsın |
</threat_model>

<verification>
1. `grep "WordPressConnectionSection" src/app/(dashboard)/projeler/[id]/page.tsx` — 2 satır (import + render)
2. `grep "hasWordPressCredentials" src/app/(dashboard)/projeler/[id]/page.tsx` — 1 satır (SSR çağrısı)
3. `grep "export function WordPressConnectionSection" src/app/(dashboard)/projeler/[id]/wordpress-section.tsx` — 1 sonuç
4. `grep "variant" src/app/(dashboard)/projeler/[id]/wordpress-section.tsx` — 0 sonuç (Badge variant prop yasak)
5. `grep "font-medium" src/app/(dashboard)/projeler/[id]/wordpress-section.tsx` — 0 sonuç
6. `grep "min-h-\[44px\]" src/app/(dashboard)/projeler/[id]/wordpress-section.tsx` — 1 sonuç
</verification>

<success_criteria>
- wordpress-section.tsx mevcut ve WordPressConnectionSection export ediyor
- page.tsx hasWordPressCredentials ile SSR bağlantı durumunu kontrol ediyor
- Şifre alanı type="password" ile başlıyor, toggle ile type="text" geçiyor
- Badge rengi className ile atanıyor (variant prop yok)
- Kaydet butonu min-h-[44px] touch target ile
- font-medium hiçbir yerde kullanılmıyor
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/13-wordpress-publishing/13-03-SUMMARY.md` oluştur.
</output>
