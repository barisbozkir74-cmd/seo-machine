---
phase: 13-wordpress-publishing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/vault.ts
  - src/app/(dashboard)/projeler/[id]/actions.ts
autonomous: true
requirements:
  - PUBL-01
must_haves:
  truths:
    - "vault.ts'te saveWpCredentials ve getWordPressCredentials fonksiyonları mevcuttur"
    - "saveWordPressCredentials server action proje sahipliğini doğrular ve Vault'a yazar"
    - "wp_app_password hiçbir zaman loglanmaz, response'ta görünmez"
  artifacts:
    - path: "src/lib/supabase/vault.ts"
      provides: "WP credential Vault okuma/yazma fonksiyonları"
      exports: ["getWordPressCredentials", "saveWpCredentials", "hasWordPressCredentials"]
    - path: "src/app/(dashboard)/projeler/[id]/actions.ts"
      provides: "saveWordPressCredentials server action"
      exports: ["saveWordPressCredentials", "SaveWpCredentialsResult"]
  key_links:
    - from: "src/app/(dashboard)/projeler/[id]/actions.ts"
      to: "src/lib/supabase/vault.ts"
      via: "import { saveWpCredentials } from '@/lib/supabase/vault'"
      pattern: "saveWpCredentials"
---

<objective>
WordPress kimlik bilgilerinin Supabase Vault'ta güvenli saklanması ve okunması için vault.ts'e yeni fonksiyonlar ekle; proje detay sayfası için `saveWordPressCredentials` server action'ı oluştur.

Purpose: PUBL-01 — wp_url ve wp_app_password proje başına Vault key'leri olarak saklanır. Plan 03 (UI) ve Plan 04 (publishToWordPress) bu layer'a bağımlıdır.
Output: vault.ts genişletilmiş (3 yeni fonksiyon) + projeler/[id]/actions.ts genişletilmiş (1 yeni action).
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-wordpress-publishing/13-CONTEXT.md
@.planning/phases/13-wordpress-publishing/13-PATTERNS.md

<interfaces>
<!-- Mevcut vault.ts (tümü) — genişletilecek: -->
From src/lib/supabase/vault.ts:
```typescript
import 'server-only'
import { createClient } from '@supabase/supabase-js'

const serviceClient = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!
)

export async function getDataForSeoCredentials(): Promise<{ login: string; password: string }> {
  // env var fallback + vault read pattern...
}
```

<!-- Vault key naming convention (CONTEXT.md D-01): -->
wp_url_{projectId}           — WordPress site URL
wp_app_password_{projectId}  — WordPress Application Password

<!-- Supabase Vault write API — projede ilk kez; serviceClient üzerinden RPC: -->
// Secret oluşturma:
await serviceClient.rpc('vault.create_secret', {
  secret: value,
  name: keyName,
})
// Secret güncelleme — önce mevcut ID'yi bul, sonra güncelle:
const { data } = await serviceClient
  .from('vault.secrets')
  .select('id')
  .eq('name', keyName)
  .single()
// ID bulunduysa güncelle:
await serviceClient.rpc('vault.update_secret', {
  secret_id: data.id,
  new_secret: value,
})

<!-- projeler/[id]/actions.ts mevcut pattern (auth + ownership): -->
From src/app/(dashboard)/projeler/[id]/actions.ts:
```typescript
'use server'
import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'

// Auth pattern:
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) return { success: false, error: 'Oturum bulunamadı.' }

// Ownership check pattern:
const { data: project } = await supabase
  .from('projects')
  .select('id')
  .eq('id', projectId)
  .eq('user_id', user.id)
  .single()
if (!project) return { success: false, error: 'Proje bulunamadı.' }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: vault.ts — WP credential fonksiyonları ekle</name>
  <files>src/lib/supabase/vault.ts</files>
  <read_first>
    - src/lib/supabase/vault.ts (mevcut dosyanın tamamı — genişletilecek)
    - .planning/phases/13-wordpress-publishing/13-CONTEXT.md (D-01 vault key naming)
    - .planning/phases/13-wordpress-publishing/13-PATTERNS.md (vault write pattern bölümü)
  </read_first>
  <behavior>
    - saveWpCredentials('proj-123', 'https://ex.com', 'xxxx xxxx'): mevcut key varsa günceller, yoksa oluşturur. Hata fırlatır, null dönmez.
    - getWordPressCredentials('proj-123'): her iki key de mevcutsa { wpUrl, appPassword } döner; biri eksikse null döner.
    - hasWordPressCredentials('proj-123'): her iki key de mevcutsa true döner, yoksa false.
    - appPassword hiçbir console.log, error message veya return value içinde görünmez.
  </behavior>
  <action>
Mevcut vault.ts dosyasını oku, ardından dosyanın sonuna (getDataForSeoCredentials fonksiyonundan sonra) aşağıdaki 3 fonksiyonu ekle:

**saveWpCredentials:**
```typescript
// wp_url ve wp_app_password'u Vault'a kaydeder (upsert: varsa güncelle, yoksa oluştur)
// SECURITY: appPassword hiçbir zaman loglanmaz
export async function saveWpCredentials(
  projectId: string,
  wpUrl: string,
  appPassword: string
): Promise<void> {
  const urlKey = `wp_url_${projectId}`
  const passKey = `wp_app_password_${projectId}`

  // Her iki key için upsert: önce mevcut secret ID'yi ara
  async function upsertSecret(keyName: string, value: string): Promise<void> {
    const { data: existing } = await serviceClient
      .from('vault.secrets')
      .select('id')
      .eq('name', keyName)
      .maybeSingle()

    if (existing?.id) {
      const { error } = await serviceClient.rpc('vault.update_secret', {
        secret_id: existing.id,
        new_secret: value,
      })
      if (error) throw new Error(`Vault güncellenemedi: ${keyName}`)
    } else {
      const { error } = await serviceClient.rpc('vault.create_secret', {
        secret: value,
        name: keyName,
      })
      if (error) throw new Error(`Vault'a yazılamadı: ${keyName}`)
    }
  }

  await upsertSecret(urlKey, wpUrl)
  await upsertSecret(passKey, appPassword) // appPassword loglanmaz
}
```

**getWordPressCredentials:**
```typescript
// wp_url ve wp_app_password'u Vault'tan okur
// Her iki key de mevcutsa { wpUrl, appPassword } döner; biri eksikse null döner
export async function getWordPressCredentials(
  projectId: string
): Promise<{ wpUrl: string; appPassword: string } | null> {
  const urlKey = `wp_url_${projectId}`
  const passKey = `wp_app_password_${projectId}`

  const { data, error } = await serviceClient
    .from('vault.decrypted_secrets')
    .select('name, decrypted_secret')
    .in('name', [urlKey, passKey])

  if (error || !data?.length) return null

  const urlRow = data.find((s: { name: string; decrypted_secret: string }) => s.name === urlKey)
  const passRow = data.find((s: { name: string; decrypted_secret: string }) => s.name === passKey)
  if (!urlRow || !passRow) return null

  return { wpUrl: urlRow.decrypted_secret, appPassword: passRow.decrypted_secret }
}
```

**hasWordPressCredentials:**
```typescript
// Her iki WP key de Vault'ta mevcutsa true döner (bağlantı durumu badge'i için)
export async function hasWordPressCredentials(projectId: string): Promise<boolean> {
  const creds = await getWordPressCredentials(projectId)
  return creds !== null
}
```

`import 'server-only'` dosyanın en üstünde zaten mevcuttur — tekrar ekleme.
  </action>
  <verify>
    <automated>grep -c "export async function getWordPressCredentials" "C:/Users/baris/Documents/seo-machine/src/lib/supabase/vault.ts"</automated>
  </verify>
  <done>vault.ts 3 yeni export içerir: saveWpCredentials, getWordPressCredentials, hasWordPressCredentials. 'server-only' import korunur. appPassword hiçbir string interpolasyonda veya throw mesajında geçmez.</done>
</task>

<task type="auto">
  <name>Task 2: projeler/[id]/actions.ts — saveWordPressCredentials action ekle</name>
  <files>src/app/(dashboard)/projeler/[id]/actions.ts</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/actions.ts (mevcut dosyanın tamamı)
    - src/lib/supabase/vault.ts (Task 1'de eklenen saveWpCredentials imzası)
    - .planning/phases/13-wordpress-publishing/13-CONTEXT.md (D-01 — action contract)
    - .planning/phases/13-wordpress-publishing/13-PATTERNS.md (saveWordPressCredentials imzası)
  </read_first>
  <action>
Mevcut `src/app/(dashboard)/projeler/[id]/actions.ts` dosyasını oku. Dosyanın üstündeki import bloğuna `saveWpCredentials` import'unu ekle, ardından dosyanın sonuna aşağıdaki kodu ekle:

**Import eklemesi (mevcut 'use server' + revalidatePath + createClient import'larının altına):**
```typescript
import { saveWpCredentials } from '@/lib/supabase/vault'
```

**Yeni type + action (dosyanın sonuna):**
```typescript
export type SaveWpCredentialsResult =
  | { success: true }
  | { success: false; error: string }

export async function saveWordPressCredentials(
  projectId: string,
  wpUrl: string,
  appPassword: string
): Promise<SaveWpCredentialsResult> {
  // Client-side validation zaten yapılmış ama server'da da doğrula
  if (!wpUrl || !wpUrl.startsWith('https://')) {
    return { success: false, error: 'Geçerli bir WordPress URL\'si girin (https:// ile başlamalı).' }
  }
  if (!appPassword || appPassword.trim().length === 0) {
    return { success: false, error: 'Uygulama Şifresi gereklidir.' }
  }

  const supabase = await createClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Oturum bulunamadı.' }

  // Proje sahipliği doğrula
  const { data: project } = await supabase
    .from('projects')
    .select('id')
    .eq('id', projectId)
    .eq('user_id', user.id)
    .single()
  if (!project) return { success: false, error: 'Proje bulunamadı.' }

  try {
    // SECURITY: appPassword vault.ts'e iletilir — loglanmaz, response'ta dönmez
    await saveWpCredentials(projectId, wpUrl, appPassword)
  } catch {
    return { success: false, error: 'WordPress bağlantısı kaydedilemedi. Lütfen tekrar deneyin.' }
  }

  revalidatePath(`/projeler/${projectId}`)
  return { success: true }
}
```
  </action>
  <verify>
    <automated>grep -c "export async function saveWordPressCredentials" "C:/Users/baris/Documents/seo-machine/src/app/(dashboard)/projeler/[id]/actions.ts"</automated>
  </verify>
  <done>actions.ts'te saveWordPressCredentials export'u mevcuttur. SaveWpCredentialsResult tipi export edilir. Fonksiyon appPassword'u loglamaz, hata mesajlarında göstermez. URL https:// validation ve appPassword boşluk validation içerir.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → vault.ts | appPassword server action'dan vault.ts'e geçer; client bundle'a sızmaz |
| vault.ts → Supabase Vault | serviceClient ile SUPABASE_SERVICE_ROLE_KEY kullanılır; NEXT_PUBLIC_ değil |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Information Disclosure | vault.ts saveWpCredentials | mitigate | appPassword hiçbir error message veya log içinde geçmez; catch bloğu sadece generic mesaj döner |
| T-13-02-02 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | vault.ts'te import 'server-only' ile client bundle'a sızma engellenir; NEXT_PUBLIC_ prefix yasak |
| T-13-02-03 | Spoofing | saveWordPressCredentials action | mitigate | getUser() + eq('user_id', user.id) ownership check — başka kullanıcının projesine yazılamaz |
| T-13-02-04 | Elevation of Privilege | vault.ts serviceClient | mitigate | serviceClient yalnızca vault.ts içinde tanımlanır; diğer dosyalar import edemez (server-only) |
</threat_model>

<verification>
1. `grep "export async function saveWpCredentials" src/lib/supabase/vault.ts` — 1 sonuç
2. `grep "export async function getWordPressCredentials" src/lib/supabase/vault.ts` — 1 sonuç
3. `grep "export async function hasWordPressCredentials" src/lib/supabase/vault.ts` — 1 sonuç
4. `grep "appPassword" src/lib/supabase/vault.ts` — appPassword hiçbir console/throw mesajında geçmez
5. `grep "export async function saveWordPressCredentials" src/app/(dashboard)/projeler/[id]/actions.ts` — 1 sonuç
6. `grep "import 'server-only'" src/lib/supabase/vault.ts` — 1 sonuç (korunur)
</verification>

<success_criteria>
- vault.ts: 3 yeni export (saveWpCredentials, getWordPressCredentials, hasWordPressCredentials)
- actions.ts: saveWordPressCredentials + SaveWpCredentialsResult export
- appPassword hiçbir hata mesajında veya log satırında görünmez
- 'server-only' import vault.ts'te korunur
- URL https:// validasyonu server-side yapılır
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/13-wordpress-publishing/13-02-SUMMARY.md` oluştur.
</output>
