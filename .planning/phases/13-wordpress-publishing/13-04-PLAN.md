---
phase: 13-wordpress-publishing
plan: 04
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
files_modified:
  - src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts
autonomous: true
requirements:
  - PUBL-02
  - PUBL-03
  - PUBL-04
must_haves:
  truths:
    - "publishToWordPress action paket sahipliğini ve 'locked' durumunu doğrular"
    - "WordPress REST API'ye Basic Auth ile POST yapılır"
    - "Plugin tespiti yapılır (Yoast / RankMath / native) ve doğru meta key'ler seçilir"
    - "Başarılı gönderim sonrası wp_post_id, wp_post_url, wp_status, wp_published_at page_packages'a yazılır"
    - "wp_app_password Basic Auth header'ında kullanılır, loglanmaz"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts"
      provides: "publishToWordPress server action"
      exports: ["publishToWordPress", "PublishResult"]
  key_links:
    - from: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts"
      to: "src/lib/supabase/vault.ts"
      via: "import { getWordPressCredentials }"
      pattern: "getWordPressCredentials"
    - from: "publishToWordPress"
      to: "public.page_packages"
      via: "supabase UPDATE wp_post_id, wp_post_url, wp_status, wp_published_at"
      pattern: "wp_post_id"
---

<objective>
`publishToWordPress` server action'ını oluştur: Vault'tan WP credentials okur, plugin algılar (Yoast/RankMath/native), WordPress REST API'ye HTML içeriği + SEO meta + schema JSON-LD ile POST eder, gönderim sonucunu page_packages'a kaydeder.

Purpose: PUBL-02, PUBL-03, PUBL-04 — asıl publish akışının backend mantığı. Plan 05'teki PublishDialog bu action'ı çağırır.
Output: `sayfa-paketi/actions.ts` genişletilmiş — publishToWordPress + PublishResult tipi.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-wordpress-publishing/13-CONTEXT.md
@.planning/phases/13-wordpress-publishing/13-PATTERNS.md

<interfaces>
<!-- Plan 01'de oluşturulan kolonlar (DB'de mevcut): -->
page_packages:
  wp_post_id       INTEGER
  wp_post_url      TEXT
  wp_published_at  TIMESTAMPTZ
  wp_status        TEXT  -- 'publish' | 'draft'

<!-- Plan 02'de eklenen vault fonksiyonu: -->
From src/lib/supabase/vault.ts:
```typescript
export async function getWordPressCredentials(
  projectId: string
): Promise<{ wpUrl: string; appPassword: string } | null>
```

<!-- Mevcut sayfa-paketi/actions.ts'ten verifyOwnership helper: -->
From src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts:
```typescript
async function verifyOwnership(supabase, projectId: string, userId: string) {
  const { data } = await supabase
    .from('projects')
    .select('id')
    .eq('id', projectId)
    .eq('user_id', userId)
    .single()
  return data
}
```

<!-- CONTEXT.md D-03 — Plugin tespiti ve meta key seçimi: -->
GET /wp-json/wp/v2/plugins (Application Password ile)
Plugin tespiti:
  Yoast SEO:  plugin slug 'wordpress-seo/wp-seo.php' içerir
    meta: { _yoast_wpseo_title, _yoast_wpseo_metadesc }
    schema: _yoast_wpseo_schema (JSON string)
  RankMath:   plugin slug 'seo-by-rank-math/rank-math.php' içerir
    meta: { rank_math_title, rank_math_description }
    schema: rank_math_schema (JSON string)
  Yoksa:
    meta: { description: meta_description }
    schema: skip

<!-- CONTEXT.md D-02, D-04 — WP REST POST payload: -->
POST /wp-json/wp/v2/posts
Body:
  title:   page_packages.seo_title (focus keyword fallback)
  content: page_packages.html_content
  status:  'publish' | 'draft'
  meta:    plugin bazlı meta key'ler
Response:
  { id: number, link: string, date: string }

<!-- CONTEXT.md D-03 — Basic Auth: -->
Authorization: Basic base64(username:application_password)
username = WordPress kullanıcı adı
NOT: WP Application Password'da kullanıcı adı wp_url'den türetilemez —
  appPassword zaten "user:password" formatında saklanabilir VEYA
  ayrı bir username field gerekebilir.
  ÇÖZÜM: appPassword alanını "username APP_PASSWORD" formatında saklama.
  Basit yaklaşım: wp_app_password'u doğrudan base64 encode et.
  WP REST API Application Password: Authorization: Basic base64(username:app_password)
  username WP admin username'i, app_password ise oluşturulan şifre.
  CONTEXT.md D-01'de username ayrı field yok — sadece appPassword.
  ÇÖZÜM: Vault'ta appPassword "username:app_password" formatında saklansın (örn: "admin:xxxx xxxx xxxx xxxx").
  Eğer ":" içermiyorsa: Basic auth sadece appPassword'u kullan (bazı WP kurulumlarında çalışır).
  Yani: authHeader = 'Basic ' + Buffer.from(creds.appPassword).toString('base64')
  Bu WP REST API Application Password'un desteklediği format: sadece app_password'u base64 encode etmek yeterli değil.
  GERÇEK FORMAT: WP Application Password, username:app_password base64 ister.
  KARAR: wpUrl'den username çekme (karmaşık). appPassword'u "username:app_password" olarak sakla.
  Form placeholder'ı bunu yansıtacak şekilde güncellenebilir — ama bu Plan 03'te zaten yapıldı.
  Şimdilik: appPassword vault key'i "admin:xxxx xxxx xxxx xxxx" formatında saklandığını varsay.
  Eğer ":" yoksa hata döndür: "Uygulama Şifresi 'kullaniciadi:sifre' formatında olmalı."

NOT ABOVE — basit yaklaşım:
  Kullanıcı appPassword alanına "kullaniciadi:xxxx xxxx xxxx" girer.
  Action bunu doğrudan base64 encode eder.
  Plan 05'te UI'da placeholder bu formatı gösterir.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: publishToWordPress server action ekle</name>
  <files>src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts (mevcut dosyanın tamamı — import bloğu + verifyOwnership helper)
    - .planning/phases/13-wordpress-publishing/13-CONTEXT.md (D-02, D-03, D-04 — tam publish akışı)
    - .planning/phases/13-wordpress-publishing/13-PATTERNS.md (publishToWordPress imzası + External fetch pattern)
  </read_first>
  <action>
Mevcut `src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` dosyasını oku. Dosyanın üstündeki import bloğuna ekleme yap, ardından dosyanın sonuna aşağıdaki kodu ekle:

**Import eklemesi (mevcut import bloğunun sonuna):**
```typescript
import { getWordPressCredentials } from '@/lib/supabase/vault'
```

**Yeni type + action (dosyanın sonuna):**
```typescript
export type PublishResult =
  | { success: true; wpPostId: number; wpPostUrl: string; wpStatus: string }
  | { success: false; error: string }

// WordPress plugin tespiti için yardımcı
type WpPlugin = { plugin: string; status: string }

function detectSeoPlugin(plugins: WpPlugin[]): 'yoast' | 'rankmath' | 'none' {
  const slugs = plugins.map((p) => p.plugin)
  if (slugs.some((s) => s.includes('wordpress-seo'))) return 'yoast'
  if (slugs.some((s) => s.includes('rank-math'))) return 'rankmath'
  return 'none'
}

// Plugin tipine göre WP REST meta payload oluştur
function buildMetaPayload(
  plugin: 'yoast' | 'rankmath' | 'none',
  seoTitle: string | null,
  metaDescription: string | null,
  schemaJsonld: unknown | null
): Record<string, string> {
  const meta: Record<string, string> = {}

  if (plugin === 'yoast') {
    if (seoTitle) meta['_yoast_wpseo_title'] = seoTitle
    if (metaDescription) meta['_yoast_wpseo_metadesc'] = metaDescription
    if (schemaJsonld) meta['_yoast_wpseo_schema'] = JSON.stringify(schemaJsonld)
  } else if (plugin === 'rankmath') {
    if (seoTitle) meta['rank_math_title'] = seoTitle
    if (metaDescription) meta['rank_math_description'] = metaDescription
    if (schemaJsonld) meta['rank_math_schema'] = JSON.stringify(schemaJsonld)
  } else {
    // Plugin yok — WP native meta.description
    if (metaDescription) meta['description'] = metaDescription
    // Schema skip (native WP desteklemez)
  }

  return meta
}

export async function publishToWordPress(
  projectId: string,
  pageId: string,
  status: 'publish' | 'draft'
): Promise<PublishResult> {
  const supabase = await createClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Oturum bulunamadı.' }

  // Proje sahipliği doğrula
  const project = await verifyOwnership(supabase, projectId, user.id)
  if (!project) return { success: false, error: 'Proje bulunamadı.' }

  // Paket sahipliği + gerekli veri
  const { data: pkg } = await supabase
    .from('page_packages')
    .select('id, status, seo_title, html_content, meta_description, schema_jsonld')
    .eq('page_id', pageId)
    .eq('project_id', projectId)
    .eq('user_id', user.id)
    .maybeSingle()

  if (!pkg) return { success: false, error: 'Sayfa paketi bulunamadı.' }
  if (pkg.status !== 'locked') return { success: false, error: 'Paket kilitli değil. Önce paketi kilitleyin.' }
  if (!pkg.html_content) return { success: false, error: 'HTML içerik henüz oluşturulmamış. İçerik Stüdyosunu tamamlayın.' }

  // Vault'tan WP credentials al
  const creds = await getWordPressCredentials(projectId)
  if (!creds) {
    return { success: false, error: 'WordPress kimlik bilgileri bulunamadı. Proje ayarlarından ekleyin.' }
  }

  // Basic Auth header
  // appPassword formatı: "kullaniciadi:uygulama_sifresi"
  // Eğer ":" içermiyorsa formatı geçersiz say
  if (!creds.appPassword.includes(':')) {
    return {
      success: false,
      error: 'Uygulama Şifresi geçersiz format. "kullaniciadi:sifre" formatında kaydedin.',
    }
  }
  const authHeader = 'Basic ' + Buffer.from(creds.appPassword).toString('base64')

  const apiBase = creds.wpUrl.replace(/\/$/, '') + '/wp-json/wp/v2'

  // Plugin algıla (her gönderimde — cache yok, D-03)
  let detectedPlugin: 'yoast' | 'rankmath' | 'none' = 'none'
  try {
    const pluginsRes = await fetch(`${apiBase}/plugins`, {
      headers: {
        Authorization: authHeader,
        'Content-Type': 'application/json',
      },
    })
    if (pluginsRes.ok) {
      const plugins: WpPlugin[] = await pluginsRes.json()
      detectedPlugin = detectSeoPlugin(plugins)
    }
    // Plugin endpoint 403 dönebilir (yeterli yetki yoksa) — native'e düş
  } catch {
    // Ağ hatası — native'e düş
    detectedPlugin = 'none'
  }

  // Meta payload oluştur
  const meta = buildMetaPayload(
    detectedPlugin,
    pkg.seo_title,
    pkg.meta_description,
    pkg.schema_jsonld
  )

  // WordPress'e POST
  let wpPostId: number
  let wpPostUrl: string

  try {
    const postRes = await fetch(`${apiBase}/posts`, {
      method: 'POST',
      headers: {
        Authorization: authHeader, // SECURITY: loglanmaz
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        title: pkg.seo_title ?? '',
        content: pkg.html_content,
        status,
        meta,
      }),
    })

    if (!postRes.ok) {
      if (postRes.status === 401 || postRes.status === 403) {
        return {
          success: false,
          error: 'WordPress bağlantısı kurulamadı. URL ve Uygulama Şifresini kontrol edin.',
        }
      }
      return {
        success: false,
        error: 'Gönderim sırasında hata oluştu. Tekrar deneyin.',
      }
    }

    const wpPost = await postRes.json()
    wpPostId = wpPost.id
    wpPostUrl = wpPost.link
  } catch {
    return {
      success: false,
      error: 'WordPress\'e bağlanılamadı. İnternet bağlantınızı ve site URL\'sini kontrol edin.',
    }
  }

  // page_packages güncelle
  const { error: updateError } = await supabase
    .from('page_packages')
    .update({
      wp_post_id: wpPostId,
      wp_post_url: wpPostUrl,
      wp_status: status,
      wp_published_at: new Date().toISOString(),
      updated_at: new Date().toISOString(),
    })
    .eq('id', pkg.id)
    .eq('user_id', user.id)

  if (updateError) {
    // WP'de başarılı ama DB'ye yazılamadı — partial success, wpPostId döndür
    return {
      success: false,
      error: `WordPress'e gönderildi (Post ID: ${wpPostId}) ancak durum kaydedilemedi. Lütfen destek alın.`,
    }
  }

  revalidatePath(`/projeler/${projectId}/icerik-studio/${pageId}`)
  revalidatePath(`/projeler/${projectId}/sayfa-paketi`)

  return { success: true, wpPostId, wpPostUrl, wpStatus: status }
}
```
  </action>
  <verify>
    <automated>grep -c "export async function publishToWordPress" "C:/Users/baris/Documents/seo-machine/src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts"</automated>
  </verify>
  <done>publishToWordPress export mevcuttur. PublishResult tipi export edilmiştir. getWordPressCredentials import'u mevcut. authHeader console.log veya hata mesajında görünmez. Başarı durumunda wp_post_id, wp_post_url, wp_status, wp_published_at DB'ye yazılır.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → WordPress REST API | Basic Auth header'ı burada oluşturulur ve gönderilir |
| WordPress REST API → page_packages | WP response'tan alınan id ve link DB'ye yazılır |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Information Disclosure | authHeader | mitigate | authHeader hiçbir console.log, throw mesajı veya return value içinde geçmez; catch blokları generic mesaj döner |
| T-13-04-02 | Information Disclosure | WP API response | mitigate | Sadece id ve link alanları kullanılır; tam response body loglanmaz |
| T-13-04-03 | Spoofing | publishToWordPress | mitigate | getUser() + verifyOwnership() + eq('user_id', user.id) page_packages query'sinde — triple ownership check |
| T-13-04-04 | Elevation of Privilege | pkg.status !== 'locked' | mitigate | Kilitli olmayan paket publish edilemez — locked guard zorunlu |
| T-13-04-05 | Denial of Service | WP API fetch | accept | Timeout yok (Next.js default 30s); tek kullanıcılı internal tool — kabul edilebilir |
| T-13-04-06 | Tampering | wpPostId DB write | mitigate | .eq('id', pkg.id).eq('user_id', user.id) — başka kullanıcının paketi güncellenemez |
</threat_model>

<verification>
1. `grep "export async function publishToWordPress" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` — 1 sonuç
2. `grep "export type PublishResult" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` — 1 sonuç
3. `grep "getWordPressCredentials" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` — 2 satır (import + kullanım)
4. `grep "authHeader" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` ile catch/log kombinasyonu: authHeader console veya error'a geçmez
5. `grep "wp_post_id" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` — UPDATE satırında mevcut
6. `grep "wp_published_at" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` — UPDATE satırında mevcut
</verification>

<success_criteria>
- publishToWordPress ve PublishResult sayfa-paketi/actions.ts'ten export edilir
- Basic Auth header oluşturulur ama loglanmaz
- Plugin tespiti (Yoast/RankMath/none) çalışır
- Başarılı gönderimde wp_post_id, wp_post_url, wp_status, wp_published_at DB'ye yazılır
- Locked guard — kilitli olmayan paket için { success: false } döner
- Credential eksikliği için anlamlı Türkçe hata mesajı döner
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/13-wordpress-publishing/13-04-SUMMARY.md` oluştur.
</output>
