---
phase: 13-wordpress-publishing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260426000002_add_wp_columns.sql
autonomous: true
requirements:
  - PUBL-04
must_haves:
  truths:
    - "page_packages tablosunda wp_post_id, wp_post_url, wp_published_at, wp_status kolonları mevcuttur"
    - "supabase db push başarıyla çalışır ve migration uygulanır"
  artifacts:
    - path: "supabase/migrations/20260426000002_add_wp_columns.sql"
      provides: "wp_* kolonlarını page_packages'a ekleyen ALTER TABLE migration"
      contains: "ADD COLUMN IF NOT EXISTS wp_post_id"
  key_links:
    - from: "supabase/migrations/20260426000002_add_wp_columns.sql"
      to: "public.page_packages"
      via: "ALTER TABLE"
      pattern: "ADD COLUMN IF NOT EXISTS wp_post_id"
---

<objective>
WordPress yayın durumu takibi için page_packages tablosuna 4 yeni kolon ekleyen migration dosyasını oluştur ve veritabanına uygula.

Purpose: publishToWordPress action'ının WP post ID, URL, tarih ve yayın durumunu saklayabilmesi için schema altyapısını hazırlar. Bu kolonlar olmadan Plan 04'teki server action çalışamaz.
Output: `supabase/migrations/20260426000002_add_wp_columns.sql` dosyası + başarıyla uygulanmış migration.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-wordpress-publishing/13-CONTEXT.md
@.planning/phases/13-wordpress-publishing/13-PATTERNS.md

<interfaces>
<!-- Analog migration (Phase 12) — aynı pattern uygulanacak -->
From supabase/migrations/20260426000001_add_content_studio_columns.sql:
```sql
ALTER TABLE public.page_packages
  ADD COLUMN IF NOT EXISTS content_sections JSONB,
  ADD COLUMN IF NOT EXISTS html_content     TEXT;
```

<!-- CONTEXT.md D-04 ve Data Model Changes bölümünden — tam kolon listesi: -->
ALTER TABLE public.page_packages:
  wp_post_id       INTEGER   -- WordPress post ID (successful publish'ten sonra)
  wp_post_url      TEXT      -- WordPress'teki sayfanın public URL'si
  wp_published_at  TIMESTAMPTZ -- Publish/draft kayıt zaman damgası
  wp_status        TEXT      -- 'publish' | 'draft'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration SQL dosyasını oluştur</name>
  <files>supabase/migrations/20260426000002_add_wp_columns.sql</files>
  <read_first>
    - supabase/migrations/20260426000001_add_content_studio_columns.sql (exact analog pattern)
    - supabase/migrations/20260425000001_add_schema_jsonld.sql (yorum stili referansı)
  </read_first>
  <action>
Aşağıdaki içerikle `supabase/migrations/20260426000002_add_wp_columns.sql` dosyasını oluştur:

```sql
-- Phase 13: WordPress Publishing — publish result tracking
-- Adds four columns to page_packages:
--   wp_post_id INTEGER: WordPress post ID after successful publish
--   wp_post_url TEXT: Public URL of the WordPress post
--   wp_published_at TIMESTAMPTZ: Timestamp of publish/draft save
--   wp_status TEXT: 'publish' | 'draft'
ALTER TABLE public.page_packages
  ADD COLUMN IF NOT EXISTS wp_post_id       INTEGER,
  ADD COLUMN IF NOT EXISTS wp_post_url      TEXT,
  ADD COLUMN IF NOT EXISTS wp_published_at  TIMESTAMPTZ,
  ADD COLUMN IF NOT EXISTS wp_status        TEXT;
```

Dosya başka hiçbir şey içermez. IF NOT EXISTS kullanımı zorunlu — idempotency garantisi sağlar.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS wp_post_id" "C:/Users/baris/Documents/seo-machine/supabase/migrations/20260426000002_add_wp_columns.sql"</automated>
  </verify>
  <done>Migration dosyası 4 ADD COLUMN IF NOT EXISTS satırı içerir: wp_post_id, wp_post_url, wp_published_at, wp_status</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] supabase db push — migration'ı uygula</name>
  <files></files>
  <read_first>
    - supabase/migrations/20260426000002_add_wp_columns.sql (Task 1'de oluşturulan dosya)
  </read_first>
  <action>
Proje kök dizininde aşağıdaki komutu çalıştır:

```bash
cd C:/Users/baris/Documents/seo-machine && npx supabase db push
```

Başarılı çıktı beklentisi: "Applying migration 20260426000002_add_wp_columns.sql" mesajı veya "No migrations to apply" (zaten uygulandıysa).

Eğer "Do you want to push these migrations?" gibi interaktif soru gelirse: `--yes` flag ekleyerek tekrar çalıştır:
```bash
npx supabase db push --yes
```

Eğer authentication hatası alınırsa (token expired), kullanıcıya `npx supabase login` çalıştırmasını bildir ve `autonomous: false` ile işaretle.
  </action>
  <verify>
    <automated>cd C:/Users/baris/Documents/seo-machine && npx supabase db push --dry-run 2>&1 | head -5</automated>
  </verify>
  <done>supabase db push hatasız tamamlanır; remote DB'de page_packages.wp_post_id kolonu mevcuttur</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → Database | SQL migration dosyası Supabase remote DB'ye push edilir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering | migration SQL | mitigate | IF NOT EXISTS kullanılır — idempotent; manuel edit koruması yok ama CI/CD dışı araç yok |
| T-13-01-02 | Information Disclosure | supabase db push output | accept | Push çıktısı sadece migration adını loglar; kolon içerikleri sıfır, credential exposure yok |
</threat_model>

<verification>
1. `supabase/migrations/20260426000002_add_wp_columns.sql` dosyası mevcut
2. Dosya 4 ADD COLUMN satırı içerir: wp_post_id, wp_post_url, wp_published_at, wp_status
3. `supabase db push` başarıyla tamamlanmış (çıktıda hata yok)
4. Remote DB'de `page_packages` tablosunda yeni kolonlar mevcut (Plan 04 bunu kullanır)
</verification>

<success_criteria>
- Migration dosyası `supabase/migrations/20260426000002_add_wp_columns.sql` mevcuttur
- `IF NOT EXISTS` ile tüm 4 kolon tanımlanmıştır (INTEGER, TEXT, TIMESTAMPTZ, TEXT)
- `supabase db push` sıfır hata ile tamamlanmıştır
- Bu planın artifact'ları Plan 04'ün `publishToWordPress` action'ına doğrudan hazır durumdadır
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/13-wordpress-publishing/13-01-SUMMARY.md` oluştur.
</output>
