---
phase: 13-wordpress-publishing
plan: 05
type: execute
wave: 3
depends_on:
  - 13-03
  - 13-04
files_modified:
  - src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/PublishDialog.tsx
  - src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/HtmlReadyBanner.tsx
  - src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/ContentStudioShell.tsx
  - src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/page.tsx
autonomous: true
requirements:
  - PUBL-02
  - PUBL-03
  - PUBL-04
must_haves:
  truths:
    - "Tüm bölümler onaylandığında HtmlReadyBanner'da 'WordPress'e Gönder' butonu görünür"
    - "PublishDialog açılınca 'Hemen Yayınla' varsayılan seçili gelir"
    - "Gönderim başarısında HtmlReadyBanner 'WordPress'te Yayında' / 'Taslak Olarak Kaydedildi' görünümüne geçer"
    - "WP bağlantısı yapılandırılmamışsa buton disabled ve tooltip gösterir"
    - "Gönderim sırasında dialog disabled + spinner görünür"
    - "icerik-studio/page.tsx wp_post_url ve wp_status alanlarını SELECT sorgusuna dahil eder"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/PublishDialog.tsx"
      provides: "PublishDialog client component"
      exports: ["PublishDialog"]
    - path: "src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/HtmlReadyBanner.tsx"
      provides: "Genişletilmiş HtmlReadyBanner — WP durumuna göre 3 farklı görünüm"
      exports: ["HtmlReadyBanner"]
    - path: "src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/ContentStudioShell.tsx"
      provides: "wp alanları prop'lar olarak HtmlReadyBanner'a geçer"
    - path: "src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/page.tsx"
      provides: "wp_post_url, wp_status, wp_post_id SELECT sorgusuna dahil"
  key_links:
    - from: "HtmlReadyBanner.tsx"
      to: "PublishDialog.tsx"
      via: "render (conditional)"
      pattern: "PublishDialog"
    - from: "PublishDialog.tsx"
      to: "sayfa-paketi/actions.ts"
      via: "import { publishToWordPress }"
      pattern: "publishToWordPress"
    - from: "ContentStudioShell.tsx"
      to: "HtmlReadyBanner.tsx"
      via: "props: projectId, pageId, wpPostUrl, wpStatus, isWpConfigured"
      pattern: "wpPostUrl"
---

<objective>
PublishDialog client component'ini oluştur, HtmlReadyBanner'ı WP durumuna göre 3 farklı görünüm verecek şekilde genişlet, ContentStudioShell'i wp prop'larını geçecek şekilde güncelle ve icerik-studio/page.tsx'e wp alanlarını SELECT sorgusuna ekle.

Purpose: PUBL-02, PUBL-03, PUBL-04 — kullanıcının publish akışını başlatabileceği ve sonucu görebileceği UI katmanı. Plan 04'teki publishToWordPress action'ının UI entry point'i.
Output: PublishDialog.tsx (yeni) + HtmlReadyBanner.tsx (genişletilmiş) + ContentStudioShell.tsx (prop geçişi) + icerik-studio/page.tsx (SELECT genişletme).
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-wordpress-publishing/13-CONTEXT.md
@.planning/phases/13-wordpress-publishing/13-PATTERNS.md
@.planning/phases/13-wordpress-publishing/13-UI-SPEC.md

<interfaces>
<!-- Plan 04'te eklenen action: -->
From src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts:
```typescript
export type PublishResult =
  | { success: true; wpPostId: number; wpPostUrl: string; wpStatus: string }
  | { success: false; error: string }

export async function publishToWordPress(
  projectId: string,
  pageId: string,
  status: 'publish' | 'draft'
): Promise<PublishResult>
```

<!-- Mevcut HtmlReadyBanner (genişletilecek): -->
From src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/HtmlReadyBanner.tsx:
```typescript
'use client'
import { HugeiconsIcon } from '@hugeicons/react'
import { Tick02Icon } from '@hugeicons/core-free-icons'
export function HtmlReadyBanner() {
  return (
    <div className="sticky bottom-0 z-10 mx-6 mb-6 p-4 rounded-lg bg-emerald-500/10 border border-emerald-500/30 flex items-center gap-3">
      <HugeiconsIcon icon={Tick02Icon} className="text-emerald-400 shrink-0" size={20} />
      <div>
        <p className="text-sm font-semibold text-emerald-400">HTML Çıktısı Hazır</p>
        <p className="text-sm text-muted-foreground">Tüm bölümler onaylandı. İçerik WordPress'e yayınlanmaya hazır.</p>
      </div>
    </div>
  )
}
```

<!-- Mevcut ContentStudioShell render kısmı (değiştirilecek satır): -->
{allApproved && <HtmlReadyBanner />}
// → yeni:
{allApproved && (
  <HtmlReadyBanner
    projectId={projectId}
    pageId={pageId}
    wpPostUrl={wpPostUrl}
    wpStatus={wpStatus}
    isWpConfigured={isWpConfigured}
  />
)}

<!-- ContentStudioShellProps genişletilecek: -->
type ContentStudioShellProps = {
  projectId: string
  pageId: string
  pageTitle: string
  pkg: PackageData  // wp_post_url, wp_status eklendi
  resolvedRules: Record<string, boolean>
  isWpConfigured: boolean  // YENİ
}

<!-- PackageData genişletilecek: -->
type PackageData = {
  // ... mevcut alanlar ...
  wp_post_url: string | null    // YENİ
  wp_status: string | null      // YENİ
}

<!-- icerik-studio/page.tsx SELECT (genişletilecek): -->
// Mevcut:
'id, status, seo_title, meta_description, h1, focus_keyword_id, search_intent, page_type, strategic_purpose, heading_hierarchy, content_sections, html_content'
// Yeni (wp alanları eklenir):
'id, status, seo_title, meta_description, h1, focus_keyword_id, search_intent, page_type, strategic_purpose, heading_hierarchy, content_sections, html_content, wp_post_url, wp_status, wp_post_id'

<!-- UI-SPEC kritik kurallar: -->
- DialogTrigger asChild YASAK — render prop kullanılır
- font-medium YASAK
- Badge variant prop YASAK — className ile renk
- Loading03Icon — Gönderiliyor spinner
- aria-busy="true" gönder butonunda
- fieldset + legend sr-only RadioGroup için
- Kopya: "WordPress'e Gönder", "Hemen Yayınla", "Taslak Kaydet", "Gönder", "Vazgeç"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PublishDialog.tsx — yeni client component oluştur</name>
  <files>src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/PublishDialog.tsx</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/ic-link-haritasi/AddLinkDialog.tsx (Dialog + state + submit pattern analog)
    - .planning/phases/13-wordpress-publishing/13-UI-SPEC.md (PublishDialog §3, Interaction Contracts §PublishDialog Akışı, Copywriting Contract)
    - .planning/phases/13-wordpress-publishing/13-PATTERNS.md (PublishDialog RadioGroup pattern)
  </read_first>
  <action>
Aşağıdaki içerikle `src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/PublishDialog.tsx` dosyasını oluştur:

```typescript
'use client'

import { useState } from 'react'
import { HugeiconsIcon } from '@hugeicons/react'
import { Loading03Icon } from '@hugeicons/core-free-icons'
import { Button } from '@/components/ui/button'
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogFooter,
} from '@/components/ui/dialog'
import { publishToWordPress } from '@/app/(dashboard)/projeler/[id]/sayfa-paketi/actions'

type Props = {
  projectId: string
  pageId: string
  open: boolean
  onOpenChange: (open: boolean) => void
  onSuccess: (wpPostUrl: string, wpStatus: string) => void
}

export function PublishDialog({ projectId, pageId, open, onOpenChange, onSuccess }: Props) {
  const [publishStatus, setPublishStatus] = useState<'publish' | 'draft'>('publish')
  const [isPending, setIsPending] = useState(false)
  const [error, setError] = useState<string | null>(null)

  const handleOpenChange = (isOpen: boolean) => {
    if (isPending) return // gönderim süresince kapatılamaz
    onOpenChange(isOpen)
    if (!isOpen) {
      setError(null)
      setPublishStatus('publish')
    }
  }

  const handleSubmit = async () => {
    setIsPending(true)
    setError(null)

    try {
      const result = await publishToWordPress(projectId, pageId, publishStatus)

      if (!result.success) {
        setError(result.error)
        setIsPending(false)
        return
      }

      onOpenChange(false)
      onSuccess(result.wpPostUrl, result.wpStatus)
    } catch {
      setError('Gönderim sırasında hata oluştu. Tekrar deneyin.')
      setIsPending(false)
    }
  }

  return (
    <Dialog open={open} onOpenChange={handleOpenChange}>
      <DialogContent className="max-w-sm">
        <DialogHeader>
          <DialogTitle>WordPress&apos;e Gönder</DialogTitle>
        </DialogHeader>

        {/* RadioGroup — erişilebilirlik için fieldset + legend */}
        <fieldset className="space-y-3 border-0 p-0 m-0">
          <legend className="sr-only">Yayın seçeneği</legend>

          {([
            {
              value: 'publish' as const,
              label: 'Hemen Yayınla',
              description: 'İçerik hemen yayınlanır.',
            },
            {
              value: 'draft' as const,
              label: 'Taslak Kaydet',
              description: 'WordPress taslak olarak kaydedilir, yayınlanmaz.',
            },
          ] as const).map((opt) => (
            <label
              key={opt.value}
              className="flex items-start gap-3 cursor-pointer"
            >
              <input
                type="radio"
                name="publish-status"
                value={opt.value}
                checked={publishStatus === opt.value}
                onChange={() => setPublishStatus(opt.value)}
                disabled={isPending}
                className="mt-0.5 shrink-0"
              />
              <div>
                <p className="text-sm font-semibold">{opt.label}</p>
                <p className="text-xs text-muted-foreground">{opt.description}</p>
              </div>
            </label>
          ))}
        </fieldset>

        {/* Hata mesajı */}
        {error && (
          <p className="text-xs text-destructive">{error}</p>
        )}

        <DialogFooter>
          <Button
            variant="outline"
            onClick={() => handleOpenChange(false)}
            disabled={isPending}
          >
            Vazgeç
          </Button>
          <Button
            onClick={handleSubmit}
            disabled={isPending}
            aria-busy={isPending}
            aria-label={isPending ? 'Gönderiliyor' : undefined}
          >
            {isPending ? (
              <>
                <HugeiconsIcon
                  icon={Loading03Icon}
                  size={16}
                  className="animate-spin mr-2"
                />
                Gönderiliyor...
              </>
            ) : error ? (
              'Tekrar Dene'
            ) : (
              'Gönder'
            )}
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  )
}
```
  </action>
  <verify>
    <automated>grep -c "export function PublishDialog" "C:/Users/baris/Documents/seo-machine/src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/PublishDialog.tsx"</automated>
  </verify>
  <done>PublishDialog.tsx mevcuttur. asChild kullanılmamış. font-medium yok. fieldset + legend sr-only erişilebilirlik sağlanmış. Loading03Icon spinner mevcut. aria-busy gönder butonunda var. Hata durumunda "Tekrar Dene" label'ı değişir.</done>
</task>

<task type="auto">
  <name>Task 2: HtmlReadyBanner.tsx + ContentStudioShell.tsx + icerik-studio/page.tsx — güncelle</name>
  <files>
    src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/HtmlReadyBanner.tsx,
    src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/ContentStudioShell.tsx,
    src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/page.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/HtmlReadyBanner.tsx (mevcut — tam dosya)
    - src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/ContentStudioShell.tsx (mevcut — tam dosya)
    - src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/page.tsx (mevcut — tam dosya)
    - .planning/phases/13-wordpress-publishing/13-UI-SPEC.md (Layout Contract §2 HtmlReadyBanner, State Machine §WP Yayın Durumu, Copywriting Contract)
    - .planning/phases/13-wordpress-publishing/13-PATTERNS.md (HtmlReadyBanner genişletme pattern)
  </read_first>
  <action>
**HtmlReadyBanner.tsx — tamamen yeniden yaz:**

Mevcut dosyayı oku, ardından aşağıdaki içerikle tamamen değiştir:

```typescript
'use client'

import { useState } from 'react'
import { HugeiconsIcon } from '@hugeicons/react'
import { Tick02Icon, Link03Icon } from '@hugeicons/core-free-icons'
import { Button } from '@/components/ui/button'
import { PublishDialog } from './PublishDialog'

type Props = {
  projectId: string
  pageId: string
  wpPostUrl?: string | null
  wpStatus?: string | null
  isWpConfigured: boolean
}

export function HtmlReadyBanner({
  projectId,
  pageId,
  wpPostUrl,
  wpStatus,
  isWpConfigured,
}: Props) {
  const [dialogOpen, setDialogOpen] = useState(false)
  const [localWpPostUrl, setLocalWpPostUrl] = useState<string | null>(wpPostUrl ?? null)
  const [localWpStatus, setLocalWpStatus] = useState<string | null>(wpStatus ?? null)

  const handlePublishSuccess = (url: string, status: string) => {
    setLocalWpPostUrl(url)
    setLocalWpStatus(status)
  }

  // Yayında
  if (localWpPostUrl && localWpStatus === 'publish') {
    return (
      <div className="sticky bottom-0 z-10 mx-6 mb-6 p-4 rounded-lg bg-emerald-500/10 border border-emerald-500/30 flex items-center gap-3">
        <HugeiconsIcon icon={Tick02Icon} className="text-emerald-400 shrink-0" size={20} />
        <div className="flex-1 min-w-0">
          <p className="text-sm font-semibold text-emerald-400">WordPress&apos;te Yayında</p>
          <p className="text-sm text-muted-foreground">
            <a
              href={localWpPostUrl}
              target="_blank"
              rel="noopener noreferrer"
              className="underline underline-offset-2 hover:text-foreground flex items-center gap-1"
              aria-label="WordPress'teki sayfayı yeni sekmede görüntüle"
            >
              Sayfayı Görüntüle
              <HugeiconsIcon icon={Link03Icon} size={14} className="shrink-0" />
            </a>
          </p>
        </div>
      </div>
    )
  }

  // Taslak
  if (localWpPostUrl && localWpStatus === 'draft') {
    return (
      <div className="sticky bottom-0 z-10 mx-6 mb-6 p-4 rounded-lg bg-slate-800/50 border border-slate-700 flex items-center gap-3">
        <HugeiconsIcon icon={Tick02Icon} className="text-slate-400 shrink-0" size={20} />
        <div className="flex-1 min-w-0">
          <p className="text-sm font-semibold text-slate-300">WordPress Taslak Olarak Kaydedildi</p>
          <p className="text-sm text-muted-foreground">
            <a
              href={localWpPostUrl}
              target="_blank"
              rel="noopener noreferrer"
              className="underline underline-offset-2 hover:text-foreground"
              aria-label="WordPress'teki sayfayı yeni sekmede görüntüle"
            >
              Sayfayı Görüntüle
            </a>
          </p>
        </div>
      </div>
    )
  }

  // Henüz gönderilmemiş — buton + dialog
  return (
    <>
      <div className="sticky bottom-0 z-10 mx-6 mb-6 p-4 rounded-lg bg-emerald-500/10 border border-emerald-500/30 flex items-center gap-3">
        <HugeiconsIcon icon={Tick02Icon} className="text-emerald-400 shrink-0" size={20} />
        <div className="flex-1 min-w-0">
          <p className="text-sm font-semibold text-emerald-400">HTML Çıktısı Hazır</p>
          <p className="text-sm text-muted-foreground">
            Tüm bölümler onaylandı. İçerik WordPress&apos;e yayınlanmaya hazır.
          </p>
        </div>
        <Button
          onClick={() => setDialogOpen(true)}
          disabled={!isWpConfigured}
          title={!isWpConfigured ? 'Önce WordPress bağlantısını yapılandırın' : undefined}
          className="shrink-0"
        >
          WordPress&apos;e Gönder
        </Button>
      </div>

      <PublishDialog
        projectId={projectId}
        pageId={pageId}
        open={dialogOpen}
        onOpenChange={setDialogOpen}
        onSuccess={handlePublishSuccess}
      />
    </>
  )
}
```

---

**ContentStudioShell.tsx — 3 değişiklik:**

1. `PackageData` tipine `wp_post_url` ve `wp_status` ekle:
```typescript
type PackageData = {
  id: string
  status: string
  seo_title: string | null
  meta_description: string | null
  h1: string | null
  focus_keyword_id: string | null
  search_intent: string | null
  page_type: string | null
  strategic_purpose: string | null
  heading_hierarchy: unknown
  content_sections: unknown
  html_content: string | null
  wp_post_url: string | null   // YENİ
  wp_status: string | null     // YENİ
}
```

2. `ContentStudioShellProps`'a `isWpConfigured: boolean` ekle:
```typescript
export type ContentStudioShellProps = {
  projectId: string
  pageId: string
  pageTitle: string
  pkg: PackageData
  resolvedRules: Record<string, boolean>
  isWpConfigured: boolean   // YENİ
}
```

3. Fonksiyon imzasına `isWpConfigured` ekle ve `HtmlReadyBanner` render satırını güncelle:
```typescript
export function ContentStudioShell({
  projectId,
  pageId,
  pageTitle,
  pkg,
  isWpConfigured,  // YENİ
}: ContentStudioShellProps) {
```

```typescript
// Mevcut satırı değiştir:
// {allApproved && <HtmlReadyBanner />}
// YENİ:
{allApproved && (
  <HtmlReadyBanner
    projectId={projectId}
    pageId={pageId}
    wpPostUrl={pkg.wp_post_url}
    wpStatus={pkg.wp_status}
    isWpConfigured={isWpConfigured}
  />
)}
```

---

**icerik-studio/page.tsx — 3 değişiklik:**

1. `PackageData` tipine `wp_post_url` ve `wp_status` ekle:
```typescript
type PackageData = {
  // ... mevcut alanlar ...
  wp_post_url: string | null   // YENİ
  wp_status: string | null     // YENİ
}
```

2. Supabase SELECT sorgusunu genişlet (mevcut string'e ekle):
```typescript
'id, status, seo_title, meta_description, h1, focus_keyword_id, search_intent, page_type, strategic_purpose, heading_hierarchy, content_sections, html_content, wp_post_url, wp_status, wp_post_id'
```

3. `hasWordPressCredentials` import + SSR çağrısı ekle, ContentStudioShell'e `isWpConfigured` prop geç:
```typescript
// Import bloğuna (mevcut import'ların altına):
import { hasWordPressCredentials } from '@/lib/supabase/vault'

// Paket sorgusu yapıldıktan sonra (pkg null check'inden önce):
const isWpConfigured = await hasWordPressCredentials(id)

// ContentStudioShell render'ında:
<ContentStudioShell
  projectId={id}
  pageId={pageId}
  pageTitle={page.title}
  pkg={pkg as PackageData}
  resolvedRules={resolvedRules}
  isWpConfigured={isWpConfigured}  // YENİ
/>
```
  </action>
  <verify>
    <automated>grep -c "export function HtmlReadyBanner" "C:/Users/baris/Documents/seo-machine/src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/HtmlReadyBanner.tsx"</automated>
  </verify>
  <done>HtmlReadyBanner.tsx 3 farklı render durumu içerir (yayında / taslak / henüz gönderilmemiş). ContentStudioShell.tsx isWpConfigured prop'u alır ve HtmlReadyBanner'a geçer. icerik-studio/page.tsx SELECT sorgusunda wp_post_url ve wp_status vardır. font-medium hiçbir yerde kullanılmamıştır.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Component → Server Action | PublishDialog, publishToWordPress'i client'tan çağırır |
| SSR → Vault | hasWordPressCredentials boolean döner; credential expose etmez |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | Information Disclosure | PublishDialog | mitigate | publishToWordPress server action — credentials client bundle'da yok; PublishDialog sadece projectId/pageId/status geçer |
| T-13-05-02 | Tampering | HtmlReadyBanner localState | accept | localWpPostUrl/localWpStatus client local state — revalidatePath ile SSR güncellenir; optimistic update, risk yok |
| T-13-05-03 | Information Disclosure | "Sayfayı Görüntüle" linki | accept | wp_post_url zaten WordPress'te public — expose etmek sorun değil |
| T-13-05-04 | Spoofing | projectId/pageId in PublishDialog | mitigate | Plan 04'te verifyOwnership + getUser() server-side guard mevcut |
| T-13-05-05 | Information Disclosure | wp_post_id SELECT | accept | WP post ID public bilgi; schema içermiyor |
</threat_model>

<verification>
1. `grep "export function PublishDialog" src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/PublishDialog.tsx` — 1 sonuç
2. `grep "Loading03Icon" src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/PublishDialog.tsx` — 1 sonuç (spinner)
3. `grep "fieldset" src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/PublishDialog.tsx` — 1 sonuç
4. `grep "asChild" src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/PublishDialog.tsx` — 0 sonuç (yasak)
5. `grep "font-medium" src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/HtmlReadyBanner.tsx` — 0 sonuç
6. `grep "wp_post_url" src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/page.tsx` — SELECT sorgusunda mevcut
7. `grep "isWpConfigured" src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/components/ContentStudioShell.tsx` — prop tanımında ve HtmlReadyBanner'a geçişte mevcut
8. `grep "hasWordPressCredentials" src/app/(dashboard)/projeler/[id]/icerik-studio/[pageId]/page.tsx` — 1 SSR çağrısı
</verification>

<success_criteria>
- PublishDialog.tsx mevcuttur, Dialog + RadioGroup + spinner + hata durumu tam implementeddir
- HtmlReadyBanner.tsx 3 render state içerir: henüz gönderilmemiş / taslak / yayında
- WP bağlantısı yoksa "WordPress'e Gönder" butonu disabled
- ContentStudioShell isWpConfigured prop'unu HtmlReadyBanner'a geçer
- icerik-studio/page.tsx wp_post_url, wp_status SELECT sorgusunda ve hasWordPressCredentials SSR çağrısında
- font-medium hiçbir dosyada kullanılmamış
- asChild hiçbir yerde kullanılmamış
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/13-wordpress-publishing/13-05-SUMMARY.md` oluştur.
</output>
