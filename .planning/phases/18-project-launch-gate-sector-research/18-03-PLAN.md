---
phase: 18
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
  - 18-02
files_modified:
  - src/app/(dashboard)/projeler/[id]/ProjectInfoSection.tsx
  - src/app/(dashboard)/projeler/[id]/page.tsx
  - src/app/(dashboard)/projeler/[id]/arastirma/page.tsx
  - src/app/(dashboard)/projeler/[id]/arastirma/ResearchRerunButton.tsx
autonomous: true
requirements:
  - PROJ-06
  - PROJ-07
  - SRCH-03

user_setup:
  - service: serpapi
    why: "Sektör araştırması için Google arama sonuçları"
    env_vars:
      - name: SERPAPI_KEY
        source: "https://serpapi.com/manage-api-key — üye ol, API key kopyala, .env.local dosyasına SERPAPI_KEY=xxx ekle"
  - service: anthropic
    why: "Araştırma raporunu analiz eden AI modeli"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "https://console.anthropic.com/settings/keys — API key oluştur, .env.local dosyasına ANTHROPIC_API_KEY=sk-ant-xxx ekle"

must_haves:
  truths:
    - "ProjectInfoSection'da sector, initial_competitors, target_keywords alanlarından biri boşken buton disabled görünür"
    - "Disabled butonun altında 'Eksik: sektör, rakipler' gibi inline hint gösterilir"
    - "Tüm 3 alan doluyken buton aktif hale gelir ve tıklanabilir"
    - "Butona tıklanınca loading spinner ve 'Araştırılıyor...' etiketi görünür, aynı sayfada kalınır"
    - "Araştırma tamamlanınca tamamlandı bloğu (emerald renkli) butonun yerini alır ve /arastirma linkini gösterir"
    - "Araştırma daha önce tamamlanmışsa (research_reports dolu) sayfa yüklenişinde tamamlandı bloğu görünür"
    - "/arastirma sayfasında 'Yeniden Araştır' butonu h1 yanında sağda yer alır"
    - "Hata durumunda toast ile bildirim verilir, buton enabled state'e döner"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/ProjectInfoSection.tsx"
      provides: "Launch gate butonu + disabled hint + completed block"
      contains: "canLaunch"
    - path: "src/app/(dashboard)/projeler/[id]/arastirma/ResearchRerunButton.tsx"
      provides: "Yeniden Araştır client component"
      exports: ["ResearchRerunButton"]
    - path: "src/app/(dashboard)/projeler/[id]/arastirma/page.tsx"
      provides: "ResearchRerunButton entegrasyonu + project query genişletmesi"
      contains: "ResearchRerunButton"
    - path: "src/app/(dashboard)/projeler/[id]/page.tsx"
      provides: "hasResearch prop ile ProjectInfoSection'a araştırma durumu aktarımı"
      contains: "hasResearch"
  key_links:
    - from: "ProjectInfoSection.tsx"
      to: "/api/research/trigger"
      via: "fetch('/api/research/trigger', { method: 'POST', body: JSON.stringify({ projectId, userId }) })"
      pattern: "api/research/trigger"
    - from: "page.tsx"
      to: "research_reports"
      via: "supabase.from('research_reports').select('section').eq('project_id', id).limit(1)"
      pattern: "research_reports.*select"
---

<objective>
Launch Gate UI ve "Yeniden Araştır" butonunun oluşturulması — kullanıcıya görünen tüm araştırma arayüzü.

Purpose: Proje bilgilerindeki gate logic ile araştırma tetikleme ve durum gösterimi; /arastirma sayfasına yeniden araştırma butonu.
Output: ProjectInfoSection.tsx güncellemesi + page.tsx query güncellemesi + ResearchRerunButton.tsx + arastirma/page.tsx güncellemesi.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-project-launch-gate-sector-research/18-CONTEXT.md
@.planning/phases/18-project-launch-gate-sector-research/18-UI-SPEC.md
@.planning/phases/18-project-launch-gate-sector-research/18-PATTERNS.md

<interfaces>
<!-- ProjectInfoSection.tsx — mevcut Props tipi ve state yapısı -->
From src/app/(dashboard)/projeler/[id]/ProjectInfoSection.tsx:
```typescript
type Props = {
  projectId: string
  initialData: ProjectData
}

export function ProjectInfoSection({ projectId, initialData }: Props)
// data state: const [data, setData] = useState<ProjectData>(initialData)
// data['sector'], data['initial_competitors'], data['target_keywords'] — gate için kullanılacak
```

<!-- page.tsx — mevcut select query -->
From src/app/(dashboard)/projeler/[id]/page.tsx:
```typescript
const { data: project } = await supabase
  .from('projects')
  .select(
    'id, name, domain, sector, target_country, target_language, business_model, site_type, brand_tone, target_customer, main_goal, initial_competitors, notes, custom_rules, target_keywords, created_at, gsc_property_url, import_status, import_current, import_total, import_completed_at'
  )
  .eq('id', id)
  .eq('user_id', user.id)
  .single()

// ProjectInfoSection'a şu şekilde geçiliyor:
<ProjectInfoSection
  projectId={project.id}
  initialData={{ name, domain, sector, ..., initial_competitors, ..., target_keywords }}
/>
```

<!-- arastirma/page.tsx — mevcut SSR yapısı -->
From src/app/(dashboard)/projeler/[id]/arastirma/page.tsx:
```typescript
// h1 bloğu — ResearchRerunButton buraya eklenecek:
<div className="p-8 pb-4">
  <Link href={`/projeler/${id}`} ...>← {project.name}</Link>
  <h1 className="text-xl font-semibold">Araştırma</h1>  {/* bu satır değişecek */}
</div>
```

<!-- stage-transition.tsx — tamamlandı block style referansı -->
From src/app/(dashboard)/projeler/[id]/stage-transition.tsx:
```typescript
// completed state pattern:
<div className="rounded-lg border border-emerald-500/30 bg-emerald-500/5 p-4">
  <p className="text-sm text-emerald-400">Tüm aşamalar tamamlandı. ...</p>
</div>
```

<!-- ClusterButton.tsx — async loading + error state pattern -->
From src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterButton.tsx:
```typescript
'use client'
import { useState, useTransition } from 'react'
import { Button } from '@/components/ui/button'

const [isPending, startTransition] = useTransition()
const [error, setError] = useState<string | null>(null)
// spinner: <Loader2 className="animate-spin h-4 w-4" /> (UI-SPEC: Loader2 from lucide-react)
```

<!-- Toast pattern — projedeki mevcut kullanım -->
<!-- Projede toast için 'sonner' kullanılıyor — import { toast } from 'sonner' -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx — hasResearch prop + ProjectInfoSection'a userId aktarımı</name>
  <files>
    src/app/(dashboard)/projeler/[id]/page.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/page.tsx (mevcut dosyanın tamamı — query ve ProjectInfoSection kullanımı)
  </read_first>
  <action>
    page.tsx'te şu iki değişikliği yap:

    **1. research_reports doluluk kontrolü** (migration gerektirmeden araştırma durumu tespiti — Claude's Discretion çözümü):
    Proje query'sinin ALTINA, auth ve WP check'lerin yanına ekle:
    ```typescript
    // Araştırma tamamlanmış mı? research_reports tablosunda en az 1 row varsa tamamlanmış
    const { data: researchCheck } = await supabase
      .from('research_reports')
      .select('section')
      .eq('project_id', id)
      .eq('user_id', user.id)
      .limit(1)
    const hasResearch = (researchCheck?.length ?? 0) > 0
    ```

    **2. ProjectInfoSection Props güncellemesi** — `userId` ve `hasResearch` prop'larını ekle:
    ```typescript
    <ProjectInfoSection
      projectId={project.id}
      userId={user.id}          // yeni — fetch çağrısı için
      hasResearch={hasResearch}  // yeni — tamamlandı bloğu için
      initialData={{
        name: project.name,
        domain: project.domain,
        sector: project.sector,
        // ... mevcut alanlar aynen korunur
      }}
    />
    ```

    Mevcut hiçbir diğer bileşen (WordPressConnectionSection, GscConnectionSection, SiteImportSection) değiştirilmez.
  </action>
  <verify>
    <automated>grep -n "hasResearch\|researchCheck\|userId.*user\.id" "C:/Users/baris/Documents/seo-machine/src/app/(dashboard)/projeler/[id]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `research_reports` tablosuna `.select('section').eq('project_id', id).limit(1)` sorgusu var
    - `const hasResearch = (researchCheck?.length ?? 0) > 0` satırı var
    - `ProjectInfoSection` çağrısında `userId={user.id}` prop'u var
    - `ProjectInfoSection` çağrısında `hasResearch={hasResearch}` prop'u var
    - Diğer bileşenler (WordPressConnectionSection, GscConnectionSection, SiteImportSection) değişmedi
    - TypeScript: `npx tsc --noEmit` 0 hata
  </acceptance_criteria>
  <done>page.tsx hasResearch doluluk kontrolü yapıyor ve ProjectInfoSection'a yeni prop'ları geçiyor</done>
</task>

<task type="auto">
  <name>Task 2: ProjectInfoSection.tsx — Launch Gate butonu</name>
  <files>
    src/app/(dashboard)/projeler/[id]/ProjectInfoSection.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/ProjectInfoSection.tsx (mevcut dosyanın tamamı — Props, state, return yapısı)
    - src/app/(dashboard)/projeler/[id]/site-import-section.tsx (loading+error pattern referans)
    - src/app/(dashboard)/projeler/[id]/stage-transition.tsx (tamamlandı block emerald style)
  </read_first>
  <action>
    ProjectInfoSection.tsx'te şu değişiklikleri yap:

    **1. Imports güncellemesi** — şunları ekle:
    ```typescript
    import { Separator } from '@/components/ui/separator'
    import { Loader2 } from 'lucide-react'
    import { toast } from 'sonner'
    import Link from 'next/link'
    ```

    **2. Props tipi güncellemesi:**
    ```typescript
    type Props = {
      projectId: string
      userId: string           // yeni
      hasResearch: boolean     // yeni — başlangıç tamamlanma durumu
      initialData: ProjectData
    }
    ```

    **3. Export fonksiyon signature:**
    ```typescript
    export function ProjectInfoSection({ projectId, userId, hasResearch: initialHasResearch, initialData }: Props)
    ```

    **4. State eklemeleri** (mevcut state'lerin yanına):
    ```typescript
    const [isResearching, setIsResearching] = useState(false)
    const [researchDone, setResearchDone] = useState(initialHasResearch)
    ```

    **5. Gate logic** (return bloğunun hemen öncesine, rows hesaplamasının altına):
    ```typescript
    const canLaunch = Boolean(
      data['sector']?.trim() &&
      data['initial_competitors']?.trim() &&
      data['target_keywords']?.trim()
    )

    const missingFields = [
      !data['sector']?.trim() && 'sektör',
      !data['initial_competitors']?.trim() && 'rakipler',
      !data['target_keywords']?.trim() && 'hedef kelimeler',
    ].filter(Boolean) as string[]
    ```

    **6. handleLaunch fonksiyonu** (gate logic'in hemen altına):
    ```typescript
    const handleLaunch = async () => {
      if (!canLaunch || isResearching) return
      setIsResearching(true)
      try {
        const res = await fetch('/api/research/trigger', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ projectId, userId }),
        })
        const json = await res.json()
        if (!res.ok) {
          if (json.code === 'SERPAPI_NOT_CONFIGURED') {
            toast.error('SerpAPI anahtarı yapılandırılmamış. Lütfen sistem ayarlarını kontrol edin.')
          } else {
            toast.error('Araştırma başarısız oldu. Lütfen tekrar deneyin.')
          }
          return
        }
        setResearchDone(true)
        toast.success('Araştırma tamamlandı. Araştırma sayfasında sonuçları görebilirsiniz.')
      } catch {
        toast.error('Araştırma başarısız oldu. Lütfen tekrar deneyin.')
      } finally {
        setIsResearching(false)
      }
    }
    ```

    **7. Return bloğuna Launch Gate UI ekleme** — mevcut `</div>` kapanışından (space-y-1 div) hemen ÖNCE:
    Yani `return ( <div className="space-y-1"> ... {rows.map(...)} ... </div> )` yapısının dışına, wrapping bir fragment veya div içine al:

    Mevcut return'ü şu şekilde güncelle:
    ```tsx
    return (
      <div>
        <div className="space-y-1">
          {rows.map((row, ri) => (
            // ... mevcut row render kodu aynen korunur
          ))}
        </div>

        {/* Launch Gate — D-07 per CONTEXT.md */}
        <Separator className="my-4" />
        {researchDone ? (
          <div className="rounded-lg border border-emerald-500/30 bg-emerald-500/5 p-3">
            <p className="text-sm text-emerald-400">Araştırma tamamlandı ✓</p>
            <Link
              href={`/projeler/${projectId}/arastirma`}
              className="text-xs text-muted-foreground mt-2 block hover:text-foreground"
            >
              Araştırma sayfasına git →
            </Link>
          </div>
        ) : (
          <div className="flex flex-col gap-1">
            <Button
              onClick={handleLaunch}
              disabled={!canLaunch || isResearching}
              className="w-full"
            >
              {isResearching ? (
                <span className="flex items-center gap-2">
                  <Loader2 className="animate-spin h-4 w-4" />
                  Araştırılıyor...
                </span>
              ) : (
                'Projeyi Başlat'
              )}
            </Button>
            {!canLaunch && missingFields.length > 0 && (
              <p className="text-xs text-muted-foreground mt-1">
                Eksik: {missingFields.join(', ')}
              </p>
            )}
          </div>
        )}
      </div>
    )
    ```

    Notlar:
    - `researchDone` state'i `initialHasResearch` ile başlar — SSR'dan gelen değer
    - Araştırma başarılı olunca `setResearchDone(true)` — buton completed block'a dönüşür
    - Hata durumunda buton enabled state'e döner (setIsResearching(false) finally'de)
    - Button `variant` belirtilmez — default (primary) kullanılır (UI-SPEC: variant="default")
  </action>
  <verify>
    <automated>grep -n "canLaunch\|handleLaunch\|isResearching\|researchDone\|Araştırılıyor" "C:/Users/baris/Documents/seo-machine/src/app/(dashboard)/projeler/[id]/ProjectInfoSection.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `canLaunch` boolean değişkeni var, 3 alanı kontrol ediyor (sector, initial_competitors, target_keywords)
    - `missingFields` array'i var, eksik alan label'larını içeriyor (sektör, rakipler, hedef kelimeler)
    - `handleLaunch` async fonksiyonu var, `/api/research/trigger` POST çağrısı yapıyor
    - `SERPAPI_NOT_CONFIGURED` code kontrolü ve özel toast mesajı var
    - `researchDone` state'i var, `initialHasResearch` ile initialize ediliyor
    - `<Separator className="my-4" />` kullanımı var
    - `<Loader2 className="animate-spin h-4 w-4" />` spinner var
    - `toast.success` ve `toast.error` çağrıları var (sonner)
    - `Eksik:` prefix'li hint paragraph var, disabled koşulunda gösteriliyor
    - `Araştırma tamamlandı ✓` completed block var, emerald style ile
    - `href={/projeler/${projectId}/arastirma}` link var
    - TypeScript: `npx tsc --noEmit` 0 hata
  </acceptance_criteria>
  <done>ProjectInfoSection gate logic + loading + completed block + error toast tam işlevsel</done>
</task>

<task type="auto">
  <name>Task 3: ResearchRerunButton.tsx + arastirma/page.tsx güncellemesi</name>
  <files>
    src/app/(dashboard)/projeler/[id]/arastirma/ResearchRerunButton.tsx
    src/app/(dashboard)/projeler/[id]/arastirma/page.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/arastirma/page.tsx (mevcut dosyanın tamamı — SSR yapısı, h1 bloğu)
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterButton.tsx (loading pattern referans)
  </read_first>
  <action>
    **A. ResearchRerunButton.tsx oluştur** (yeni dosya):
    ClusterButton.tsx pattern'iyle AYNI yapı — bağımsız Client Component:

    ```typescript
    'use client'

    import { useState } from 'react'
    import { useRouter } from 'next/navigation'
    import { Button } from '@/components/ui/button'
    import { Loader2 } from 'lucide-react'
    import { toast } from 'sonner'

    interface ResearchRerunButtonProps {
      projectId: string
      userId: string
    }

    export function ResearchRerunButton({ projectId, userId }: ResearchRerunButtonProps) {
      const [isResearching, setIsResearching] = useState(false)
      const router = useRouter()

      const handleRerun = async () => {
        if (isResearching) return
        setIsResearching(true)
        try {
          const res = await fetch('/api/research/trigger', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ projectId, userId }),
          })
          const json = await res.json()
          if (!res.ok) {
            if (json.code === 'SERPAPI_NOT_CONFIGURED') {
              toast.error('SerpAPI anahtarı yapılandırılmamış. Lütfen sistem ayarlarını kontrol edin.')
            } else {
              toast.error('Araştırma başarısız oldu. Lütfen tekrar deneyin.')
            }
            return
          }
          toast.success('Araştırma tamamlandı. Araştırma sayfasında sonuçları görebilirsiniz.')
          router.refresh()  // SSR veri yenileme
        } catch {
          toast.error('Araştırma başarısız oldu. Lütfen tekrar deneyin.')
        } finally {
          setIsResearching(false)
        }
      }

      return (
        <Button
          variant="outline"
          size="sm"
          onClick={handleRerun}
          disabled={isResearching}
        >
          {isResearching ? (
            <span className="flex items-center gap-2">
              <Loader2 className="animate-spin h-4 w-4" />
              Araştırılıyor...
            </span>
          ) : (
            'Yeniden Araştır'
          )}
        </Button>
      )
    }
    ```

    **B. arastirma/page.tsx güncelle:**

    1. Imports'a ekle:
       ```typescript
       import { ResearchRerunButton } from './ResearchRerunButton'
       ```

    2. Supabase `project` query'sini güncelle — `user_id` de gerekiyor (ResearchRerunButton'a geçmek için):
       Mevcut: `.select('id, name')`
       Yeni: `.select('id, name')` — değişmez; userId ayrıca `user.id`'den alınır (zaten var)

    3. h1 bloğunu güncelle — UI-SPEC Layout Contract:
       Mevcut:
       ```tsx
       <h1 className="text-xl font-semibold">Araştırma</h1>
       ```
       Yeni:
       ```tsx
       <div className="flex justify-between items-center">
         <h1 className="text-xl font-semibold">Araştırma</h1>
         <ResearchRerunButton projectId={id} userId={user.id} />
       </div>
       ```

    Diğer hiçbir şey değişmez — ResearchSection, SECTION_CONFIGS, rowsMap, navigasyon aynen korunur.
  </action>
  <verify>
    <automated>grep -n "ResearchRerunButton\|Yeniden Araştır\|justify-between" "C:/Users/baris/Documents/seo-machine/src/app/(dashboard)/projeler/[id]/arastirma/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `ResearchRerunButton.tsx` dosyası mevcut: `'use client'` ile başlıyor
    - `export function ResearchRerunButton` imzası var
    - `variant="outline" size="sm"` Button kullanımı var (UI-SPEC)
    - `router.refresh()` başarı durumunda çağrılıyor
    - arastirma/page.tsx'te `import { ResearchRerunButton }` var
    - arastirma/page.tsx'te `<div className="flex justify-between items-center">` wrap'i var
    - arastirma/page.tsx'te `<ResearchRerunButton projectId={id} userId={user.id} />` var
    - arastirma/page.tsx TypeScript: `npx tsc --noEmit` 0 hata
    - ResearchRerunButton.tsx TypeScript: 0 hata
  </acceptance_criteria>
  <done>"Yeniden Araştır" butonu /arastirma sayfasında h1 yanında sağda mevcut; tıklanınca araştırmayı yeniden başlatıp sayfayı yeniliyor</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/research/trigger | Client-side fetch — projectId + userId body'de; ownership Plan 02'de doğrulanıyor |
| SSR → research_reports | Doluluk kontrolü read-only; user_id filtresi ile sadece kendi verisi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10 | Spoofing | ProjectInfoSection fetch userId | accept | userId prop olarak SSR'dan geçiyor (user.id — Supabase auth); server-side ownership check Plan 02'de zaten var; ikinci katman güvence |
| T-18-11 | Information Disclosure | toast error mesajları | mitigate | Toast mesajları genel ifadeler ("Araştırma başarısız oldu") — internal hata detayları kullanıcıya gösterilmez |
| T-18-12 | Tampering | researchDone client state | accept | Client state optimistik güncelleme; gerçek veri DB'de; sayfa yenilenince SSR'dan doğru değer gelir |
</threat_model>

<verification>
1. Gate logic: `grep -n "canLaunch" src/app/(dashboard)/projeler/[id]/ProjectInfoSection.tsx`
2. Missing fields hint: `grep -n "Eksik:" src/app/(dashboard)/projeler/[id]/ProjectInfoSection.tsx`
3. Completed block: `grep -n "emerald" src/app/(dashboard)/projeler/[id]/ProjectInfoSection.tsx`
4. hasResearch prop: `grep -n "hasResearch" src/app/(dashboard)/projeler/[id]/page.tsx`
5. ResearchRerunButton export: `grep -n "export function ResearchRerunButton" src/app/(dashboard)/projeler/[id]/arastirma/ResearchRerunButton.tsx`
6. arastirma page integration: `grep -n "ResearchRerunButton" src/app/(dashboard)/projeler/[id]/arastirma/page.tsx`
7. TypeScript genel: `npx tsc --noEmit` (proje geneli 0 hata)
</verification>

<success_criteria>
- "Projeyi Başlat" butonu gate logic ile eksiksiz çalışıyor (disabled/enabled/loading/completed)
- Tamamlanma durumu SSR'dan geliyor (research_reports doluluk kontrolü)
- "Yeniden Araştır" butonu /arastirma sayfasında mevcut
- UI-SPEC ile tam uyumlu (emerald completed block, outline/sm buton, Loader2 spinner)
- Tüm 5 requirement (PROJ-06, PROJ-07, SRCH-01, SRCH-02, SRCH-03) Plan 01-03 ile kapsanıyor
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/18-project-launch-gate-sector-research/18-03-SUMMARY.md` oluştur.
</output>
