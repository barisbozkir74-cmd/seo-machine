---
phase: 18
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/vault.ts
  - src/lib/research/sector-research.ts
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02

must_haves:
  truths:
    - "getSerpApiKey() fonksiyonu vault.ts'te mevcuttur ve env var öncelikli pattern'i takip eder"
    - "sector-research.ts, proje verilerinden otomatik arama sorguları üretir (sektör + rakip + keyword kombinasyonları)"
    - "SerpAPI'den gelen ham sonuçlar Claude'a (claude-sonnet-4-6) aktarılır ve 5 bölümlü JSON çıktısı üretilir"
    - "Üretilen rapor research_reports tablosuna upsert ile yazılır (replace semantiği, 5 section ayrı ayrı)"
    - "Hata durumunda servis throw eder — çağıran route katmanı hatayı yönetir"
  artifacts:
    - path: "src/lib/supabase/vault.ts"
      provides: "getSerpApiKey() — env var önce, vault fallback"
      contains: "getSerpApiKey"
    - path: "src/lib/research/sector-research.ts"
      provides: "runSectorResearch() — tam araştırma pipeline'ı"
      exports: ["runSectorResearch"]
  key_links:
    - from: "src/lib/research/sector-research.ts"
      to: "https://serpapi.com/search.json"
      via: "fetch() with SERPAPI_KEY from vault.ts"
      pattern: "serpapi.com/search.json"
    - from: "src/lib/research/sector-research.ts"
      to: "research_reports table"
      via: "serviceClient.from('research_reports').upsert()"
      pattern: "research_reports.*upsert"
---

<objective>
SerpAPI anahtarı yönetimi ve sektör araştırması servis katmanının oluşturulması.

Purpose: Araştırma pipeline'ının çekirdeğini kurmak — SerpAPI ile Google aramaları yapan, Claude ile analiz eden ve research_reports'a yazan servis.
Output: vault.ts'e getSerpApiKey() eklenir; src/lib/research/sector-research.ts oluşturulur.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-project-launch-gate-sector-research/18-CONTEXT.md
@.planning/phases/18-project-launch-gate-sector-research/18-PATTERNS.md

<interfaces>
<!-- vault.ts — mevcut pattern (kopyalanacak) -->
From src/lib/supabase/vault.ts:
```typescript
import 'server-only'
import { createClient } from '@supabase/supabase-js'

const serviceClient = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!
)

export async function getDataForSeoCredentials(): Promise<{ login: string; password: string }> {
  if (process.env.DATAFORSEO_LOGIN && process.env.DATAFORSEO_PASSWORD) {
    return { login: process.env.DATAFORSEO_LOGIN, password: process.env.DATAFORSEO_PASSWORD }
  }
  const { data, error } = await serviceClient
    .rpc('vault_get_secrets', { p_names: ['dataforseo_login', 'dataforseo_password'] })
  if (error || !data?.length) {
    throw new Error('DataForSEO credentials okunamadı. ...')
  }
  const loginRow = data.find((s: { name: string; decrypted_secret: string }) => s.name === 'dataforseo_login')
  const passwordRow = data.find((s: { name: string; decrypted_secret: string }) => s.name === 'dataforseo_password')
  if (!loginRow || !passwordRow) throw new Error('DataForSEO credentials eksik.')
  return { login: loginRow.decrypted_secret, password: passwordRow.decrypted_secret }
}
```

<!-- arastirma/actions.ts — upsertSection imzası -->
From src/app/(dashboard)/projeler/[id]/arastirma/actions.ts:
```typescript
export type SectionKey =
  | 'market_structures'
  | 'competitor_strengths'
  | 'competitor_weaknesses'
  | 'quick_wins'
  | 'high_value_opportunities'

export async function upsertSection(
  projectId: string,
  section: SectionKey,
  rows: Record<string, string>[]
): Promise<ActionResult>
```

<!-- arastirma/page.tsx — bölüm kolon şeması (AI çıktısı bu kolonlara uymalı) -->
From src/app/(dashboard)/projeler/[id]/arastirma/page.tsx:
```
market_structures      → { alan, aciklama, onem }
competitor_strengths   → { alan, neden_guclu, seviye }
competitor_weaknesses  → { alan, zayiflik, firsat_notu }
quick_wins             → { keyword_alan, zorluk, tahmini_etki }
high_value_opportunities → { sayfa_alan, ticari_deger, oncelik }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: vault.ts — getSerpApiKey() ekle</name>
  <files>src/lib/supabase/vault.ts</files>
  <read_first>
    - src/lib/supabase/vault.ts (mevcut dosyanın tamamı — yeni fonksiyon aynı pattern'i takip edecek)
  </read_first>
  <action>
    vault.ts dosyasının sonuna aşağıdaki fonksiyonu ekle. getDataForSeoCredentials() ile AYNI pattern:
    1. Env var önce kontrol: `if (process.env.SERPAPI_KEY) return process.env.SERPAPI_KEY`
    2. Vault RPC çağrısı: `vault_get_secrets` ile `['serpapi_key']`
    3. Hata: `throw new Error('SERPAPI_KEY env var olarak .env.local dosyasına ekleyin.')`
    4. Row bul: `data.find((s) => s.name === 'serpapi_key')`
    5. Row yoksa: `throw new Error('SerpAPI key bulunamadı.')`
    6. Başarı: `return row.decrypted_secret`

    Tam fonksiyon imzası:
    ```typescript
    export async function getSerpApiKey(): Promise<string> {
      if (process.env.SERPAPI_KEY) return process.env.SERPAPI_KEY

      const { data, error } = await serviceClient
        .rpc('vault_get_secrets', { p_names: ['serpapi_key'] })

      if (error || !data?.length) {
        throw new Error('SERPAPI_KEY env var olarak .env.local dosyasına ekleyin.')
      }

      const row = data.find((s: { name: string; decrypted_secret: string }) => s.name === 'serpapi_key')
      if (!row) throw new Error('SerpAPI key bulunamadı.')
      return row.decrypted_secret
    }
    ```
  </action>
  <verify>
    <automated>grep -n "getSerpApiKey" "C:/Users/baris/Documents/seo-machine/src/lib/supabase/vault.ts"</automated>
  </verify>
  <acceptance_criteria>
    - vault.ts içinde `export async function getSerpApiKey(): Promise<string>` satırı var
    - `if (process.env.SERPAPI_KEY) return process.env.SERPAPI_KEY` satırı var (env var önce)
    - `vault_get_secrets.*serpapi_key` pattern'i var (vault fallback)
    - Mevcut getDataForSeoCredentials() ve getWordPressCredentials() fonksiyonları değişmedi
  </acceptance_criteria>
  <done>getSerpApiKey() vault.ts'te mevcut ve DataForSEO pattern'iyle identik</done>
</task>

<task type="auto">
  <name>Task 2: src/lib/research/sector-research.ts oluştur</name>
  <files>src/lib/research/sector-research.ts</files>
  <read_first>
    - src/lib/supabase/vault.ts (getSerpApiKey() imzası — az önce eklendi)
    - src/app/(dashboard)/projeler/[id]/arastirma/page.tsx (5 bölümün kolon şeması: alan, aciklama, onem vb.)
    - src/app/(dashboard)/projeler/[id]/arastirma/actions.ts (upsertSection imzası, SectionKey tipi)
  </read_first>
  <action>
    `src/lib/research/` dizini oluştur (mkdir yoksa) ve sector-research.ts dosyasını oluştur.

    **Dosya yapısı:**

    1. `import 'server-only'` (ilk satır — zorunlu)
    2. Imports: `@anthropic-ai/sdk`, `@supabase/supabase-js`, `getSerpApiKey` from `@/lib/supabase/vault`
    3. Service client (wp/import/route.ts pattern'i):
       ```typescript
       function getServiceClient() {
         return createClient(
           process.env.NEXT_PUBLIC_SUPABASE_URL!,
           process.env.SUPABASE_SERVICE_ROLE_KEY!
         )
       }
       ```
    4. Anthropic client:
       ```typescript
       function getAnthropicClient() {
         if (!process.env.ANTHROPIC_API_KEY) throw new Error('ANTHROPIC_API_KEY env var eksik.')
         return new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })
       }
       ```
    5. Tip tanımları:
       ```typescript
       export type SectionKey =
         | 'market_structures'
         | 'competitor_strengths'
         | 'competitor_weaknesses'
         | 'quick_wins'
         | 'high_value_opportunities'

       export interface ResearchInput {
         projectId: string
         userId: string
         sector: string
         initial_competitors: string
         target_keywords: string
       }
       ```

    6. **buildSearchQueries(input: ResearchInput): string[]** — arama sorguları üretir.
       Strateji (SRCH-01 per D-03):
       - Sektör sorgusu: `"${sector} sektörü pazar analizi"`
       - Her rakip için (initial_competitors'u virgül/yeni satır ile böl, max 3 rakip):
         `"${competitor} SEO içerik stratejisi"`
       - İlk 2 keyword için (target_keywords'ü virgül/yeni satır ile böl):
         `"${keyword} rakip analiz"`
       - Genel fırsat: `"${sector} içerik boşluğu fırsatları SEO"`
       Toplam max 7 sorgu döner.

    7. **fetchSerpResults(query: string, serpApiKey: string): Promise<string>** — tek sorgu için SerpAPI çağrısı.
       ```typescript
       const url = `https://serpapi.com/search.json?q=${encodeURIComponent(query)}&api_key=${serpApiKey}&hl=tr&gl=tr&num=10`
       const res = await fetch(url)
       if (!res.ok) throw new Error(`SerpAPI hatası: ${res.status}`)
       const data = await res.json()
       // organic_results'tan title + snippet + link al, metin olarak birleştir
       const results = (data.organic_results ?? []).slice(0, 5).map((r: { title?: string; snippet?: string; link?: string }) =>
         `Başlık: ${r.title ?? ''}\nÖzet: ${r.snippet ?? ''}\nLink: ${r.link ?? ''}`
       ).join('\n\n')
       return results || 'Sonuç yok.'
       ```

    8. **analyzeWithClaude(serpResults: string, input: ResearchInput): Promise<ClaudeReport>** — claude-sonnet-4-6 ile analiz.
       Model: `claude-sonnet-4-6`, max_tokens: 4000
       
       ClaudeReport tipi:
       ```typescript
       interface ClaudeReport {
         market_structures: Array<{ alan: string; aciklama: string; onem: string }>
         competitor_strengths: Array<{ alan: string; neden_guclu: string; seviye: string }>
         competitor_weaknesses: Array<{ alan: string; zayiflik: string; firsat_notu: string }>
         quick_wins: Array<{ keyword_alan: string; zorluk: string; tahmini_etki: string }>
         high_value_opportunities: Array<{ sayfa_alan: string; ticari_deger: string; oncelik: string }>
       }
       ```
       
       Prompt (Türkçe):
       ```
       Sen bir SEO stratejisti uzmanısın. Aşağıdaki Google arama sonuçlarını analiz ederek ${input.sector} sektöründeki
       pazar durumunu ve fırsatları değerlendir.

       Proje bilgileri:
       - Sektör: ${input.sector}
       - Rakipler: ${input.initial_competitors}
       - Hedef Kelimeler: ${input.target_keywords}

       Google Arama Sonuçları:
       ${serpResults}

       SADECE aşağıdaki JSON formatında yanıt ver, başka hiçbir şey ekleme:
       {
         "market_structures": [{"alan": "...", "aciklama": "...", "onem": "yüksek/orta/düşük"}],
         "competitor_strengths": [{"alan": "...", "neden_guclu": "...", "seviye": "yüksek/orta/düşük"}],
         "competitor_weaknesses": [{"alan": "...", "zayiflik": "...", "firsat_notu": "..."}],
         "quick_wins": [{"keyword_alan": "...", "zorluk": "Düşük/Orta/Yüksek", "tahmini_etki": "..."}],
         "high_value_opportunities": [{"sayfa_alan": "...", "ticari_deger": "Yüksek/Orta/Düşük", "oncelik": "1/2/3"}]
       }
       Her bölümde en az 3, en fazla 8 satır üret.
       ```

       JSON parse: `text.match(/```json\s*([\s\S]*?)```/) ?? text.match(/(\{[\s\S]*\})/)`
       Parse hatası durumunda throw: `throw new Error('Claude JSON çıktısı parse edilemedi.')`

    9. **saveReport(report: ClaudeReport, input: ResearchInput): Promise<void>** — 5 section'ı tek tek upsert.
       Her section için:
       ```typescript
       await serviceClient.from('research_reports').upsert(
         {
           user_id: input.userId,
           project_id: input.projectId,
           section: sectionKey,
           rows: report[sectionKey],
           updated_at: new Date().toISOString(),
         },
         { onConflict: 'project_id,section' }
       )
       ```
       Hata varsa throw.

    10. **export async function runSectorResearch(input: ResearchInput): Promise<void>** — ana export.
        Sıra:
        a. `const serpApiKey = await getSerpApiKey()`
        b. `const queries = buildSearchQueries(input)`
        c. Her sorgu için sırayla fetchSerpResults (paralel değil — rate limit riski)
        d. Tüm sonuçları birleştir: `const allResults = results.join('\n\n---\n\n')`
        e. `const report = await analyzeWithClaude(allResults, input)`
        f. `await saveReport(report, input)`
        
        Timeout: fetchSerpResults için Promise.race ile 10000ms timeout.
  </action>
  <verify>
    <automated>grep -n "export async function runSectorResearch" "C:/Users/baris/Documents/seo-machine/src/lib/research/sector-research.ts"</automated>
  </verify>
  <acceptance_criteria>
    - Dosya `import 'server-only'` ile başlıyor
    - `export async function runSectorResearch(input: ResearchInput): Promise<void>` mevcut
    - `buildSearchQueries` fonksiyonu var ve sektör + rakip + keyword kombinasyonları üretiyor
    - `fetchSerpResults` serpapi.com/search.json URL'ini kullanıyor
    - `analyzeWithClaude` içinde `claude-sonnet-4-6` model adı var
    - `saveReport` içinde `research_reports` tablosuna upsert yapılıyor, `onConflict: 'project_id,section'` var
    - `getSerpApiKey` import'u `@/lib/supabase/vault` üzerinden yapılıyor
    - TypeScript derleme hatası yok: `npx tsc --noEmit` 0 çıkar (src/lib/research/sector-research.ts için)
  </acceptance_criteria>
  <done>runSectorResearch() eksiksiz çalışır — SerpAPI sorguları + Claude analiz + research_reports yazma</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → SerpAPI | SERPAPI_KEY credentials ile dış API çağrısı — key sızarsa kota tükenir |
| server → Anthropic | ANTHROPIC_API_KEY ile dış AI çağrısı — key sızarsa maliyet oluşur |
| Claude JSON output → DB | AI çıktısı parse edilip DB'ye yazılır — malformed JSON sistemi bozabilir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | vault.ts getSerpApiKey | mitigate | API key asla console.log/error'a yazılmaz; env var veya vault üzerinden okunur; dosya `server-only` ile korunur |
| T-18-02 | Information Disclosure | vault.ts getSerpApiKey | mitigate | `import 'server-only'` — key client bundle'a sızmaz |
| T-18-03 | Tampering | Claude JSON parse | mitigate | JSON parse try/catch; parse hatasında throw — bozuk veri DB'ye yazılmaz |
| T-18-04 | Denial of Service | fetchSerpResults rate limit | mitigate | Sorgular paralel değil sıralı; max 7 sorgu; 10s timeout per query |
| T-18-05 | Elevation of Privilege | serviceClient DB write | accept | Service role key server-only; write sadece araştırma pipeline'ında kullanılıyor; user_id her kayıtta zorunlu |
</threat_model>

<verification>
1. vault.ts TypeScript derleme: `npx tsc --noEmit` — 0 hata
2. getSerpApiKey mevcut: `grep -n "getSerpApiKey" src/lib/supabase/vault.ts` — satır numarası döner
3. runSectorResearch export: `grep -n "export async function runSectorResearch" src/lib/research/sector-research.ts` — mevcut
4. server-only import: `grep -n "server-only" src/lib/research/sector-research.ts` — ilk satır
5. claude-sonnet-4-6 kullanımı: `grep -n "claude-sonnet-4-6" src/lib/research/sector-research.ts` — mevcut
</verification>

<success_criteria>
- vault.ts'e getSerpApiKey() eklendi, DataForSEO pattern'iyle birebir uyumlu
- sector-research.ts oluşturuldu, runSectorResearch() export edildi
- TypeScript derleme hatası yok
- Tüm SRCH-01 ve SRCH-02 servis katmanı kapsamı tamamlandı
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/18-project-launch-gate-sector-research/18-01-SUMMARY.md` oluştur.
</output>
