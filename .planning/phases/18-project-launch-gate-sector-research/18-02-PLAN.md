---
phase: 18
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/research/trigger/route.ts
autonomous: true
requirements:
  - PROJ-07
  - SRCH-01
  - SRCH-02

must_haves:
  truths:
    - "POST /api/research/trigger endpoint'i mevcuttur"
    - "IDOR koruması: userId + projectId ownership kontrolü yapılır (user_id eşleşmezse 404)"
    - "Proje sector, initial_competitors, target_keywords alanları API route'da validate edilir — eksikse 400 döner"
    - "runSectorResearch() başarılıyla tamamlanınca { success: true } döner"
    - "Hata durumunda { error: string } ve uygun HTTP status kodu döner"
  artifacts:
    - path: "src/app/api/research/trigger/route.ts"
      provides: "POST /api/research/trigger — araştırma pipeline tetikleyicisi"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/research/trigger/route.ts"
      to: "src/lib/research/sector-research.ts"
      via: "runSectorResearch(input)"
      pattern: "runSectorResearch"
    - from: "ProjectInfoSection.tsx (Plan 03)"
      to: "src/app/api/research/trigger/route.ts"
      via: "fetch('/api/research/trigger', { method: 'POST', body: JSON.stringify({ projectId, userId }) })"
      pattern: "api/research/trigger"
---

<objective>
Araştırma pipeline'ını tetikleyen Next.js API route'unun oluşturulması.

Purpose: Güvenli bir HTTP katmanı kurmak — IDOR koruması, input validation ve servis çağrısı.
Output: src/app/api/research/trigger/route.ts — wp/import/route.ts pattern'ini takip eden POST handler.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-project-launch-gate-sector-research/18-CONTEXT.md
@.planning/phases/18-project-launch-gate-sector-research/18-PATTERNS.md

<interfaces>
<!-- wp/import/route.ts — referans pattern (satır 1-60 özeti) -->
From src/app/api/wp/import/route.ts:
```typescript
import { type NextRequest, NextResponse } from 'next/server'
import { createClient as createServiceClient } from '@supabase/supabase-js'

function getServiceClient() {
  return createServiceClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!
  )
}

export async function POST(request: NextRequest) {
  let body: { projectId?: string; userId?: string }
  try {
    body = await request.json()
  } catch {
    return NextResponse.json({ error: 'Invalid JSON' }, { status: 400 })
  }

  const { projectId, userId } = body
  if (!projectId || !userId) {
    return NextResponse.json({ error: 'projectId and userId required' }, { status: 400 })
  }

  const serviceClient = getServiceClient()

  // IDOR koruması
  const { data: project } = await serviceClient
    .from('projects')
    .select('id, name, gsc_property_url')
    .eq('id', projectId)
    .eq('user_id', userId)
    .single()

  if (!project) {
    return NextResponse.json({ error: 'Project not found' }, { status: 404 })
  }
  // ...pipeline...
  } catch (err) {
    console.error('[wp/import] Pipeline error:', err)
    return NextResponse.json({ error: 'Import pipeline failed' }, { status: 500 })
  }
}
```

<!-- sector-research.ts — runSectorResearch imzası (Plan 01'de oluşturulacak) -->
```typescript
import { runSectorResearch, type ResearchInput } from '@/lib/research/sector-research'

// ResearchInput:
interface ResearchInput {
  projectId: string
  userId: string
  sector: string
  initial_competitors: string
  target_keywords: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /api/research/trigger/route.ts oluştur</name>
  <files>src/app/api/research/trigger/route.ts</files>
  <read_first>
    - src/app/api/wp/import/route.ts (referans — IDOR, parse, error handling pattern'i için; satır 1-60)
    - src/lib/research/sector-research.ts (runSectorResearch imzası ve ResearchInput tipi — Plan 01'de oluşturulacak)
  </read_first>
  <action>
    `src/app/api/research/trigger/` dizini oluştur ve route.ts yaz. wp/import/route.ts ile AYNI yapısal pattern:

    **Tam dosya içeriği:**

    ```typescript
    import { type NextRequest, NextResponse } from 'next/server'
    import { createClient as createServiceClient } from '@supabase/supabase-js'
    import { runSectorResearch } from '@/lib/research/sector-research'

    // Service role client — RLS bypass, uzun pipeline için
    function getServiceClient() {
      return createServiceClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.SUPABASE_SERVICE_ROLE_KEY!
      )
    }

    export async function POST(request: NextRequest) {
      // Body parse
      let body: { projectId?: string; userId?: string }
      try {
        body = await request.json()
      } catch {
        return NextResponse.json({ error: 'Invalid JSON' }, { status: 400 })
      }

      const { projectId, userId } = body
      if (!projectId || !userId) {
        return NextResponse.json({ error: 'projectId and userId required' }, { status: 400 })
      }

      const serviceClient = getServiceClient()

      // T-18-06: IDOR koruması — userId ownership check
      const { data: project } = await serviceClient
        .from('projects')
        .select('id, name, sector, initial_competitors, target_keywords')
        .eq('id', projectId)
        .eq('user_id', userId)
        .single()

      if (!project) {
        return NextResponse.json({ error: 'Project not found' }, { status: 404 })
      }

      // Gate validation — 3 zorunlu alan kontrolü (D-01 per CONTEXT.md)
      if (!project.sector?.trim() || !project.initial_competitors?.trim() || !project.target_keywords?.trim()) {
        return NextResponse.json(
          { error: 'Proje bilgileri eksik: sektör, rakipler ve hedef kelimeler dolu olmalıdır.' },
          { status: 400 }
        )
      }

      try {
        await runSectorResearch({
          projectId: project.id,
          userId,
          sector: project.sector,
          initial_competitors: project.initial_competitors,
          target_keywords: project.target_keywords,
        })

        return NextResponse.json({ success: true })
      } catch (err) {
        const message = err instanceof Error ? err.message : 'Araştırma pipeline başarısız oldu.'
        console.error('[research/trigger] Pipeline error:', err)
        // T-18-07: SerpAPI key hatası özel mesajıyla dön
        if (message.includes('SERPAPI_KEY') || message.includes('SerpAPI key')) {
          return NextResponse.json(
            { error: 'SerpAPI anahtarı yapılandırılmamış.', code: 'SERPAPI_NOT_CONFIGURED' },
            { status: 503 }
          )
        }
        return NextResponse.json({ error: 'Research pipeline failed' }, { status: 500 })
      }
    }
    ```

    Notlar:
    - `console.error` içine hiçbir zaman API key veya kullanıcı verisi yazılmaz — sadece err objesi
    - SerpAPI key eksikliği özel `code: 'SERPAPI_NOT_CONFIGURED'` ile dönülür — UI bunu farklı toast'la gösterir (Plan 03)
    - `sector`, `initial_competitors`, `target_keywords` — Supabase'den gelen değerleri `.trim()` ile kontrol et
  </action>
  <verify>
    <automated>grep -n "IDOR\|ownership\|user_id.*userId\|userId.*user_id" "C:/Users/baris/Documents/seo-machine/src/app/api/research/trigger/route.ts"</automated>
  </verify>
  <acceptance_criteria>
    - Dosya `src/app/api/research/trigger/route.ts` yolunda mevcut
    - `export async function POST(request: NextRequest)` imzası var
    - `.eq('user_id', userId)` satırı var (IDOR koruması)
    - `if (!project)` kontrolü ve 404 response var
    - `!project.sector?.trim() || !project.initial_competitors?.trim() || !project.target_keywords?.trim()` gate kontrolü var
    - `runSectorResearch(` çağrısı var
    - `SERPAPI_NOT_CONFIGURED` code'u olan 503 response var
    - `console.error('[research/trigger]` log prefix var
    - TypeScript: `npx tsc --noEmit` 0 hata
  </acceptance_criteria>
  <done>POST /api/research/trigger — IDOR korumalı, gate-validated, runSectorResearch'ü çağıran route hazır</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/research/trigger | HTTP POST — body'de projectId + userId gelir; ownership doğrulanmadan işlem yapılmaz |
| route → serviceClient | Service role ile projects tablosundan okuma — userId ownership zorunlu |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Elevation of Privilege | POST /api/research/trigger | mitigate | `.eq('user_id', userId)` — projectId + userId birlikte ownership check; eşleşmezse 404; başka kullanıcının projesini tetikleyemez |
| T-18-07 | Information Disclosure | error response body | mitigate | SerpAPI hatası için özel kod döner ama key içeriği asla response'a yazılmaz; console.error'a sadece err objesi yazılır |
| T-18-08 | Denial of Service | pipeline uzun süre | accept | Vercel serverless 60s timeout zaten limiti koyar; kullanıcı tekrar deneyebilir; rate limiting ileride eklenebilir |
| T-18-09 | Spoofing | userId body parametresi | mitigate | userId client'tan gelir ancak projects tablosunda `.eq('user_id', userId)` ile doğrulanır; Supabase RLS ek güvence sağlar |
</threat_model>

<verification>
1. Route dosyası: `ls src/app/api/research/trigger/route.ts` — mevcut
2. POST export: `grep -n "export async function POST" src/app/api/research/trigger/route.ts`
3. IDOR kontrolü: `grep -n "user_id.*userId\|\.eq.*user_id" src/app/api/research/trigger/route.ts`
4. Gate validation: `grep -n "sector.*trim\|initial_competitors.*trim" src/app/api/research/trigger/route.ts`
5. TypeScript: `npx tsc --noEmit` 0 hata
</verification>

<success_criteria>
- POST /api/research/trigger route oluşturuldu
- IDOR koruması ve gate validation eksiksiz
- runSectorResearch() çağrısı doğru imzayla yapılıyor
- SerpAPI config hatası özel kodla dönülüyor (UI plan 03'te kullanacak)
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/18-project-launch-gate-sector-research/18-02-SUMMARY.md` oluştur.
</output>
