---
phase: 04-competitor-intelligence
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - src/app/(dashboard)/projeler/[id]/rakipler/page.tsx
  - src/app/(dashboard)/projeler/[id]/rakipler/CompetitorDiscoveryDialog.tsx
  - src/app/(dashboard)/projeler/[id]/rakipler/CompetitorFetchButton.tsx
  - src/app/(dashboard)/projeler/[id]/rakipler/OwnDomainAnalyzeButton.tsx
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04

must_haves:
  truths:
    - "Kullanıcı /projeler/[id]/rakipler sayfasını açıp rakip listesini görebilir"
    - "Kullanıcı 'Rakip Ekle' formuyla manuel domain ekleyebilir ve liste güncellenir"
    - "Kullanıcı 'Rakip Keşfet' butonuyla dialog açar, 1-3 keyword girer, domain listesi görünür, seçip ekleyebilir"
    - "Kullanıcı her rakip satırında 'Veri Çek' butonuna basar, top pages ve kategori yapısı çekilir"
    - "Rakip listesinin altında Gap Analizi tablosu görünür — satır=kategori, sütun=kullanıcının domain'i (ilk) + her rakip (D-10)"
    - "Veri çekilmemiş rakipler için gap tablosu yerine bilgilendirme mesajı gösterilir"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/rakipler/page.tsx"
      provides: "Rakipler sayfası — Server Component, rakip listesi + gap tablosu"
      contains: "Gap Analizi"
    - path: "src/app/(dashboard)/projeler/[id]/rakipler/CompetitorDiscoveryDialog.tsx"
      provides: "SERP keşif dialog — Client Component, 2 adımlı keyword → domain seçimi"
      contains: "'use client'"
    - path: "src/app/(dashboard)/projeler/[id]/rakipler/CompetitorFetchButton.tsx"
      provides: "Veri Çek butonu — Client Component, loading state ile"
      contains: "'use client'"
  key_links:
    - from: "page.tsx"
      to: "actions.ts addCompetitor"
      via: "form action"
      pattern: "addCompetitor"
    - from: "CompetitorDiscoveryDialog.tsx"
      to: "actions.ts discoverCompetitors + addCompetitors"
      via: "Client Component → Server Action"
      pattern: "discoverCompetitors"
    - from: "CompetitorFetchButton.tsx"
      to: "actions.ts fetchCompetitorData"
      via: "Client Component → Server Action"
      pattern: "fetchCompetitorData"
    - from: "page.tsx buildGapReport()"
      to: "competitors.category_structure JSONB + fetchOwnDomainData result"
      via: "SSR hesaplama"
      pattern: "buildGapReport"
    - from: "page.tsx fetchOwnDomainData()"
      to: "actions.ts fetchOwnDomainData"
      via: "Server Action import"
      pattern: "fetchOwnDomainData"
---

<objective>
Rakipler sayfası UI'ını oluştur: Server Component page, SERP keşif dialog (Client), veri çekme butonu (Client) ve SSR gap raporu tablosu.

Purpose: COMP-01, COMP-02, COMP-03, COMP-04 gereksinimlerinin tüm kullanıcı arayüzü bu planda tamamlanır. Wave 2'deki action'lar burada bağlanır.
Output: rakipler/page.tsx + CompetitorDiscoveryDialog.tsx + CompetitorFetchButton.tsx
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-competitor-intelligence/04-CONTEXT.md
@.planning/phases/04-competitor-intelligence/04-RESEARCH.md
@.planning/phases/04-competitor-intelligence/04-PATTERNS.md
@.planning/phases/04-competitor-intelligence/04-01-SUMMARY.md
@.planning/phases/04-competitor-intelligence/04-02-SUMMARY.md

<interfaces>
<!-- Wave 1-2'den gelen arayüzler — executor bu signature'ları kullanır -->

From src/app/(dashboard)/projeler/[id]/rakipler/actions.ts (Wave 2):
```typescript
export type ActionResult = { success: true } | { success: false; error: string }
export type DiscoverResult = { success: true; domains: string[] } | { success: false; error: string }
export async function addCompetitor(projectId: string, domain: string): Promise<ActionResult>
export async function discoverCompetitors(projectId: string, keywords: string[]): Promise<DiscoverResult>
export async function addCompetitors(projectId: string, domains: string[]): Promise<ActionResult>
export async function fetchCompetitorData(competitorId: string, projectId: string): Promise<ActionResult>
```

From src/lib/competitors/url-categories.ts (Wave 1):
```typescript
export type CategoryStructure = {
  [category: string]: { pageCount: number; sampleUrls: string[]; totalEtv: number }
}
```

Supabase competitors tablosu:
```typescript
// .select('id, domain, source, top_pages, category_structure, updated_at')
type Competitor = {
  id: string
  domain: string
  source: 'manual' | 'serp'
  top_pages: Array<{ url: string; etv: number }> | null
  category_structure: CategoryStructure | null
  updated_at: string
}
```

Kurallar page.tsx layout pattern (birebir kopyalanacak yapı):
```tsx
// 2-sütunlu layout:
<div className="flex flex-col h-screen">
  <div className="p-8 pb-4">/* breadcrumb */</div>
  <div className="flex flex-1 min-h-0">
    <div className="w-64 shrink-0 border-r border-border overflow-y-auto p-4">/* sol sütun */</div>
    <div className="flex-1 min-w-0 overflow-y-auto p-8">/* sağ sütun */</div>
  </div>
</div>

// Sol sütun aktif link stili:
className="text-sm text-foreground font-semibold flex items-center gap-1 px-3 py-2 rounded-md bg-secondary"
// Pasif link:
className="text-sm text-muted-foreground hover:text-foreground flex items-center gap-1 px-3 py-2 rounded-md hover:bg-secondary"
```

Badge styling (variant prop YOK — doğrudan className):
```tsx
<Badge className="bg-slate-500/20 text-slate-400 border border-slate-500/30 text-xs">Manuel</Badge>
<Badge className="bg-blue-500/20 text-blue-400 border border-blue-500/30 text-xs">SERP</Badge>
```

DialogTrigger pattern (@base-ui/react — asChild YOK):
```tsx
<DialogTrigger render={<Button variant="outline">...</Button>} />
```

RuleToggleRow loading state pattern:
```typescript
const [isPending, setIsPending] = useState(false)
const [error, setError] = useState<string | null>(null)
// button: disabled={isPending} className={isPending ? 'opacity-50 cursor-wait' : ''}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CompetitorFetchButton.tsx + CompetitorDiscoveryDialog.tsx — Client Components</name>
  <files>
    src/app/(dashboard)/projeler/[id]/rakipler/CompetitorFetchButton.tsx
    src/app/(dashboard)/projeler/[id]/rakipler/CompetitorDiscoveryDialog.tsx
  </files>
  <read_first>
    - src/components/rules/RuleToggleRow.tsx (useState + isPending + Server Action çağrı pattern'ı — tam dosya)
    - src/components/ui/dialog.tsx (Dialog bileşen export'ları ve DialogTrigger render prop pattern'ı)
    - src/components/ui/button.tsx (Button bileşeni export'ları)
    - src/components/ui/input.tsx (Input bileşeni — dialog içinde keyword girişi için)
    - .planning/phases/04-competitor-intelligence/04-PATTERNS.md (satır 462-547 — Dialog ve loading state pattern'ları)
  </read_first>
  <action>
**CompetitorFetchButton.tsx oluştur** (`src/app/(dashboard)/projeler/[id]/rakipler/CompetitorFetchButton.tsx`):

Her rakip satırında "Veri Çek" butonunu render eder. Loading state ve hata mesajı gösterir.

```typescript
'use client'

import { useState } from 'react'
import { Button } from '@/components/ui/button'
import { fetchCompetitorData } from './actions'

type Props = {
  competitorId: string
  projectId: string
  lastFetched: string | null  // updated_at — son çekim tarihi
}

export function CompetitorFetchButton({ competitorId, projectId, lastFetched }: Props) {
  const [isPending, setIsPending] = useState(false)
  const [error, setError] = useState<string | null>(null)

  const handleFetch = async () => {
    setIsPending(true)
    setError(null)
    try {
      const result = await fetchCompetitorData(competitorId, projectId)
      if (!result.success) {
        setError(result.error)
      }
    } catch {
      setError('Veri çekme başarısız. Lütfen tekrar deneyin.')
    } finally {
      setIsPending(false)
    }
  }

  return (
    <div className="flex flex-col items-end gap-1">
      <Button
        size="sm"
        variant="outline"
        onClick={handleFetch}
        disabled={isPending}
        className={isPending ? 'opacity-50 cursor-wait' : ''}
      >
        {isPending ? 'Çekiliyor...' : 'Veri Çek'}
      </Button>
      {lastFetched && !isPending && (
        <span className="text-xs text-muted-foreground">
          {new Date(lastFetched).toLocaleDateString('tr-TR')}
        </span>
      )}
      {error && (
        <span className="text-xs text-destructive">{error}</span>
      )}
    </div>
  )
}
```

**CompetitorDiscoveryDialog.tsx oluştur** (`src/app/(dashboard)/projeler/[id]/rakipler/CompetitorDiscoveryDialog.tsx`):

2 adımlı dialog: (1) keyword girişi → "Ara" → (2) domain checkbox listesi → "Seçilenleri Ekle". D-03 kararı.

```typescript
'use client'

import { useState } from 'react'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
  DialogFooter,
} from '@/components/ui/dialog'
import { discoverCompetitors, addCompetitors } from './actions'

type Props = {
  projectId: string
}

type Step = 'keyword-input' | 'domain-select'

export function CompetitorDiscoveryDialog({ projectId }: Props) {
  const [open, setOpen] = useState(false)
  const [step, setStep] = useState<Step>('keyword-input')
  const [keywords, setKeywords] = useState(['', '', ''])
  const [discoveredDomains, setDiscoveredDomains] = useState<string[]>([])
  const [selectedDomains, setSelectedDomains] = useState<Set<string>>(new Set())
  const [isPending, setIsPending] = useState(false)
  const [error, setError] = useState<string | null>(null)

  const resetDialog = () => {
    setStep('keyword-input')
    setKeywords(['', '', ''])
    setDiscoveredDomains([])
    setSelectedDomains(new Set())
    setError(null)
    setIsPending(false)
  }

  const handleOpenChange = (isOpen: boolean) => {
    setOpen(isOpen)
    if (!isOpen) resetDialog()
  }

  const handleDiscover = async () => {
    const filledKeywords = keywords.map((k) => k.trim()).filter(Boolean)
    if (!filledKeywords.length) {
      setError('En az bir keyword girin.')
      return
    }

    setIsPending(true)
    setError(null)
    try {
      const result = await discoverCompetitors(projectId, filledKeywords)
      if (!result.success) {
        setError(result.error)
      } else {
        setDiscoveredDomains(result.domains)
        setSelectedDomains(new Set(result.domains)) // hepsini varsayılan seçili yap
        setStep('domain-select')
      }
    } catch {
      setError('Rakip keşfi başarısız. Lütfen tekrar deneyin.')
    } finally {
      setIsPending(false)
    }
  }

  const handleAddSelected = async () => {
    if (!selectedDomains.size) {
      setError('En az bir rakip seçin.')
      return
    }

    setIsPending(true)
    setError(null)
    try {
      const result = await addCompetitors(projectId, Array.from(selectedDomains))
      if (!result.success) {
        setError(result.error)
      } else {
        setOpen(false)
        resetDialog()
      }
    } catch {
      setError('Rakipler eklenemedi. Lütfen tekrar deneyin.')
    } finally {
      setIsPending(false)
    }
  }

  const toggleDomain = (domain: string) => {
    setSelectedDomains((prev) => {
      const next = new Set(prev)
      if (next.has(domain)) {
        next.delete(domain)
      } else {
        next.add(domain)
      }
      return next
    })
  }

  return (
    <Dialog open={open} onOpenChange={handleOpenChange}>
      <DialogTrigger render={<Button variant="outline">Rakip Keşfet</Button>} />
      <DialogContent className="max-w-md">
        <DialogHeader>
          <DialogTitle>
            {step === 'keyword-input' ? 'Rakip Keşfet' : 'Rakipleri Seç'}
          </DialogTitle>
        </DialogHeader>

        {step === 'keyword-input' && (
          <div className="space-y-4 py-4">
            <p className="text-sm text-muted-foreground">
              1-3 hedef keyword girin. DataForSEO bu keyword'ler için SERP'te sıralanan rakipleri bulacak.
            </p>
            {keywords.map((kw, i) => (
              <Input
                key={i}
                placeholder={`Keyword ${i + 1}${i > 0 ? ' (opsiyonel)' : ''}`}
                value={kw}
                onChange={(e) => {
                  const next = [...keywords]
                  next[i] = e.target.value
                  setKeywords(next)
                }}
                disabled={isPending}
              />
            ))}
            {error && <p className="text-sm text-destructive">{error}</p>}
            <DialogFooter>
              <Button
                onClick={handleDiscover}
                disabled={isPending}
                className={isPending ? 'opacity-50 cursor-wait' : ''}
              >
                {isPending ? 'Aranıyor...' : 'Ara'}
              </Button>
            </DialogFooter>
          </div>
        )}

        {step === 'domain-select' && (
          <div className="space-y-4 py-4">
            <p className="text-sm text-muted-foreground">
              {discoveredDomains.length} rakip bulundu. Eklemek istediklerinizi seçin.
            </p>
            <div className="max-h-64 overflow-y-auto space-y-2 border border-border rounded-md p-3">
              {discoveredDomains.map((domain) => (
                <label
                  key={domain}
                  className="flex items-center gap-3 cursor-pointer hover:bg-secondary/50 px-2 py-1.5 rounded"
                >
                  <input
                    type="checkbox"
                    checked={selectedDomains.has(domain)}
                    onChange={() => toggleDomain(domain)}
                    className="rounded"
                  />
                  <span className="text-sm">{domain}</span>
                </label>
              ))}
            </div>
            {error && <p className="text-sm text-destructive">{error}</p>}
            <DialogFooter className="gap-2">
              <Button variant="ghost" onClick={() => setStep('keyword-input')}>
                ← Geri
              </Button>
              <Button
                onClick={handleAddSelected}
                disabled={isPending || !selectedDomains.size}
                className={isPending ? 'opacity-50 cursor-wait' : ''}
              >
                {isPending ? 'Ekleniyor...' : `Seçilenleri Ekle (${selectedDomains.size})`}
              </Button>
            </DialogFooter>
          </div>
        )}
      </DialogContent>
    </Dialog>
  )
}
```

**Kritik:**
- `DialogTrigger render={<Button>}` pattern — `asChild` prop KULLANMA (@base-ui/react)
- Dialog içinde 2 step state machine: `keyword-input` → `domain-select`
- discoveredDomains varsayılan olarak hepsi seçili gelir, kullanıcı deselect edebilir
  </action>
  <verify>
    <automated>grep -n "'use client'\|export function\|DialogTrigger\|discoverCompetitors\|fetchCompetitorData" C:/Users/baris/Documents/seo-machine/src/app/\(dashboard\)/projeler/\[id\]/rakipler/CompetitorDiscoveryDialog.tsx C:/Users/baris/Documents/seo-machine/src/app/\(dashboard\)/projeler/\[id\]/rakipler/CompetitorFetchButton.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CompetitorFetchButton.tsx başında `'use client'` direktifi mevcut
    - CompetitorFetchButton.tsx içinde `export function CompetitorFetchButton(` tanımı mevcut
    - CompetitorFetchButton.tsx içinde `fetchCompetitorData` import edilmiş ve çağrılıyor
    - CompetitorFetchButton.tsx içinde `disabled={isPending}` ve `'Çekiliyor...'` loading state mevcut
    - CompetitorFetchButton.tsx içinde `lastFetched` prop'u tarih olarak gösteriliyor
    - CompetitorDiscoveryDialog.tsx başında `'use client'` direktifi mevcut
    - CompetitorDiscoveryDialog.tsx içinde `export function CompetitorDiscoveryDialog(` tanımı mevcut
    - CompetitorDiscoveryDialog.tsx içinde `DialogTrigger render={` pattern mevcut (asChild YOK)
    - CompetitorDiscoveryDialog.tsx içinde `step === 'keyword-input'` ve `step === 'domain-select'` iki adım mevcut
    - CompetitorDiscoveryDialog.tsx içinde `discoverCompetitors` çağrısı mevcut
    - CompetitorDiscoveryDialog.tsx içinde `addCompetitors` çağrısı mevcut
    - CompetitorDiscoveryDialog.tsx içinde checkbox listesi ile domain seçimi mevcut
  </acceptance_criteria>
  <done>CompetitorFetchButton ve CompetitorDiscoveryDialog Client Component'ları oluşturuldu. SERP keşif flow 2 adımlı: keyword giriş → domain seçimi → toplu ekle. D-03, D-04 kararları uygulandı.</done>
</task>

<task type="auto">
  <name>Task 2: rakipler/page.tsx — Server Component, rakip listesi + gap raporu + kullanıcı domain sütunu (COMP-01, COMP-03, COMP-04, D-10)</name>
  <files>
    src/app/(dashboard)/projeler/[id]/rakipler/page.tsx
    src/app/(dashboard)/projeler/[id]/rakipler/OwnDomainAnalyzeButton.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/kurallar/page.tsx (tam dosya — 2-sütunlu layout, auth guard, Supabase sorgu, Table bileşen kullanımı — birebir template)
    - src/app/(dashboard)/projeler/[id]/rakipler/CompetitorDiscoveryDialog.tsx (import edilecek — Props type'ı)
    - src/app/(dashboard)/projeler/[id]/rakipler/CompetitorFetchButton.tsx (import edilecek — Props type'ı)
    - src/app/(dashboard)/projeler/[id]/rakipler/actions.ts (addCompetitor + fetchOwnDomainData import edilecek)
    - .planning/phases/04-competitor-intelligence/04-RESEARCH.md (Pattern 5 — buildGapReport algoritması, satır 418-473; Gap tablosu D-08, D-09, D-10 kararları)
    - .planning/phases/04-competitor-intelligence/04-CONTEXT.md (D-09, D-10 — gap tablosu satır/sütun yapısı)
  </read_first>
  <action>
`src/app/(dashboard)/projeler/[id]/rakipler/page.tsx` dosyasını oluştur:

Sayfa 3 bölümden oluşur:
1. Breadcrumb + başlık + aksiyon butonları ("Rakip Ekle" + "Rakip Keşfet")
2. Rakip listesi tablosu (domain, kaynak badge, sayfa sayısı, son güncelleme, "Veri Çek" butonu)
3. Gap Analizi tablosu (satır=kategori, sütun=rakip domain'leri, hücre=sayfa sayısı veya ✗)

```typescript
import { notFound } from 'next/navigation'
import Link from 'next/link'
import { createClient } from '@/lib/supabase/server'
import { Badge } from '@/components/ui/badge'
import { Separator } from '@/components/ui/separator'
import {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from '@/components/ui/table'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { CompetitorDiscoveryDialog } from './CompetitorDiscoveryDialog'
import { CompetitorFetchButton } from './CompetitorFetchButton'
import { OwnDomainAnalyzeButton } from './OwnDomainAnalyzeButton'
import { addCompetitor, fetchOwnDomainData } from './actions'

// ─── Types ────────────────────────────────────────────────────────────────────

type CategoryEntry = {
  pageCount: number
  sampleUrls: string[]
  totalEtv: number
}

type CategoryStructure = { [category: string]: CategoryEntry } | null

type Competitor = {
  id: string
  domain: string
  source: string
  top_pages: Array<{ url: string; etv: number }> | null
  category_structure: CategoryStructure
  updated_at: string
}

type GapMatrix = {
  categories: string[]
  matrix: {
    [domain: string]: {
      [category: string]: { exists: boolean; pageCount: number }
    }
  }
}

// ─── Gap raporu hesaplama (SSR — RESEARCH.md Pattern 5, D-10 genişletilmiş) ──

function buildGapReport(
  competitors: Competitor[],
  userDomain: string,
  userCategoryStructure: Record<string, number> | null
): GapMatrix {
  const allCategories = new Set<string>()

  for (const comp of competitors) {
    if (comp.category_structure) {
      Object.keys(comp.category_structure).forEach((cat) => allCategories.add(cat))
    }
  }
  // Kullanıcı domain kategorilerini de union'a ekle (D-10)
  if (userCategoryStructure) {
    Object.keys(userCategoryStructure).forEach((cat) => allCategories.add(cat))
  }

  // "Diğer" kategorisini gap tablosundan filtrele — anlamsız karşılaştırma yaratır
  const categories = Array.from(allCategories).filter((c) => c !== 'Diğer')

  const matrix: GapMatrix['matrix'] = {}

  // Kullanıcı domain'i ilk sütun olarak eklenir (D-10: "Sizin Siteniz" soldan ilk)
  matrix[userDomain] = {}
  for (const cat of categories) {
    const count = userCategoryStructure?.[cat] ?? 0
    matrix[userDomain][cat] = {
      exists: count > 0,
      pageCount: count,
    }
  }

  // Rakipler
  for (const comp of competitors) {
    matrix[comp.domain] = {}
    for (const cat of categories) {
      const catData = comp.category_structure?.[cat]
      matrix[comp.domain][cat] = {
        exists: !!catData,
        pageCount: catData?.pageCount ?? 0,
      }
    }
  }

  return { categories, matrix }
}

// ─── Page ─────────────────────────────────────────────────────────────────────

export default async function RakiplerPage({
  params,
}: {
  params: Promise<{ id: string }>
}) {
  const { id } = await params
  const supabase = await createClient()

  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) notFound()

  // Proje ownership check
  const { data: project } = await supabase
    .from('projects')
    .select('id, name, domain')
    .eq('id', id)
    .eq('user_id', user.id)
    .single()

  if (!project) notFound()

  // Rakip listesi
  const { data: competitorsRaw } = await supabase
    .from('competitors')
    .select('id, domain, source, top_pages, category_structure, updated_at')
    .eq('project_id', id)
    .eq('user_id', user.id)
    .order('created_at', { ascending: true })

  const competitors = (competitorsRaw ?? []) as Competitor[]

  // Kullanıcı kendi domain'i için kategori verisi — D-10, Q3 RESOLVED
  // DB'ye yazılmaz; SSR render için anlık çekilir
  const ownCategoryData = await fetchOwnDomainData(id, project.domain).catch(() => null)

  // Gap raporu — en az bir rakibin category_structure'ı dolu olmalı (RESEARCH.md Pitfall 5)
  const competitorsWithData = competitors.filter((c) => c.category_structure !== null)
  const gapReport =
    competitorsWithData.length > 0
      ? buildGapReport(competitorsWithData, project.domain, ownCategoryData)
      : null

  // Manuel rakip ekleme — inline Server Action (addCompetitor ile bağlı)
  const addCompetitorAction = addCompetitor.bind(null, id)

  return (
    <div className="flex flex-col h-screen">
      {/* Breadcrumb + başlık */}
      <div className="p-8 pb-4">
        <Link
          href={`/projeler/${id}`}
          className="text-sm text-muted-foreground hover:text-foreground flex items-center gap-1 mb-4"
        >
          ← {project.name}
        </Link>
        <h1 className="text-xl font-semibold">Rakipler</h1>
      </div>

      {/* 2 sütunlu içerik */}
      <div className="flex flex-1 min-h-0">
        {/* Sol sütun */}
        <div className="w-64 shrink-0 border-r border-border overflow-y-auto p-4">
          <Separator className="my-4" />
          {/* Rakipler — aktif (bg-secondary font-semibold) */}
          <Link
            href={`/projeler/${id}/rakipler`}
            className="text-sm text-foreground font-semibold flex items-center gap-1 px-3 py-2 rounded-md bg-secondary"
          >
            Rakipler
          </Link>
          {/* Kurallar — pasif */}
          <Link
            href={`/projeler/${id}/kurallar`}
            className="text-sm text-muted-foreground hover:text-foreground flex items-center gap-1 px-3 py-2 rounded-md hover:bg-secondary"
          >
            Proje Kuralları
          </Link>
        </div>

        {/* Sağ sütun */}
        <div className="flex-1 min-w-0 overflow-y-auto p-8 space-y-8">

          {/* Aksiyon butonları */}
          <div className="flex items-center justify-between">
            <h2 className="text-base font-semibold">Rakip Listesi</h2>
            <div className="flex items-center gap-2">
              <CompetitorDiscoveryDialog projectId={id} />
            </div>
          </div>

          {/* Manuel rakip ekleme formu */}
          <form
            action={async (formData: FormData) => {
              'use server'
              const domain = formData.get('domain') as string
              if (domain?.trim()) {
                await addCompetitorAction(domain.trim())
              }
            }}
            className="flex gap-2"
          >
            <Input
              name="domain"
              placeholder="domain.com"
              className="max-w-xs"
              required
            />
            <Button type="submit" variant="outline">
              Rakip Ekle
            </Button>
          </form>

          {/* Rakip listesi tablosu */}
          {competitors.length === 0 ? (
            <div className="text-sm text-muted-foreground py-8 text-center">
              Henüz rakip eklenmemiş. Yukarıdan domain ekleyin veya SERP ile keşfedin.
            </div>
          ) : (
            <Table>
              <TableHeader>
                <TableRow>
                  <TableHead>Domain</TableHead>
                  <TableHead>Kaynak</TableHead>
                  <TableHead>Sayfa Sayısı</TableHead>
                  <TableHead className="text-right">Veri</TableHead>
                </TableRow>
              </TableHeader>
              <TableBody>
                {competitors.map((comp) => (
                  <TableRow key={comp.id}>
                    <TableCell className="font-medium">{comp.domain}</TableCell>
                    <TableCell>
                      {comp.source === 'serp' ? (
                        <Badge className="bg-blue-500/20 text-blue-400 border border-blue-500/30 text-xs">
                          SERP
                        </Badge>
                      ) : (
                        <Badge className="bg-slate-500/20 text-slate-400 border border-slate-500/30 text-xs">
                          Manuel
                        </Badge>
                      )}
                    </TableCell>
                    <TableCell>
                      {comp.top_pages ? comp.top_pages.length : (
                        <span className="text-muted-foreground text-xs">—</span>
                      )}
                    </TableCell>
                    <TableCell className="text-right">
                      <CompetitorFetchButton
                        competitorId={comp.id}
                        projectId={id}
                        lastFetched={comp.top_pages ? comp.updated_at : null}
                      />
                    </TableCell>
                  </TableRow>
                ))}
              </TableBody>
            </Table>
          )}

          <Separator />

          {/* Gap Analizi — D-09, D-10 */}
          <div>
            <h2 className="text-base font-semibold mb-4">Gap Analizi</h2>

            {!ownCategoryData && (
              <div className="flex items-center gap-3 mb-4 p-3 rounded-md border border-border bg-secondary/30">
                <p className="text-sm text-muted-foreground flex-1">
                  Kendi siteniz için veri çekilmedi. Gap tablosunda &quot;Sizin Siteniz&quot; sütunu boş görünecek.
                </p>
                <OwnDomainAnalyzeButton projectId={id} domain={project.domain} />
              </div>
            )}
            {competitorsWithData.length === 0 ? (
              <p className="text-sm text-muted-foreground">
                Gap analizi için en az bir rakibin veri çekilmiş olması gerekiyor. Her rakip için &quot;Veri Çek&quot; butonuna basın.
              </p>
            ) : gapReport && gapReport.categories.length === 0 ? (
              <p className="text-sm text-muted-foreground">
                Kategori yapısı belirlenemedi. Rakiplerin URL yapısı tanınan pattern&apos;lere uymuyor olabilir.
              </p>
            ) : gapReport ? (
              <div className="overflow-x-auto">
                <Table>
                  <TableHeader>
                    <TableRow>
                      <TableHead className="min-w-36">Kategori</TableHead>
                      {/* Kullanıcı domain'i — ilk sütun (D-10) */}
                      <TableHead className="text-center min-w-36 font-semibold">
                        Sizin Siteniz
                        <span className="block text-xs font-normal text-muted-foreground truncate max-w-32">
                          {project.domain}
                        </span>
                      </TableHead>
                      {competitorsWithData.map((comp) => (
                        <TableHead key={comp.id} className="text-center min-w-28">
                          {comp.domain}
                        </TableHead>
                      ))}
                    </TableRow>
                  </TableHeader>
                  <TableBody>
                    {gapReport.categories.map((cat) => (
                      <TableRow key={cat}>
                        <TableCell className="font-medium">{cat}</TableCell>
                        {/* Kullanıcı domain'i hücresi — ilk veri sütunu (D-10) */}
                        {(() => {
                          const cell = gapReport.matrix[project.domain]?.[cat]
                          return (
                            <TableCell className="text-center">
                              {cell?.exists ? (
                                <span className="text-emerald-400 font-medium">
                                  {cell.pageCount > 0 ? cell.pageCount : '✓'}
                                </span>
                              ) : (
                                <span className="text-muted-foreground">✗</span>
                              )}
                            </TableCell>
                          )
                        })()}
                        {competitorsWithData.map((comp) => {
                          const cell = gapReport.matrix[comp.domain]?.[cat]
                          return (
                            <TableCell key={comp.id} className="text-center">
                              {cell?.exists ? (
                                <span className="text-emerald-400 font-medium">
                                  {cell.pageCount > 0 ? cell.pageCount : '✓'}
                                </span>
                              ) : (
                                <span className="text-muted-foreground">✗</span>
                              )}
                            </TableCell>
                          )
                        })}
                      </TableRow>
                    ))}
                  </TableBody>
                </Table>
              </div>
            ) : null}
          </div>

        </div>
      </div>
    </div>
  )
}
```

**Ek: OwnDomainAnalyzeButton.tsx oluştur** (`src/app/(dashboard)/projeler/[id]/rakipler/OwnDomainAnalyzeButton.tsx`):

Kullanıcı kendi domain analizini manuel tetikleyebilsin diye küçük Client Component. `ownCategoryData` null ise page.tsx bunu render eder.

```typescript
'use client'

import { useState } from 'react'
import { Button } from '@/components/ui/button'

type Props = {
  projectId: string
  domain: string
}

export function OwnDomainAnalyzeButton({ projectId, domain }: Props) {
  const [isPending, setIsPending] = useState(false)
  const [error, setError] = useState<string | null>(null)

  const handleAnalyze = async () => {
    setIsPending(true)
    setError(null)
    try {
      // fetchOwnDomainData DB'ye yazmaz — SSR render için veri döner
      // Server Action import burada dynamic yapılır (server-only modülü)
      const { fetchOwnDomainData } = await import('./actions')
      await fetchOwnDomainData(projectId, domain)
      // DB'ye yazılmadığı için revalidatePath etkisiz — sayfayı yenile
      window.location.reload()
    } catch {
      setError('Analiz başarısız. Lütfen tekrar deneyin.')
    } finally {
      setIsPending(false)
    }
  }

  return (
    <div className="flex flex-col items-end gap-1">
      <Button
        size="sm"
        variant="outline"
        onClick={handleAnalyze}
        disabled={isPending}
        className={isPending ? 'opacity-50 cursor-wait' : ''}
      >
        {isPending ? 'Analiz ediliyor...' : 'Kendi Sitemi Analiz Et'}
      </Button>
      {error && <span className="text-xs text-destructive">{error}</span>}
    </div>
  )
}
```

**Not:** `fetchOwnDomainData` DB'ye yazmadığı için `revalidatePath` sayfayı güncellemez. Bu yüzden `window.location.reload()` kullanılır — kullanıcı analiz tetikler, sayfa yenilenir, SSR `fetchOwnDomainData` tekrar çalışır ve yeni veri ile gap sütunu dolar.

**Kritik detaylar:**
- Sol sütun: Rakipler linki `bg-secondary font-semibold` (aktif), Kurallar linki `hover:bg-secondary` (pasif) — D-01 kararı
- Manuel form: `action={async (formData: FormData) => { 'use server' ... }}` inline Server Action — `addCompetitor.bind(null, id)` kullanılır
- Gap tablosu: Sadece `category_structure` dolu rakipler dahil edilir — null olanlar "Veri Çek" uyarısıyla gösterilir (RESEARCH.md Pitfall 5)
- Badge: `variant` prop yok — doğrudan `className` (PROJECT.md pattern)
- "Diğer" kategorisi gap tablosundan filtre edilir
  </action>
  <verify>
    <automated>grep -n "Gap Analizi\|buildGapReport\|CompetitorDiscoveryDialog\|CompetitorFetchButton\|OwnDomainAnalyzeButton\|fetchOwnDomainData\|Sizin Siteniz" C:/Users/baris/Documents/seo-machine/src/app/\(dashboard\)/projeler/\[id\]/rakipler/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx içinde `if (!user) notFound()` auth guard mevcut
    - page.tsx içinde `.eq('project_id', id).eq('user_id', user.id)` ownership check mevcut
    - page.tsx içinde `CompetitorDiscoveryDialog` import edilip kullanılıyor
    - page.tsx içinde `CompetitorFetchButton` import edilip her rakip satırında render ediliyor
    - page.tsx içinde `addCompetitor` import edilip form action olarak kullanılıyor
    - page.tsx içinde `buildGapReport` fonksiyonu tanımlı ve 3 parametre alıyor: `(competitors, userDomain, userCategoryStructure)`
    - page.tsx içinde `fetchOwnDomainData(id, project.domain)` SSR'da çağrılıyor
    - page.tsx içinde `Gap Analizi` başlığı mevcut
    - page.tsx içinde gap tablosu: `gapReport.categories.map` ile satırlar render ediliyor
    - page.tsx içinde gap tablosu başlığında "Sizin Siteniz" sütunu mevcut ve rakip sütunlarından önce gelir (D-10)
    - page.tsx içinde `gapReport.matrix[project.domain]` kullanıcı domain hücresi render edilir
    - page.tsx içinde `ownCategoryData` null olduğunda `OwnDomainAnalyzeButton` gösterilir
    - page.tsx içinde `competitorsWithData.length === 0` kontrolü ve uyarı mesajı mevcut
    - page.tsx sol sütununda Rakipler linki `bg-secondary font-semibold` (aktif) Kurallar linki `hover:bg-secondary` (pasif)
    - page.tsx içinde Badge bileşeni `variant` prop olmadan `className` ile kullanılıyor
    - page.tsx TypeScript hataları yok
  </acceptance_criteria>
  <done>rakipler/page.tsx oluşturuldu. COMP-01 (manuel ekleme), COMP-02 (SERP keşif dialog), COMP-03 (veri çek butonu), COMP-04 (gap tablosu) UI'ları hazır. D-01, D-08, D-09, D-10 kararları uygulandı. fetchOwnDomainData SSR'da çağrılır; kullanıcı domain'i gap tablosunda ilk sütun olarak gösterilir.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → CompetitorDiscoveryDialog | Kullanıcı keyword'leri client'ta girer, server action'a gönderilir — server validate eder |
| Browser → Manuel form | domain input server action'a gönderilir — server normalize ve validate eder |
| SSR render → Supabase | page.tsx server-side user.id ile veri çeker — başka kullanıcının verisi render edilemez |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Information Disclosure | page.tsx SSR render | mitigate | `eq('project_id', id).eq('user_id', user.id)` her iki filtre — farklı kullanıcının rakipleri render edilmez; auth guard `notFound()` ile |
| T-04-09 | Tampering | CompetitorDiscoveryDialog — domain seçimi | accept | Client'ta sadece server'dan dönen domain'ler gösterilir — kullanıcı yeni domain inject edemez (addCompetitors server-side ownership check yapar) |
| T-04-10 | Denial of Service | CompetitorFetchButton — paralel tıklama | accept | `disabled={isPending}` buton disabled — kullanıcı işlem sürerken tekrar tetikleyemez; human-triggered model yeterli koruma sağlar |
| T-04-11 | Tampering | Manuel form — domain injection | mitigate | `addCompetitor` server action'da `replace(/^https?:\/\//, '').replace(/^www\./, '').trim()` sanitize + Supabase parameterized query |
</threat_model>

<verification>
Plan tamamlandığında şunlar doğrulanabilir olmalı:
1. `/projeler/[id]/rakipler` route çalışıyor — 404 vermiyor
2. Rakip listesi boşken "Henüz rakip eklenmemiş" mesajı görünüyor
3. Domain girerek "Rakip Ekle" butonuna basılınca liste güncelleniyor
4. "Rakip Keşfet" butonuna basılınca Dialog açılıyor
5. Dialog'da keyword girip "Ara" butonuna basılınca domain listesi geliyor
6. Gap tablosu "Veri Çek" yapılmış rakipler için kategori matrisi gösteriyor
7. `npx tsc --noEmit` hata üretmiyor
</verification>

<success_criteria>
- 3 yeni dosya: page.tsx (Server Component) + CompetitorDiscoveryDialog.tsx (Client) + CompetitorFetchButton.tsx (Client)
- COMP-01: Manuel domain ekleme formu çalışıyor
- COMP-02: 2-adımlı SERP keşif dialog çalışıyor
- COMP-03: Her rakip satırında "Veri Çek" butonu — loading state + son çekim tarihi
- COMP-04: Gap Analizi tablosu SSR'da hesaplanıp render ediliyor
- D-01, D-02, D-08, D-09, D-10 kararları uygulandı
- D-10: Kullanıcı domain'i gap tablosunda ilk sütun; fetchOwnDomainData SSR'da çağrılır (DB'ye yazılmaz)
- OwnDomainAnalyzeButton: ownCategoryData null ise kullanıcıya analiz tetikleme imkânı sunulur
- TypeScript hataları yok
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/04-competitor-intelligence/04-03-SUMMARY.md` oluştur.
</output>
