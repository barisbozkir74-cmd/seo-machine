---
phase: 04-competitor-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/vault.ts
  - src/lib/dataforseo/client.ts
  - src/lib/competitors/url-categories.ts
  - src/app/(dashboard)/projeler/[id]/page.tsx
  - .env.example
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "DataForSEO credentials env var (DATAFORSEO_LOGIN/PASSWORD) üzerinden server-side okunabilir"
    - "fetchSerpDomains() fonksiyonu DataForSEO SERP endpoint'ini çağırır ve domain[] döner"
    - "fetchTopPages() fonksiyonu DataForSEO Relevant Pages endpoint'ini çağırır ve TopPageItem[] döner"
    - "extractCategories() URL pattern listesi üzerinden CategoryStructure döner"
    - "Proje detay sayfasında sol sütunda Rakipler linki Kurallar'ın üstünde görünür"
  artifacts:
    - path: "src/lib/supabase/vault.ts"
      provides: "getDataForSeoCredentials() — server-only, env var fallback"
      exports: ["getDataForSeoCredentials"]
    - path: "src/lib/dataforseo/client.ts"
      provides: "fetchSerpDomains(), fetchTopPages() — DataForSEO HTTP client"
      exports: ["fetchSerpDomains", "fetchTopPages", "TopPageItem"]
    - path: "src/lib/competitors/url-categories.ts"
      provides: "extractCategories() — pure function, URL → kategori"
      exports: ["extractCategories", "CategoryStructure", "TopPageItem"]
    - path: "src/app/(dashboard)/projeler/[id]/page.tsx"
      provides: "Sol sütunda Rakipler linki"
      contains: "href={`/projeler/${id}/rakipler`}"
  key_links:
    - from: "src/lib/supabase/vault.ts"
      to: "process.env.DATAFORSEO_LOGIN"
      via: "env var okuma"
      pattern: "DATAFORSEO_LOGIN"
    - from: "src/lib/dataforseo/client.ts"
      to: "https://api.dataforseo.com"
      via: "fetch POST + Basic Auth"
      pattern: "Buffer.from"
---

<objective>
DataForSEO entegrasyonu için lib katmanını oluştur ve proje detay sayfasına Rakipler navigasyon linkini ekle.

Purpose: Sonraki planların (actions.ts, page.tsx) bağımlı olduğu lib dosyalarını Wave 1'de hazır hale getir. Bu plan daha büyük planların alt yapısı — burada oluşturulan fonksiyonlar Wave 2 ve Wave 3'te import edilecek.
Output: 3 yeni lib dosyası (vault.ts, dataforseo/client.ts, url-categories.ts) + 1 güncelleme (projeler/[id]/page.tsx sol sütun nav) + .env.example
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-competitor-intelligence/04-CONTEXT.md
@.planning/phases/04-competitor-intelligence/04-RESEARCH.md
@.planning/phases/04-competitor-intelligence/04-PATTERNS.md

<interfaces>
<!-- Mevcut dosyalardan çıkarılan arayüzler — executor codebase'e bakmadan bu bilgileri kullanır -->

From src/lib/supabase/server.ts (pattern referansı):
```typescript
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'
// createClient() async fonksiyon döner
```

From src/app/(dashboard)/projeler/[id]/page.tsx satır 167-174 (mevcut nav):
```tsx
<Separator className="my-4" />
<Link
  href={`/projeler/${id}/kurallar`}
  className="text-sm text-muted-foreground hover:text-foreground flex items-center gap-1 px-3 py-2 rounded-md hover:bg-secondary"
>
  Proje Kuralları
</Link>
```

Env vars (mevcut .env.local):
- NEXT_PUBLIC_SUPABASE_URL
- SUPABASE_SERVICE_ROLE_KEY (vault okuma için)
- NEXT_PUBLIC_SUPABASE_ANON_KEY
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: vault.ts + dataforseo/client.ts oluştur</name>
  <files>
    src/lib/supabase/vault.ts
    src/lib/dataforseo/client.ts
    .env.example
  </files>
  <read_first>
    - src/lib/supabase/server.ts (import pattern ve createClient yapısı)
    - .env.example (mevcut env var listesi — üzerine eklenecek)
    - .planning/phases/04-competitor-intelligence/04-PATTERNS.md (vault.ts ve client.ts pattern'ları — satır 282-422)
    - .planning/phases/04-competitor-intelligence/04-RESEARCH.md (Pattern 1, Pattern 2, Pattern 3 — tam kod blokları)
  </read_first>
  <action>
**vault.ts oluştur** (`src/lib/supabase/vault.ts`):

```typescript
import 'server-only'
import { createClient } from '@supabase/supabase-js'

// Service role client — Vault okuma için anon key değil service role key gerekir
const serviceClient = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!
)

export async function getDataForSeoCredentials(): Promise<{ login: string; password: string }> {
  // Önce env var fallback — Phase 1'de Vault kurulmamış olabilir (RESEARCH.md Pitfall 4)
  if (process.env.DATAFORSEO_LOGIN && process.env.DATAFORSEO_PASSWORD) {
    return {
      login: process.env.DATAFORSEO_LOGIN,
      password: process.env.DATAFORSEO_PASSWORD,
    }
  }

  // Supabase Vault'tan oku
  const { data, error } = await serviceClient
    .from('vault.decrypted_secrets')
    .select('name, decrypted_secret')
    .in('name', ['dataforseo_login', 'dataforseo_password'])

  if (error || !data?.length) {
    throw new Error(
      'DataForSEO credentials okunamadı. DATAFORSEO_LOGIN ve DATAFORSEO_PASSWORD env var olarak .env.local dosyasına ekleyin.'
    )
  }

  const loginRow = data.find((s: { name: string; decrypted_secret: string }) => s.name === 'dataforseo_login')
  const passwordRow = data.find((s: { name: string; decrypted_secret: string }) => s.name === 'dataforseo_password')

  if (!loginRow || !passwordRow) {
    throw new Error('DataForSEO credentials eksik.')
  }

  return { login: loginRow.decrypted_secret, password: passwordRow.decrypted_secret }
}
```

**dataforseo/client.ts oluştur** (`src/lib/dataforseo/client.ts`):

```typescript
import 'server-only'

export type TopPageItem = {
  page_address: string
  metrics?: {
    organic?: {
      etv?: number
      count?: number
    }
  }
}

export async function fetchSerpDomains(
  keywords: string[],
  credentials: { login: string; password: string }
): Promise<string[]> {
  const authHeader = `Basic ${Buffer.from(`${credentials.login}:${credentials.password}`).toString('base64')}`

  const tasks = keywords.map((kw) => ({
    keyword: kw,
    location_code: 2792, // Turkey — RESEARCH.md Pitfall 2
    language_code: 'tr',
    depth: 10,
  }))

  const response = await fetch(
    'https://api.dataforseo.com/v3/serp/google/organic/live/regular',
    {
      method: 'POST',
      headers: {
        Authorization: authHeader,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(tasks),
    }
  )

  if (!response.ok) {
    throw new Error(`DataForSEO SERP API hatası: ${response.status}`)
  }

  const data = await response.json()
  const domains = new Set<string>()

  for (const task of data.tasks ?? []) {
    for (const result of task.result ?? []) {
      for (const item of result.items ?? []) {
        if (item.type === 'organic' && item.domain) {
          // www. normalizasyonu — RESEARCH.md Pitfall 3
          domains.add((item.domain as string).replace(/^www\./, ''))
        }
      }
    }
  }

  return Array.from(domains)
}

export async function fetchTopPages(
  domain: string,
  credentials: { login: string; password: string }
): Promise<TopPageItem[]> {
  const authHeader = `Basic ${Buffer.from(`${credentials.login}:${credentials.password}`).toString('base64')}`

  const cleanDomain = domain.replace(/^https?:\/\//, '').replace(/^www\./, '')

  const response = await fetch(
    'https://api.dataforseo.com/v3/dataforseo_labs/google/relevant_pages/live',
    {
      method: 'POST',
      headers: {
        Authorization: authHeader,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify([
        {
          target: cleanDomain,
          location_code: 2792,
          language_code: 'tr',
          limit: 10,
          order_by: [['metrics.organic.etv', 'desc']],
        },
      ]),
    }
  )

  if (!response.ok) {
    throw new Error(`DataForSEO Relevant Pages API hatası: ${response.status}`)
  }

  const data = await response.json()
  return (data.tasks?.[0]?.result?.[0]?.items ?? []) as TopPageItem[]
}
```

**.env.example güncellemesi**: Dosyayı oku, sonuna şunu ekle:
```
# DataForSEO (Phase 4 — Competitor Intelligence)
DATAFORSEO_LOGIN=your_dataforseo_login_email
DATAFORSEO_PASSWORD=your_dataforseo_api_password
```
  </action>
  <verify>
    <automated>grep -n "getDataForSeoCredentials" C:/Users/baris/Documents/seo-machine/src/lib/supabase/vault.ts && grep -n "fetchSerpDomains\|fetchTopPages" C:/Users/baris/Documents/seo-machine/src/lib/dataforseo/client.ts && grep -n "DATAFORSEO_LOGIN" C:/Users/baris/Documents/seo-machine/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - vault.ts içinde `import 'server-only'` satırı mevcut
    - vault.ts içinde `export async function getDataForSeoCredentials(` tanımı mevcut
    - vault.ts içinde `process.env.DATAFORSEO_LOGIN` env var fallback mevcut
    - vault.ts içinde `vault.decrypted_secrets` Vault sorgusu mevcut
    - dataforseo/client.ts içinde `import 'server-only'` satırı mevcut
    - dataforseo/client.ts içinde `export type TopPageItem` tanımı mevcut
    - dataforseo/client.ts içinde `export async function fetchSerpDomains(` tanımı mevcut
    - dataforseo/client.ts içinde `export async function fetchTopPages(` tanımı mevcut
    - dataforseo/client.ts içinde `Buffer.from(` ile Basic Auth header oluşturuluyor
    - dataforseo/client.ts içinde `location_code: 2792` Turkey kodu kullanılıyor
    - dataforseo/client.ts içinde `replace(/^www\./, '')` normalizasyonu mevcut
    - .env.example içinde `DATAFORSEO_LOGIN=` satırı mevcut
    - .env.example içinde `DATAFORSEO_PASSWORD=` satırı mevcut
  </acceptance_criteria>
  <done>vault.ts ve dataforseo/client.ts dosyaları oluşturuldu, .env.example güncellendi. DataForSEO çağrıları server-only — hiçbir credential client bundle'a girmiyor.</done>
</task>

<task type="auto">
  <name>Task 2: url-categories.ts oluştur + proje detay sol sütun nav güncelle</name>
  <files>
    src/lib/competitors/url-categories.ts
    src/app/(dashboard)/projeler/[id]/page.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/page.tsx (satır 167-174 — mevcut Kurallar nav linki, tam dosyayı oku)
    - .planning/phases/04-competitor-intelligence/04-RESEARCH.md (Pattern 4 — extractCategories tam implementasyon, satır 349-408)
    - .planning/phases/04-competitor-intelligence/04-PATTERNS.md (satır 427-458 — url-categories.ts pattern)
  </read_first>
  <action>
**url-categories.ts oluştur** (`src/lib/competitors/url-categories.ts`):

```typescript
// server-only değil — pure function, test edilebilir, client da kullanabilir
// TopPageItem client.ts'deki ile aynı shape ama bağımsız tanım (bağımsız bundle)

export type TopPageItem = {
  page_address: string
  metrics?: {
    organic?: {
      etv?: number
    }
  }
}

export type CategoryStructure = {
  [category: string]: {
    pageCount: number
    sampleUrls: string[]
    totalEtv: number
  }
}

const CATEGORY_PATTERNS: Array<{ pattern: RegExp; category: string }> = [
  { pattern: /\/blog\//i,              category: 'Blog' },
  { pattern: /\/urunler?\//i,          category: 'Ürün Sayfaları' },
  { pattern: /\/hizmetler?\//i,        category: 'Hizmetler' },
  { pattern: /\/kategori\//i,          category: 'Kategori Sayfaları' },
  { pattern: /\/products?\//i,         category: 'Ürün Sayfaları' },
  { pattern: /\/services?\//i,         category: 'Hizmetler' },
  { pattern: /\/category\//i,          category: 'Kategori Sayfaları' },
  { pattern: /\/shop\//i,              category: 'Mağaza' },
  { pattern: /\/news\//i,              category: 'Haberler' },
  { pattern: /\/about\//i,             category: 'Hakkında' },
  { pattern: /\/hakkimizda\//i,        category: 'Hakkında' },
  { pattern: /\/iletisim\//i,          category: 'İletişim' },
  { pattern: /\/contact\//i,           category: 'İletişim' },
  { pattern: /\/fiyat\//i,             category: 'Fiyatlandırma' },
  { pattern: /\/pricing\//i,           category: 'Fiyatlandırma' },
]

export function extractCategories(pages: TopPageItem[]): CategoryStructure {
  const result: CategoryStructure = {}

  for (const page of pages) {
    let matched = false
    for (const { pattern, category } of CATEGORY_PATTERNS) {
      if (pattern.test(page.page_address)) {
        if (!result[category]) {
          result[category] = { pageCount: 0, sampleUrls: [], totalEtv: 0 }
        }
        result[category].pageCount++
        result[category].totalEtv += page.metrics?.organic?.etv ?? 0
        if (result[category].sampleUrls.length < 3) {
          result[category].sampleUrls.push(page.page_address)
        }
        matched = true
        break
      }
    }
    if (!matched) {
      if (!result['Diğer']) {
        result['Diğer'] = { pageCount: 0, sampleUrls: [], totalEtv: 0 }
      }
      result['Diğer'].pageCount++
    }
  }

  return result
}
```

**projeler/[id]/page.tsx güncelleme:**

Satır 167-174 arasındaki mevcut nav bloğunu bul:
```tsx
{/* Proje Kuralları — Separator + nav linki (Phase 3) */}
<Separator className="my-4" />
<Link
  href={`/projeler/${id}/kurallar`}
  className="text-sm text-muted-foreground hover:text-foreground flex items-center gap-1 px-3 py-2 rounded-md hover:bg-secondary"
>
  Proje Kuralları
</Link>
```

Bu bloğu şununla değiştir (Separator kalır, Rakipler linki Kurallar'ın üstüne gelir — D-02 kararı):
```tsx
{/* Proje araçları — Separator + nav linkleri (Phase 3+) */}
<Separator className="my-4" />
<Link
  href={`/projeler/${id}/rakipler`}
  className="text-sm text-muted-foreground hover:text-foreground flex items-center gap-1 px-3 py-2 rounded-md hover:bg-secondary"
>
  Rakipler
</Link>
<Link
  href={`/projeler/${id}/kurallar`}
  className="text-sm text-muted-foreground hover:text-foreground flex items-center gap-1 px-3 py-2 rounded-md hover:bg-secondary"
>
  Proje Kuralları
</Link>
```

Import satırlarına dokunma — Link zaten import edilmiş.
  </action>
  <verify>
    <automated>grep -n "extractCategories\|CategoryStructure\|CATEGORY_PATTERNS" C:/Users/baris/Documents/seo-machine/src/lib/competitors/url-categories.ts && grep -n "rakipler" C:/Users/baris/Documents/seo-machine/src/app/\(dashboard\)/projeler/\[id\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - url-categories.ts içinde `export type CategoryStructure` tanımı mevcut
    - url-categories.ts içinde `export function extractCategories(pages: TopPageItem[])` tanımı mevcut
    - url-categories.ts içinde `CATEGORY_PATTERNS` array'i en az 10 pattern içeriyor
    - url-categories.ts içinde `'Blog'`, `'Ürün Sayfaları'`, `'Hizmetler'` kategorileri mevcut
    - url-categories.ts içinde `'Diğer'` fallback kategorisi mevcut
    - projeler/[id]/page.tsx içinde `href={`/projeler/${id}/rakipler`}` mevcut
    - projeler/[id]/page.tsx içinde `Rakipler` link metni mevcut
    - projeler/[id]/page.tsx içinde Rakipler linki Kurallar linkinden önce geliyor (satır sırasına göre)
    - projeler/[id]/page.tsx derleme hatası yok — TypeScript hataları kontrol et
  </acceptance_criteria>
  <done>url-categories.ts pure function olarak oluşturuldu (test edilebilir, server-only değil). Proje detay sol sütununda Rakipler linki Kurallar'ın üstünde yerini aldı. D-01 ve D-02 kararları uygulandı.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → server code | DATAFORSEO_LOGIN/PASSWORD sadece server-side okunur, client bundle'a girmez |
| vault.ts → Supabase Vault | Service role key ile sorgu — anon key yetkilendirmesi dışında |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | vault.ts / client.ts | mitigate | `import 'server-only'` her iki dosyada — Next.js build time'da client bundle'a girmeyi engeller; DATAFORSEO_LOGIN hiçbir zaman NEXT_PUBLIC_ prefix'i almaz |
| T-04-02 | Information Disclosure | .env.example | accept | .env.example sadece placeholder değerler içerir, gerçek credentials değil; .gitignore .env.local'ı hariç tutar |
| T-04-03 | Tampering | extractCategories() | accept | Pure function, dış input almaz — sadece server'dan gelen DataForSEO response'unu işler; injection riski yok |
</threat_model>

<verification>
Plan tamamlandığında şunlar doğrulanabilir olmalı:
1. `src/lib/supabase/vault.ts` dosyası mevcut ve `getDataForSeoCredentials` export ediyor
2. `src/lib/dataforseo/client.ts` dosyası mevcut ve `fetchSerpDomains`, `fetchTopPages` export ediyor
3. `src/lib/competitors/url-categories.ts` dosyası mevcut ve `extractCategories` export ediyor
4. `.env.example` içinde `DATAFORSEO_LOGIN` ve `DATAFORSEO_PASSWORD` satırları mevcut
5. `/projeler/[id]` sayfasında Rakipler linki sol sütunda Kurallar'ın üstünde görünüyor
6. `npx tsc --noEmit` hata üretmiyor (TypeScript kontrolü)
</verification>

<success_criteria>
- vault.ts, dataforseo/client.ts, url-categories.ts dosyaları oluşturuldu
- Her iki DataForSEO client fonksiyonu server-only — client bundle'a girmiyor
- Env var fallback çalışıyor — Vault kurulmamışsa DATAFORSEO_LOGIN/PASSWORD env var yeterli
- Proje detay sayfasında Rakipler nav linki mevcut (D-01, D-02 kararları uygulandı)
- TypeScript hataları yok
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/04-competitor-intelligence/04-01-SUMMARY.md` oluştur.
</output>
