---
phase: 04-competitor-intelligence
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/app/(dashboard)/projeler/[id]/rakipler/actions.ts
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "addCompetitor() domain'i competitors tablosuna source='manual' ile INSERT eder"
    - "discoverCompetitors() DataForSEO SERP API'yi çağırır ve domain[] döner — DB'ye yazmaz"
    - "addCompetitors() domain listesini toplu INSERT eder (source='serp')"
    - "fetchCompetitorData() DataForSEO Relevant Pages çağırır, top_pages + category_structure JSONB'ye yazar"
    - "Her action auth guard + ownership check içeriyor"
    - "domain INSERT öncesi www. normalizasyonu uygulanıyor"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/rakipler/actions.ts"
      provides: "addCompetitor, discoverCompetitors, addCompetitors, fetchCompetitorData Server Actions"
      exports: ["addCompetitor", "discoverCompetitors", "addCompetitors", "fetchCompetitorData", "ActionResult"]
  key_links:
    - from: "actions.ts addCompetitor"
      to: "supabase.from('competitors').insert"
      via: "Supabase client"
      pattern: "from('competitors').insert"
    - from: "actions.ts discoverCompetitors"
      to: "src/lib/dataforseo/client.ts fetchSerpDomains"
      via: "import"
      pattern: "fetchSerpDomains"
    - from: "actions.ts fetchCompetitorData"
      to: "src/lib/dataforseo/client.ts fetchTopPages"
      via: "import"
      pattern: "fetchTopPages"
    - from: "actions.ts fetchCompetitorData"
      to: "src/lib/competitors/url-categories.ts extractCategories"
      via: "import"
      pattern: "extractCategories"
---

<objective>
Rakip yönetimi için tüm Server Action'ları tek dosyada oluştur: manuel ekleme, SERP keşfi, toplu ekleme, veri çekme.

Purpose: Wave 3'teki UI (page.tsx + Dialog) bu action'ları import edecek. Tüm DataForSEO çağrıları, auth guard ve ownership check mantığı bu dosyada yaşar — UI saf presentation layer olarak kalır.
Output: src/app/(dashboard)/projeler/[id]/rakipler/actions.ts — 4 export function + ActionResult type
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-competitor-intelligence/04-CONTEXT.md
@.planning/phases/04-competitor-intelligence/04-RESEARCH.md
@.planning/phases/04-competitor-intelligence/04-PATTERNS.md
@.planning/phases/04-competitor-intelligence/04-01-SUMMARY.md

<interfaces>
<!-- Wave 1'de oluşturulan lib dosyaları — executor bu signature'ları kullanır -->

From src/lib/supabase/vault.ts (Wave 1'de oluşturuldu):
```typescript
export async function getDataForSeoCredentials(): Promise<{ login: string; password: string }>
```

From src/lib/dataforseo/client.ts (Wave 1'de oluşturuldu):
```typescript
export type TopPageItem = {
  page_address: string
  metrics?: { organic?: { etv?: number; count?: number } }
}
export async function fetchSerpDomains(keywords: string[], credentials: { login: string; password: string }): Promise<string[]>
export async function fetchTopPages(domain: string, credentials: { login: string; password: string }): Promise<TopPageItem[]>
```

From src/lib/competitors/url-categories.ts (Wave 1'de oluşturuldu):
```typescript
export type CategoryStructure = {
  [category: string]: { pageCount: number; sampleUrls: string[]; totalEtv: number }
}
export function extractCategories(pages: TopPageItem[]): CategoryStructure
```

From src/app/(dashboard)/projeler/[id]/kurallar/actions.ts (pattern referansı):
```typescript
'use server'
import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'

export type ToggleProjectRuleResult = { success: true } | { success: false; error: string }

// Auth guard pattern:
const { data: { user } } = await supabase.auth.getUser()
if (!user) return { success: false, error: 'Oturum bulunamadı.' }

// Ownership check pattern:
const { data: project } = await supabase
  .from('projects').select('id')
  .eq('id', projectId).eq('user_id', user.id).single()
if (!project) return { success: false, error: 'Proje bulunamadı.' }
```

Supabase competitors tablosu şeması:
```sql
id UUID, user_id UUID, project_id UUID,
domain TEXT NOT NULL,
source TEXT NOT NULL DEFAULT 'manual',  -- 'manual' | 'serp'
top_pages JSONB, category_structure JSONB,
content_areas JSONB, gap_report JSONB,
created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: actions.ts — addCompetitor + addCompetitors + discoverCompetitors</name>
  <files>
    src/app/(dashboard)/projeler/[id]/rakipler/actions.ts
  </files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/kurallar/actions.ts (tam dosya — auth guard, ownership check, ActionResult pattern'ı)
    - .planning/phases/04-competitor-intelligence/04-PATTERNS.md (satır 140-242 — actions.ts pattern)
    - .planning/phases/04-competitor-intelligence/04-01-SUMMARY.md (Wave 1'de oluşturulan lib dosyalarının path'leri)
  </read_first>
  <action>
`src/app/(dashboard)/projeler/[id]/rakipler/` dizinini oluştur ve `actions.ts` dosyasını yaz:

```typescript
'use server'

import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'
import { getDataForSeoCredentials } from '@/lib/supabase/vault'
import { fetchSerpDomains } from '@/lib/dataforseo/client'

export type ActionResult =
  | { success: true }
  | { success: false; error: string }

export type DiscoverResult =
  | { success: true; domains: string[] }
  | { success: false; error: string }

// ─── Yardımcı: ownership check ────────────────────────────────────────────────

async function verifyProjectOwnership(
  supabase: Awaited<ReturnType<typeof createClient>>,
  projectId: string,
  userId: string
): Promise<boolean> {
  const { data } = await supabase
    .from('projects')
    .select('id')
    .eq('id', projectId)
    .eq('user_id', userId)
    .single()
  return !!data
}

// ─── COMP-01: Manuel rakip ekleme ─────────────────────────────────────────────

export async function addCompetitor(
  projectId: string,
  domain: string
): Promise<ActionResult> {
  const supabase = await createClient()

  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) {
    return { success: false, error: 'Oturum bulunamadı.' }
  }

  const isOwner = await verifyProjectOwnership(supabase, projectId, user.id)
  if (!isOwner) {
    return { success: false, error: 'Proje bulunamadı.' }
  }

  // Zod validate edilmiş domain buraya ulaşır — ek normalize (www. strip)
  const normalizedDomain = domain.replace(/^https?:\/\//, '').replace(/^www\./, '').trim()

  if (!normalizedDomain) {
    return { success: false, error: 'Geçersiz domain.' }
  }

  const { error } = await supabase.from('competitors').insert({
    user_id: user.id,
    project_id: projectId,
    domain: normalizedDomain,
    source: 'manual',
  })

  if (error) {
    return { success: false, error: 'Rakip eklenemedi. Lütfen tekrar deneyin.' }
  }

  revalidatePath(`/projeler/${projectId}/rakipler`)
  return { success: true }
}

// ─── COMP-02: SERP keşfi (DB'ye yazmaz — sadece domain listesi döner) ─────────

export async function discoverCompetitors(
  projectId: string,
  keywords: string[]
): Promise<DiscoverResult> {
  const supabase = await createClient()

  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) {
    return { success: false, error: 'Oturum bulunamadı.' }
  }

  const isOwner = await verifyProjectOwnership(supabase, projectId, user.id)
  if (!isOwner) {
    return { success: false, error: 'Proje bulunamadı.' }
  }

  // Keyword validasyonu: 1-3 arası, her biri max 700 karakter
  if (!keywords.length || keywords.length > 3) {
    return { success: false, error: '1 ile 3 arasında keyword girin.' }
  }
  if (keywords.some((kw) => kw.trim().length === 0 || kw.length > 700)) {
    return { success: false, error: 'Keyword boş olamaz ve 700 karakterden uzun olamaz.' }
  }

  try {
    const credentials = await getDataForSeoCredentials()
    const domains = await fetchSerpDomains(keywords, credentials)
    return { success: true, domains }
  } catch (err) {
    const message = err instanceof Error ? err.message : 'Bilinmeyen hata'
    return { success: false, error: `Rakip keşfi başarısız: ${message}` }
  }
}

// ─── COMP-02: Seçilen rakipleri toplu ekle (SERP keşif onayından sonra) ───────

export async function addCompetitors(
  projectId: string,
  domains: string[]
): Promise<ActionResult> {
  const supabase = await createClient()

  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) {
    return { success: false, error: 'Oturum bulunamadı.' }
  }

  const isOwner = await verifyProjectOwnership(supabase, projectId, user.id)
  if (!isOwner) {
    return { success: false, error: 'Proje bulunamadı.' }
  }

  if (!domains.length) {
    return { success: false, error: 'En az bir domain seçin.' }
  }

  const rows = domains.map((d) => ({
    user_id: user.id,
    project_id: projectId,
    domain: d.replace(/^www\./, '').trim(),
    source: 'serp' as const,
  }))

  // ignoreDuplicates: true — aynı domain tekrar eklenirse hata verme
  const { error } = await supabase.from('competitors').insert(rows)

  if (error) {
    return { success: false, error: 'Rakipler eklenemedi. Lütfen tekrar deneyin.' }
  }

  revalidatePath(`/projeler/${projectId}/rakipler`)
  return { success: true }
}
```

**Önemli:** `discoverCompetitors` sadece domain listesi döner, DB'ye yazmaz. Kullanıcı seçtikten sonra ayrı `addCompetitors` çağrısı yapılır (D-03 dialog flow).
  </action>
  <verify>
    <automated>grep -n "export async function\|export type" C:/Users/baris/Documents/seo-machine/src/app/\(dashboard\)/projeler/\[id\]/rakipler/actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - actions.ts başında `'use server'` direktifi mevcut
    - actions.ts içinde `export type ActionResult` tanımı mevcut
    - actions.ts içinde `export type DiscoverResult` tanımı mevcut
    - actions.ts içinde `export async function addCompetitor(` tanımı mevcut
    - actions.ts içinde `export async function discoverCompetitors(` tanımı mevcut
    - actions.ts içinde `export async function addCompetitors(` tanımı mevcut
    - actions.ts içinde `from('competitors').insert` çağrısı mevcut
    - actions.ts içinde `replace(/^www\./, '')` normalizasyonu mevcut
    - actions.ts içinde `source: 'manual'` ve `source: 'serp'` ayrımı mevcut
    - actions.ts içinde `getDataForSeoCredentials` import edilmiş
    - actions.ts içinde `fetchSerpDomains` import edilmiş
    - actions.ts içinde her action'da `supabase.auth.getUser()` auth guard mevcut
    - actions.ts içinde `revalidatePath(\`/projeler/${projectId}/rakipler\`)` çağrısı mevcut
  </acceptance_criteria>
  <done>addCompetitor, discoverCompetitors, addCompetitors Server Action'ları oluşturuldu. COMP-01 (manuel ekleme) ve COMP-02 (SERP keşfi) kapsandı.</done>
</task>

<task type="auto">
  <name>Task 2: actions.ts — fetchCompetitorData (COMP-03)</name>
  <files>
    src/app/(dashboard)/projeler/[id]/rakipler/actions.ts
  </files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/rakipler/actions.ts (Task 1'de oluşturuldu — mevcut içerik ve import'lar)
    - .planning/phases/04-competitor-intelligence/04-RESEARCH.md (Pattern 3 — fetchTopPages response shape, satır 295-338; Pattern 4 — extractCategories, satır 349-408)
    - .planning/phases/04-competitor-intelligence/04-CONTEXT.md (D-05, D-06, D-07 kararları)
  </read_first>
  <action>
`actions.ts` dosyasına iki yeni import ve `fetchCompetitorData` fonksiyonu ekle. Dosyayı oku, mevcut import bloğuna şunları ekle:

```typescript
import { fetchTopPages } from '@/lib/dataforseo/client'
import { extractCategories } from '@/lib/competitors/url-categories'
```

Ardından dosyanın sonuna `fetchCompetitorData` fonksiyonunu ekle:

```typescript
// ─── COMP-03: Rakip top pages + kategori yapısı çek ──────────────────────────

export async function fetchCompetitorData(
  competitorId: string,
  projectId: string
): Promise<ActionResult> {
  const supabase = await createClient()

  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) {
    return { success: false, error: 'Oturum bulunamadı.' }
  }

  // Rakip ownership check — hem project_id hem user_id filtresi (T-04-02)
  const { data: competitor } = await supabase
    .from('competitors')
    .select('id, domain')
    .eq('id', competitorId)
    .eq('project_id', projectId)
    .eq('user_id', user.id)
    .single()

  if (!competitor) {
    return { success: false, error: 'Rakip bulunamadı.' }
  }

  try {
    const credentials = await getDataForSeoCredentials()

    // DataForSEO Relevant Pages API — top 10 organik sayfa (D-05)
    const pages = await fetchTopPages(competitor.domain, credentials)

    // Top pages — URL + tahmini trafik (title bu endpoint'te gelmiyor — RESEARCH.md Pitfall 1)
    const topPages = pages.map((p) => ({
      url: p.page_address,
      etv: p.metrics?.organic?.etv ?? 0,
    }))

    // URL pattern analizi ile kategori çıkarımı (D-05)
    const categoryStructure = extractCategories(pages)

    // competitors tablosuna JSONB UPDATE (D-07)
    const { error } = await supabase
      .from('competitors')
      .update({
        top_pages: topPages,
        category_structure: categoryStructure,
        updated_at: new Date().toISOString(),
      })
      .eq('id', competitorId)
      .eq('user_id', user.id)

    if (error) {
      return { success: false, error: 'Veri kaydedilemedi. Lütfen tekrar deneyin.' }
    }

    revalidatePath(`/projeler/${projectId}/rakipler`)
    return { success: true }
  } catch (err) {
    const message = err instanceof Error ? err.message : 'Bilinmeyen hata'
    return { success: false, error: `Veri çekme başarısız: ${message}` }
  }
}
```

**Kritik detaylar:**
- `fetchCompetitorData` competitor'ı `competitorId + projectId + user_id` üçlüsüyle çeker — başka kullanıcının competitor'ına erişim engellenir (T-04-02)
- `top_pages` JSONB'de title yok — RESEARCH.md Pitfall 1 uyarısı uygulandı, sadece url + etv saklanır
- `updated_at` manuel set edilir — kullanıcı son çekim zamanını görebilir (D-06'daki "son çekim tarihi")

Ardından `fetchCompetitorData`'nın hemen altına `fetchOwnDomainData` fonksiyonunu ekle:

```typescript
// ─── D-10: Kullanıcı domain'i için kategori yapısı çek (DB'ye yazılmaz) ────────

export async function fetchOwnDomainData(
  projectId: string,
  domain: string
): Promise<Record<string, number> | null> {
  const supabase = await createClient()

  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) return null

  // Ownership check — sadece kendi projesini analiz edebilir
  const { data: project } = await supabase
    .from('projects')
    .select('id')
    .eq('id', projectId)
    .eq('user_id', user.id)
    .single()
  if (!project) return null

  try {
    const credentials = await getDataForSeoCredentials()

    // relevant_pages endpoint — lokasyon bağımsız (Q2 RESOLVED)
    const pages = await fetchTopPages(domain, credentials)

    // URL pattern analizi ile kategori → sayfa sayısı haritası
    const categoryStructure = extractCategories(pages)

    // Record<string, number>: kategori → pageCount (SSR render için yeterli)
    const result: Record<string, number> = {}
    for (const [cat, data] of Object.entries(categoryStructure)) {
      result[cat] = data.pageCount
    }

    return result
  } catch {
    return null
  }
}
```

**Kritik detaylar:**
- `fetchOwnDomainData` sonucu DB'ye yazılmaz — sadece çağıran SSR page.tsx'e döner (Q3 RESOLVED)
- Auth + ownership check zorunlu — başka projenin domain'i analiz edilemez
- `Record<string, number>` döner: kategori adı → sayfa sayısı — gap tablosu bu veriyi `userCategoryStructure` olarak kullanır
- `null` dönerse page.tsx gap tablosunda "Kendi siteniz için veri çekilmedi" mesajı + `OwnDomainAnalyzeButton` gösterilir
  </action>
  <verify>
    <automated>grep -n "fetchCompetitorData\|fetchTopPages\|extractCategories\|category_structure\|fetchOwnDomainData" C:/Users/baris/Documents/seo-machine/src/app/\(dashboard\)/projeler/\[id\]/rakipler/actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - actions.ts içinde `export async function fetchCompetitorData(` tanımı mevcut
    - actions.ts içinde `fetchTopPages` import edilmiş
    - actions.ts içinde `extractCategories` import edilmiş
    - actions.ts içinde `from('competitors').update({` çağrısı mevcut
    - actions.ts içinde `top_pages: topPages` ve `category_structure: categoryStructure` update payload'da mevcut
    - actions.ts içinde `updated_at: new Date().toISOString()` mevcut
    - actions.ts içinde competitor sorgusu `.eq('id', competitorId).eq('project_id', projectId).eq('user_id', user.id)` üçlü filtre kullanıyor
    - actions.ts içinde `export async function fetchOwnDomainData(` tanımı mevcut
    - actions.ts içinde `fetchOwnDomainData` fonksiyonu `Record<string, number> | null` döndürüyor
    - actions.ts içinde `fetchOwnDomainData` auth guard + ownership check içeriyor
    - actions.ts içinde `fetchOwnDomainData` sonucu DB'ye yazmıyor (insert/update çağrısı yok)
    - actions.ts TypeScript hataları yok — `npx tsc --noEmit` temiz geçiyor
  </acceptance_criteria>
  <done>fetchCompetitorData ve fetchOwnDomainData eklendi. COMP-03 kapsandı: DataForSEO Relevant Pages çağrısı → URL pattern analizi → top_pages + category_structure JSONB UPDATE. fetchOwnDomainData D-10 için kullanıcı domain kategorilerini DB'ye yazmadan döndürür. Tüm DataForSEO çağrıları server-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server Action | Form input (domain, keywords) server'da validate edilir — client doğrudan DB'ye yazamaz |
| Server Action → Supabase | user_id + project_id double filter — başka kullanıcının verisi okunamaz/yazılamaz |
| Server Action → DataForSEO | API key server-only — client bundle'a sızmaz |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | DataForSEO credentials → actions.ts | mitigate | `getDataForSeoCredentials()` vault.ts'den import — server-only chain; DATAFORSEO_LOGIN asla client'a gitmez |
| T-04-05 | Information Disclosure | fetchCompetitorData — başka kullanıcının verisi | mitigate | `competitorId + projectId + user_id` üçlü ownership filter; tek bir filter eksik olursa veri sızmaz |
| T-04-06 | Tampering | addCompetitor — domain injection | mitigate | `domain.replace(/^https?:\/\//, '').replace(/^www\./, '').trim()` sanitize + Supabase parameterized query; SQL injection yok |
| T-04-07 | Denial of Service | discoverCompetitors — aşırı SERP çağrısı | mitigate | keyword sayısı 1-3 ile sınırlandırılıyor; human-triggered (D-04) — otomatik tetikleme yok |
</threat_model>

<verification>
Plan tamamlandığında şunlar doğrulanabilir olmalı:
1. `src/app/(dashboard)/projeler/[id]/rakipler/actions.ts` dosyası mevcut
2. `addCompetitor`, `addCompetitors`, `discoverCompetitors`, `fetchCompetitorData` fonksiyonları export edilmiş
3. Her fonksiyonda auth guard (`getUser()`) + ownership check (`eq('user_id', ...)`) mevcut
4. DataForSEO çağrıları server-only lib'den import ediliyor — doğrudan fetch yok
5. `npx tsc --noEmit` TypeScript hatası üretmiyor
</verification>

<success_criteria>
- 4 Server Action export edildi: addCompetitor (COMP-01), discoverCompetitors (COMP-02), addCompetitors (COMP-02), fetchCompetitorData (COMP-03)
- Her action auth guard + ownership check içeriyor
- Domain normalize ediliyor (www. strip)
- DataForSEO çağrıları try/catch ile sarılmış, hata mesajları Türkçe
- TypeScript hataları yok
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/04-competitor-intelligence/04-02-SUMMARY.md` oluştur.
</output>
