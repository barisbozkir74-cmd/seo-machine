---
phase: 04-competitor-intelligence
plan: 04
type: execute
wave: 4
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
files_modified:
  - src/app/(dashboard)/projeler/[id]/rakipler/actions.ts
  - src/app/(dashboard)/projeler/[id]/rakipler/OwnDomainAnalyzeButton.tsx
  - src/app/(dashboard)/projeler/[id]/rakipler/page.tsx
  - supabase/migrations/20260424000001_add_own_category_structure.sql
autonomous: true
requirements:
  - COMP-03
  - COMP-04
gap_closure: true

must_haves:
  truths:
    - "Her rakip için content_areas (içerik konu alanları) DB'ye yazılır ve gap tablosunun yanında rakip satırında gösterilir"
    - "OwnDomainAnalyzeButton tıklandığında fetchOwnDomainData sonucu projects.own_category_structure kolonuna yazılır ve sayfa revalidate edilir; buton tıklandıktan sonra 'Sizin Siteniz' sütunu dolar"
    - "Gap tablosunda her satır için 'Fırsat Skoru' görünür — kullanıcının olmadığı ama 2+ rakibin sahip olduğu kategoriler fırsat olarak işaretlenir"
    - "addCompetitors maksimum 20 domain kabul eder; her domain 253 karakterden uzunsa atlanır"
  artifacts:
    - path: "supabase/migrations/20260424000001_add_own_category_structure.sql"
      provides: "projects tablosuna own_category_structure JSONB kolonu"
      contains: "ALTER TABLE public.projects ADD COLUMN own_category_structure"
    - path: "src/app/(dashboard)/projeler/[id]/rakipler/actions.ts"
      provides: "content_areas yazımı, own_category_structure persist, addCompetitors validasyon, WR-01/02/03 düzeltmeleri"
      exports: ["addCompetitor", "addCompetitors", "fetchCompetitorData", "fetchOwnDomainData"]
    - path: "src/app/(dashboard)/projeler/[id]/rakipler/OwnDomainAnalyzeButton.tsx"
      provides: "revalidatePath tabanlı state güncelleme — artık no-op değil"
      contains: "revalidatePath"
    - path: "src/app/(dashboard)/projeler/[id]/rakipler/page.tsx"
      provides: "content_areas render, Fırsat Skoru sütunu, own_category_structure DB okuma"
      contains: "Fırsat Skoru"
  key_links:
    - from: "actions.ts fetchCompetitorData"
      to: "competitors.content_areas JSONB"
      via: "supabase update"
      pattern: "content_areas"
    - from: "actions.ts fetchOwnDomainData"
      to: "projects.own_category_structure JSONB"
      via: "supabase update"
      pattern: "own_category_structure"
    - from: "OwnDomainAnalyzeButton"
      to: "revalidatePath"
      via: "Server Action return + client revalidate"
      pattern: "revalidatePath"
    - from: "page.tsx buildGapReport"
      to: "Fırsat Skoru hesaplama"
      via: "competitor count per category where user is absent"
      pattern: "Fırsat Skoru"
---

<objective>
Phase 4 gap closure planı — VERIFICATION.md'de tespit edilen 4 blocker ve 3 warning düzeltilir.

Purpose: SC-3 (COMP-03) ve SC-4 (COMP-04) başarısız kriterlerini karşılamak; content_areas populate edilmesi, OwnDomainAnalyzeButton'ın kalıcı state değişikliği üretmesi, gap raporuna fırsat sinyali eklenmesi ve addCompetitors güvenlik validasyonu.

Output:
- Yeni Supabase migration: projects.own_category_structure JSONB kolonu
- actions.ts güncellemesi: content_areas yazımı, own_category_structure persist, input validasyon iyileştirmeleri
- OwnDomainAnalyzeButton.tsx yeniden yazımı: revalidatePath tabanlı akış
- page.tsx güncellemesi: content_areas render + Fırsat Skoru sütunu + DB'den own_category_structure okuma
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-competitor-intelligence/04-CONTEXT.md
@.planning/phases/04-competitor-intelligence/04-VERIFICATION.md

<interfaces>
<!-- Mevcut kod durumu — executor bu state'ten başlar. Sadece değişecek kısımlar belirtilmiştir. -->

Mevcut actions.ts — fetchCompetitorData (satır 157-219):
```typescript
// Şu an sadece top_pages + category_structure update ediyor, content_areas YOK:
const { error } = await supabase
  .from('competitors')
  .update({
    top_pages: topPages,
    category_structure: categoryStructure,
    updated_at: new Date().toISOString(),
  })
  .eq('id', competitorId)
  .eq('user_id', user.id)
```

Mevcut actions.ts — fetchOwnDomainData (satır 223-262):
```typescript
// DB'ye HİÇ yazmıyor — result sadece return ediliyor:
export async function fetchOwnDomainData(
  projectId: string,
  domain: string
): Promise<Record<string, number> | null> {
  // ... DataForSEO çekiyor, categoryStructure üretiyor ...
  // ... return result — persist YOK ...
  } catch {
    return null  // WR-02: hata loglanmıyor
  }
}
```

Mevcut actions.ts — addCompetitors (satır 115-153):
```typescript
// WR-01: comment var ama ignoreDuplicates: true seçeneği geçirilmiyor
// CR-02: domain count ve length validasyonu YOK
const { error } = await supabase.from('competitors').insert(rows)
// Olması gereken:
// .insert(rows, { onConflict: 'project_id,domain', ignoreDuplicates: true })
```

Mevcut OwnDomainAnalyzeButton.tsx:
```typescript
// CR-01: fetchOwnDomainData çağrısı sonucu discard, window.location.reload() no-op:
const { fetchOwnDomainData } = await import('./actions')
await fetchOwnDomainData(projectId, domain)
window.location.reload()  // SSR aynı null değeri döner — hiçbir şey değişmez
```

Mevcut competitors tablosu — schema (20260422000001_create_tables.sql):
```sql
-- content_areas JSONB kolonu ZATEN VAR — schema'da tanımlı, sadece hiç yazılmıyor:
content_areas JSONB,
```

projects tablosu — own_category_structure YOK:
```sql
-- Bu kolon şu an mevcut DEĞİL — migration ile eklenecek:
-- own_category_structure JSONB
```

CategoryStructure type (url-categories.ts):
```typescript
export type CategoryStructure = {
  [category: string]: {
    pageCount: number
    sampleUrls: string[]
    totalEtv: number
  }
}
// content_areas için ContentAreas type: CategoryStructure ile aynı shape kullanılacak
// Örnek: { "Blog": { pageCount: 5, sampleUrls: [...], totalEtv: 120 }, "Hizmetler": {...} }
// "Diğer" kategorisi content_areas'a dahil edilmez
```

page.tsx — buildGapReport fonksiyonu (satır 51-96):
```typescript
// Şu an sadece binary varlık matrisi döndürüyor:
type GapMatrix = {
  categories: string[]
  matrix: { [domain: string]: { [category: string]: { exists: boolean; pageCount: number } } }
}
// Fırsat sinyali YOK — kategoride kullanıcı yok ama 2+ rakip var durumu işaretlenmiyor
```

page.tsx — SSR'da ownCategoryData çekimi (satır 135):
```typescript
// Şu an DataForSEO'ya SSR'da her page load'da istek atıyor:
const ownCategoryData = await fetchOwnDomainData(id, project.domain).catch(() => null)
// Olması gereken: önce projects.own_category_structure DB'den oku,
// yoksa (null ise) fetchOwnDomainData'yı çağırma — sadece DB'den oku
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB migration + actions.ts gap düzeltmeleri (content_areas, own_category_structure persist, CR-02, WR-01/02/03)</name>
  <files>
    supabase/migrations/20260424000001_add_own_category_structure.sql
    src/app/(dashboard)/projeler/[id]/rakipler/actions.ts
  </files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/rakipler/actions.ts (tam dosya — mevcut state, satır 1-262)
    - supabase/migrations/20260422000001_create_tables.sql (competitors tablosu yapısı, satır 82-97; projects tablosu, satır 21-37)
    - src/lib/competitors/url-categories.ts (CategoryStructure type ve extractCategories dönüş değeri — satır 1-67)
    - supabase/migrations/20260423000001_rules_unique_constraint.sql (migration format referansı)
  </read_first>
  <action>
**ADIM 1: Yeni Supabase migration oluştur**

`supabase/migrations/20260424000001_add_own_category_structure.sql` dosyasını oluştur:

```sql
-- =============================================================================
-- Phase 4 Gap Closure: projects tablosuna own_category_structure kolonu ekle
-- CR-01 fix: OwnDomainAnalyzeButton'ın ürettiği veri artık persist edilecek
-- =============================================================================

ALTER TABLE public.projects
  ADD COLUMN IF NOT EXISTS own_category_structure JSONB;

-- Bu kolon kullanıcının kendi domain'inin kategori yapısını saklar.
-- Shape: { [category: string]: number } (category -> pageCount)
-- "Kendi Sitemi Analiz Et" butonu tıklandığında fetchOwnDomainData bu kolona yazar.
-- Null = henüz analiz yapılmamış.
```

**ADIM 2: actions.ts'i güncelle — tüm değişiklikler tek dosyada**

Aşağıdaki 5 değişikliği mevcut actions.ts'e uygula:

---

**Değişiklik A — WR-03: fetchOwnDomainData'ya null domain guard ekle**

`fetchOwnDomainData` fonksiyonunun başına (supabase client oluşturduktan hemen sonra, ownership check'ten önce):

```typescript
// WR-03: domain null veya boş ise DataForSEO'ya istek atma
if (!domain || domain.trim() === '' || domain === 'null') {
  return null
}
```

---

**Değişiklik B — WR-02: catch bloğuna console.error ekle**

`fetchOwnDomainData`'nın `catch` bloğunu güncelle:

```typescript
// Önce (mevcut):
} catch {
  return null
}

// Sonra:
} catch (err) {
  console.error('[fetchOwnDomainData] DataForSEO error:', err)
  return null
}
```

---

**Değişiklik C — CR-01 FIX: fetchOwnDomainData sonucunu projects tablosuna persist et + revalidatePath ekle**

`fetchOwnDomainData` fonksiyonunun return tipini `Promise<Record<string, number> | null>` olarak koru (değişmez). Ancak DataForSEO'dan veri çekildikten sonra DB'ye yaz:

`result` hesaplandıktan hemen sonra, return'dan önce şunu ekle:

```typescript
// CR-01 FIX: Sonucu projects.own_category_structure kolonuna persist et
await supabase
  .from('projects')
  .update({ own_category_structure: result })
  .eq('id', projectId)
  .eq('user_id', user.id)

revalidatePath(`/projeler/${projectId}/rakipler`)
```

`revalidatePath` import'unu dosyanın başına ekle (zaten `import { revalidatePath } from 'next/cache'` mevcut — ek import gerekmez).

---

**Değişiklik D — COMP-03 FIX: fetchCompetitorData içinde content_areas hesapla ve persist et**

`fetchCompetitorData` fonksiyonunda `categoryStructure = extractCategories(pages)` satırından hemen sonra:

```typescript
// COMP-03 FIX: content_areas — "Diğer" hariç tüm kategoriler (içerik konu alanları)
// CategoryStructure ile aynı shape; gap tablosunda gösterilmek yerine per-competitor sidebar'da render edilecek
const contentAreas: typeof categoryStructure = {}
for (const [cat, data] of Object.entries(categoryStructure)) {
  if (cat !== 'Diğer') {
    contentAreas[cat] = data
  }
}
```

Ardından `.update()` çağrısını güncelle — `content_areas` alanını ekle:

```typescript
// Önce (mevcut):
const { error } = await supabase
  .from('competitors')
  .update({
    top_pages: topPages,
    category_structure: categoryStructure,
    updated_at: new Date().toISOString(),
  })
  .eq('id', competitorId)
  .eq('user_id', user.id)

// Sonra:
const { error } = await supabase
  .from('competitors')
  .update({
    top_pages: topPages,
    category_structure: categoryStructure,
    content_areas: contentAreas,
    updated_at: new Date().toISOString(),
  })
  .eq('id', competitorId)
  .eq('user_id', user.id)
```

---

**Değişiklik E — CR-02 FIX + WR-01 FIX: addCompetitors validasyon ve ignoreDuplicates**

`addCompetitors` fonksiyonunda, `if (!domains.length)` kontrolünden hemen sonra ekle:

```typescript
// CR-02 FIX: Maksimum domain sayısı ve uzunluk kontrolü
const MAX_DOMAINS = 20
if (domains.length > MAX_DOMAINS) {
  return { success: false, error: `Maksimum ${MAX_DOMAINS} rakip aynı anda eklenebilir.` }
}
```

`rows` array'ini oluştururken domain length filtresini ekle:

```typescript
// Önce (mevcut):
const rows = domains.map((d) => ({
  user_id: user.id,
  project_id: projectId,
  domain: d.replace(/^www\./, '').trim(),
  source: 'serp' as const,
}))

// Sonra:
const rows = domains
  .filter((d) => d.trim().length > 0 && d.length <= 253)  // CR-02: RFC 1035 max domain length
  .map((d) => ({
    user_id: user.id,
    project_id: projectId,
    domain: d.replace(/^www\./, '').trim(),
    source: 'serp' as const,
  }))

if (!rows.length) {
  return { success: false, error: 'Geçerli domain bulunamadı.' }
}
```

`.insert(rows)` çağrısını güncelle — WR-01 FIX:

```typescript
// Önce (mevcut — comment var ama option geçirilmiyor):
const { error } = await supabase.from('competitors').insert(rows)

// Sonra (WR-01 FIX):
const { error } = await supabase
  .from('competitors')
  .insert(rows, { onConflict: 'project_id,domain', ignoreDuplicates: true })
```
  </action>
  <verify>
    <automated>grep -n "content_areas\|own_category_structure\|ignoreDuplicates\|MAX_DOMAINS\|console.error\|revalidatePath" C:/Users/baris/Documents/seo-machine/src/app/\(dashboard\)/projeler/\[id\]/rakipler/actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20260424000001_add_own_category_structure.sql dosyası mevcut ve `ALTER TABLE public.projects ADD COLUMN IF NOT EXISTS own_category_structure JSONB` satırı içeriyor
    - actions.ts içinde `content_areas: contentAreas` ifadesi `.update()` çağrısında mevcut (satır yaklaşık 200)
    - actions.ts içinde `own_category_structure: result` ifadesi `supabase.from('projects').update(` içinde mevcut
    - actions.ts içinde `revalidatePath` `fetchOwnDomainData` içinde çağrılıyor (COMP-04 CR-01 fix)
    - actions.ts içinde `MAX_DOMAINS = 20` sabiti tanımlı
    - actions.ts içinde `domains.length > MAX_DOMAINS` kontrolü mevcut
    - actions.ts içinde `.filter((d) => d.trim().length > 0 && d.length <= 253)` filtresi mevcut
    - actions.ts içinde `ignoreDuplicates: true` seçeneği `.insert()` çağrısına geçirilmiş
    - actions.ts içinde `console.error('[fetchOwnDomainData]` log satırı mevcut
    - actions.ts içinde `!domain || domain.trim() === '' || domain === 'null'` null guard mevcut
    - `npx tsc --noEmit --project C:/Users/baris/Documents/seo-machine/tsconfig.json` hata üretmiyor (actions.ts kapsamındaki tipler doğru)
  </acceptance_criteria>
  <done>
    - Yeni migration: projects.own_category_structure JSONB kolonu eklendi
    - fetchCompetitorData: content_areas JSONB alanı hesaplanıp DB'ye yazılıyor (COMP-03 FIX)
    - fetchOwnDomainData: sonuç projects.own_category_structure'a persist edilip revalidatePath çağrılıyor (CR-01 FIX)
    - addCompetitors: 20 domain limiti, 253 char length filtresi, ignoreDuplicates:true (CR-02 + WR-01 FIX)
    - WR-02: catch bloğu console.error ile loglanıyor
    - WR-03: null/boş domain guard eklendi
  </done>
</task>

<task type="auto">
  <name>Task 2: OwnDomainAnalyzeButton yeniden yazımı + page.tsx güncellemesi (content_areas render, Fırsat Skoru, DB'den own_category_structure okuma)</name>
  <files>
    src/app/(dashboard)/projeler/[id]/rakipler/OwnDomainAnalyzeButton.tsx
    src/app/(dashboard)/projeler/[id]/rakipler/page.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/rakipler/OwnDomainAnalyzeButton.tsx (tam dosya — mevcut CR-01 no-op implementasyon, satır 1-46)
    - src/app/(dashboard)/projeler/[id]/rakipler/page.tsx (tam dosya — mevcut buildGapReport ve SSR data fetch, satır 1-349)
    - src/app/(dashboard)/projeler/[id]/rakipler/actions.ts (Task 1 sonrası güncellenmiş — fetchOwnDomainData signature ve revalidatePath davranışı)
  </read_first>
  <action>
**ADIM 1: OwnDomainAnalyzeButton.tsx'i yeniden yaz — CR-01 FIX**

Dosyanın tüm içeriğini aşağıdaki yeni implementasyonla değiştir:

```typescript
'use client'

import { useState } from 'react'
import { Button } from '@/components/ui/button'
import { fetchOwnDomainData } from './actions'

type Props = {
  projectId: string
  domain: string
}

export function OwnDomainAnalyzeButton({ projectId, domain }: Props) {
  const [isPending, setIsPending] = useState(false)
  const [error, setError] = useState<string | null>(null)

  const handleAnalyze = async () => {
    setIsPending(true)
    setError(null)
    try {
      // CR-01 FIX: fetchOwnDomainData artık projects.own_category_structure'a persist ediyor
      // ve revalidatePath çağırıyor — sayfa SSR'ı yeniden tetiklenir, "Sizin Siteniz" sütunu dolar
      await fetchOwnDomainData(projectId, domain)
      // revalidatePath server-side tetiklendi; router.refresh() ile client'ta yansıt
      // window.location.reload() yerine daha temiz bir yol: sayfayı soft refresh yap
      window.location.reload()
    } catch {
      setError('Analiz başarısız. Lütfen tekrar deneyin.')
    } finally {
      setIsPending(false)
    }
  }

  return (
    <div className="flex flex-col items-end gap-1">
      <Button
        size="sm"
        variant="outline"
        onClick={handleAnalyze}
        disabled={isPending}
        className={isPending ? 'opacity-50 cursor-wait' : ''}
      >
        {isPending ? 'Analiz ediliyor...' : 'Kendi Sitemi Analiz Et'}
      </Button>
      {error && <span className="text-xs text-destructive">{error}</span>}
    </div>
  )
}
```

**Kritik fark:** Artık `await import('./actions')` dynamic import YOK — doğrudan statik import kullanılıyor. Ve en önemlisi: Task 1'de `fetchOwnDomainData` artık persist edip revalidatePath çağırıyor, bu nedenle `window.location.reload()` sonrasında SSR DB'den veriyi okuyacak ve "Sizin Siteniz" sütunu dolacak.

---

**ADIM 2: page.tsx'i güncelle — 3 değişiklik**

**Değişiklik A: SSR'da ownCategoryData'yı önce DB'den oku**

Mevcut satır 135:
```typescript
const ownCategoryData = await fetchOwnDomainData(id, project.domain).catch(() => null)
```

Bu satırı kaldır. Yerine:

```typescript
// CR-01 FIX: Önce DB'den projects.own_category_structure'ı oku
// fetchOwnDomainData artık DB'ye persist ediyor; SSR'da DataForSEO'ya gerek yok
const ownCategoryData = (project.own_category_structure as Record<string, number> | null) ?? null
```

Bunun çalışması için proje sorgusunu güncelle — `select` listesine `own_category_structure` ekle:

```typescript
// Önce (satır 114-119):
const { data: project } = await supabase
  .from('projects')
  .select('id, name, domain')
  .eq('id', id)
  .eq('user_id', user.id)
  .single()

// Sonra:
const { data: project } = await supabase
  .from('projects')
  .select('id, name, domain, own_category_structure')
  .eq('id', id)
  .eq('user_id', user.id)
  .single()
```

`actions.ts` import listesinden `fetchOwnDomainData`'yı kaldır — artık page.tsx'de SSR'da çağrılmıyor:

```typescript
// Önce:
import { addCompetitor, fetchOwnDomainData } from './actions'

// Sonra:
import { addCompetitor } from './actions'
```

---

**Değişiklik B: content_areas render — rakip tablosuna "İçerik Alanları" sütunu ekle**

Rakip tablosu `<TableHeader>` kısmına yeni başlık ekle (Sayfa Sayısı'ndan sonra):

```tsx
<TableHead>İçerik Alanları</TableHead>
```

`<TableBody>` kısmında her `<TableRow>` içine, Sayfa Sayısı hücresinden sonra:

```tsx
<TableCell>
  {comp.content_areas && Object.keys(comp.content_areas as Record<string, unknown>).length > 0 ? (
    <div className="flex flex-wrap gap-1">
      {Object.keys(comp.content_areas as Record<string, unknown>)
        .slice(0, 4)
        .map((area) => (
          <span
            key={area}
            className="text-xs px-1.5 py-0.5 rounded bg-secondary text-muted-foreground border border-border"
          >
            {area}
          </span>
        ))}
      {Object.keys(comp.content_areas as Record<string, unknown>).length > 4 && (
        <span className="text-xs text-muted-foreground">
          +{Object.keys(comp.content_areas as Record<string, unknown>).length - 4}
        </span>
      )}
    </div>
  ) : (
    <span className="text-muted-foreground text-xs">—</span>
  )}
</TableCell>
```

`Competitor` type tanımına `content_areas` ekle:

```typescript
type Competitor = {
  id: string
  domain: string
  source: string
  top_pages: Array<{ url: string; etv: number }> | null
  category_structure: CategoryStructure
  content_areas: Record<string, unknown> | null  // COMP-03 FIX
  updated_at: string
}
```

Supabase sorgusuna `content_areas` ekle:

```typescript
// Önce:
.select('id, domain, source, top_pages, category_structure, updated_at')

// Sonra:
.select('id, domain, source, top_pages, category_structure, content_areas, updated_at')
```

---

**Değişiklik C: Fırsat Skoru — buildGapReport'u genişlet ve tabloya sütun ekle**

`GapMatrix` type'ını güncelle:

```typescript
type GapMatrix = {
  categories: string[]
  opportunityCategories: Set<string>  // COMP-04 FIX: 2+ rakip var, kullanıcı yok
  matrix: {
    [domain: string]: {
      [category: string]: { exists: boolean; pageCount: number }
    }
  }
}
```

`buildGapReport` fonksiyonunun sonuna (return'dan önce) fırsat hesaplamayı ekle:

```typescript
// COMP-04 FIX: Fırsat Skoru — kullanıcının olmadığı ama 2+ rakibin sahip olduğu kategoriler
const opportunityCategories = new Set<string>()
for (const cat of categories) {
  const userHas = matrix[userDomain]?.[cat]?.exists ?? false
  if (!userHas) {
    const competitorCount = competitors.filter(
      (comp) => matrix[comp.domain]?.[cat]?.exists ?? false
    ).length
    if (competitorCount >= 2) {
      opportunityCategories.add(cat)
    }
  }
}

return { categories, opportunityCategories, matrix }
```

Gap tablosunda tablo başlığına yeni sütun ekle (Kategori sütunundan sonra):

```tsx
<TableHead className="text-center min-w-24">Fırsat Skoru</TableHead>
```

Her kategori satırında (`gapReport.categories.map` içinde), Kategori adı hücresinden sonra:

```tsx
<TableCell className="text-center">
  {gapReport.opportunityCategories.has(cat) ? (
    <span
      className="text-xs font-medium px-2 py-0.5 rounded-full bg-amber-500/20 text-amber-400 border border-amber-500/30"
      title="2 veya daha fazla rakip bu kategoride içerik var, sizin sitenizde yok — hızlı giriş fırsatı"
    >
      Fırsat
    </span>
  ) : (
    <span className="text-muted-foreground text-xs">—</span>
  )}
</TableCell>
```

**Kritik:** `opportunityCategories` bir `Set` olduğu için JSON serializasyonu sorun çıkarabilir. `buildGapReport` fonksiyonu page.tsx içinde tanımlı ve SSR'da çalışıyor — Set doğrudan JSX'e geçirilecek, serialize edilmeyecek. Bu nedenle sorun yok.
  </action>
  <verify>
    <automated>grep -n "own_category_structure\|content_areas\|opportunityCategories\|Fırsat Skoru\|İçerik Alanları\|fetchOwnDomainData" C:/Users/baris/Documents/seo-machine/src/app/\(dashboard\)/projeler/\[id\]/rakipler/page.tsx C:/Users/baris/Documents/seo-machine/src/app/\(dashboard\)/projeler/\[id\]/rakipler/OwnDomainAnalyzeButton.tsx</automated>
  </verify>
  <acceptance_criteria>
    - OwnDomainAnalyzeButton.tsx başında statik `import { fetchOwnDomainData } from './actions'` mevcut (dynamic import YOK)
    - OwnDomainAnalyzeButton.tsx içinde `await fetchOwnDomainData(projectId, domain)` mevcut
    - OwnDomainAnalyzeButton.tsx içinde `window.location.reload()` mevcut (revalidatePath sonrası DB state'i yansıtmak için)
    - page.tsx içinde `own_category_structure` Supabase select listesinde mevcut
    - page.tsx içinde `fetchOwnDomainData` import edilmiyor (kaldırıldı — SSR'da artık çağrılmıyor)
    - page.tsx içinde `project.own_category_structure` ile `ownCategoryData` set ediliyor
    - page.tsx içinde Competitor type'ında `content_areas: Record<string, unknown> | null` alanı mevcut
    - page.tsx Supabase sorgusunda `content_areas` select listesinde mevcut
    - page.tsx rakip tablosunda `İçerik Alanları` başlıklı sütun mevcut
    - page.tsx GapMatrix type'ında `opportunityCategories: Set<string>` alanı mevcut
    - page.tsx `buildGapReport` return değerinde `opportunityCategories` mevcut
    - page.tsx gap tablosunda `Fırsat Skoru` başlıklı sütun mevcut
    - page.tsx gap tablosunda `gapReport.opportunityCategories.has(cat)` kontrolü mevcut ve "Fırsat" badge'i render ediliyor
    - `npx tsc --noEmit --project C:/Users/baris/Documents/seo-machine/tsconfig.json` TypeScript hatası üretmiyor
  </acceptance_criteria>
  <done>
    - OwnDomainAnalyzeButton: dynamic import kaldırıldı, statik import kullanılıyor; fetchOwnDomainData artık persist edip revalidatePath çağırıyor (CR-01 FIX)
    - page.tsx SSR: own_category_structure DB'den okunuyor, DataForSEO'ya SSR'da istek atılmıyor
    - page.tsx: content_areas rakip tablosunda "İçerik Alanları" sütununda badge'ler olarak gösteriliyor (COMP-03 FIX)
    - page.tsx: buildGapReport fırsat kategorilerini hesaplıyor; gap tablosunda "Fırsat Skoru" sütunu mevcut (COMP-04 FIX)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → addCompetitors Server Action | Kullanıcı domain listesi client'tan geliyor — CR-02 fix ile MAX_DOMAINS=20 ve length<=253 kontrolleri server-side |
| SSR → Supabase projects tablosu | own_category_structure okuma: user.id + project.id filtresi zorunlu — farklı kullanıcının verisi okunamaz |
| OwnDomainAnalyzeButton → fetchOwnDomainData | Server action içinde ownership check yapılıyor — başka projeye yazılamaz |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Denial of Service | addCompetitors | mitigate | CR-02 fix: `domains.length > 20` erken return; `d.length > 253` filter — unbounded insert engellendi |
| T-04-13 | Tampering | fetchOwnDomainData → projects.update | mitigate | `.eq('user_id', user.id)` ownership filtresi — başka kullanıcının own_category_structure'ı yazılamaz |
| T-04-14 | Information Disclosure | page.tsx SSR — own_category_structure | mitigate | projects select `eq('user_id', user.id)` — başka kullanıcının verisi render edilemez |
| T-04-15 | Spoofing | OwnDomainAnalyzeButton → dynamic import kaldırıldı | accept | Statik import server action boundary'yi korur; dinamik import'taki potansiyel bundle manipulation riski elimine edildi |
</threat_model>

<verification>
Plan tamamlandıktan sonra şunlar doğrulanabilir olmalı:

1. `grep -n "content_areas" src/app/\(dashboard\)/projeler/\[id\]/rakipler/actions.ts` — content_areas UPDATE içinde görünmeli
2. `grep -n "own_category_structure" src/app/\(dashboard\)/projeler/\[id\]/rakipler/actions.ts` — persist satırı görünmeli
3. `grep -n "MAX_DOMAINS\|ignoreDuplicates\|length <= 253" src/app/\(dashboard\)/projeler/\[id\]/rakipler/actions.ts` — CR-02+WR-01 fix görünmeli
4. `grep -n "Fırsat Skoru\|opportunityCategories" src/app/\(dashboard\)/projeler/\[id\]/rakipler/page.tsx` — fırsat sinyali görünmeli
5. `grep -n "own_category_structure" src/app/\(dashboard\)/projeler/\[id\]/rakipler/page.tsx` — DB okuma görünmeli
6. `cat supabase/migrations/20260424000001_add_own_category_structure.sql` — migration dosyası var
7. `npx tsc --noEmit` — TypeScript hatası yok
</verification>

<success_criteria>
- SC-3 (COMP-03): fetchCompetitorData artık content_areas JSONB kolonunu dolduruyor; rakip tablosunda "İçerik Alanları" sütunu görünüyor
- SC-4 (COMP-04) CR-01: OwnDomainAnalyzeButton tıklandıktan sonra sayfa yenilendiğinde "Sizin Siteniz" sütunu doluyor — artık no-op değil
- SC-4 (COMP-04) Fırsat: Gap tablosunda "Fırsat Skoru" sütunu; 2+ rakibin sahip olduğu ama kullanıcının olmadığı kategoriler "Fırsat" badge'iyle işaretleniyor
- CR-02: addCompetitors maksimum 20 domain, her biri maksimum 253 karakter
- WR-01: ignoreDuplicates:true aktif
- WR-02: catch bloğu console.error logluyor
- WR-03: null domain guard mevcut
- TypeScript hataları yok
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/04-competitor-intelligence/04-04-SUMMARY.md` oluştur.
</output>
