---
plan: 02-03
phase: 2
wave: 2
title: "Yeni proje modal + Server Action + Zod validasyon"
goal: "Dialog modal ile yeni proje oluşturma formunu, Zod validasyonunu ve Supabase INSERT Server Action'ını ekle"
depends_on: [02-01]
files_modified:
  - src/app/(dashboard)/projeler/new-project-modal.tsx
  - src/app/(dashboard)/projeler/actions.ts
  - src/app/(dashboard)/projeler/page.tsx
requirements: [PROJ-01]
autonomous: true
must_haves:
  truths:
    - "Kullanıcı 'Yeni Proje Oluştur' butonuna tıklayınca modal açılır"
    - "Modal zorunlu: Ad ve Domain alanları, opsiyonel: Sektör, Hedef Ülke, Hedef Dil, İş Modeli, Site Tipi, Marka Tonu"
    - "Ad boş bırakılınca 'Proje adı zorunludur' hatası gösterilir"
    - "Domain geçersizse 'Geçerli bir domain girin (örn. musteri.com)' hatası gösterilir"
    - "Başarılı oluşturmada: projects tablosuna 1 satır + stages tablosuna 10 satır INSERT edilir"
    - "İlk stage (Alım) status='active', diğer 9 stage status='pending' olarak oluşturulur"
    - "Oluşturma sonrası modal kapanır ve proje listesi güncellenir"
  artifacts:
    - path: "src/app/(dashboard)/projeler/new-project-modal.tsx"
      provides: "Client Component — Dialog modal, react-hook-form + zod ile form yönetimi"
      exports: ["NewProjectModal"]
    - path: "src/app/(dashboard)/projeler/actions.ts"
      provides: "Server Action — createProject(formData) — projects ve stages INSERT"
      exports: ["createProject"]
  key_links:
    - from: "new-project-modal.tsx"
      to: "actions.ts createProject()"
      via: "Server Action call: await createProject(values)"
      pattern: "createProject"
    - from: "actions.ts createProject()"
      to: "supabase.projects + supabase.stages"
      via: "createClient() from @/lib/supabase/server — INSERT transactions"
      pattern: "from\\('projects'\\)"
    - from: "projeler/page.tsx"
      to: "new-project-modal.tsx"
      via: "import NewProjectModal — 'Yeni Proje Oluştur' butonunu sarmala"
      pattern: "NewProjectModal"
---

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-project-core/02-CONTEXT.md
@.planning/phases/02-project-core/02-UI-SPEC.md

<interfaces>
<!-- Executor'ın kullanacağı mevcut kod arayüzleri -->

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
// Server Actions içinde kullanım:
// 'use server'
// const supabase = await createClient()
// const { data: { user } } = await supabase.auth.getUser()
```

From src/components/ui/dialog.tsx (02-01 tarafından eklendi):
```typescript
export { Dialog, DialogContent, DialogDescription, DialogFooter,
         DialogHeader, DialogTitle, DialogTrigger }
// Usage:
// <Dialog open={open} onOpenChange={setOpen}>
//   <DialogTrigger asChild><Button>Yeni Proje Oluştur</Button></DialogTrigger>
//   <DialogContent>
//     <DialogHeader><DialogTitle>Yeni Proje Oluştur</DialogTitle></DialogHeader>
//     {/* form */}
//     <DialogFooter>...</DialogFooter>
//   </DialogContent>
// </Dialog>
```

From src/components/ui/separator.tsx (02-01 tarafından eklendi):
```typescript
export { Separator }
// Usage: <Separator /> — horizontal divider
```

From src/components/ui/form.tsx (zaten mevcut):
```typescript
export { Form, FormField, FormItem, FormLabel, FormControl, FormMessage }
// react-hook-form Provider tabanlı — useForm() hook ile kullanılır
```

From src/components/ui/input.tsx:
```typescript
export { Input }
```

From src/components/ui/textarea.tsx (02-01 tarafından eklendi):
```typescript
export { Textarea }
```

Stage names (10 adet, sırasıyla):
const STAGE_NAMES = [
  'Alım', 'Keşif', 'Keyword Stratejisi', 'Site Blueprint',
  'Sayfa Planlaması', 'Sayfa Paketi', 'İçerik Üretimi',
  'SEO Denetimi', 'Yayın Hazırlığı', 'Yayın Sonrası'
]
// İlk eleman (index 0) = status 'active', geri kalanlar = status 'pending'

Database schema:
```sql
-- projects INSERT fields:
user_id UUID, name TEXT NOT NULL, domain TEXT NOT NULL,
sector TEXT, target_country TEXT, target_language TEXT,
business_model TEXT, site_type TEXT, brand_tone TEXT, notes TEXT

-- stages INSERT fields (10 satır):
project_id UUID, user_id UUID, stage_name TEXT,
status TEXT ('active' | 'pending'), started_at TIMESTAMPTZ (aktif için now(), pending için NULL)
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server Action | Modal form verisi (name, domain, diğer alanlar) Server Action'a POST edilir |
| Server Action → Supabase | Server Action kullanıcı adına INSERT yapar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | createProject Server Action | mitigate | Server Action içinde `supabase.auth.getUser()` ile user ID doğrulanır — form'dan gelen user_id kabul edilmez. INSERT'e `user_id: user.id` (auth'dan alınan) yazılır. |
| T-02-03-02 | Tampering | domain field | mitigate | Zod şeması: `z.string().min(1).regex(/^[^\s]+\.[^\s]+$/, 'Geçerli bir domain girin (örn. musteri.com)')` — boşluk ve nokta gerektiren basit validasyon. XSS için Supabase parametrize query yeterli. |
| T-02-03-03 | Denial of Service | text fields max length | mitigate | Zod şemalarında max() sınırları: name max(100), domain max(253), sector/country/language/business_model/site_type/brand_tone max(100). notes max(2000). |
| T-02-03-04 | Elevation of Privilege | stages INSERT | mitigate | stages INSERT'e `user_id: user.id` yazılır (auth'dan). Project_id oluşturulan projenin ID'sinden alınır — dışarıdan gelmez. |
</threat_model>

<tasks>

<task id="T-02-03-01">
  <title>createProject Server Action oluştur</title>
  <read_first>
    - src/lib/supabase/server.ts — createClient() kullanım kalıbı
    - src/app/(dashboard)/layout.tsx — auth guard kalıbı (user doğrulama için referans)
    - .planning/phases/02-project-core/02-CONTEXT.md — D-04 (zorunlu/opsiyonel alanlar), D-05 (validasyon kuralları)
  </read_first>
  <action>
`src/app/(dashboard)/projeler/actions.ts` dosyasını oluştur:

```typescript
'use server'

import { z } from 'zod'
import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'

const STAGE_NAMES = [
  'Alım', 'Keşif', 'Keyword Stratejisi', 'Site Blueprint',
  'Sayfa Planlaması', 'Sayfa Paketi', 'İçerik Üretimi',
  'SEO Denetimi', 'Yayın Hazırlığı', 'Yayın Sonrası'
] as const

const createProjectSchema = z.object({
  name: z.string().min(1, 'Proje adı zorunludur').max(100),
  domain: z
    .string()
    .min(1, 'Domain zorunludur')
    .max(253)
    .regex(/^[^\s]+\.[^\s]+$/, 'Geçerli bir domain girin (örn. musteri.com)'),
  sector: z.string().max(100).optional().or(z.literal('')),
  target_country: z.string().max(100).optional().or(z.literal('')),
  target_language: z.string().max(100).optional().or(z.literal('')),
  business_model: z.string().max(100).optional().or(z.literal('')),
  site_type: z.string().max(100).optional().or(z.literal('')),
  brand_tone: z.string().max(100).optional().or(z.literal('')),
})

export type CreateProjectInput = z.infer<typeof createProjectSchema>

export type CreateProjectResult =
  | { success: true; projectId: string }
  | { success: false; error: string; fieldErrors?: Record<string, string[]> }

export async function createProject(
  input: CreateProjectInput
): Promise<CreateProjectResult> {
  // 1. Validate input
  const parsed = createProjectSchema.safeParse(input)
  if (!parsed.success) {
    return {
      success: false,
      error: 'Form verileri geçersiz.',
      fieldErrors: parsed.error.flatten().fieldErrors as Record<string, string[]>,
    }
  }

  // 2. Authenticate
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) {
    return { success: false, error: 'Oturum bulunamadı. Lütfen tekrar giriş yapın.' }
  }

  const data = parsed.data

  // 3. Insert project
  const { data: project, error: projectError } = await supabase
    .from('projects')
    .insert({
      user_id: user.id,
      name: data.name,
      domain: data.domain,
      sector: data.sector || null,
      target_country: data.target_country || null,
      target_language: data.target_language || null,
      business_model: data.business_model || null,
      site_type: data.site_type || null,
      brand_tone: data.brand_tone || null,
    })
    .select('id')
    .single()

  if (projectError || !project) {
    return { success: false, error: 'Proje oluşturulamadı. Lütfen tekrar deneyin.' }
  }

  // 4. Insert 10 stages — first one active, rest pending
  const stageRows = STAGE_NAMES.map((stageName, index) => ({
    project_id: project.id,
    user_id: user.id,
    stage_name: stageName,
    status: index === 0 ? 'active' : 'pending',
    started_at: index === 0 ? new Date().toISOString() : null,
  }))

  const { error: stagesError } = await supabase.from('stages').insert(stageRows)

  if (stagesError) {
    // Project inserted but stages failed — clean up project to avoid orphan
    await supabase.from('projects').delete().eq('id', project.id)
    return { success: false, error: 'Proje aşamaları oluşturulamadı. Lütfen tekrar deneyin.' }
  }

  // 5. Revalidate and return
  revalidatePath('/dashboard/projeler')
  return { success: true, projectId: project.id }
}
```

**zod paketi:** `zod` zaten package.json'da mevcut olmalı (shadcn form tarafından yüklenmiş). Eğer yoksa `npm install zod` ile ekle.
  </action>
  <acceptance_criteria>
    - src/app/(dashboard)/projeler/actions.ts dosyası mevcut
    - Dosya `'use server'` direktifi içeriyor: `grep "'use server'" src/app/(dashboard)/projeler/actions.ts` çıktı verir
    - Dosya `createProject` export'u içeriyor: `grep "export async function createProject" src/app/(dashboard)/projeler/actions.ts` çıktı verir
    - STAGE_NAMES dizisi 10 eleman içeriyor: `grep "Alım.*Keşif\|Yayın Sonrası" src/app/(dashboard)/projeler/actions.ts` VEYA dosyada 10 stage adı bulunuyor
    - Dosyada `user.id` ile INSERT yapılıyor (form'dan user_id alınmıyor): `grep "user_id: user.id" src/app/(dashboard)/projeler/actions.ts` çıktı verir
    - Dosyada `revalidatePath('/dashboard/projeler')` var: `grep "revalidatePath" src/app/(dashboard)/projeler/actions.ts` çıktı verir
    - Dosyada stages orphan cleanup var: `grep "delete.*eq.*project.id\|clean" src/app/(dashboard)/projeler/actions.ts` çıktı verir
    - `npx tsc --noEmit` temiz çıkar
  </acceptance_criteria>
</task>

<task id="T-02-03-02">
  <title>Yeni proje modal Client Component oluştur ve sayfaya bağla</title>
  <read_first>
    - src/app/(dashboard)/projeler/actions.ts — createProject fonksiyon imzası ve CreateProjectInput tipi
    - src/components/ui/dialog.tsx — Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter export'ları
    - src/components/ui/form.tsx — Form, FormField, FormItem, FormLabel, FormControl, FormMessage
    - src/components/ui/separator.tsx — Separator
    - src/app/(dashboard)/projeler/page.tsx — "Yeni Proje Oluştur" butonunun NewProjectModal ile sarmalanacağı yer
    - .planning/phases/02-project-core/02-UI-SPEC.md — New Project Modal şeması ve Copywriting Contract
  </read_first>
  <action>
**1. `src/app/(dashboard)/projeler/new-project-modal.tsx` dosyasını oluştur:**

'use client' direktifi gerekli — Dialog ve form state için.

Kullanılacak kütüphaneler:
- `react-hook-form` + `@hookform/resolvers/zod` (shadcn form bağımlılıkları — zaten yüklü)
- `zod` (actions.ts ile aynı schema, client'ta da kullanılacak)
- Dialog, Form, FormField, FormItem, FormLabel, FormControl, FormMessage, Input, Textarea, Separator, Button

**Form yapısı:**

State: `const [open, setOpen] = useState(false)` — modal açık/kapalı

```tsx
<Dialog open={open} onOpenChange={setOpen}>
  <DialogTrigger asChild>
    {children} {/* Trigger butonu dışarıdan alınır — esneklik için */}
  </DialogTrigger>
  <DialogContent className="max-w-lg max-h-[90vh] overflow-y-auto">
    <DialogHeader>
      <DialogTitle>Yeni Proje Oluştur</DialogTitle>
    </DialogHeader>
    <Form {...form}>
      <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-4">
        {/* Zorunlu alanlar */}
        <FormField name="name" render={...}>
          <FormLabel>Proje Adı <span className="text-destructive">*</span></FormLabel>
          <FormControl><Input placeholder="örn. Müşteri Projesi" /></FormControl>
          <FormMessage />
        </FormField>
        <FormField name="domain" render={...}>
          <FormLabel>Domain <span className="text-destructive">*</span></FormLabel>
          <FormControl><Input placeholder="örn. musteri.com" /></FormControl>
          <FormMessage />
        </FormField>

        {/* Opsiyonel alanlar başlığı */}
        <div className="flex items-center gap-4">
          <Separator className="flex-1" />
          <span className="text-xs text-muted-foreground">Opsiyonel Bilgiler</span>
          <Separator className="flex-1" />
        </div>

        {/* Opsiyonel alanlar */}
        <FormField name="sector" ...><FormLabel>Sektör</FormLabel>...</FormField>
        <FormField name="target_country" ...><FormLabel>Hedef Ülke</FormLabel>...</FormField>
        <FormField name="target_language" ...><FormLabel>Hedef Dil</FormLabel>...</FormField>
        <FormField name="business_model" ...><FormLabel>İş Modeli</FormLabel>...</FormField>
        <FormField name="site_type" ...><FormLabel>Site Tipi</FormLabel>...</FormField>
        <FormField name="brand_tone" ...><FormLabel>Marka Tonu</FormLabel>...</FormField>

        {/* Hata mesajı */}
        {formError && (
          <p className="text-sm text-destructive">{formError}</p>
        )}

        <DialogFooter>
          <Button type="button" variant="outline" onClick={() => setOpen(false)}>
            Formu Kapat
          </Button>
          <Button type="submit" disabled={isSubmitting}>
            {isSubmitting ? 'Oluşturuluyor...' : 'Oluştur'}
          </Button>
        </DialogFooter>
      </form>
    </Form>
  </DialogContent>
</Dialog>
```

**onSubmit handler:**
```typescript
const onSubmit = async (values: CreateProjectInput) => {
  setIsSubmitting(true)
  setFormError(null)
  try {
    const result = await createProject(values)
    if (result.success) {
      form.reset()
      setOpen(false)
      // revalidatePath server'da çalıştı — liste kendiliğinden güncellenir
    } else {
      setFormError(result.error)
      if (result.fieldErrors) {
        Object.entries(result.fieldErrors).forEach(([field, errors]) => {
          form.setError(field as keyof CreateProjectInput, { message: errors[0] })
        })
      }
    }
  } finally {
    setIsSubmitting(false)
  }
}
```

**Zod schema (client-side):** actions.ts'teki ile aynı kuralları kullan — name min(1), domain regex validation. Bu dosyada tekrar tanımla (server ve client ayrı bundle'da).

**2. `src/app/(dashboard)/projeler/page.tsx` dosyasını güncelle:**

"Yeni Proje Oluştur" butonunu (hem header'daki hem boş durum'daki) NewProjectModal ile sarmala:

```tsx
import { NewProjectModal } from './new-project-modal'

// Header butonu:
<NewProjectModal>
  <Button>Yeni Proje Oluştur</Button>
</NewProjectModal>

// Boş durum butonu:
<NewProjectModal>
  <Button>Yeni Proje Oluştur</Button>
</NewProjectModal>
```

NewProjectModal bileşeni `children` prop'u alır ve DialogTrigger olarak kullanır.
  </action>
  <acceptance_criteria>
    - src/app/(dashboard)/projeler/new-project-modal.tsx dosyası mevcut
    - Dosya `'use client'` direktifi içeriyor: `grep "'use client'" src/app/(dashboard)/projeler/new-project-modal.tsx` çıktı verir
    - Dosya `NewProjectModal` export'u içeriyor: `grep "export.*NewProjectModal" src/app/(dashboard)/projeler/new-project-modal.tsx` çıktı verir
    - Dosyada "Yeni Proje Oluştur" başlığı var: `grep "Yeni Proje Oluştur" src/app/(dashboard)/projeler/new-project-modal.tsx` çıktı verir
    - Dosyada "Formu Kapat" butonu var: `grep "Formu Kapat" src/app/(dashboard)/projeler/new-project-modal.tsx` çıktı verir
    - Dosyada "Oluştur" butonu var: `grep "Oluştur" src/app/(dashboard)/projeler/new-project-modal.tsx` çıktı verir
    - Dosyada "Proje adı zorunludur" hata mesajı var: `grep "Proje adı zorunludur" src/app/(dashboard)/projeler/new-project-modal.tsx` çıktı verir
    - Dosyada "Geçerli bir domain girin" hata mesajı var: `grep "Geçerli bir domain girin" src/app/(dashboard)/projeler/new-project-modal.tsx` çıktı verir
    - Dosyada "Opsiyonel Bilgiler" bölümü var: `grep "Opsiyonel Bilgiler" src/app/(dashboard)/projeler/new-project-modal.tsx` çıktı verir
    - src/app/(dashboard)/projeler/page.tsx'de NewProjectModal import'u var: `grep "NewProjectModal" src/app/(dashboard)/projeler/page.tsx` çıktı verir
    - `npx tsc --noEmit` temiz çıkar
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
# Dosya varlığı
ls src/app/(dashboard)/projeler/actions.ts
ls src/app/(dashboard)/projeler/new-project-modal.tsx

# Server Action kontrolleri
grep "'use server'" src/app/(dashboard)/projeler/actions.ts
grep "export async function createProject" src/app/(dashboard)/projeler/actions.ts
grep "user_id: user.id" src/app/(dashboard)/projeler/actions.ts
grep "revalidatePath" src/app/(dashboard)/projeler/actions.ts

# Modal kontrolleri
grep "'use client'" src/app/(dashboard)/projeler/new-project-modal.tsx
grep "Proje adı zorunludur" src/app/(dashboard)/projeler/new-project-modal.tsx
grep "Geçerli bir domain girin" src/app/(dashboard)/projeler/new-project-modal.tsx
grep "NewProjectModal" src/app/(dashboard)/projeler/page.tsx

# TypeScript
npx tsc --noEmit
```
</verification>

<success_criteria>
- createProject Server Action: auth doğrulama, Zod validasyon, projects INSERT + 10 stages INSERT yapıyor
- NewProjectModal: Dialog ile açılıyor, zorunlu/opsiyonel alanlar gösteriliyor, Türkçe hata mesajları çalışıyor
- Başarılı oluşturmada modal kapanıyor ve liste revalidate edilmesiyle güncelleniyor
- TypeScript hata yok
</success_criteria>

<output>
Tamamlandığında `.planning/phases/02-project-core/02-03-SUMMARY.md` oluştur.
</output>
