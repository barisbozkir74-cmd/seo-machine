---
plan: 02-05
phase: 2
wave: 4
title: "Stage geçiş mantığı + onay dialogu"
goal: "Aktif stage'i tamamlayıp bir sonrakine geçiren Server Action ve onay dialogu Client Component'ini oluştur"
depends_on: [02-04]
files_modified:
  - src/app/(dashboard)/projeler/[id]/stage-transition.tsx
  - src/app/(dashboard)/projeler/[id]/actions.ts
  - src/app/(dashboard)/projeler/[id]/page.tsx
requirements: [PROJ-04]
autonomous: true
must_haves:
  truths:
    - "'Sonraki Aşamaya Geç' butonu sağ sütun alt kısmında görünüyor"
    - "Butona tıklayınca onay dialogu açılıyor"
    - "Dialog: aktif stage adını içeren Türkçe onay metni, 'Evet, Tamamla' ve 'Vazgeç' butonları"
    - "'Evet, Tamamla' tıklanınca: aktif stage completed, sonraki stage active yapılıyor"
    - "Geçiş sonrası sayfa revalidate edilip güncel stage listesi gösteriliyor"
    - "Son stage (Yayın Sonrası) tamamlandığında buton yerine tamamlanma mesajı gösteriliyor"
    - "Stage geçişi geri alınamaz — sadece ilerleme yönünde çalışır"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/stage-transition.tsx"
      provides: "Client Component — onay dialogu ve geçiş tetikleyici buton"
      exports: ["StageTransition"]
    - path: "src/app/(dashboard)/projeler/[id]/actions.ts"
      provides: "Server Action — advanceStage(projectId, currentStageId)"
      exports: ["advanceStage"]
  key_links:
    - from: "stage-transition.tsx"
      to: "actions.ts advanceStage()"
      via: "Server Action call: await advanceStage(projectId, currentStageId)"
      pattern: "advanceStage"
    - from: "advanceStage()"
      to: "supabase.stages"
      via: "UPDATE aktif stage'i completed yap + sonraki stage'i active yap"
      pattern: "from\\('stages'\\)"
    - from: "projeler/[id]/page.tsx"
      to: "stage-transition.tsx"
      via: "import StageTransition — sağ sütun alt kısmına yerleştir"
      pattern: "StageTransition"
---

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-project-core/02-CONTEXT.md
@.planning/phases/02-project-core/02-UI-SPEC.md

<interfaces>
<!-- Executor'ın kullanacağı mevcut kod arayüzleri -->

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
```

From src/components/ui/dialog.tsx (02-01 tarafından eklendi):
```typescript
export { Dialog, DialogContent, DialogDescription, DialogFooter,
         DialogHeader, DialogTitle, DialogTrigger }
```

From src/components/ui/button.tsx:
```typescript
export { Button }
// Variant options: 'default' | 'outline' | 'ghost' | 'destructive' | 'secondary' | 'link'
// Stage transition confirm = default (primary, white fill) — NOT destructive per UI-SPEC
```

Stage data types (from Supabase stages table):
```typescript
interface Stage {
  id: string
  stage_name: string
  status: 'active' | 'completed' | 'pending'
  started_at: string | null
  completed_at: string | null
}
```

advanceStage logic:
// 1. UPDATE stages SET status='completed', completed_at=now() WHERE id=currentStageId AND status='active'
// 2. Find next stage: SELECT id FROM stages WHERE project_id=projectId AND status='pending' ORDER BY created_at ASC LIMIT 1
// 3. UPDATE stages SET status='active', started_at=now() WHERE id=nextStageId
// If no next stage exists: project is complete (all stages done)

Copywriting (from UI-SPEC):
- Button: "Sonraki Aşamaya Geç"
- Dialog title: "Aşamayı Tamamla"
- Dialog body: '"[Stage Adı]" aşamasını tamamlamak istediğinizden emin misiniz? Bu işlem geri alınamaz.'
- Confirm: "Evet, Tamamla"
- Cancel: "Vazgeç"
- Last stage message: "Tüm aşamalar tamamlandı. Proje yayın sonrası takibindedir."
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server Action | advanceStage(projectId, currentStageId) — her iki ID dışarıdan gelir |
| Server Action → Supabase | Stage UPDATE işlemleri kullanıcı adına yapılır |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | advanceStage — projectId ownership | mitigate | Server Action içinde `supabase.auth.getUser()` ile user ID alınır. Stage query'ye `.eq('user_id', user.id)` filtresi eklenir — başka kullanıcının stage'ini değiştirme girişimi 0 satır UPDATE ile sonuçlanır (RLS zaten bloklar, ikinci güvence). |
| T-02-05-02 | Tampering | advanceStage — currentStageId manipulation | mitigate | currentStageId'nin gerçekten aktif ve o projeye ait olduğu doğrulanır: `WHERE id=currentStageId AND project_id=projectId AND status='active' AND user_id=user.id`. Farklı stage ID gönderme etkisizdir. |
| T-02-05-03 | Tampering | double-click / race condition | mitigate | advanceStage içinde aktif stage kontrolü yapılır — `status='active'` olmayan stage'i complete etme girişimi 0 satır günceller. İdempotent: iki kez tıklanırsa ikincisi no-op olur. |
| T-02-05-04 | Denial of Service | rapid stage transitions | accept | Bu bir tek kullanıcı iç araç (ajans OS). Rate limiting gerekmez MVP'de. |
</threat_model>

<tasks>

<task id="T-02-05-01">
  <title>advanceStage Server Action oluştur</title>
  <read_first>
    - src/lib/supabase/server.ts — createClient() kullanımı
    - src/app/(dashboard)/projeler/actions.ts — aynı pattern (Server Action yapısı referans)
    - .planning/phases/02-project-core/02-CONTEXT.md — D-07 (stage geçişi geri alınamaz)
  </read_first>
  <action>
`src/app/(dashboard)/projeler/[id]/actions.ts` dosyasını oluştur:

```typescript
'use server'

import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'

export type AdvanceStageResult =
  | { success: true; isLastStage: boolean }
  | { success: false; error: string }

export async function advanceStage(
  projectId: string,
  currentStageId: string
): Promise<AdvanceStageResult> {
  const supabase = await createClient()

  // 1. Authenticate
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) {
    return { success: false, error: 'Oturum bulunamadı.' }
  }

  // 2. Complete current stage — only if it's active, belongs to this project and user
  const { error: completeError, count } = await supabase
    .from('stages')
    .update({
      status: 'completed',
      completed_at: new Date().toISOString(),
    })
    .eq('id', currentStageId)
    .eq('project_id', projectId)
    .eq('user_id', user.id)
    .eq('status', 'active')

  if (completeError) {
    return { success: false, error: 'Aşama tamamlanamadı. Lütfen tekrar deneyin.' }
  }

  // 3. Find and activate next pending stage
  const { data: nextStage } = await supabase
    .from('stages')
    .select('id')
    .eq('project_id', projectId)
    .eq('user_id', user.id)
    .eq('status', 'pending')
    .order('created_at', { ascending: true })
    .limit(1)
    .single()

  let isLastStage = false

  if (nextStage) {
    const { error: activateError } = await supabase
      .from('stages')
      .update({
        status: 'active',
        started_at: new Date().toISOString(),
      })
      .eq('id', nextStage.id)
      .eq('project_id', projectId)
      .eq('user_id', user.id)

    if (activateError) {
      return { success: false, error: 'Sonraki aşama başlatılamadı. Lütfen tekrar deneyin.' }
    }
  } else {
    // No more pending stages — all completed
    isLastStage = true
  }

  revalidatePath(`/dashboard/projeler/${projectId}`)
  return { success: true, isLastStage }
}
```
  </action>
  <acceptance_criteria>
    - src/app/(dashboard)/projeler/[id]/actions.ts dosyası mevcut
    - Dosya `'use server'` direktifi içeriyor: `grep "'use server'" "src/app/(dashboard)/projeler/[id]/actions.ts"` çıktı verir
    - Dosya `advanceStage` export'u içeriyor: `grep "export async function advanceStage" "src/app/(dashboard)/projeler/[id]/actions.ts"` çıktı verir
    - Dosyada `user.id` ile stage ownership doğrulaması var: `grep "user_id.*user.id\|eq.*user.id" "src/app/(dashboard)/projeler/[id]/actions.ts"` çıktı verir
    - Dosyada `status.*active` kontrolü var (idempotency): `grep "eq.*status.*active" "src/app/(dashboard)/projeler/[id]/actions.ts"` çıktı verir
    - Dosyada `revalidatePath` var: `grep "revalidatePath" "src/app/(dashboard)/projeler/[id]/actions.ts"` çıktı verir
    - `npx tsc --noEmit` temiz çıkar
  </acceptance_criteria>
</task>

<task id="T-02-05-02">
  <title>StageTransition Client Component oluştur ve detay sayfasına bağla</title>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/actions.ts — advanceStage imzası ve AdvanceStageResult tipi
    - src/components/ui/dialog.tsx — Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter
    - src/components/ui/button.tsx — Button variant'ları
    - src/app/(dashboard)/projeler/[id]/page.tsx — stage transition placeholder'ın yeri
    - .planning/phases/02-project-core/02-UI-SPEC.md — Stage Transition Confirmation dialog şeması ve Copywriting Contract
  </read_first>
  <action>
**1. `src/app/(dashboard)/projeler/[id]/stage-transition.tsx` dosyasını oluştur:**

```typescript
'use client'
```

Props:
```typescript
interface StageTransitionProps {
  projectId: string
  activeStage: { id: string; stage_name: string } | null
  isLastStage: boolean // Tüm stage'ler tamamlandıysa true
}
```

**Son stage tamamlandıysa (`isLastStage === true` veya `activeStage === null`):**
```tsx
<div className="rounded-lg border border-emerald-500/30 bg-emerald-500/5 p-4">
  <p className="text-sm text-emerald-400">
    Tüm aşamalar tamamlandı. Proje yayın sonrası takibindedir.
  </p>
</div>
```

**Aktif stage varsa:**
```tsx
<Dialog open={open} onOpenChange={setOpen}>
  <DialogTrigger asChild>
    <Button className="w-full h-11">Sonraki Aşamaya Geç</Button>
  </DialogTrigger>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>Aşamayı Tamamla</DialogTitle>
    </DialogHeader>
    <p className="text-sm">
      "{activeStage.stage_name}" aşamasını tamamlamak istediğinizden emin misiniz?
    </p>
    <p className="text-sm text-muted-foreground">Bu işlem geri alınamaz.</p>
    {error && <p className="text-sm text-destructive">{error}</p>}
    <DialogFooter>
      <Button
        variant="outline"
        onClick={() => setOpen(false)}
        disabled={isPending}
      >
        Vazgeç
      </Button>
      <Button
        onClick={handleConfirm}
        disabled={isPending}
        className="h-11"
      >
        {isPending ? 'İşleniyor...' : 'Evet, Tamamla'}
      </Button>
    </DialogFooter>
  </DialogContent>
</Dialog>
```

**handleConfirm:**
```typescript
const handleConfirm = async () => {
  if (!activeStage) return
  setIsPending(true)
  setError(null)
  try {
    const result = await advanceStage(projectId, activeStage.id)
    if (result.success) {
      setOpen(false)
      // revalidatePath server'da çalıştı — sayfa kendiliğinden güncellenir
    } else {
      setError(result.error)
    }
  } finally {
    setIsPending(false)
  }
}
```

**State'ler:** `open`, `isPending`, `error` — useState ile.

**2. `src/app/(dashboard)/projeler/[id]/page.tsx` dosyasını güncelle:**

Stage transition placeholder'ı gerçek bileşenle değiştir:

```tsx
import { StageTransition } from './stage-transition'

// activeStage ve isLastStage hesapla:
const activeStage = stages?.find(s => s.status === 'active') ?? null
const isLastStage = stages ? stages.every(s => s.status === 'completed') : false

// Sağ sütun alt kısmına ekle (Separator'dan sonra):
<StageTransition
  projectId={project.id}
  activeStage={activeStage ? { id: activeStage.id, stage_name: activeStage.stage_name } : null}
  isLastStage={isLastStage}
/>
```
  </action>
  <acceptance_criteria>
    - src/app/(dashboard)/projeler/[id]/stage-transition.tsx dosyası mevcut
    - Dosya `'use client'` direktifi içeriyor: `grep "'use client'" "src/app/(dashboard)/projeler/[id]/stage-transition.tsx"` çıktı verir
    - Dosya `StageTransition` export'u içeriyor: `grep "export.*StageTransition" "src/app/(dashboard)/projeler/[id]/stage-transition.tsx"` çıktı verir
    - Dosyada "Sonraki Aşamaya Geç" butonu var: `grep "Sonraki Aşamaya Geç" "src/app/(dashboard)/projeler/[id]/stage-transition.tsx"` çıktı verir
    - Dosyada "Aşamayı Tamamla" dialog başlığı var: `grep "Aşamayı Tamamla" "src/app/(dashboard)/projeler/[id]/stage-transition.tsx"` çıktı verir
    - Dosyada "Evet, Tamamla" butonu var: `grep "Evet, Tamamla" "src/app/(dashboard)/projeler/[id]/stage-transition.tsx"` çıktı verir
    - Dosyada "Vazgeç" butonu var: `grep "Vazgeç" "src/app/(dashboard)/projeler/[id]/stage-transition.tsx"` çıktı verir
    - Dosyada "Bu işlem geri alınamaz" uyarısı var: `grep "Bu işlem geri alınamaz" "src/app/(dashboard)/projeler/[id]/stage-transition.tsx"` çıktı verir
    - Dosyada "Tüm aşamalar tamamlandı" son stage mesajı var: `grep "Tüm aşamalar tamamlandı" "src/app/(dashboard)/projeler/[id]/stage-transition.tsx"` çıktı verir
    - src/app/(dashboard)/projeler/[id]/page.tsx'de StageTransition import'u var: `grep "StageTransition" "src/app/(dashboard)/projeler/[id]/page.tsx"` çıktı verir
    - `npx tsc --noEmit` temiz çıkar
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
# Dosya varlığı
ls "src/app/(dashboard)/projeler/[id]/actions.ts"
ls "src/app/(dashboard)/projeler/[id]/stage-transition.tsx"

# Server Action kontrolleri
grep "'use server'" "src/app/(dashboard)/projeler/[id]/actions.ts"
grep "export async function advanceStage" "src/app/(dashboard)/projeler/[id]/actions.ts"
grep "eq.*status.*active" "src/app/(dashboard)/projeler/[id]/actions.ts"

# Client Component kontrolleri
grep "Sonraki Aşamaya Geç" "src/app/(dashboard)/projeler/[id]/stage-transition.tsx"
grep "Evet, Tamamla" "src/app/(dashboard)/projeler/[id]/stage-transition.tsx"
grep "Bu işlem geri alınamaz" "src/app/(dashboard)/projeler/[id]/stage-transition.tsx"

# Sayfa entegrasyonu
grep "StageTransition" "src/app/(dashboard)/projeler/[id]/page.tsx"

# TypeScript
npx tsc --noEmit
```
</verification>

<success_criteria>
- advanceStage Server Action: auth + ownership doğrulama yapıyor, aktif stage'i completed, sonrakini active yapıyor
- StageTransition bileşeni: onay dialogu ile stage geçişi çalışıyor, son stage'de tamamlanma mesajı gösteriyor
- Detay sayfasına StageTransition bağlanmış durumda
- TypeScript hata yok
</success_criteria>

<output>
Tamamlandığında `.planning/phases/02-project-core/02-05-SUMMARY.md` oluştur.
</output>
