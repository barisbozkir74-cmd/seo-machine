---
plan: 02-01
phase: 2
wave: 1
title: "shadcn bileşenleri kurulumu"
goal: "Phase 2 UI için gerekli 5 shadcn bileşenini projeye ekle"
depends_on: []
files_modified:
  - src/components/ui/dialog.tsx
  - src/components/ui/table.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/separator.tsx
requirements: []
autonomous: true
must_haves:
  truths:
    - "Dialog bileşeni import edilebilir ve kullanılabilir halde"
    - "Table bileşeni import edilebilir ve kullanılabilir halde"
    - "Badge bileşeni import edilebilir ve kullanılabilir halde"
    - "Textarea bileşeni import edilebilir ve kullanılabilir halde"
    - "Separator bileşeni import edilebilir ve kullanılabilir halde"
  artifacts:
    - path: "src/components/ui/dialog.tsx"
      provides: "Modal dialog wrapper — Phase 2 yeni proje modal ve stage geçiş onayı için"
    - path: "src/components/ui/table.tsx"
      provides: "Proje listesi tablo bileşeni"
    - path: "src/components/ui/badge.tsx"
      provides: "Stage durum badge'i (Aktif / Tamamlandı / Bekliyor)"
    - path: "src/components/ui/textarea.tsx"
      provides: "Serbest not alanı"
    - path: "src/components/ui/separator.tsx"
      provides: "Detay sayfası sütun bölücü"
  key_links:
    - from: "src/components/ui/dialog.tsx"
      to: "@base-ui/react"
      via: "import from '@base-ui/react/dialog'"
      pattern: "base-ui/react"
---

<objective>
Phase 2'nin UI bileşen altyapısını kurar. Beş shadcn bileşeni `npx shadcn add` komutu ile projeye eklenir: dialog, table, badge, textarea, separator.

Purpose: Sonraki plan'ların (02-02 dashboard, 02-03 modal, 02-04 detay, 02-05 geçiş, 02-06 not) bu bileşenleri hazır bulması için temel altyapı.
Output: 5 adet `src/components/ui/*.tsx` dosyası
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-project-core/02-CONTEXT.md
@.planning/phases/02-project-core/02-UI-SPEC.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → filesystem | `npx shadcn add` komutu üçüncü taraf registry'den kod indirir ve `src/components/ui/` altına yazar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | npx shadcn add | mitigate | Yalnızca shadcn official registry kullanılır — üçüncü taraf registry URL'si verilmez. UI-SPEC Registry Safety bölümü: "No third-party registries declared." |
| T-02-01-02 | Information Disclosure | components.json | accept | components.json sadece bileşen konfigürasyonu içerir; secret veya credential yoktur. Düşük risk. |
</threat_model>

<tasks>

<task id="T-02-01-01">
  <title>shadcn bileşenlerini yükle</title>
  <read_first>
    - src/components/ui/ — mevcut bileşenleri görmek için (button.tsx, card.tsx, form.tsx, input.tsx, label.tsx zaten var — tekrar ekleme)
    - components.json — shadcn preset (base-mira), iconLibrary (hugeicons), ve registry URL'sini doğrulamak için
  </read_first>
  <action>
Proje kök dizininden aşağıdaki komutu çalıştır:

```bash
npx shadcn add dialog table badge textarea separator
```

Bu komut `components.json` içindeki konfigürasyonu okuyarak her bileşeni `src/components/ui/` altına yazar.

Komut çalışırken "Would you like to install X?" gibi onay sorarsa "y" ile onayla.

Kurulum tamamlandıktan sonra her dosyanın oluştuğunu doğrula:
- src/components/ui/dialog.tsx
- src/components/ui/table.tsx
- src/components/ui/badge.tsx
- src/components/ui/textarea.tsx
- src/components/ui/separator.tsx

**Önemli:** UI-SPEC'te belirtilen renk sistemi bu bileşenler içinde zaten CSS değişkenleri (`--primary`, `--border`, vb.) kullanır. Herhangi bir renk override yapmaya gerek yok.
  </action>
  <acceptance_criteria>
    - src/components/ui/dialog.tsx dosyası mevcut ve `export` içeriyor
    - src/components/ui/table.tsx dosyası mevcut ve `TableHeader`, `TableBody`, `TableRow`, `TableCell` export'ları var
    - src/components/ui/badge.tsx dosyası mevcut ve `Badge` export'u var
    - src/components/ui/textarea.tsx dosyası mevcut ve `Textarea` export'u var
    - src/components/ui/separator.tsx dosyası mevcut ve `Separator` export'u var
    - Hiçbir dosyada `@radix-ui` import'u yok (shadcn base-mira @base-ui/react kullanır)
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
# Tüm bileşenlerin mevcut olduğunu doğrula
ls src/components/ui/dialog.tsx src/components/ui/table.tsx src/components/ui/badge.tsx src/components/ui/textarea.tsx src/components/ui/separator.tsx

# @radix-ui import'u olmadığını doğrula
grep -r "@radix-ui" src/components/ui/dialog.tsx src/components/ui/table.tsx src/components/ui/badge.tsx src/components/ui/textarea.tsx src/components/ui/separator.tsx || echo "OK: no radix-ui imports"

# TypeScript derleme hatasız
npx tsc --noEmit
```
</verification>

<success_criteria>
- 5 shadcn bileşen dosyası src/components/ui/ altında mevcut
- Hiçbirinde @radix-ui import'u yok
- TypeScript hata yok (`npx tsc --noEmit` temiz çıkar)
</success_criteria>

<output>
Tamamlandığında `.planning/phases/02-project-core/02-01-SUMMARY.md` oluştur.
</output>
