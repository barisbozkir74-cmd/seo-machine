---
plan: 02-06
phase: 2
wave: 5
title: "Karar hafızası — not formu + audits tablosu"
goal: "Aktif stage için serbest metin not ekleme formunu ve audits tablosuna yazma/okuma Server Action'larını oluştur"
depends_on: [02-04, 02-05]
files_modified:
  - src/app/(dashboard)/projeler/[id]/notes-section.tsx
  - src/app/(dashboard)/projeler/[id]/actions.ts
  - src/app/(dashboard)/projeler/[id]/page.tsx
requirements: [PROJ-05]
autonomous: true
must_haves:
  truths:
    - "Aktif stage için not ekleme formu sağ sütunda gösteriliyor (textarea + 'Notu Ekle' butonu)"
    - "Not gönderilince audits tablosuna event_type='note', entity_type='stage', entity_id=activeStageId, payload={'content': '...'} INSERT yapılıyor"
    - "Geçmiş notlar aktif stage için listeleniyor (en yeni üstte)"
    - "Her not: timestamp (TR locale) + not içeriği"
    - "Notlar oturumlar arası kalıcı — sayfa yenilenince hâlâ görünüyor"
    - "Hiç not yokken 'Bu aşama için henüz not eklenmemiş.' mesajı gösteriliyor"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/notes-section.tsx"
      provides: "Client Component — not formu (textarea + buton) ve not geçmişi listesi"
      exports: ["NotesSection"]
    - path: "src/app/(dashboard)/projeler/[id]/actions.ts"
      provides: "addNote(stageId, projectId, content) Server Action — audits INSERT + notlar okuma"
      exports: ["addNote"]
  key_links:
    - from: "notes-section.tsx"
      to: "actions.ts addNote()"
      via: "Server Action call: await addNote(stageId, projectId, content)"
      pattern: "addNote"
    - from: "actions.ts addNote()"
      to: "supabase.audits"
      via: "INSERT event_type='note', entity_type='stage', entity_id=stageId, payload={content}"
      pattern: "from\\('audits'\\)"
    - from: "projeler/[id]/page.tsx"
      to: "notes-section.tsx"
      via: "import NotesSection — not placeholder'ı ile değiştir, initialNotes prop'u ile"
      pattern: "NotesSection"
---

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-project-core/02-CONTEXT.md
@.planning/phases/02-project-core/02-UI-SPEC.md

<interfaces>
<!-- Executor'ın kullanacağı mevcut kod arayüzleri -->

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
```

From src/components/ui/textarea.tsx (02-01 tarafından eklendi):
```typescript
export { Textarea }
// Usage: <Textarea placeholder="Bu aşama için notunuzu buraya yazın..." rows={3} />
```

From src/components/ui/button.tsx:
```typescript
export { Button }
// Note submit: <Button type="submit" size="sm" disabled={isPending}>Notu Ekle</Button>
```

Database schema — audits table:
```sql
id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
project_id UUID REFERENCES projects(id) ON DELETE CASCADE NOT NULL,
user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
event_type TEXT NOT NULL,        -- 'note'
entity_type TEXT,                -- 'stage'
entity_id UUID,                  -- stage.id
payload JSONB,                   -- {"content": "not metni"}
created_at TIMESTAMPTZ DEFAULT now() NOT NULL
```

Note record structure:
```typescript
interface NoteRecord {
  id: string
  created_at: string
  payload: { content: string }
}
```

Copywriting (from UI-SPEC):
- Textarea placeholder: "Bu aşama için notunuzu buraya yazın..."
- Submit button: "Notu Ekle"
- Empty state: "Bu aşama için henüz not eklenmemiş."
- Timestamp format: TR locale "22 Nis 2026, 14:30"

Notes behavior (from UI-SPEC Interaction States):
- Newest note first (ORDER BY created_at DESC)
- No editing or deletion — append-only
- Show all notes (no pagination in Phase 2)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server Action | addNote(stageId, projectId, content) — tüm parametreler dışarıdan gelir |
| Server Action → Supabase | audits tablosuna INSERT yapılır |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Tampering | addNote — stageId ownership | mitigate | Server Action içinde stageId'nin gerçekten bu kullanıcıya ve bu projeye ait olduğu doğrulanır: `SELECT id FROM stages WHERE id=stageId AND project_id=projectId AND user_id=user.id`. Doğrulama başarısız olursa INSERT yapılmaz. |
| T-02-06-02 | Tampering | note content injection | mitigate | Content JSONB payload'a parametrize olarak yazılır (SQL injection yok). Content Supabase'e string olarak gider. XSS için Next.js JSX rendering otomatik escape yapar. |
| T-02-06-03 | Denial of Service | note content max length | mitigate | Server Action'da content uzunluğu doğrulanır: max 5000 karakter. Aşılırsa "Not en fazla 5000 karakter olabilir." hata mesajı döner. |
| T-02-06-04 | Information Disclosure | notes query | mitigate | Notlar sorgusu `WHERE project_id=projectId AND user_id=user.id` filtresi ile yapılır (RLS ayrıca filtreler). Başka kullanıcının notlarına erişim imkânsız. |
</threat_model>

<tasks>

<task id="T-02-06-01">
  <title>addNote Server Action oluştur ve page.tsx'te notları server'da çek</title>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/actions.ts — mevcut dosya (advanceStage zaten var — bu dosyaya ekle, yeniden oluşturma)
    - src/lib/supabase/server.ts — createClient() kullanımı
    - .planning/phases/02-project-core/02-CONTEXT.md — D-11 (audits tablosu yapısı), D-12 (notlar stage bazlı)
  </read_first>
  <action>
`src/app/(dashboard)/projeler/[id]/actions.ts` dosyasına `addNote` fonksiyonunu ekle.

**ÖNEMLİ:** Bu dosya 02-05-PLAN tarafından oluşturulmuştur ve `advanceStage` fonksiyonu içeriyor. Dosyayı baştan yazmak yerine, mevcut içeriğin sonuna bu fonksiyonu EKLE.

Eklenecek kod:

```typescript
export type AddNoteResult =
  | { success: true }
  | { success: false; error: string }

export async function addNote(
  stageId: string,
  projectId: string,
  content: string
): Promise<AddNoteResult> {
  // 1. Validate content
  if (!content || content.trim().length === 0) {
    return { success: false, error: 'Not içeriği boş olamaz.' }
  }
  if (content.length > 5000) {
    return { success: false, error: 'Not en fazla 5000 karakter olabilir.' }
  }

  // 2. Authenticate
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) {
    return { success: false, error: 'Oturum bulunamadı.' }
  }

  // 3. Verify stage ownership
  const { data: stage } = await supabase
    .from('stages')
    .select('id')
    .eq('id', stageId)
    .eq('project_id', projectId)
    .eq('user_id', user.id)
    .single()

  if (!stage) {
    return { success: false, error: 'Aşama bulunamadı.' }
  }

  // 4. Insert note into audits
  const { error } = await supabase.from('audits').insert({
    project_id: projectId,
    user_id: user.id,
    event_type: 'note',
    entity_type: 'stage',
    entity_id: stageId,
    payload: { content: content.trim() },
  })

  if (error) {
    return { success: false, error: 'Not kaydedilemedi. Lütfen tekrar deneyin.' }
  }

  revalidatePath(`/dashboard/projeler/${projectId}`)
  return { success: true }
}
```

**Ayrıca:** `src/app/(dashboard)/projeler/[id]/page.tsx` dosyasına, aktif stage için geçmiş notları server-side çeken sorguyu ekle:

```typescript
// page.tsx içindeki veri çekimi bölümünde, mevcut stages sorgusunun ardından:
const activeStage = stages?.find(s => s.status === 'active') ?? null

const { data: notes } = activeStage ? await supabase
  .from('audits')
  .select('id, created_at, payload')
  .eq('entity_type', 'stage')
  .eq('entity_id', activeStage.id)
  .eq('project_id', project.id)
  .eq('user_id', user.id)  // explicit, defense-in-depth (RLS already filters)
  .order('created_at', { ascending: false })
  : { data: [] }
```

NotesSection bileşenine `initialNotes` prop'u olarak geç.
  </action>
  <acceptance_criteria>
    - src/app/(dashboard)/projeler/[id]/actions.ts dosyası `addNote` export'u içeriyor: `grep "export async function addNote" "src/app/(dashboard)/projeler/[id]/actions.ts"` çıktı verir
    - Dosyada `advanceStage` hâlâ mevcut (silinmemiş): `grep "export async function advanceStage" "src/app/(dashboard)/projeler/[id]/actions.ts"` çıktı verir
    - Dosyada stage ownership doğrulaması var: `grep "entity_type.*stage\|from.*stages.*stageId" "src/app/(dashboard)/projeler/[id]/actions.ts"` çıktı verir
    - Dosyada audits INSERT var: `grep "from.*audits.*insert\|audits.*insert" "src/app/(dashboard)/projeler/[id]/actions.ts"` çıktı verir
    - Dosyada max length kontrolü var: `grep "5000" "src/app/(dashboard)/projeler/[id]/actions.ts"` çıktı verir
    - Dosyada `event_type.*note` var: `grep "event_type.*note" "src/app/(dashboard)/projeler/[id]/actions.ts"` çıktı verir
    - `npx tsc --noEmit` temiz çıkar
  </acceptance_criteria>
</task>

<task id="T-02-06-02">
  <title>NotesSection Client Component oluştur ve detay sayfasına bağla</title>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/actions.ts — addNote imzası ve AddNoteResult tipi
    - src/components/ui/textarea.tsx — Textarea bileşeni
    - src/components/ui/button.tsx — Button bileşeni (size="sm")
    - src/app/(dashboard)/projeler/[id]/page.tsx — not placeholder'ının yeri (02-04-PLAN tarafından oluşturuldu)
    - .planning/phases/02-project-core/02-UI-SPEC.md — Note List interaction states ve Copywriting Contract
  </read_first>
  <action>
**1. `src/app/(dashboard)/projeler/[id]/notes-section.tsx` dosyasını oluştur:**

```typescript
'use client'
```

Props:
```typescript
interface Note {
  id: string
  created_at: string
  payload: { content: string }
}

interface NotesSectionProps {
  stageId: string
  projectId: string
  initialNotes: Note[]
}
```

**Bileşen state:**
- `content: string` — textarea değeri
- `isPending: boolean` — submit işlemi sırasında
- `error: string | null` — hata mesajı

**Form yapısı:**
```tsx
<div className="space-y-4">
  <h2 className="text-base font-semibold">Notlar</h2>

  {/* Not ekleme formu */}
  <div className="space-y-2">
    <Textarea
      value={content}
      onChange={(e) => setContent(e.target.value)}
      placeholder="Bu aşama için notunuzu buraya yazın..."
      rows={3}
      disabled={isPending}
    />
    {error && <p className="text-sm text-destructive">{error}</p>}
    <Button
      size="sm"
      onClick={handleSubmit}
      disabled={isPending || !content.trim()}
    >
      {isPending ? 'Kaydediliyor...' : 'Notu Ekle'}
    </Button>
  </div>

  {/* Geçmiş notlar */}
  <div className="space-y-3 mt-6">
    {notes.length === 0 ? (
      <p className="text-sm text-muted-foreground">
        Bu aşama için henüz not eklenmemiş.
      </p>
    ) : (
      notes.map((note) => (
        <div key={note.id} className="space-y-1 border-l-2 border-border pl-3">
          <p className="text-xs text-muted-foreground">
            {new Date(note.created_at).toLocaleString('tr-TR', {
              day: 'numeric', month: 'short', year: 'numeric',
              hour: '2-digit', minute: '2-digit'
            })}
          </p>
          <p className="text-sm">{note.payload.content}</p>
        </div>
      ))
    )}
  </div>
</div>
```

**handleSubmit:**
```typescript
const handleSubmit = async () => {
  setIsPending(true)
  setError(null)
  try {
    const result = await addNote(stageId, projectId, content)
    if (result.success) {
      setContent('') // Textarea'yı temizle
      // revalidatePath server'da çalıştı — liste server'dan güncel gelir
    } else {
      setError(result.error)
    }
  } finally {
    setIsPending(false)
  }
}
```

**Notların client'ta güncellenmesi:** `revalidatePath` çalıştığında Next.js Server Component'i yeniden render eder ve güncel `initialNotes` prop'u gelir. Client'ta ayrı state tutmaya gerek yok — props'u doğrudan render et.

**2. `src/app/(dashboard)/projeler/[id]/page.tsx` dosyasını güncelle:**

Not placeholder'ını NotesSection bileşeni ile değiştir:

```tsx
import { NotesSection } from './notes-section'

// Sağ sütunda "Notlar" bölümünü değiştir:
{activeStage && (
  <NotesSection
    stageId={activeStage.id}
    projectId={project.id}
    initialNotes={notes ?? []}
  />
)}
```

activeStage yoksa (tüm aşamalar tamamlandı) not bölümü gösterilmez.
  </action>
  <acceptance_criteria>
    - src/app/(dashboard)/projeler/[id]/notes-section.tsx dosyası mevcut
    - Dosya `'use client'` direktifi içeriyor: `grep "'use client'" "src/app/(dashboard)/projeler/[id]/notes-section.tsx"` çıktı verir
    - Dosya `NotesSection` export'u içeriyor: `grep "export.*NotesSection" "src/app/(dashboard)/projeler/[id]/notes-section.tsx"` çıktı verir
    - Dosyada "Notu Ekle" butonu var: `grep "Notu Ekle" "src/app/(dashboard)/projeler/[id]/notes-section.tsx"` çıktı verir
    - Dosyada "Bu aşama için notunuzu buraya yazın" placeholder var: `grep "notunuzu buraya yazın" "src/app/(dashboard)/projeler/[id]/notes-section.tsx"` çıktı verir
    - Dosyada "Bu aşama için henüz not eklenmemiş" boş durum mesajı var: `grep "henüz not eklenmemiş" "src/app/(dashboard)/projeler/[id]/notes-section.tsx"` çıktı verir
    - Dosyada TR locale tarih formatı var: `grep "tr-TR" "src/app/(dashboard)/projeler/[id]/notes-section.tsx"` çıktı verir
    - src/app/(dashboard)/projeler/[id]/page.tsx'de NotesSection import'u var: `grep "NotesSection" "src/app/(dashboard)/projeler/[id]/page.tsx"` çıktı verir
    - `npx tsc --noEmit` temiz çıkar
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
# Dosya varlığı
ls "src/app/(dashboard)/projeler/[id]/notes-section.tsx"

# addNote Server Action
grep "export async function addNote" "src/app/(dashboard)/projeler/[id]/actions.ts"
grep "export async function advanceStage" "src/app/(dashboard)/projeler/[id]/actions.ts"
grep "event_type.*note" "src/app/(dashboard)/projeler/[id]/actions.ts"
grep "5000" "src/app/(dashboard)/projeler/[id]/actions.ts"

# NotesSection kontrolleri
grep "Notu Ekle" "src/app/(dashboard)/projeler/[id]/notes-section.tsx"
grep "henüz not eklenmemiş" "src/app/(dashboard)/projeler/[id]/notes-section.tsx"
grep "tr-TR" "src/app/(dashboard)/projeler/[id]/notes-section.tsx"

# Sayfa entegrasyonu
grep "NotesSection" "src/app/(dashboard)/projeler/[id]/page.tsx"

# TypeScript
npx tsc --noEmit
```
</verification>

<success_criteria>
- addNote Server Action: stage ownership doğruluyor, audits tablosuna event_type='note' INSERT yapıyor
- NotesSection: textarea + buton ile not ekleniyor, geçmiş notlar TR locale tarihle listeleniyor
- Hiç not yokken "Bu aşama için henüz not eklenmemiş." gösteriliyor
- Detay sayfasına NotesSection bağlanmış durumda
- TypeScript hata yok
</success_criteria>

<output>
Tamamlandığında `.planning/phases/02-project-core/02-06-SUMMARY.md` oluştur.
</output>
