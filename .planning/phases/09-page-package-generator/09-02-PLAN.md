---
phase: 09-page-package-generator
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts
  - src/app/api/ai/generate-page-package/route.ts
autonomous: true
requirements:
  - BLUE-04
  - PAGE-01

must_haves:
  truths:
    - "updatePagePackage server action artık pages tablosuna değil page_packages tablosuna yazar (upsert)"
    - "createPagePackage action: Manuel Başlat ve AI generate için boş draft package satırı oluşturur"
    - "updatePackageStatus action: draft→approved→locked status geçişlerini yapar, timestamp alanlarını günceller"
    - "AI route locked package'ı reddeder (HTTP 403) — kilidi açılmadan regenerate edilemez"
    - "Tüm server actions getUser() ile auth kontrolü yapar ve project ownership doğrular"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts"
      provides: "updatePagePackage, createPagePackage, updatePackageStatus server actions"
      exports: ["updatePagePackage", "createPagePackage", "updatePackageStatus", "PagePackageData", "ActionResult"]
    - path: "src/app/api/ai/generate-page-package/route.ts"
      provides: "Streaming AI endpoint — locked package kontrolü + page_packages ile uyumlu"
      contains: "existingPkg?.status === 'locked'"
  key_links:
    - from: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts"
      to: "page_packages table"
      via: "supabase.from('page_packages').upsert()"
      pattern: "from\\('page_packages'\\)"
    - from: "src/app/api/ai/generate-page-package/route.ts"
      to: "page_packages table"
      via: "locked status check"
      pattern: "existingPkg\\.status === 'locked'"
---

<objective>
`actions.ts` dosyasını `page_packages` tablosunu hedef alacak şekilde tamamen yeniden yazar. Üç action ekler: mevcut `updatePagePackage` (yeni tabloya), yeni `createPagePackage` (Manuel Başlat + AI generate için), yeni `updatePackageStatus` (status workflow). AI route'u locked package kontrolü ile günceller.

Purpose: Wave 3 UI bileşenlerinin çağıracağı server-side veri katmanını kurar. Bu plan olmadan Wave 3 planları compile edilemez.
Output: 3 server action + güncellenmiş AI route.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-page-package-generator/09-CONTEXT.md
@.planning/phases/09-page-package-generator/09-PATTERNS.md
@.planning/phases/09-page-package-generator/09-01-SUMMARY.md

<interfaces>
<!-- actions.ts'nin export edeceği tipler ve fonksiyonlar (Wave 3 UI bunları import edecek): -->

```typescript
// ActionResult — tüm actions için standart dönüş tipi
export type ActionResult = { success: true } | { success: false; error: string }

// CreatePackageResult — createPagePackage için id de döner
export type CreatePackageResult =
  | { success: true; id: string }
  | { success: false; error: string }

// PagePackageData — updatePagePackage'a geçilen veri şekli
export type PagePackageData = {
  page_type?: string
  strategic_purpose?: string
  search_intent?: string
  slug?: string
  seo_title?: string
  meta_description?: string
  h1?: string
  canonical_url?: string
  heading_hierarchy?: unknown
  schema_type?: string
  content_blocks?: unknown
  cta_blocks?: unknown
  image_plan?: unknown
  alt_texts?: unknown
  secondary_keywords?: unknown
  faq?: unknown
  qa_scores?: unknown
}

// updatePagePackage: page_packages tablosuna upsert (onConflict: 'page_id')
export async function updatePagePackage(
  projectId: string,
  pageId: string,
  data: PagePackageData
): Promise<ActionResult>

// createPagePackage: yeni draft package satırı oluştur
export async function createPagePackage(
  projectId: string,
  pageId: string,
  generatedBy: 'ai' | 'manual'
): Promise<CreatePackageResult>

// updatePackageStatus: status geçişi (draft|approved|locked)
// Kurallar: locked → approved (unlock), draft → approved, approved → locked, approved → draft
export async function updatePackageStatus(
  projectId: string,
  packageId: string,
  newStatus: 'draft' | 'approved' | 'locked'
): Promise<ActionResult>
```

<!-- Auth + ownership pattern (ic-link-haritasi/actions.ts'den): -->
```typescript
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) return { success: false, error: 'Oturum bulunamadı.' }

const { data: project } = await supabase
  .from('projects')
  .select('id')
  .eq('id', projectId)
  .eq('user_id', user.id)
  .single()
if (!project) return { success: false, error: 'Proje bulunamadı.' }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: actions.ts — page_packages tablosuna yeniden yaz</name>
  <files>src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts (mevcut dosya — tam içeriği oku, ne değişeceğini anla)
    - src/app/(dashboard)/projeler/[id]/ic-link-haritasi/actions.ts (auth + ownership pattern referansı)
    - .planning/phases/09-page-package-generator/09-CONTEXT.md (D-03 status workflow, D-05 migration stratejisi)
    - .planning/phases/09-page-package-generator/09-PATTERNS.md (actions.ts pattern blokları — SQL upsert, createPackage, updatePackageStatus)
  </read_first>
  <action>
`actions.ts` dosyasını tamamen aşağıdaki içerikle değiştir. Eski dosyada `pages` tablosuna yazan `updatePagePackage` fonksiyonu vardı — artık `page_packages` tablosuna upsert yapar.

Yeni dosya tam içeriği:

```typescript
'use server'

import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'

export type ActionResult = { success: true } | { success: false; error: string }

export type CreatePackageResult =
  | { success: true; id: string }
  | { success: false; error: string }

export type PagePackageData = {
  // Temel Bilgiler
  page_type?: string
  strategic_purpose?: string
  search_intent?: string
  // Meta & URL
  slug?: string
  seo_title?: string
  meta_description?: string
  h1?: string
  canonical_url?: string
  // Heading
  heading_hierarchy?: unknown
  // Schema
  schema_type?: string
  // İçerik
  content_blocks?: unknown
  cta_blocks?: unknown
  // Görsel
  image_plan?: unknown
  alt_texts?: unknown
  // Keywords
  secondary_keywords?: unknown
  // FAQ
  faq?: unknown
  // QA
  qa_scores?: unknown
}

// Proje sahipliğini doğrulayan yardımcı fonksiyon
async function verifyOwnership(supabase: Awaited<ReturnType<typeof createClient>>, projectId: string, userId: string) {
  const { data } = await supabase
    .from('projects')
    .select('id')
    .eq('id', projectId)
    .eq('user_id', userId)
    .single()
  return data
}

/**
 * updatePagePackage: page_packages tablosuna upsert yapar.
 * Çakışma durumunda (aynı page_id) mevcut satırı günceller.
 * D-05: Artık pages tablosuna değil, page_packages tablosuna yazar.
 */
export async function updatePagePackage(
  projectId: string,
  pageId: string,
  data: PagePackageData
): Promise<ActionResult> {
  const supabase = await createClient()

  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Oturum bulunamadı.' }

  const project = await verifyOwnership(supabase, projectId, user.id)
  if (!project) return { success: false, error: 'Proje bulunamadı.' }

  const { error } = await supabase
    .from('page_packages')
    .upsert(
      {
        page_id: pageId,
        project_id: projectId,
        user_id: user.id,
        ...data,
        updated_at: new Date().toISOString(),
      },
      { onConflict: 'page_id' }
    )

  if (error) return { success: false, error: 'Paket kaydedilemedi: ' + error.message }

  revalidatePath(`/projeler/${projectId}/sayfa-paketi`)
  return { success: true }
}

/**
 * createPagePackage: Boş draft package satırı oluşturur.
 * D-02: Kullanıcı "AI ile Üret" veya "Manuel Başlat" aksiyonunu aldığında çağrılır.
 * Sayfa eklenince otomatik çağrılmaz.
 */
export async function createPagePackage(
  projectId: string,
  pageId: string,
  generatedBy: 'ai' | 'manual'
): Promise<CreatePackageResult> {
  const supabase = await createClient()

  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Oturum bulunamadı.' }

  const project = await verifyOwnership(supabase, projectId, user.id)
  if (!project) return { success: false, error: 'Proje bulunamadı.' }

  // Sayfa sahipliğini doğrula
  const { data: page } = await supabase
    .from('pages')
    .select('id')
    .eq('id', pageId)
    .eq('project_id', projectId)
    .eq('user_id', user.id)
    .single()
  if (!page) return { success: false, error: 'Sayfa bulunamadı.' }

  const { data, error } = await supabase
    .from('page_packages')
    .insert({
      page_id: pageId,
      project_id: projectId,
      user_id: user.id,
      status: 'draft',
      generated_by: generatedBy,
      ai_model: generatedBy === 'ai' ? 'claude-sonnet-4-6' : null,
    })
    .select('id')
    .single()

  if (error) {
    // Zaten mevcut ise (UNIQUE violation) mevcut paketi döndür
    if (error.code === '23505') {
      const { data: existing } = await supabase
        .from('page_packages')
        .select('id')
        .eq('page_id', pageId)
        .single()
      if (existing) return { success: true, id: existing.id }
    }
    return { success: false, error: 'Paket oluşturulamadı: ' + error.message }
  }

  revalidatePath(`/projeler/${projectId}/sayfa-paketi`)
  return { success: true, id: data.id }
}

/**
 * updatePackageStatus: Package status geçişi.
 * D-03: draft → approved → locked, locked → approved (unlock), approved → draft
 * Status'a göre timestamp alanları güncellenir.
 */
export async function updatePackageStatus(
  projectId: string,
  packageId: string,
  newStatus: 'draft' | 'approved' | 'locked'
): Promise<ActionResult> {
  const supabase = await createClient()

  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Oturum bulunamadı.' }

  const project = await verifyOwnership(supabase, projectId, user.id)
  if (!project) return { success: false, error: 'Proje bulunamadı.' }

  const now = new Date().toISOString()
  const timestampField =
    newStatus === 'approved' ? { approved_at: now } :
    newStatus === 'locked'   ? { locked_at: now } :
    {}

  const { error } = await supabase
    .from('page_packages')
    .update({
      status: newStatus,
      ...timestampField,
      updated_at: now,
    })
    .eq('id', packageId)
    .eq('project_id', projectId)
    .eq('user_id', user.id)

  if (error) return { success: false, error: 'Durum güncellenemedi: ' + error.message }

  revalidatePath(`/projeler/${projectId}/sayfa-paketi`)
  return { success: true }
}
```
  </action>
  <verify>
    <automated>grep -c "from('page_packages')" "src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from('page_packages')" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` → 4 (verifyOwnership içindeki projects hariç, page_packages için: upsert, insert, select existing, update)
    - Dosyada `from('pages')` satırı yok (eski updatePagePackage pages'e yazıyordu): `grep "\.from('pages')" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` → sadece page sahipliği kontrolü (1 satır, page_packages değil pages tablosu)
    - `export async function createPagePackage` satırı mevcut
    - `export async function updatePackageStatus` satırı mevcut
    - `export async function updatePagePackage` satırı mevcut
    - `export type ActionResult` satırı mevcut
    - `export type CreatePackageResult` satırı mevcut
    - `onConflict: 'page_id'` upsert pattern mevcut
    - `revalidatePath` her üç action'da çağrılıyor: `grep -c "revalidatePath" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` → 3
    - `getUser()` her üç action'da çağrılıyor: `grep -c "getUser()" src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts` → 3
  </acceptance_criteria>
  <done>actions.ts tamamen yeniden yazıldı; updatePagePackage page_packages'a upsert eder, createPagePackage ve updatePackageStatus eklendi.</done>
</task>

<task type="auto">
  <name>Task 2: AI route — locked package kontrolü ekle</name>
  <files>src/app/api/ai/generate-page-package/route.ts</files>
  <read_first>
    - src/app/api/ai/generate-page-package/route.ts (mevcut dosya — tam içeriği oku)
    - .planning/phases/09-page-package-generator/09-CONTEXT.md (D-08 AI provider kararı, D-03 locked state)
    - .planning/phases/09-page-package-generator/09-PATTERNS.md (route.ts değişiklik açıklaması)
  </read_first>
  <action>
Mevcut `route.ts` dosyasına minimum değişiklik yap. Sadece iki şey değişir:

**1. Proje sahipliği kontrolü SONRASINA, streaming ÖNCESINE şu bloğu ekle** (yaklaşık line 31'den sonra, `page` sorgusu ve `if (!page)` kontrolünden sonra):

```typescript
  // D-03: Locked package'ı AI ile regenerate edemezsin — önce kilidi aç
  const { data: existingPkg } = await supabase
    .from('page_packages')
    .select('id, status')
    .eq('page_id', pageId)
    .eq('project_id', projectId)
    .single()

  if (existingPkg?.status === 'locked') {
    return new Response('Package is locked. Unlock before regenerating.', { status: 403 })
  }
```

**2. Sayfa sorgusu sadeleştir** — `page` sorgusunda artık package alanlarını çekmemize gerek yok (onlar page_packages'ta). Sadece identity alanları yeterli. Mevcut SELECT'i şu şekilde değiştir:

```typescript
  const { data: page } = await supabase
    .from('pages')
    .select('id, title, slug, page_type, focus_keyword_id')
    .eq('id', pageId)
    .eq('project_id', projectId)
    .eq('user_id', user.id)
    .single()
```

**Dikkat:** `buildPrompt` fonksiyonu `page` argümanı için `strategic_purpose`, `search_intent` vb. kullanıyor olabilir — bu alanları sil veya boş string olarak geç. `buildPrompt` içinde `page.strategic_purpose ?? ''` ve `page.search_intent ?? ''` gibi alanlar varsa bunları kaldır. Streaming pattern, model seçimi, prompt yapısı, encoder, ReadableStream — hiçbirini değiştirme.

Değişiklikler özetlenir:
- `page` SELECT'i sadeleştirildi (5 alan)
- Locked package kontrolü eklendi (3-satır Supabase sorgu + if blok)
- Geri kalan her şey aynı
  </action>
  <verify>
    <automated>grep -c "existingPkg?.status === 'locked'" "src/app/api/ai/generate-page-package/route.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "existingPkg?.status === 'locked'" src/app/api/ai/generate-page-package/route.ts` → eşleşme bulundu
    - `grep "status: 403" src/app/api/ai/generate-page-package/route.ts` → eşleşme bulundu
    - `grep "from('page_packages')" src/app/api/ai/generate-page-package/route.ts` → eşleşme bulundu (yeni locked check sorgusu)
    - Dosyada `Anthropic` import ve streaming pattern korunuyor: `grep "ReadableStream" src/app/api/ai/generate-page-package/route.ts` → eşleşme bulundu
    - `grep "model: 'claude-sonnet-4-6'" src/app/api/ai/generate-page-package/route.ts` → eşleşme bulundu (model değiştirilmedi)
  </acceptance_criteria>
  <done>AI route, locked package'lara HTTP 403 döndürür; sayfa sorgusu sadeleştirildi; streaming korundu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server Action | Server action'lar Next.js Server Actions mekanizması üzerinden çağrılır; her çağrıda auth kontrolü zorunlu |
| Server Action → Supabase DB | Supabase client server-side oluşturulur; RLS ek koruma sağlar |
| Client → AI Route | POST /api/ai/generate-page-package — pageId, projectId client'tan gelir; doğrulanması zorunlu |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Spoofing | updatePagePackage / createPagePackage / updatePackageStatus | mitigate | Her action'da `getUser()` ile oturum doğrulanır; `verifyOwnership()` ile projectId ownership kontrol edilir |
| T-09-02-02 | Tampering | updatePackageStatus — status geçişleri | mitigate | status değeri server-side geçirilir; client yalnızca geçerli enum değeri gönderebilir; RLS update policy user_id'yi korur |
| T-09-02-03 | Elevation of Privilege | createPagePackage — başka kullanıcının page_id | mitigate | pages tablosunda `eq('user_id', user.id)` kontrolü — başka kullanıcının sayfasına package oluşturulamaz |
| T-09-02-04 | Tampering | AI route — locked package AI regenerate | mitigate | existingPkg.status === 'locked' kontrolü ile HTTP 403 döndürülür |
| T-09-02-05 | Information Disclosure | AI route — project/page verisi | mitigate | project: eq('user_id', user.id) + page: eq('user_id', user.id) — yabancı proje/sayfa verisine erişilemez |
</threat_model>

<verification>
```bash
# TypeScript derleme kontrolü
npx tsc --noEmit 2>&1 | grep -E "sayfa-paketi/actions|generate-page-package" || echo "NO_ERRORS"

# actions.ts export kontrolü
grep "export async function" "src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts"
# Beklenen çıktı: updatePagePackage, createPagePackage, updatePackageStatus

# AI route locked check
grep "status === 'locked'" "src/app/api/ai/generate-page-package/route.ts"
```
</verification>

<success_criteria>
- `updatePagePackage` artık `page_packages` tablosuna upsert yapar — `pages` tablosuna yazmaz
- `createPagePackage` yeni draft package satırı oluşturur
- `updatePackageStatus` approved_at / locked_at timestamp'larını doğru günceller
- AI route locked package'a HTTP 403 döndürür
- TypeScript derleme hatası yok
- Wave 3 UI bileşenleri bu action'ları import edebilir
</success_criteria>

<output>
After completion, create `.planning/phases/09-page-package-generator/09-02-SUMMARY.md`
</output>
