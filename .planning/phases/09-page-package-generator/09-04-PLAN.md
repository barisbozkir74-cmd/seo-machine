---
phase: 09-page-package-generator
plan: 04
type: execute
wave: 3
depends_on:
  - "09-02"
  - "09-03"
files_modified:
  - src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx
  - src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx
autonomous: true
requirements:
  - BLUE-04
  - PAGE-01

must_haves:
  truths:
    - "page.tsx sayfa listesinde her satırda PackageStatusBadge gösterir — package yoksa 'Paket Yok'"
    - "Package olmayan satırlarda 'Üret' ve 'Manuel Başlat' inline butonları görünür"
    - "PagePackageEditor başlığında QaBadge + PackageStatusBadge + status aksiyon butonları var"
    - "status === 'locked' ise tüm Input/Textarea alanları disabled, LockedBanner görünür, Kaydet butonu gizli"
    - "Manuel Başlat: createPagePackage('manual') çağrılır, editor boş alanlarla açılır"
    - "Status geçişleri (Onayla/Kilitle/Taslağa Al/Kilidini Aç) updatePackageStatus server action'ı çağırır"
    - "Kilidini Aç için Dialog confirmation gösterilir (DialogTrigger render={} pattern — asChild yasak)"
    - "Hiçbir yerde font-medium CSS sınıfı yok — page.tsx line 161 ve PagePackageEditor.tsx line 43 ve 161 düzeltilmiş"
    - "pages tablosundan artık package alanları (seo_title, h1, vb.) sorgulanmıyor — sadece identity alanları"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx"
      provides: "3-panel layout — pages + page_packages join, PackageStatusBadge, inline Create butonu"
      contains: "from('page_packages')"
    - path: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx"
      provides: "Migrate edilmiş editor — QaBadge, PackageStatusBadge, LockedBanner, status workflow"
      contains: "updatePackageStatus"
  key_links:
    - from: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx"
      to: "page_packages table"
      via: "supabase.from('page_packages').select().in('page_id', pageIds)"
      pattern: "packageMap\\.get\\(page\\.id\\)"
    - from: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx"
      to: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts"
      via: "import { updatePackageStatus, createPagePackage } from './actions'"
      pattern: "updatePackageStatus"
---

<objective>
`page.tsx` ve `PagePackageEditor.tsx` dosyalarını `page_packages` tablosuna migrate eder. `page.tsx` sayfa listesine PackageStatusBadge + inline create butonları ekler. `PagePackageEditor.tsx` header'a QaBadge + PackageStatusBadge + status aksiyon butonları ekler, locked state'i uygular, tüm `font-medium` kullanımlarını temizler.

Purpose: Phase 9'un son teslim edilebilir UI katmanı — kullanıcı artık page_packages ile çalışır.
Output: İki güncellenmiş dosya. Phase 9 başarı kriterlerinin tamamı karşılanır.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-page-package-generator/09-CONTEXT.md
@.planning/phases/09-page-package-generator/09-UI-SPEC.md
@.planning/phases/09-page-package-generator/09-PATTERNS.md
@.planning/phases/09-page-package-generator/09-02-SUMMARY.md
@.planning/phases/09-page-package-generator/09-03-SUMMARY.md

<interfaces>
<!-- 09-02'den export edilen actions: -->
```typescript
import {
  updatePagePackage,
  createPagePackage,
  updatePackageStatus,
  type PagePackageData,
  type ActionResult,
  type CreatePackageResult,
} from './actions'
```

<!-- 09-03'ten export edilen bileşenler: -->
```typescript
import { QaBadge, type QaBadgeProps } from './QaBadge'
import { PackageStatusBadge } from './PackageStatusBadge'
import { LockedBanner, type LockedBannerProps } from './LockedBanner'
```

<!-- Dialog pattern (base-ui — asChild YASAK, render={} kullanılır): -->
```typescript
// DOĞRU:
<DialogTrigger render={<Button variant="outline" size="sm">Kilidini Aç</Button>} />
// YANLIŞ — YASAK:
<DialogTrigger asChild><Button>...</Button></DialogTrigger>
```

<!-- UI-SPEC'ten action buton kuralları: -->
// draft: "Onayla" (primary) + "AI ile Üret" (outline)
// approved: "Kilitle" (primary) + "AI ile Üret" (outline) + "Taslağa Al" (ghost)
// locked: "Kilidini Aç" (outline) — Dialog confirmation tetikler
// no package: "AI ile Üret" (primary) + "Manuel Başlat" (outline)

<!-- page.tsx için yeni PageRow tipi (package alanları kaldırıldı): -->
```typescript
type PageRow = {
  id: string
  title: string
  slug: string | null
  page_type: string | null
  focus_keyword_id: string | null
}

type PackageRow = {
  id: string
  page_id: string
  status: string
  generated_by: string
}
```

<!-- PageData tipi (PagePackageEditor için — package_packages alanları buraya taşındı): -->
```typescript
export type PageData = {
  // Page identity (pages tablosundan)
  id: string
  title: string
  slug: string | null
  page_type: string | null
  focus_keyword_id: string | null
  focus_keyword_text?: string | null
  // Package verisi (page_packages tablosundan — null ise package yok)
  pkg?: {
    id: string
    status: string
    generated_by: string
    seo_title: string | null
    meta_description: string | null
    h1: string | null
    slug: string | null
    search_intent: string | null
    strategic_purpose: string | null
    secondary_keywords: unknown
    heading_hierarchy: unknown
    content_blocks: unknown
    cta_blocks: unknown
    image_plan: unknown
    alt_texts: unknown
    schema_type: string | null
    canonical_url: string | null
    faq: unknown
  } | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx — pages sorgusu sadeleştir + package_packages join + inline create butonları</name>
  <files>src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx (mevcut dosya — tam içeriği oku, ne değişeceğini anla)
    - .planning/phases/09-page-package-generator/09-PATTERNS.md (page.tsx değişiklik pattern blokları — çift sorgu, sayfa listesi row pattern)
    - .planning/phases/09-page-package-generator/09-UI-SPEC.md (Page List Panel layout, renk sınıfları)
    - .planning/phases/09-page-package-generator/09-CONTEXT.md (D-02: package yoksa "Üret"/"Manuel Başlat")
  </read_first>
  <action>
`page.tsx` dosyasını aşağıdaki değişikliklerle güncelle. Mevcut auth, proje query, ProjectNav, genel layout — korunur.

**1. Type güncellemeleri — dosyanın tepesindeki type'ları değiştir:**

```typescript
// ESKİ PageRow'u tamamen kaldır (package alanları vardı)
// YENİ:
type PageRow = {
  id: string
  title: string
  slug: string | null
  page_type: string | null
  focus_keyword_id: string | null
}

type PackageRow = {
  id: string
  page_id: string
  status: string
  generated_by: string
}
```

**2. STATUS_LABELS ve StatusBadge fonksiyonunu kaldır** — artık PackageStatusBadge bileşeni kullanılıyor.

**3. Import'lara ekle:**

```typescript
import { PackageStatusBadge } from './PackageStatusBadge'
import { createPagePackage } from './actions'
```

**4. pages sorgusu sadeleştir** (mevcut uzun SELECT'i değiştir):

```typescript
const { data: pagesRaw } = await supabase
  .from('pages')
  .select('id, title, slug, page_type, focus_keyword_id')
  .eq('project_id', id)
  .eq('user_id', user.id)
  .order('title', { ascending: true })

const pages: PageRow[] = (pagesRaw ?? []) as PageRow[]
```

**5. page_packages join sorgusunu ekle** (focusKwIds sorgusundan önce, pages sorgusundan sonra):

```typescript
// package durumlarını çek
const pageIds = pages.map((p) => p.id)
const packagesRaw: PackageRow[] = []
if (pageIds.length > 0) {
  const { data } = await supabase
    .from('page_packages')
    .select('id, page_id, status, generated_by')
    .in('page_id', pageIds)
  if (data) packagesRaw.push(...(data as PackageRow[]))
}
const packageMap = new Map(packagesRaw.map((pkg) => [pkg.page_id, pkg]))
```

**6. selectedPageData inşasını güncelle** — artık PageData'da `pkg` alanı var. Seçili sayfa için page_packages'tan tam veriyi çek:

```typescript
let selectedPageData: PageData | null = null
if (selectedPage) {
  const focusKwText = selectedPage.focus_keyword_id
    ? (kwMap[selectedPage.focus_keyword_id] ?? null)
    : null

  // Seçili sayfanın package verisini çek (eğer varsa)
  let pkg: PageData['pkg'] = null
  if (packageMap.has(selectedPage.id)) {
    const { data: pkgData } = await supabase
      .from('page_packages')
      .select('id, status, generated_by, seo_title, meta_description, h1, slug, search_intent, strategic_purpose, secondary_keywords, heading_hierarchy, content_blocks, cta_blocks, image_plan, alt_texts, schema_type, canonical_url, faq')
      .eq('page_id', selectedPage.id)
      .single()
    pkg = pkgData as PageData['pkg']
  }

  selectedPageData = {
    ...selectedPage,
    focus_keyword_text: focusKwText,
    pkg,
  }
}
```

**7. Sayfa listesi row'larını güncelle** — `StatusBadge` yerine `PackageStatusBadge` + inline create butonları:

```typescript
{pages.map((page) => {
  const isActive = page.id === selectedPageId
  const pkg = packageMap.get(page.id)

  return (
    <li key={page.id}>
      <Link
        href={`/projeler/${id}/sayfa-paketi?page=${page.id}`}
        className={cn(
          'flex flex-col gap-1 px-4 py-3 text-sm transition-colors border-l-2',
          isActive
            ? 'bg-secondary border-l-foreground'
            : 'border-l-transparent hover:bg-secondary/50 hover:border-l-border'
        )}
      >
        <span
          className={cn(
            'leading-tight',
            isActive ? 'text-foreground' : 'text-foreground/80'
          )}
        >
          {page.title}
        </span>
        <div className="flex items-center gap-2 flex-wrap">
          {page.page_type && (
            <span className="text-xs text-muted-foreground">{page.page_type}</span>
          )}
          <PackageStatusBadge status={pkg?.status ?? null} />
        </div>
      </Link>
    </li>
  )
})}
```

**NOT:** "Üret" ve "Manuel Başlat" inline butonları Server Component'de form action ile çalışmaz (createPagePackage'ı doğrudan Server Component'ten çağıramayız). Bu butonların işlevselliği PagePackageEditor içindeki "no package" durumunda ele alınır — sayfa listesi yalnızca badge gösterir.

**8. font-medium kaldır** — mevcut dosyada line 161'de `font-medium` var (`<span className={cn('font-medium leading-tight', ...)}`). Değiştir:

```typescript
// ESKİ:
<span className={cn('font-medium leading-tight', isActive ? 'text-foreground' : 'text-foreground/80')}>
// YENİ:
<span className={cn('leading-tight', isActive ? 'text-foreground' : 'text-foreground/80')}>
```

**9. PagePackageEditor'a prop geçişini güncelle:**

```typescript
{selectedPageData ? (
  <PagePackageEditor projectId={id} page={selectedPageData} />
) : (...)}
```

`PageData` tipi artık `pkg` alanını taşıyor — bu yeterli.
  </action>
  <verify>
    <automated>grep -c "from('page_packages')" "src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from('page_packages')" src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx` → eşleşme bulundu (package sorgusu eklendi)
    - `grep "PackageStatusBadge" src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx` → eşleşme bulundu (import + kullanım)
    - `grep "font-medium" src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx` → çıktı yok (yasak sınıf kaldırıldı)
    - `grep "StatusBadge" src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx` → çıktı yok (eski bileşen kaldırıldı)
    - `grep "seo_title\|meta_description\|h1,\|heading_hierarchy\|content_blocks" src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx | grep "select(" | head -1` → pages SELECT'te bu alanlar yok (sadece package SELECT'te)
    - `grep "packageMap" src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx` → eşleşme bulundu
    - TypeScript tip uyumluluğu: `npx tsc --noEmit 2>&1 | grep "sayfa-paketi/page"` → çıktı yok
  </acceptance_criteria>
  <done>page.tsx sadeleştirildi: pages sorgusu yalnızca identity alanları, page_packages join eklendi, PackageStatusBadge entegre edildi, font-medium kaldırıldı.</done>
</task>

<task type="auto">
  <name>Task 2: PagePackageEditor.tsx — page_packages'a migrate, status workflow, locked state, QaBadge</name>
  <files>src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx (mevcut dosya — tam içeriği oku)
    - .planning/phases/09-page-package-generator/09-PATTERNS.md (PagePackageEditor değişiklik pattern blokları — Field disabled prop, status aksiyon butonları, streaming pattern)
    - .planning/phases/09-page-package-generator/09-UI-SPEC.md (Package Editor Header layout, Action buttons by status, Locked State, Interaction Contracts, Copywriting Contract)
    - .planning/phases/09-page-package-generator/09-CONTEXT.md (D-03 status workflow, D-04 QA rules)
    - src/app/(dashboard)/projeler/[id]/ic-link-haritasi/SuggestLinksDialog.tsx (Dialog base-ui pattern referansı)
  </read_first>
  <action>
`PagePackageEditor.tsx` dosyasını kapsamlı şekilde güncelle. Mevcut dosyadan korunanlar: `jsonString`, `parseJsonField` helpers, `handleAiGenerate` streaming logic, `handleSave` useTransition pattern, tüm form section'ları (9 bölüm), `Field` component'in temel yapısı.

**1. Yeni importlar ekle:**

```typescript
import { QaBadge } from './QaBadge'
import { PackageStatusBadge } from './PackageStatusBadge'
import { LockedBanner } from './LockedBanner'
import { Dialog, DialogTrigger, DialogPopup, DialogTitle, DialogDescription, DialogClose } from '@/components/ui/dialog'
import { updatePagePackage, createPagePackage, updatePackageStatus } from './actions'
```

**2. PageData tipini güncelle** — artık `pkg` alanı var, eski package alanları kaldırıldı:

```typescript
export type PageData = {
  id: string
  title: string
  slug: string | null
  page_type: string | null
  focus_keyword_id: string | null
  focus_keyword_text?: string | null
  pkg?: {
    id: string
    status: string
    generated_by: string
    seo_title: string | null
    meta_description: string | null
    h1: string | null
    slug: string | null
    search_intent: string | null
    strategic_purpose: string | null
    secondary_keywords: unknown
    heading_hierarchy: unknown
    content_blocks: unknown
    cta_blocks: unknown
    image_plan: unknown
    alt_texts: unknown
    schema_type: string | null
    canonical_url: string | null
    faq: unknown
  } | null
}
```

**3. Field component'i `disabled` ve `maxLenWarning` prop'ları ile güncelle:**

```typescript
type FieldProps = {
  label: string
  id: string
  value: string
  onChange: (v: string) => void
  multiline?: boolean
  rows?: number
  placeholder?: string
  mono?: boolean
  maxLen?: number
  maxLenWarning?: number  // soft limit threshold
  disabled?: boolean       // locked state için
}

function Field({ label, id, value, onChange, multiline, rows = 4, placeholder, mono, maxLen, maxLenWarning, disabled }: FieldProps) {
  const isOverHard = maxLen ? value.length > maxLen : false
  const isOverSoft = maxLenWarning ? value.length > maxLenWarning : false

  return (
    <div className="space-y-1.5">
      <div className="flex items-center justify-between">
        <Label htmlFor={id} className="text-xs font-normal text-muted-foreground uppercase tracking-wide">
          {label}
        </Label>
        {maxLen && value.length > 0 && (
          <span className={cn(
            'text-xs',
            isOverHard ? 'text-red-400' : isOverSoft ? 'text-amber-400' : 'text-muted-foreground'
          )}>
            {value.length}/{maxLen}
          </span>
        )}
      </div>
      {multiline ? (
        <Textarea
          id={id}
          value={value}
          onChange={(e) => onChange(e.target.value)}
          rows={rows}
          placeholder={placeholder}
          disabled={disabled}
          className={cn('text-sm resize-y', mono && 'font-mono text-xs')}
        />
      ) : (
        <Input
          id={id}
          value={value}
          onChange={(e) => onChange(e.target.value)}
          placeholder={placeholder}
          disabled={disabled}
          className="text-sm"
        />
      )}
    </div>
  )
}
```

**KRITIK: Label'da `font-medium` → `font-normal` — bu mevcut dosyada line 43 ve 161'deki hata.**

**4. useState'leri pkg verisinden initialize et** (page.pkg varsa):

```typescript
const pkg = page.pkg ?? null
const isLocked = pkg?.status === 'locked'

// Temel Bilgiler
const [pageType, setPageType] = useState(page.page_type ?? '')
const [strategicPurpose, setStrategicPurpose] = useState(pkg?.strategic_purpose ?? '')
const [searchIntent, setSearchIntent] = useState(pkg?.search_intent ?? '')

// Meta & URL
const [slug, setSlug] = useState(pkg?.slug ?? page.slug ?? '')
const [seoTitle, setSeoTitle] = useState(pkg?.seo_title ?? '')
const [metaDescription, setMetaDescription] = useState(pkg?.meta_description ?? '')
const [h1, setH1] = useState(pkg?.h1 ?? '')
const [canonicalUrl, setCanonicalUrl] = useState(pkg?.canonical_url ?? '')

// Heading, Schema, İçerik... (pkg?.heading_hierarchy gibi)
const [headingHierarchy, setHeadingHierarchy] = useState(jsonString(pkg?.heading_hierarchy))
const [schemaType, setSchemaType] = useState(pkg?.schema_type ?? '')
const [contentBlocks, setContentBlocks] = useState(jsonString(pkg?.content_blocks))
const [ctaBlocks, setCtaBlocks] = useState(jsonString(pkg?.cta_blocks))
const [imagePlan, setImagePlan] = useState(jsonString(pkg?.image_plan))
const [altTexts, setAltTexts] = useState(jsonString(pkg?.alt_texts))
const [secondaryKeywords, setSecondaryKeywords] = useState(jsonString(pkg?.secondary_keywords))
const [faq, setFaq] = useState(jsonString(pkg?.faq))
```

**5. Status action handler'ları ekle:**

```typescript
async function handleManualStart() {
  startTransition(async () => {
    const result = await createPagePackage(projectId, page.id, 'manual')
    if (result.success) {
      router.refresh()
    } else {
      setSaveStatus('error')
      setErrorMsg(result.error)
    }
  })
}

async function handleStatusChange(newStatus: 'draft' | 'approved' | 'locked') {
  if (!pkg?.id) return
  startTransition(async () => {
    const result = await updatePackageStatus(projectId, pkg.id, newStatus)
    if (result.success) {
      router.refresh()
    } else {
      setSaveStatus('error')
      setErrorMsg(result.error)
    }
  })
}
```

**6. handleSave'i güncelle** — artık pkg.id varsa updatePagePackage'a pageId geçiyor, package yoksa önce createPagePackage çağır:

```typescript
function handleSave() {
  setSaveStatus('idle')
  startTransition(async () => {
    // package yoksa önce oluştur
    if (!pkg) {
      const createResult = await createPagePackage(projectId, page.id, 'manual')
      if (!createResult.success) {
        setSaveStatus('error')
        setErrorMsg(createResult.error)
        return
      }
    }

    const result = await updatePagePackage(projectId, page.id, {
      page_type: pageType || undefined,
      strategic_purpose: strategicPurpose || undefined,
      search_intent: searchIntent || undefined,
      slug: slug || undefined,
      seo_title: seoTitle || undefined,
      meta_description: metaDescription || undefined,
      h1: h1 || undefined,
      canonical_url: canonicalUrl || undefined,
      heading_hierarchy: parseJsonField(headingHierarchy),
      schema_type: schemaType || undefined,
      content_blocks: parseJsonField(contentBlocks),
      cta_blocks: parseJsonField(ctaBlocks),
      image_plan: parseJsonField(imagePlan),
      alt_texts: parseJsonField(altTexts),
      secondary_keywords: parseJsonField(secondaryKeywords),
      faq: parseJsonField(faq),
    })

    if (result.success) {
      setSaveStatus('success')
      router.refresh()
    } else {
      setSaveStatus('error')
      setErrorMsg(result.error)
    }
  })
}
```

**7. JSX header'ını yeniden yaz:**

```typescript
{/* Header: sayfa adı + badge'ler + aksiyon butonları */}
<div className="space-y-2 mb-6">
  <div>
    <h2 className="text-base font-semibold">{page.title}</h2>
    {page.focus_keyword_text && (
      <p className="text-sm text-muted-foreground mt-0.5">
        Focus keyword: <span className="text-foreground">{page.focus_keyword_text}</span>
      </p>
    )}
  </div>

  <div className="flex items-center justify-between gap-4 flex-wrap">
    <div className="flex items-center gap-2">
      <QaBadge
        seoTitle={seoTitle}
        metaDescription={metaDescription}
        h1={h1}
        focusKeyword={page.focus_keyword_text ?? null}
      />
      <PackageStatusBadge status={pkg?.status ?? null} />
    </div>

    <div className="flex items-center gap-2 shrink-0">
      {/* No package state */}
      {!pkg && (
        <>
          <Button size="sm" onClick={handleAiGenerate} disabled={aiStatus === 'loading' || isPending}>
            {aiStatus === 'loading' ? 'Üretiliyor...' : 'AI ile Üret'}
          </Button>
          <Button variant="outline" size="sm" onClick={handleManualStart} disabled={isPending}>
            Manuel Başlat
          </Button>
        </>
      )}

      {/* draft */}
      {pkg?.status === 'draft' && (
        <>
          <Button size="sm" onClick={() => handleStatusChange('approved')} disabled={isPending}>Onayla</Button>
          <Button variant="outline" size="sm" onClick={handleAiGenerate} disabled={aiStatus === 'loading' || isPending}>
            {aiStatus === 'loading' ? 'Üretiliyor...' : 'AI ile Üret'}
          </Button>
        </>
      )}

      {/* approved */}
      {pkg?.status === 'approved' && (
        <>
          <Button size="sm" onClick={() => handleStatusChange('locked')} disabled={isPending}>Kilitle</Button>
          <Button variant="outline" size="sm" onClick={handleAiGenerate} disabled={aiStatus === 'loading' || isPending}>
            {aiStatus === 'loading' ? 'Üretiliyor...' : 'AI ile Üret'}
          </Button>
          <Button variant="ghost" size="sm" onClick={() => handleStatusChange('draft')} disabled={isPending}>Taslağa Al</Button>
        </>
      )}

      {/* locked — Dialog confirmation */}
      {pkg?.status === 'locked' && (
        <Dialog>
          <DialogTrigger render={<Button variant="outline" size="sm">Kilidini Aç</Button>} />
          <DialogPopup className="bg-card border border-border rounded-lg p-6 max-w-sm w-full shadow-lg">
            <DialogTitle className="text-base font-semibold mb-2">Paketi kilidden çıkar</DialogTitle>
            <DialogDescription className="text-sm text-muted-foreground mb-4">
              Bu sayfa paketi kilitli. Kilidini açarsan düzenlenebilir hale gelir ve onay durumuna döner.
            </DialogDescription>
            <div className="flex gap-2 justify-end">
              <DialogClose render={<Button variant="ghost" size="sm">İptal</Button>} />
              <DialogClose
                render={
                  <Button
                    variant="outline"
                    size="sm"
                    onClick={() => handleStatusChange('approved')}
                    disabled={isPending}
                  >
                    Kilidini Aç
                  </Button>
                }
              />
            </div>
          </DialogPopup>
        </Dialog>
      )}
    </div>
  </div>

  {/* AI feedback */}
  {aiStatus === 'done' && (
    <p className="text-xs text-emerald-500">Alanlar dolduruldu — kaydetmeyi unutma.</p>
  )}
  {aiStatus === 'error' && (
    <p className="text-xs text-red-400">{aiError || 'AI üretimi başarısız oldu. Lütfen tekrar dene.'}</p>
  )}
</div>

{/* Locked banner */}
{isLocked && (
  <LockedBanner
    onUnlockClick={() => handleStatusChange('approved')}
    isPending={isPending}
  />
)}
```

**8. Tüm Field bileşenlerine `disabled={isLocked}` prop ekle** (9 bölümdeki tüm Field'lar).

**9. Kaydet butonu'nu `isLocked` durumunda gizle:**

```typescript
{!isLocked && (
  <div className="flex items-center gap-4 pt-2 pb-8">
    <Button onClick={handleSave} disabled={isPending} className="min-w-24">
      {isPending ? 'Kaydediliyor...' : 'Kaydet'}
    </Button>
    {saveStatus === 'success' && <span className="text-sm text-emerald-600">Kaydedildi.</span>}
    {saveStatus === 'error' && <span className="text-sm text-red-500">{errorMsg || 'Kayıt sırasında hata oluştu. Lütfen tekrar dene.'}</span>}
  </div>
)}
```

**KRITIK son kontrol:**
- Dosyada `font-medium` yok — `font-normal` veya `font-semibold` kullan
- Label `className="text-xs font-normal text-muted-foreground uppercase tracking-wide"` (font-medium DEĞİL)
- DialogTrigger `render={}` kullanıyor (asChild YOK)
- focus_keyword_text gösterimi `<span className="text-foreground">` (font-medium YOK)
  </action>
  <verify>
    <automated>grep -c "font-medium" "src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "font-medium" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → çıktı yok (tamamen kaldırıldı)
    - `grep "updatePackageStatus" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → eşleşme bulundu
    - `grep "createPagePackage" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → eşleşme bulundu
    - `grep "QaBadge" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → eşleşme bulundu (import + kullanım)
    - `grep "LockedBanner" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → eşleşme bulundu
    - `grep "isLocked" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → eşleşme bulundu (disabled prop ve save gizle)
    - `grep "disabled={isLocked}" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → birden fazla eşleşme (her Field için)
    - `grep "asChild" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → çıktı yok (yasak)
    - `grep "render={" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → eşleşme bulundu (DialogTrigger + DialogClose)
    - `grep "Kilidini Aç" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → eşleşme bulundu (dialog copy)
    - `grep "Paketi kilidden çıkar" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → eşleşme bulundu (dialog title)
    - TypeScript derleme: `npx tsc --noEmit 2>&1 | grep "PagePackageEditor"` → çıktı yok (hata yok)
  </acceptance_criteria>
  <done>PagePackageEditor.tsx page_packages'a migrate edildi; status workflow, locked state, QaBadge ve font-medium temizliği tamamlandı.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → Server Action | createPagePackage / updatePackageStatus çağrıları — her ikisinde de server-side auth + ownership |
| Locked State | UI locked state = UX katmanı; gerçek koruma server action'da (updatePackageStatus auth check) |
| Dialog confirmation | Unlock dialog client-side; server action gerçek yetkiyi kontrol eder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Tampering | PagePackageEditor — locked bypass (disabled kaldır) | mitigate | UI disabled prop yalnızca UX; server-side updatePagePackage'da locked kontrolü eklenebilir Phase 10'da. Phase 9'da kabul: kullanıcı kendi paketi üzerinde çalışır, RLS zaten user_id kısıtlıyor |
| T-09-04-02 | Spoofing | page.tsx — selectedPageData pkg verisi | mitigate | pkg verisi SSR'da supabase.auth.getUser() + eq('user_id', user.id) ile çekilir — başka kullanıcı verisi gelemez |
| T-09-04-03 | Denial of Service | AI generate — locked kontrolü | mitigate | AI route 09-02 planında locked check (HTTP 403) eklendi — UI'daki isLocked check ile uyumlu |
| T-09-04-04 | Information Disclosure | PageData tipi — pkg alanı SSR'dan gelir | accept | pkg verisi yalnızca o projenin sahibine gösterilir; RLS + server-side ownership doğrulama yeterli |
</threat_model>

<verification>
```bash
# TypeScript derleme — tüm Phase 9 dosyaları
npx tsc --noEmit 2>&1 | grep -E "sayfa-paketi" || echo "NO_ERRORS"

# font-medium yasak kontrolü — tüm sayfa-paketi dosyaları
grep -rn "font-medium" src/app/(dashboard)/projeler/[id]/sayfa-paketi/ 2>/dev/null
# Beklenen: çıktı yok

# variant prop yasak kontrolü
grep -rn "variant=" src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx \
  src/app/(dashboard)/projeler/[id]/sayfa-paketi/PackageStatusBadge.tsx \
  src/app/(dashboard)/projeler/[id]/sayfa-paketi/LockedBanner.tsx 2>/dev/null
# Beklenen: çıktı yok

# asChild yasak kontrolü
grep -rn "asChild" src/app/(dashboard)/projeler/[id]/sayfa-paketi/ 2>/dev/null
# Beklenen: çıktı yok

# page_packages sorguları doğrulama
grep -rn "from('page_packages')" \
  src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx \
  src/app/(dashboard)/projeler/[id]/sayfa-paketi/actions.ts \
  src/app/api/ai/generate-page-package/route.ts 2>/dev/null
# Beklenen: 3+ eşleşme

# Dev server başlatma kontrolü
npm run build 2>&1 | grep -E "error|Error" | head -10
```
</verification>

<success_criteria>
- `page.tsx`: pages'ten sadece identity alanları çekiliyor, page_packages join yapılıyor, PackageStatusBadge her satırda gösteriliyor, font-medium yok
- `PagePackageEditor.tsx`: QaBadge ve PackageStatusBadge header'da, LockedBanner locked state'de, tüm Field'lar `disabled={isLocked}`, status workflow butonları çalışıyor, font-medium yok, asChild yok
- TypeScript derleme hatası yok
- `npm run build` başarılı
- Kullanıcı "AI ile Üret" → package draft olarak oluşur
- Kullanıcı "Onayla" → status approved
- Kullanıcı "Kilitle" → status locked, editor tamamen disabled
- Kullanıcı "Kilidini Aç" → Dialog gösterilir → onaylanırsa approved'a döner
</success_criteria>

<output>
After completion, create `.planning/phases/09-page-package-generator/09-04-SUMMARY.md`
</output>
