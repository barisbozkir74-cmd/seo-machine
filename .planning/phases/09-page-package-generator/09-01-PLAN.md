---
phase: 09-page-package-generator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260424000005_create_page_packages.sql
autonomous: true
requirements:
  - BLUE-04
  - PAGE-01

must_haves:
  truths:
    - "page_packages tablosu Supabase'de mevcuttur ve pages.id üzerinden foreign key bağlantısı vardır"
    - "page_id kolonu UNIQUE constraint taşır (Phase 9'da tek package per sayfa)"
    - "RLS politikaları: kullanıcı sadece kendi paketlerini görebilir/yazabilir"
    - "set_page_packages_updated_at trigger aktif ve updated_at otomatik güncellenir"
    - "idx_page_packages_project_id, idx_page_packages_page_id, idx_page_packages_user_id performans indexleri mevcuttur"
  artifacts:
    - path: "supabase/migrations/20260424000005_create_page_packages.sql"
      provides: "page_packages tablo tanımı, RLS politikaları, indexler, updated_at trigger"
      contains: "CREATE TABLE IF NOT EXISTS public.page_packages"
  key_links:
    - from: "supabase/migrations/20260424000005_create_page_packages.sql"
      to: "Supabase live DB"
      via: "supabase db push"
      pattern: "UNIQUE\\(page_id\\)"
---

<objective>
`page_packages` tablosunu Supabase'e deploy eder. Bu Wave 1 BLOCKING planıdır — Wave 2 ve Wave 3 planları bu tablonun varlığına bağımlıdır.

Purpose: Tüm Phase 9 çalışmasının veri katmanı temelidir. Tablo push edilmeden actions çalışamaz, UI kaydedemez.
Output: Supabase live DB'de `page_packages` tablosu, 4 RLS politikası, 3 performans indexi, updated_at trigger.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-page-package-generator/09-CONTEXT.md
@.planning/phases/09-page-package-generator/09-PATTERNS.md

<interfaces>
<!-- Mevcut migration pattern — supabase/migrations/20260423000003_product_layers_schema.sql baz alınır -->
<!-- updated_at trigger fonksiyonu mevcut DB'de tanımlı: public.set_updated_at() -->
<!-- pages tablosu mevcut: public.pages(id UUID PK) -->
<!-- projects tablosu mevcut: public.projects(id UUID PK) -->
<!-- auth.users tablosu Supabase'de mevcut -->

<!-- D-01'den page_packages şeması (tam sütun listesi): -->
<!--
  id uuid PK, page_id uuid → pages(id) CASCADE, project_id uuid → projects(id) CASCADE,
  user_id uuid → auth.users(id) CASCADE, status text DEFAULT 'draft',
  seo_title text, meta_description text, h1 text, slug text,
  search_intent text, strategic_purpose text,
  secondary_keywords jsonb DEFAULT '[]', heading_hierarchy jsonb DEFAULT '[]',
  content_blocks jsonb DEFAULT '[]', cta_blocks jsonb DEFAULT '[]',
  image_plan jsonb DEFAULT '[]', alt_texts jsonb DEFAULT '[]',
  schema_type text, canonical_url text, faq jsonb DEFAULT '[]',
  qa_scores jsonb DEFAULT '{}', generated_by text NOT NULL DEFAULT 'manual',
  ai_model text, created_at timestamptz DEFAULT now() NOT NULL,
  updated_at timestamptz DEFAULT now() NOT NULL,
  approved_at timestamptz, locked_at timestamptz,
  UNIQUE(page_id)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration SQL dosyasını yaz</name>
  <files>supabase/migrations/20260424000005_create_page_packages.sql</files>
  <read_first>
    - supabase/migrations/20260424000004_competitors_analysis_column.sql (son migration — timestamp sıralaması için)
    - supabase/migrations/20260423000003_product_layers_schema.sql (tablo oluşturma + trigger pattern)
    - supabase/migrations/20260423000004_product_layers_rls.sql (RLS politika pattern)
    - .planning/phases/09-page-package-generator/09-CONTEXT.md (D-01 — tam şema kararı)
    - .planning/phases/09-page-package-generator/09-PATTERNS.md (SQL pattern blokları)
  </read_first>
  <action>
Aşağıdaki tam içerikle migration dosyasını oluştur. Dosya adı: `supabase/migrations/20260424000005_create_page_packages.sql`

```sql
-- =============================================================================
-- SEO Machine — Page Packages Table
-- Phase 9: page_packages tablosu (page_id UNIQUE — Phase 9'da versioning yok)
-- =============================================================================

CREATE TABLE IF NOT EXISTS public.page_packages (
  id                UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  page_id           UUID REFERENCES public.pages(id) ON DELETE CASCADE NOT NULL,
  project_id        UUID REFERENCES public.projects(id) ON DELETE CASCADE NOT NULL,
  user_id           UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  status            TEXT NOT NULL DEFAULT 'draft',
  -- SEO Fields
  seo_title         TEXT,
  meta_description  TEXT,
  h1                TEXT,
  slug              TEXT,
  search_intent     TEXT,
  strategic_purpose TEXT,
  secondary_keywords JSONB DEFAULT '[]',
  heading_hierarchy  JSONB DEFAULT '[]',
  content_blocks     JSONB DEFAULT '[]',
  cta_blocks         JSONB DEFAULT '[]',
  image_plan         JSONB DEFAULT '[]',
  alt_texts          JSONB DEFAULT '[]',
  schema_type        TEXT,
  canonical_url      TEXT,
  faq                JSONB DEFAULT '[]',
  -- QA (client-side hesaplanan skorlar)
  qa_scores          JSONB DEFAULT '{}',
  -- Traceability
  generated_by       TEXT NOT NULL DEFAULT 'manual',
  ai_model           TEXT,
  -- Timestamps
  created_at         TIMESTAMPTZ DEFAULT now() NOT NULL,
  updated_at         TIMESTAMPTZ DEFAULT now() NOT NULL,
  approved_at        TIMESTAMPTZ,
  locked_at          TIMESTAMPTZ,
  -- Phase 9: tek package per sayfa (versioning Phase 12'de gelir)
  UNIQUE(page_id)
);

-- Performans indexleri
CREATE INDEX IF NOT EXISTS idx_page_packages_project_id ON public.page_packages(project_id);
CREATE INDEX IF NOT EXISTS idx_page_packages_page_id    ON public.page_packages(page_id);
CREATE INDEX IF NOT EXISTS idx_page_packages_user_id    ON public.page_packages(user_id);

-- updated_at trigger (set_updated_at fonksiyonu mevcut tablolarda tanımlı)
CREATE TRIGGER set_page_packages_updated_at
  BEFORE UPDATE ON public.page_packages
  FOR EACH ROW EXECUTE FUNCTION public.set_updated_at();

-- =============================================================================
-- RLS
-- =============================================================================

ALTER TABLE public.page_packages ENABLE ROW LEVEL SECURITY;

CREATE POLICY "page_packages_select_own" ON public.page_packages
  FOR SELECT USING (auth.uid() = user_id);

CREATE POLICY "page_packages_insert_own" ON public.page_packages
  FOR INSERT WITH CHECK (auth.uid() = user_id);

CREATE POLICY "page_packages_update_own" ON public.page_packages
  FOR UPDATE USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id);

CREATE POLICY "page_packages_delete_own" ON public.page_packages
  FOR DELETE USING (auth.uid() = user_id);
```

Dosyayı tam olarak bu içerikle oluştur — hiçbir değişiklik yapma.
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS public.page_packages" supabase/migrations/20260424000005_create_page_packages.sql</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260424000005_create_page_packages.sql` dosyası mevcut
    - Dosyada `UNIQUE(page_id)` satırı var: `grep -c "UNIQUE(page_id)" supabase/migrations/20260424000005_create_page_packages.sql` → 1
    - Dosyada 4 RLS politikası var: `grep -c "CREATE POLICY" supabase/migrations/20260424000005_create_page_packages.sql` → 4
    - Dosyada 3 performans indexi var: `grep -c "CREATE INDEX IF NOT EXISTS" supabase/migrations/20260424000005_create_page_packages.sql` → 3
    - Dosyada `set_page_packages_updated_at` trigger tanımı var
    - Dosyada `ALTER TABLE public.page_packages ENABLE ROW LEVEL SECURITY` satırı var
  </acceptance_criteria>
  <done>Migration SQL dosyası yazıldı, tüm sütunlar, indexler, trigger ve RLS politikaları dahil edildi.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] supabase db push — page_packages tablosunu deploy et</name>
  <files></files>
  <read_first>
    - supabase/migrations/20260424000005_create_page_packages.sql (az önce oluşturulan migration — push öncesi doğrula)
  </read_first>
  <action>
Migration dosyasını Supabase remote DB'ye push et.

Komut:
```bash
supabase db push
```

Eğer interactive prompt gelirse (non-TTY ortamı için):
```bash
SUPABASE_ACCESS_TOKEN=$SUPABASE_ACCESS_TOKEN supabase db push --yes
```

Push başarılıysa: komut 0 exit kodu ile biter ve "Applying migration 20260424000005_create_page_packages" çıktısı görünür.

Push başarısız olursa:
- `set_updated_at` fonksiyon bulunamadı hatası: Bu trigger fonksiyonu mevcut migration'larda `product_layers_schema.sql`'da tanımlı olmalı. Supabase dashboard'dan kontrol et.
- Bağlantı hatası: `supabase status` ile bağlantıyı kontrol et.
- Herhangi bir hata mesajını olduğu gibi kaydet — manuel müdahale gerekebilir.

Bu adım BLOCKING'dir: başarısız olursa Wave 2 ve Wave 3 planları çalışamaz.
  </action>
  <verify>
    <automated>supabase db push --dry-run 2>&1 | grep -E "(No migrations|20260424000005|error)" || echo "CHECK_MANUALLY"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` komutu 0 exit kodu ile tamamlanır
    - Çıktıda "20260424000005_create_page_packages" migration adı görünür
    - Supabase dashboard'da `page_packages` tablosu Table Editor'de gözüküyor
    - Tabloda `page_id`, `status`, `seo_title`, `meta_description`, `h1`, `slug`, `generated_by` sütunları mevcut
  </acceptance_criteria>
  <done>page_packages tablosu Supabase remote DB'de canlı, RLS aktif, indexler oluşturuldu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → Supabase DB | SQL dosyası doğrudan DB'ye uygulanır; hatalı SQL veri bütünlüğünü bozabilir |
| RLS politikaları → kullanıcı izolasyonu | Yanlış RLS, kullanıcıların birbirinin paketlerine erişmesine izin verir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Spoofing | RLS policy — auth.uid() check | mitigate | UPDATE policy hem USING hem WITH CHECK kullanıyor — user_id tampering önlenir |
| T-09-01-02 | Tampering | Migration SQL | accept | Migration dosyası git'te versiyonlanır; production'da supabase db push ile uygulanır |
| T-09-01-03 | Information Disclosure | Başka kullanıcının page_packages | mitigate | SELECT USING (auth.uid() = user_id) — yalnızca kendi kayıtları görünür |
| T-09-01-04 | Elevation of Privilege | user_id kolonu INSERT | mitigate | INSERT WITH CHECK (auth.uid() = user_id) — client user_id'yi sahte veremez |
</threat_model>

<verification>
Migration başarılı sayılır:
1. `supabase db push` 0 exit kodu döndürdü
2. `supabase/migrations/20260424000005_create_page_packages.sql` git'e staged edildi
3. Tabloda UNIQUE(page_id) constraint aktif (aynı page_id için ikinci INSERT hata verir)
4. RLS aktif: farklı user_id ile giriş yapılmış bir oturumda SELECT boş döner
</verification>

<success_criteria>
- Migration dosyası doğru timestamp ile oluşturuldu (20260424000005)
- `supabase db push` başarılı — migration applied
- `page_packages` tablosu Supabase'de canlı
- RLS enabled — 4 politika aktif
- UNIQUE(page_id) constraint var
- Wave 2 planları çalışmaya hazır
</success_criteria>

<output>
After completion, create `.planning/phases/09-page-package-generator/09-01-SUMMARY.md`
</output>
