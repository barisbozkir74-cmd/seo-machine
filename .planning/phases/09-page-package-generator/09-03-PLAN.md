---
phase: 09-page-package-generator
plan: 03
type: execute
wave: 3
depends_on:
  - "09-02"
files_modified:
  - src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx
  - src/app/(dashboard)/projeler/[id]/sayfa-paketi/PackageStatusBadge.tsx
  - src/app/(dashboard)/projeler/[id]/sayfa-paketi/LockedBanner.tsx
autonomous: true
requirements:
  - BLUE-04
  - PAGE-01

must_haves:
  truths:
    - "QaBadge gerçek zamanlı 4 QA kuralını değerlendirir ve 3 durumdan birini gösterir: QA Geçti / N Uyarı / Hata"
    - "PackageStatusBadge null status'ta 'Paket Yok' gösterir, mevcut status için doğru Türkçe etiketi ve rengi gösterir"
    - "LockedBanner amber arkaplan ile görünür, 'Kilidini Aç' tıklanabilir link içerir"
    - "Hiçbir bileşende font-medium CSS sınıfı yok"
    - "Hiçbir Badge variant prop kullanmıyor — yalnızca className ile renk"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx"
      provides: "Client-side QA validator badge — 4 kural, 3 görsel durum"
      exports: ["QaBadge", "QaBadgeProps"]
    - path: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/PackageStatusBadge.tsx"
      provides: "draft/approved/locked/null badge bileşeni"
      exports: ["PackageStatusBadge"]
    - path: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/LockedBanner.tsx"
      provides: "Locked state uyarı banner'ı — amber renk, 'Kilidini Aç' aksiyonu"
      exports: ["LockedBanner", "LockedBannerProps"]
  key_links:
    - from: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx"
      to: "QaBadge.tsx"
      via: "import { QaBadge } from './QaBadge'"
      pattern: "QaBadge"
    - from: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx"
      to: "PackageStatusBadge.tsx"
      via: "import { PackageStatusBadge } from './PackageStatusBadge'"
      pattern: "PackageStatusBadge"
---

<objective>
Phase 9 için gerekli üç yeni Client Component'i oluşturur: `QaBadge` (gerçek zamanlı QA validator), `PackageStatusBadge` (draft/approved/locked/null durumu), `LockedBanner` (kilitli paket uyarısı).

Purpose: Wave 3 planının ikinci parçası olan `PagePackageEditor.tsx` ve `page.tsx` migrasyonu bu bileşenlere bağımlıdır. Bileşenleri önce oluşturmak Wave 3'ü paralel çalıştırır.
Output: 3 yeni bileşen dosyası — toplamda ~110 satır TypeScript/TSX.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-page-package-generator/09-UI-SPEC.md
@.planning/phases/09-page-package-generator/09-PATTERNS.md
@.planning/phases/09-page-package-generator/09-CONTEXT.md

<interfaces>
<!-- cn() helper — tüm bileşenlerde kullanılır -->
```typescript
import { cn } from '@/lib/utils'
```

<!-- QaBadge props ve QA kuralları (UI-SPEC D-04'ten): -->
```typescript
// QA-01: seo_title.length > 60 → warning, > 70 → error
// QA-02: meta_description.length > 155 → warning, > 170 → error
// QA-03: h1.trim().length === 0 → error
// QA-04: focusKeyword && !seo_title.toLowerCase().includes(focusKeyword.toLowerCase()) → warning

type QaBadgeProps = {
  seoTitle: string
  metaDescription: string
  h1: string
  focusKeyword: string | null
}
```

<!-- PackageStatusBadge renk sınıfları (UI-SPEC Color tablosundan): -->
```typescript
// null: 'bg-secondary text-muted-foreground opacity-60' → "Paket Yok"
// draft: 'bg-secondary text-muted-foreground' → "Taslak"
// approved: 'bg-blue-900/40 text-blue-400' → "Onaylandı"
// locked: 'bg-amber-900/40 text-amber-400' → "Kilitli"
```

<!-- LockedBanner (UI-SPEC Locked State + Copywriting'den): -->
```typescript
// Banner arka planı: bg-amber-900/20 border border-amber-700/40
// Metin: text-xs text-amber-400 → "Bu paket kilitli — düzenlemek için kilidini aç."
// Buton/link: text-xs text-amber-400 hover:text-amber-300 underline
type LockedBannerProps = {
  onUnlockClick: () => void
  isPending?: boolean
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QaBadge.tsx ve PackageStatusBadge.tsx oluştur</name>
  <files>
    src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx
    src/app/(dashboard)/projeler/[id]/sayfa-paketi/PackageStatusBadge.tsx
  </files>
  <read_first>
    - .planning/phases/09-page-package-generator/09-PATTERNS.md (QaBadge tam implementasyon + PackageStatusBadge tam implementasyon)
    - .planning/phases/09-page-package-generator/09-UI-SPEC.md (QA Validator kuralları, Semantic Colors tablosu, Mandatory Constraints)
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx (mevcut StatusBadge inline pattern — referans)
    - src/lib/utils.ts (cn() import yolu doğrulama için)
  </read_first>
  <action>
İki dosyayı aşağıdaki tam içerikle oluştur:

**Dosya 1: `src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx`**

```typescript
'use client'

import { cn } from '@/lib/utils'

type QaRule = {
  id: string
  severity: 'warning' | 'error'
}

export type QaBadgeProps = {
  seoTitle: string
  metaDescription: string
  h1: string
  focusKeyword: string | null
}

function computeQaRules(props: QaBadgeProps): QaRule[] {
  const rules: QaRule[] = []

  // QA-01: SEO Title uzunluğu
  if (props.seoTitle.length > 70) {
    rules.push({ id: 'QA-01', severity: 'error' })
  } else if (props.seoTitle.length > 60) {
    rules.push({ id: 'QA-01', severity: 'warning' })
  }

  // QA-02: Meta Description uzunluğu
  if (props.metaDescription.length > 170) {
    rules.push({ id: 'QA-02', severity: 'error' })
  } else if (props.metaDescription.length > 155) {
    rules.push({ id: 'QA-02', severity: 'warning' })
  }

  // QA-03: H1 dolu mu
  if (!props.h1.trim()) {
    rules.push({ id: 'QA-03', severity: 'error' })
  }

  // QA-04: Focus keyword SEO title'da var mı
  if (
    props.focusKeyword &&
    !props.seoTitle.toLowerCase().includes(props.focusKeyword.toLowerCase())
  ) {
    rules.push({ id: 'QA-04', severity: 'warning' })
  }

  return rules
}

export function QaBadge(props: QaBadgeProps) {
  const rules = computeQaRules(props)
  const errors = rules.filter((r) => r.severity === 'error')
  const warnings = rules.filter((r) => r.severity === 'warning')

  if (errors.length > 0) {
    return (
      <span className={cn('inline-flex items-center gap-1 px-1.5 py-0.5 rounded text-[10px] text-red-400')}>
        ✕ Hata
      </span>
    )
  }

  if (warnings.length > 0) {
    return (
      <span className={cn('inline-flex items-center gap-1 px-1.5 py-0.5 rounded text-[10px] text-amber-400')}>
        ⚠ {warnings.length} Uyarı
      </span>
    )
  }

  return (
    <span className={cn('inline-flex items-center gap-1 px-1.5 py-0.5 rounded text-[10px] text-emerald-400')}>
      ✓ QA Geçti
    </span>
  )
}
```

**Dosya 2: `src/app/(dashboard)/projeler/[id]/sayfa-paketi/PackageStatusBadge.tsx`**

```typescript
'use client'

import { cn } from '@/lib/utils'

const PACKAGE_STATUS_LABELS: Record<string, { label: string; className: string }> = {
  draft:    { label: 'Taslak',    className: 'bg-secondary text-muted-foreground' },
  approved: { label: 'Onaylandı', className: 'bg-blue-900/40 text-blue-400' },
  locked:   { label: 'Kilitli',   className: 'bg-amber-900/40 text-amber-400' },
}

export function PackageStatusBadge({ status }: { status: string | null }) {
  if (!status) {
    return (
      <span
        className={cn(
          'inline-flex px-1.5 py-0.5 rounded text-[10px] bg-secondary text-muted-foreground opacity-60'
        )}
      >
        Paket Yok
      </span>
    )
  }

  const cfg = PACKAGE_STATUS_LABELS[status] ?? {
    label: status,
    className: 'bg-secondary text-muted-foreground',
  }

  return (
    <span className={cn('inline-flex px-1.5 py-0.5 rounded text-[10px]', cfg.className)}>
      {cfg.label}
    </span>
  )
}
```

**Kontrol et:**
- `font-medium` yok — yalnızca `font-normal` veya `font-semibold`
- `variant` prop yok — sadece `className` ile renk
- Her iki dosyada `'use client'` direktifi var
  </action>
  <verify>
    <automated>grep -rn "font-medium" src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx src/app/(dashboard)/projeler/[id]/sayfa-paketi/PackageStatusBadge.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'use client'" src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx` → 1
    - `grep -c "'use client'" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PackageStatusBadge.tsx` → 1
    - `grep "font-medium" src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx` → çıktı yok (yasak sınıf kullanılmadı)
    - `grep "font-medium" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PackageStatusBadge.tsx` → çıktı yok
    - `grep "variant=" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PackageStatusBadge.tsx` → çıktı yok (variant prop yasak)
    - `grep "QA-01\|QA-02\|QA-03\|QA-04" src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx` → 4 eşleşme
    - `grep "Paket Yok" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PackageStatusBadge.tsx` → eşleşme bulundu
    - `grep "Onaylandı" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PackageStatusBadge.tsx` → eşleşme bulundu
    - `grep "text-emerald-400\|text-amber-400\|text-red-400" src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx` → 3 eşleşme
  </acceptance_criteria>
  <done>QaBadge 4 QA kuralını değerlendirir ve 3 durum gösterir. PackageStatusBadge null/draft/approved/locked için doğru Türkçe etiket ve renk gösterir.</done>
</task>

<task type="auto">
  <name>Task 2: LockedBanner.tsx oluştur</name>
  <files>src/app/(dashboard)/projeler/[id]/sayfa-paketi/LockedBanner.tsx</files>
  <read_first>
    - .planning/phases/09-page-package-generator/09-PATTERNS.md (LockedBanner tam implementasyon bloğu)
    - .planning/phases/09-page-package-generator/09-UI-SPEC.md (Locked State renkleri: bg-amber-900/20 border-amber-700/40 text-amber-400, Copywriting: "Bu paket kilitli...")
    - .planning/phases/09-page-package-generator/09-CONTEXT.md (D-03: locked → approved unlock flow)
  </read_first>
  <action>
Aşağıdaki tam içerikle `LockedBanner.tsx` dosyasını oluştur:

```typescript
'use client'

export type LockedBannerProps = {
  onUnlockClick: () => void
  isPending?: boolean
}

export function LockedBanner({ onUnlockClick, isPending }: LockedBannerProps) {
  return (
    <div className="flex items-center justify-between py-3 px-4 rounded-md bg-amber-900/20 border border-amber-700/40">
      <p className="text-xs text-amber-400">
        Bu paket kilitli — düzenlemek için kilidini aç.
      </p>
      <button
        onClick={onUnlockClick}
        disabled={isPending}
        className="text-xs text-amber-400 hover:text-amber-300 underline disabled:opacity-50"
      >
        Kilidini Aç
      </button>
    </div>
  )
}
```

**Dikkat:** Bu bileşen yalnızca unlock aksiyonunu tetikler. Confirmation dialog `PagePackageEditor.tsx` içinde tutulur (Dialog bileşeni orada import edilecek). `LockedBanner` sadece göster + tetikle sorumluluğunu taşır.

**Kontrol et:**
- `font-medium` yok
- `variant` prop yok
- `'use client'` direktifi var
- `bg-amber-900/20 border border-amber-700/40` amber renk sınıfları doğru
  </action>
  <verify>
    <automated>grep -c "Bu paket kilitli" "src/app/(dashboard)/projeler/[id]/sayfa-paketi/LockedBanner.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'use client'" src/app/(dashboard)/projeler/[id]/sayfa-paketi/LockedBanner.tsx` → 1
    - `grep "bg-amber-900/20" src/app/(dashboard)/projeler/[id]/sayfa-paketi/LockedBanner.tsx` → eşleşme bulundu
    - `grep "border-amber-700/40" src/app/(dashboard)/projeler/[id]/sayfa-paketi/LockedBanner.tsx` → eşleşme bulundu
    - `grep "font-medium" src/app/(dashboard)/projeler/[id]/sayfa-paketi/LockedBanner.tsx` → çıktı yok (yasak)
    - `grep "onUnlockClick" src/app/(dashboard)/projeler/[id]/sayfa-paketi/LockedBanner.tsx` → eşleşme bulundu (prop ve handler)
    - `grep "isPending" src/app/(dashboard)/projeler/[id]/sayfa-paketi/LockedBanner.tsx` → eşleşme bulundu (disabled state)
    - `grep "Kilidini Aç" src/app/(dashboard)/projeler/[id]/sayfa-paketi/LockedBanner.tsx` → eşleşme bulundu
  </acceptance_criteria>
  <done>LockedBanner amber renk paleti ile oluşturuldu, onUnlockClick prop'u alır, isPending disabled state destekler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side QA compute | QaBadge tamamen client-side çalışır — sunucuya veri göndermez, sadece form state'i değerlendirir |
| LockedBanner → unlock trigger | Unlock aksiyonu yalnızca UI tetikler; server action'da auth + ownership kontrolü yapılır (09-02 planında) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering | QaBadge — client-side QA bypass | accept | QA sadece UX feedback'tir; server-side herhangi bir zorlama yapmaz. Severity: low — kullanıcı kendi içeriğini düzenler |
| T-09-03-02 | Spoofing | LockedBanner unlock trigger | mitigate | LockedBanner yalnızca UI aksiyonu tetikler; gerçek unlock `updatePackageStatus` server action'da auth + RLS korumasıyla gerçekleşir |
| T-09-03-03 | Information Disclosure | PackageStatusBadge | accept | Yalnızca mevcut status gösterir; status zaten sayfa SSR'sında kullanıcıya açık |
</threat_model>

<verification>
```bash
# TypeScript derleme — 3 yeni dosya için hata yok
npx tsc --noEmit 2>&1 | grep -E "QaBadge|PackageStatusBadge|LockedBanner" || echo "NO_ERRORS"

# font-medium yasak kontrolü (tüm 3 yeni bileşen)
grep -rn "font-medium" \
  src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx \
  src/app/(dashboard)/projeler/[id]/sayfa-paketi/PackageStatusBadge.tsx \
  src/app/(dashboard)/projeler/[id]/sayfa-paketi/LockedBanner.tsx \
  2>/dev/null
# Beklenen: çıktı yok

# variant prop yasak kontrolü
grep -rn "variant=" \
  src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx \
  src/app/(dashboard)/projeler/[id]/sayfa-paketi/PackageStatusBadge.tsx \
  src/app/(dashboard)/projeler/[id]/sayfa-paketi/LockedBanner.tsx \
  2>/dev/null
# Beklenen: çıktı yok
```
</verification>

<success_criteria>
- 3 dosya oluşturuldu: QaBadge.tsx, PackageStatusBadge.tsx, LockedBanner.tsx
- QaBadge tüm 4 QA kuralını (QA-01..QA-04) uygular
- PackageStatusBadge null/draft/approved/locked durumları için Türkçe etiket ve doğru renk kullanır
- LockedBanner amber arkaplan ve "Kilidini Aç" aksiyonu içerir
- Hiçbir bileşende `font-medium` veya `variant` prop yok
- TypeScript hata yok
</success_criteria>

<output>
After completion, create `.planning/phases/09-page-package-generator/09-03-SUMMARY.md`
</output>
