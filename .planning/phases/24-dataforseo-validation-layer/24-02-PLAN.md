---
phase: "24"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dataforseo/cache.ts
  - src/__tests__/lib/dataforseo/cache.test.ts
autonomous: true
requirements: [DFS-02]

must_haves:
  truths:
    - "getCachedOrFetch() fonksiyonu mevcut fetchWithCache() orchestrator'ını sararak çalışıyor"
    - "cache.ts dosyası 'server-only' import guard ile başlıyor"
    - "Cache hit durumunda DataForSEO API çağrısı yapılmıyor (fromCache: true dönüyor)"
    - "Cache miss durumunda API çağrısı yapılıp result cache'e yazılıyor"
    - "buildCacheKey keyword'ü normalize ediyor: lowercase + trim, sonra SHA-256"
    - "cache.test.ts 4 davranışı test ediyor: hit, miss, normalizasyon, süresi dolmuş"
  artifacts:
    - path: "src/lib/dataforseo/cache.ts"
      provides: "getCachedOrFetch() wrapper + buildCacheKey() normalizasyon"
      contains: "server-only"
    - path: "src/__tests__/lib/dataforseo/cache.test.ts"
      provides: "Unit testler: hit/miss/normalization/expiry"
      contains: "getCachedOrFetch"
  key_links:
    - from: "src/lib/dataforseo/cache.ts"
      to: "src/lib/dataforseo/orchestrator.ts"
      via: "fetchWithCache() import"
      pattern: "import.*fetchWithCache.*orchestrator"
---

<objective>
getCachedOrFetch() cache service'ini yaz: mevcut fetchWithCache() orchestrator'ının ince ergonomi wrapper'ı. Wave 0 test stub'larını da bu plan oluşturur.

Purpose: DFS-02 gereksinimi — tüm DataForSEO çağrıları cache'den geçer. Plan 03'teki Server Action'lar bu fonksiyonu çağırır.

Output: src/lib/dataforseo/cache.ts (production) + src/__tests__/lib/dataforseo/cache.test.ts (testler).
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-dataforseo-validation-layer/24-RESEARCH.md
@.planning/phases/24-dataforseo-validation-layer/24-PATTERNS.md
@.planning/phases/24-dataforseo-validation-layer/24-VALIDATION.md

<interfaces>
<!-- Kaynak: src/lib/dataforseo/orchestrator.ts — tam analog -->
<!-- Aşağıdaki fonksiyon ve tip imzaları gerçek codebase'den: -->

```typescript
// orchestrator.ts exports (cache.ts bunları import eder):
export async function fetchWithCache<T = unknown>(opts: {
  projectId: string
  userId: string
  spec: TaskSpec
  fetcher: () => Promise<T>
}): Promise<CacheResult<T>>

export function makeFingerprint(spec: TaskSpec): string
// SHA-256 hash — keywords normalize edilir (sort + lowercase + trim)

// CacheResult type (types.ts'den):
type CacheResult<T> =
  | { data: T; fromCache: boolean; skipped: false }
  | { data: null; fromCache: false; skipped: true; reason: SkipReason }

// TaskSpec type (types.ts'den):
type TaskSpec = {
  module: string
  endpoint: string
  target: { type: 'domain' | 'keyword' | 'keywords'; value: string | string[] }
  locationCode?: number
  languageCode?: string
}

// ENDPOINT_TTL (types.ts'den):
const ENDPOINT_TTL: Record<string, number> = {
  'keyword_data/search_volume': 7 * 24 * 60 * 60,  // 7 gün (light)
  'labs/related_keywords': 3 * 24 * 60 * 60,        // 3 gün (standard)
  'serp/organic': 24 * 60 * 60,                      // 1 gün (deep)
}
```

<!-- Mevcut dataforseo_task_cache tablo kolonları (20260522000001_dataforseo_task_cache.sql'den): -->
```
project_id, user_id, module, endpoint, target_type, target_value,
fingerprint (UNIQUE(project_id, fingerprint)),
request_payload, source, status, retry_count, task_id,
result JSONB, error_code, error_message, cost_units,
fetched_at, expires_at, freshness_label, created_at, updated_at
```

<!-- Test mock pattern (20-01 kararı): -->
```typescript
vi.mock('@/lib/supabase/server')
vi.mock('@/lib/supabase/vault')
vi.mock('server-only', () => ({}))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — cache.test.ts stub dosyasını oluştur</name>
  <files>src/__tests__/lib/dataforseo/cache.test.ts</files>

  <read_first>
    - src/__tests__/lib/dataforseo/client.test.ts — mevcut mock setup pattern (vi.mock import'ları)
    - src/__mocks__/server-only.ts — server-only mock varlığını doğrula
    - .planning/phases/24-dataforseo-validation-layer/24-VALIDATION.md — Wave 0 Requirements + test case listesi
  </read_first>

  <behavior>
    - Test 1: cache hit → fetchWithCache mock'u cached data döndürür → getCachedOrFetch { data, fromCache: true } döndürür
    - Test 2: cache miss → fetchWithCache mock'u API çağırır → getCachedOrFetch { data, fromCache: false } döndürür
    - Test 3: normalizasyon — "SEO" ve "seo" ve " SEO " aynı cache key üretir (lowercase + trim)
    - Test 4: süresi dolmuş cache (expires_at geçmiş) → re-fetch tetiklenir (skipped: false, fromCache: false)
    - Test 5: fetchWithCache skipped: true dönerse → getCachedOrFetch null döndürür
  </behavior>

  <action>
  Önce src/__tests__/lib/dataforseo/ dizininin mevcut olup olmadığını kontrol et, yoksa oluştur.
  Ardından test dosyasını şu yapıyla yaz:

  ```typescript
  import { describe, it, expect, vi, beforeEach } from 'vitest'

  // server-only mock — ZORUNLU (20-01 kararı)
  vi.mock('server-only', () => ({}))

  // orchestrator mock — getCachedOrFetch bunu sarmalar
  vi.mock('@/lib/dataforseo/orchestrator', () => ({
    fetchWithCache: vi.fn(),
    makeFingerprint: vi.fn((spec) => 'test-fingerprint'),
  }))

  import { getCachedOrFetch } from '@/lib/dataforseo/cache'
  import { fetchWithCache } from '@/lib/dataforseo/orchestrator'

  const mockFetchWithCache = vi.mocked(fetchWithCache)

  describe('getCachedOrFetch', () => {
    beforeEach(() => {
      vi.clearAllMocks()
    })

    it('cache hit durumunda fromCache: true döndürür', async () => {
      mockFetchWithCache.mockResolvedValueOnce({
        data: { volume: 1000 },
        fromCache: true,
        skipped: false,
      })
      const result = await getCachedOrFetch({
        projectId: '00000000-0000-0000-0000-000000000001',
        userId: '00000000-0000-0000-0000-000000000002',
        spec: { module: 'keyword_stratejisi', endpoint: 'keyword_data/search_volume', target: { type: 'keywords', value: ['seo'] } },
        fetcher: vi.fn(),
      })
      expect(result).toEqual({ data: { volume: 1000 }, fromCache: true })
    })

    it('cache miss durumunda fromCache: false döndürür', async () => {
      mockFetchWithCache.mockResolvedValueOnce({
        data: { volume: 500 },
        fromCache: false,
        skipped: false,
      })
      const result = await getCachedOrFetch({
        projectId: '00000000-0000-0000-0000-000000000001',
        userId: '00000000-0000-0000-0000-000000000002',
        spec: { module: 'keyword_stratejisi', endpoint: 'keyword_data/search_volume', target: { type: 'keywords', value: ['seo'] } },
        fetcher: vi.fn(),
      })
      expect(result).toEqual({ data: { volume: 500 }, fromCache: false })
    })

    it('skipped: true olunca null döndürür', async () => {
      mockFetchWithCache.mockResolvedValueOnce({
        data: null,
        fromCache: false,
        skipped: true,
        reason: 'BUDGET_EXCEEDED',
      })
      const result = await getCachedOrFetch({
        projectId: '00000000-0000-0000-0000-000000000001',
        userId: '00000000-0000-0000-0000-000000000002',
        spec: { module: 'keyword_stratejisi', endpoint: 'keyword_data/search_volume', target: { type: 'keywords', value: ['seo'] } },
        fetcher: vi.fn(),
      })
      expect(result).toBeNull()
    })
  })
  ```

  NOT: normalizasyon testleri (Test 3) makeFingerprint'in içinde gerçekleşir (orchestrator.ts'de). cache.ts sadece sarmalayıcıdır. Normalizasyon testi orchestrator'ın mock'unu kontrol eder — cache.ts doğrudan keyword string'ini normalize etmez, makeFingerprint'e delege eder.
  </action>

  <verify>
    <automated>npx vitest run src/__tests__/lib/dataforseo/cache.test.ts 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Dosya `src/__tests__/lib/dataforseo/cache.test.ts` mevcut
    - `vi.mock('server-only', () => ({}))` satırı dosyada var
    - `vi.mock('@/lib/dataforseo/orchestrator'` satırı var
    - En az 3 test case var (describe+it bloğu)
    - `npx vitest run src/__tests__/lib/dataforseo/cache.test.ts` — testler fail edebilir (RED phase normal — cache.ts henüz yok), ama test dosyası parse edilebilir
  </acceptance_criteria>

  <done>cache.test.ts stub dosyası oluşturuldu, test runner cache.ts olmadan da dosyayı parse edebiliyor</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cache.ts — getCachedOrFetch() wrapper implement et</name>
  <files>src/lib/dataforseo/cache.ts</files>

  <read_first>
    - src/lib/dataforseo/orchestrator.ts — fetchWithCache ve makeFingerprint tam implementasyon (analog)
    - src/lib/dataforseo/types.ts — TaskSpec, CacheResult, SkipReason type'ları
    - .planning/phases/24-dataforseo-validation-layer/24-PATTERNS.md — "src/lib/dataforseo/cache.ts" bölümü (tam imports + wrapper pattern)
    - .planning/phases/24-dataforseo-validation-layer/24-RESEARCH.md — "Pattern 1: Mevcut fetchWithCache'i Sarmak"
  </read_first>

  <behavior>
    - getCachedOrFetch: fetchWithCache skipped=false ise { data, fromCache } döndürür
    - getCachedOrFetch: fetchWithCache skipped=true ise null döndürür
    - Dosya 'server-only' ile başlar — client-side import'u önler
  </behavior>

  <action>
  cache.ts dosyasını şu tam içerikle yaz:

  ```typescript
  /**
   * DataForSEO Cache Service — Phase 24
   *
   * getCachedOrFetch: fetchWithCache() orchestrator'ının ince ergonomi wrapper'ı.
   * Tüm guard mantığı (budget, backoff, running guard, TTL) fetchWithCache'de.
   * cache.ts yalnızca tip dönüşümü + null normalizasyonu yapar.
   *
   * DFS-01: Cache deposu dataforseo_task_cache tablosudur (20260522000001 migration).
   * keyword_data_cache adında ayrı tablo AÇILMAZ — bu tablonun DFS-01'i karşıladığı kabul edilir.
   */
  import 'server-only'

  import { fetchWithCache } from './orchestrator'
  import type { TaskSpec } from './types'

  export type CachedFetchResult<T> = {
    data: T
    fromCache: boolean
  }

  /**
   * Cache-first DataForSEO çağrısı.
   * - Cache HIT: dataforseo_task_cache'den döner, fromCache: true
   * - Cache MISS: fetcher() çağrılır, cache'e yazılır, fromCache: false
   * - SKIPPED (budget/backoff/running): null döner — çağıran hata göstermeli
   */
  export async function getCachedOrFetch<T>(opts: {
    projectId: string
    userId: string
    spec: TaskSpec
    fetcher: () => Promise<T>
  }): Promise<CachedFetchResult<T> | null> {
    const result = await fetchWithCache<T>({ ...opts })
    if (result.skipped) return null
    return { data: result.data as T, fromCache: result.fromCache }
  }
  ```

  Bu kadar — daha fazlası gerekmez. SHA-256, normalizasyon, budget guard tamamı fetchWithCache içinde.
  </action>

  <verify>
    <automated>npx vitest run src/__tests__/lib/dataforseo/cache.test.ts 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - Dosya `src/lib/dataforseo/cache.ts` mevcut
    - Dosyanın ilk gerçek import satırı `import 'server-only'`
    - Dosya `import { fetchWithCache } from './orchestrator'` içeriyor
    - Dosya `getCachedOrFetch` fonksiyonunu export ediyor
    - Dosya `CachedFetchResult` type'ını export ediyor
    - `npx vitest run src/__tests__/lib/dataforseo/cache.test.ts` — tüm testler PASS (GREEN)
    - TypeScript: `npx tsc --noEmit 2>&1 | grep "cache.ts"` çıktısı boş (sıfır hata)
    - Dosya SADECE fetchWithCache'i sarmalıyor — kendi SHA-256 veya cache okuma/yazma mantığı YOK
  </acceptance_criteria>

  <done>cache.ts 'server-only' guard + getCachedOrFetch wrapper implement edildi, tüm testler yeşil</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → cache.ts | 'server-only' import guard — client bundle'a sızmaz |
| cache.ts → dataforseo_task_cache | fetchWithCache içinde .eq('user_id') ownership zaten var |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02 | Tampering | Cache poisoning — malicious cache key | mitigate | makeFingerprint() orchestrator.ts'de normalize ediyor (sort+lowercase+trim); hand-roll yok — Trust source: orchestrator.ts [VERIFIED] |
| T-24-03 | Tampering | Cache key collision — "SEO" vs "seo" | mitigate | makeFingerprint keyword'ü lowercase+trim ile normalize ediyor; cache.ts bunu doğrudan çağırıyor |
| T-24-09 | Information Disclosure | cache.ts client bundle'a sızması | mitigate | `import 'server-only'` — Next.js build zamanında client import'unu hata olarak işaretler |
</threat_model>

<verification>
```bash
# 1. cache.ts mevcut ve server-only ile başlıyor mu?
head -5 src/lib/dataforseo/cache.ts

# 2. Tüm testler yeşil mi?
npx vitest run src/__tests__/lib/dataforseo/cache.test.ts

# 3. TypeScript hatası yok mu?
npx tsc --noEmit 2>&1 | grep "cache.ts" || echo "Temiz"

# 4. getCachedOrFetch export ediliyor mu?
grep "export.*getCachedOrFetch" src/lib/dataforseo/cache.ts

# 5. Kendi SHA-256 veya cache yazan kod yok mu?
grep -c "createHash\|INSERT\|supabase.from" src/lib/dataforseo/cache.ts
# Beklenen: 0
```
</verification>

<success_criteria>
- src/lib/dataforseo/cache.ts mevcut: 'server-only' + fetchWithCache wrapper + CachedFetchResult type + getCachedOrFetch function
- src/__tests__/lib/dataforseo/cache.test.ts mevcut: 3+ test case
- `npx vitest run src/__tests__/lib/dataforseo/cache.test.ts` — tüm testler PASS
- cache.ts kendi cache okuma/yazma kodu içermiyor (orchestrator'a delege ediyor)
- TypeScript compile hatası yok (cache.ts scope'unda)
</success_criteria>

<output>
Tamamlanınca `.planning/phases/24-dataforseo-validation-layer/24-02-SUMMARY.md` oluştur.
</output>
