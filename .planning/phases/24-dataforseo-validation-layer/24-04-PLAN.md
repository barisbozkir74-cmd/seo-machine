---
phase: "24"
plan: "04"
type: execute
wave: 3
depends_on: ["24-01", "24-03"]
files_modified:
  - src/app/api/dataforseo/deep-analysis/route.ts
  - src/app/api/dataforseo/deep-analysis/callback/route.ts
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/DeepAnalysisPoller.tsx
autonomous: true
requirements: [DFS-05, DFS-06, DFS-08]

must_haves:
  truths:
    - "POST /api/dataforseo/deep-analysis X-N8n-Webhook-Secret header ile korunuyor"
    - "Deep analiz trigger'ı workflow_runs tablosuna status='pending' satır INSERT ediyor"
    - "n8n tamamlanınca /api/dataforseo/deep-analysis/callback route'u workflow_runs'ı 'done'/'failed' yapıyor"
    - "Callback route'u keywords.dfs_fetched_at'ı bulk günceller (status='done' ise)"
    - "DeepAnalysisPoller.tsx 5 saniyelik polling ile çalışıyor, 144 döngü timeout var"
    - "Deep analiz çalışırken ikinci tetikleme concurrent guard tarafından engelleniyor"
  artifacts:
    - path: "src/app/api/dataforseo/deep-analysis/route.ts"
      provides: "n8n trigger route — workflow_runs INSERT + n8n webhook POST"
      contains: "X-N8n-Webhook-Secret"
    - path: "src/app/api/dataforseo/deep-analysis/callback/route.ts"
      provides: "n8n tamamlama callback — workflow_runs UPDATE + dfs_fetched_at bulk update"
      contains: "workflow_runs"
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/DeepAnalysisPoller.tsx"
      provides: "5s polling component, stop conditions: done/failed/timeout"
      contains: "setInterval"
  key_links:
    - from: "AnalysisButtons.tsx onDeepAnalysisStart"
      to: "POST /api/dataforseo/deep-analysis/route.ts"
      via: "fetch() call from client component"
      pattern: "fetch.*deep-analysis"
    - from: "DeepAnalysisPoller.tsx"
      to: "page.tsx SSR router.refresh()"
      via: "setInterval + router.refresh()"
      pattern: "router.refresh"
    - from: "n8n webhook"
      to: "/api/dataforseo/deep-analysis/callback"
      via: "POST with X-N8n-Webhook-Secret header + workflowRunId"
      pattern: "X-N8n-Webhook-Secret"
---

<objective>
Deep analiz async flow'unu kur: n8n trigger route, n8n callback route, ve DeepAnalysisPoller bileşeni. Phase 24'ün son halkası — deep analiz job lifecycle'ı tamamlanıyor.

Purpose: DFS-05/06/08 — deep analiz n8n'e gönderilir, workflow_runs ile izlenir, UI 5 saniyelik polling ile güncellenir. Phase 25 (competitor keyword intelligence) bu workflow_runs pattern'ını ve dfs_fetched_at güncellemesini bağımlılık olarak alır.

Output: 2 API route + 1 polling component. Phase 24 tamamlandığında Phase 25 için gerekli tüm altyapı (strategy_decisions, getCachedOrFetch, dfs_fetched_at, workflow_runs wiring) hazır.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-dataforseo-validation-layer/24-RESEARCH.md
@.planning/phases/24-dataforseo-validation-layer/24-PATTERNS.md
@.planning/phases/24-dataforseo-validation-layer/24-UI-SPEC.md
@.planning/phases/24-dataforseo-validation-layer/24-03-SUMMARY.md

<interfaces>
<!-- Kaynak: src/app/api/recovery/detect/route.ts — tam analog (Phase 16'dan VERIFIED) -->

```typescript
// Service role client pattern (detect/route.ts'den):
import { type NextRequest, NextResponse } from 'next/server'
import { createClient as createServiceClient } from '@supabase/supabase-js'

function getServiceClient() {
  return createServiceClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
  )
}

// Webhook secret check:
export async function POST(request: NextRequest) {
  const secret = request.headers.get('X-N8n-Webhook-Secret')
  const expectedSecret = process.env.N8N_WEBHOOK_SECRET
  if (expectedSecret && secret !== expectedSecret) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  }
  // Body parse:
  let body: { projectId?: string; userId?: string }
  try { body = await request.json() }
  catch { return NextResponse.json({ error: 'Invalid JSON' }, { status: 400 }) }
  // Ownership check:
  const serviceClient = getServiceClient()
  const { data: project } = await serviceClient
    .from('projects').select('id').eq('id', body.projectId).eq('user_id', body.userId).single()
  if (!project) return NextResponse.json({ error: 'Project not found' }, { status: 404 })
}
```

<!-- workflow_runs tablo şeması (create_tables.sql'den): -->
```
id, user_id, project_id, workflow_type TEXT, status TEXT DEFAULT 'pending',
input_payload JSONB, result_payload JSONB, error_message TEXT,
started_at TIMESTAMPTZ, completed_at TIMESTAMPTZ, created_at, updated_at
```

<!-- ResearchAutoTrigger.tsx polling pattern (VERIFIED: codebase): -->
```typescript
'use client'
import { useEffect, useRef, useState } from 'react'
import { useRouter } from 'next/navigation'

// Phase 24: DeepAnalysisPoller için bu pattern kopyalanır,
// 5000ms interval + 144 döngü timeout eklenir
```

<!-- AnalysisButtons.tsx prop interface (Plan 03'ten): -->
```typescript
interface AnalysisButtonsProps {
  // ...
  onDeepAnalysisStart?: () => void
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Deep Analysis trigger route — POST /api/dataforseo/deep-analysis/route.ts</name>
  <files>src/app/api/dataforseo/deep-analysis/route.ts</files>

  <read_first>
    - src/app/api/recovery/detect/route.ts — TAM analog: service client, webhook secret, ownership check, workflow_runs INSERT pattern
    - .planning/phases/24-dataforseo-validation-layer/24-PATTERNS.md — "deep-analysis/route.ts" bölümü (tam code örneği)
    - .planning/phases/24-dataforseo-validation-layer/24-RESEARCH.md — "Pattern 3: Deep Analiz n8n Route" ve "Pitfall 6: n8n Callback Route'unda Anon Key"
  </read_first>

  <action>
  `src/app/api/dataforseo/deep-analysis/` dizinini oluştur (yoksa).
  `src/app/api/dataforseo/deep-analysis/route.ts` dosyasını yaz:

  ```typescript
  /**
   * POST /api/dataforseo/deep-analysis
   * 
   * Deep analysis n8n trigger endpoint — Phase 24 DFS-05
   * 
   * Pattern: src/app/api/recovery/detect/route.ts kopyası (Phase 16)
   * - X-N8n-Webhook-Secret header auth
   * - Service role client (RLS bypass — anon key KULLANMA)
   * - Ownership check: projects.user_id
   * - workflow_runs INSERT (status='pending')
   * - n8n webhook POST
   * 
   * Body: { projectId: string, userId: string, keywordIds?: string[] }
   * Response: { workflowRunId: string } | { error: string }
   */
  import { type NextRequest, NextResponse } from 'next/server'
  import { createClient as createServiceClient } from '@supabase/supabase-js'

  function getServiceClient() {
    return createServiceClient(
      process.env.NEXT_PUBLIC_SUPABASE_URL!,
      process.env.SUPABASE_SERVICE_ROLE_KEY!,
    )
  }

  export async function POST(request: NextRequest) {
    // 1. Webhook secret auth (T-24-05 mitigation)
    const secret = request.headers.get('X-N8n-Webhook-Secret')
    const expectedSecret = process.env.N8N_WEBHOOK_SECRET
    if (expectedSecret && secret !== expectedSecret) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
    }

    // 2. Body parse
    let body: { projectId?: string; userId?: string; keywordIds?: string[] }
    try {
      body = await request.json()
    } catch {
      return NextResponse.json({ error: 'Invalid JSON' }, { status: 400 })
    }

    const { projectId, userId, keywordIds } = body
    if (!projectId || !userId) {
      return NextResponse.json(
        { error: 'projectId and userId required' },
        { status: 400 },
      )
    }

    // 3. UUID validation
    const uuidRegex = /^[0-9a-f-]{36}$/i
    if (!uuidRegex.test(projectId) || !uuidRegex.test(userId)) {
      return NextResponse.json({ error: 'Invalid UUID format' }, { status: 400 })
    }

    const serviceClient = getServiceClient()

    // 4. Ownership check (T-24-01 mitigation — IDOR önlemi)
    const { data: project } = await serviceClient
      .from('projects')
      .select('id')
      .eq('id', projectId)
      .eq('user_id', userId)
      .single()

    if (!project) {
      return NextResponse.json({ error: 'Project not found' }, { status: 404 })
    }

    // 5. DFS-08 Concurrent guard — aynı projede aktif deep analysis var mı?
    const { data: existingRun } = await serviceClient
      .from('workflow_runs')
      .select('id, status')
      .eq('project_id', projectId)
      .eq('user_id', userId)
      .eq('workflow_type', 'dfs_deep_analysis')
      .in('status', ['pending', 'running'])
      .maybeSingle()

    if (existingRun) {
      return NextResponse.json(
        { error: 'Analiz devam ediyor. Tamamlanmasını bekleyin.' },
        { status: 409 },
      )
    }

    // 6. workflow_runs INSERT (status='pending')
    const { data: workflowRun, error: insertError } = await serviceClient
      .from('workflow_runs')
      .insert({
        user_id: userId,
        project_id: projectId,
        workflow_type: 'dfs_deep_analysis',
        status: 'pending',
        input_payload: {
          keywordIds: keywordIds ?? [],
          analysisLevel: 'deep',
          triggeredAt: new Date().toISOString(),
        },
      })
      .select('id')
      .single()

    if (insertError || !workflowRun) {
      return NextResponse.json({ error: 'Workflow kaydı oluşturulamadı.' }, { status: 500 })
    }

    // 7. n8n webhook tetikle (N8N_DEEP_ANALYSIS_WEBHOOK_URL env var)
    const n8nUrl = process.env.N8N_DEEP_ANALYSIS_WEBHOOK_URL
    if (n8nUrl) {
      try {
        await fetch(n8nUrl, {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'X-N8n-Webhook-Secret': process.env.N8N_WEBHOOK_SECRET ?? '',
          },
          body: JSON.stringify({
            projectId,
            userId,
            workflowRunId: workflowRun.id,
            keywordIds: keywordIds ?? [],
          }),
        })
      } catch (e) {
        // n8n erişilemez — workflow_runs 'failed' yap
        await serviceClient
          .from('workflow_runs')
          .update({ status: 'failed', error_message: 'n8n webhook ulaşılamadı.' })
          .eq('id', workflowRun.id)
        return NextResponse.json({ error: 'n8n webhook tetiklenemedi.' }, { status: 502 })
      }
    }
    // N8N_DEEP_ANALYSIS_WEBHOOK_URL yoksa workflow_runs 'pending'te kalır (n8n kurulmamış ortam)

    return NextResponse.json({ workflowRunId: workflowRun.id }, { status: 200 })
  }
  ```

  **KRITIK:** Service role client ZORUNLU (anon key değil). n8n callback'ten gelen RLS bypass gerektirir.
  </action>

  <verify>
    <automated>grep -n "X-N8n-Webhook-Secret\|SUPABASE_SERVICE_ROLE_KEY\|dfs_deep_analysis" src/app/api/dataforseo/deep-analysis/route.ts</automated>
  </verify>

  <acceptance_criteria>
    - Dosya `src/app/api/dataforseo/deep-analysis/route.ts` mevcut
    - `X-N8n-Webhook-Secret` header check mevcut: `grep "X-N8n-Webhook-Secret" src/app/api/dataforseo/deep-analysis/route.ts`
    - `SUPABASE_SERVICE_ROLE_KEY` kullanılıyor (createClient çağrısında): `grep "SUPABASE_SERVICE_ROLE_KEY" src/app/api/dataforseo/deep-analysis/route.ts`
    - `dfs_deep_analysis` workflow_type: `grep "dfs_deep_analysis" src/app/api/dataforseo/deep-analysis/route.ts`
    - Ownership check var: `grep "eq.*user_id.*userId" src/app/api/dataforseo/deep-analysis/route.ts`
    - workflow_runs INSERT var: `grep "workflow_runs" src/app/api/dataforseo/deep-analysis/route.ts`
    - Concurrent guard var (409 response): `grep "409" src/app/api/dataforseo/deep-analysis/route.ts`
    - `createClient` (anon) IMPORT EDİLMİYOR — sadece `createServiceClient` kullanılıyor
    - TypeScript: `npx tsc --noEmit 2>&1 | grep "deep-analysis/route"` boş
  </acceptance_criteria>

  <done>Deep analysis trigger route yazıldı: service role client, webhook secret, concurrent guard, workflow_runs INSERT, n8n call</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Deep Analysis callback route — POST /api/dataforseo/deep-analysis/callback/route.ts</name>
  <files>src/app/api/dataforseo/deep-analysis/callback/route.ts</files>

  <read_first>
    - src/app/api/dataforseo/deep-analysis/route.ts — (Task 1'de yazılan) service client, secret check, ownership pattern
    - src/app/api/recovery/detect/route.ts — detect route tam pattern (service client, body parse, ownership)
    - .planning/phases/24-dataforseo-validation-layer/24-PATTERNS.md — "callback/route.ts" bölümü (workflow_runs UPDATE + dfs_fetched_at bulk update)
  </read_first>

  <action>
  `src/app/api/dataforseo/deep-analysis/callback/` dizinini oluştur.
  `route.ts` dosyasını yaz:

  ```typescript
  /**
   * POST /api/dataforseo/deep-analysis/callback
   * 
   * n8n deep analysis tamamlama callback'i — Phase 24 DFS-06
   * 
   * n8n analizi tamamlayınca bu endpoint'i çağırır:
   * - workflow_runs satırını 'done' veya 'failed' yapar
   * - status='done' ise keywords.dfs_fetched_at günceller
   * 
   * Body: {
   *   projectId: string
   *   userId: string
   *   workflowRunId: string
   *   status: 'done' | 'failed'
   *   keywordIds?: string[]
   *   error?: string
   *   result?: unknown
   * }
   */
  import { type NextRequest, NextResponse } from 'next/server'
  import { createClient as createServiceClient } from '@supabase/supabase-js'

  function getServiceClient() {
    return createServiceClient(
      process.env.NEXT_PUBLIC_SUPABASE_URL!,
      process.env.SUPABASE_SERVICE_ROLE_KEY!,
    )
  }

  export async function POST(request: NextRequest) {
    // 1. Webhook secret check (T-24-05 — n8n spoofing önlemi)
    const secret = request.headers.get('X-N8n-Webhook-Secret')
    const expectedSecret = process.env.N8N_WEBHOOK_SECRET
    if (expectedSecret && secret !== expectedSecret) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
    }

    // 2. Body parse
    let body: {
      projectId?: string
      userId?: string
      workflowRunId?: string
      status?: 'done' | 'failed'
      keywordIds?: string[]
      error?: string
      result?: unknown
    }
    try {
      body = await request.json()
    } catch {
      return NextResponse.json({ error: 'Invalid JSON' }, { status: 400 })
    }

    const { projectId, userId, workflowRunId, status, keywordIds, result } = body

    if (!projectId || !userId || !workflowRunId || !status) {
      return NextResponse.json(
        { error: 'projectId, userId, workflowRunId, status required' },
        { status: 400 },
      )
    }

    if (!['done', 'failed'].includes(status)) {
      return NextResponse.json({ error: 'status must be done or failed' }, { status: 400 })
    }

    // 3. UUID validation
    const uuidRegex = /^[0-9a-f-]{36}$/i
    if (!uuidRegex.test(projectId) || !uuidRegex.test(userId) || !uuidRegex.test(workflowRunId)) {
      return NextResponse.json({ error: 'Invalid UUID format' }, { status: 400 })
    }

    const serviceClient = getServiceClient()

    // 4. Ownership check (IDOR önlemi — T-24-01)
    const { data: project } = await serviceClient
      .from('projects')
      .select('id')
      .eq('id', projectId)
      .eq('user_id', userId)
      .single()

    if (!project) {
      return NextResponse.json({ error: 'Project not found' }, { status: 404 })
    }

    // 5. workflow_runs UPDATE
    const now = new Date().toISOString()
    const { error: updateError } = await serviceClient
      .from('workflow_runs')
      .update({
        status,
        result_payload: result ?? null,
        error_message: status === 'failed' ? (body.error ?? null) : null,
        completed_at: now,
        updated_at: now,
      })
      .eq('id', workflowRunId)
      .eq('project_id', projectId)

    if (updateError) {
      return NextResponse.json({ error: 'Workflow güncellenemedi.' }, { status: 500 })
    }

    // 6. dfs_fetched_at bulk update (DFS-07 — sadece status='done' ise)
    if (status === 'done' && Array.isArray(keywordIds) && keywordIds.length > 0) {
      await serviceClient
        .from('keywords')
        .update({ dfs_fetched_at: now })
        .in('id', keywordIds)
        .eq('project_id', projectId)
      // Hata non-fatal — workflow başarılı sayılır, sadece freshness güncellenmedi
    }

    return NextResponse.json({ ok: true }, { status: 200 })
  }
  ```
  </action>

  <verify>
    <automated>grep -n "X-N8n-Webhook-Secret\|dfs_fetched_at\|workflow_runs" src/app/api/dataforseo/deep-analysis/callback/route.ts</automated>
  </verify>

  <acceptance_criteria>
    - Dosya `src/app/api/dataforseo/deep-analysis/callback/route.ts` mevcut
    - `X-N8n-Webhook-Secret` check mevcut
    - `SUPABASE_SERVICE_ROLE_KEY` kullanılıyor (anon key değil)
    - `workflow_runs` UPDATE mevcut: `.update({ status, result_payload, completed_at })`
    - `dfs_fetched_at` bulk update mevcut: `grep "dfs_fetched_at" src/app/api/dataforseo/deep-analysis/callback/route.ts`
    - `status === 'done'` koşuluyla `dfs_fetched_at` güncellemesi var
    - Ownership check `.eq('user_id', userId)` var
    - TypeScript: `npx tsc --noEmit 2>&1 | grep "callback/route"` boş
  </acceptance_criteria>

  <done>Callback route yazıldı: webhook secret, ownership, workflow_runs UPDATE status+completed_at, dfs_fetched_at bulk update (done ise)</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: DeepAnalysisPoller.tsx + AnalysisButtons deep trigger wiring</name>
  <files>
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/DeepAnalysisPoller.tsx
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/AnalysisButtons.tsx
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx
  </files>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/arastirma/ResearchAutoTrigger.tsx — TAM analog: setInterval, useRef, router.refresh(), phase state machine
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/AnalysisButtons.tsx — (Plan 03'te yazılan) onDeepAnalysisStart prop
    - .planning/phases/24-dataforseo-validation-layer/24-PATTERNS.md — "DeepAnalysisPoller.tsx" bölümü (5s polling, timeout guard, phase state)
    - .planning/phases/24-dataforseo-validation-layer/24-UI-SPEC.md — Polling Lifecycle state machine + Copywriting (deep analiz states)
  </read_first>

  <action>
  *** ADIM 1: DeepAnalysisPoller.tsx oluştur ***

  ```tsx
  'use client'

  /**
   * DeepAnalysisPoller — Phase 24 DFS-06
   * 
   * Pattern: ResearchAutoTrigger.tsx kopyası (Phase 18)
   * 5 saniye interval + router.refresh() → SSR page workflow_runs durumunu okur
   * Stop conditions: completed / failed / timeout (12 dakika = 144 × 5s)
   */
  import { useEffect, useRef, useState } from 'react'
  import { useRouter } from 'next/navigation'

  type PollerStatus = 'pending' | 'running' | 'done' | 'failed' | 'timeout'

  interface DeepAnalysisPollerProps {
    initialStatus: 'pending' | 'running'
    currentStatus?: PollerStatus  // SSR re-render'da güncel durum
  }

  export function DeepAnalysisPoller({ initialStatus, currentStatus }: DeepAnalysisPollerProps) {
    const router = useRouter()
    const triggered = useRef(false)
    const [phase, setPhase] = useState<PollerStatus>(initialStatus)

    // currentStatus SSR'dan gelince güncelle
    useEffect(() => {
      if (currentStatus && currentStatus !== phase) {
        if (currentStatus === 'done' || currentStatus === 'failed') {
          setPhase(currentStatus)
        }
      }
    }, [currentStatus])

    useEffect(() => {
      if (triggered.current) return
      if (phase === 'done' || phase === 'failed' || phase === 'timeout') return
      triggered.current = true

      let loopCount = 0
      const MAX_LOOPS = 144 // 12 dakika timeout (UI-SPEC)

      const pollInterval = setInterval(() => {
        loopCount++

        if (loopCount >= MAX_LOOPS) {
          clearInterval(pollInterval)
          setPhase('timeout')
          return
        }

        // SSR page'i yenile → workflow_runs durumu SSR'dan okunur
        router.refresh()
      }, 5000)

      return () => clearInterval(pollInterval)
    }, [router, phase])

    if (phase === 'done') {
      return (
        <p className="text-emerald-400 text-xs">
          ✓ Derinlemesine analiz tamamlandı
        </p>
      )
    }

    if (phase === 'failed') {
      return (
        <p className="text-destructive text-xs">
          Analiz başarısız oldu. Sayfayı yenileyin veya tekrar deneyin.
        </p>
      )
    }

    if (phase === 'timeout') {
      return (
        <p className="text-amber-400 text-xs">
          Analiz yanıt vermedi. Sayfayı yenileyin.
        </p>
      )
    }

    // pending veya running — polling devam ediyor
    return (
      <span className="flex items-center gap-2 text-cyan-400/60 text-xs">
        <svg className="animate-spin h-3 w-3" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
          <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
          <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z" />
        </svg>
        Analiz Devam Ediyor...
      </span>
    )
  }
  ```

  *** ADIM 2: AnalysisButtons.tsx'i güncelle — onDeepAnalysisStart wiring ***

  AnalysisButtons.tsx'i oku. `onDeepAnalysisStart` prop zaten var (Plan 03'ten).
  "Derinlemesine Analiz" butonunun `openDialog('deep')` → `handleConfirm` → `onDeepAnalysisStart?.()` akışı çalışıyor.

  Eğer Plan 03'teki AnalysisButtons.tsx'de `onDeepAnalysisStart` sadece `() => void` ise, deep analiz için
  trigger route'u çağıran gerçek implementasyonu ekle:

  ```typescript
  // AnalysisButtons.tsx'e eklenecek — deep analiz fetch çağrısı
  // Bu fonksiyon AnalysisButtons'ı kullanan parent (page.tsx) tarafından sağlanır:
  // async function handleDeepAnalysisStart() {
  //   await fetch('/api/dataforseo/deep-analysis', {
  //     method: 'POST',
  //     headers: { 'Content-Type': 'application/json' },
  //     body: JSON.stringify({ projectId, userId: currentUserId }),
  //   })
  //   router.refresh()
  // }
  ```

  NOT: n8n trigger'ı için userId gerekli. AnalysisButtons'ın userId prop'u olması veya page.tsx'den sağlanması gerekir. En temiz çözüm: `onDeepAnalysisStart` prop'unun imzasını `() => Promise<void>` yap, gerçek fetch'i page.tsx'de implement et.

  *** ADIM 3: page.tsx'i güncelle — DeepAnalysisPoller mount + deep trigger handler ***

  page.tsx'i oku (24-03 değişiklikleri dahil). Aşağıdakileri ekle:

  ```tsx
  import { DeepAnalysisPoller } from './DeepAnalysisPoller'

  // SSR'dan activeWorkflow zaten var (Plan 03'te eklendi)
  // JSX içinde — activeWorkflow varsa poller'ı mount et:
  {activeWorkflow && (
    <DeepAnalysisPoller
      initialStatus={activeWorkflow.status as 'pending' | 'running'}
      currentStatus={activeWorkflow.status as PollerStatus}
    />
  )}
  ```

  `onDeepAnalysisStart` için page.tsx'de client-side fetch handler:
  ```typescript
  // page.tsx'de deep analiz trigger — NOT: page.tsx Server Component.
  // Bu yüzden onDeepAnalysisStart'ı bir Client Component'e taşı veya
  // AnalysisButtons.tsx içinde fetch'i doğrudan yap (userId server action'dan al).
  //
  // EN SADE ÇÖZÜM (RECOMMENDED):
  // AnalysisButtons.tsx'de "Derinlemesine Analizi Başlat" tıklanınca:
  // 1. lightAnalysisAction benzeri bir Server Action yaz (triggerDeepAnalysisAction)
  // 2. Bu action workflow_runs INSERT + n8n call yapar (route.ts yerine)
  // VEYA:
  // AnalysisButtons.tsx'de fetch('/api/dataforseo/deep-analysis', { method: 'POST', ... })
  // ile doğrudan route'u çağır — userId'yi supabase.auth.getSession() ile client'ta al
  ```

  **KARAR: Executor bu iki yaklaşımdan birini seçer:**
  
  Yaklaşım A (Önerilen — Basit): `triggerDeepAnalysisAction` Server Action ekle — actions.ts'e lightAnalysisAction pattern'ı ile, route.ts mantığını Server Action olarak implement et. AnalysisButtons'da bu action'ı çağır.

  Yaklaşım B (Eğer A tıkandıysa): Client-side `fetch('/api/dataforseo/deep-analysis', ...)` — userId için supabase.auth.getSession() client SDK'sıyla al.

  Yaklaşım A'yı seç — tutarlı (tüm tetiklemeler Server Action). Route sadece n8n → callback için.
  
  **Yaklaşım A implementasyonu (actions.ts'e ekle):**
  ```typescript
  export type DeepAnalysisResult =
    | { success: true; workflowRunId: string }
    | { success: false; error: string }

  export async function triggerDeepAnalysisAction(projectId: string): Promise<DeepAnalysisResult> {
    const uuidRegex = /^[0-9a-f-]{36}$/i
    if (!uuidRegex.test(projectId)) return { success: false, error: 'Geçersiz proje ID.' }

    const supabase = await createClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) return { success: false, error: 'Oturum bulunamadı.' }

    const { data: project } = await supabase
      .from('projects').select('id').eq('id', projectId).eq('user_id', user.id).single()
    if (!project) return { success: false, error: 'Proje bulunamadı.' }

    // Concurrent guard (DFS-08)
    const { data: runningJob } = await supabase
      .from('workflow_runs').select('id')
      .eq('project_id', projectId).eq('user_id', user.id)
      .in('status', ['pending', 'running']).maybeSingle()
    if (runningJob) return { success: false, error: 'Analiz devam ediyor. Tamamlanmasını bekleyin.' }

    // workflow_runs INSERT (anon client — user RLS ile yazıyor)
    const { data: workflowRun, error: insertError } = await supabase
      .from('workflow_runs')
      .insert({
        user_id: user.id, project_id: projectId,
        workflow_type: 'dfs_deep_analysis', status: 'pending',
        input_payload: { analysisLevel: 'deep', triggeredAt: new Date().toISOString() },
      })
      .select('id').single()

    if (insertError || !workflowRun) return { success: false, error: 'Workflow kaydı oluşturulamadı.' }

    // n8n webhook — NEXT_PUBLIC değil, server-only env var
    const n8nUrl = process.env.N8N_DEEP_ANALYSIS_WEBHOOK_URL
    if (n8nUrl) {
      try {
        await fetch(n8nUrl, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json', 'X-N8n-Webhook-Secret': process.env.N8N_WEBHOOK_SECRET ?? '' },
          body: JSON.stringify({ projectId, userId: user.id, workflowRunId: workflowRun.id }),
        })
      } catch {
        return { success: false, error: 'n8n webhook tetiklenemedi.' }
      }
    }

    revalidatePath(`/projeler/${projectId}/keyword-stratejisi`)
    return { success: true, workflowRunId: workflowRun.id }
  }
  ```

  AnalysisButtons.tsx'de `onDeepAnalysisStart` prop'u yerine `triggerDeepAnalysisAction`'ı doğrudan import et:
  ```typescript
  import { lightAnalysisAction, standardAnalysisAction, triggerDeepAnalysisAction } from './actions'
  // handleConfirm'de pending === 'deep' ise:
  startTransitionDeep(async () => {
    const result = await triggerDeepAnalysisAction(projectId)
    if (!result.success) setResultMsg({ type: 'error', text: result.error })
    else router.refresh() // DeepAnalysisPoller SSR'dan mount edilir
  })
  ```
  (Bunun için `useTransition` ve isPendingDeep state'i de ekle — AnalysisButtons'ı güncelle.)
  </action>

  <verify>
    <automated>grep -c "setInterval\|MAX_LOOPS\|router.refresh" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/DeepAnalysisPoller.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `src/app/(dashboard)/projeler/[id]/keyword-stratejisi/DeepAnalysisPoller.tsx` mevcut
    - Dosyada `setInterval` ve `MAX_LOOPS = 144` var: `grep "MAX_LOOPS\|setInterval" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/DeepAnalysisPoller.tsx`
    - Dosyada `router.refresh()` polling loop içinde: `grep "router.refresh" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/DeepAnalysisPoller.tsx`
    - Dosyada 3 stop condition mesajı var: `grep "tamamlandı\|başarısız\|yanıt vermedi" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/DeepAnalysisPoller.tsx`
    - `page.tsx`'de `DeepAnalysisPoller` import + render: `grep "DeepAnalysisPoller" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx`
    - `actions.ts`'e `triggerDeepAnalysisAction` eklendi: `grep "triggerDeepAnalysisAction" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts`
    - `triggerDeepAnalysisAction` concurrent guard içeriyor: `grep -A5 "triggerDeepAnalysisAction" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts | grep "workflow_runs"`
    - TypeScript: `npx tsc --noEmit 2>&1 | grep "DeepAnalysisPoller\|callback/route"` boş
  </acceptance_criteria>

  <done>DeepAnalysisPoller.tsx oluşturuldu (5s polling, 144 döngü timeout), triggerDeepAnalysisAction actions.ts'e eklendi, page.tsx'de poller mount edildi</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n → /api/dataforseo/deep-analysis/callback | Dış servis çağrısı — kimlik doğrulama zorunlu |
| n8n → /api/dataforseo/deep-analysis (trigger) | User-triggered ama n8n pattern taklit ediliyor |
| Server Action → workflow_runs | Kullanıcının kendi workflow_runs satırlarına yazıyor (anon key + RLS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Spoofing | n8n webhook — callback endpoint sahte çağrı | mitigate | `X-N8n-Webhook-Secret` header check her iki route'da; expectedSecret null ise check atlanır (geliştirme ortamı toleransı) |
| T-24-04 | Race Condition | Concurrent guard — trigger route + Server Action aynı anda | mitigate | workflow_runs `.in('status', ['pending','running'])` check her tetiklemede; 409 dönülür; atomik değil ama worst case = duplicate job (budget cost, kullanıcıya gösterilir) |
| T-24-06 | DoS (Cost Escalation) | Tekrarlayan deep analiz tetiklemesi | mitigate | Concurrent guard (sadece 1 aktif job izin verilir); no auto-retry on failure; dialog onayı zorunlu |
| T-24-01 | Elevation of Privilege | Callback route — başka kullanıcının workflow_runs güncellemesi | mitigate | `.eq('project_id', projectId)` ile workflow_runs update sınırlandırılmış; ownership check project.user_id ile her iki route'da |
| T-24-12 | Information Disclosure | workflowRunId tahmin edilebilir | accept | UUID v4 — 122 bit entropy; brute force pratik değil |
</threat_model>

<verification>
```bash
# 1. Route dosyaları mevcut mu?
ls src/app/api/dataforseo/deep-analysis/route.ts
ls src/app/api/dataforseo/deep-analysis/callback/route.ts

# 2. Service role her iki route'da kullanılıyor mu?
grep "SUPABASE_SERVICE_ROLE_KEY" src/app/api/dataforseo/deep-analysis/route.ts
grep "SUPABASE_SERVICE_ROLE_KEY" src/app/api/dataforseo/deep-analysis/callback/route.ts

# 3. Webhook secret her iki route'da mevcut mu?
grep "X-N8n-Webhook-Secret" src/app/api/dataforseo/deep-analysis/route.ts
grep "X-N8n-Webhook-Secret" src/app/api/dataforseo/deep-analysis/callback/route.ts

# 4. Poller dosyası ve timeout guard:
grep "MAX_LOOPS\|setInterval" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/DeepAnalysisPoller.tsx

# 5. dfs_fetched_at callback'te:
grep "dfs_fetched_at" src/app/api/dataforseo/deep-analysis/callback/route.ts

# 6. triggerDeepAnalysisAction actions.ts'te:
grep "export.*triggerDeepAnalysisAction" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts

# 7. TypeScript:
npx tsc --noEmit 2>&1 | grep "deep-analysis" | head -5
```
</verification>

<success_criteria>
- POST /api/dataforseo/deep-analysis: service role + webhook secret + concurrent guard + workflow_runs INSERT
- POST /api/dataforseo/deep-analysis/callback: service role + webhook secret + ownership + workflow_runs UPDATE + dfs_fetched_at bulk update
- DeepAnalysisPoller.tsx: 5s interval, MAX_LOOPS=144, 3 stop condition (done/failed/timeout), stop mesajları UI-SPEC copywriting ile birebir
- triggerDeepAnalysisAction: concurrent guard, workflow_runs INSERT, n8n webhook call — actions.ts'de mevcut
- page.tsx: DeepAnalysisPoller activeWorkflow varsa mount ediliyor
- TypeScript hatası artmadı
</success_criteria>

---

## Scope: Phase 24 vs Phase 25+

**Phase 24 teslim eder:**
- `strategy_decisions` tablosu (tüm v5.0 fazları için temel)
- `keywords.dfs_fetched_at` kolonu
- `getCachedOrFetch()` — dataforseo_task_cache üzerinde cache-first wrapper
- 3 analiz seviyesi tetikleyici (light sync / standard sync / deep async n8n)
- `workflow_runs` wiring — deep analiz job lifecycle
- 5s polling UI + dfs_fetched_at badge'leri

**Phase 24 teslim ETMEZ (Phase 25+ scope):**
- `competitor_keywords` staging tablosu
- Keyword gap haritası UI
- SERP overlap hesaplama (Phase 26)
- Cluster diff engine (Phase 26)
- Strategy decision locking UI (Phase 29)
- `is_starred` / `star_reason` kolonları (Phase 30)

## Phase 25 Bağımlılıkları (Bu Phase'den Alınanlar)

Phase 25 (Competitor Keyword Intelligence) Phase 24'ten şunları alır:

| Bağımlılık | Nereden | Nasıl Kullanılır |
|-----------|---------|-----------------|
| `strategy_decisions` tablosu | Plan 01 migration | Phase 25'te competitor analysis kararları buraya yazılır |
| `getCachedOrFetch()` | Plan 02 cache.ts | Phase 25 competitor keyword DataForSEO çağrıları bu wrapper'ı kullanır |
| `workflow_runs` pattern | Plan 04 route.ts | Phase 25 competitor deep analysis aynı workflow_runs pattern'ını kullanır |
| `dfs_fetched_at` güncelleme pattern | Plan 03+04 | Phase 25 competitor keywords için de dfs_fetched_at güncellemesi uygular |
| concurrent guard pattern | Plan 03+04 | Phase 25 server action'ları aynı `.in('status', ['pending','running'])` guard'ı kullanır |

<output>
Tamamlanınca `.planning/phases/24-dataforseo-validation-layer/24-04-SUMMARY.md` oluştur.
</output>
