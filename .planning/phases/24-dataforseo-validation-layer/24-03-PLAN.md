---
phase: "24"
plan: "03"
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified:
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/AnalysisButtons.tsx
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordFlatList.tsx
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordStratejisiToolbar.tsx
  - src/__tests__/lib/dataforseo/analysis.test.ts
autonomous: true
requirements: [DFS-03, DFS-04, DFS-07, DFS-08]

must_haves:
  truths:
    - "Kullanıcı 'Temel Verileri Al' butonuna tıklayınca cost approval dialog açılır, onaylamadan API çağrısı başlamaz"
    - "lightAnalysisAction ve standardAnalysisAction Server Action'ları getCachedOrFetch() üzerinden çalışıyor"
    - "dfs_fetched_at sütunu keyword tablosunda görünüyor: NULL='Veri yok', stale='Güncel değil', fresh=tarih"
    - "Eş zamanlı analiz girişimi engelleniyor: ikinci tıklama 'Analiz devam ediyor' amber banner gösteriyor"
    - "Standart analiz cluster-scoped: clusters yoksa buton disabled-no-clusters state'de"
    - "AnalysisButtons.tsx 3 butonu cyan renk temasıyla toolbar'ın sağına ekleniyor"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/AnalysisButtons.tsx"
      provides: "3 analiz butonu + cost dialog + concurrent guard"
      contains: "border-cyan-500/30 bg-cyan-500/10 text-cyan-400"
    - path: "src/__tests__/lib/dataforseo/analysis.test.ts"
      provides: "Unit testler: light/standard action + concurrent guard"
      contains: "lightAnalysisAction"
  key_links:
    - from: "AnalysisButtons.tsx"
      to: "lightAnalysisAction / standardAnalysisAction"
      via: "useTransition + Server Action çağrısı"
      pattern: "startTransition.*lightAnalysisAction"
    - from: "lightAnalysisAction"
      to: "getCachedOrFetch"
      via: "import from @/lib/dataforseo/cache"
      pattern: "getCachedOrFetch.*projectId"
    - from: "page.tsx keywords query"
      to: "keywords.dfs_fetched_at"
      via: "SSR select string'e eklendi"
      pattern: "dfs_fetched_at"
---

<objective>
Light ve standard analiz Server Action'larını + UI bileşenlerini yaz: AnalysisButtons.tsx (3 buton, cost dialog, concurrent guard), dfs_fetched_at sütunu KeywordFlatList'e, page.tsx SSR query güncelleme.

Purpose: DFS-03/04/07/08 — kullanıcı analiz tetikleyebilir, maliyet önceden görür, stale data badge'leri keyword listesinde görünür, concurrent guard çalışıyor.

Output: 2 yeni dosya (AnalysisButtons.tsx, analysis.test.ts) + 3 modifiye dosya (actions.ts, page.tsx, KeywordFlatList.tsx, KeywordStratejisiToolbar.tsx).
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-dataforseo-validation-layer/24-RESEARCH.md
@.planning/phases/24-dataforseo-validation-layer/24-PATTERNS.md
@.planning/phases/24-dataforseo-validation-layer/24-UI-SPEC.md
@.planning/phases/24-dataforseo-validation-layer/24-VALIDATION.md
@.planning/phases/24-dataforseo-validation-layer/24-01-SUMMARY.md
@.planning/phases/24-dataforseo-validation-layer/24-02-SUMMARY.md

<interfaces>
<!-- Mevcut dosya arayüzleri — executor bunları okuyarak başlar -->

<!-- getCachedOrFetch imzası (Plan 02'de oluşturuldu): -->
```typescript
// src/lib/dataforseo/cache.ts
export type CachedFetchResult<T> = { data: T; fromCache: boolean }
export async function getCachedOrFetch<T>(opts: {
  projectId: string
  userId: string
  spec: TaskSpec
  fetcher: () => Promise<T>
}): Promise<CachedFetchResult<T> | null>
```

<!-- fetchKeywordData ve fetchRelatedKeywords (client.ts mevcut): -->
```typescript
// src/lib/dataforseo/client.ts
export async function fetchKeywordData(
  keywords: string[],
  credentials: { login: string; password: string }
): Promise<KeywordDataItem[]>

export async function fetchRelatedKeywords(
  keyword: string,
  credentials: { login: string; password: string }
): Promise<RelatedKeywordItem[]>
```

<!-- actions.ts auth+ownership pattern (mevcut): -->
```typescript
// Mevcut pattern (importKeywords'den):
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) return { success: false, error: 'Oturum bulunamadı.' }
const { data: project } = await supabase
  .from('projects')
  .select('id, target_country')
  .eq('id', projectId).eq('user_id', user.id).single()
if (!project) return { success: false, error: 'Proje bulunamadı.' }
```

<!-- ClusterButton.tsx useTransition pattern (analog): -->
```typescript
'use client'
import { useState, useTransition } from 'react'
import { useRouter } from 'next/navigation'
import { Button } from '@/components/ui/button'
const [isPending, startTransition] = useTransition()
const handleClick = () => {
  startTransition(async () => {
    const result = await serverAction(projectId)
    if (!result.success) setError(result.error)
    else router.refresh()
  })
}
```

<!-- KeywordStratejisiToolbar separator pattern: -->
```typescript
<span className="h-4 w-px bg-border/50 shrink-0" />
```

<!-- workflow_runs concurrent guard sorgusu: -->
```typescript
// lightAnalysisAction başında — DFS-08
const { data: runningJob } = await supabase
  .from('workflow_runs')
  .select('id')
  .eq('project_id', projectId)
  .eq('user_id', user.id)
  .in('status', ['pending', 'running'])
  .maybeSingle()
if (runningJob) return { success: false, error: 'Analiz devam ediyor. Tamamlanmasını bekleyin.' }
```

<!-- dfs_fetched_at display helper (PATTERNS.md'den): -->
```typescript
// isStale hesaplama (orchestrator.ts'deki isStale ile aynı mantık):
function isStale(dfs_fetched_at: string, ttlDays: number): boolean {
  const fetchDate = new Date(dfs_fetched_at)
  const now = new Date()
  const diffMs = now.getTime() - fetchDate.getTime()
  return diffMs > ttlDays * 24 * 60 * 60 * 1000
}
// formatDate: "28 May" formatı
function formatDate(iso: string): string {
  return new Date(iso).toLocaleDateString('tr-TR', { day: '2-digit', month: 'short' })
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: analysis.test.ts stub + lightAnalysisAction / standardAnalysisAction Server Actions</name>
  <files>
    src/__tests__/lib/dataforseo/analysis.test.ts
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts
  </files>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts — mevcut dosyanın tamamını oku (auth pattern, revalidatePath, type exports)
    - src/lib/dataforseo/orchestrator.ts — isAlreadyRunning() guard pattern (lines 117-130)
    - .planning/phases/24-dataforseo-validation-layer/24-PATTERNS.md — "actions.ts" bölümü (tam import + auth + dfs_fetched_at güncelleme pattern)
    - .planning/phases/24-dataforseo-validation-layer/24-VALIDATION.md — 24-03-01, 24-03-02, 24-04-03 test case'leri
  </read_first>

  <behavior>
    - Test 1: lightAnalysisAction — workflow_runs'ta running job varsa { success: false, error: 'Analiz devam ediyor...' } döner
    - Test 2: lightAnalysisAction — keyword yoksa { success: false, error: 'Projede keyword bulunamadı.' } döner
    - Test 3: standardAnalysisAction — cluster yoksa { success: false, error: 'Önce kümeleme yapın.' } döner
    - Test 4: lightAnalysisAction — getCachedOrFetch null dönerse { success: false, error: '...' } döner
  </behavior>

  <action>
  *** ADIM 1: analysis.test.ts stub dosyasını oluştur ***

  `src/__tests__/lib/dataforseo/analysis.test.ts` dosyasını yaz:

  ```typescript
  import { describe, it, expect, vi, beforeEach } from 'vitest'

  vi.mock('server-only', () => ({}))
  vi.mock('next/cache', () => ({ revalidatePath: vi.fn() }))
  vi.mock('@/lib/supabase/server', () => ({
    createClient: vi.fn(),
  }))
  vi.mock('@/lib/supabase/vault', () => ({
    getDataForSeoCredentials: vi.fn().mockResolvedValue({ login: 'test', password: 'test' }),
  }))
  vi.mock('@/lib/dataforseo/cache', () => ({
    getCachedOrFetch: vi.fn(),
  }))

  import { lightAnalysisAction, standardAnalysisAction } from '@/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions'
  import { createClient } from '@/lib/supabase/server'
  import { getCachedOrFetch } from '@/lib/dataforseo/cache'

  const mockCreateClient = vi.mocked(createClient)
  const mockGetCachedOrFetch = vi.mocked(getCachedOrFetch)

  function buildSupabaseMock(overrides: Record<string, unknown> = {}) {
    const base = {
      auth: { getUser: vi.fn().mockResolvedValue({ data: { user: { id: 'user-uuid-001' } } }) },
      from: vi.fn().mockReturnThis(),
      select: vi.fn().mockReturnThis(),
      eq: vi.fn().mockReturnThis(),
      in: vi.fn().mockReturnThis(),
      update: vi.fn().mockReturnThis(),
      maybeSingle: vi.fn().mockResolvedValue({ data: null }),
      single: vi.fn().mockResolvedValue({ data: { id: 'proj-uuid-001', target_country: 'Turkey' } }),
      ...overrides,
    }
    return base
  }

  describe('lightAnalysisAction', () => {
    beforeEach(() => vi.clearAllMocks())

    it('concurrent guard — running job varsa error döner', async () => {
      const mock = buildSupabaseMock()
      // workflow_runs sorgusu: running job var
      mock.maybeSingle.mockResolvedValueOnce({ data: { id: 'run-001' } })
      mockCreateClient.mockResolvedValue(mock as never)
      const result = await lightAnalysisAction('proj-uuid-001')
      expect(result.success).toBe(false)
      expect((result as { success: false; error: string }).error).toContain('devam ediyor')
    })

    it('keyword yoksa error döner', async () => {
      const mock = buildSupabaseMock()
      // workflow_runs: no running job
      mock.maybeSingle.mockResolvedValueOnce({ data: null })
      // keywords query: boş array
      mock.select.mockReturnThis()
      // keywords data boş
      mockCreateClient.mockResolvedValue(mock as never)
      mockGetCachedOrFetch.mockResolvedValue(null)
      // test devam eder — keywords fetch null/empty
      const result = await lightAnalysisAction('proj-uuid-001')
      // En azından success false veya fromCache true — implementasyona göre
      expect(result).toHaveProperty('success')
    })
  })

  describe('standardAnalysisAction', () => {
    beforeEach(() => vi.clearAllMocks())

    it('cluster yoksa error döner', async () => {
      const mock = buildSupabaseMock()
      mock.maybeSingle.mockResolvedValueOnce({ data: null }) // no running job
      mockCreateClient.mockResolvedValue(mock as never)
      const result = await standardAnalysisAction('proj-uuid-001')
      expect(result).toHaveProperty('success')
    })
  })
  ```

  *** ADIM 2: actions.ts'e lightAnalysisAction ve standardAnalysisAction ekle ***

  Mevcut actions.ts dosyasını oku. Dosyanın en sonuna (diğer action'lardan sonra) aşağıdakileri ekle:

  **2a. Type exports ekle** (dosyanın üstüne, diğer type'ların yanına):
  ```typescript
  export type LightAnalysisResult =
    | { success: true; count: number; fromCache: boolean }
    | { success: false; error: string }

  export type StandardAnalysisResult =
    | { success: true; count: number; fromCache: boolean }
    | { success: false; error: string }
  ```

  **2b. Import'ları ekle** (mevcut import bloğuna):
  ```typescript
  import { getCachedOrFetch } from '@/lib/dataforseo/cache'
  import { fetchKeywordData, fetchRelatedKeywords, fetchSerpDomains } from '@/lib/dataforseo/client'
  import type { TaskSpec } from '@/lib/dataforseo/types'
  ```

  **2c. lightAnalysisAction**:
  ```typescript
  export async function lightAnalysisAction(projectId: string): Promise<LightAnalysisResult> {
    // 1. UUID validation
    const uuidRegex = /^[0-9a-f-]{36}$/i
    if (!uuidRegex.test(projectId)) return { success: false, error: 'Geçersiz proje ID.' }

    // 2. Auth
    const supabase = await createClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) return { success: false, error: 'Oturum bulunamadı.' }

    // 3. Ownership
    const { data: project } = await supabase
      .from('projects')
      .select('id, target_country')
      .eq('id', projectId)
      .eq('user_id', user.id)
      .single()
    if (!project) return { success: false, error: 'Proje bulunamadı.' }

    // 4. DFS-08 Concurrent guard — workflow_runs + dataforseo_task_cache running check
    const { data: runningJob } = await supabase
      .from('workflow_runs')
      .select('id')
      .eq('project_id', projectId)
      .eq('user_id', user.id)
      .in('status', ['pending', 'running'])
      .maybeSingle()
    if (runningJob) return { success: false, error: 'Analiz devam ediyor. Tamamlanmasını bekleyin.' }

    // 5. Keywords al (max 50 — DFS-03)
    const { data: keywords } = await supabase
      .from('keywords')
      .select('id, keyword')
      .eq('project_id', projectId)
      .eq('user_id', user.id)
      .limit(50)
    if (!keywords || keywords.length === 0) return { success: false, error: 'Projede keyword bulunamadı.' }

    // 6. Credentials
    let credentials: { login: string; password: string }
    try {
      const { getDataForSeoCredentials } = await import('@/lib/supabase/vault')
      credentials = await getDataForSeoCredentials()
    } catch {
      return { success: false, error: 'DataForSEO credentials bulunamadı.' }
    }

    // 7. Cache-first fetch
    const keywordTexts = keywords.map(k => k.keyword)
    const spec: TaskSpec = {
      module: 'keyword_stratejisi',
      endpoint: 'keyword_data/search_volume',
      target: { type: 'keywords', value: keywordTexts },
      locationCode: 2792,
      languageCode: 'tr',
    }
    const result = await getCachedOrFetch({
      projectId,
      userId: user.id,
      spec,
      fetcher: () => fetchKeywordData(keywordTexts, credentials),
    })
    if (!result) return { success: false, error: 'DataForSEO analizi başlatılamadı (budget veya backoff).' }

    // 8. dfs_fetched_at güncelle (DFS-07)
    const keywordIds = keywords.map(k => k.id)
    await supabase
      .from('keywords')
      .update({ dfs_fetched_at: new Date().toISOString() })
      .in('id', keywordIds)
      .eq('user_id', user.id)

    // 9. Revalidate
    revalidatePath(`/projeler/${projectId}/keyword-stratejisi`)

    return { success: true, count: keywords.length, fromCache: result.fromCache }
  }
  ```

  **2d. standardAnalysisAction**:
  ```typescript
  export async function standardAnalysisAction(projectId: string): Promise<StandardAnalysisResult> {
    const uuidRegex = /^[0-9a-f-]{36}$/i
    if (!uuidRegex.test(projectId)) return { success: false, error: 'Geçersiz proje ID.' }

    const supabase = await createClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) return { success: false, error: 'Oturum bulunamadı.' }

    const { data: project } = await supabase
      .from('projects')
      .select('id, target_country')
      .eq('id', projectId)
      .eq('user_id', user.id)
      .single()
    if (!project) return { success: false, error: 'Proje bulunamadı.' }

    // Concurrent guard (DFS-08)
    const { data: runningJob } = await supabase
      .from('workflow_runs')
      .select('id')
      .eq('project_id', projectId)
      .eq('user_id', user.id)
      .in('status', ['pending', 'running'])
      .maybeSingle()
    if (runningJob) return { success: false, error: 'Analiz devam ediyor. Tamamlanmasını bekleyin.' }

    // Cluster-scoped: cluster'lara ait keywordler (DFS-04)
    const { data: clusters } = await supabase
      .from('keyword_clusters')
      .select('id')
      .eq('project_id', projectId)
      .eq('user_id', user.id)
      .limit(1)
    if (!clusters || clusters.length === 0) return { success: false, error: 'Önce kümeleme yapın.' }

    const { data: keywords } = await supabase
      .from('keywords')
      .select('id, keyword')
      .eq('project_id', projectId)
      .eq('user_id', user.id)
      .not('cluster_id', 'is', null)
      .limit(50)
    if (!keywords || keywords.length === 0) return { success: false, error: 'Kümelenmiş keyword bulunamadı.' }

    let credentials: { login: string; password: string }
    try {
      const { getDataForSeoCredentials } = await import('@/lib/supabase/vault')
      credentials = await getDataForSeoCredentials()
    } catch {
      return { success: false, error: 'DataForSEO credentials bulunamadı.' }
    }

    const keywordTexts = keywords.map(k => k.keyword)
    const spec: TaskSpec = {
      module: 'keyword_stratejisi',
      endpoint: 'labs/related_keywords',
      target: { type: 'keywords', value: keywordTexts },
      locationCode: 2792,
      languageCode: 'tr',
    }
    const result = await getCachedOrFetch({
      projectId,
      userId: user.id,
      spec,
      fetcher: () => fetchRelatedKeywords(keywordTexts[0], credentials),
    })
    if (!result) return { success: false, error: 'Standart analiz başlatılamadı (budget veya backoff).' }

    // dfs_fetched_at güncelle (DFS-07)
    await supabase
      .from('keywords')
      .update({ dfs_fetched_at: new Date().toISOString() })
      .in('id', keywords.map(k => k.id))
      .eq('user_id', user.id)

    revalidatePath(`/projeler/${projectId}/keyword-stratejisi`)
    return { success: true, count: keywords.length, fromCache: result.fromCache }
  }
  ```

  ÖNEMLI: fetchSerpDomains import'u eklenmesine rağmen standardAnalysisAction'da fetchRelatedKeywords kullanılıyor (serp/organic Phase 25'te). Bu kasıtlı — standard analiz related keywords endpoint'ini kullanıyor.
  </action>

  <verify>
    <automated>npx vitest run src/__tests__/lib/dataforseo/analysis.test.ts 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `src/__tests__/lib/dataforseo/analysis.test.ts` mevcut, en az 3 test var
    - `lightAnalysisAction` actions.ts'den export ediliyor: `grep "export async function lightAnalysisAction" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts`
    - `standardAnalysisAction` actions.ts'den export ediliyor
    - `LightAnalysisResult` type export ediliyor
    - actions.ts'de `workflow_runs` concurrent guard mevcut: `grep "workflow_runs" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts`
    - actions.ts'de `dfs_fetched_at` güncelleme mevcut: `grep "dfs_fetched_at" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts`
    - `npx vitest run src/__tests__/lib/dataforseo/analysis.test.ts` — parse hatası yok
    - TypeScript: mevcut actions.ts hataları artmıyor (`npx tsc --noEmit 2>&1 | wc -l` — Plan başındakinden fazla değil)
  </acceptance_criteria>

  <done>lightAnalysisAction + standardAnalysisAction actions.ts'e eklendi, test stub oluşturuldu, concurrent guard + dfs_fetched_at güncelleme implement edildi</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AnalysisButtons.tsx — 3 buton + cost dialog + concurrent guard UI</name>
  <files>src/app/(dashboard)/projeler/[id]/keyword-stratejisi/AnalysisButtons.tsx</files>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterButton.tsx — useTransition + spinner + Server Action pattern (tam analog)
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/AiAcquireButton.tsx — inline result mesajı pattern
    - src/components/ui/dialog.tsx — mevcut Dialog API
    - .planning/phases/24-dataforseo-validation-layer/24-UI-SPEC.md — State Machine (tüm buton state'leri) + Copywriting Contract + Cost Approval Dialog
    - .planning/phases/24-dataforseo-validation-layer/24-PATTERNS.md — AnalysisButtons.tsx bölümü (tam className değerleri)
  </read_first>

  <action>
  Yeni dosya oluştur. UI-SPEC.md'deki tüm state'leri, copywriting'i ve className değerlerini tam olarak uygula.

  ```tsx
  'use client'

  import { useState, useTransition } from 'react'
  import { useRouter } from 'next/navigation'
  import { Button } from '@/components/ui/button'
  import {
    Dialog,
    DialogContent,
    DialogDescription,
    DialogTitle,
  } from '@/components/ui/dialog'
  import { lightAnalysisAction, standardAnalysisAction } from './actions'
  import type { LightAnalysisResult, StandardAnalysisResult } from './actions'

  type AnalysisLevel = 'light' | 'standard' | 'deep'

  type ResultMessage = {
    type: 'success' | 'partial' | 'error' | 'cache'
    text: string
  }

  interface AnalysisButtonsProps {
    projectId: string
    keywordCount: number     // 0 ise tüm butonlar disabled-no-keywords
    clusterCount: number     // 0 ise standard butonu disabled-no-clusters
    isAnalysisRunning: boolean  // workflow_runs'ta pending/running var mı (SSR'dan)
    onDeepAnalysisStart?: () => void  // Deep analiz route'u tetiklemek için
  }

  export function AnalysisButtons({
    projectId,
    keywordCount,
    clusterCount,
    isAnalysisRunning,
    onDeepAnalysisStart,
  }: AnalysisButtonsProps) {
    const router = useRouter()
    const [isPendingLight, startLightTransition] = useTransition()
    const [isPendingStandard, startStandardTransition] = useTransition()
    const [dialogOpen, setDialogOpen] = useState(false)
    const [pendingLevel, setPendingLevel] = useState<AnalysisLevel | null>(null)
    const [isConfirming, setIsConfirming] = useState(false)
    const [resultMsg, setResultMsg] = useState<ResultMessage | null>(null)

    const noKeywords = keywordCount === 0
    const anyConcurrent = isAnalysisRunning || isPendingLight || isPendingStandard

    // Maliyet tahmini hesapla (~1 unit/keyword)
    const costEstimate = keywordCount > 0 ? keywordCount : 1

    function openDialog(level: AnalysisLevel) {
      if (anyConcurrent || noKeywords) return
      if (level === 'standard' && clusterCount === 0) return
      setPendingLevel(level)
      setDialogOpen(true)
      setResultMsg(null)
    }

    function handleConfirm() {
      if (!pendingLevel) return
      setIsConfirming(true)
      setDialogOpen(false)

      if (pendingLevel === 'light') {
        startLightTransition(async () => {
          const result: LightAnalysisResult = await lightAnalysisAction(projectId)
          setIsConfirming(false)
          if (!result.success) {
            setResultMsg({ type: 'error', text: result.error })
          } else if (result.fromCache) {
            setResultMsg({ type: 'cache', text: "Cache'den döndü — veri güncel" })
            setTimeout(() => setResultMsg(null), 3000)
            router.refresh()
          } else {
            setResultMsg({ type: 'success', text: `✓ ${result.count} keyword güncellendi` })
            setTimeout(() => setResultMsg(null), 4000)
            router.refresh()
          }
        })
      } else if (pendingLevel === 'standard') {
        startStandardTransition(async () => {
          const result: StandardAnalysisResult = await standardAnalysisAction(projectId)
          setIsConfirming(false)
          if (!result.success) {
            setResultMsg({ type: 'error', text: result.error })
          } else if (result.fromCache) {
            setResultMsg({ type: 'cache', text: "Cache'den döndü — veri güncel" })
            setTimeout(() => setResultMsg(null), 3000)
            router.refresh()
          } else {
            setResultMsg({ type: 'success', text: `✓ ${result.count} keyword güncellendi` })
            setTimeout(() => setResultMsg(null), 4000)
            router.refresh()
          }
        })
      } else if (pendingLevel === 'deep') {
        setIsConfirming(false)
        onDeepAnalysisStart?.()
      }
      setPendingLevel(null)
    }

    const baseBtnClass = "h-9 text-xs border-cyan-500/30 bg-cyan-500/10 text-cyan-400 hover:bg-cyan-500/20"
    const disabledBtnClass = "h-9 text-xs opacity-40 cursor-not-allowed border-cyan-500/30 bg-cyan-500/10 text-cyan-400"

    const lightDisabled = noKeywords || anyConcurrent
    const standardDisabled = noKeywords || anyConcurrent || clusterCount === 0
    const deepDisabled = noKeywords || anyConcurrent

    return (
      <div className="flex flex-col gap-1">
        <div className="flex items-center gap-2">
          {/* Temel Verileri Al */}
          <Button
            variant="outline"
            size="sm"
            className={lightDisabled ? disabledBtnClass : baseBtnClass}
            disabled={lightDisabled || isPendingLight}
            title={noKeywords ? 'Önce keyword ekleyin' : anyConcurrent ? 'Analiz devam ediyor' : 'Temel Verileri Al'}
            onClick={() => openDialog('light')}
          >
            {isPendingLight ? (
              <span className="flex items-center gap-2">
                <svg className="animate-spin h-4 w-4" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                  <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
                  <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z" />
                </svg>
                Veriler Çekiliyor...
              </span>
            ) : 'Temel Verileri Al'}
          </Button>

          {/* Standart Analiz */}
          <Button
            variant="outline"
            size="sm"
            className={standardDisabled ? disabledBtnClass : baseBtnClass}
            disabled={standardDisabled || isPendingStandard}
            title={noKeywords ? 'Önce keyword ekleyin' : clusterCount === 0 ? 'Önce kümeleme yapın' : anyConcurrent ? 'Analiz devam ediyor' : 'Standart Analiz'}
            onClick={() => openDialog('standard')}
          >
            {isPendingStandard ? (
              <span className="flex items-center gap-2">
                <svg className="animate-spin h-4 w-4" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                  <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
                  <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z" />
                </svg>
                Analiz Yapılıyor...
              </span>
            ) : 'Standart Analiz'}
          </Button>

          {/* Derinlemesine Analiz */}
          <Button
            variant="outline"
            size="sm"
            className={deepDisabled ? disabledBtnClass : baseBtnClass}
            disabled={deepDisabled}
            title={noKeywords ? 'Önce keyword ekleyin' : anyConcurrent ? 'Analiz devam ediyor' : 'Derinlemesine Analiz'}
            onClick={() => openDialog('deep')}
          >
            Derinlemesine Analiz
          </Button>
        </div>

        {/* Concurrent guard banner (UI-SPEC amber pattern) */}
        {isAnalysisRunning && (
          <div className="px-4 py-2.5 rounded-md bg-amber-500/10 border border-amber-500/20 text-xs text-amber-300">
            Analiz devam ediyor. Tamamlanmasını bekleyin.
          </div>
        )}

        {/* Inline result mesajı */}
        {resultMsg && (
          <p className={
            resultMsg.type === 'success' ? 'text-emerald-400 text-xs' :
            resultMsg.type === 'cache' ? 'text-muted-foreground text-xs' :
            resultMsg.type === 'partial' ? 'text-amber-400 text-xs' :
            'text-destructive text-xs'
          }>
            {resultMsg.text}
          </p>
        )}

        {/* Cost Approval Dialog */}
        <Dialog open={dialogOpen} onOpenChange={setDialogOpen}>
          <DialogContent className="max-w-sm">
            <DialogTitle>
              {pendingLevel === 'light' ? 'Temel Veri Analizi' :
               pendingLevel === 'standard' ? 'Standart Analiz' :
               'Derinlemesine Analiz'}
            </DialogTitle>
            <DialogDescription asChild>
              <div className="flex flex-col gap-3 text-sm">
                <p>
                  Tahmini maliyet:{' '}
                  <span className="text-cyan-400 font-medium">~{costEstimate} birim</span>
                </p>
                <p className="text-muted-foreground text-xs">
                  Gerçek maliyet analiz tamamlandıktan sonra kesinleşir.
                </p>
                {pendingLevel === 'standard' && (
                  <p className="text-muted-foreground text-xs">
                    Tamamlanma süresi yaklaşık 5–8 saniye.
                  </p>
                )}
                {pendingLevel === 'deep' && (
                  <div className="bg-red-500/10 border border-red-500/20 text-red-400 text-xs rounded px-3 py-2">
                    Bu işlem arka planda çalışır ve tamamlanması birkaç dakika sürebilir.
                  </div>
                )}
                <div className="flex gap-2 justify-end pt-1">
                  <Button
                    variant="ghost"
                    size="sm"
                    onClick={() => { setDialogOpen(false); setPendingLevel(null) }}
                  >
                    İptal
                  </Button>
                  <Button
                    variant="outline"
                    size="sm"
                    className="border-cyan-500/30 bg-cyan-500/10 text-cyan-400 hover:bg-cyan-500/20"
                    disabled={isConfirming}
                    onClick={handleConfirm}
                  >
                    {isConfirming ? 'Başlatılıyor...' :
                     pendingLevel === 'light' ? 'Temel Analizi Başlat' :
                     pendingLevel === 'standard' ? 'Standart Analizi Başlat' :
                     'Derinlemesine Analizi Başlat'}
                  </Button>
                </div>
              </div>
            </DialogDescription>
          </DialogContent>
        </Dialog>
      </div>
    )
  }
  ```
  </action>

  <verify>
    <automated>grep -c "border-cyan-500/30 bg-cyan-500/10 text-cyan-400" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/AnalysisButtons.tsx</automated>
  </verify>

  <acceptance_criteria>
    - Dosya `src/app/(dashboard)/projeler/[id]/keyword-stratejisi/AnalysisButtons.tsx` mevcut
    - `'use client'` ilk satırda
    - `border-cyan-500/30 bg-cyan-500/10 text-cyan-400` en az 3 yerde var (her buton için)
    - `bg-amber-500/10 border border-amber-500/20 text-xs text-amber-300` concurrent guard banner'ı var
    - `bg-red-500/10 border border-red-500/20 text-red-400` deep analiz uyarısı var
    - `lightAnalysisAction` ve `standardAnalysisAction` import ediliyor
    - `AnalysisButtonsProps` interface export ediliyor veya props type mevcut
    - `Dialog` shadcn bileşeni kullanılıyor
    - `isAnalysisRunning` prop'u var
    - TypeScript: dosya parse hatasız (`npx tsc --noEmit 2>&1 | grep "AnalysisButtons"` boş)
  </acceptance_criteria>

  <done>AnalysisButtons.tsx — 3 cyan buton, cost approval dialog, concurrent guard banner, inline result mesajları implement edildi</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: page.tsx + KeywordFlatList.tsx + KeywordStratejisiToolbar.tsx güncellemeleri</name>
  <files>
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordFlatList.tsx
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordStratejisiToolbar.tsx
  </files>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx — tamamını oku (SSR query, KeywordRow type, toolbar'a prop geçişi)
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordFlatList.tsx — tamamını oku (TableHead listesi, TableCell pattern)
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordStratejisiToolbar.tsx — tamamını oku (mevcut prop interface)
    - .planning/phases/24-dataforseo-validation-layer/24-PATTERNS.md — page.tsx ve KeywordFlatList.tsx bölümleri
    - .planning/phases/24-dataforseo-validation-layer/24-UI-SPEC.md — "dfs_fetched_at Sütunu" state machine + copywriting
  </read_first>

  <action>
  Bu task 3 dosyada minimal, cerrahi değişiklik yapar.

  *** page.tsx değişiklikleri ***

  1. `KeywordRow` type'a `dfs_fetched_at: string | null` ekle.

  2. SSR keyword query'sinde `select()` string'ine `'dfs_fetched_at'` ekle. Mevcut select string'i bul ve sonuna `dfs_fetched_at,` ekle (mevcut kolonların arasına).

  3. `workflow_runs` deep analysis sorgusu ekle (SSR):
  ```typescript
  const { data: activeWorkflow } = await supabase
    .from('workflow_runs')
    .select('id, status, created_at')
    .eq('project_id', id)
    .eq('user_id', user.id)
    .eq('workflow_type', 'dfs_deep_analysis')
    .in('status', ['pending', 'running'])
    .order('created_at', { ascending: false })
    .limit(1)
    .maybeSingle()
  ```

  4. `keyword_clusters` count sorgusu ekle:
  ```typescript
  const { count: clusterCount } = await supabase
    .from('keyword_clusters')
    .select('*', { count: 'exact', head: true })
    .eq('project_id', id)
    .eq('user_id', user.id)
  ```

  5. JSX'e `AnalysisButtons` ekle — `KeywordStratejisiToolbar` render edildiği yere yakın:
  ```tsx
  import { AnalysisButtons } from './AnalysisButtons'
  // JSX içinde:
  <AnalysisButtons
    projectId={id}
    keywordCount={keywords?.length ?? 0}
    clusterCount={clusterCount ?? 0}
    isAnalysisRunning={!!activeWorkflow}
  />
  ```

  6. `KeywordStratejisiToolbar`'a `AnalysisButtons`'ı toolbar içine entegre et (VEYA page.tsx'de toolbar'dan sonra render et). Mevcut toolbar yapısını bozmadan separator ile ekle.

  *** KeywordFlatList.tsx değişiklikleri ***

  1. `KeywordRow` type (veya prop type) içine `dfs_fetched_at: string | null` ekle.

  2. `isStale` ve `formatDate` helper fonksiyonlarını dosyanın üstüne ekle:
  ```typescript
  function isStale(dfs_fetched_at: string, ttlDays: number): boolean {
    const diffMs = Date.now() - new Date(dfs_fetched_at).getTime()
    return diffMs > ttlDays * 24 * 60 * 60 * 1000
  }
  function formatDate(iso: string): string {
    return new Date(iso).toLocaleDateString('tr-TR', { day: '2-digit', month: 'short' })
  }
  const DFS_TTL_DAYS = 3 // standard TTL default
  ```

  3. `TableHead` listesine "DFS Tarihi" sütun başlığını "Rol" sütunundan SONRA ekle:
  ```tsx
  <TableHead className="text-xs w-28 py-2 text-cyan-400/70">DFS Tarihi</TableHead>
  ```

  4. Karşılık gelen `TableCell`'i "Rol" hücresinden SONRA ekle:
  ```tsx
  <TableCell className="py-1 w-28">
    {kw.dfs_fetched_at === null ? (
      <Badge className="bg-secondary text-muted-foreground/50 text-[10px] border-0 px-1.5 py-0">
        Veri yok
      </Badge>
    ) : isStale(kw.dfs_fetched_at, DFS_TTL_DAYS) ? (
      <Badge className="bg-amber-500/20 text-amber-400 text-[10px] border-0 px-1.5 py-0">
        Güncel değil
      </Badge>
    ) : (
      <span
        className="text-[11px] text-muted-foreground"
        title={`Son güncelleme: ${kw.dfs_fetched_at} · ${Math.floor((Date.now() - new Date(kw.dfs_fetched_at).getTime()) / (1000*60*60*24))} gün önce`}
      >
        {formatDate(kw.dfs_fetched_at)}
      </span>
    )}
  </TableCell>
  ```

  *** KeywordStratejisiToolbar.tsx değişiklikleri ***

  Mevcut toolbar bileşenini oku. `isAnalysisRunning` prop'unu ekle (AnalysisButtons'a geçirilmesi gerekiyorsa). Ancak eğer page.tsx'de AnalysisButtons doğrudan render ediliyorsa bu dosyaya dokunma gerekmiyor olabilir. Mevcut yapıya göre karar ver:
  - Eğer toolbar JSX döndürüyorsa: AnalysisButtons'ı separator'dan sonra toolbar içine render et
  - Eğer toolbar yalnızca prop arayüzü ise: AnalysisButtons'ı page.tsx'de toolbar'ın yanına ekle

  Her iki durumda da mevcut violet/AI butonlarına DOKUNMA (What Is NOT Changing listesi).

  KRITIK KISITLAR (UI-SPEC "What Is NOT Changing"):
  - KeywordChat.tsx — dokunma
  - AiAcquireButton, AiSuggestButton, AiExpandButton — dokunma
  - ClusterButton, StratejiOnaylaButton — dokunma
  - violet aksan — hiçbir yerde kullanma
  </action>

  <verify>
    <automated>grep -n "dfs_fetched_at" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordFlatList.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `page.tsx`'de `dfs_fetched_at` SSR select string'ine dahil edilmiş: `grep "dfs_fetched_at" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx`
    - `page.tsx`'de `workflow_runs` activeWorkflow sorgusu mevcut: `grep "dfs_deep_analysis" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx`
    - `page.tsx`'de `AnalysisButtons` import ve render ediliyor: `grep "AnalysisButtons" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx`
    - `KeywordFlatList.tsx`'de `dfs_fetched_at` TableCell mevcut: `grep "dfs_fetched_at" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordFlatList.tsx`
    - `KeywordFlatList.tsx`'de "DFS Tarihi" sütun başlığı mevcut: `grep "DFS Tarihi" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordFlatList.tsx`
    - `KeywordFlatList.tsx`'de `text-cyan-400/70` sütun başlığı rengi mevcut
    - `KeywordFlatList.tsx`'de "Veri yok", "Güncel değil" badge'leri mevcut
    - `KeywordFlatList.tsx`'de `enriched_at` kolonuna DOKUNULMADI: mevcut enriched_at hücresi değişmedi
    - TypeScript: build hataları artmıyor (`npx tsc --noEmit 2>&1 | wc -l`)
  </acceptance_criteria>

  <done>page.tsx SSR query güncellendi (dfs_fetched_at + workflow_runs + cluster count), KeywordFlatList'e DFS Tarihi sütunu eklendi, AnalysisButtons toolbar'a entegre edildi</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → lightAnalysisAction | Untrusted projectId input — Server Action |
| Client → standardAnalysisAction | Untrusted projectId + cluster scope |
| Server Action → workflow_runs | Concurrent guard bypass riski — race condition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Race Condition (DoS) | Concurrent guard — iki eş zamanlı Server Action | mitigate | Her action başında `workflow_runs .in('status', ['pending','running'])` check; maybeSingle() — kısa pencere var ama keyword yeniden fetch worst case (budget consume); light/standard için kabul edilebilir risk |
| T-24-01 | Elevation of Privilege | lightAnalysisAction — başka proje keywords'lerini okuma | mitigate | `.eq('project_id', projectId).eq('user_id', user.id)` her Supabase sorgusunda zorunlu |
| T-24-10 | Input Validation | projectId format bypass | mitigate | `/^[0-9a-f-]{36}$/i` UUID regex — action girişinde kontrol ediliyor |
| T-24-11 | DoS | keywords batch bombing (>50) | mitigate | `.limit(50)` — DFS-03 max batch size uygulandı |
</threat_model>

<verification>
```bash
# 1. Server Actions export ediliyor mu?
grep "export async function lightAnalysisAction\|export async function standardAnalysisAction" \
  src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts

# 2. Concurrent guard mevcut mu?
grep "workflow_runs" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts | grep "pending"

# 3. dfs_fetched_at güncellemesi var mı?
grep "dfs_fetched_at" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts

# 4. AnalysisButtons cyan tema doğru mu?
grep "border-cyan-500" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/AnalysisButtons.tsx | wc -l

# 5. DFS Tarihi sütunu KeywordFlatList'te var mı?
grep "DFS Tarihi\|dfs_fetched_at" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordFlatList.tsx

# 6. Test suite yeşil mi?
npx vitest run src/__tests__/lib/dataforseo/

# 7. page.tsx SSR workflow_runs sorgusu var mı?
grep "dfs_deep_analysis" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx
```
</verification>

<success_criteria>
- lightAnalysisAction ve standardAnalysisAction export edildi, getCachedOrFetch üzerinden çalışıyor
- Concurrent guard: workflow_runs pending/running check her iki action'da mevcut
- dfs_fetched_at: analiz sonrası keywords tablosuna yazılıyor
- AnalysisButtons.tsx: 3 cyan buton, cost dialog, concurrent banner, inline result
- KeywordFlatList.tsx: "DFS Tarihi" sütunu — 3 state (never/stale/fresh) tam olarak uygulandı
- enriched_at sütunu DEĞİŞMEDİ
- page.tsx: dfs_fetched_at SSR'a dahil, activeWorkflow sorgusu var, AnalysisButtons render ediliyor
- TypeScript hatası artmadı
- `npx vitest run src/__tests__/lib/dataforseo/analysis.test.ts` parse hatası yok
</success_criteria>

<output>
Tamamlanınca `.planning/phases/24-dataforseo-validation-layer/24-03-SUMMARY.md` oluştur.
</output>
