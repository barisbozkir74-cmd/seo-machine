---
phase: "24"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260606000001_strategy_decisions.sql
  - supabase/migrations/20260606000002_keywords_dfs_fetched_at.sql
autonomous: true
requirements: [DFS-01, DFS-07]

must_haves:
  truths:
    - "strategy_decisions tablosu Supabase'de mevcut: UNIQUE(project_id,module,key), is_locked, is_active kolonları var"
    - "keywords.dfs_fetched_at kolonu mevcut, DEFAULT now() yok — mevcut satırlar NULL kalır"
    - "strategy_decisions'da RLS aktif: kullanıcılar sadece kendi satırlarını okur/yazar"
    - "strategy_decisions migration comment'inde precedence rule belgeli: is_locked=true satırlar keyword_clusters.status'dan öncelikli"
    - "dataforseo_task_cache tablosu DFS-01'in cache deposu olarak kullanılıyor (duplikasyon yok)"
  artifacts:
    - path: "supabase/migrations/20260606000001_strategy_decisions.sql"
      provides: "strategy_decisions DDL + RLS + index"
      contains: "UNIQUE(project_id, module, key)"
    - path: "supabase/migrations/20260606000002_keywords_dfs_fetched_at.sql"
      provides: "keywords.dfs_fetched_at column — no DEFAULT"
      contains: "dfs_fetched_at TIMESTAMPTZ"
  key_links:
    - from: "strategy_decisions"
      to: "keyword_clusters.status"
      via: "is_locked=true precedence rule (application-level enforcement)"
      pattern: "PRECEDENCE RULE.*keyword_clusters"
---

<objective>
Phase 24'ün veritabanı temelini kur: strategy_decisions tablosunu sıfırdan yaz, keywords.dfs_fetched_at kolonunu ekle ve schema push'u tamamla.

Purpose: strategy_decisions tablosu v5.0'ın tüm fazları için (Phase 29 locking UI, Phase 26 clustering guard, Phase 30 starred keywords) temel bağımlılıktır. Bu plan olmadan hiçbir sonraki plan çalışamaz.

Output: 2 migration dosyası + supabase db push tamamlanmış + tablo Supabase'de aktif.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-dataforseo-validation-layer/24-RESEARCH.md
@.planning/phases/24-dataforseo-validation-layer/24-PATTERNS.md

<interfaces>
<!-- Referans: mevcut ai_memory migration — strategy_decisions için tam analog -->
<!-- supabase/migrations/20260512000001_ai_memory.sql -->

Mevcut UNIQUE pattern (ai_memory'den):
```sql
UNIQUE(project_id, module, key)
```

Mevcut RLS pattern (ai_memory'den):
```sql
ALTER TABLE public.ai_memory ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users own their ai_memory"
  ON public.ai_memory FOR ALL
  USING (user_id = auth.uid())
  WITH CHECK (user_id = auth.uid());
```

Mevcut ADD COLUMN pattern (keyword_command_center_fields.sql'den):
```sql
ALTER TABLE public.keywords
  ADD COLUMN IF NOT EXISTS keyword_role TEXT,
  ...
```

workflow_runs mevcut şema (create_tables.sql):
```sql
id, user_id, project_id, workflow_type TEXT, status TEXT DEFAULT 'pending',
input_payload JSONB, result_payload JSONB, error_message TEXT,
started_at, completed_at, created_at, updated_at
```

DFS-01 kararı: dataforseo_task_cache tablosu ZATEN MEVCUT (20260522000001_dataforseo_task_cache.sql).
keyword_data_cache adında ayrı tablo AÇILMAYACAK — duplication riski (RESEARCH.md Pitfall 2).
getCachedOrFetch() wrapper'ı mevcut dataforseo_task_cache'i kullanan fetchWithCache() üzerinde çalışacak.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: strategy_decisions migration yaz</name>
  <files>supabase/migrations/20260606000001_strategy_decisions.sql</files>

  <read_first>
    - supabase/migrations/20260512000001_ai_memory.sql — UNIQUE(project_id,module,key) + RLS + index pattern tam analog
    - .planning/phases/24-dataforseo-validation-layer/24-RESEARCH.md — "strategy_decisions Tablo Tasarımı" code example (tam DDL)
    - .planning/phases/24-dataforseo-validation-layer/24-PATTERNS.md — "supabase/migrations/YYYYMMDD_strategy_decisions.sql" bölümü
  </read_first>

  <action>
  RESEARCH.md "strategy_decisions Tablo Tasarımı" bölümündeki tam DDL'yi kullanarak migration yaz.

  Tam DDL (bu değerleri kullan, değiştirme):
  ```sql
  -- ============================================================
  -- strategy_decisions — v5.0 Keyword Strategy Command Center
  --
  -- PRECEDENCE RULE (v5.0):
  --   is_locked=true satırlar keyword_clusters.status,
  --   arch_status ve keyword_strategy_approved'dan her zaman
  --   önceliklidir. Bu kural uygulama katmanında uygulanır.
  --
  -- module değerleri:
  --   'cluster_priority' | 'primary_keyword' | 'page_type' |
  --   'cannibalization' | 'authority_structure' |
  --   'target_url' | 'starred_keywords'
  -- ============================================================
  CREATE TABLE public.strategy_decisions (
    id          UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id     UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
    project_id  UUID REFERENCES public.projects(id) ON DELETE CASCADE NOT NULL,
    module      TEXT NOT NULL,
    key         TEXT NOT NULL,
    value       JSONB NOT NULL DEFAULT '{}',
    reason      TEXT,
    locked_at   TIMESTAMPTZ,
    locked_by   UUID REFERENCES auth.users(id),
    is_locked   BOOLEAN NOT NULL DEFAULT false,
    is_active   BOOLEAN NOT NULL DEFAULT true,
    created_at  TIMESTAMPTZ DEFAULT now() NOT NULL,
    updated_at  TIMESTAMPTZ DEFAULT now() NOT NULL,
    UNIQUE(project_id, module, key)
  );
  ```

  Ardından RLS ekle (ai_memory.sql pattern):
  ```sql
  ALTER TABLE public.strategy_decisions ENABLE ROW LEVEL SECURITY;

  CREATE POLICY "Users own their strategy_decisions"
    ON public.strategy_decisions
    FOR ALL
    USING (user_id = auth.uid())
    WITH CHECK (user_id = auth.uid());
  ```

  Ardından index'leri ekle:
  ```sql
  CREATE INDEX IF NOT EXISTS idx_strategy_decisions_project_id
    ON public.strategy_decisions(project_id);

  CREATE INDEX IF NOT EXISTS idx_strategy_decisions_project_module
    ON public.strategy_decisions(project_id, module);

  CREATE INDEX IF NOT EXISTS idx_strategy_decisions_locked
    ON public.strategy_decisions(project_id, is_locked)
    WHERE is_locked = true;
  ```

  KRITIK: is_locked index'i WHERE is_locked = true — kilitli kararları hızla sorgulayan Phase 26/29 için.
  </action>

  <verify>
    <automated>grep -c "UNIQUE(project_id, module, key)" supabase/migrations/20260606000001_strategy_decisions.sql</automated>
  </verify>

  <acceptance_criteria>
    - migration dosyası `supabase/migrations/20260606000001_strategy_decisions.sql` yolunda mevcut
    - Dosya `UNIQUE(project_id, module, key)` string'ini içeriyor
    - Dosya `is_locked BOOLEAN NOT NULL DEFAULT false` string'ini içeriyor
    - Dosya `is_active BOOLEAN NOT NULL DEFAULT true` string'ini içeriyor
    - Dosya `PRECEDENCE RULE` string'ini comment'de içeriyor
    - Dosya `ENABLE ROW LEVEL SECURITY` satırını içeriyor
    - Dosya `idx_strategy_decisions_locked` index'ini içeriyor
    - `strategy_decisions` adında tablo oluşturuluyor (ai_memory'e yazılmıyor)
  </acceptance_criteria>

  <done>strategy_decisions migration dosyası tam DDL + RLS + 3 index + precedence rule comment ile yazılmış</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: keywords.dfs_fetched_at migration yaz</name>
  <files>supabase/migrations/20260606000002_keywords_dfs_fetched_at.sql</files>

  <read_first>
    - supabase/migrations/20260520000001_keyword_command_center_fields.sql (veya benzeri ADD COLUMN migration) — ADD COLUMN IF NOT EXISTS pattern
    - .planning/phases/24-dataforseo-validation-layer/24-RESEARCH.md — "keywords.dfs_fetched_at Migration" code example + Pitfall 1
    - .planning/phases/24-dataforseo-validation-layer/24-PATTERNS.md — "supabase/migrations/YYYYMMDD_dfs_fetched_at.sql" bölümü
  </read_first>

  <action>
  Aşağıdaki içerikle migration yaz. DEFAULT now() KESINLIKLE EKLEME — bu CRITICAL Pitfall 1'dir.

  ```sql
  -- keywords.dfs_fetched_at — Phase 24 DataForSEO Validation Layer
  --
  -- NOT: DEFAULT now() kasıtlı olarak eklenmedi.
  -- Mevcut tüm keywordler NULL kalır → UI "Veri yok" badge gösterir.
  -- Bu, stale-data check'in doğru çalışması için zorunludur.
  -- enriched_at kolonundan AYRI — enriched_at "keyword data vardı" anlamına gelir,
  -- dfs_fetched_at "DataForSEO'dan ne zaman çekildi" anlamına gelir.
  ALTER TABLE public.keywords
    ADD COLUMN IF NOT EXISTS dfs_fetched_at TIMESTAMPTZ;

  -- İndeks: stale check sorguları için (project bazlı keyword freshness scan)
  CREATE INDEX IF NOT EXISTS idx_keywords_dfs_fetched_at
    ON public.keywords(project_id, dfs_fetched_at)
    WHERE dfs_fetched_at IS NOT NULL;
  ```
  </action>

  <verify>
    <automated>grep "dfs_fetched_at TIMESTAMPTZ" supabase/migrations/20260606000002_keywords_dfs_fetched_at.sql</automated>
  </verify>

  <acceptance_criteria>
    - Dosya `supabase/migrations/20260606000002_keywords_dfs_fetched_at.sql` yolunda mevcut
    - Dosya `ADD COLUMN IF NOT EXISTS dfs_fetched_at TIMESTAMPTZ` içeriyor
    - Dosya `DEFAULT now()` ifadesi IÇERMIYOR (grep `DEFAULT now()` 0 sonuç döner)
    - Dosya `CREATE INDEX IF NOT EXISTS idx_keywords_dfs_fetched_at` içeriyor
    - Migration comment'inde "enriched_at kolonundan AYRI" açıklaması var
  </acceptance_criteria>

  <done>dfs_fetched_at migration dosyası DEFAULT now() olmadan yazılmış, index eklenmiş</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: [BLOCKING] supabase db push — schema uygula</name>
  <files></files>

  <read_first>
    - supabase/migrations/20260606000001_strategy_decisions.sql — push'lanacak migration 1
    - supabase/migrations/20260606000002_keywords_dfs_fetched_at.sql — push'lanacak migration 2
  </read_first>

  <action>
  Migration'ları Supabase'e uygula:

  ```bash
  npx supabase db push
  ```

  Eğer non-TTY ortamı (CI) gerekiyorsa:
  ```bash
  SUPABASE_ACCESS_TOKEN=$SUPABASE_ACCESS_TOKEN npx supabase db push
  ```

  Push başarılı olduktan sonra tabloların mevcut olduğunu doğrula:
  ```bash
  npx supabase db diff --use-migra
  ```

  Beklenen: her iki migration da uygulandı, diff output'u boş veya sadece mevcut tablolar.

  EĞER PUSH BAŞARISIZ OLURSA:
  - "already exists" hatası: migration timestamp çakışması — dosyayı daha yeni bir timestamp ile yeniden adlandır
  - "permission denied": SUPABASE_ACCESS_TOKEN env var kontrol et
  - "connection refused": supabase start ile local Supabase'i başlat
  </action>

  <verify>
    <automated>npx supabase db push --dry-run 2>&1 | grep -E "No migrations|strategy_decisions|dfs_fetched_at"</automated>
  </verify>

  <acceptance_criteria>
    - `supabase db push` komutu 0 exit code ile tamamlandı
    - Push output'unda "20260606000001_strategy_decisions" ve "20260606000002_keywords_dfs_fetched_at" migration'ları listelendi
    - Supabase Studio'da (veya db diff ile) strategy_decisions ve keywords.dfs_fetched_at görülebilir
  </acceptance_criteria>

  <done>Her iki migration Supabase'e uygulandı, schema canlı</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon key → strategy_decisions | RLS olmadan kullanıcılar birbirinin kilitli kararlarına erişir |
| Service role → strategy_decisions | Deep analysis callback route service role kullanır — RLS bypass; ownership check zorunlu |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Elevation of Privilege | strategy_decisions RLS | mitigate | `ENABLE ROW LEVEL SECURITY` + `USING (user_id = auth.uid())` — migration Task 1'de uygulandı |
| T-24-07 | Tampering | strategy_decisions migration — ai_memory'ye yanlış yazım | mitigate | strategy_decisions ayrı tablo; migration dosya adı ve tablo adı ai_memory'den farklı; PATTERNS.md kontrol noktası |
| T-24-08 | Tampering | dfs_fetched_at DEFAULT now() tuzağı | mitigate | Migration'da DEFAULT koyulmadı; Task 2 acceptance criteria grep ile doğrulanıyor |
</threat_model>

<verification>
Tüm wave 1 doğrulaması:

```bash
# 1. Migration dosyaları mevcut mu?
ls supabase/migrations/20260606000001_strategy_decisions.sql
ls supabase/migrations/20260606000002_keywords_dfs_fetched_at.sql

# 2. strategy_decisions UNIQUE constraint var mı?
grep "UNIQUE(project_id, module, key)" supabase/migrations/20260606000001_strategy_decisions.sql

# 3. dfs_fetched_at DEFAULT now() YOK mu?
grep -c "DEFAULT now()" supabase/migrations/20260606000002_keywords_dfs_fetched_at.sql
# Beklenen çıktı: 0

# 4. RLS mevcut mu?
grep "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260606000001_strategy_decisions.sql

# 5. Push tamamlandı mı?
npx supabase db push --dry-run 2>&1 | head -5
```
</verification>

<success_criteria>
- strategy_decisions tablosu Supabase'de aktif: id, user_id, project_id, module, key, value, reason, locked_at, locked_by, is_locked, is_active, created_at, updated_at kolonları mevcut
- UNIQUE(project_id, module, key) constraint aktif
- RLS politikası aktif: kullanıcılar sadece kendi satırlarını görür
- keywords.dfs_fetched_at kolonu mevcut, tüm mevcut satırlar NULL
- 3 index mevcut: idx_strategy_decisions_project_id, idx_strategy_decisions_project_module, idx_strategy_decisions_locked
- idx_keywords_dfs_fetched_at mevcut
- supabase db push 0 exit code ile tamamlandı
</success_criteria>

<output>
Tamamlanınca `.planning/phases/24-dataforseo-validation-layer/24-01-SUMMARY.md` oluştur.
</output>
