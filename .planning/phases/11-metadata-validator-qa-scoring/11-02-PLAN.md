---
phase: 11-metadata-validator-qa-scoring
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx
  - src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx
autonomous: true
requirements:
  - PAGE-03
  - QUAL-01
  - QUAL-02

must_haves:
  truths:
    - "page.tsx rules engine'den global + project kuralları çeker, resolvedRules olarak PagePackageEditor'a geçirir"
    - "page.tsx'in page_packages SELECT sorgusu qa_scores sütununu içeriyor"
    - "Kilitle butonu artık doğrudan handleStatusChange değil handleLockClick çağırıyor — QA dialog flow başlatır"
    - "Rules ihlali varsa 'Kural İhlalleri Tespit Edildi' diyaloğu açılır; kullanıcı iptal veya devam edebilir"
    - "Claude QA denetimi loading → result → lock sekansıyla tek dialog içinde çalışır"
    - "qa_scores DB'ye yazılıyor: lock işlemi öncesinde updatePagePackage ile content_score + human_score + seo + schema + readiness saklanıyor"
    - "Score Row editör header'ında QaBadge'in altında görünüyor: SEO | İçerik | İnsan | Schema | Hazırlık"
    - "Renk kodlaması: ≥80 emerald-400, 60-79 amber-400, <60 red-400, null muted-foreground"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx"
      provides: "Rules sorgulama + resolvedRules prop + qa_scores SELECT"
      contains: "resolvedRules"
    - path: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx"
      provides: "QA dialog flow + score row + projectRules prop"
      exports: ["PagePackageEditor", "PageData"]
  key_links:
    - from: "page.tsx"
      to: "PagePackageEditor"
      via: "projectRules={resolvedRules} prop"
      pattern: "projectRules.*resolvedRules"
    - from: "PagePackageEditor handleLockClick"
      to: "computeQaRules"
      via: "named import from QaBadge"
      pattern: "import.*computeQaRules.*QaBadge"
    - from: "PagePackageEditor handleConfirmLock"
      to: "updatePagePackage (qa_scores)"
      via: "server action call before updatePackageStatus"
      pattern: "qa_scores.*updatePagePackage"
    - from: "PagePackageEditor proceedToQA"
      to: "/api/ai/qa-audit"
      via: "fetch POST"
      pattern: "fetch.*qa-audit"
---

<objective>
page.tsx'e rules sorgulama ekle ve PagePackageEditor.tsx'i tam QA flow + score row ile güncelle.

Purpose: Phase 11'in kullanıcıya görünür katmanı — rules validator, LLM QA dialog ve 5-boyutlu score gösterimi.

Output:
- page.tsx: rules sorgusu + resolvedRules prop + qa_scores SELECT
- PagePackageEditor.tsx: projectRules prop, QA dialog state makinesi, score row, qa_scores DB yazımı
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-metadata-validator-qa-scoring/11-CONTEXT.md
@.planning/phases/11-metadata-validator-qa-scoring/11-PATTERNS.md
@.planning/phases/11-metadata-validator-qa-scoring/11-UI-SPEC.md
@.planning/phases/11-metadata-validator-qa-scoring/11-01-SUMMARY.md

<interfaces>
<!-- Plan 01 çıktıları — bu planın giriş noktaları -->

From src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx (Plan 01 sonrası):
```typescript
export type QaRule = { id: string; severity: 'warning' | 'error' }

export type QaBadgeProps = {
  seoTitle: string
  metaDescription: string
  h1: string
  focusKeyword: string | null
  slug: string | null            // Plan 01'de eklendi
  projectRules: Record<string, boolean>  // Plan 01'de eklendi
}

export function computeQaRules(props: QaBadgeProps): QaRule[]  // Plan 01'de export edildi
export function QaBadge(props: QaBadgeProps): JSX.Element
```

From src/app/api/ai/qa-audit/route.ts (Plan 01'de oluşturuldu):
```typescript
// POST /api/ai/qa-audit
// Request:  { packageId: string, projectId: string }
// Response: { checks: QaCheck[], content_score: number, human_score: number }
// QaCheck:  { id: 'intent_drift'|'robotic_language'|'entity_gap'|'duplicate_risk', severity: 'ok'|'warning'|'critical', note: string }
```

From src/lib/rules/rule-meta.ts:
```typescript
export const RULE_META: Record<string, { label: string; recommended: boolean; category: string }>
// 12 kural: title_starts_with_keyword, title_max_length_enforced, title_includes_brand,
//            h1_exact_match, h1_single_per_page, h1_includes_keyword,
//            slug_exact_match, slug_lowercase_hyphen, slug_no_stopwords,
//            meta_desc_required, meta_desc_includes_keyword, meta_desc_length_enforced
```

From src/app/(dashboard)/projeler/[id]/kurallar/page.tsx (rules sorgu pattern — ANALOG):
```typescript
// Global kurallar sorgusu:
const { data: globalRules } = await supabase
  .from('rules').select('rule_key, rule_value')
  .eq('user_id', user.id).eq('scope', 'global').is('project_id', null)

// Proje override'ları:
const { data: projectRules } = await supabase
  .from('rules').select('rule_key, rule_value')
  .eq('user_id', user.id).eq('project_id', id).eq('scope', 'project')

// Merge:
const resolvedRules: Record<string, boolean> = Object.fromEntries(
  Object.keys(RULE_META).map((ruleKey) => {
    const hasOverride = ruleKey in projectOverrides
    const value = hasOverride
      ? projectOverrides[ruleKey] === 'true'
      : (globalValues[ruleKey] ?? 'true') === 'true'
    return [ruleKey, value]
  })
)
```

From src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx (MEVCUT):
```typescript
// Bileşen imzası (genişletilecek):
export function PagePackageEditor({ projectId, page }: { projectId: string; page: PageData })

// PageData tipi (genişletilecek):
export type PageData = {
  id: string; title: string; slug: string | null; page_type: string | null;
  focus_keyword_id: string | null; focus_keyword_text?: string | null;
  pkg?: { id: string; status: string; ... schema_jsonld: unknown } | null
  // qa_scores henüz yok — bu planda PageData'ya eklenir
}

// Mevcut kilitleme (değiştirilecek):
// {pkg?.status === 'approved' && <Button onClick={() => handleStatusChange('locked')}>Kilitle</Button>}

// Dialog pattern (base-ui render prop — zorunlu):
<Dialog><DialogTrigger render={<Button>...</Button>} />
  <DialogContent showCloseButton={false} className="max-w-sm">
    <DialogClose render={<Button>...</Button>} />
  </DialogContent>
</Dialog>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx — rules sorgulama + resolvedRules prop + qa_scores SELECT</name>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx (tam dosyayı oku)
    - src/app/(dashboard)/projeler/[id]/kurallar/page.tsx (rules sorgu pattern — satır 37-71)
    - src/lib/rules/rule-meta.ts (RULE_META key listesi)
    - .planning/phases/11-metadata-validator-qa-scoring/11-PATTERNS.md (page.tsx bölümü)
  </read_first>

  <files>src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx</files>

  <action>
Mevcut sayfa-paketi/page.tsx'e şu değişiklikleri uygula:

**1. Import ekle** (dosyanın üstüne, createClient import'unun altına):
```typescript
import { RULE_META } from '@/lib/rules/rule-meta'
```

**2. Rules sorgulama** — `user` ve `project` doğrulaması yapıldıktan hemen SONRA, `pagesRaw` sorgusundan ÖNCE şu bloğu ekle:

```typescript
// Rules Engine — global + proje override'ları
const { data: globalRulesData } = await supabase
  .from('rules')
  .select('rule_key, rule_value')
  .eq('user_id', user.id)
  .eq('scope', 'global')
  .is('project_id', null)

const { data: projectRulesData } = await supabase
  .from('rules')
  .select('rule_key, rule_value')
  .eq('user_id', user.id)
  .eq('project_id', id)
  .eq('scope', 'project')

const projectOverrides = Object.fromEntries(
  (projectRulesData ?? []).map((r) => [r.rule_key, r.rule_value])
)
const globalValues = Object.fromEntries(
  (globalRulesData ?? []).map((r) => [r.rule_key, r.rule_value])
)

const resolvedRules: Record<string, boolean> = Object.fromEntries(
  Object.keys(RULE_META).map((ruleKey) => {
    const hasOverride = ruleKey in projectOverrides
    const value = hasOverride
      ? projectOverrides[ruleKey] === 'true'
      : (globalValues[ruleKey] ?? 'true') === 'true'
    return [ruleKey, value]
  })
)
```

**3. page_packages SELECT sorgusu** — mevcut satır (yaklaşık satır 104):
```typescript
'id, status, generated_by, seo_title, meta_description, h1, slug, search_intent, strategic_purpose, secondary_keywords, heading_hierarchy, content_blocks, cta_blocks, image_plan, alt_texts, schema_type, canonical_url, faq, schema_jsonld'
```
Bu satıra `qa_scores` ekle:
```typescript
'id, status, generated_by, seo_title, meta_description, h1, slug, search_intent, strategic_purpose, secondary_keywords, heading_hierarchy, content_blocks, cta_blocks, image_plan, alt_texts, schema_type, canonical_url, faq, schema_jsonld, qa_scores'
```

**4. PagePackageEditor çağrısı** — mevcut:
```tsx
<PagePackageEditor projectId={id} page={selectedPageData} />
```
Şu şekilde güncelle:
```tsx
<PagePackageEditor projectId={id} page={selectedPageData} projectRules={resolvedRules} />
```
</action>

  <verify>
    <automated>cd C:/Users/baris/Documents/seo-machine && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "resolvedRules" src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx` → en az 2 satır (tanım + prop geçme)
    - `grep -n "RULE_META" src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx` → import var
    - `grep -n "qa_scores" src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx` → SELECT sorgusunda qa_scores mevcut
    - `grep -n "projectRules.*resolvedRules" src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx` → prop geçiriliyor
    - `grep -n "scope.*global\|scope.*project" src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx` → iki kural sorgusu var
    - `npx tsc --noEmit` → page.tsx kaynaklı TypeScript hatası yok
  </acceptance_criteria>

  <done>
page.tsx: RULE_META import eklendi, global + project rules sorgusu eklendi, resolvedRules hesaplandı, qa_scores SELECT'e eklendi, PagePackageEditor çağrısına projectRules prop geçirildi. TypeScript hatası yok.
  </done>
</task>

<task type="auto">
  <name>Task 2: PagePackageEditor.tsx — projectRules prop + QA dialog state makinesi + score row + qa_scores kaydetme</name>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx (tam dosyayı oku — mevcut Dialog pattern, handleStatusChange, QaBadge çağrısı, header bölgesi)
    - .planning/phases/11-metadata-validator-qa-scoring/11-PATTERNS.md (PagePackageEditor pattern bölümü — state tanımları, handleLockClick, proceedToQA, score row render)
    - .planning/phases/11-metadata-validator-qa-scoring/11-UI-SPEC.md (dialog copy, renk kodlaması, score row HTML yapısı)
    - .planning/phases/11-metadata-validator-qa-scoring/11-CONTEXT.md (D-01, D-03, D-04, D-05)
  </read_first>

  <files>src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx</files>

  <action>
Mevcut PagePackageEditor.tsx'e şu değişiklikleri uygula (dosyayı sıfırdan yazma — tüm mevcut mantık korunur):

---

**ADIM 1 — Import güncellemesi:**

Mevcut QaBadge import satırı:
```typescript
import { QaBadge } from './QaBadge'
```
Şu şekilde güncelle:
```typescript
import { QaBadge, computeQaRules, type QaRule } from './QaBadge'
```

---

**ADIM 2 — PageData tipine qa_scores ekle** (mevcut `schema_jsonld: unknown` satırından sonra):
```typescript
qa_scores?: {
  seo?: number | null
  metadata?: number | null
  content?: number | null
  human?: number | null
  schema?: number | null
  readiness?: number | null
  last_qa_run?: string | null
} | null
```

---

**ADIM 3 — Bileşen imzasına projectRules prop ekle:**

Mevcut:
```typescript
export function PagePackageEditor({
  projectId,
  page,
}: {
  projectId: string
  page: PageData
})
```
Şu şekilde güncelle:
```typescript
export function PagePackageEditor({
  projectId,
  page,
  projectRules,
}: {
  projectId: string
  page: PageData
  projectRules: Record<string, boolean>
})
```

---

**ADIM 4 — Yeni type tanımları** (bileşen fonksiyonunun ÜSTÜNE, PageData tipinden sonra):
```typescript
type QaCheck = {
  id: 'intent_drift' | 'robotic_language' | 'entity_gap' | 'duplicate_risk'
  severity: 'ok' | 'warning' | 'critical'
  note: string
}

type QaResult = {
  checks: QaCheck[]
  content_score: number
  human_score: number
}
```

---

**ADIM 5 — Yeni state tanımları** (bileşen fonksiyonu içinde, mevcut state'lerin sonuna):
```typescript
// QA Dialog state
const [qaDialogOpen, setQaDialogOpen] = useState(false)
const [qaDialogPhase, setQaDialogPhase] = useState<'rules' | 'loading' | 'result' | 'error'>('rules')
const [ruleViolations, setRuleViolations] = useState<QaRule[]>([])
const [qaResult, setQaResult] = useState<QaResult | null>(null)

// Score row state — DB'den gelen qa_scores ile başlar
const [qaScores, setQaScores] = useState(pkg?.qa_scores ?? null)
```

---

**ADIM 6 — Yardımcı fonksiyonlar** (handleSave fonksiyonundan ÖNCE):

```typescript
function scoreColor(n: number | null | undefined): string {
  if (n == null) return 'text-muted-foreground'
  if (n >= 80) return 'text-emerald-400'
  if (n >= 60) return 'text-amber-400'
  return 'text-red-400'
}

function computeSchemaScore(schemaJsonLdVal: string): number {
  if (!schemaJsonLdVal.trim()) return 0
  try {
    JSON.parse(schemaJsonLdVal)
    return 100
  } catch {
    return 50
  }
}

function computeSeoScore(violations: QaRule[]): number {
  // Her kural ihlali için düşüş: error = 20 puan, warning = 10 puan
  // Başlangıç: 100
  const errorCount = violations.filter((v) => v.severity === 'error').length
  const warningCount = violations.filter((v) => v.severity === 'warning').length
  return Math.max(0, 100 - errorCount * 20 - warningCount * 10)
}

function computeMetadataScore(
  seoTitleVal: string,
  metaDescVal: string,
  h1Val: string
): number {
  // 4 alan: seoTitle dolu, metaDescription dolu, h1 dolu, meta uzunluk uygun
  let score = 100
  if (!seoTitleVal.trim()) score -= 25
  if (!metaDescVal.trim()) score -= 25
  if (!h1Val.trim()) score -= 25
  if (metaDescVal.length > 160 || (metaDescVal.length > 0 && metaDescVal.length < 120)) score -= 25
  return Math.max(0, score)
}

async function proceedToQA() {
  setQaDialogPhase('loading')
  setQaDialogOpen(true)
  try {
    const res = await fetch('/api/ai/qa-audit', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ packageId: pkg!.id, projectId }),
    })
    if (!res.ok) throw new Error(`HTTP ${res.status}`)
    const data = await res.json() as QaResult
    setQaResult(data)

    // Anlık skor hesapla (DB'ye henüz yazmıyor — lock'ta yazılacak)
    const currentViolations = computeQaRules({
      seoTitle,
      metaDescription,
      h1,
      focusKeyword: page.focus_keyword_text ?? null,
      slug,
      projectRules,
    })
    const seoScore = computeSeoScore(currentViolations)
    const metadataScore = computeMetadataScore(seoTitle, metaDescription, h1)
    const schemaScore = computeSchemaScore(schemaJsonLd)
    const contentScore = data.content_score
    const humanScore = data.human_score
    const readiness = Math.round((seoScore + metadataScore + contentScore + humanScore + schemaScore) / 5)

    setQaScores({
      seo: seoScore,
      metadata: metadataScore,
      content: contentScore,
      human: humanScore,
      schema: schemaScore,
      readiness,
      last_qa_run: new Date().toISOString(),
    })

    setQaDialogPhase('result')
  } catch {
    setQaDialogPhase('error')
  }
}

async function handleLockClick() {
  if (!pkg?.id) return
  // 1. Client-side rules check
  const violations = computeQaRules({
    seoTitle,
    metaDescription,
    h1,
    focusKeyword: page.focus_keyword_text ?? null,
    slug,
    projectRules,
  })
  if (violations.length > 0) {
    setRuleViolations(violations)
    setQaDialogPhase('rules')
    setQaDialogOpen(true)
    return
  }
  // 2. İhlal yok — doğrudan QA'ye git
  await proceedToQA()
}

async function handleConfirmLock() {
  if (!pkg?.id) return
  startTransition(async () => {
    // Önce qa_scores kaydet (QA sonucu varsa)
    if (qaResult && qaScores) {
      await updatePagePackage(projectId, page.id, { qa_scores: qaScores })
    }
    // Sonra status geçişi
    const result = await updatePackageStatus(projectId, pkg.id, 'locked')
    if (result.success) {
      setQaDialogOpen(false)
      router.refresh()
    } else {
      setSaveStatus('error')
      setErrorMsg(result.error)
    }
  })
}
```

---

**ADIM 7 — QaBadge çağrısını güncelle** — mevcut:
```tsx
<QaBadge
  seoTitle={seoTitle}
  metaDescription={metaDescription}
  h1={h1}
  focusKeyword={page.focus_keyword_text ?? null}
/>
```
Şu şekilde güncelle:
```tsx
<QaBadge
  seoTitle={seoTitle}
  metaDescription={metaDescription}
  h1={h1}
  focusKeyword={page.focus_keyword_text ?? null}
  slug={slug}
  projectRules={projectRules}
/>
```

---

**ADIM 8 — Score Row** — QaBadge + PackageStatusBadge satırının hemen ALTINA (aynı `space-y-2` div içinde):

```tsx
{pkg !== null && (
  <div className="flex items-center gap-1.5 text-[10px] text-muted-foreground">
    <span>SEO <span className={scoreColor(qaScores?.seo)}>{qaScores?.seo ?? '—'}</span></span>
    <span className="text-muted-foreground/40">|</span>
    <span>İçerik <span className={scoreColor(qaScores?.content)}>{qaScores?.content ?? '—'}</span></span>
    <span className="text-muted-foreground/40">|</span>
    <span>İnsan <span className={scoreColor(qaScores?.human)}>{qaScores?.human ?? '—'}</span></span>
    <span className="text-muted-foreground/40">|</span>
    <span>Schema <span className={scoreColor(qaScores?.schema)}>{qaScores?.schema ?? '—'}</span></span>
    <span className="text-muted-foreground/40">|</span>
    <span>Hazırlık <span className={scoreColor(qaScores?.readiness)}>{qaScores?.readiness ?? '—'}</span></span>
    {qaScores?.last_qa_run && (
      <>
        <span className="text-muted-foreground/40">|</span>
        <span>Son denetim: {new Date(qaScores.last_qa_run).toLocaleTimeString('tr-TR', { hour: '2-digit', minute: '2-digit' })}</span>
      </>
    )}
  </div>
)}
```

---

**ADIM 9 — Kilitle butonunu handleLockClick'e bağla** — approved state'teki mevcut:
```tsx
<Button size="sm" onClick={() => handleStatusChange('locked')} disabled={isPending}>
  Kilitle
</Button>
```
Şu şekilde değiştir:
```tsx
<Button size="sm" onClick={handleLockClick} disabled={isPending}>
  Kilitle
</Button>
```

---

**ADIM 10 — QA Dialog** — locked Dialog'un hemen SONRASINA (return içinde, aksiyon butonları div'inin kapanışından sonra) şu controlled Dialog'u ekle:

```tsx
{/* QA Dialog — tek dialog, phase'a göre içerik değişir */}
<Dialog open={qaDialogOpen} onOpenChange={setQaDialogOpen}>
  <DialogContent showCloseButton={false} className="max-w-sm">

    {/* Phase: rules — kural ihlalleri */}
    {qaDialogPhase === 'rules' && (
      <>
        <DialogTitle>Kural İhlalleri Tespit Edildi</DialogTitle>
        <DialogDescription>
          Aşağıdaki kural ihlalleri bulundu. Yine de kilitlemek istersen onaylayabilirsin.
        </DialogDescription>
        <div className="space-y-1 my-2">
          {ruleViolations.map((v) => (
            <div key={v.id} className="flex items-center gap-1.5 text-xs">
              <span className={v.severity === 'error' ? 'text-red-400' : 'text-amber-400'}>
                {v.severity === 'error' ? '✕' : '⚠'}
              </span>
              <span className="text-foreground/80">{v.id.replace(/_/g, ' ')}</span>
            </div>
          ))}
        </div>
        <div className="flex gap-2 justify-end mt-2">
          <DialogClose render={<Button variant="ghost" size="sm">İptal</Button>} />
          <Button size="sm" onClick={proceedToQA}>
            Anlıyorum, yine de kilitle
          </Button>
        </div>
      </>
    )}

    {/* Phase: loading — Claude analiz ediyor */}
    {qaDialogPhase === 'loading' && (
      <>
        <DialogTitle>Kalite Denetimi</DialogTitle>
        <div className="flex flex-col items-center gap-3 py-4">
          <div className="w-5 h-5 rounded-full border-2 border-muted border-t-foreground animate-spin" />
          <p className="text-sm text-muted-foreground text-center">Claude analiz ediyor...</p>
        </div>
      </>
    )}

    {/* Phase: result — QA sonuçları */}
    {qaDialogPhase === 'result' && qaResult && (
      <>
        <DialogTitle>Kalite Denetimi Sonucu</DialogTitle>
        <div className="space-y-2 my-2">
          {qaResult.checks.map((check) => {
            const labelMap: Record<string, string> = {
              intent_drift: 'Intent Uyumu',
              robotic_language: 'Robotik Dil Kontrolü',
              entity_gap: 'Entity Kapsamı',
              duplicate_risk: 'Duplicate Risk',
            }
            const icon = check.severity === 'ok' ? '✓' : check.severity === 'warning' ? '⚠' : '✕'
            const cls = check.severity === 'ok' ? 'text-emerald-400' : check.severity === 'warning' ? 'text-amber-400' : 'text-red-400'
            return (
              <div key={check.id} className="space-y-0.5">
                <div className="flex items-center gap-2 text-sm">
                  <span className={cls}>{icon}</span>
                  <span className={cls}>{labelMap[check.id] ?? check.id}</span>
                </div>
                {check.note && (
                  <p className="text-xs text-muted-foreground pl-5">{check.note}</p>
                )}
              </div>
            )
          })}
        </div>
        <div className="flex gap-2 justify-end mt-2">
          <DialogClose render={<Button variant="ghost" size="sm">İptal</Button>} />
          <Button size="sm" onClick={handleConfirmLock} disabled={isPending}>
            Anlayarak Kilitle
          </Button>
        </div>
      </>
    )}

    {/* Phase: error — denetim hatası */}
    {qaDialogPhase === 'error' && (
      <>
        <DialogTitle>Kalite Denetimi</DialogTitle>
        <p className="text-sm text-red-400 my-2">
          Denetim sırasında hata oluştu. Yine de kilitlemek istersen devam edebilirsin.
        </p>
        <div className="flex gap-2 justify-end">
          <DialogClose render={<Button variant="ghost" size="sm">İptal</Button>} />
          <Button size="sm" onClick={handleConfirmLock} disabled={isPending}>
            Yine de Kilitle
          </Button>
        </div>
      </>
    )}

  </DialogContent>
</Dialog>
```

---

**UI constraints (zorunlu):**
- `font-medium` YASAK — yeni eklenen hiçbir element için kullanılmaz
- `variant` prop YASAK Badge'de — renk className ile
- `DialogTrigger` ve `DialogClose` → `render={}` prop, `asChild` değil
- Tüm severity renkleri: `text-emerald-400` / `text-amber-400` / `text-red-400` (CSS var değil)
</action>

  <verify>
    <automated>cd C:/Users/baris/Documents/seo-machine && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "projectRules" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → en az 4 satır (prop tanımı, QaBadge çağrısı, computeQaRules çağrısı x2)
    - `grep -n "computeQaRules" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → import'ta ve en az 2 çağrıda var
    - `grep -n "qaDialogOpen\|qaDialogPhase\|qaResult\|ruleViolations" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → 4 state var
    - `grep -n "qaScores" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → score state var + score row render
    - `grep -n "handleLockClick" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → fonksiyon tanımı + onClick bağlantısı
    - `grep -n "proceedToQA\|qa-audit" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → QA API call var
    - `grep -n "Kural İhlalleri\|Kalite Denetimi\|Anlayarak Kilitle\|Anlıyorum, yine de kilitle" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → 4 copy string var (UI-SPEC'e uygun)
    - `grep -n "scoreColor\|text-emerald-400\|text-amber-400\|text-red-400" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → renk fonksiyonu + kullanım var
    - `grep -n "font-medium" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → eşleşme YOK
    - `grep -n "qa_scores.*updatePagePackage\|updatePagePackage.*qa_scores" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → handleConfirmLock'ta qa_scores DB'ye yazılıyor
    - `grep -n "qa_scores" src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx` → PageData tipinde qa_scores field var
    - `npx tsc --noEmit` → PagePackageEditor.tsx kaynaklı TypeScript hatası yok
  </acceptance_criteria>

  <done>
PagePackageEditor.tsx: projectRules prop eklendi, QaCheck + QaResult tipleri eklendi, 5 yeni state eklendi (qaDialogOpen, qaDialogPhase, ruleViolations, qaResult, qaScores), scoreColor + computeSchemaScore + computeSeoScore + computeMetadataScore + proceedToQA + handleLockClick + handleConfirmLock fonksiyonları eklendi, QaBadge çağrısı güncellendi (slug + projectRules), score row render edildi, Kilitle butonu handleLockClick'e bağlandı, 4-state QA dialog eklendi, handleConfirmLock qa_scores'u DB'ye yazıyor. TypeScript hatası yok.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page.tsx (server) → PagePackageEditor (client) | resolvedRules prop client bundle'a geçiyor — kural değerleri boolean, hassas veri değil |
| client → /api/ai/qa-audit | fetch POST — Plan 01'de auth + ownership koruması mevcut |
| client → Server Action updatePagePackage | qa_scores kaydı — mevcut verifyOwnership pattern koruyor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Elevation of Privilege | PagePackageEditor handleConfirmLock → updatePagePackage | mitigate | `verifyOwnership(supabase, projectId, user.id)` server action'da mevcut; client-side `pkg.id` manipülasyonu DB'de user_id ownership ile engellenir |
| T-11-07 | Tampering | QA Dialog — user proceeds despite critical findings | accept | "Sert blok yok" D-03 kararı; kullanıcı içeriğine sahip çıkma sistemi; low risk for internal tool |
| T-11-08 | Information Disclosure | resolvedRules client prop | accept | Kural değerleri (boolean) hassas değil; kullanıcı zaten kendi kurallarını görebiliyor |
| T-11-09 | Denial of Service | qaScores state client'ta sonsuz QA döngüsü | accept | QA sadece "Kilitle" akışında tetikleniyor; tek kullanıcı iç araç; tekrar çalıştırma manuel |
</threat_model>

<verification>
## Phase 11 Plan 02 Doğrulama

```bash
# TypeScript derleme
cd C:/Users/baris/Documents/seo-machine && npx tsc --noEmit

# page.tsx değişiklikleri
grep -n "resolvedRules\|RULE_META\|qa_scores\|projectRules" \
  "src/app/(dashboard)/projeler/[id]/sayfa-paketi/page.tsx"

# PagePackageEditor yeni state ve fonksiyonlar
grep -n "qaDialogOpen\|qaDialogPhase\|handleLockClick\|proceedToQA\|handleConfirmLock\|scoreColor" \
  "src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx"

# UI copy doğrulama
grep -n "Kural İhlalleri\|Kalite Denetimi\|Anlayarak Kilitle\|Claude analiz" \
  "src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx"

# Yasaklı pattern kontrolü
grep -n "font-medium" \
  "src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx" || echo "PASS"
grep -n "asChild" \
  "src/app/(dashboard)/projeler/[id]/sayfa-paketi/PagePackageEditor.tsx" || echo "PASS"
```

## Uçtan Uca Akış Doğrulaması

```bash
# Uygulama build kontrolü
cd C:/Users/baris/Documents/seo-machine && npm run build 2>&1 | tail -20
```
</verification>

<success_criteria>
- page.tsx: RULE_META import'u var, global + project rules sorgusu var, resolvedRules hesaplandı, qa_scores SELECT'te var, PagePackageEditor çağrısında projectRules={resolvedRules} var
- PagePackageEditor.tsx: projectRules prop var, PageData.qa_scores field var, 4 yeni fonksiyon var (handleLockClick, proceedToQA, handleConfirmLock, scoreColor), QA dialog 4 state (rules/loading/result/error), score row render var, Kilitle butonu handleLockClick'e bağlı, handleConfirmLock qa_scores DB'ye yazıyor
- UI-SPEC copy: "Kural İhlalleri Tespit Edildi", "Kalite Denetimi", "Kalite Denetimi Sonucu", "Anlayarak Kilitle", "Anlıyorum, yine de kilitle", "Claude analiz ediyor..." — tümü mevcut
- `npx tsc --noEmit` sıfır hata
- `npm run build` başarılı
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/11-metadata-validator-qa-scoring/11-02-SUMMARY.md` dosyasını oluştur.
</output>
