---
phase: 11-metadata-validator-qa-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx
  - src/app/api/ai/qa-audit/route.ts
autonomous: true
requirements:
  - PAGE-03
  - QUAL-01

must_haves:
  truths:
    - "QaBadge, projectRules prop alır ve aktif rules engine kurallarını hardcoded kurallarla birleştirerek değerlendirir"
    - "computeQaRules fonksiyonu QaBadge.tsx'ten export edilir — PagePackageEditor lock flow tarafından tüketilir"
    - "POST /api/ai/qa-audit endpoint'i auth + ownership doğrulaması yaparak structured JSON QA sonucu döndürür"
    - "Endpoint sadece authenticated user'ın kendi page_packages'larını QA eder — başkasının paketine erişim 404 döner"
    - "Claude'a gönderilen prompt injection'a karşı korumalı — user-controlled içerik template içine gömülür, instruction olarak yorumlanamaz"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx"
      provides: "Genişletilmiş QaBadge — projectRules prop + computeQaRules export"
      exports: ["QaBadge", "computeQaRules", "QaBadgeProps", "QaRule"]
    - path: "src/app/api/ai/qa-audit/route.ts"
      provides: "Claude QA audit endpoint — non-streaming JSON response"
      exports: ["POST"]
  key_links:
    - from: "PagePackageEditor.tsx"
      to: "computeQaRules (QaBadge.tsx)"
      via: "named export import"
      pattern: "import.*computeQaRules.*QaBadge"
    - from: "PagePackageEditor.tsx"
      to: "/api/ai/qa-audit"
      via: "fetch POST"
      pattern: "fetch.*qa-audit"
    - from: "src/app/api/ai/qa-audit/route.ts"
      to: "supabase projects + page_packages"
      via: "server-side ownership check"
      pattern: "eq.*user_id.*user\\.id"
---

<objective>
QaBadge.tsx'i rules engine entegrasyonuyla genişlet ve yeni /api/ai/qa-audit endpoint'ini oluştur.

Purpose: Metadata validator (PAGE-03) ve LLM QA denetiminin (QUAL-01) altyapı katmanı — hem client-side rules validation hem de Claude'a bağlanan API route bu planda tamamlanır.

Output:
- Genişletilmiş QaBadge.tsx (projectRules prop + computeQaRules export)
- Yeni src/app/api/ai/qa-audit/route.ts (non-streaming Anthropic call + ownership check)
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-metadata-validator-qa-scoring/11-CONTEXT.md
@.planning/phases/11-metadata-validator-qa-scoring/11-PATTERNS.md
@.planning/phases/11-metadata-validator-qa-scoring/11-UI-SPEC.md

<interfaces>
<!-- Mevcut dosyaların anahtar export'ları — okumadan önce bu bölümü referans al -->

From src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx (CURRENT — to be extended):
```typescript
'use client'
import { cn } from '@/lib/utils'

type QaRule = {
  id: string
  severity: 'warning' | 'error'
}

export type QaBadgeProps = {
  seoTitle: string
  metaDescription: string
  h1: string
  focusKeyword: string | null
  // slug: string | null   ← HENÜZ YOK, bu planda eklenecek
}

function computeQaRules(props: QaBadgeProps): QaRule[]  // HENÜZ iç fonksiyon, export edilecek

export function QaBadge(props: QaBadgeProps): JSX.Element
```

From src/lib/rules/rule-meta.ts:
```typescript
export const RULE_META: Record<string, { label: string; recommended: boolean; category: string }> = {
  title_starts_with_keyword: { label: '...', recommended: true, category: 'SEO Title' },
  title_max_length_enforced: { label: '...', recommended: true, category: 'SEO Title' },
  title_includes_brand:      { label: '...', recommended: false, category: 'SEO Title' },
  h1_exact_match:            { label: '...', recommended: false, category: 'H1' },
  h1_single_per_page:        { label: '...', recommended: true, category: 'H1' },
  h1_includes_keyword:       { label: '...', recommended: true, category: 'H1' },
  slug_exact_match:          { label: '...', recommended: false, category: 'Slug' },
  slug_lowercase_hyphen:     { label: '...', recommended: true, category: 'Slug' },
  slug_no_stopwords:         { label: '...', recommended: false, category: 'Slug' },
  meta_desc_required:        { label: '...', recommended: true, category: 'Meta Description' },
  meta_desc_includes_keyword:{ label: '...', recommended: true, category: 'Meta Description' },
  meta_desc_length_enforced: { label: '...', recommended: true, category: 'Meta Description' },
}
```

From src/app/api/ai/generate-page-package/route.ts (ANALOG — follow this pattern):
```typescript
import { NextRequest } from 'next/server'
import Anthropic from '@anthropic-ai/sdk'
import { createClient } from '@/lib/supabase/server'

const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })

export async function POST(req: NextRequest) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return new Response('Unauthorized', { status: 401 })
  // ... body parse → ownership check → DB fetch → Anthropic call
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QaBadge.tsx — projectRules prop + rules engine entegrasyonu + computeQaRules export</name>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx (tam dosyayı oku — mevcut computeQaRules mantığını koru)
    - src/lib/rules/rule-meta.ts (rule_key isimlerini doğrula — slug, h1, title, meta key'leri)
    - .planning/phases/11-metadata-validator-qa-scoring/11-CONTEXT.md (D-02 kararı)
    - .planning/phases/11-metadata-validator-qa-scoring/11-PATTERNS.md (QaBadge pattern bölümü)
  </read_first>

  <files>src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx</files>

  <action>
Mevcut QaBadge.tsx'i şu değişikliklerle güncelle (sıfırdan yazma — mevcut hardcoded kurallar korunur):

**1. Tip genişletmeleri:**

```typescript
type ProjectRules = Record<string, boolean>  // rule_key → aktif mi

export type QaBadgeProps = {
  seoTitle: string
  metaDescription: string
  h1: string
  focusKeyword: string | null
  slug: string | null           // YENİ — slug_lowercase_hyphen için
  projectRules: ProjectRules    // YENİ — rules engine kararları
}
```

**2. computeQaRules — mevcut 4 kural korunur, sonuna 7 rules-engine kuralı eklenir:**

Fonksiyon imzasını `export` yap: `export function computeQaRules(props: QaBadgeProps): QaRule[]`

Mevcut 4 kural bloğundan sonra (return'den önce) şunları ekle:

```typescript
// Rules Engine entegrasyonu — yalnızca aktif kurallar değerlendirilir
if (props.projectRules['title_starts_with_keyword'] && props.focusKeyword) {
  if (!props.seoTitle.toLowerCase().startsWith(props.focusKeyword.toLowerCase())) {
    rules.push({ id: 'title_starts_with_keyword', severity: 'warning' })
  }
}

if (props.projectRules['title_max_length_enforced']) {
  if (props.seoTitle.length > 60) {
    rules.push({ id: 'title_max_length_enforced', severity: 'error' })
  }
}

if (props.projectRules['title_includes_brand']) {
  // brand name kontrolü — client context'te mevcut değil, skip (no-op for now)
}

if (props.projectRules['h1_includes_keyword'] && props.focusKeyword) {
  if (!props.h1.toLowerCase().includes(props.focusKeyword.toLowerCase())) {
    rules.push({ id: 'h1_includes_keyword', severity: 'warning' })
  }
}

if (props.projectRules['h1_exact_match'] && props.focusKeyword) {
  if (props.h1.toLowerCase() !== props.focusKeyword.toLowerCase()) {
    rules.push({ id: 'h1_exact_match', severity: 'warning' })
  }
}

if (props.projectRules['slug_lowercase_hyphen'] && props.slug) {
  if (!/^[a-z0-9-]+$/.test(props.slug)) {
    rules.push({ id: 'slug_lowercase_hyphen', severity: 'error' })
  }
}

if (props.projectRules['meta_desc_includes_keyword'] && props.focusKeyword) {
  if (!props.metaDescription.toLowerCase().includes(props.focusKeyword.toLowerCase())) {
    rules.push({ id: 'meta_desc_includes_keyword', severity: 'warning' })
  }
}

if (props.projectRules['meta_desc_required']) {
  if (!props.metaDescription.trim()) {
    rules.push({ id: 'meta_desc_required', severity: 'error' })
  }
}

if (props.projectRules['meta_desc_length_enforced']) {
  const len = props.metaDescription.length
  if (len > 0 && (len > 160 || len < 120)) {
    rules.push({ id: 'meta_desc_length_enforced', severity: 'warning' })
  }
}
```

**3. QaBadge render — değişmez.** Sadece `computeQaRules(props)` çağrısı yeni prop'larla otomatik çalışır.

**Not:** `title_includes_brand` ve `h1_single_per_page` kuralları bu bileşende uygulanamaz (client'ta brand name ve DOM H1 sayısı yok) — bu key'ler projectRules'ta true olsa bile no-op olarak bırakılır.

**UI constraints (zorunlu):**
- `font-medium` YASAK
- `variant` prop YASAK — renk className ile
</action>

  <verify>
    <automated>cd C:/Users/baris/Documents/seo-machine && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "export function computeQaRules" src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx` → satır numarasıyla eşleşme döner (export edildi)
    - `grep -n "projectRules" src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx` → en az 3 satırda görünür (tip tanımı + kurallar + fonksiyon içi)
    - `grep -n "slug: string | null" src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx` → slug field var
    - `grep -n "slug_lowercase_hyphen" src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx` → rules engine bloku var
    - `grep -n "meta_desc_includes_keyword\|meta_desc_required\|meta_desc_length_enforced" src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx` → 3 meta kural var
    - `grep -n "title_starts_with_keyword\|title_max_length_enforced" src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx` → 2 title kural var
    - `grep -n "font-medium" src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx` → eşleşme YOK
    - `npx tsc --noEmit` → QaBadge.tsx kaynaklı TypeScript hatası yok
  </acceptance_criteria>

  <done>
QaBadge.tsx: projectRules prop eklendi, slug field eklendi, computeQaRules export edildi, 9 rules-engine kuralı entegre edildi, mevcut 4 hardcoded kural korundu. TypeScript hatası yok.
  </done>
</task>

<task type="auto">
  <name>Task 2: /api/ai/qa-audit/route.ts — Claude QA endpoint (non-streaming, auth + ownership)</name>

  <read_first>
    - src/app/api/ai/generate-page-package/route.ts (auth + ownership + Anthropic SDK pattern — tam olarak oku)
    - .planning/phases/11-metadata-validator-qa-scoring/11-CONTEXT.md (D-03 ve D-06 kararları)
    - .planning/phases/11-metadata-validator-qa-scoring/11-PATTERNS.md (qa-audit/route.ts bölümü — prompt ve JSON parse pattern)
  </read_first>

  <files>src/app/api/ai/qa-audit/route.ts</files>

  <action>
Yeni dosya oluştur: `src/app/api/ai/qa-audit/route.ts`

**Tam implementasyon:**

```typescript
import { NextRequest } from 'next/server'
import Anthropic from '@anthropic-ai/sdk'
import { createClient } from '@/lib/supabase/server'

const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })

export async function POST(req: NextRequest) {
  // Auth check
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return new Response('Unauthorized', { status: 401 })

  // Body parse
  let body: unknown
  try {
    body = await req.json()
  } catch {
    return new Response('Invalid JSON body', { status: 400 })
  }

  const { packageId, projectId } = body as { packageId?: string; projectId?: string }
  if (!packageId || !projectId) {
    return new Response('packageId and projectId are required', { status: 400 })
  }

  // Proje ownership doğrula (T-11-01: unauthenticated bypass önlenir)
  const { data: project } = await supabase
    .from('projects')
    .select('id, name, domain, sector, target_language, brand_tone')
    .eq('id', projectId)
    .eq('user_id', user.id)
    .single()
  if (!project) return new Response('Project not found', { status: 404 })

  // Package ownership doğrula (T-11-02: başkasının package'ı QA edilemez)
  const { data: pkg } = await supabase
    .from('page_packages')
    .select('id, status, seo_title, meta_description, h1, search_intent, strategic_purpose, content_blocks, schema_jsonld')
    .eq('id', packageId)
    .eq('project_id', projectId)
    .eq('user_id', user.id)
    .single()
  if (!pkg) return new Response('Package not found', { status: 404 })

  // Focus keyword için pages tablosundan page + keyword join
  const { data: pageRow } = await supabase
    .from('pages')
    .select('page_type, focus_keyword_id, title')
    .eq('project_id', projectId)
    .eq('user_id', user.id)
    .limit(1)
    .single()

  let focusKeyword = ''
  if (pageRow?.focus_keyword_id) {
    const { data: kw } = await supabase
      .from('keywords')
      .select('keyword')
      .eq('id', pageRow.focus_keyword_id)
      .single()
    if (kw) focusKeyword = kw.keyword
  }

  // Prompt oluştur — user-controlled içerik template'e gömülür (T-11-03: prompt injection azaltma)
  const prompt = buildQaPrompt(pkg, project, focusKeyword)

  // Non-streaming Anthropic call (D-06: streaming değil, tek JSON yanıt)
  let message
  try {
    message = await client.messages.create({
      model: 'claude-sonnet-4-6',
      max_tokens: 1000,
      messages: [{ role: 'user', content: prompt }],
    })
  } catch {
    return new Response('Claude API error', { status: 502 })
  }

  const text = message.content
    .filter((b) => b.type === 'text')
    .map((b) => (b as { type: 'text'; text: string }).text)
    .join('')

  // JSON extract — kod bloğu veya ham JSON
  const jsonMatch =
    text.match(/```json\s*([\s\S]*?)```/) ?? text.match(/(\{[\s\S]*\})/)
  const jsonText = jsonMatch ? jsonMatch[1] : text

  let result: unknown
  try {
    result = JSON.parse(jsonText.trim())
  } catch {
    return new Response('Invalid JSON from Claude', { status: 502 })
  }

  return new Response(JSON.stringify(result), {
    headers: { 'Content-Type': 'application/json' },
  })
}

function buildQaPrompt(
  pkg: Record<string, unknown>,
  project: Record<string, unknown>,
  focusKeyword: string
): string {
  // T-11-03: user-controlled alanlar açık label'larla sınırlandırılır
  // "---BAŞLIK---" ve "---BİTİŞ---" marker'ları injection riskini azaltır
  const contentBlocksSafe = (() => {
    try {
      return JSON.stringify(pkg.content_blocks ?? [], null, 2)
    } catch {
      return '[]'
    }
  })()

  return `Sen bir SEO içerik denetçisisin. Aşağıdaki sayfa paketini 4 boyutta denetle ve structured JSON döndür.

## Sayfa Paketi
---BAŞLIK---
SEO Title: ${String(pkg.seo_title ?? '(boş)').slice(0, 200)}
Meta Description: ${String(pkg.meta_description ?? '(boş)').slice(0, 400)}
H1: ${String(pkg.h1 ?? '(boş)').slice(0, 200)}
Search Intent: ${String(pkg.search_intent ?? '(belirtilmemiş)').slice(0, 100)}
Stratejik Amaç: ${String(pkg.strategic_purpose ?? '(boş)').slice(0, 500)}
Focus Keyword: ${String(focusKeyword || '(belirtilmemiş)').slice(0, 200)}
---BİTİŞ---

## İçerik Blokları
---IÇERIK-BAŞLANGIÇ---
${contentBlocksSafe.slice(0, 3000)}
---IÇERIK-BİTİŞ---

## Proje Bağlamı
Domain: ${String(project.domain ?? '').slice(0, 200)}
Sektör: ${String(project.sector ?? 'belirtilmemiş').slice(0, 200)}
Dil: ${String(project.target_language ?? 'Türkçe').slice(0, 50)}
Marka Tonu: ${String(project.brand_tone ?? 'profesyonel').slice(0, 100)}

## Denetim Boyutları
1. intent_drift — İçerik hedef search intent ile uyuşuyor mu?
2. robotic_language — AI şablonculuğu, tekrarlayan yapılar, doğal olmayan dil var mı?
3. entity_gap — Bağlam için beklenen entity'ler (markalar, lokasyonlar, terimler) yazıda var mı?
4. duplicate_risk — İçerik yapısı site genelinde benzer sayfalarla çakışma riski taşıyor mu?

## Yanıt Formatı
Sadece JSON döndür, başka açıklama yazma:
\`\`\`json
{
  "checks": [
    { "id": "intent_drift", "severity": "ok|warning|critical", "note": "Kısa Türkçe açıklama" },
    { "id": "robotic_language", "severity": "ok|warning|critical", "note": "..." },
    { "id": "entity_gap", "severity": "ok|warning|critical", "note": "..." },
    { "id": "duplicate_risk", "severity": "ok|warning|critical", "note": "..." }
  ],
  "content_score": 0,
  "human_score": 0
}
\`\`\``
}
```

**Güvenlik notları (CONTEXT.md T-11-*):**
- `user.id` ile tüm DB sorguları — ownership bypass imkansız
- Prompt'ta user-controlled içerik `.slice()` ile sınırlandırıldı
- `ANTHROPIC_API_KEY` sadece server-side — `NEXT_PUBLIC_` prefix'i kullanılmaz
</action>

  <verify>
    <automated>cd C:/Users/baris/Documents/seo-machine && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - `test -f src/app/api/ai/qa-audit/route.ts` → dosya mevcut
    - `grep -n "export async function POST" src/app/api/ai/qa-audit/route.ts` → POST handler var
    - `grep -n "if (!user) return new Response" src/app/api/ai/qa-audit/route.ts` → auth guard var (401)
    - `grep -n "eq.*user_id.*user\\.id" src/app/api/ai/qa-audit/route.ts` → en az 2 satırda ownership check (projects + page_packages)
    - `grep -n "packageId.*projectId\|projectId.*packageId" src/app/api/ai/qa-audit/route.ts` → her iki parametre doğrulanıyor
    - `grep -n "client.messages.create" src/app/api/ai/qa-audit/route.ts` → non-streaming Anthropic call var
    - `grep -n "claude-sonnet-4-6" src/app/api/ai/qa-audit/route.ts` → doğru model
    - `grep -n "NEXT_PUBLIC_" src/app/api/ai/qa-audit/route.ts` → eşleşme YOK (API key client'a sızmıyor)
    - `grep -n "streaming\|stream" src/app/api/ai/qa-audit/route.ts` → streaming YOK (D-06: tek JSON yanıt)
    - `npx tsc --noEmit` → qa-audit/route.ts kaynaklı TypeScript hatası yok
  </acceptance_criteria>

  <done>
src/app/api/ai/qa-audit/route.ts oluşturuldu: POST handler, auth guard (401), double ownership check (projects + page_packages her ikisi user_id ile), non-streaming Anthropic call (claude-sonnet-4-6), JSON extract + parse, buildQaPrompt helper (slice ile injection sınırlaması). TypeScript hatası yok.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/ai/qa-audit | Unauthenticated HTTP istekler bu sınırı geçmeye çalışabilir |
| user input → Claude prompt | seo_title, meta_description, h1, content_blocks — prompt injection riski |
| client → Supabase | page_packages ownership bypass girişimi |
| server env → client bundle | ANTHROPIC_API_KEY sızma riski |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Spoofing | POST /api/ai/qa-audit | mitigate | `supabase.auth.getUser()` → 401 if !user; no session = no access |
| T-11-02 | Elevation of Privilege | page_packages DB query | mitigate | `.eq('user_id', user.id)` on BOTH projects AND page_packages; 404 if mismatch |
| T-11-03 | Tampering (Prompt Injection) | buildQaPrompt — seo_title, meta_description, content_blocks | mitigate | User-controlled fields wrapped in `---BAŞLIK---/---BİTİŞ---` delimiters + `.slice()` length cap; fields labeled as "data to analyze" not instructions |
| T-11-04 | Information Disclosure | ANTHROPIC_API_KEY | mitigate | Key read via `process.env.ANTHROPIC_API_KEY` (server-only); verified no NEXT_PUBLIC_ prefix |
| T-11-05 | Denial of Service | Unbounded Claude token consumption | accept | max_tokens: 1000 cap; content_blocks sliced to 3000 chars; low risk for single-user internal tool |
</threat_model>

<verification>
## Phase 11 Plan 01 Doğrulama

```bash
# TypeScript derleme kontrolü
cd C:/Users/baris/Documents/seo-machine && npx tsc --noEmit

# QaBadge export kontrolü
grep -n "export function computeQaRules\|export type QaBadgeProps\|export function QaBadge" \
  src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx

# QaBadge slug + projectRules prop kontrolü
grep -n "slug: string | null\|projectRules: ProjectRules" \
  src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx

# API route var ve doğru yapıda
grep -n "export async function POST\|getUser\|eq.*user_id\|claude-sonnet-4-6" \
  src/app/api/ai/qa-audit/route.ts

# Güvenlik: API key client'a sızmıyor
grep -rn "NEXT_PUBLIC_ANTHROPIC" src/ || echo "PASS: no public API key"

# font-medium yasak kontrolü
grep -rn "font-medium" src/app/(dashboard)/projeler/[id]/sayfa-paketi/QaBadge.tsx || echo "PASS"
```
</verification>

<success_criteria>
- QaBadge.tsx: `computeQaRules` export edilmiş, `projectRules: Record<string, boolean>` prop var, `slug: string | null` prop var, 9 rules-engine kuralı implementasyonu mevcut, mevcut 4 hardcoded kural korunmuş
- qa-audit/route.ts: POST handler var, `!user → 401` guard var, double ownership check var (projects + page_packages her ikisi `.eq('user_id', user.id)`), `client.messages.create` non-streaming call var, `claude-sonnet-4-6` modeli var, JSON extract + parse var, `NEXT_PUBLIC_` prefix YOK
- `npx tsc --noEmit` sıfır hata
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/11-metadata-validator-qa-scoring/11-01-SUMMARY.md` dosyasını oluştur.
</output>
