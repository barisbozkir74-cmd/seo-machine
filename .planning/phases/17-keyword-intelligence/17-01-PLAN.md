---
phase: 17-keyword-intelligence
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/lib/keywords/niche-scoring.ts
  - src/lib/keywords/niche-scoring.test.ts
autonomous: true
requirements:
  - NICH-01
  - RVEN-01

must_haves:
  truths:
    - "calculateNicheScore fonksiyonu 3 bileşenli formülle 0-100 aralığında skor döner"
    - "calculateNicheScore boş keyword listesi için 0 döner"
    - "classifyRevenueType çoğunluk informational için 'bilgi' döner"
    - "classifyRevenueType çoğunluk commercial/transactional için 'ticari' döner"
    - "classifyRevenueType karma dağılım için 'mixed' döner"
  artifacts:
    - path: "src/lib/keywords/niche-scoring.ts"
      provides: "calculateNicheScore + classifyRevenueType export"
      exports: ["calculateNicheScore", "classifyRevenueType", "ClusterKeywordData"]
    - path: "src/lib/keywords/niche-scoring.test.ts"
      provides: "5 unit test — Nyquist Wave 0 zorunluluğu"
      contains: "calculateNicheScore"
  key_links:
    - from: "src/lib/keywords/niche-scoring.ts"
      to: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts"
      via: "recalculateClusterNicheScore helper (Wave 1'de inject edilecek)"
      pattern: "import.*niche-scoring"
---

<objective>
Wave 0: Niche skor hesaplama lib'i implement et ve Nyquist testlerini yaz.

Purpose: Wave 1'de actions.ts'e inject edilecek `calculateNicheScore` ve `classifyRevenueType` fonksiyonları burada test-first yazılır. Bu plan blocking — Wave 1 bu dosyaya bağımlı.
Output: `niche-scoring.ts` (tam implementasyon) + `niche-scoring.test.ts` (5 unit test, hepsi green).
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-keyword-intelligence/17-CONTEXT.md
@.planning/phases/17-keyword-intelligence/17-RESEARCH.md

<interfaces>
<!-- Mevcut scoring.ts — adaptasyon kaynağı -->
<!-- src/lib/keywords/scoring.ts -->
```typescript
export function calculateOpportunityScore(
  keyword: { volume: number | null; cpc: number | null; difficulty: number | null; search_intent: string | null },
  context: { maxVolume: number; maxCpc: number }
): number

export function buildScoringContext(
  keywords: Array<{ volume: number | null; cpc: number | null }>
): { maxVolume: number; maxCpc: number }
```

<!-- Bu fazda oluşturulacak niche-scoring.ts arayüzü -->
```typescript
export type ClusterKeywordData = {
  volume: number | null
  cpc: number | null
  difficulty: number | null
  search_intent: string | null
}

export function calculateNicheScore(
  keywords: ClusterKeywordData[],
  context: { maxClusterVolume: number; maxCpc: number }
): number  // 0-100, 1 decimal (Math.round * 10) / 10

export function classifyRevenueType(
  keywords: ClusterKeywordData[]
): 'bilgi' | 'mixed' | 'ticari'
```
</interfaces>
</context>

<feature>
  <name>Cluster-level niche score ve revenue type hesaplama</name>
  <files>src/lib/keywords/niche-scoring.ts, src/lib/keywords/niche-scoring.test.ts</files>
  <behavior>
    calculateNicheScore:
    - Boş array → 0 döner
    - niche_score = (volumeScore * 0.4) + (competitionScore * 0.35) + (cpcScore * 0.25) → 0-100 aralığı, 1 decimal
    - volumeScore = totalVolume / maxClusterVolume (min-max, max 1.0)
    - competitionScore = (100 - avgDifficulty) / 100 (düşük KD = iyi, null → 50 varsayılan)
    - cpcScore = avgCpc / maxCpc (min-max, max 1.0, null → 0)
    - maxClusterVolume = 0 ise volumeScore = 0 (ZeroDivisionError yok)

    classifyRevenueType:
    - Boş array → 'mixed' döner
    - informational count / total > 0.5 → 'bilgi'
    - (commercial + transactional) count / total > 0.5 → 'ticari'
    - Diğer her durum → 'mixed'
    - Büyük/küçük harf insensitive (toLowerCase)
  </behavior>
  <implementation>
    RED: niche-scoring.test.ts yaz (5 test, hepsi fail). Import path: '../niche-scoring' (vitest root aliases ile src/ prefix gerekmez).
    GREEN: niche-scoring.ts implementasyonu yaz, testler green olana kadar düzelt.
    REFACTOR: Gereksiz tekrar yok, scoring.ts pattern tutarlı.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: Test stub yaz — RED fazı</name>
  <files>src/lib/keywords/niche-scoring.test.ts</files>
  <read_first>
    - src/lib/keywords/scoring.ts (referans pattern — test yapısı için)
  </read_first>
  <action>
`src/lib/keywords/niche-scoring.test.ts` dosyasını oluştur. Tam içerik:

```typescript
import { describe, it, expect } from 'vitest'
import { calculateNicheScore, classifyRevenueType } from './niche-scoring'

describe('calculateNicheScore', () => {
  it('boş keyword listesi için 0 döner', () => {
    expect(calculateNicheScore([], { maxClusterVolume: 1000, maxCpc: 5 })).toBe(0)
  })

  it('3 bileşenli formülle 0-100 aralığında skor döner', () => {
    const keywords = [
      { volume: 500, cpc: 2.5, difficulty: 30, search_intent: 'commercial' },
      { volume: 300, cpc: 1.5, difficulty: 40, search_intent: 'transactional' },
    ]
    const score = calculateNicheScore(keywords, { maxClusterVolume: 1000, maxCpc: 5 })
    expect(score).toBeGreaterThanOrEqual(0)
    expect(score).toBeLessThanOrEqual(100)
    // volumeScore = 800/1000 = 0.8; competitionScore = (100-35)/100 = 0.65; cpcScore = 2/5 = 0.4
    // raw = (0.8*0.4) + (0.65*0.35) + (0.4*0.25) = 0.32 + 0.2275 + 0.10 = 0.6475 → 64.8
    expect(score).toBeCloseTo(64.8, 0)
  })
})

describe('classifyRevenueType', () => {
  it('boş array için mixed döner', () => {
    expect(classifyRevenueType([])).toBe('mixed')
  })

  it('çoğunluk informational için bilgi döner', () => {
    const keywords = [
      { volume: null, cpc: null, difficulty: null, search_intent: 'informational' },
      { volume: null, cpc: null, difficulty: null, search_intent: 'informational' },
      { volume: null, cpc: null, difficulty: null, search_intent: 'commercial' },
    ]
    expect(classifyRevenueType(keywords)).toBe('bilgi')
  })

  it('çoğunluk commercial/transactional için ticari döner', () => {
    const keywords = [
      { volume: null, cpc: null, difficulty: null, search_intent: 'commercial' },
      { volume: null, cpc: null, difficulty: null, search_intent: 'transactional' },
      { volume: null, cpc: null, difficulty: null, search_intent: 'informational' },
    ]
    expect(classifyRevenueType(keywords)).toBe('ticari')
  })

  it('karma dağılım için mixed döner', () => {
    const keywords = [
      { volume: null, cpc: null, difficulty: null, search_intent: 'informational' },
      { volume: null, cpc: null, difficulty: null, search_intent: 'commercial' },
    ]
    expect(classifyRevenueType(keywords)).toBe('mixed')
  })
})
```

Kaydet. Henüz `niche-scoring.ts` yoksa testler import hatasıyla fail etmeli — bu RED fazı.
  </action>
  <verify>
    <automated>cd /c/Users/baris/Documents/seo-machine && npx vitest run src/lib/keywords/niche-scoring.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/keywords/niche-scoring.test.ts` dosyası mevcut
    - 5 `it(` bloğu içeriyor: `grep -c "it('" src/lib/keywords/niche-scoring.test.ts` → 5
    - `calculateNicheScore` ve `classifyRevenueType` import ediliyor: `grep "import.*niche-scoring" src/lib/keywords/niche-scoring.test.ts` → sonuç mevcut
    - Vitest çalışır (import hatası veya test failure — GREEN değil, henüz RED bekleniyor)
  </acceptance_criteria>
  <done>5 test stub yazılmış, import yolu doğru, vitest komutu çalışır hata almaz</done>
</task>

<task type="tdd">
  <name>Task 2: Implementasyon yaz — GREEN fazı</name>
  <files>src/lib/keywords/niche-scoring.ts</files>
  <read_first>
    - src/lib/keywords/scoring.ts (min-max pattern, Math.round formülü)
    - src/lib/keywords/niche-scoring.test.ts (az önce yazılan testler)
  </read_first>
  <action>
`src/lib/keywords/niche-scoring.ts` dosyasını oluştur. Tam içerik:

```typescript
// Pure math — no imports needed
// Cluster-level niche scoring (keyword-level scoring.ts'in cluster adaptasyonu)

export type ClusterKeywordData = {
  volume: number | null
  cpc: number | null
  difficulty: number | null
  search_intent: string | null
}

/**
 * Cluster-level niche selection skoru.
 * Formül: (volumeScore * 0.4) + (competitionScore * 0.35) + (cpcScore * 0.25)
 * Tüm bileşenler min-max normalize (0–1), sonuç 0–100 aralığında 1 decimal.
 *
 * @param keywords - Cluster'daki tüm keyword'ler
 * @param context  - Proje geneli max değerler (normalizasyon için)
 */
export function calculateNicheScore(
  keywords: ClusterKeywordData[],
  context: { maxClusterVolume: number; maxCpc: number }
): number {
  if (keywords.length === 0) return 0

  const totalVolume = keywords.reduce((s, k) => s + (k.volume ?? 0), 0)
  const avgDifficulty = keywords.reduce((s, k) => s + (k.difficulty ?? 50), 0) / keywords.length
  const avgCpc = keywords.reduce((s, k) => s + (k.cpc ?? 0), 0) / keywords.length

  const volumeScore = context.maxClusterVolume > 0
    ? Math.min(totalVolume / context.maxClusterVolume, 1)
    : 0
  const competitionScore = (100 - avgDifficulty) / 100  // düşük KD = iyi
  const cpcScore = context.maxCpc > 0
    ? Math.min(avgCpc / context.maxCpc, 1)
    : 0

  const raw = (volumeScore * 0.4) + (competitionScore * 0.35) + (cpcScore * 0.25)
  return Math.round(raw * 100 * 10) / 10  // 0-100, 1 decimal
}

/**
 * Cluster'daki keyword'lerin search_intent dağılımına göre revenue tipi atar.
 * - Çoğunluk (>50%) informational → 'bilgi'
 * - Çoğunluk (>50%) commercial veya transactional → 'ticari'
 * - Karma → 'mixed'
 */
export function classifyRevenueType(
  keywords: ClusterKeywordData[]
): 'bilgi' | 'mixed' | 'ticari' {
  if (keywords.length === 0) return 'mixed'

  let informational = 0
  let commercial = 0

  for (const kw of keywords) {
    const intent = kw.search_intent?.toLowerCase() ?? ''
    if (intent === 'informational') informational++
    else if (intent === 'commercial' || intent === 'transactional') commercial++
  }

  const total = keywords.length
  if (informational / total > 0.5) return 'bilgi'
  if (commercial / total > 0.5) return 'ticari'
  return 'mixed'
}
```

Testleri çalıştır, hepsi green olana kadar düzelt.
  </action>
  <verify>
    <automated>cd /c/Users/baris/Documents/seo-machine && npx vitest run src/lib/keywords/niche-scoring.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/keywords/niche-scoring.ts` dosyası mevcut
    - `grep "export function calculateNicheScore" src/lib/keywords/niche-scoring.ts` → sonuç mevcut
    - `grep "export function classifyRevenueType" src/lib/keywords/niche-scoring.ts` → sonuç mevcut
    - `grep "export type ClusterKeywordData" src/lib/keywords/niche-scoring.ts` → sonuç mevcut
    - `npx vitest run src/lib/keywords/niche-scoring.test.ts` → çıktıda "5 passed" içeriyor, "failed" içermiyor
  </acceptance_criteria>
  <done>5 test green, niche-scoring.ts export'ları doğru, full suite çalışıyor: npm test passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pure lib → actions.ts | Bu plan pure math — I/O yok, trust boundary yok |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-W0-01 | Tampering | niche-scoring.ts (pure function) | accept | Pure math lib, hiçbir DB/network erişimi yok — dış girdi yalnızca action katmanından gelir (Wave 1'de mitigate edilir) |
</threat_model>

<verification>
- `npx vitest run src/lib/keywords/niche-scoring.test.ts` → 5 passed, 0 failed
- `npm test` → full suite green (regresyon yok)
</verification>

<success_criteria>
- `src/lib/keywords/niche-scoring.ts` — `calculateNicheScore` ve `classifyRevenueType` export edilmiş
- `src/lib/keywords/niche-scoring.test.ts` — 5 test, hepsi green
- `calculateNicheScore([], {...})` → 0
- `classifyRevenueType([])` → 'mixed'
- Full test suite (npm test) green
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/17-keyword-intelligence/17-01-SUMMARY.md` oluştur.
</output>
