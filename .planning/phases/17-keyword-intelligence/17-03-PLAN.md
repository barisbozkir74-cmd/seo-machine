---
phase: 17-keyword-intelligence
plan: 03
type: execute
wave: 2
depends_on:
  - "17-01"
  - "17-02"
files_modified:
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterPanel.tsx
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/RevenueBadge.tsx
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/RevenueOverrideSelect.tsx
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx
autonomous: false
requirements:
  - NICH-02
  - RVEN-02

must_haves:
  truths:
    - "Cluster header satırında 'Niche Skoru' sütunu görünür — hesaplanmış skor ScoreBadge ile, hesaplanmamış '—' ile gösterilir"
    - "Cluster header satırında 'Revenue' sütunu görünür — revenu badge renk kodlu, dropdown ile override edilebilir"
    - "Cluster listesi 'Niche Skoru' başlığına tıklanınca URL sort parametresiyle sıralanır"
    - "page.tsx SELECT sorgusu opportunity_score ve revenue_type kolonlarını içeriyor"
    - "ClusterData tipi opportunity_score ve revenue_type alanlarını içeriyor"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/RevenueBadge.tsx"
      provides: "revenue_type → renk kodlu badge (IntentBadge pattern)"
      exports: ["RevenueBadge"]
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/RevenueOverrideSelect.tsx"
      provides: "Client component — dropdown ile updateClusterRevenue action çağrısı"
      exports: ["RevenueOverrideSelect"]
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterPanel.tsx"
      provides: "Cluster header'a 2 yeni sütun + NicheScoreHeader sıralama butonu"
      contains: "NicheScoreHeader"
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx"
      provides: "searchParams sort desteği + SELECT genişletme + ClusterData mapping güncellemesi"
      contains: "opportunity_score"
  key_links:
    - from: "page.tsx searchParams sort"
      to: "keyword_clusters ORDER BY opportunity_score"
      via: "supabase .order(sortColumn)"
      pattern: "opportunity_score.*ascending"
    - from: "RevenueOverrideSelect"
      to: "updateClusterRevenue (actions.ts)"
      via: "server action import + startTransition"
      pattern: "updateClusterRevenue"
    - from: "ClusterPanel"
      to: "RevenueBadge + RevenueOverrideSelect"
      via: "JSX import"
      pattern: "RevenueBadge.*RevenueOverrideSelect"
---

<objective>
Wave 2: UI katmanı — ClusterPanel genişletme, RevenueBadge, RevenueOverrideSelect ve page.tsx sıralama desteği.

Purpose: NICH-02 ve RVEN-02 gereksinimlerini karşıla. Kullanıcı cluster görünümünde niche skoru ve revenue sınıflandırmasını görebilir, sıralama ve override yapabilir.
Output: 4 dosya değişikliği — 2 yeni bileşen (RevenueBadge, RevenueOverrideSelect) + ClusterPanel genişletme + page.tsx sıralama + SELECT güncellemesi.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-keyword-intelligence/17-CONTEXT.md
@.planning/phases/17-keyword-intelligence/17-UI-SPEC.md
@.planning/phases/17-keyword-intelligence/17-02-SUMMARY.md

<interfaces>
<!-- Wave 1'de oluşturulan updateClusterRevenue action -->
```typescript
// src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts
export type UpdateClusterRevenueResult =
  | { success: true }
  | { success: false; error: string }

export async function updateClusterRevenue(
  clusterId: string,
  revenueType: string,
  projectId: string
): Promise<UpdateClusterRevenueResult>
```

<!-- Mevcut ClusterPanel.tsx — genişletilecek ClusterData tipi -->
```typescript
// Mevcut (eksik alanlar var):
type ClusterData = {
  id: string
  cluster_name: string
  intent: string | null
  primary_keyword_id: string | null
  keywords: ClusterKeyword[]
}

// Hedef (Phase 17 sonrası):
type ClusterData = {
  id: string
  cluster_name: string
  intent: string | null
  primary_keyword_id: string | null
  opportunity_score: number | null   // YENİ
  revenue_type: string | null        // YENİ
  keywords: ClusterKeyword[]
}
```

<!-- Mevcut ScoreBadge (ClusterPanel.tsx içinde inline — değiştirilmez) -->
```typescript
function ScoreBadge({ score }: { score: number | null }) {
  if (score === null) return <span className="text-sm text-muted-foreground">—</span>
  if (score >= 70) return <Badge className="bg-violet-500/20 text-violet-400 text-xs border-0">{score.toFixed(1)}</Badge>
  if (score >= 40) return <Badge className="bg-amber-500/20 text-amber-400 text-xs border-0">{score.toFixed(1)}</Badge>
  return <Badge className="bg-secondary text-muted-foreground text-xs border-0">{score.toFixed(1)}</Badge>
}
```

<!-- Mevcut IntentBadge.tsx — RevenueBadge pattern referansı -->
```typescript
// src/app/(dashboard)/projeler/[id]/keyword-stratejisi/IntentBadge.tsx
const intentConfig: Record<string, { className: string; label: string }> = {
  commercial:    { className: 'bg-blue-500/20 text-blue-400',      label: 'Commercial' },
  informational: { className: 'bg-emerald-500/20 text-emerald-400', label: 'Informational' },
  navigational:  { className: 'bg-gray-500/20 text-gray-400',      label: 'Navigational' },
  transactional: { className: 'bg-orange-500/20 text-orange-400',  label: 'Transactional' },
}
// variant prop kullanılmaz — STATE.md proje kararı
```

<!-- Mevcut page.tsx cluster SELECT — genişletilecek -->
```typescript
// Mevcut (eksik):
const { data: clustersRaw } = await supabase
  .from('keyword_clusters')
  .select('id, cluster_name, intent, primary_keyword_id')
  .eq('project_id', id)
  .eq('user_id', user.id)
  .order('total_volume', { ascending: false, nullsFirst: false })

// Hedef (Phase 17 sonrası):
const { data: clustersRaw } = await supabase
  .from('keyword_clusters')
  .select('id, cluster_name, intent, primary_keyword_id, opportunity_score, revenue_type')
  .eq('project_id', id)
  .eq('user_id', user.id)
  .order(sortColumn, { ascending, nullsFirst: false })
```

<!-- page.tsx searchParams — genişletilecek -->
```typescript
// Mevcut:
searchParams: Promise<{ view?: string }>

// Hedef:
searchParams: Promise<{ view?: string; sort?: string; dir?: string }>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RevenueBadge + RevenueOverrideSelect bileşenleri oluştur</name>
  <files>
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/RevenueBadge.tsx,
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/RevenueOverrideSelect.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/IntentBadge.tsx (pattern kaynağı — bire bir kopyalanacak)
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts (updateClusterRevenue import için)
    - .planning/phases/17-keyword-intelligence/17-UI-SPEC.md (Interaction Contract §2, RevenueBadge renk tablosu)
  </read_first>
  <action>
**RevenueBadge.tsx** oluştur (IntentBadge bire bir kopyası):

```typescript
import { Badge } from '@/components/ui/badge'

const revenueConfig: Record<string, { className: string; label: string }> = {
  bilgi:   { className: 'bg-emerald-500/20 text-emerald-400', label: 'Bilgi' },
  mixed:   { className: 'bg-yellow-500/20 text-yellow-400',  label: 'Mixed' },
  ticari:  { className: 'bg-red-500/20 text-red-400',        label: 'Ticari' },
}

export function RevenueBadge({ revenueType }: { revenueType: string | null }) {
  if (!revenueType) return <span className="text-sm text-muted-foreground">—</span>
  const config = revenueConfig[revenueType.toLowerCase()]
  if (!config) return <span className="text-sm text-muted-foreground">{revenueType}</span>
  return (
    <Badge className={`${config.className} text-xs border-0`}>
      {config.label}
    </Badge>
  )
}
```

**RevenueOverrideSelect.tsx** oluştur (client component):

```typescript
'use client'

import { useTransition } from 'react'
import { updateClusterRevenue } from './actions'

const REVENUE_OPTIONS = [
  { value: 'bilgi',  label: 'Bilgi' },
  { value: 'mixed',  label: 'Mixed' },
  { value: 'ticari', label: 'Ticari' },
] as const

export function RevenueOverrideSelect({
  clusterId,
  projectId,
  currentRevenue,
}: {
  clusterId: string
  projectId: string
  currentRevenue: string | null
}) {
  const [isPending, startTransition] = useTransition()

  function handleChange(e: React.ChangeEvent<HTMLSelectElement>) {
    const newValue = e.target.value
    if (!newValue) return
    startTransition(async () => {
      await updateClusterRevenue(clusterId, newValue, projectId)
    })
  }

  return (
    <select
      value={currentRevenue ?? ''}
      onChange={handleChange}
      disabled={isPending}
      aria-label="Gelir tipi seç"
      className="bg-transparent text-xs text-muted-foreground cursor-pointer border-0 outline-none focus:ring-0 min-w-[80px] disabled:opacity-50"
    >
      <option value="" disabled>Tür seç</option>
      {REVENUE_OPTIONS.map((opt) => (
        <option key={opt.value} value={opt.value}>
          {opt.label}
        </option>
      ))}
    </select>
  )
}
```

NOT: `RevenueOverrideSelect` bir `<select>` kullanır (shadcn Select yerine). UI-SPEC "shadcn Select bileşeni veya native `<select>`" demiştir. Native select proje bağlamlı — mevcut form bileşenleri proje genelinde nadiren kullanılıyor, bg-transparent ile tema uyumlu.
  </action>
  <verify>
    <automated>cd /c/Users/baris/Documents/seo-machine && npx tsc --noEmit 2>&1 | grep -E "RevenueBadge|RevenueOverrideSelect" | head -5 || echo "no errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(dashboard)/projeler/[id]/keyword-stratejisi/RevenueBadge.tsx` dosyası mevcut
    - `grep "export function RevenueBadge" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/RevenueBadge.tsx` → sonuç mevcut
    - `grep "revenueConfig" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/RevenueBadge.tsx | grep "bilgi"` → sonuç mevcut (renk config)
    - `grep "variant" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/RevenueBadge.tsx` → boş (variant prop kullanılmıyor — STATE.md kararı)
    - `src/app/(dashboard)/projeler/[id]/keyword-stratejisi/RevenueOverrideSelect.tsx` dosyası mevcut
    - `grep "'use client'" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/RevenueOverrideSelect.tsx` → sonuç mevcut
    - `grep "updateClusterRevenue" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/RevenueOverrideSelect.tsx` → sonuç mevcut
    - `grep "useTransition" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/RevenueOverrideSelect.tsx` → sonuç mevcut
    - TypeScript: `npx tsc --noEmit 2>&1 | grep -E "RevenueBadge|RevenueOverrideSelect"` → boş çıktı
  </acceptance_criteria>
  <done>RevenueBadge ve RevenueOverrideSelect dosyaları mevcut, TypeScript temiz</done>
</task>

<task type="auto">
  <name>Task 2: ClusterPanel.tsx genişlet + page.tsx SELECT ve sıralama desteği</name>
  <files>
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterPanel.tsx,
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterPanel.tsx (mevcut tam içerik — her satır okunacak)
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx (mevcut tam içerik — her satır okunacak)
    - .planning/phases/17-keyword-intelligence/17-UI-SPEC.md (Layout Delta, Interaction Contract §1-3, Copywriting Contract)
    - .planning/phases/17-keyword-intelligence/17-RESEARCH.md (Pattern 4: Sıralama, Pitfall 1 ve 5)
  </read_first>
  <action>
**ClusterPanel.tsx değişiklikleri:**

1. Import ekle (dosya başına):
```typescript
import { RevenueBadge } from './RevenueBadge'
import { RevenueOverrideSelect } from './RevenueOverrideSelect'
```

2. `ClusterData` tipini genişlet (mevcut tip tanımının sonuna 2 alan ekle):
```typescript
type ClusterData = {
  id: string
  cluster_name: string
  intent: string | null
  primary_keyword_id: string | null
  opportunity_score: number | null   // Phase 17
  revenue_type: string | null        // Phase 17
  keywords: ClusterKeyword[]
}
```

3. Cluster header `<div className="group flex items-center justify-between ...">` içindeki layout'u güncelle. Mevcut layout:
```jsx
<div className="flex items-center gap-2 min-w-0">
  <span ...>{cluster.cluster_name}</span>
  <IntentBadge intent={cluster.intent} />
</div>
<ClusterDeleteButton ... />
```

Yeni layout (UI-SPEC Layout Delta'ya göre — sağ tarafa wrapper ekle):
```jsx
<div className="flex items-center gap-2 min-w-0">
  <span className="text-sm font-semibold truncate">{cluster.cluster_name}</span>
  <IntentBadge intent={cluster.intent} />
</div>
{/* Phase 17: Revenue + Niche Skoru sütunları */}
<div className="flex items-center gap-3 shrink-0">
  {/* Revenue sütunu — w-28 */}
  <div className="w-28 flex items-center gap-1">
    <RevenueBadge revenueType={cluster.revenue_type} />
    <RevenueOverrideSelect
      clusterId={cluster.id}
      projectId={projectId}
      currentRevenue={cluster.revenue_type}
    />
  </div>
  {/* Niche Skoru sütunu — w-24, text-right */}
  <div className="w-24 flex justify-end">
    <ScoreBadge score={cluster.opportunity_score} />
  </div>
  <ClusterDeleteButton
    projectId={projectId}
    clusterId={cluster.id}
    clusterName={cluster.cluster_name}
  />
</div>
```

ClusterPanel bileşeni header satırına da sütun başlıklarını ekle (mevcut cluster'lar listesinden önce, clusters.length > 0 kontrolünden sonra):

```jsx
{/* Cluster header sütun başlıkları */}
<div className="flex items-center justify-end gap-3 px-3 pb-2">
  <span className="text-xs text-muted-foreground w-28 text-left">Revenue</span>
  <NicheScoreHeader />
  <div className="w-8" />{/* ClusterDeleteButton placeholder */}
</div>
```

`NicheScoreHeader` ClusterPanel.tsx içine inline ekle (aşağıda tanımlanacak, prop almayan basit link bileşeni). NOT: Sıralama için `useSearchParams` + `useRouter` kullanılmaz — bu SSR bileşenidir. Başlık bir `<a href>` veya dış URL linki olacak. Ancak mevcut ClusterPanel Server Component — sıralama butonunu ClusterPanel içine koymak için ya ClusterPanel'ı Client Component yapmalı ya da ayrı bir `NicheScoreHeaderButton` Client Component oluşturmalısın.

**Tercih:** Sıralama butonunu küçük bir inline Client Component yap, ClusterPanel içine import et:

`NicheScoreHeaderButton` adlı ayrı dosya değil, `ClusterPanel.tsx` içinde `'use client'` olmadan kullanılamaz. Bu nedenle page.tsx içinde sütun başlığı butonu SSR'da render edilir ve searchParams'tan mevcut sort yönü okunarak `href` ile link verilir. ClusterPanel buna gerek duymaz — sadece veri gösterir.

**Dolayısıyla:** ClusterPanel.tsx'te sütun başlıkları **statik metin** olarak gösterilir. Sıralama butonunu page.tsx içindeki ClusterPanel'ın üstüne ekle.

**page.tsx değişiklikleri:**

1. `searchParams` tipini genişlet:
```typescript
searchParams: Promise<{ view?: string; sort?: string; dir?: string }>
```

2. `{ view }` destructure'ını güncelle:
```typescript
const { view, sort, dir } = await searchParams
```

3. Cluster SELECT sorgusunu güncelle (Pitfall 1 önlemi):
```typescript
// Sıralama mantığı (D-04: niche skoruna göre sıralanabilir)
const sortColumn = sort === 'niche_score' ? 'opportunity_score' : 'total_volume'
const ascending = dir === 'asc'

const { data: clustersRaw } = await supabase
  .from('keyword_clusters')
  .select('id, cluster_name, intent, primary_keyword_id, opportunity_score, revenue_type')
  .eq('project_id', id)
  .eq('user_id', user.id)
  .order(sortColumn, { ascending, nullsFirst: false })
```

4. `ClusterWithKeywords` tipini genişlet:
```typescript
type ClusterWithKeywords = {
  id: string
  cluster_name: string
  intent: string | null
  primary_keyword_id: string | null
  opportunity_score: number | null  // Phase 17
  revenue_type: string | null       // Phase 17
  keywords: KeywordRow[]
}
```

5. `clustersWithKeywords` mapping'ini güncelle (opportunity_score ve revenue_type spread ile geliyor):
```typescript
const clustersWithKeywords: ClusterWithKeywords[] = clusters.map((c) => ({
  ...c,  // opportunity_score + revenue_type artık spread'e dahil
  keywords: clusterKeywordMap[c.id] ?? [],
}))
```

6. Küme görünümünde ClusterPanel üstüne sıralama başlığı ekle (sadece isClusterView === true ise):

```tsx
{isClusterView && clusters.length > 0 && (
  <div className="flex items-center justify-end gap-3 px-3 pb-1">
    <span className="text-xs text-muted-foreground w-28 text-left">Revenue</span>
    {/* Niche Skoru sıralama butonu */}
    {(() => {
      const isActive = sort === 'niche_score'
      const nextDir = isActive && dir === 'desc' ? 'asc'
        : isActive && dir === 'asc' ? undefined
        : 'desc'
      const href = nextDir
        ? `/projeler/${id}/keyword-stratejisi?view=cluster&sort=niche_score&dir=${nextDir}`
        : `/projeler/${id}/keyword-stratejisi?view=cluster`
      return (
        <Link
          href={href}
          className="text-xs text-muted-foreground hover:text-foreground w-24 text-right transition-colors"
        >
          Niche Skoru{isActive && dir === 'desc' ? ' ↓' : isActive && dir === 'asc' ? ' ↑' : ''}
        </Link>
      )
    })()}
    <div className="w-8" />
  </div>
)}
```

Link import'u zaten var (dosya başında `import Link from 'next/link'`).
  </action>
  <verify>
    <automated>cd /c/Users/baris/Documents/seo-machine && npx tsc --noEmit 2>&1 | grep -E "keyword-stratejisi" | head -10 || echo "no ts errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "opportunity_score" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterPanel.tsx` → sonuç mevcut (ClusterData tipinde)
    - `grep "revenue_type" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterPanel.tsx` → sonuç mevcut
    - `grep "RevenueBadge" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterPanel.tsx` → sonuç mevcut (import + JSX)
    - `grep "RevenueOverrideSelect" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterPanel.tsx` → sonuç mevcut (import + JSX)
    - `grep "opportunity_score.*revenue_type" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx` → sonuç mevcut (SELECT sorgusunda)
    - `grep "sort.*dir" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx` → sonuç mevcut (searchParams destructure)
    - `grep "niche_score" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx` → sonuç mevcut (sort koşulu)
    - `grep "Niche Skoru" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx` → sonuç mevcut (sıralama linki)
    - TypeScript temiz: `npx tsc --noEmit 2>&1 | grep "keyword-stratejisi"` → boş çıktı
    - `npm test` → full suite green
  </acceptance_criteria>
  <done>
    ClusterPanel opportunity_score + revenue_type sütunları gösteriyor; page.tsx SELECT genişledi, sıralama linki mevcut; TypeScript temiz; npm test green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Küme görünümünde her cluster satırına "Revenue" ve "Niche Skoru" sütunları eklendi. Kümeleme çalıştırıldıktan sonra skorlar DB'ye yazılıyor, sayfa yenilenmeden gösteriliyor. Revenue dropdown ile manuel override yapılabiliyor. Niche Skoru başlığına tıklanınca URL'de sort parametresi değişiyor, sıralama değişiyor.
  </what-built>
  <how-to-verify>
    1. `npm run dev` ile uygulamayı başlat
    2. Keyword cluster'ları olan bir projeye git: `/projeler/[id]/keyword-stratejisi?view=cluster`
    3. Şunları kontrol et:
       a. Cluster satırlarında "Revenue" sütunu görünür (badge veya "—" dash)
       b. Cluster satırlarında "Niche Skoru" sütunu görünür (badge veya "—" dash)
       c. Revenue dropdown'a tıklayınca "Bilgi / Mixed / Ticari" seçenekleri görünür
       d. Seçim yapılınca sayfa yenilenir ve badge güncellenir
       e. "Niche Skoru" başlık linkine tıklayınca URL'de `?sort=niche_score&dir=desc` eklenir
       f. URL değişince cluster listesi yeniden sıralanır
    4. Eğer cluster'larda henüz skor hesaplanmamışsa (null): "Kümelere Böl" butonuna tıkla ve yenile — skorların dolduğunu kontrol et
  </how-to-verify>
  <resume-signal>"onaylıyorum" yaz veya gördüğün sorunları describe et</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → RevenueOverrideSelect → updateClusterRevenue | Revenue type değeri kullanıcı seçiminden geliyor — Wave 1'de whitelist + ownership kontrolü eklendi |
| Client → Niche Skoru sort link | searchParams sort değeri kullanıcıdan geliyor — page.tsx'te sadece bilinen değerler kabul ediliyor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | RevenueOverrideSelect → updateClusterRevenue | mitigate | Wave 1'de `VALID_REVENUE_TYPES` whitelist + ownership check uygulandı — UI katmanı bunu tekrarlamaz |
| T-17-05 | Tampering | page.tsx searchParams sort | accept | sort değeri SQL ORDER BY kolonuna map ediliyor — whitelist: `sort === 'niche_score' ? 'opportunity_score' : 'total_volume'`. Bilinmeyen sort değerleri default'a düşer — injection yok |
| T-17-06 | Information Disclosure | ClusterPanel SSR render | accept | Yalnızca oturumdaki kullanıcının kendi cluster'ları SSR'da render ediliyor — RLS + `user_id` WHERE koşulu ile garanti |
</threat_model>

<verification>
- `grep "opportunity_score" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterPanel.tsx` → mevcut
- `grep "revenue_type" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterPanel.tsx` → mevcut
- `grep "opportunity_score.*revenue_type" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx` → mevcut (SELECT)
- `grep "niche_score" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx` → mevcut (sort link)
- `npx tsc --noEmit` → 0 TypeScript hatası
- `npm test` → full suite green
- Checkpoint: kullanıcı cluster görünümünde Revenue + Niche Skoru sütunlarını gördüğünü onaylar
</verification>

<success_criteria>
- RevenueBadge.tsx: IntentBadge pattern, 3 renk (emerald/yellow/red), variant prop yok
- RevenueOverrideSelect.tsx: client component, useTransition, updateClusterRevenue çağrısı
- ClusterPanel.tsx: ClusterData tipinde opportunity_score + revenue_type, cluster header'da 2 yeni sütun
- page.tsx: SELECT genişledi (opportunity_score, revenue_type), searchParams sort desteği, sıralama linki
- TypeScript temiz (tüm dosyalar)
- npm test green
- Kullanıcı checkpoint'i onayladı
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/17-keyword-intelligence/17-03-SUMMARY.md` oluştur.
</output>
