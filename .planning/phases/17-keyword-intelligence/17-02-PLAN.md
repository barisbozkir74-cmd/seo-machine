---
phase: 17-keyword-intelligence
plan: 02
type: execute
wave: 1
depends_on:
  - "17-01"
files_modified:
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts
autonomous: true
requirements:
  - NICH-01
  - RVEN-01

must_haves:
  truths:
    - "Keyword taşındığında hem eski hem yeni cluster'ın niche skoru güncellenir"
    - "Keyword silindiğinde cluster'ın niche skoru güncellenir"
    - "Kümeleme tamamlandığında tüm cluster'ların niche skoru ve revenue_type'ı DB'ye yazılır"
    - "Revenue override action'ı sadece whitelist değerleri kabul eder"
    - "Tüm cluster mutasyonlarında user_id + project_id ownership doğrulanır"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts"
      provides: "recalculateClusterNicheScore helper + updateClusterRevenue action + 3 injection point"
      exports: ["updateClusterRevenue"]
      contains: "recalculateClusterNicheScore"
  key_links:
    - from: "actions.ts recalculateClusterNicheScore"
      to: "keyword_clusters.opportunity_score + revenue_type"
      via: "supabase UPDATE"
      pattern: "opportunity_score.*revenue_type"
    - from: "actions.ts updateClusterRevenue"
      to: "RevenueOverrideSelect (Wave 2)"
      via: "server action import"
      pattern: "export async function updateClusterRevenue"
---

<objective>
Wave 1: actions.ts'e niche skor hesaplama entegrasyonu ve `updateClusterRevenue` action ekle.

Purpose: D-01 kararı — skor DB'ye kaydedilir, her sayfa yükünde hesaplanmaz. Cluster mutasyonları (move, delete, cluster) skor güncellemesini tetikler. D-03 kararı — kullanıcı revenue_type'ı override edebilir.
Output: `recalculateClusterNicheScore` private helper + `updateClusterRevenue` exported action + 3 injection point (moveKeywordToCluster, deleteKeyword, clusterAndScoreKeywords).
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-keyword-intelligence/17-CONTEXT.md
@.planning/phases/17-keyword-intelligence/17-RESEARCH.md
@.planning/phases/17-keyword-intelligence/17-01-SUMMARY.md

<interfaces>
<!-- Wave 0'da oluşturulan niche-scoring.ts -->
```typescript
// src/lib/keywords/niche-scoring.ts
export type ClusterKeywordData = {
  volume: number | null
  cpc: number | null
  difficulty: number | null
  search_intent: string | null
}

export function calculateNicheScore(
  keywords: ClusterKeywordData[],
  context: { maxClusterVolume: number; maxCpc: number }
): number  // 0-100, 1 decimal

export function classifyRevenueType(
  keywords: ClusterKeywordData[]
): 'bilgi' | 'mixed' | 'ticari'
```

<!-- Mevcut actions.ts — değiştirilecek action'lar -->
```typescript
// 3 injection noktası:
// 1. moveKeywordToCluster — eski ve yeni cluster için recalculate
// 2. deleteKeyword — kw.cluster_id varsa recalculate (cluster silinmeyecekse)
// 3. clusterAndScoreKeywords — batch recalculate (tüm cluster'lar)

// Yeni export:
export async function updateClusterRevenue(
  clusterId: string,
  revenueType: string,
  projectId: string
): Promise<{ success: true } | { success: false; error: string }>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: recalculateClusterNicheScore private helper + injection points</name>
  <files>src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts (mevcut tüm action'lar — değiştirilecek)
    - src/lib/keywords/niche-scoring.ts (Wave 0 çıktısı — import edilecek)
    - .planning/phases/17-keyword-intelligence/17-RESEARCH.md (injection points tablosu, Pitfall 2 ve 3)
  </read_first>
  <behavior>
    - recalculateClusterNicheScore(clusterId, projectId, userId, supabase, allClusterVolumes) çağrıldığında:
      * keywords SELECT → volume, cpc, difficulty, search_intent WHERE cluster_id=? AND user_id=?
      * keywords boşsa erken return (cluster zaten silinmiş olabilir)
      * maxClusterVolume = Math.max(...allClusterVolumes, 0)
      * maxCpc = Math.max(...keywords.map(k => k.cpc ?? 0), 0)
      * calculateNicheScore + classifyRevenueType çağır
      * keyword_clusters UPDATE opportunity_score + revenue_type WHERE id=? AND user_id=?
    - moveKeywordToCluster: mevcut revalidatePath'den ÖNCE hem eski (kw.cluster_id) hem yeni (newClusterId) cluster için recalculate
    - deleteKeyword: keyword silinip cluster hâlâ mevcutsa (count > 0) kw.cluster_id için recalculate
    - clusterAndScoreKeywords: for döngüsü tamamlanıp clusterCount hesaplandıktan sonra tüm cluster'lar için batch recalculate (Pitfall 3: tek normalizasyon context — tüm cluster toplam volume'larını önce topla, sonra normalize et)
  </behavior>
  <action>
actions.ts dosyasının başına import ekle:
```typescript
import { calculateNicheScore, classifyRevenueType } from '@/lib/keywords/niche-scoring'
```

Dosyanın sonuna (setPrimaryKeyword'den sonra) `recalculateClusterNicheScore` private helper ekle:

```typescript
// ─── Phase 17: Niche Score Recalculation Helper ──────────────────────────────

async function recalculateClusterNicheScore(
  clusterId: string,
  userId: string,
  supabase: Awaited<ReturnType<typeof import('@/lib/supabase/server').createClient>>,
  allClusterVolumes: number[]
): Promise<void> {
  const { data: keywords } = await supabase
    .from('keywords')
    .select('volume, cpc, difficulty, search_intent')
    .eq('cluster_id', clusterId)
    .eq('user_id', userId)

  if (!keywords || keywords.length === 0) return

  const maxClusterVolume = Math.max(...allClusterVolumes, 0)
  const maxCpc = Math.max(...keywords.map((k) => k.cpc ?? 0), 0)

  const nicheScore = calculateNicheScore(keywords, { maxClusterVolume, maxCpc })
  const revenueType = classifyRevenueType(keywords)

  await supabase
    .from('keyword_clusters')
    .update({ opportunity_score: nicheScore, revenue_type: revenueType })
    .eq('id', clusterId)
    .eq('user_id', userId)
}
```

**moveKeywordToCluster** fonksiyonu içinde, mevcut `revalidatePath(...)` satırından ÖNCE şu bloğu ekle:

```typescript
  // Phase 17: Hem eski hem yeni cluster'ın niche skorunu güncelle
  // Normalizasyon için proje cluster volume'larını çek
  const { data: allClusters } = await supabase
    .from('keyword_clusters')
    .select('id, total_volume')
    .eq('project_id', projectId)
    .eq('user_id', user.id)
  const allClusterVolumes = (allClusters ?? []).map((c) => c.total_volume ?? 0)

  // Eski cluster (keyword çıktı — Pitfall 2)
  if (kw.cluster_id && kw.cluster_id !== newClusterId) {
    await recalculateClusterNicheScore(kw.cluster_id, user.id, supabase, allClusterVolumes)
  }
  // Yeni cluster
  await recalculateClusterNicheScore(newClusterId, user.id, supabase, allClusterVolumes)
```

**deleteKeyword** fonksiyonu içinde, mevcut `revalidatePath(...)` satırından ÖNCE şu bloğu ekle (cluster silinmeyecekse):

```typescript
  // Phase 17: Keyword silindi ama cluster hâlâ varsa niche skorunu güncelle
  if (kw.cluster_id) {
    const { count: remaining } = await supabase
      .from('keywords')
      .select('id', { count: 'exact', head: true })
      .eq('cluster_id', kw.cluster_id)
    if ((remaining ?? 0) > 0) {
      const { data: allClusters } = await supabase
        .from('keyword_clusters')
        .select('id, total_volume')
        .eq('project_id', projectId)
        .eq('user_id', user.id)
      const allClusterVolumes = (allClusters ?? []).map((c) => c.total_volume ?? 0)
      await recalculateClusterNicheScore(kw.cluster_id, user.id, supabase, allClusterVolumes)
    }
  }
```

NOT: deleteKeyword'deki mevcut "kümedeki son keyword ise kümeyi sil" bloğu dokunulmaz. Yeni blok ondan ÖNCE eklenecek — remaining > 0 kontrolü zaten garanti eder.

**clusterAndScoreKeywords** fonksiyonu içinde, `revalidatePath(...)` satırından ÖNCE şu bloğu ekle (Pitfall 3 — tek normalizasyon context):

```typescript
  // Phase 17: Tüm cluster'lar için niche skoru batch hesaplama
  // Pitfall 3: Her cluster kendi max değerine göre normalize edilmemeli — proje geneli max kullanılır
  const { data: allClusterRows } = await supabase
    .from('keyword_clusters')
    .select('id, total_volume')
    .eq('project_id', projectId)
    .eq('user_id', user.id)
  const allClusterVolumes = (allClusterRows ?? []).map((c) => c.total_volume ?? 0)

  await Promise.all(
    (allClusterRows ?? []).map((cluster) =>
      recalculateClusterNicheScore(cluster.id, user.id, supabase, allClusterVolumes)
    )
  )
```
  </action>
  <verify>
    <automated>cd /c/Users/baris/Documents/seo-machine && npx vitest run src/lib/keywords/niche-scoring.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import.*niche-scoring" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → sonuç mevcut
    - `grep "recalculateClusterNicheScore" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts | wc -l` → en az 5 (1 tanım + 4 çağrı)
    - `grep "opportunity_score.*revenue_type" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → sonuç mevcut (UPDATE satırı)
    - TypeScript derleme hatası yok: `npx tsc --noEmit 2>&1 | grep "actions.ts"` → boş çıktı
    - Niche scoring testleri hâlâ green: `npx vitest run src/lib/keywords/niche-scoring.test.ts` → 5 passed
  </acceptance_criteria>
  <done>recalculateClusterNicheScore 3 action'a inject edilmiş, TypeScript temiz, testler green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: updateClusterRevenue server action (revenue override — D-03)</name>
  <files>src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts</files>
  <read_first>
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts (Task 1 sonrası güncel hali)
    - .planning/phases/17-keyword-intelligence/17-RESEARCH.md (Security domain — whitelist kontrolü T-17-02)
  </read_first>
  <behavior>
    - updateClusterRevenue(clusterId, revenueType, projectId) çağrıldığında:
      * Input whitelist: revenueType sadece 'bilgi', 'mixed', 'ticari' olabilir
      * Geçersiz değer → { success: false, error: 'Geçersiz gelir tipi.' }
      * UUID format kontrolü: clusterId, projectId
      * Auth: getUser() → user yok → { success: false, error: 'Oturum bulunamadı.' }
      * Ownership: keyword_clusters SELECT WHERE id=clusterId AND project_id=projectId AND user_id=user.id → yok → { success: false, error: 'Küme bulunamadı.' }
      * UPDATE revenue_type WHERE id=clusterId AND user_id=user.id
      * revalidatePath
      * { success: true }
  </behavior>
  <action>
actions.ts dosyasının sonuna (recalculateClusterNicheScore'dan sonra) yeni export action ekle:

```typescript
// ─── Phase 17: Revenue Override Action ───────────────────────────────────────

export type UpdateClusterRevenueResult =
  | { success: true }
  | { success: false; error: string }

const VALID_REVENUE_TYPES = ['bilgi', 'mixed', 'ticari'] as const

export async function updateClusterRevenue(
  clusterId: string,
  revenueType: string,
  projectId: string
): Promise<UpdateClusterRevenueResult> {
  // Whitelist kontrolü — T-17-02 (Tampering: geçersiz değer enjeksiyonu)
  if (!VALID_REVENUE_TYPES.includes(revenueType as typeof VALID_REVENUE_TYPES[number])) {
    return { success: false, error: 'Geçersiz gelir tipi.' }
  }

  // UUID format validation
  const uuidRegex = /^[0-9a-f-]{36}$/i
  if (!uuidRegex.test(clusterId) || !uuidRegex.test(projectId)) {
    return { success: false, error: 'Geçersiz ID formatı.' }
  }

  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Oturum bulunamadı.' }

  // Ownership doğrula — T-17-01 (Tampering: başka projenin cluster'ına yazma)
  const { data: cluster } = await supabase
    .from('keyword_clusters')
    .select('id')
    .eq('id', clusterId)
    .eq('project_id', projectId)
    .eq('user_id', user.id)
    .single()
  if (!cluster) return { success: false, error: 'Küme bulunamadı.' }

  const { error } = await supabase
    .from('keyword_clusters')
    .update({ revenue_type: revenueType })
    .eq('id', clusterId)
    .eq('user_id', user.id)

  if (error) return { success: false, error: 'Güncelleme başarısız.' }

  revalidatePath(`/projeler/${projectId}/keyword-stratejisi`)
  return { success: true }
}
```
  </action>
  <verify>
    <automated>cd /c/Users/baris/Documents/seo-machine && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export async function updateClusterRevenue" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → sonuç mevcut
    - `grep "VALID_REVENUE_TYPES" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → sonuç mevcut (whitelist array)
    - `grep "export type UpdateClusterRevenueResult" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → sonuç mevcut
    - Ownership satırı: `grep "project_id.*projectId" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts | grep "user_id"` → sonuç mevcut
    - TypeScript temiz: `npx tsc --noEmit` → 0 error TS hatası
    - `npm test` → full suite green
  </acceptance_criteria>
  <done>updateClusterRevenue action exported, whitelist ve ownership kontrolü var, TypeScript temiz</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → updateClusterRevenue | Revenue type değeri kullanıcıdan geliyor — whitelist zorunlu |
| Client → moveKeywordToCluster / deleteKeyword | clusterId kullanıcıdan geliyor — ownership doğrulanmış (mevcut) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | updateClusterRevenue — başka projenin cluster'ına revenue_type yazma | mitigate | `project_id + user_id` ownership SELECT — cluster bulunamazsa { success: false } döner |
| T-17-02 | Tampering | updateClusterRevenue — geçersiz revenue_type değeri enjeksiyonu | mitigate | `VALID_REVENUE_TYPES` whitelist array; değer listede değilse erken return |
| T-17-03 | Elevation of Privilege | recalculateClusterNicheScore — başka kullanıcının cluster skorunu tetikleme | mitigate | keywords SELECT'te `user_id` filtresi zorunlu; UPDATE'te `user_id` filtresi zorunlu |
</threat_model>

<verification>
- `grep "export async function updateClusterRevenue" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → mevcut
- `grep "VALID_REVENUE_TYPES" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → mevcut
- `grep "recalculateClusterNicheScore" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts | wc -l` → ≥5
- `npx tsc --noEmit` → 0 TypeScript hatası
- `npm test` → full suite green
</verification>

<success_criteria>
- `updateClusterRevenue` action exported, whitelist + UUID + ownership kontrolü tam
- `recalculateClusterNicheScore` private helper 3 action'a inject edilmiş: moveKeywordToCluster, deleteKeyword, clusterAndScoreKeywords
- Eski cluster da güncelleniyor (moveKeywordToCluster Pitfall 2)
- Batch normalizasyon tek context ile yapılıyor (clusterAndScoreKeywords Pitfall 3)
- TypeScript temiz, npm test green
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/17-keyword-intelligence/17-02-SUMMARY.md` oluştur.
</output>
