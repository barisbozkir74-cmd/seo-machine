---
phase: 05-keyword-import-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dataforseo/client.ts
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts
autonomous: true
requirements:
  - KEYW-01
  - KEYW-02
  - KEYW-03

must_haves:
  truths:
    - "importKeywords çağrıldığında DB insert'ten sonra DataForSEO'dan volume/CPC/KD/intent otomatik çekilir"
    - "Enrichment tamamlanan keyword satırlarında enriched_at IS NOT NULL olur"
    - "deleteKeyword çağrıldığında keyword DB'den silinir; kullanıcı + proje sahipliği doğrulanır"
    - "Kümedeki son keyword silinirse küme de silinir"
    - "Enrichment başarısız olsa bile importKeywords başarı döner — satırlar enriched_at=null kalır"
  artifacts:
    - path: "src/lib/dataforseo/client.ts"
      provides: "fetchKeywordData fonksiyonu — DataForSEO keywords_data/google_ads/search_volume/live endpoint"
      exports: ["fetchKeywordData", "KeywordDataItem"]
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts"
      provides: "importKeywords (enrichment akışı eklenmiş) + deleteKeyword Server Action"
      exports: ["importKeywords", "deleteKeyword", "DeleteKeywordResult"]
  key_links:
    - from: "actions.ts importKeywords"
      to: "client.ts fetchKeywordData"
      via: "doğrudan import + await çağrısı"
      pattern: "fetchKeywordData\\(keywords, credentials\\)"
    - from: "actions.ts importKeywords"
      to: "supabase keywords tablosu"
      via: ".update({ volume, cpc, difficulty, search_intent, enriched_at })"
      pattern: "enriched_at.*new Date\\(\\)\\.toISOString\\(\\)"
    - from: "actions.ts deleteKeyword"
      to: "supabase keywords tablosu"
      via: ".delete().eq('id', keywordId).eq('user_id', user.id)"
      pattern: "deleteKeyword"
---

<objective>
DataForSEO'dan keyword verisi çeken lib fonksiyonunu ekle ve keyword-stratejisi actions'ına enrichment akışı ile deleteKeyword action'ını ekle.

Purpose: Tüm enrichment mantığı (fetchKeywordData + importKeywords genişlemesi + deleteKeyword) bu planda tamamlanır. Plan 2 bu altyapıya bağımlı.
Output: src/lib/dataforseo/client.ts'e fetchKeywordData + KeywordDataItem; actions.ts'e deleteKeyword + importKeywords enrichment genişlemesi.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-keyword-import-enrichment/05-CONTEXT.md
@.planning/phases/05-keyword-import-enrichment/05-PATTERNS.md

<interfaces>
<!-- src/lib/dataforseo/client.ts — mevcut export'lar (değişmeyecek, yeni fonksiyon eklenecek) -->
```typescript
// server-only modül
export async function fetchSerpDomains(keywords, credentials, location?): Promise<string[]>
export async function fetchTopPages(domain, credentials, location?): Promise<{ items: TopPageItem[]; totalCount: number }>
export async function fetchRankedKeywords(domain, credentials, limit?, location?): Promise<RankedKeywordItem[]>
export async function fetchBacklinksSummary(domain, credentials): Promise<BacklinksSummary | null>
// Auth header pattern (tüm fonksiyonlarda tekrar eder):
// const authHeader = `Basic ${Buffer.from(`${credentials.login}:${credentials.password}`).toString('base64')}`
// Error pattern:
// if (!response.ok) throw new Error(`DataForSEO [Ad] API hatası: ${response.status}`)
// Result pattern: data.tasks?.[0]?.result ?? []
```

<!-- src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts — mevcut export'lar -->
```typescript
'use server'
export type ImportKeywordsResult = { success: true; clusterCount: number; keywordCount: number } | { success: false; error: string }
export async function importKeywords(projectId: string, text: string): Promise<ImportKeywordsResult>
export type DeleteClusterResult = { success: true } | { success: false; error: string }
export async function deleteCluster(projectId: string, clusterId: string): Promise<DeleteClusterResult>
// Auth pattern: createClient() → getUser() → project ownership check
// revalidatePath(`/projeler/${projectId}/keyword-stratejisi`)
```

<!-- src/lib/supabase/vault.ts -->
```typescript
export async function getDataForSeoCredentials(): Promise<{ login: string; password: string }>
// Env fallback: DATAFORSEO_LOGIN + DATAFORSEO_PASSWORD env var mevcutsa Vault'u atlar
```

<!-- supabase keywords tablosu alanları (20260422000001_create_tables.sql'den) -->
// id, user_id, project_id, cluster_id, keyword, volume, cpc, difficulty, search_intent, enriched_at, source
// upsert onConflict: 'project_id,keyword'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fetchKeywordData fonksiyonunu client.ts'e ekle</name>
  <files>src/lib/dataforseo/client.ts</files>

  <read_first>
    - src/lib/dataforseo/client.ts — mevcut auth header, error ve result pattern'larını gör; yeni fonksiyonu dosyanın sonuna ekle
    - src/lib/supabase/vault.ts — credentials shape'ini doğrula: { login: string; password: string }
    - .planning/phases/05-keyword-import-enrichment/05-PATTERNS.md — fetchKeywordData tam implementasyon bloğu (satır 305-353)
  </read_first>

  <action>
Dosyanın mevcut içeriğini koruyarak sonuna şunu ekle:

```typescript
export type KeywordDataItem = {
  keyword: string
  search_volume: number | null
  cpc: number | null
  keyword_difficulty: number | null
  search_intent: string | null
}

export async function fetchKeywordData(
  keywords: string[],
  credentials: { login: string; password: string },
  location: { locationCode: number; languageCode: string } = { locationCode: 2792, languageCode: 'tr' }
): Promise<KeywordDataItem[]> {
  if (keywords.length === 0) return []

  const authHeader = `Basic ${Buffer.from(`${credentials.login}:${credentials.password}`).toString('base64')}`

  const response = await fetch(
    'https://api.dataforseo.com/v3/keywords_data/google_ads/search_volume/live',
    {
      method: 'POST',
      headers: { Authorization: authHeader, 'Content-Type': 'application/json' },
      body: JSON.stringify([
        {
          keywords,
          location_code: location.locationCode,
          language_code: location.languageCode,
        },
      ]),
    }
  )

  if (!response.ok) throw new Error(`DataForSEO Keyword Data API hatası: ${response.status}`)

  const data = await response.json()
  const items: Record<string, unknown>[] = data.tasks?.[0]?.result ?? []

  return items.map((item) => ({
    keyword: item.keyword as string,
    search_volume: (item.search_volume as number) ?? null,
    cpc: (item.cpc as number) ?? null,
    keyword_difficulty: (item.keyword_difficulty as number) ?? null,
    search_intent: (item.search_intent as string) ?? null,
  }))
}
```

Notlar:
- `keywords.length === 0` guard: boş liste için API çağrısı yapma.
- `location_code: 2792` → Türkiye, `language_code: 'tr'` → CONTEXT.md D-08 uyarınca.
- Endpoint: `keywords_data/google_ads/search_volume/live` (PATTERNS.md satır 325 — `google_ads` doğru path).
- Mevcut hiçbir fonksiyona dokunma.
  </action>

  <verify>
    <automated>cd C:/Users/baris/Documents/seo-machine && npx tsc --noEmit 2>&1 | grep "client.ts" || echo "client.ts: no TS errors"</automated>
  </verify>

  <acceptance_criteria>
    - `src/lib/dataforseo/client.ts` dosyası `export type KeywordDataItem` içeriyor
    - `src/lib/dataforseo/client.ts` dosyası `export async function fetchKeywordData` içeriyor
    - `fetchKeywordData` içinde `keywords_data/google_ads/search_volume/live` string'i mevcut
    - `fetchKeywordData` içinde `location_code: location.locationCode` mevcut (hardcode değil)
    - `fetchKeywordData` içinde `if (!response.ok) throw new Error` satırı mevcut
    - TypeScript derleme hatası yok (`npx tsc --noEmit` çıkışında `client.ts` hatası görünmüyor)
  </acceptance_criteria>

  <done>KeywordDataItem tipi ve fetchKeywordData fonksiyonu client.ts'e eklendi; mevcut fonksiyonlar değişmedi; TypeScript hatası yok.</done>
</task>

<task type="auto">
  <name>Task 2: actions.ts — importKeywords'e enrichment + deleteKeyword ekle</name>
  <files>src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts</files>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts — mevcut importKeywords ve deleteCluster implementasyonlarını gör; hangi import'ların mevcut olduğunu doğrula
    - src/lib/dataforseo/client.ts — Task 1'de eklenen fetchKeywordData ve KeywordDataItem export'larını kontrol et
    - .planning/phases/05-keyword-import-enrichment/05-PATTERNS.md — importKeywords enrichment bloğu (satır 192-228) ve deleteKeyword tam kodu (satır 230-279)
    - src/app/(dashboard)/projeler/[id]/rakipler/actions.ts — verifyProjectOwnership helper pattern'ı (satır 19-32)
  </read_first>

  <action>
Dosyayı tamamen yeniden yaz (tüm mevcut kod korunur, üstüne eklenir):

**Import değişikliği** — dosyanın başına şu iki import'u ekle:
```typescript
import { getDataForSeoCredentials } from '@/lib/supabase/vault'
import { fetchKeywordData } from '@/lib/dataforseo/client'
```

**ImportKeywordsResult tipini güncelle** — başarı durumuna `enrichedCount` ekle (D-05 gereği enrichment durumu mesajında gösterilir):
```typescript
export type ImportKeywordsResult =
  | { success: true; clusterCount: number; keywordCount: number; enrichedCount: number }
  | { success: false; error: string }
```

**importKeywords fonksiyonu** — mevcut `revalidatePath` satırından ÖNCE enrichment bloğunu ekle:
```typescript
  // Enrichment — D-05: import sonrası otomatik başlar
  let enrichedCount = 0
  try {
    const credentials = await getDataForSeoCredentials()
    const keywordTexts = parsed.map((p) => p.keyword)
    const enriched = await fetchKeywordData(keywordTexts, credentials)

    for (const item of enriched) {
      if (!item.keyword) continue
      const updatePayload: Record<string, unknown> = {
        enriched_at: new Date().toISOString(),
      }
      // D-06: DataForSEO verisi manuel girilen değerlerin üstüne yazar
      if (item.search_volume !== null) updatePayload.volume = item.search_volume
      if (item.cpc !== null) updatePayload.cpc = item.cpc
      if (item.keyword_difficulty !== null) updatePayload.difficulty = item.keyword_difficulty
      if (item.search_intent !== null) updatePayload.search_intent = item.search_intent

      const { error: updateErr } = await supabase
        .from('keywords')
        .update(updatePayload)
        .eq('project_id', projectId)
        .eq('user_id', user.id)
        .eq('keyword', item.keyword)
      if (!updateErr) enrichedCount++
    }
  } catch (err) {
    // Enrichment sessiz fail — import başarılı sayılır, satırlar enriched_at=null kalır (D-07)
    console.error('[importKeywords] enrichment failed:', err)
  }

  revalidatePath(`/projeler/${projectId}/keyword-stratejisi`)
  return { success: true, clusterCount: clusters.length, keywordCount: parsed.length, enrichedCount }
```

**deleteKeyword Server Action** — dosyanın sonuna ekle (deleteCluster'dan SONRA):
```typescript
export type DeleteKeywordResult = { success: true } | { success: false; error: string }

export async function deleteKeyword(
  keywordId: string,
  projectId: string
): Promise<DeleteKeywordResult> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Oturum bulunamadı.' }

  // Ownership: keyword'ün bu projeye ve bu user'a ait olduğunu doğrula
  const { data: kw } = await supabase
    .from('keywords')
    .select('id, cluster_id')
    .eq('id', keywordId)
    .eq('project_id', projectId)
    .eq('user_id', user.id)
    .single()
  if (!kw) return { success: false, error: 'Keyword bulunamadı.' }

  const { error } = await supabase
    .from('keywords')
    .delete()
    .eq('id', keywordId)
    .eq('user_id', user.id)

  if (error) return { success: false, error: 'Keyword silinemedi.' }

  // Kümedeki son keyword ise kümeyi de sil (D-12 interaction contract)
  if (kw.cluster_id) {
    const { count } = await supabase
      .from('keywords')
      .select('id', { count: 'exact', head: true })
      .eq('cluster_id', kw.cluster_id)
    if ((count ?? 0) === 0) {
      await supabase
        .from('keyword_clusters')
        .delete()
        .eq('id', kw.cluster_id)
        .eq('user_id', user.id)
    }
  }

  revalidatePath(`/projeler/${projectId}/keyword-stratejisi`)
  return { success: true }
}
```

**Notlar:**
- `'use server'` direktifi dosyanın başında korunur.
- `parseKeywordText` ve `clusterKeywords` import'ları değişmez.
- `deleteCluster` fonksiyonu değişmez — olduğu gibi kalır.
- `KeywordImport.tsx` başarı mesajı `enrichedCount` göstermez (KeywordImport.tsx DEĞIŞMEZ — D-01). Başarı mesajı zaten `{keywordCount} keyword, {clusterCount} kümeye ayrıldı.` formatında KeywordImport.tsx'te hardcoded. `enrichedCount` field'ı ileride kullanılabilir, result tipinde var.
  </action>

  <verify>
    <automated>cd C:/Users/baris/Documents/seo-machine && npx tsc --noEmit 2>&1 | grep "actions.ts" || echo "actions.ts: no TS errors"</automated>
  </verify>

  <acceptance_criteria>
    - `actions.ts` başında `import { getDataForSeoCredentials } from '@/lib/supabase/vault'` satırı mevcut
    - `actions.ts` başında `import { fetchKeywordData } from '@/lib/dataforseo/client'` satırı mevcut
    - `actions.ts` içinde `export async function deleteKeyword` tanımı mevcut
    - `deleteKeyword` içinde `.eq('project_id', projectId).eq('user_id', user.id)` ownership check mevcut
    - `deleteKeyword` içinde `count === 0` → küme silme bloğu mevcut (`keyword_clusters delete`)
    - `importKeywords` içinde `enriched_at: new Date().toISOString()` satırı mevcut
    - `importKeywords` içinde `console.error('[importKeywords] enrichment failed'` catch bloğu mevcut
    - `DeleteKeywordResult` type export edilmiş
    - TypeScript derleme hatası yok
  </acceptance_criteria>

  <done>importKeywords enrichment akışıyla (DataForSEO çağrısı + toplu update + sessiz fail) genişledi; deleteKeyword ownership doğrulamalı ve küme cleanup'lı olarak eklendi; TypeScript hatası yok.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | importKeywords ve deleteKeyword'e gelen projectId, keywordId parametreleri güvenilmez |
| Server Action → DataForSEO | DATAFORSEO_LOGIN/PASSWORD credentials, network dışına çıkar |
| Server Action → Supabase | SQL injection riski — Supabase client parametreli sorgu kullanır |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Spoofing | deleteKeyword — keywordId/projectId | mitigate | Her iki parametreye ve user.id'ye göre ownership check: `.eq('id', keywordId).eq('project_id', projectId).eq('user_id', user.id)` — üçlü doğrulama |
| T-05-01-02 | Elevation of Privilege | importKeywords — projectId manipülasyonu | mitigate | `projects` tablosunda `.eq('user_id', user.id)` ile proje sahipliği doğrulanıyor; başka kullanıcının projesine keyword eklenemez |
| T-05-01-03 | Information Disclosure | DATAFORSEO_LOGIN/PASSWORD | mitigate | `vault.ts` server-only import; env var fallback — NEXT_PUBLIC_ prefix'i yok; client bundle'a sızmaz |
| T-05-01-04 | Denial of Service | DataForSEO API çağrısı — büyük keyword listesi | accept | Tek batch call, API rate limit DataForSEO tarafında yönetilir; enrichment başarısız olsa bile import devam eder (sessiz fail) |
| T-05-01-05 | Tampering | deleteKeyword — başka kullanıcının keyword'ünü silme | mitigate | `.eq('user_id', user.id)` ile delete sorgusu kısıtlandı — user_id eşleşmeden silme yapılamaz |
| T-05-01-06 | Injection | keyword metni — Supabase'e yazılan değerler | accept | Supabase JS client parametreli sorgular kullanır — SQL injection riski yok; `keyword` text alanı XSS riski taşımaz (server-rendered tablo) |
</threat_model>

<verification>
```bash
# 1. TypeScript derlemesi temiz
cd C:/Users/baris/Documents/seo-machine
npx tsc --noEmit

# 2. fetchKeywordData export doğrulaması
grep -n "export.*fetchKeywordData\|export.*KeywordDataItem" src/lib/dataforseo/client.ts

# 3. deleteKeyword export doğrulaması
grep -n "export.*deleteKeyword\|export.*DeleteKeywordResult" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts

# 4. Enrichment bloğu doğrulaması
grep -n "enriched_at\|getDataForSeoCredentials\|fetchKeywordData" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts

# 5. Ownership üçlüsü doğrulaması (deleteKeyword)
grep -A5 "deleteKeyword" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts | grep "project_id\|user_id"
```
</verification>

<success_criteria>
- `fetchKeywordData` ve `KeywordDataItem` client.ts'ten export edilmiş
- `importKeywords` enrichment akışını içeriyor (DataForSEO çağrısı → toplu update → sessiz fail)
- `deleteKeyword` ownership doğrulamalı (projectId + userId + keywordId üçlüsü) ve küme cleanup'lı
- `npx tsc --noEmit` sıfır hata
- parser.ts ve clustering.ts dokunulmadı
- KeywordImport.tsx dokunulmadı
</success_criteria>

<output>
Tamamlanınca `.planning/phases/05-keyword-import-enrichment/05-01-SUMMARY.md` oluştur.
</output>
