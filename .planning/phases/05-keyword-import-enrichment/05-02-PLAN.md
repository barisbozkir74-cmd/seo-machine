---
phase: 05-keyword-import-enrichment
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordDeleteButton.tsx
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/IntentBadge.tsx
autonomous: true
requirements:
  - KEYW-01
  - KEYW-02
  - KEYW-03

must_haves:
  truths:
    - "Keyword listesi küme-gruplu değil, tek düz tablo olarak gösterilir"
    - "Tablo sütunları: (×) | Keyword | Volume | CPC | KD | Küme | Intent — bu sırayla"
    - "enriched_at IS NULL olan satırlar opacity-50 + spinner gösterir"
    - "× butonu satır hover'ında görünür (opacity-0 group-hover:opacity-100); tıklanınca anında siler"
    - "Intent badge'leri doğru renk className ile gösterilir (variant prop kullanılmaz)"
    - "Boş durumda Türkçe yönlendirme metni gösterilir"
    - "Volume azalan sırada sıralanır (DB sorgusu .order('volume', { ascending: false, nullsFirst: false }))"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx"
      provides: "Düz keyword tablosu — tüm sütunlar, enrichment loading state, boş durum"
      contains: "KeywordDeleteButton"
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordDeleteButton.tsx"
      provides: "× silme butonu — useTransition, opacity-0 group-hover:opacity-100, destructive hover"
      exports: ["KeywordDeleteButton"]
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/IntentBadge.tsx"
      provides: "Intent string → renkli badge mapping"
      exports: ["IntentBadge"]
  key_links:
    - from: "page.tsx TableRow"
      to: "KeywordDeleteButton"
      via: "doğrudan import + render in <TableCell>"
      pattern: "<KeywordDeleteButton projectId=\\{id\\} keywordId=\\{kw\\.id\\}"
    - from: "page.tsx TableRow"
      to: "IntentBadge"
      via: "doğrudan import + render in <TableCell>"
      pattern: "<IntentBadge intent=\\{kw\\.search_intent\\}"
    - from: "page.tsx"
      to: "supabase keywords tablosu"
      via: ".order('volume', { ascending: false, nullsFirst: false })"
      pattern: "ascending.*false.*nullsFirst.*false"
---

<objective>
keyword-stratejisi sayfasını düz tabloya dönüştür, KeywordDeleteButton ve IntentBadge bileşenlerini oluştur.

Purpose: Kullanıcı keyword listesini enrichment durumu ile birlikte (opacity-50 + spinner), intent badge ve × silme butonu ile görebilir. Mevcut cluster-grouped görünüm düz tabloya değişir.
Output: Güncellenmiş page.tsx (düz tablo) + iki yeni Client Component.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-keyword-import-enrichment/05-CONTEXT.md
@.planning/phases/05-keyword-import-enrichment/05-UI-SPEC.md
@.planning/phases/05-keyword-import-enrichment/05-PATTERNS.md
@.planning/phases/05-keyword-import-enrichment/05-01-SUMMARY.md

<interfaces>
<!-- KeywordDeleteButton — actions.ts'ten import edilecek (05-01 tarafından oluşturuldu) -->
```typescript
// src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts
export type DeleteKeywordResult = { success: true } | { success: false; error: string }
export async function deleteKeyword(keywordId: string, projectId: string): Promise<DeleteKeywordResult>
```

<!-- ClusterDeleteButton.tsx — KeywordDeleteButton için analog (EXACT MATCH, onay dialogu YOK) -->
```typescript
// src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterDeleteButton.tsx
'use client'
import { useTransition } from 'react'
import { Button } from '@/components/ui/button'
// useTransition + startTransition(async () => { await action(...) }) pattern
```

<!-- page.tsx — mevcut yapı (tamamen değiştirilecek) -->
// Mevcut sütunlar: Keyword | Volume | KD% | Zorluk (cluster-grouped)
// Yeni sütunlar: (×) | Keyword | Volume | CPC | KD | Küme | Intent
// Mevcut yardımcı fonksiyonlar KORUNUR: kdColor, formatVolume

<!-- Tablo sorgusu — yeni (cluster-grouped sorgu YERINE) -->
```typescript
// Tek düz sorgu, volume azalan (D-13)
const { data: keywordsRaw } = await supabase
  .from('keywords')
  .select('id, keyword, volume, cpc, difficulty, search_intent, enriched_at, cluster_id')
  .eq('project_id', id)
  .eq('user_id', user.id)
  .order('volume', { ascending: false, nullsFirst: false })

// Cluster adları için ayrı sorgu
const { data: clustersRaw } = await supabase
  .from('keyword_clusters')
  .select('id, cluster_name')
  .eq('project_id', id)
  .eq('user_id', user.id)

const clusterMap: Record<string, string> = {}
for (const c of clustersRaw ?? []) {
  clusterMap[c.id] = c.cluster_name
}
```

<!-- Intent badge renk atamaları (CONTEXT.md D-11 + UI-SPEC — className ile, variant prop YOK) -->
// 'commercial'     → 'bg-blue-500/20 text-blue-400'
// 'informational'  → 'bg-emerald-500/20 text-emerald-400'
// 'navigational'   → 'bg-gray-500/20 text-gray-400'
// 'transactional'  → 'bg-orange-500/20 text-orange-400'
// null / bilinmiyor → <span className="text-sm text-muted-foreground">—</span>

<!-- KeywordDeleteButton UI-SPEC -->
// className="h-7 w-7 p-0 opacity-0 group-hover:opacity-100 transition-opacity text-muted-foreground hover:text-destructive hover:bg-destructive/10"
// title="Keyword'ü sil"
// × (×) veya "×" string — isPending durumunda rotate spinner

<!-- Spinner (enrichment loading state, satır başına) -->
// 16px SVG spinner — animate-spin, text-muted-foreground
// enriched_at IS NULL → satır className'e 'opacity-50' eklenir + sağ uç Intent sütununa spinner

<!-- Küme badge -->
// <Badge className="bg-secondary text-muted-foreground text-xs border-0">{clusterMap[kw.cluster_id ?? '']}</Badge>
// cluster_id yoksa veya clusterMap'te bulunmazsa: <span className="text-sm text-muted-foreground">—</span>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IntentBadge ve KeywordDeleteButton bileşenlerini oluştur</name>
  <files>
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/IntentBadge.tsx
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordDeleteButton.tsx
  </files>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterDeleteButton.tsx — KeywordDeleteButton için exact analog; useTransition pattern'ını aynen kullan
    - src/components/ui/badge.tsx — Badge bileşeni API'sini gör (className prop kabul ediyor mu doğrula)
    - src/components/ui/button.tsx — Button variant="ghost" size="sm" kullanımını gör
    - .planning/phases/05-keyword-import-enrichment/05-PATTERNS.md — IntentBadge tam kod (satır 420-451), KeywordDeleteButton tam kod (satır 368-408)
    - .planning/phases/05-keyword-import-enrichment/05-UI-SPEC.md — × butonu ve intent badge renk spesifikasyonu
  </read_first>

  <action>
**IntentBadge.tsx — yeni dosya oluştur:**

```typescript
import { Badge } from '@/components/ui/badge'

// D-11 + UI-SPEC renk atamaları — className ile direkt, variant prop kullanılmaz
const intentConfig: Record<string, { className: string; label: string }> = {
  commercial:    { className: 'bg-blue-500/20 text-blue-400',     label: 'Commercial' },
  informational: { className: 'bg-emerald-500/20 text-emerald-400', label: 'Informational' },
  navigational:  { className: 'bg-gray-500/20 text-gray-400',    label: 'Navigational' },
  transactional: { className: 'bg-orange-500/20 text-orange-400', label: 'Transactional' },
}

export function IntentBadge({ intent }: { intent: string | null }) {
  if (!intent) return <span className="text-sm text-muted-foreground">—</span>

  // DataForSEO farklı case döndürebilir — normalize et
  const key = intent.toLowerCase().trim()
  const config = intentConfig[key]

  if (!config) return <span className="text-sm text-muted-foreground">{intent}</span>

  // variant prop kullanılmaz — STATE.md karar notu
  return (
    <Badge className={`${config.className} text-xs border-0`}>
      {config.label}
    </Badge>
  )
}
```

**KeywordDeleteButton.tsx — yeni dosya oluştur:**

```typescript
'use client'

import { useTransition } from 'react'
import { Button } from '@/components/ui/button'
import { deleteKeyword } from './actions'

export function KeywordDeleteButton({
  projectId,
  keywordId,
}: {
  projectId: string
  keywordId: string
}) {
  const [isPending, startTransition] = useTransition()

  return (
    <Button
      variant="ghost"
      size="sm"
      title="Keyword'ü sil"
      disabled={isPending}
      // UI-SPEC: opacity-0 group-hover:opacity-100, destructive hover (D-12)
      className="h-7 w-7 p-0 opacity-0 group-hover:opacity-100 transition-opacity text-muted-foreground hover:text-destructive hover:bg-destructive/10"
      onClick={() => {
        // D-12: onay dialogu YOK — anında sil
        startTransition(async () => {
          await deleteKeyword(keywordId, projectId)
        })
      }}
    >
      {isPending ? (
        <svg
          className="animate-spin h-3 w-3"
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
        >
          <circle
            className="opacity-25"
            cx="12" cy="12" r="10"
            stroke="currentColor"
            strokeWidth="4"
          />
          <path
            className="opacity-75"
            fill="currentColor"
            d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"
          />
        </svg>
      ) : (
        '×'
      )}
    </Button>
  )
}
```

Notlar:
- `IntentBadge.tsx`'te `'use client'` direktifi GEREKMİYOR — sadece JSX render eder, hook yok.
- `KeywordDeleteButton.tsx`'te `confirm()` kullanılmaz — D-12 kararı gereği onay yok.
- `ClusterDeleteButton.tsx` dokunulmaz.
  </action>

  <verify>
    <automated>cd C:/Users/baris/Documents/seo-machine && npx tsc --noEmit 2>&1 | grep -E "IntentBadge|KeywordDeleteButton" || echo "New components: no TS errors"</automated>
  </verify>

  <acceptance_criteria>
    - `IntentBadge.tsx` dosyası mevcut ve `export function IntentBadge` içeriyor
    - `IntentBadge.tsx` içinde `bg-blue-500/20 text-blue-400` (commercial), `bg-emerald-500/20 text-emerald-400` (informational), `bg-gray-500/20 text-gray-400` (navigational), `bg-orange-500/20 text-orange-400` (transactional) renkleri mevcut
    - `IntentBadge.tsx` içinde `intent.toLowerCase().trim()` normalizasyon mevcut
    - `KeywordDeleteButton.tsx` dosyası mevcut ve `'use client'` direktifini içeriyor
    - `KeywordDeleteButton.tsx` içinde `opacity-0 group-hover:opacity-100` className mevcut
    - `KeywordDeleteButton.tsx` içinde `confirm(` string'i YOK (onay dialogu olmamalı — D-12)
    - `KeywordDeleteButton.tsx` içinde `hover:text-destructive hover:bg-destructive/10` mevcut
    - TypeScript hatası yok
  </acceptance_criteria>

  <done>IntentBadge (intent string → renkli badge) ve KeywordDeleteButton (× silme, useTransition, onay yok) oluşturuldu; TypeScript hatası yok.</done>
</task>

<task type="auto">
  <name>Task 2: page.tsx'i düz tabloya dönüştür</name>
  <files>src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx</files>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx — mevcut cluster-grouped yapıyı gör; nelerin korunacağını (kdColor, formatVolume, header section, 2 sütunlu layout, import section) ve nelerin değişeceğini belirle
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/IntentBadge.tsx — Task 1'de oluşturuldu, import yolu doğrula
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordDeleteButton.tsx — Task 1'de oluşturuldu, import yolu doğrula
    - .planning/phases/05-keyword-import-enrichment/05-UI-SPEC.md — tablo sütun anatomisi, spinner pattern, boş durum copy
    - .planning/phases/05-keyword-import-enrichment/05-PATTERNS.md — page.tsx tam düz tablo sorgusu (satır 62-115) ve TypeRow tipi (satır 144-157)
  </read_first>

  <action>
Dosyayı tamamen yeniden yaz. Aşağıdaki yapıyı uygula:

**Import değişiklikleri:**
- `ClusterDeleteButton` import'unu KALDIR
- `Badge` import'unu EKLE: `import { Badge } from '@/components/ui/badge'`
- `IntentBadge` import'unu EKLE: `import { IntentBadge } from './IntentBadge'`
- `KeywordDeleteButton` import'unu EKLE: `import { KeywordDeleteButton } from './KeywordDeleteButton'`
- Diğer tüm import'lar korunur

**Yardımcı fonksiyonlar — korunur:**
```typescript
function kdColor(kd: number): { dot: string; label: string } {
  if (kd < 30) return { dot: 'bg-emerald-400', label: 'Kolay' }
  if (kd <= 60) return { dot: 'bg-amber-400', label: 'Orta' }
  return { dot: 'bg-red-400', label: 'Zor' }
}

function formatVolume(v: number): string {
  if (v >= 1_000_000) return (v / 1_000_000).toFixed(1).replace('.0', '') + 'M'
  if (v >= 1000) return (v / 1000).toFixed(1).replace('.0', '') + 'K'
  return v.toString()
}
```

**Type güncellemesi — ClusterRow kaldır, KeywordRow genişlet:**
```typescript
type KeywordRow = {
  id: string
  keyword: string
  volume: number | null
  cpc: number | null
  difficulty: number | null
  search_intent: string | null
  enriched_at: string | null
  cluster_id: string | null
}
```

**Server Component içi veri sorgusu — cluster-grouped sorgu YERINE:**
```typescript
// Keyword düz sorgusu — volume azalan (D-13)
const { data: keywordsRaw } = await supabase
  .from('keywords')
  .select('id, keyword, volume, cpc, difficulty, search_intent, enriched_at, cluster_id')
  .eq('project_id', id)
  .eq('user_id', user.id)
  .order('volume', { ascending: false, nullsFirst: false })

const keywords: KeywordRow[] = (keywordsRaw ?? []) as KeywordRow[]

// Cluster adları badge gösterimi için
const { data: clustersRaw } = await supabase
  .from('keyword_clusters')
  .select('id, cluster_name')
  .eq('project_id', id)
  .eq('user_id', user.id)

const clusterMap: Record<string, string> = {}
for (const c of clustersRaw ?? []) {
  clusterMap[c.id] = c.cluster_name
}

// Özet sayıları
const totalKeywords = keywords.length
const totalClusters = Object.keys(clusterMap).length
const pendingEnrichment = keywords.filter((kw) => !kw.enriched_at).length
```

**JSX — header ve import section değişmez; sadece "Keyword Kümeleri" bölümünü değiştir:**

Bölüm başlığını `"Keyword Listesi"` yap:
```tsx
<h2 className="text-base font-semibold">
  Keyword Listesi
  {totalKeywords > 0 && (
    <span className="ml-2 text-sm font-normal text-muted-foreground">
      {totalKeywords} keyword · {totalClusters} küme
    </span>
  )}
</h2>
{pendingEnrichment > 0 && (
  <p className="text-xs text-muted-foreground">
    {pendingEnrichment} keyword zenginleştirme bekliyor
  </p>
)}
```

Boş durum:
```tsx
{keywords.length === 0 ? (
  <div className="py-8 text-center">
    <p className="text-sm text-muted-foreground">Henüz keyword eklenmemiş.</p>
    <p className="text-sm text-muted-foreground">Yukarıdan keyword listeni içe aktar.</p>
  </div>
) : (
  <div className="rounded-md border border-border overflow-hidden">
    <Table>
      <TableHeader>
        <TableRow className="bg-secondary/40">
          <TableHead className="text-xs w-10">{/* × */}</TableHead>
          <TableHead className="text-xs">Keyword</TableHead>
          <TableHead className="text-xs text-right w-20">Volume</TableHead>
          <TableHead className="text-xs text-right w-18">CPC</TableHead>
          <TableHead className="text-xs text-right w-16">KD</TableHead>
          <TableHead className="text-xs w-28">Küme</TableHead>
          <TableHead className="text-xs w-28">Intent</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        {keywords.map((kw) => {
          const isEnriching = !kw.enriched_at
          const { dot, label } = kdColor(kw.difficulty ?? 0)
          const clusterName = kw.cluster_id ? clusterMap[kw.cluster_id] : null

          return (
            <TableRow key={kw.id} className="group">
              <TableCell className="w-10 py-2">
                <KeywordDeleteButton projectId={id} keywordId={kw.id} />
              </TableCell>
              <TableCell className={`text-sm${isEnriching ? ' opacity-50' : ''}`}>
                {kw.keyword}
              </TableCell>
              <TableCell className={`text-sm text-right font-normal${isEnriching ? ' opacity-50' : ''}`}>
                {kw.volume !== null ? formatVolume(kw.volume) : '—'}
              </TableCell>
              <TableCell className={`text-sm text-right font-normal${isEnriching ? ' opacity-50' : ''}`}>
                {kw.cpc !== null ? `$${kw.cpc.toFixed(2)}` : '—'}
              </TableCell>
              <TableCell className={`text-sm text-right${isEnriching ? ' opacity-50' : ''}`}>
                {kw.difficulty !== null ? (
                  <span className="flex items-center justify-end gap-1.5">
                    <span className={`w-2 h-2 rounded-full shrink-0 ${dot}`} />
                    <span className="text-xs text-muted-foreground">{label}</span>
                  </span>
                ) : '—'}
              </TableCell>
              <TableCell className="w-28">
                {clusterName ? (
                  <Badge className="bg-secondary text-muted-foreground text-xs border-0">
                    {clusterName}
                  </Badge>
                ) : (
                  <span className="text-sm text-muted-foreground">—</span>
                )}
              </TableCell>
              <TableCell className="w-28">
                {isEnriching ? (
                  <svg
                    className="animate-spin h-4 w-4 text-muted-foreground"
                    xmlns="http://www.w3.org/2000/svg"
                    fill="none"
                    viewBox="0 0 24 24"
                  >
                    <circle
                      className="opacity-25"
                      cx="12" cy="12" r="10"
                      stroke="currentColor"
                      strokeWidth="4"
                    />
                    <path
                      className="opacity-75"
                      fill="currentColor"
                      d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"
                    />
                  </svg>
                ) : (
                  <IntentBadge intent={kw.search_intent} />
                )}
              </TableCell>
            </TableRow>
          )
        })}
      </TableBody>
    </Table>
  </div>
)}
```

**Notlar:**
- `ClusterDeleteButton` artık import edilmez ve kullanılmaz — cluster-grouped yapı kaldırıldı.
- `ClusterDeleteButton.tsx` dosyası silinmez — var olmaya devam eder (ileride kullanılabilir veya Phase 6'da kaldırılır).
- `KeywordImport` bileşeni değişmez — aynı şekilde render edilir.
- Sayfa başlığı, header, import section, sol sidebar — değişmez.
- `font-medium` kullanılmaz — UI-SPEC typography kuralı: sadece `font-normal` ve `font-semibold`.
- CPC formatı: `$${kw.cpc.toFixed(2)}` — null ise "—".
  </action>

  <verify>
    <automated>cd C:/Users/baris/Documents/seo-machine && npx tsc --noEmit 2>&1 | grep "page.tsx" || echo "page.tsx: no TS errors"</automated>
  </verify>

  <acceptance_criteria>
    - `page.tsx` içinde `import { KeywordDeleteButton }` satırı mevcut
    - `page.tsx` içinde `import { IntentBadge }` satırı mevcut
    - `page.tsx` içinde `import { Badge }` satırı mevcut
    - `page.tsx` içinde `ClusterDeleteButton` import'u YOK
    - `page.tsx` içinde `ClusterRow` type tanımı YOK
    - `page.tsx` içinde `.select('id, keyword, volume, cpc, difficulty, search_intent, enriched_at, cluster_id')` sorgusu mevcut
    - `page.tsx` içinde `.order('volume', { ascending: false, nullsFirst: false })` mevcut (D-13)
    - `page.tsx` içinde `opacity-50` ve `animate-spin` mevcut (enrichment loading state)
    - `page.tsx` içinde `group-hover:opacity-100` mevcut (× butonu hover)
    - `page.tsx` içinde `Keyword Listesi` h2 başlığı mevcut (Türkçe — UI-SPEC copywriting)
    - `page.tsx` içinde `Henüz keyword eklenmemiş.` boş durum metni mevcut
    - `page.tsx` içinde `font-medium` string'i YOK (sadece font-normal ve font-semibold — UI-SPEC typography)
    - TypeScript hatası yok
  </acceptance_criteria>

  <done>page.tsx cluster-grouped yapıdan düz tabloya dönüştürüldü; tüm 7 sütun mevcut; enrichment loading state (opacity-50 + spinner) uygulandı; × silme butonu group-hover ile görünür; intent badge ve küme badge doğru renk ve format ile gösteriliyor; TypeScript hatası yok.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | KeywordDeleteButton'dan gelen keywordId + projectId parametreleri güvenilmez (client-side) |
| Server Component render | page.tsx'te gösterilen keyword verileri Supabase'den kullanıcı filtrelemeli gelir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Spoofing | KeywordDeleteButton — keywordId client'tan gelir | mitigate | deleteKeyword (05-01'de) `.eq('user_id', user.id).eq('project_id', projectId)` ile doğrulama yapar; UI kararı değil, server kararı |
| T-05-02-02 | Information Disclosure | page.tsx — keyword listesi gösterimi | mitigate | Supabase sorgusu `.eq('user_id', user.id)` ile filtrelenmiş; başka kullanıcının keywordleri görüntülenemez |
| T-05-02-03 | Tampering | Intent badge — DataForSEO'dan gelen string render | accept | `intent.toLowerCase().trim()` normalizasyon sonrası sadece bilinen 4 değer badge olarak render edilir; bilinmeyenler ham string döner — XSS riski yok (JSX escaping) |
| T-05-02-04 | Denial of Service | Büyük keyword listesi — tek sayfa render | accept | Sayfalama Phase 6 kapsamında; Phase 5 scope'u MVP içindir; tablo performansı kabul edilebilir seviyede |
</threat_model>

<verification>
```bash
# 1. TypeScript derlemesi temiz
cd C:/Users/baris/Documents/seo-machine
npx tsc --noEmit

# 2. Yeni bileşen dosyaları mevcut
ls src/app/\(dashboard\)/projeler/\[id\]/keyword-stratejisi/IntentBadge.tsx
ls src/app/\(dashboard\)/projeler/\[id\]/keyword-stratejisi/KeywordDeleteButton.tsx

# 3. Intent renk atamaları doğrulama
grep "bg-blue-500/20\|bg-emerald-500/20\|bg-gray-500/20\|bg-orange-500/20" \
  src/app/\(dashboard\)/projeler/\[id\]/keyword-stratejisi/IntentBadge.tsx

# 4. KeywordDeleteButton onay dialogu YOK doğrulaması
grep "confirm" src/app/\(dashboard\)/projeler/\[id\]/keyword-stratejisi/KeywordDeleteButton.tsx \
  && echo "HATA: confirm kullanılmamalı" || echo "OK: onay dialogu yok"

# 5. page.tsx düz tablo sorgusu doğrulaması
grep "ascending.*false.*nullsFirst.*false" \
  "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx"

# 6. Enrichment loading state doğrulaması
grep "opacity-50\|animate-spin" \
  "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx"

# 7. font-medium yasak kontrolü
grep "font-medium" "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx" \
  && echo "HATA: font-medium kullanılmamalı" || echo "OK: font-medium yok"
```
</verification>

<success_criteria>
- `IntentBadge.tsx` 4 renk için doğru className mapping ile mevcut; normalizasyon var; variant prop kullanılmıyor
- `KeywordDeleteButton.tsx` opacity-0 group-hover:opacity-100 ile mevcut; confirm() yok; useTransition ile async silme
- `page.tsx` küme-gruplu yapıdan düz tek tabloya dönüştürüldü
- Tablo sütunları: (×) | Keyword | Volume | CPC | KD | Küme | Intent sırasında
- enriched_at IS NULL → opacity-50 + animate-spin spinner doğrulanabilir
- Volume azalan sıralama (.order ascending:false nullsFirst:false) mevcut
- `npx tsc --noEmit` sıfır hata
- font-medium kullanılmıyor (UI-SPEC typography — font-normal ve font-semibold)
</success_criteria>

<output>
Tamamlanınca `.planning/phases/05-keyword-import-enrichment/05-02-SUMMARY.md` oluştur.
</output>
