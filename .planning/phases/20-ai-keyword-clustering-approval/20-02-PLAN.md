---
phase: 20-ai-keyword-clustering-approval
plan: 02
type: execute
wave: 1
depends_on:
  - "20-01"
files_modified:
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterButton.tsx
autonomous: true
requirements:
  - KWST-03
  - KWST-04

must_haves:
  truths:
    - "updateClusterStatus server action: UUID validate → getUser → ownership select → status update (revalidatePath YOK)"
    - "removeKeywordFromCluster server action: keyword.cluster_id = null yapar, cluster'ı silmez, total_volume günceller (revalidatePath YOK)"
    - "approveStrategy server action: project ownership check → keyword_strategy_approved yazar → revalidatePath çağrılır"
    - "clusterAndScoreKeywords: approved cluster'ları korur, eski draft'ları siler, yeni cluster'ları status='draft' ile yazar, clusters[] dönüş değeri içerir"
    - "ClusterButton: onSuccess?: (draftClusters: DraftCluster[]) => void prop alır; başarıda overlay açılışını tetikler"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts"
      provides: "3 yeni server action + genişletilmiş clusterAndScoreKeywords"
      exports: ["updateClusterStatus", "removeKeywordFromCluster", "approveStrategy", "DraftCluster"]
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterButton.tsx"
      provides: "onSuccess callback prop eklendi"
      exports: ["ClusterButton"]
  key_links:
    - from: "ClusterButton.tsx"
      to: "clusterAndScoreKeywords (modified)"
      via: "startTransition + onSuccess callback"
      pattern: "onSuccess.*draftClusters"
    - from: "updateClusterStatus"
      to: "keyword_clusters.status"
      via: "supabase.update({ status })"
      pattern: "update.*status.*eq.*user_id"
    - from: "approveStrategy"
      to: "projects.keyword_strategy_approved"
      via: "supabase.update({ keyword_strategy_approved })"
      pattern: "keyword_strategy_approved.*revalidatePath"
---

<objective>
Wave 1: Backend server action'ları ekle ve ClusterButton'ı genişlet.

Phase 20'nin tüm iş mantığı bu planda hayata geçer: cluster onaylama/reddetme, keyword kaldırma, strateji onaylama. `clusterAndScoreKeywords` draft status ile yazar ve overlay'e data aktarır. ClusterButton overlay callback'ini alır.

Purpose: Overlay ve toolbar'ın çağıracağı tüm server tarafı fonksiyonları sağla.
Output: 3 yeni server action (`updateClusterStatus`, `removeKeywordFromCluster`, `approveStrategy`) + genişletilmiş `clusterAndScoreKeywords` + genişletilmiş `ClusterButton`.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-ai-keyword-clustering-approval/20-CONTEXT.md
@.planning/phases/20-ai-keyword-clustering-approval/20-RESEARCH.md
@.planning/phases/20-ai-keyword-clustering-approval/20-PATTERNS.md
@.planning/phases/20-ai-keyword-clustering-approval/20-01-SUMMARY.md

<interfaces>
<!-- Mevcut actions.ts'den extract edildi — Wave 1 bu pattern'leri takip eder -->

ClusterAndScoreResult (mevcut — genişletilecek):
```typescript
export type ClusterAndScoreResult =
  | { success: true; clusterCount: number }
  | { success: false; error: string }
```

Ownership check pattern (actions.ts'deki tüm action'larda tekrarlanan):
```typescript
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) return { success: false, error: 'Oturum bulunamadı.' }

const uuidRegex = /^[0-9a-f-]{36}$/i
if (!uuidRegex.test(clusterId) || !uuidRegex.test(projectId)) {
  return { success: false, error: 'Geçersiz ID formatı.' }
}

const { data: cluster } = await supabase
  .from('keyword_clusters')
  .select('id')
  .eq('id', clusterId)
  .eq('project_id', projectId)
  .eq('user_id', user.id)
  .single()
if (!cluster) return { success: false, error: 'Küme bulunamadı.' }
```

ClusterButton mevcut prop:
```typescript
export function ClusterButton({
  projectId,
  hasExistingClusters,
}: {
  projectId: string
  hasExistingClusters: boolean
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: actions.ts — 3 yeni server action ekle (updateClusterStatus, removeKeywordFromCluster, approveStrategy)</name>
  <files>src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts</files>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts (satır 1-10 import'lar; satır 410-492 moveKeywordToCluster; satır 657-703 updateClusterRevenue — ownership + whitelist pattern)
    - .planning/phases/20-ai-keyword-clustering-approval/20-RESEARCH.md (Pattern 2: Server Action Ownership Check; updateClusterStatus ve approveStrategy kod örnekleri)
    - .planning/phases/20-ai-keyword-clustering-approval/20-PATTERNS.md (actions.ts bölümü — action imzaları)
  </read_first>

  <behavior>
    - updateClusterStatus('invalid-uuid', 'approved', 'proj') → `{ success: false, error: 'Geçersiz ID formatı.' }`
    - updateClusterStatus(validId, 'invalid_status', validProjId) → `{ success: false, error: 'Geçersiz durum değeri.' }`
    - updateClusterStatus(validId, 'approved', validProjId) başarılı → `{ success: true }`, revalidatePath ÇAĞRILMAZ
    - removeKeywordFromCluster(validKwId, validClusterId, validProjId) başarılı → cluster_id=null, total_volume güncellendi, `{ success: true }`, revalidatePath ÇAĞRILMAZ
    - approveStrategy(validProjId, true) başarılı → `{ success: true }`, revalidatePath('/projeler/{projectId}/keyword-stratejisi') çağrılır
  </behavior>

  <action>
`actions.ts` dosyasının SONUNA (son export'un altına) aşağıdaki 3 action'ı ekle. Mevcut koda dokunma.

**1. DraftCluster tipi ve updateClusterStatus:**

```typescript
// ─── Phase 20: Clustering Approval Actions ────────────────────────────────────

// DraftCluster: clusterAndScoreKeywords'ün overlay'e döneceği tip
export type DraftCluster = {
  id: string
  cluster_name: string
  intent: string | null
  total_volume: number
  status: 'draft'
  keywords: Array<{ id: string; keyword: string; volume: number | null }>
}

// VALID_CLUSTER_STATUSES whitelist — T-20-02: geçersiz status injection önlemi
const VALID_CLUSTER_STATUSES = ['draft', 'approved', 'rejected'] as const

export type UpdateClusterStatusResult =
  | { success: true }
  | { success: false; error: string }

export async function updateClusterStatus(
  clusterId: string,
  status: string,
  projectId: string
): Promise<UpdateClusterStatusResult> {
  // T-20-02: Input whitelist validation
  if (!VALID_CLUSTER_STATUSES.includes(status as typeof VALID_CLUSTER_STATUSES[number])) {
    return { success: false, error: 'Geçersiz durum değeri.' }
  }
  // V5: UUID format validation
  const uuidRegex = /^[0-9a-f-]{36}$/i
  if (!uuidRegex.test(clusterId) || !uuidRegex.test(projectId)) {
    return { success: false, error: 'Geçersiz ID formatı.' }
  }

  // V2: Authentication
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Oturum bulunamadı.' }

  // V4 + T-20-01: Ownership check — IDOR önlemi
  const { data: cluster } = await supabase
    .from('keyword_clusters')
    .select('id')
    .eq('id', clusterId)
    .eq('project_id', projectId)
    .eq('user_id', user.id)
    .single()
  if (!cluster) return { success: false, error: 'Küme bulunamadı.' }

  const { error } = await supabase
    .from('keyword_clusters')
    .update({ status })
    .eq('id', clusterId)
    .eq('user_id', user.id)

  if (error) return { success: false, error: 'Durum güncellenemedi.' }
  // revalidatePath YOK — overlay state sıfırlanmasın (Pitfall 3)
  return { success: true }
}
```

**2. removeKeywordFromCluster:**

```typescript
export type RemoveKeywordFromClusterResult =
  | { success: true }
  | { success: false; error: string }

export async function removeKeywordFromCluster(
  keywordId: string,
  clusterId: string,
  projectId: string
): Promise<RemoveKeywordFromClusterResult> {
  const uuidRegex = /^[0-9a-f-]{36}$/i
  if (!uuidRegex.test(keywordId) || !uuidRegex.test(clusterId) || !uuidRegex.test(projectId)) {
    return { success: false, error: 'Geçersiz ID formatı.' }
  }

  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Oturum bulunamadı.' }

  // T-20-01: Cluster ownership check
  const { data: clusterRow } = await supabase
    .from('keyword_clusters')
    .select('id')
    .eq('id', clusterId)
    .eq('project_id', projectId)
    .eq('user_id', user.id)
    .single()
  if (!clusterRow) return { success: false, error: 'Küme bulunamadı.' }

  // keyword.cluster_id = null — havuza gönder (D-03)
  const { error: kwErr } = await supabase
    .from('keywords')
    .update({ cluster_id: null })
    .eq('id', keywordId)
    .eq('user_id', user.id)

  if (kwErr) return { success: false, error: 'Keyword kaldırılamadı.' }

  // Pitfall 7: total_volume güncelle — kalan keyword'lerin volume'unu topla
  const { data: remaining } = await supabase
    .from('keywords')
    .select('volume')
    .eq('cluster_id', clusterId)
    .eq('user_id', user.id)

  const newVolume = (remaining ?? []).reduce((sum, k) => sum + (k.volume ?? 0), 0)

  await supabase
    .from('keyword_clusters')
    .update({ total_volume: newVolume })
    .eq('id', clusterId)
    .eq('user_id', user.id)

  // revalidatePath YOK — overlay state korunur (Pitfall 3)
  // Not: Cluster boşalırsa silinmez — kullanıcı ayrıca reddeder (Pitfall 4)
  return { success: true }
}
```

**3. approveStrategy:**

```typescript
export type ApproveStrategyResult =
  | { success: true }
  | { success: false; error: string }

export async function approveStrategy(
  projectId: string,
  approved: boolean
): Promise<ApproveStrategyResult> {
  const uuidRegex = /^[0-9a-f-]{36}$/i
  if (!uuidRegex.test(projectId)) {
    return { success: false, error: 'Geçersiz proje ID.' }
  }

  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Oturum bulunamadı.' }

  // T-20-03: Project ownership check
  const { data: project } = await supabase
    .from('projects')
    .select('id')
    .eq('id', projectId)
    .eq('user_id', user.id)
    .single()
  if (!project) return { success: false, error: 'Proje bulunamadı.' }

  const { error } = await supabase
    .from('projects')
    .update({ keyword_strategy_approved: approved })
    .eq('id', projectId)
    .eq('user_id', user.id)

  if (error) return { success: false, error: 'Güncelleme başarısız.' }

  // revalidatePath VAR — SSR'ı güncelle; D-06 gereği toolbar durumu yenilenir
  revalidatePath(`/projeler/${projectId}/keyword-stratejisi`)
  return { success: true }
}
```
  </action>

  <verify>
    <automated>cd C:/Users/baris/Documents/seo-machine && npx vitest run src/lib/keywords/clustering-approval.test.ts 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "export async function updateClusterStatus" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → satır numarasını bulur
    - `grep -n "export async function removeKeywordFromCluster" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → satır numarasını bulur
    - `grep -n "export async function approveStrategy" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → satır numarasını bulur
    - `grep -n "export type DraftCluster" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → bulunur
    - `grep -n "VALID_CLUSTER_STATUSES" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → bulunur
    - `updateClusterStatus`'da `revalidatePath` ÇAĞRILMAZ — grep: `removeKeywordFromCluster` fonksiyon gövdesinde `revalidatePath` satırı yok
    - `approveStrategy`'de `revalidatePath` çağrılır — grep: `revalidatePath.*keyword-stratejisi` bulunur
    - Wave 0 testleri hâlâ pass: `npx vitest run src/lib/keywords/clustering-approval.test.ts` yeşil
  </acceptance_criteria>

  <done>3 yeni server action eklendi; DraftCluster tipi export'landı; revalidatePath kuralları doğru uygulandı; testler yeşil.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: actions.ts — clusterAndScoreKeywords'ü status='draft' + DraftCluster dönüş değeri için güncelle</name>
  <files>src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts</files>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts (satır 285-408 — clusterAndScoreKeywords mevcut implementasyon; upsert mantığı, keyword update döngüsü, revalidatePath)
    - .planning/phases/20-ai-keyword-clustering-approval/20-RESEARCH.md (Pattern 6: Yeniden Kümelendirmede Draft Yönetimi; Pitfall 2: upsert conflict önlemi)
    - .planning/phases/20-ai-keyword-clustering-approval/20-CONTEXT.md (D-05: approved cluster'lara dokunma; D-08: overlay tetikleme)
  </read_first>

  <behavior>
    - Yeniden kümelendirme çalışınca: approved cluster'lar ve keyword'leri dokunulmaz (D-05)
    - Eski draft cluster'lar silinir; keyword'leri cluster_id=null olur
    - Yeni cluster'lar `status: 'draft'` ile INSERT edilir (upsert değil — Pitfall 2)
    - Dönüş tipi `{ success: true; clusterCount: number; clusters: DraftCluster[] }` olarak genişler
    - ClusterButton `result.clusters` ile overlay'e data aktarabilir
    - revalidatePath bu action'dan KALDIRILIR (overlay mount'tan sonra çağrılacak)
  </behavior>

  <action>
`clusterAndScoreKeywords` fonksiyonunu şu değişikliklerle güncelle:

**1. ClusterAndScoreResult tipini genişlet (satır 285-287 civarı):**

```typescript
export type ClusterAndScoreResult =
  | { success: true; clusterCount: number; clusters: DraftCluster[] }
  | { success: false; error: string }
```

**2. Fonksiyon gövdesinde — keyword fetch'ten önce — approved cluster koruması ekle (satır 308 civarı — `const { data: keywordsRaw }` üstüne):**

```typescript
  // D-05: Approved cluster'ların keyword ID'lerini topla — bunlara dokunulmayacak
  const { data: approvedClusters } = await supabase
    .from('keyword_clusters')
    .select('id')
    .eq('project_id', projectId)
    .eq('user_id', user.id)
    .eq('status', 'approved')

  const approvedClusterIds = (approvedClusters ?? []).map((c) => c.id)

  // Eski draft cluster'ları sil (D-05: approved olanlar korunur)
  await supabase
    .from('keyword_clusters')
    .delete()
    .eq('project_id', projectId)
    .eq('user_id', user.id)
    .eq('status', 'draft')
```

**3. Keyword fetch query'sine filtre ekle (satır 309-315 arasındaki `.not('enriched_at', 'is', null)` satırından sonra):**

```typescript
  // Sadece approved cluster'da olmayan keyword'leri çek (D-05)
  // cluster_id = null VEYA approved olmayan cluster'daki keyword'ler
```

keyword query'e `.is('cluster_id', null)` filtresi EKLEME — bu approved cluster'daki keyword'leri de dışarıda bırakır. Bunun yerine mevcut `keywordsRaw` listesini filtrele:

```typescript
  const keywords = (keywordsRaw ?? []).filter(
    (kw) => !kw.cluster_id || !approvedClusterIds.includes(kw.cluster_id)
  )
```

Not: `keywordsRaw` query'sine `cluster_id` kolonunu ekle: `'id, keyword, volume, cpc, difficulty, search_intent, cluster_id'`

**4. Upsert'i INSERT'e değiştir (Pitfall 2: approved cluster üzerine yazma önlemi):**

Mevcut upsert:
```typescript
.upsert({ user_id, project_id, cluster_name, total_volume, intent }, { onConflict: '...' })
```

Yerine INSERT kullan ve `status: 'draft'` ekle:
```typescript
const { data: clusterRow, error: clusterErr } = await supabase
  .from('keyword_clusters')
  .insert({
    user_id: user.id,
    project_id: projectId,
    cluster_name: cluster.name,
    total_volume: cluster.totalVolume,
    intent: cluster.intent,
    status: 'draft',  // D-08: AI önerisi → draft
  })
  .select('id')
  .single()
```

**5. DraftCluster[] listesi oluştur — for döngüsü içinde push et:**

Döngü başına `const draftClusters: DraftCluster[] = []` ekle. Her başarılı cluster insert'ten sonra:
```typescript
draftClusters.push({
  id: clusterRow.id,
  cluster_name: cluster.name,
  intent: cluster.intent ?? null,
  total_volume: cluster.totalVolume,
  status: 'draft',
  keywords: cluster.keywords.map((kw) => ({
    id: kw.id,
    keyword: kw.keyword,
    volume: kw.volume ?? null,
  })),
})
```

**6. revalidatePath satırını KALDIR (satır 406 civarı):**

Mevcut: `revalidatePath(`/projeler/${projectId}/keyword-stratejisi`)`
Kaldır — overlay kapandıktan sonra ClusterButton'ın wrapper'ı `router.refresh()` veya overlay kapat → `approveStrategy` ile tetikler.

**7. Dönüş değerini güncelle:**

Mevcut: `return { success: true, clusterCount }`
Yeni: `return { success: true, clusterCount, clusters: draftClusters }`
  </action>

  <verify>
    <automated>cd C:/Users/baris/Documents/seo-machine && npx tsc --noEmit 2>&1 | grep "actions.ts" | head -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "clusters: DraftCluster\[\]" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → ClusterAndScoreResult tipinde bulunur
    - `grep -n "status: 'draft'" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → insert bloğunda bulunur
    - `grep -n "\.delete().*eq.*status.*draft" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → eski draft silme mantığı bulunur (multiline grep ile veya dosyayı oku)
    - `grep -n "approvedClusterIds" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → approved koruma mantığı bulunur
    - `grep -n "revalidatePath" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → SADECE `approveStrategy` içinde mevcut (clusterAndScoreKeywords'da yok)
    - `npx tsc --noEmit` çalıştırılınca actions.ts'de tip hatası yok
  </acceptance_criteria>

  <done>clusterAndScoreKeywords status='draft' ile INSERT eder, approved cluster'ları korur, DraftCluster[] döner, revalidatePath kaldırıldı.</done>
</task>

<task type="auto">
  <name>Task 3: ClusterButton.tsx — onSuccess callback prop ekle</name>
  <files>src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterButton.tsx</files>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterButton.tsx (mevcut dosyanın tamamı — 52 satır)
    - .planning/phases/20-ai-keyword-clustering-approval/20-PATTERNS.md (ClusterButton.tsx bölümü — Phase 20 değişikliği, sadece 2 satır değişir)
    - .planning/phases/20-ai-keyword-clustering-approval/20-RESEARCH.md (ClusterButton Genişletme Analizi; Seçenek A — önerilen)
  </read_first>

  <action>
`ClusterButton.tsx` dosyasına minimum 2 değişiklik yap:

**1. Import'a DraftCluster tipini ekle:**

Mevcut: `import { clusterAndScoreKeywords } from './actions'`
Yeni: `import { clusterAndScoreKeywords, type DraftCluster } from './actions'`

**2. Prop tipine onSuccess ekle:**

```typescript
export function ClusterButton({
  projectId,
  hasExistingClusters,
  onSuccess,
}: {
  projectId: string
  hasExistingClusters: boolean
  onSuccess?: (draftClusters: DraftCluster[]) => void  // YENİ
}) {
```

**3. handleCluster içinde success bloğunu güncelle:**

Mevcut:
```typescript
const result = await clusterAndScoreKeywords(projectId)
if (!result.success) {
  setError(result.error)
}
// success: revalidatePath sayfa günceller, toast yok
```

Yeni:
```typescript
const result = await clusterAndScoreKeywords(projectId)
if (!result.success) {
  setError(result.error)
} else if (onSuccess && result.clusters) {
  onSuccess(result.clusters)  // D-08: overlay'i aç
}
// revalidatePath artık action içinde yok; overlay kapanışında tetiklenecek
```

Dosyanın geri kalanı (Button JSX, spinner, hata gösterimi) değişmez.
  </action>

  <verify>
    <automated>cd C:/Users/baris/Documents/seo-machine && npx tsc --noEmit 2>&1 | grep "ClusterButton" | head -5</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "onSuccess" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterButton.tsx` → prop imzasında ve handleCluster içinde bulunur
    - `grep -n "DraftCluster" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterButton.tsx` → import ve tip imzasında bulunur
    - `grep -n "result.clusters" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterButton.tsx` → success bloğunda bulunur
    - `npx tsc --noEmit` ClusterButton.tsx için tip hatası üretmez
    - Dosya 60 satırdan kısa kalmalı (minimal değişiklik)
  </acceptance_criteria>

  <done>ClusterButton onSuccess callback prop alıyor; başarıda overlay açılışı tetiklenebilir; tip güvenli.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server Action | `updateClusterStatus`, `removeKeywordFromCluster`, `approveStrategy` — her çağrı server'da çalışır; client'tan gelen tüm argümanlar doğrulanır |
| Server Action → Supabase DB | Sorgu parametreleri (UUID format, whitelist) doğrulandıktan sonra DB'ye ulaşır |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering (IDOR) | updateClusterStatus, removeKeywordFromCluster | mitigate | `.eq('user_id', user.id)` + `.eq('project_id', projectId)` her ownership sorgusunda; başka kullanıcının cluster'ı bulunamaz |
| T-20-02 | Tampering | updateClusterStatus — status input | mitigate | `VALID_CLUSTER_STATUSES = ['draft','approved','rejected'] as const` — includes() ile whitelist check; geçersiz değer erken return |
| T-20-03 | Elevation of Privilege | approveStrategy — keyword_strategy_approved | mitigate | `.eq('user_id', user.id)` project ownership check; başka kullanıcının projesi için flag yazılamaz |
</threat_model>

<verification>
Wave 1 tamamlanma kriterleri:
1. `grep -n "export async function updateClusterStatus\|removeKeywordFromCluster\|approveStrategy" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → 3 satır çıktı
2. `grep -n "clusters: DraftCluster\[\]" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts` → ClusterAndScoreResult tipinde bulunur
3. `grep -n "onSuccess" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterButton.tsx` → prop ve handler'da bulunur
4. `npx tsc --noEmit` → 0 hata (actions.ts ve ClusterButton.tsx için)
5. `npx vitest run src/lib/keywords/clustering-approval.test.ts` → yeşil
</verification>

<success_criteria>
- 3 yeni server action (updateClusterStatus, removeKeywordFromCluster, approveStrategy) eklendi
- Her action 4-adım güvenlik zinciri uygular: UUID validate → getUser → ownership select → business logic
- updateClusterStatus ve removeKeywordFromCluster'da revalidatePath YOK
- approveStrategy'de revalidatePath VAR
- clusterAndScoreKeywords: approved cluster'ları korur, draft'ları siler, status='draft' ile INSERT eder, DraftCluster[] döner
- ClusterButton onSuccess callback prop alıyor
- TypeScript hata yok; unit testler yeşil
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/20-ai-keyword-clustering-approval/20-02-SUMMARY.md` oluştur.
</output>
