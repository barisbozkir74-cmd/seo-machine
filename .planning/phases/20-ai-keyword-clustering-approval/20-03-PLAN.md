---
phase: 20-ai-keyword-clustering-approval
plan: 03
type: execute
wave: 2
depends_on:
  - "20-01"
  - "20-02"
files_modified:
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusteringApprovalOverlay.tsx
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ApprovalClusterRow.tsx
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ApprovalKeywordRow.tsx
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/StatusBadge.tsx
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/StratejiOnaylaButton.tsx
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordStratejisiToolbar.tsx
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx
  - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterPanel.tsx
autonomous: true
requirements:
  - KWST-03
  - KWST-04

must_haves:
  truths:
    - "ClusterButton tıklanınca AI kümeleme çalışır → overlay otomatik açılır → sol panelde draft cluster'lar listelenir"
    - "Kullanıcı cluster satırında 'Onayla' veya 'Reddet'e tıkladığında UI anında güncellenir (optimistic) ve DB'ye yazılır"
    - "Cluster adına çift tıklanınca inline edit aktif olur; Enter/blur ile kaydedilir, Escape ile iptal edilir"
    - "Keyword satırında '✕ Kaldır' tıklanınca keyword anında listeden kalkar (optimistic); DB'de cluster_id=null olur"
    - "StratejiOnaylaButton: approved cluster yokken disabled; ≥1 approved cluster varken aktif; tıklanınca projects.keyword_strategy_approved=true"
    - "ClusterPanel'de her cluster header'ında StatusBadge görünür; rejected cluster'lar opacity-60 ile soluk görünür"
  artifacts:
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusteringApprovalOverlay.tsx"
      provides: "fixed inset-0 z-50 full-page overlay shell — sol/sağ panel + header/footer"
      exports: ["ClusteringApprovalOverlay"]
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ApprovalClusterRow.tsx"
      provides: "Sol panel cluster satırı — inline edit + onayla/reddet"
      exports: ["ApprovalClusterRow"]
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ApprovalKeywordRow.tsx"
      provides: "Sağ panel keyword satırı — kaldır butonu + optimistic UI"
      exports: ["ApprovalKeywordRow"]
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/StatusBadge.tsx"
      provides: "draft/approved/rejected durum badge"
      exports: ["StatusBadge"]
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/StratejiOnaylaButton.tsx"
      provides: "Toolbar butonu — 3 durum (disabled/aktif/onaylandı)"
      exports: ["StratejiOnaylaButton"]
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordStratejisiToolbar.tsx"
      provides: "Client wrapper — ClusterButton + ClusteringApprovalOverlay + StratejiOnaylaButton state yönetimi"
      exports: ["KeywordStratejisiToolbar"]
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx"
      provides: "keyword_strategy_approved + status SSR query; toolbar client wrapper kullanımı"
    - path: "src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterPanel.tsx"
      provides: "StatusBadge + rejected opacity eklendi; status prop ClusterData tipine girdi"
  key_links:
    - from: "KeywordStratejisiToolbar.tsx"
      to: "ClusteringApprovalOverlay.tsx"
      via: "useState<boolean>(false) + onSuccess callback"
      pattern: "overlayOpen.*setOverlayOpen"
    - from: "ClusteringApprovalOverlay.tsx"
      to: "updateClusterStatus (server action)"
      via: "optimistic state + async call"
      pattern: "updateClusterStatus.*projectId"
    - from: "page.tsx"
      to: "keyword_strategy_approved (project query)"
      via: "Supabase select"
      pattern: "keyword_strategy_approved"
    - from: "ClusterPanel.tsx"
      to: "StatusBadge"
      via: "cluster.status prop"
      pattern: "StatusBadge.*status"
---

<objective>
Wave 2: Frontend overlay, toolbar bileşenleri ve mevcut dosya güncellemeleri.

Bu plan Phase 20'nin görünür çıktısını teslim eder: tam ekran onay overlay'i, her cluster satırı, keyword satırları, status badge'leri ve toolbar butonu. Mevcut page.tsx ve ClusterPanel.tsx güncellenerek yeni kolonları kullanır.

Purpose: Kullanıcının AI kümeleme önerilerini inceleyip onaylayabileceği arayüzü tamamla.
Output: 6 yeni bileşen + 2 mevcut dosya güncellemesi + 1 yeni client wrapper.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-ai-keyword-clustering-approval/20-CONTEXT.md
@.planning/phases/20-ai-keyword-clustering-approval/20-RESEARCH.md
@.planning/phases/20-ai-keyword-clustering-approval/20-UI-SPEC.md
@.planning/phases/20-ai-keyword-clustering-approval/20-PATTERNS.md
@.planning/phases/20-ai-keyword-clustering-approval/20-02-SUMMARY.md

<interfaces>
<!-- Wave 2'nin kullanacağı tipler — 20-02 planından -->

DraftCluster (actions.ts'den export):
```typescript
export type DraftCluster = {
  id: string
  cluster_name: string
  intent: string | null
  total_volume: number
  status: 'draft'
  keywords: Array<{ id: string; keyword: string; volume: number | null }>
}
```

Server Actions (actions.ts'den):
```typescript
export async function updateClusterStatus(clusterId: string, status: string, projectId: string): Promise<UpdateClusterStatusResult>
export async function removeKeywordFromCluster(keywordId: string, clusterId: string, projectId: string): Promise<RemoveKeywordFromClusterResult>
export async function approveStrategy(projectId: string, approved: boolean): Promise<ApproveStrategyResult>
```

ClusterButton (genişletilmiş):
```typescript
export function ClusterButton({
  projectId: string,
  hasExistingClusters: boolean,
  onSuccess?: (draftClusters: DraftCluster[]) => void,
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atom bileşenler — StatusBadge, ApprovalKeywordRow, ApprovalClusterRow</name>
  <files>
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/StatusBadge.tsx,
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ApprovalKeywordRow.tsx,
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ApprovalClusterRow.tsx
  </files>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterPanel.tsx (satır 53-58 ScoreBadge pattern + satır 83-171 cluster header ve keyword satır yapısı)
    - .planning/phases/20-ai-keyword-clustering-approval/20-UI-SPEC.md (StatusBadge bölümü; Sol Panel CSS contract; Sağ Panel keyword satır CSS; Inline Cluster Name Edit etkileşim akışı)
    - .planning/phases/20-ai-keyword-clustering-approval/20-PATTERNS.md (StatusBadge, ApprovalClusterRow, ApprovalKeywordRow bölümleri)
  </read_first>

  <action>
**Dosya 1: `src/app/(dashboard)/projeler/[id]/keyword-stratejisi/StatusBadge.tsx`**

```typescript
'use client'

import { Badge } from '@/components/ui/badge'

export function StatusBadge({ status }: { status: string | null }) {
  if (status === 'approved') {
    return (
      <Badge className="bg-emerald-500/20 text-emerald-400 text-xs border-0 px-2 py-0.5">
        Onaylandı
      </Badge>
    )
  }
  if (status === 'rejected') {
    return (
      <Badge className="bg-red-500/20 text-red-400 text-xs border-0 px-2 py-0.5">
        Reddedildi
      </Badge>
    )
  }
  // 'draft' veya null
  return (
    <Badge className="bg-secondary text-muted-foreground text-xs border-0 px-2 py-0.5">
      Beklemede
    </Badge>
  )
}
```

**Dosya 2: `src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ApprovalKeywordRow.tsx`**

UI-SPEC sağ panel keyword satırı CSS contract'ına uygun:

```typescript
'use client'

interface ApprovalKeywordRowProps {
  keyword: { id: string; keyword: string; volume: number | null }
  onRemove: () => void
  isPending: boolean
}

function formatVolume(v: number): string {
  if (v >= 1_000_000) return (v / 1_000_000).toFixed(1).replace('.0', '') + 'M'
  if (v >= 1000) return (v / 1000).toFixed(1).replace('.0', '') + 'K'
  return v.toString()
}

export function ApprovalKeywordRow({ keyword, onRemove, isPending }: ApprovalKeywordRowProps) {
  return (
    <div className="group flex items-center px-4 py-2 border-b border-border/50 hover:bg-secondary/20 gap-2">
      <span className="flex-1 text-sm truncate">{keyword.keyword}</span>
      <span className="text-sm text-muted-foreground w-16 text-right shrink-0 tabular-nums">
        {keyword.volume !== null ? formatVolume(keyword.volume) : '—'}
      </span>
      <button
        className="text-xs text-muted-foreground opacity-0 group-hover:opacity-100 transition-opacity hover:text-red-400 shrink-0 disabled:cursor-not-allowed"
        onClick={onRemove}
        disabled={isPending}
        aria-label="Keyword'ü kümeden kaldır"
      >
        ✕ Kaldır
      </button>
    </div>
  )
}
```

**Dosya 3: `src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ApprovalClusterRow.tsx`**

UI-SPEC sol panel cluster satırı — çift tıkla inline edit, onayla/reddet butonları:

```typescript
'use client'

import { useState } from 'react'
import { Button } from '@/components/ui/button'
import { StatusBadge } from './StatusBadge'

function formatVolume(v: number): string {
  if (v >= 1_000_000) return (v / 1_000_000).toFixed(1).replace('.0', '') + 'M'
  if (v >= 1000) return (v / 1000).toFixed(1).replace('.0', '') + 'K'
  return v.toString()
}

interface ApprovalClusterRowProps {
  cluster: {
    id: string
    cluster_name: string
    intent: string | null
    status: string
    keyword_count: number
    total_volume: number
  }
  isSelected: boolean
  isPending: boolean
  onSelect: () => void
  onApprove: () => void
  onReject: () => void
  onRename: (name: string) => void
}

export function ApprovalClusterRow({
  cluster,
  isSelected,
  isPending,
  onSelect,
  onApprove,
  onReject,
  onRename,
}: ApprovalClusterRowProps) {
  const [isEditing, setIsEditing] = useState(false)
  const [editValue, setEditValue] = useState(cluster.cluster_name)
  const [nameError, setNameError] = useState(false)

  const saveClusterName = () => {
    if (!editValue.trim()) {
      setNameError(true)
      return
    }
    setNameError(false)
    setIsEditing(false)
    if (editValue.trim() !== cluster.cluster_name) {
      onRename(editValue.trim())
    }
  }

  const cancelEdit = () => {
    setEditValue(cluster.cluster_name)
    setNameError(false)
    setIsEditing(false)
  }

  return (
    <div
      className={`flex flex-col gap-1 px-3 py-3 border-b border-border/50 cursor-pointer transition-colors ${
        isSelected
          ? 'bg-secondary/40'
          : 'hover:bg-secondary/20'
      } ${isPending ? 'pointer-events-none opacity-70' : ''}`}
      onClick={onSelect}
    >
      {/* Birinci satır: status dot + isim + meta */}
      <div className="flex items-center gap-2 min-w-0">
        <StatusBadge status={cluster.status} />
        {isEditing ? (
          <input
            autoFocus
            className={`flex-1 h-6 bg-transparent border-0 border-b text-sm font-semibold focus:outline-none px-0 ${
              nameError ? 'border-destructive' : 'border-border focus:border-foreground'
            }`}
            value={editValue}
            onChange={(e) => { setEditValue(e.target.value); setNameError(false) }}
            onKeyDown={(e) => {
              e.stopPropagation()
              if (e.key === 'Enter') saveClusterName()
              if (e.key === 'Escape') cancelEdit()
            }}
            onBlur={saveClusterName}
            onClick={(e) => e.stopPropagation()}
            title={nameError ? 'Küme adı boş bırakılamaz' : undefined}
          />
        ) : (
          <span
            className="text-sm font-semibold truncate flex-1 cursor-text"
            onDoubleClick={(e) => {
              e.stopPropagation()
              setIsEditing(true)
            }}
          >
            {cluster.cluster_name}
          </span>
        )}
        <span className="text-xs text-muted-foreground shrink-0">
          {cluster.keyword_count} kw · {formatVolume(cluster.total_volume)} vol
        </span>
      </div>

      {/* İkinci satır: aksiyon butonları */}
      {/* D-03: Durum tablosu — hangi buton görünür (UI-SPEC) */}
      <div className="flex items-center gap-2" onClick={(e) => e.stopPropagation()}>
        {cluster.status !== 'approved' && (
          <Button
            size="sm"
            variant="ghost"
            className="h-7 px-2 text-xs text-emerald-400 hover:text-emerald-300 hover:bg-emerald-500/10"
            onClick={onApprove}
            disabled={isPending}
          >
            Onayla ✓
          </Button>
        )}
        {cluster.status !== 'rejected' && (
          <Button
            size="sm"
            variant="ghost"
            className="h-7 px-2 text-xs text-red-400 hover:text-red-300 hover:bg-red-500/10"
            onClick={onReject}
            disabled={isPending}
          >
            Reddet ✗
          </Button>
        )}
      </div>
    </div>
  )
}
```
  </action>

  <verify>
    <automated>cd C:/Users/baris/Documents/seo-machine && npx tsc --noEmit 2>&1 | grep -E "StatusBadge|ApprovalClusterRow|ApprovalKeywordRow" | head -10</automated>
  </verify>

  <acceptance_criteria>
    - `StatusBadge.tsx` mevcut; `grep -n "Onaylandı\|Reddedildi\|Beklemede" StatusBadge.tsx` → 3 string bulunur
    - `ApprovalClusterRow.tsx` mevcut; `grep -n "onDoubleClick\|saveClusterName\|cancelEdit" ApprovalClusterRow.tsx` → inline edit satırları bulunur
    - `ApprovalClusterRow.tsx`'te onayla butonu `status !== 'approved'` koşuluna bağlı; reddet butonu `status !== 'rejected'` koşuluna bağlı
    - `ApprovalKeywordRow.tsx` mevcut; `grep -n "group-hover:opacity-100" ApprovalKeywordRow.tsx` → Kaldır butonu hover paterni bulunur
    - `npx tsc --noEmit` 3 dosya için hata üretmez
  </acceptance_criteria>

  <done>StatusBadge, ApprovalKeywordRow, ApprovalClusterRow oluşturuldu; UI-SPEC CSS contract uygulandı; TypeScript hata yok.</done>
</task>

<task type="auto">
  <name>Task 2: ClusteringApprovalOverlay.tsx — full-page overlay shell</name>
  <files>src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusteringApprovalOverlay.tsx</files>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/MoveKeywordDialog.tsx (state management pattern — useState, async action, error handling)
    - .planning/phases/20-ai-keyword-clustering-approval/20-UI-SPEC.md (Overlay Layout Contract; Header; Footer; boş durum CSS'leri; "Tümünü Onayla" davranışı — overlay açık kalır, D-09)
    - .planning/phases/20-ai-keyword-clustering-approval/20-RESEARCH.md (Pattern 3: Optimistic UI; Anti-pattern: Radix Dialog kullanma; Pitfall 3: revalidatePath overlay resetler)
    - .planning/phases/20-ai-keyword-clustering-approval/20-PATTERNS.md (ClusteringApprovalOverlay bölümü — prop imzası, state pattern)
  </read_first>

  <action>
`ClusteringApprovalOverlay.tsx` dosyasını oluştur. Overlay:
- `fixed inset-0 z-50 bg-background flex flex-col` — native div (Radix Dialog değil)
- Header: başlık + özet + Tümünü Onayla + Tümünü Reddet + X kapat
- Body: sol panel (360px, cluster listesi) + sağ panel (flex-1, keyword listesi)
- Footer: sayaç özeti + Kapat butonu
- Her cluster/keyword aksiyonu anında DB'ye yazılır (optimistic UI + server action)
- revalidatePath hiçbir aksiyonda çağrılmaz (overlay state korunur, Pitfall 3)

```typescript
'use client'

import { useState } from 'react'
import { Button } from '@/components/ui/button'
import { ApprovalClusterRow } from './ApprovalClusterRow'
import { ApprovalKeywordRow } from './ApprovalKeywordRow'
import { updateClusterStatus, removeKeywordFromCluster, type DraftCluster } from './actions'

// Overlay'in kendi iç state'i için genişletilmiş tip
interface ClusterState {
  id: string
  cluster_name: string
  intent: string | null
  status: string  // 'draft' | 'approved' | 'rejected'
  total_volume: number
  keywords: Array<{ id: string; keyword: string; volume: number | null }>
}

interface ClusteringApprovalOverlayProps {
  projectId: string
  initialClusters: DraftCluster[]
  onClose: () => void
}

export function ClusteringApprovalOverlay({
  projectId,
  initialClusters,
  onClose,
}: ClusteringApprovalOverlayProps) {
  const [clusters, setClusters] = useState<ClusterState[]>(
    initialClusters.map((c) => ({ ...c }))
  )
  const [selectedClusterId, setSelectedClusterId] = useState<string | null>(
    initialClusters[0]?.id ?? null
  )
  const [pendingClusterId, setPendingClusterId] = useState<string | null>(null)
  const [pendingKeywordId, setPendingKeywordId] = useState<string | null>(null)

  const selectedCluster = clusters.find((c) => c.id === selectedClusterId) ?? null

  const approvedCount = clusters.filter((c) => c.status === 'approved').length
  const rejectedCount = clusters.filter((c) => c.status === 'rejected').length
  const draftCount = clusters.filter((c) => c.status === 'draft').length
  const totalKeywords = clusters.reduce((sum, c) => sum + c.keywords.length, 0)

  // Optimistic status güncelleme + server action
  const handleStatusChange = async (clusterId: string, newStatus: string) => {
    const prev = clusters
    // Optimistic güncelle
    setClusters((cs) => cs.map((c) => c.id === clusterId ? { ...c, status: newStatus } : c))
    setPendingClusterId(clusterId)

    const result = await updateClusterStatus(clusterId, newStatus, projectId)
    setPendingClusterId(null)

    if (!result.success) {
      // Rollback — hata durumunda önceki state'e dön
      setClusters(prev)
    }
  }

  // Tümünü Onayla / Reddet (D-09) — overlay açık kalır
  const handleBulkStatus = async (newStatus: 'approved' | 'rejected') => {
    const draftClusters = clusters.filter((c) => c.status === 'draft')
    for (const c of draftClusters) {
      await handleStatusChange(c.id, newStatus)
    }
  }

  // Cluster yeniden adlandır
  const handleRename = async (clusterId: string, newName: string) => {
    if (!newName.trim()) return
    // Not: updateClusterName server action Phase 20 scope dışı;
    // D-02 sadece UI'da inline edit ister. Şimdilik optimistic local güncelle.
    // (Persist için updateClusterName action'ı Wave 2+ sonrası eklenebilir)
    setClusters((cs) => cs.map((c) => c.id === clusterId ? { ...c, cluster_name: newName } : c))
  }

  // Keyword kaldır — optimistic UI (RESEARCH.md Pattern 3)
  const handleRemoveKeyword = async (clusterId: string, keywordId: string) => {
    const prevClusters = clusters
    setPendingKeywordId(keywordId)

    // Optimistic: keyword'ü listeden kaldır
    setClusters((cs) =>
      cs.map((c) =>
        c.id === clusterId
          ? { ...c, keywords: c.keywords.filter((k) => k.id !== keywordId) }
          : c
      )
    )

    const result = await removeKeywordFromCluster(keywordId, clusterId, projectId)
    setPendingKeywordId(null)

    if (!result.success) {
      // Rollback
      setClusters(prevClusters)
    }
  }

  return (
    <div className="fixed inset-0 z-50 bg-background flex flex-col animate-in fade-in-0 duration-150">

      {/* HEADER */}
      <div className="flex items-center justify-between px-6 py-3 border-b border-border shrink-0">
        <div className="flex items-center gap-3">
          <span className="text-sm font-semibold">Kümeleme Önerilerini İncele</span>
          <span className="text-xs text-muted-foreground">
            {clusters.length} küme · {totalKeywords} keyword
          </span>
        </div>
        <div className="flex items-center gap-2">
          <Button
            size="sm"
            variant="outline"
            className="h-8 text-xs text-emerald-400 border-emerald-500/30 hover:bg-emerald-500/10"
            onClick={() => handleBulkStatus('approved')}
          >
            Tümünü Onayla
          </Button>
          <Button
            size="sm"
            variant="outline"
            className="h-8 text-xs text-red-400 border-red-500/30 hover:bg-red-500/10"
            onClick={() => handleBulkStatus('rejected')}
          >
            Tümünü Reddet
          </Button>
          <Button
            size="sm"
            variant="ghost"
            className="h-8 w-8 p-0 text-muted-foreground hover:text-foreground"
            onClick={onClose}
            aria-label="Kapat"
          >
            ✕
          </Button>
        </div>
      </div>

      {/* BODY — Sol + Sağ Panel */}
      <div className="flex flex-1 min-h-0">

        {/* SOL PANEL: Cluster listesi */}
        <div className="w-[360px] shrink-0 border-r border-border overflow-y-auto">
          {clusters.length === 0 ? (
            <div className="flex flex-col items-center justify-center h-full gap-2 text-center px-8">
              <p className="text-sm text-muted-foreground">
                Henüz küme önerilmedi. AI ile Kümelendirme butonuna bas.
              </p>
            </div>
          ) : (
            clusters.map((cluster) => (
              <ApprovalClusterRow
                key={cluster.id}
                cluster={{
                  id: cluster.id,
                  cluster_name: cluster.cluster_name,
                  intent: cluster.intent,
                  status: cluster.status,
                  keyword_count: cluster.keywords.length,
                  total_volume: cluster.total_volume,
                }}
                isSelected={selectedClusterId === cluster.id}
                isPending={pendingClusterId === cluster.id}
                onSelect={() => setSelectedClusterId(cluster.id)}
                onApprove={() => handleStatusChange(cluster.id, 'approved')}
                onReject={() => handleStatusChange(cluster.id, 'rejected')}
                onRename={(name) => handleRename(cluster.id, name)}
              />
            ))
          )}
        </div>

        {/* SAĞ PANEL: Seçili cluster'ın keyword'leri */}
        <div className="flex-1 overflow-y-auto">
          {!selectedCluster ? (
            <div className="flex flex-col items-center justify-center h-full gap-2 text-center px-8">
              <p className="text-sm text-muted-foreground">
                Keyword&apos;leri görmek için soldan bir küme seçin.
              </p>
            </div>
          ) : (
            <>
              {/* Sağ panel başlık */}
              <div className="px-4 py-3 border-b border-border/50">
                <span className="text-sm font-semibold">{selectedCluster.cluster_name}</span>
                <span className="ml-2 text-xs text-muted-foreground">
                  {selectedCluster.keywords.length} keyword
                </span>
              </div>

              {/* Keyword listesi */}
              {selectedCluster.keywords.length === 0 ? (
                <div className="flex flex-col items-center justify-center py-12 gap-1 px-8 text-center">
                  <p className="text-sm text-muted-foreground">Bu kümede keyword yok.</p>
                  <p className="text-xs text-muted-foreground">
                    Tüm keyword&apos;ler kaldırıldı — kümeyi reddedin veya başka keyword&apos;ler ekleyin.
                  </p>
                </div>
              ) : (
                selectedCluster.keywords.map((kw) => (
                  <ApprovalKeywordRow
                    key={kw.id}
                    keyword={kw}
                    isPending={pendingKeywordId === kw.id}
                    onRemove={() => handleRemoveKeyword(selectedCluster.id, kw.id)}
                  />
                ))
              )}
            </>
          )}
        </div>
      </div>

      {/* FOOTER */}
      <div className="flex items-center justify-between px-6 py-3 border-t border-border shrink-0">
        <span className="text-xs text-muted-foreground">
          {approvedCount} onaylandı · {rejectedCount} reddedildi · {draftCount} beklemede
        </span>
        <Button
          size="sm"
          variant="ghost"
          onClick={onClose}
          className="h-8 text-xs"
        >
          Kapat
        </Button>
      </div>

    </div>
  )
}
```
  </action>

  <verify>
    <automated>cd C:/Users/baris/Documents/seo-machine && npx tsc --noEmit 2>&1 | grep "ClusteringApprovalOverlay" | head -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "fixed inset-0 z-50 bg-background" ClusteringApprovalOverlay.tsx` → overlay shell class'ı bulunur
    - `grep -n "w-\[360px\]" ClusteringApprovalOverlay.tsx` → sol panel genişliği bulunur
    - `grep -n "handleBulkStatus" ClusteringApprovalOverlay.tsx` → Tümünü Onayla/Reddet mantığı bulunur
    - `grep -n "rollback\|prevClusters\|prev" ClusteringApprovalOverlay.tsx` → optimistic rollback mantığı bulunur
    - `grep -n "revalidatePath" ClusteringApprovalOverlay.tsx` → BULUNAMAZ (overlay'de revalidatePath yok)
    - `npx tsc --noEmit` ClusteringApprovalOverlay.tsx için tip hatası üretmez
  </acceptance_criteria>

  <done>ClusteringApprovalOverlay oluşturuldu: fixed inset-0 overlay, sol+sağ panel, optimistic UI, bulk aksiyonlar, boş durumlar.</done>
</task>

<task type="auto">
  <name>Task 3: StratejiOnaylaButton + KeywordStratejisiToolbar + page.tsx + ClusterPanel güncellemeleri</name>
  <files>
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/StratejiOnaylaButton.tsx,
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordStratejisiToolbar.tsx,
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx,
    src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterPanel.tsx
  </files>

  <read_first>
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterButton.tsx (useTransition pattern — StratejiOnaylaButton için şablon)
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx (mevcut toolbar yapısı satır 162-195; project query satır 79-84; cluster query satır 99-104; ClusterWithKeywords tipi satır 52-60)
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterPanel.tsx (cluster card div satır ~83; ClusterData tipi)
    - .planning/phases/20-ai-keyword-clustering-approval/20-UI-SPEC.md (StratejiOnaylaButton 3 durum; Placement — toolbar sonu; ClusterPanel status gösterimi; rejected opacity-60)
    - .planning/phases/20-ai-keyword-clustering-approval/20-RESEARCH.md (Pattern 1: Overlay State; Pattern 5: page.tsx query güncellemeleri)
    - .planning/phases/20-ai-keyword-clustering-approval/20-PATTERNS.md (page.tsx ve ClusterPanel.tsx bölümleri)
  </read_first>

  <action>
**Dosya 1: `src/app/(dashboard)/projeler/[id]/keyword-stratejisi/StratejiOnaylaButton.tsx`**

ClusterButton pattern'ini taklit et — useTransition + approveStrategy action:

```typescript
'use client'

import { useTransition } from 'react'
import { Button } from '@/components/ui/button'
import { approveStrategy } from './actions'

interface StratejiOnaylaButtonProps {
  projectId: string
  isApproved: boolean       // projects.keyword_strategy_approved
  hasApprovedCluster: boolean  // en az 1 approved cluster var mı? (aktifleştirme koşulu)
}

export function StratejiOnaylaButton({
  projectId,
  isApproved,
  hasApprovedCluster,
}: StratejiOnaylaButtonProps) {
  const [isPending, startTransition] = useTransition()

  const handleClick = () => {
    startTransition(async () => {
      await approveStrategy(projectId, !isApproved)
      // revalidatePath action içinde çağrılır; SSR güncellenince buton yeni state'i gösterir
    })
  }

  // Durum 1: Pasif — onaylı cluster yok (D-06)
  if (!hasApprovedCluster && !isApproved) {
    return (
      <Button
        variant="ghost"
        disabled
        className="h-9 text-xs opacity-40 cursor-not-allowed"
      >
        Stratejiyi Onayla
      </Button>
    )
  }

  // Durum 3: Onaylandı — tıklanınca geri alınabilir (D-06)
  if (isApproved) {
    return (
      <Button
        variant="ghost"
        className={`h-9 text-xs text-emerald-400 hover:text-emerald-300 ${isPending ? 'opacity-50' : ''}`}
        onClick={handleClick}
        disabled={isPending}
      >
        ✓ Strateji Onaylandı
      </Button>
    )
  }

  // Durum 2: Aktif — ≥1 approved cluster var (D-06)
  return (
    <Button
      variant="outline"
      className={`h-9 text-xs ${isPending ? 'opacity-50' : ''}`}
      onClick={handleClick}
      disabled={isPending}
    >
      Stratejiyi Onayla
    </Button>
  )
}
```

**Dosya 2: `src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordStratejisiToolbar.tsx`**

Client wrapper — overlay state + ClusterButton + ClusteringApprovalOverlay + StratejiOnaylaButton (RESEARCH.md Pattern 1):

```typescript
'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { ClusterButton } from './ClusterButton'
import { ClusteringApprovalOverlay } from './ClusteringApprovalOverlay'
import { StratejiOnaylaButton } from './StratejiOnaylaButton'
import { type DraftCluster } from './actions'

interface KeywordStratejisiToolbarProps {
  projectId: string
  hasExistingClusters: boolean
  hasApprovedCluster: boolean
  isStrategyApproved: boolean
}

export function KeywordStratejisiToolbar({
  projectId,
  hasExistingClusters,
  hasApprovedCluster,
  isStrategyApproved,
}: KeywordStratejisiToolbarProps) {
  const router = useRouter()
  const [overlayOpen, setOverlayOpen] = useState(false)
  const [draftClusters, setDraftClusters] = useState<DraftCluster[]>([])

  const handleClusteringDone = (newClusters: DraftCluster[]) => {
    setDraftClusters(newClusters)
    setOverlayOpen(true)
  }

  const handleOverlayClose = () => {
    setOverlayOpen(false)
    // Overlay kapandığında SSR'ı yenile — status değişiklikleri ClusterPanel'e yansısın
    router.refresh()
  }

  return (
    <>
      <ClusterButton
        projectId={projectId}
        hasExistingClusters={hasExistingClusters}
        onSuccess={handleClusteringDone}
      />
      <StratejiOnaylaButton
        projectId={projectId}
        isApproved={isStrategyApproved}
        hasApprovedCluster={hasApprovedCluster}
      />
      {overlayOpen && (
        <ClusteringApprovalOverlay
          projectId={projectId}
          initialClusters={draftClusters}
          onClose={handleOverlayClose}
        />
      )}
    </>
  )
}
```

**Dosya 3: `src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx` — query + toolbar güncellemeleri**

Mevcut `page.tsx` dosyasını oku ve şu değişiklikleri uygula:

3a. `import { KeywordStratejisiToolbar }` ekle (diğer import'ların yanına):
```typescript
import { KeywordStratejisiToolbar } from './KeywordStratejisiToolbar'
```

3b. `ClusterWithKeywords` tipine `status` ekle (satır 52-60 civarı):
```typescript
type ClusterWithKeywords = {
  id: string
  cluster_name: string
  intent: string | null
  primary_keyword_id: string | null
  opportunity_score: number | null
  revenue_type: string | null
  status: string | null  // YENİ — D-07
  keywords: KeywordRow[]
}
```

3c. Project query'ye `keyword_strategy_approved` ekle (satır 79-84):
```typescript
const { data: project } = await supabase
  .from('projects')
  .select('id, name, domain, seo_arch_summary, seo_arch_built_at, keyword_strategy_approved')
  .eq('id', id)
  .eq('user_id', user.id)
  .single()
```

3d. Cluster query'ye `status` ekle (satır 99-104 civarındaki `.select(...)` string'ine):
Mevcut select: `'id, cluster_name, intent, primary_keyword_id, opportunity_score, revenue_type, total_volume, page_type, target_url, arch_status, ai_reasoning, priority_rank, content_month'`
Yeni select: `'id, cluster_name, intent, primary_keyword_id, opportunity_score, revenue_type, total_volume, page_type, target_url, arch_status, ai_reasoning, priority_rank, content_month, status'`

3e. Toolbar'da `ClusterButton` ve `AiSuggestButton` bloğunu `KeywordStratejisiToolbar` ile değiştir (satır 169-172 civarı):

Mevcut:
```tsx
{totalKeywords > 0 && (
  <>
    <AiSuggestButton projectId={id} />
    <ClusterButton projectId={id} hasExistingClusters={totalClusters > 0} />
  </>
)}
```

Yeni:
```tsx
{totalKeywords > 0 && (
  <>
    <AiSuggestButton projectId={id} />
    <KeywordStratejisiToolbar
      projectId={id}
      hasExistingClusters={totalClusters > 0}
      hasApprovedCluster={clusters.some((c) => (c as unknown as { status: string }).status === 'approved')}
      isStrategyApproved={project.keyword_strategy_approved ?? false}
    />
  </>
)}
```

3f. `clustersWithKeywords` mapping'ine `status` ekle (satır 121-124):
```typescript
const clustersWithKeywords: ClusterWithKeywords[] = clusters.map((c) => ({
  ...c,
  status: (c as unknown as { status: string | null }).status ?? null,  // YENİ
  keywords: clusterKeywordMap[c.id] ?? [],
}))
```

**Dosya 4: `src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterPanel.tsx` — StatusBadge + rejected opacity**

Mevcut `ClusterPanel.tsx` dosyasını oku ve şu değişiklikleri uygula:

4a. `StatusBadge` import'u ekle:
```typescript
import { StatusBadge } from './StatusBadge'
```

4b. `ClusterData` tipine `status` ekle:
```typescript
type ClusterData = {
  id: string
  cluster_name: string
  intent: string | null
  primary_keyword_id: string | null
  opportunity_score: number | null
  revenue_type: string | null
  status: string | null  // YENİ — 'draft' | 'approved' | 'rejected'
  keywords: ClusterKeyword[]
}
```

4c. Cluster card div'ine `status === 'rejected'` opacity koşulu ekle:

Mevcut: `<div key={cluster.id} className="rounded-md border border-border bg-card overflow-hidden">`
Yeni:
```tsx
<div
  key={cluster.id}
  className={`rounded-md border border-border bg-card overflow-hidden ${
    cluster.status === 'rejected' ? 'opacity-60' : ''
  }`}
>
```

4d. Cluster header'a `StatusBadge` ekle (cluster_name span'ının yanına):
```tsx
<span className="text-sm font-semibold truncate">{cluster.cluster_name}</span>
<StatusBadge status={cluster.status} />  {/* YENİ */}
<IntentBadge intent={cluster.intent} />
```

4e. `ClusterPanelProps`'a status'u dahil et — props'tan geçecek şekilde:
```typescript
clusters: ClusterData[]
```
`page.tsx`'ten `clustersWithKeywords` olarak geçirilen prop zaten status içerecek (3f adımından sonra).
  </action>

  <verify>
    <automated>cd C:/Users/baris/Documents/seo-machine && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "StratejiOnaylaButton\|KeywordStratejisiToolbar" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx` → her ikisi de bulunur
    - `grep -n "keyword_strategy_approved" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx` → project query'de bulunur
    - `grep -n "status" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/page.tsx` → cluster query select string'inde ve ClusterWithKeywords tipinde bulunur
    - `grep -n "StatusBadge" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterPanel.tsx` → import ve kullanım bulunur
    - `grep -n "opacity-60" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ClusterPanel.tsx` → rejected opacity koşulu bulunur
    - `grep -n "overlayOpen\|draftClusters" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordStratejisiToolbar.tsx` → state değişkenleri bulunur
    - `grep -n "router.refresh()" src/app/(dashboard)/projeler/[id]/keyword-stratejisi/KeywordStratejisiToolbar.tsx` → overlay kapanış refresh'i bulunur
    - `npx tsc --noEmit` → 0 TypeScript hatası (tüm yeni ve değiştirilen dosyalar)
  </acceptance_criteria>

  <done>StratejiOnaylaButton (3 durum), KeywordStratejisiToolbar (overlay state wrapper), page.tsx (query güncellemeleri), ClusterPanel (StatusBadge + opacity) tamamlandı; TypeScript hata yok.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | Overlay'den gelen updateClusterStatus / removeKeywordFromCluster / approveStrategy çağrıları server'da tam validation'dan geçer |
| SSR → Client Hydration | page.tsx'te project.keyword_strategy_approved ve cluster.status SSR'dan gelir; client CSRF endişesi yok (server action cookie ile auth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering (IDOR) | ClusteringApprovalOverlay → updateClusterStatus | mitigate | Server action 20-02'de implement edildi: `.eq('user_id', user.id)` + `.eq('project_id', projectId)` ownership zinciri; overlay sadece display eder |
| T-20-02 | Tampering | ClusteringApprovalOverlay → updateClusterStatus — status değeri | mitigate | `VALID_CLUSTER_STATUSES` whitelist server-side; overlay'den gelen string ne olursa olsun server validate eder |
| T-20-03 | Elevation of Privilege | StratejiOnaylaButton → approveStrategy | mitigate | Server action 20-02'de: project ownership check `.eq('user_id', user.id)`; client buton sadece UI gösterir |
</threat_model>

<verification>
Wave 2 tamamlanma kriterleri:
1. `ls src/app/(dashboard)/projeler/[id]/keyword-stratejisi/ | grep -E "StatusBadge|ApprovalCluster|ApprovalKeyword|ClusteringApproval|StratejiOnayla|KeywordStratejisiToolbar"` → 6 yeni dosya
2. `npx tsc --noEmit` → 0 hata
3. `npx vitest run` → tüm testler yeşil
4. Browser: `/projeler/{id}/keyword-stratejisi` yüklenebilir, TypeScript hata yok
5. Browser: ClusterButton tıklanınca overlay açılır; cluster listesi görünür
6. Browser: Onayla/Reddet tıklanınca StatusBadge anında güncellenir
7. Browser: Overlay kapanınca ClusterPanel'de status badge'leri görünür; rejected cluster'lar opacity-60
8. Browser: StratejiOnaylaButton — approved cluster yokken disabled; approved sonrası aktif; tıklanınca "✓ Strateji Onaylandı"
</verification>

<success_criteria>
- 6 yeni bileşen dosyası oluşturuldu
- 2 mevcut dosya (page.tsx, ClusterPanel.tsx) güncellendi
- 1 yeni client wrapper (KeywordStratejisiToolbar.tsx) oluşturuldu
- TypeScript hata yok (`npx tsc --noEmit`)
- Vitest test suite yeşil (`npx vitest run`)
- Overlay UI-SPEC'teki CSS contract'ı tam olarak uygular
- KWST-03: AI clustering → overlay açılır → draft cluster'lar listelenir
- KWST-04: Kullanıcı onayla/reddet/düzenle yapabilir; StratejiOnaylaButton keyword_strategy_approved flag'ini yazar
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/20-ai-keyword-clustering-approval/20-03-SUMMARY.md` oluştur.
</output>
