---
phase: 20-ai-keyword-clustering-approval
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260509000010_clustering_approval.sql
  - src/lib/keywords/clustering-approval.test.ts
  - src/lib/keywords/clustering.test.ts
autonomous: true
requirements:
  - KWST-03
  - KWST-04

must_haves:
  truths:
    - "keyword_clusters tablosunda status kolonu mevcuttur: 'draft', 'approved', 'rejected' dışında değer kabul etmez (CHECK constraint)"
    - "projects tablosunda keyword_strategy_approved BOOLEAN DEFAULT false kolonu mevcuttur"
    - "Mevcut tüm keyword_clusters kayıtları status='approved' olarak güncellendi (geriye dönük uyumluluk)"
    - "clustering-approval.test.ts — updateClusterStatus ve approveStrategy için 6 unit test (mocked Supabase) mevcuttur"
    - "clustering.test.ts — status='draft' yazma davranışı için en az 1 test eklenmiştir"
  artifacts:
    - path: "supabase/migrations/20260509000010_clustering_approval.sql"
      provides: "keyword_clusters.status CHECK constraint + projects.keyword_strategy_approved"
      contains: "ADD COLUMN IF NOT EXISTS status TEXT NOT NULL DEFAULT 'draft' CHECK (status IN ('draft', 'approved', 'rejected'))"
    - path: "src/lib/keywords/clustering-approval.test.ts"
      provides: "updateClusterStatus ve approveStrategy unit testleri"
      exports: ["describe('updateClusterStatus')", "describe('approveStrategy')"]
    - path: "src/lib/keywords/clustering.test.ts"
      provides: "Mevcut test dosyası genişletilir — status='draft' test eklendi"
  key_links:
    - from: "supabase/migrations/20260509000010_clustering_approval.sql"
      to: "keyword_clusters.status"
      via: "ALTER TABLE + CHECK constraint"
      pattern: "CHECK.*status.*draft.*approved.*rejected"
    - from: "supabase/migrations/20260509000010_clustering_approval.sql"
      to: "projects.keyword_strategy_approved"
      via: "ALTER TABLE"
      pattern: "keyword_strategy_approved BOOLEAN"
---

<objective>
Wave 0: DB şeması oluştur ve test iskeleti yaz.

Phase 20'nin tüm kodu `keyword_clusters.status` ve `projects.keyword_strategy_approved` kolonlarına bağlıdır. Bu kolonlar olmadan hiçbir Wave 1 / Wave 2 kodu çalışamaz. Bu plan önce migration'ı uygular, ardından server action'ları test edecek test dosyasını hazırlar.

Purpose: Blocking dependency — sonraki iki plan bu kolonları okur ve yazar.
Output: 1 migration SQL dosyası (uygulanmış), 1 yeni test dosyası, 1 mevcut test dosyasına ekleme.
</objective>

<execution_context>
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/baris/Documents/seo-machine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-ai-keyword-clustering-approval/20-CONTEXT.md
@.planning/phases/20-ai-keyword-clustering-approval/20-RESEARCH.md
@.planning/phases/20-ai-keyword-clustering-approval/20-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration SQL dosyası — keyword_clusters.status + projects.keyword_strategy_approved</name>
  <files>supabase/migrations/20260509000010_clustering_approval.sql</files>

  <read_first>
    - supabase/migrations/20260509000004_master_seo_architecture.sql (format pattern — ADD COLUMN IF NOT EXISTS, tablo genişletme stili)
    - supabase/migrations/20260509000007_project_decisions.sql (en son migration; slot'ların doğrulanması)
  </read_first>

  <action>
Aşağıdaki içerikle `supabase/migrations/20260509000010_clustering_approval.sql` dosyasını oluştur:

```sql
-- Phase 20: AI Keyword Clustering & Approval
-- keyword_clusters: clustering onay akışı için status kolonu
-- projects: keyword strateji onay gate flag'i
-- NOT: keyword_clusters'da zaten arch_status var (site blueprint akışı — farklı amaç).
-- Bu status kolonu = clustering onay aşaması (draft/approved/rejected).

-- 1. keyword_clusters.status kolonu ekle
ALTER TABLE public.keyword_clusters
  ADD COLUMN IF NOT EXISTS status TEXT NOT NULL DEFAULT 'draft'
  CHECK (status IN ('draft', 'approved', 'rejected'));

-- 2. Mevcut cluster'lara 'approved' ata (geriye dönük uyumluluk — D-07)
-- Yeni sistemde yeniden kümelendirmeden önce oluşturulan tüm cluster'lar onaylı sayılır.
UPDATE public.keyword_clusters
  SET status = 'approved'
  WHERE status = 'draft';

-- 3. projects.keyword_strategy_approved ekle (Phase 21 gate kontrolü için)
ALTER TABLE public.projects
  ADD COLUMN IF NOT EXISTS keyword_strategy_approved BOOLEAN DEFAULT false NOT NULL;
```

Dosya adı `20260509000010_clustering_approval.sql` olmalı. 000008 ve 000009 Phase 19 için rezervedir.
  </action>

  <verify>
    <automated>grep -n "status TEXT NOT NULL DEFAULT" C:/Users/baris/Documents/seo-machine/supabase/migrations/20260509000010_clustering_approval.sql && grep -n "keyword_strategy_approved" C:/Users/baris/Documents/seo-machine/supabase/migrations/20260509000010_clustering_approval.sql</automated>
  </verify>

  <acceptance_criteria>
    - Dosya `supabase/migrations/20260509000010_clustering_approval.sql` olarak mevcuttur
    - `ADD COLUMN IF NOT EXISTS status TEXT NOT NULL DEFAULT 'draft'` satırı içerir
    - `CHECK (status IN ('draft', 'approved', 'rejected'))` satırı içerir
    - `UPDATE public.keyword_clusters SET status = 'approved' WHERE status = 'draft'` satırı içerir
    - `ADD COLUMN IF NOT EXISTS keyword_strategy_approved BOOLEAN DEFAULT false NOT NULL` satırı içerir
  </acceptance_criteria>

  <done>Migration SQL dosyası doğru içerikle oluşturulmuş, grep ile doğrulanabilir.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migration push — supabase db push</name>
  <files></files>

  <read_first>
    - supabase/migrations/20260509000010_clustering_approval.sql (Task 1'de oluşturuldu — doğrulama)
  </read_first>

  <action>
Migration'ı remote Supabase veritabanına uygula:

```bash
cd C:/Users/baris/Documents/seo-machine && npx supabase db push
```

Komut etkileşimli prompt açarsa `y` ile onayla. Push başarıyla tamamlanınca `keyword_clusters.status` ve `projects.keyword_strategy_approved` kolonları remote DB'de mevcut olacak.

Hata durumunda: `npx supabase db push --debug` ile ayrıntılı çıktı al ve sorunu raporla. Migration SQL sözdizimi hatası varsa Task 1'e dön ve düzelt.
  </action>

  <verify>
    <automated>cd C:/Users/baris/Documents/seo-machine && npx supabase db push 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `npx supabase db push` başarıyla tamamlanır (exit code 0 veya "Applying migration" çıktısı görülür)
    - Hata mesajı `ERROR` veya `failed` içermez
    - Remote DB'de `keyword_clusters.status` kolonu sorgulanabilir hale gelir
  </acceptance_criteria>

  <done>Migration remote Supabase'e başarıyla uygulanmış; kolonlar artık sorgulanabilir.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 test dosyası — clustering-approval.test.ts (mocked Supabase)</name>
  <files>src/lib/keywords/clustering-approval.test.ts, src/lib/keywords/clustering.test.ts</files>

  <read_first>
    - src/lib/keywords/clustering.test.ts (mevcut test dosyası — yapı ve import pattern'i kopyalanacak)
    - src/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions.ts (satır 285-408 — clusterAndScoreKeywords, satır 663-703 — updateClusterRevenue ownership check pattern)
    - .planning/phases/20-ai-keyword-clustering-approval/20-PATTERNS.md (clustering-approval.test.ts pattern bölümü)
    - .planning/phases/20-ai-keyword-clustering-approval/20-VALIDATION.md (Wave 0 gaps)
  </read_first>

  <behavior>
    - Test 1 (updateClusterStatus): Geçersiz status değeri ('invalid') → `{ success: false, error: 'Geçersiz durum değeri.' }` döner
    - Test 2 (updateClusterStatus): Auth yokken → `{ success: false, error: 'Oturum bulunamadı.' }` döner
    - Test 3 (updateClusterStatus): Başka kullanıcının cluster'ı (ownership check başarısız) → `{ success: false, error: 'Küme bulunamadı.' }` döner
    - Test 4 (updateClusterStatus): Geçerli çağrı başarılı → `{ success: true }` döner ve revalidatePath ÇAĞRILMAZ
    - Test 5 (approveStrategy): Geçerli çağrı → `{ success: true }` döner ve revalidatePath çağrılır
    - Test 6 (approveStrategy): Başka kullanıcının projesi → `{ success: false, error: 'Proje bulunamadı.' }` döner
    - clustering.test.ts eki: `clusterAndScoreKeywords` status='draft' yazma — mocked upsert'in `status: 'draft'` içerdiğini doğrula
  </behavior>

  <action>
**Adım 1: `src/lib/keywords/clustering-approval.test.ts` dosyasını oluştur**

Aşağıdaki yapıyı kullan (mocked Supabase pattern — clustering.test.ts'deki vitest import'larını kopyala):

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

// Supabase client mock — her testten önce sıfırlanır
vi.mock('@/lib/supabase/server', () => ({
  createClient: vi.fn(),
}))

// next/cache mock — revalidatePath çağrısını izlemek için
vi.mock('next/cache', () => ({
  revalidatePath: vi.fn(),
}))

import { createClient } from '@/lib/supabase/server'
import { revalidatePath } from 'next/cache'
import { updateClusterStatus, removeKeywordFromCluster, approveStrategy } from '@/app/(dashboard)/projeler/[id]/keyword-stratejisi/actions'

function makeMockSupabase(overrides: Record<string, unknown> = {}) {
  return {
    auth: {
      getUser: vi.fn().mockResolvedValue({ data: { user: { id: 'user-123' } } }),
    },
    from: vi.fn().mockReturnValue({
      select: vi.fn().mockReturnThis(),
      eq: vi.fn().mockReturnThis(),
      update: vi.fn().mockReturnThis(),
      single: vi.fn().mockResolvedValue({ data: { id: 'cluster-abc' }, error: null }),
    }),
    ...overrides,
  }
}

beforeEach(() => {
  vi.clearAllMocks()
})

describe('updateClusterStatus', () => {
  it('rejects invalid status values', async () => {
    const result = await updateClusterStatus('cluster-abc-12345678901234567890', 'invalid', 'proj-abc-12345678901234567890')
    expect(result.success).toBe(false)
    if (!result.success) expect(result.error).toBe('Geçersiz durum değeri.')
  })

  it('returns error when auth is missing', async () => {
    const mockSupabase = makeMockSupabase()
    mockSupabase.auth.getUser = vi.fn().mockResolvedValue({ data: { user: null } })
    vi.mocked(createClient).mockResolvedValue(mockSupabase as unknown as ReturnType<typeof createClient> extends Promise<infer T> ? T : never)

    const result = await updateClusterStatus('cluster-abc-12345678901234567890', 'approved', 'proj-abc-12345678901234567890')
    expect(result.success).toBe(false)
    if (!result.success) expect(result.error).toBe('Oturum bulunamadı.')
  })

  it('returns error when cluster ownership check fails', async () => {
    const mockSupabase = makeMockSupabase()
    // single() → null veri (ownership başarısız)
    const fromMock = {
      select: vi.fn().mockReturnThis(),
      eq: vi.fn().mockReturnThis(),
      update: vi.fn().mockReturnThis(),
      single: vi.fn().mockResolvedValue({ data: null, error: null }),
    }
    mockSupabase.from = vi.fn().mockReturnValue(fromMock)
    vi.mocked(createClient).mockResolvedValue(mockSupabase as unknown as ReturnType<typeof createClient> extends Promise<infer T> ? T : never)

    const result = await updateClusterStatus('cluster-abc-12345678901234567890', 'approved', 'proj-abc-12345678901234567890')
    expect(result.success).toBe(false)
    if (!result.success) expect(result.error).toBe('Küme bulunamadı.')
  })

  it('succeeds without calling revalidatePath', async () => {
    const ownershipMock = { data: { id: 'cluster-abc-12345678901234567890' }, error: null }
    const updateMock = { error: null }
    let callCount = 0

    const fromMock = {
      select: vi.fn().mockReturnThis(),
      eq: vi.fn().mockReturnThis(),
      update: vi.fn().mockReturnThis(),
      single: vi.fn().mockImplementation(() => {
        callCount++
        if (callCount === 1) return Promise.resolve(ownershipMock)  // ownership
        return Promise.resolve(updateMock)
      }),
    }
    const mockSupabase = makeMockSupabase()
    mockSupabase.from = vi.fn().mockReturnValue(fromMock)
    vi.mocked(createClient).mockResolvedValue(mockSupabase as unknown as ReturnType<typeof createClient> extends Promise<infer T> ? T : never)

    const result = await updateClusterStatus('cluster-abc-12345678901234567890', 'approved', 'proj-abc-12345678901234567890')
    expect(result.success).toBe(true)
    // revalidatePath çağrılmamalı — overlay state korunur (Pitfall 3)
    expect(revalidatePath).not.toHaveBeenCalled()
  })
})

describe('approveStrategy', () => {
  it('calls revalidatePath after successful update', async () => {
    const ownershipMock = { data: { id: 'proj-abc-12345678901234567890' }, error: null }
    const updateMock = { error: null }
    let callCount = 0

    const fromMock = {
      select: vi.fn().mockReturnThis(),
      eq: vi.fn().mockReturnThis(),
      update: vi.fn().mockReturnThis(),
      single: vi.fn().mockImplementation(() => {
        callCount++
        if (callCount === 1) return Promise.resolve(ownershipMock)
        return Promise.resolve(updateMock)
      }),
    }
    const mockSupabase = makeMockSupabase()
    mockSupabase.from = vi.fn().mockReturnValue(fromMock)
    vi.mocked(createClient).mockResolvedValue(mockSupabase as unknown as ReturnType<typeof createClient> extends Promise<infer T> ? T : never)

    const result = await approveStrategy('proj-abc-12345678901234567890', true)
    expect(result.success).toBe(true)
    expect(revalidatePath).toHaveBeenCalledWith(expect.stringContaining('/keyword-stratejisi'))
  })

  it('returns error when project ownership fails', async () => {
    const fromMock = {
      select: vi.fn().mockReturnThis(),
      eq: vi.fn().mockReturnThis(),
      update: vi.fn().mockReturnThis(),
      single: vi.fn().mockResolvedValue({ data: null, error: null }),
    }
    const mockSupabase = makeMockSupabase()
    mockSupabase.from = vi.fn().mockReturnValue(fromMock)
    vi.mocked(createClient).mockResolvedValue(mockSupabase as unknown as ReturnType<typeof createClient> extends Promise<infer T> ? T : never)

    const result = await approveStrategy('proj-abc-12345678901234567890', true)
    expect(result.success).toBe(false)
    if (!result.success) expect(result.error).toBe('Proje bulunamadı.')
  })
})
```

**Adım 2: `src/lib/keywords/clustering.test.ts` dosyasını genişlet**

Mevcut dosyayı oku ve sonuna şu test bloğunu ekle (dosyaya MEVCUT testlere dokunmadan ekle):

```typescript
// Phase 20 — D-07: clusterAndScoreKeywords status='draft' yazma kontrolü
// Bu test mocked değil; clusterAndScoreKeywords server action gerektirdiğinden
// burada sadece clusterEnrichedKeywords çıktı yapısının 'status' içermediğini
// (clustering.ts layer'ında) ve action layer'ında eklendiğini belgelemek için
// bir smoke test eklenir:
describe('clusterEnrichedKeywords output — no status field (status is set in action layer)', () => {
  it('clusterEnrichedKeywords sonucu status alanı taşımaz — status actions.ts tarafından eklenir', () => {
    // clustering.ts'den gelen ClusterResult tipinde 'status' yok; doğru mimari
    const result = clusterEnrichedKeywords([
      { id: '1', keyword: 'test keyword', volume: 100, difficulty: 30, cpc: 1.0, search_intent: 'informational' },
    ])
    // ClusterResult'ın 'status' property'si yok; actions.ts upsert'inde eklenir
    expect(result[0]).not.toHaveProperty('status')
  })
})
```

Not: `clusterEnrichedKeywords` zaten `import { clusterEnrichedKeywords }` ile dosyanın tepesinde import'lanmış olmalı. Yoksa import'u ekle.
  </action>

  <verify>
    <automated>cd C:/Users/baris/Documents/seo-machine && npx vitest run src/lib/keywords/clustering-approval.test.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `src/lib/keywords/clustering-approval.test.ts` dosyası mevcut
    - `describe('updateClusterStatus')` ve `describe('approveStrategy')` blokları mevcut (grep ile doğrula)
    - `expect(revalidatePath).not.toHaveBeenCalled()` updateClusterStatus başarı testinde mevcut
    - `npx vitest run src/lib/keywords/clustering-approval.test.ts` çalıştırılabilir (tüm testler pass veya skip/todo olarak işaretli)
    - `src/lib/keywords/clustering.test.ts` dosyasında `status alanı taşımaz` içeren yeni test mevcut
  </acceptance_criteria>

  <done>6 unit test clustering-approval.test.ts'de mevcut; clustering.test.ts'e status smoke testi eklenmiş; vitest çalışıyor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL migration → Supabase DB | Migration SQL Remote DB'ye push edilir; SQL injection riski yok (statik DDL) |
| Test → mocked server action | Test boundary; production veri akışı yok |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | keyword_clusters.status | mitigate | CHECK constraint (status IN ('draft','approved','rejected')) migration seviyesinde; geçersiz değer DB tarafından reddedilir |
| T-20-02 | Tampering | status input validation | mitigate | Wave 0 testi `VALID_CLUSTER_STATUSES` whitelist'in çalıştığını doğrular; Wave 1'de action'a uygulanır |
| T-20-03 | Elevation of Privilege | projects.keyword_strategy_approved | mitigate | Migration yalnızca kolon ekler; yazma yetkisi Wave 1'deki `approveStrategy` action ownership check ile korunur |
</threat_model>

<verification>
Wave 0 tamamlanma kriterleri:
1. `ls supabase/migrations/ | grep 20260509000010` → dosya mevcut
2. `npx supabase db push` → başarılı (remote DB güncel)
3. `npx vitest run src/lib/keywords/clustering-approval.test.ts` → test dosyası çalışıyor
4. `grep -n "describe('updateClusterStatus')" src/lib/keywords/clustering-approval.test.ts` → bulunur
5. `grep -n "revalidatePath.*not.*toHaveBeenCalled" src/lib/keywords/clustering-approval.test.ts` → bulunur
</verification>

<success_criteria>
- Migration SQL dosyası oluşturulmuş ve remote DB'ye uygulanmış
- `keyword_clusters.status TEXT NOT NULL DEFAULT 'draft' CHECK (status IN ('draft', 'approved', 'rejected'))` kolonu var
- `projects.keyword_strategy_approved BOOLEAN DEFAULT false NOT NULL` kolonu var
- Mevcut cluster'lar `status='approved'` ile güncellendi
- Unit test dosyası 6 test ile mevcut; vitest çalışıyor
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/20-ai-keyword-clustering-approval/20-01-SUMMARY.md` oluştur.
</output>
